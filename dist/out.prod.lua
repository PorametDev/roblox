-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Auto Plot: ใช้โซนล่าสุดเสมอ", "s - carrying on without it", "HumanoidRootPart", "table", " ", "Damage", "maxed", "[Scheduler] ", "[Scheduler] ", "core/get_place_id", "off - every automation spends on its own", "autoClaimInterval", ")", "[AutoPlace] couldn't hold anything - not pressing E", "ProximityPrompt", "AutoResearch", "^%$", "ยิง Equip Best ทุกกี่วินาที", "Main", "Zones", " thing available first", "empty", "%.0f", "s", "Players", "disabled", "games/my_anime_mine/auto_claim", " wouldn't take ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "AutoPlace", "MineSpeed", "Auto Upgrade: รอบเช็ค", " ", "games/registry", "GuiService", "auto", "string", ") needs ", "ต่างคนต่างซื้อ", " research is ", "ต่ำสุดก่อน", "autoResearchCoreLast", "AutoPlace", "autoPlotKeepMoney", "autoUpgradePickaxeIndex", "AutoChest", ", you have ", "core/logger", "Workspace", "core/i18n", "[AutoPlot] ", "", "AutoResearch", "Content", "MineSellItems", "games/my_anime_mine/auto_research", "games/my_anime_mine/get_plot_parts", "[AutoSell] disabled", "", "Text", "dearest", "", ": ", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "UICorner", "\n", "M", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "core/click", "UIListLayout", "[AutoPlace] disabled", "HumanoidRootPart", " (still ", "BasePart", " items (now ", "Auto Research: รอบเช็ค", "", "TextLabel", "upgrade", "/", "games/my_anime_mine/auto_sell", "autoUpgradePickaxe", " (", "Auto Place: รอบดึงตัวเก่ง", "", "Mining ก่อน", "] job failed: ", " not bought yet (", "GetCharacterUpgradeCost", ",", "none", " ", "open_menu_hint", "0 = ไม่จำกัด", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "autoPlaceEquipBestInterval", "GetHolderSpeedLevel", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "HotbarFrame", "Content", "core/run_job", ")", "table", "TextLabel", "needs ", "", "HumanoidRootPart", "core/logger", "core/config", "Hauler", " (x", "open_menu_hint", "แพงสุดก่อน", "Auto Place: เพดานราคาปลดช่อง", ")", "UIListLayout", "works from anywhere - no teleport needed", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "; ", " while selecting", "prod", ") gave nothing", "core/ui", "costs ", "RunService", "table", "disabled", "%.3g", "core/logger", "ClickDetector", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "Players", "[AutoResearch] ", "Locked miner slot", "autoPlotInterval", " -> ", ")", "CoreGui", "/", "core/run_job", " mutation x", " (", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "[AutoChest] can't read the chest yet", "autoUpgradeMutationBudget", "TextLabel", "AutoMine", "core/logger", "Frame", "/", "[AutoChest] chest is empty", "[Logger] failed to write ", "Auto Place Miner", "MineUpgradeCharacter", "languageIndex", "games/my_anime_mine/auto_sell", ")", "ถูกสุดก่อน", "[Scheduler] ", "core/logger", "empty", "Auto Claim: รอบเช็ค", "off", "core/logger", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "core/build_env", "core/run_job", "core/spend_policy", "Exclusive", "may need us at the stand - trying that", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "M", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "core/logger", "th", " (", "อังกฤษ", "TextButton", "Players", "number", "autoUpgradeMutationTarget", "leaderstats", "[AutoUpgrade] pickaxe #", "[Config] couldn't write ", "", "VirtualUser", "core/logger", "", "none", "AutoResearch", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "ItemFrame", "string", "MineTakeChest", "[AutoResearch] idle: ", "core/logger", ") ", "off", "HttpService", "[AutoUpgrade] ", "Badge", "/", "disabled", "Players", "ถูกสุดก่อน", "AutoPlot", "autoClaim", "TextButton", "%$", "[AutoPlace] unlocked ", "slot past the usable cap", "ขนาดเมนู", "ไทย", "search_placeholder", "", "core/anti_afk", "GetCharacterMineSpeedUpgradeCost", "s", "nothing left to buy (", "[%X] ", "core/logger", "autoChestInterval", "MineSpeed", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "[AutoClaim] idle: ", "ย่อขยายเมนูทั้งชุด", "games/my_anime_mine/game_api", "index rewards are disabled in this game build", " (", "s", " ", ": ", "Damage", "affordable - waiting for a turn", "Character", "This is not a supported place.", "UIListLayout", "games/my_anime_mine/auto_mine", "Auto Sell: รอคิวได้นานสุด", "sale didn't go through", "Workspace", "core/logger", "[AutoResearch] Core ", "ProximityPrompt", ") -> ", "M", "[AutoSell] enabled", "MekPro", "AutoResearch", "Frame", "table", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "autoMineSpeed", " (power ", "", "games/my_anime_mine/auto_research", "off", "AutoUpgrade", "Backpack", "core/get_place_id", "^([%d,]+)$", "ไล่ซื้อทุกกี่วินาที", " -> ", "enabled", "cheapest open node ", " (power ", "เช็คโซนทุกกี่วินาที", "en", "Speed ก่อน", "", "Mutation: เพดานราคาต่อครั้ง", "autoResearchCheapestFirst", "^M(%d+)$", "autoChest", "Workspace", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "TextLabel", "(", "core/anti_afk", "[AutoPlace] placed ", "Badge", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "Zone buying is off", "UIScale", "Auto Research: ซื้อสาย Core", "s behind a purchase that never happened - letting it spend", "autoUpgradeInterval", "autoSell", "core/get_leaderstat", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "MekPro.log", "Auto Research (Plot)", "x", "/", "core/run_job", "TabBar", "MekPro", "%", "lowest", "modules not loaded", "^x(%d+)$", "nothing claimable", "/", ")", "Auto Research: ซื้อสูงสุดต่อรอบ", "[AutoPlace] ", "Damage", "waiting", "2026-09-21 13:01:50", "RunService", "[GameAPI] SendEvent failed: ", "s - jumping the queue", "", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "unlock", "[AutoPlace] fireproximityprompt error: ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "Auto Place: เปลี่ยนตัวอ่อนออก", "string", "🏠", " for ", "Mining", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " has been busy for ", "ItemName", "core/teleport", "PlayerGui", "autoPlaceUnlockBudget", "", "Loot", "Auto Sell", "/s", "Auto Plot: ซื้อโซนถัดไป", "GuiService", "autoPlotLatest", " still locked after paying ", "x", "%.3g", "autoResearchInterval", "%.0f", "Players", "MineEquipPickaxe", "TextLabel", "dearest", " for ", "[AutoClaim] claimed ", "Auto Plot: กันเงินไว้", "table", "Auto Chest: รอบเช็ค", "Auto Chest", "core/i18n", " ", "[MekPro] settings file: ", "TextLabel", "Frame", "[SpendPolicy] ", "TextLabel", "\n", "Damage ก่อน", "ClaimableTiers failed", ",", "autoSellInterval", "autoUpgradePriority", "ZoneBoard", ") on ", "", " ชิ้น", "games/my_anime_mine/main", "table", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", ")", " (", "=", "Nodes", "core/spend_policy", "locked slot", ") didn't take: ", "games/my_anime_mine/main", "fill", "table", "dearest", "Auto Mine: ความเร็ว", "languageIndex", "x", "AutoSell", "Open menu", "[AutoUpgrade] every stat on every miner is maxed", "", "GetHolderLevel", " -> ", "[Scheduler] turn: ", "[AutoClaim] ", "[AutoPlace] drifted out of range of ", ")", "autoPlaceUpgradeWeak", "Crew", "autoPlotBuy", ") ", " (chest holds ", "[AutoPlace] couldn't pick up ", " (money ", "Text", "games/my_anime_mine/get_best_miner", "ตามเครื่อง", "games/my_anime_mine/get_plot_parts", "Pad", "[AutoUpgrade] ", "", " costs ", "OwnerUserId", "[Config] couldn't read ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "function", "en", "table", "fill", "autoUpgradeRollMutation", "Auto Upgrade: Pickaxe ที่จะซื้อ", "autoPlace", " but no unclaimed reward in its Track", "table", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", " doesn't exist", "ไล่อัปเกรด miner ทุกกี่วินาที", "TextButton", "TextButton", "core/debug_tab", "games/my_anime_mine/game_api", " stack(s)", " waited ", "games/my_anime_mine/game_api", "Humanoid", ") from ", " ", "autoPlaceUnlock", "AutoPlot", "Auto Upgrade Unit", "ZoneBoardGui", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "core/config", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "Auto Mine: คลิกต่อรอบ", "AutoUpgrade", "[AutoClaim] ", "MineEquipBest", "AutoClaim", "x", "[AutoResearch] nothing open and affordable right now", " owned)", "core/run_job", "อ่านป้ายบน chest ทุกกี่วินาที", "AutoUpgrade", "prompt disabled", "dearest", "games/my_anime_mine/game_api", "[AutoPlace] ", "Frame", "games/my_anime_mine/auto_chest", "Players", "[SpendPolicy] ", "ตั้งค่า", "autoUpgradePickaxeIndex", "menuScale", "#", "core/dispatch", "unreadable", "games/my_anime_mine/auto_place", "[AutoResearch] ", "", "", "core/spend_policy", "Frame", "[AutoPlace] slot ", "buying the ", "core/scheduler", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "games/my_anime_mine/auto_upgrade", "[Uu]nlock", "autoUpgradeLimitPickaxes", " ", "core/get_place_id", "autoResearchKeepMoney", "both Zone and Core buying are switched off", ": ", "[AutoChest] waiting: ", "TextButton", "ลำดับการใช้เงินรวมทุกระบบ", " items)", "แพงสุดก่อน", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", " (prompt says '", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " -> ", "Frame", "Core", "core/logger", " claimable but none could be resolved", " possible holder(s) - not buying more", "Auto Chest: เกณฑ์เก็บ", "core/spend_policy", "Players", "[AutoPlot] unlocked ", "x", "TextLabel", "OwnerUserId", "PickaxesStand", "Frame", "write reported success but the line isn't in ", "Auto Research: ซื้อสาย Zone", " (cost ", "autoResearchCore", "CharMineSpeedMaxLevel", "autoClaimMaxPerPass", "List", "autoMineClicks", "off", ", have ", "Frame", "Shop Stand", "CharSpeedMaxLevel", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "AutoUpgrade", "TextButton", "TextBox", "[MekPro] build ", "Speed", "[AutoPlace] holders: ", "Humanoid", "My Anime Mine · Mining Hub", "AutoSell", "%.0f", "auto", ": State(", "autoSellMaxWait", "%$", "works from anywhere - dropping the teleport", "ChestCapacity", " (", "[AntiAFK] disabled", "autoSellMaxWait", "games/my_anime_mine/get_plot_parts", " team power", " (", "[Bb]uy", "All", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "Auto Upgrade Plot (Zone)", "table", "Speed", "MineGui", ", ", "MineBuySkill", "ฟาร์ม", "Workspace", "ScrollingFrame", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Core buying is off", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " -> ", "[AutoPlace] upgrading ", "table", "[AutoPlot] couldn't switch to ", "Auto Upgrade: สุ่ม Mutation", "cost", "[AutoUpgrade] ", " ", "string", "Auto Sell: รอบขาย", "%.3g", "Frame", ")", "[AutoPlace] Equip Best: hotbar's strongest ", "[AutoResearch] ", "AutoPlot", " items", "=empty", "s", "autoUpgradeKeepMoney", ", base ", "research", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "table", "Workspace", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "games/my_anime_mine/get_current_plot", "string", "[AutoUpgrade] bought pickaxe #", "enabled", "dearest", " (x", "/config.json", "BasePart", "[", "AutoPlace", "BodyPosition", "games/my_anime_mine/auto_upgrade", "Auto Claim: เก็บสูงสุดต่อรอบ", "TextLabel", "MineWorld", "TextButton", "nothing to place or upgrade", "s", "lowest", "no isfile/readfile available to verify the write", "Auto Mine", "%$%s*([%d,%.]+)%s*(%a*)", "MineRollMutation", "[AutoMine] ", "TextLabel", "[AutoPlot] ", ") ", "core/logger", "s", "M", " - upgrade the miners first", "Frame", " x", " waited ", " pickaxe ", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "disabled", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "AutoPlot", "Auto Place: รอบเช็ค", "Auto Plot: รอบเช็ค", "[GameAPI] couldn't load the game's modules: ", "[AutoSell] MineSellItems ", "[AutoSell] ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "games/registry", "needs us at the stand - keeping the teleport", "", "core/logger", "badge says ", "menuScale", "needs ", "ValueLabel", "[AutoUpgrade] ", "button has no size on screen", "[AutoChest] MineTakeChest had no effect (", " ", "core/logger", ")", "TextLabel", " -> ", "games/my_anime_mine/get_current_plot", "autoPlaceEquipBest", "AutoPlace", "Limited", " - ", "antiAfk", "Empty miner slot", "enabled", "[AntiAFK] enabled - watching Player.Idled", "s", " pickaxe(s) for ", "=locked(", "X", "AutoPlace", "core/get_me", "ValueLabel", ": ", "every open node is maxed", "Frame", "Players", "autoPlot", "TextLabel", ", built ", "[AutoChest] enabled (collects once >= ", "CharMaxLevel", ")", "[AutoPlot] now mining ", "[AutoPlace] picked up ", "sold ", "slot not in the hotbar", "games/my_anime_mine/auto_plot", " #", "SellStand", "?", "disabled", "%.3g", "Anti-AFK", "games/my_anime_mine/auto_place", "-", "en", "(nothing to do)", "[AutoResearch] cheapest open node: ", "write reported success but ", "ReplicatedStorage", "25d66fc+", "BasePart", "Container", " (", "MineSpeed", "Auto Place: ปลดล็อกช่อง miner", "enabled", "s", "UIPadding", "games/my_anime_mine/auto_chest", "⚙", " refused - ", " from ", "", "core/spend_policy", ",", "autoPlaceInterval", "Players", ": ", "slot ", "autoUpgradeEquipPickaxe", "search_placeholder", "Hauler", "[AntiAFK] idle detected, simulating input", "dearest", "Chest", "autoResearch", "[AutoPlace] ", "Header", "/", "[AutoPlot] ", "MineWorld", "core/logger", "en-us", "PlotIndex", ")", ")", "games/my_anime_mine/game_api", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "games/my_anime_mine/game_api", "autoMine", "table", "autoResearchMaxPerPass", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "[AutoPlace] nothing in the hotbar to place", " didn't go through (out of stock or too expensive)", "core/humanoid", "[AutoUpgrade] ", "core/teleport", "Auto Research: กันเงินไว้", "cost", " ", "Root", "games/my_anime_mine/game_api", "research", "CoreUI", "Modules", "[AutoChest] collected ", "ค้นหาฟังก์ชัน...", "s", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "neither appendfile nor writefile is available", "GetHolderMineSpeedLevel", "0 = ไม่จำกัด", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "core/run_job", "MineEquipSlot", "core/build_env", " -> x", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "TextButton", "AutoClaim", "AutoUpgrade", "games/my_anime_mine/get_current_plot", "function", "UserInputService", "server didn't equip it", "Players", "core/scheduler", "ขายทุกกี่วินาที", "core/teleport", "MineSelectZone", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/auto_claim", " power ", "table", "table", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "core/parse_money", "VirtualInputManager", "BasePart", "core/ui", "core/spend_policy", "", "TextLabel", "core/run_job", " costs ", "autoUpgrade", "Search", "th", "spendOrder", "Holders", "unlock", "waiting", " (", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "autoResearchZones", "auto", "ZoneRow", " | ", "games/my_anime_mine/auto_mine", " ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", ") ", "UIStroke", ")", "[AutoChest] disabled", "Content", "table", "Plots", "MinePower", "core/parse_money", " reward(s) looked claimable but none went through", " team power, you have ", "core/i18n", "[AutoResearch] ", "autoChestMinItems", "core/run_job", "unreadable", "enabled", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "TextLabel", " still empty after placing ", "auto", "action '", "'", "MineBuyPickaxe", "%.3g", "th", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "", "Mutation: หยุดเมื่อได้ x", "", "cheapest", "AutoChest", "UICorner", " (", "AutoPlace", "core/parse_money", "([%d,%.]+)%s*(%a*)", "ScreenGui", "TextLabel", "games/my_anime_mine/auto_plot", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "games/my_anime_mine/game_api", "AutoPlot", "th", "", "Auto Upgrade: แจก Pickaxe ให้ miner", " node(s) looked buyable but the server refused", "core/logger", "M", "games/my_anime_mine/game_api", "GetCharacterSpeedUpgradeCost", "[BestMiner] slot ", "no mine data", "core/teleport", "cheapest", "AutoPlace", ")", "autoUpgradeMaxPerPass", "ปุ่มเปิดเมนู", "Auto Claim (Index)", ": badge says ", "core/dispatch", " (", "Search functions...", "[AutoPlace] unlocking ", "')", " error: ", "Plots", "BodyVelocity", "Auto Upgrade: กันเงินไว้", "UserInputService", "[AutoUpgrade] MineBuyPickaxe ", "autoSellProtectUnits", "TextLabel"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(217)] = function()
return function(import)
local log = import(S(197))
local Players = game:GetService(S(503))
local VirtualUser = game:GetService(S(187))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(656))
        connection = player.Idled:Connect(function()
            log(S(715))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(541))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(166)] = function()
return { Env = S(121), IsDev = false, Commit = S(692), BuiltAt = S(310) }
end
modules[S(69)] = function()
local VirtualInputManager = game:GetService(S(781))
local GuiService = game:GetService(S(35))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(641)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(442)] = function()
return function(import)
local HttpService = game:GetService(S(200))
local getPlaceId = import(S(10))
local log = import(S(129))
local ROOT_FOLDER = S(298)
local placeFolder = ROOT_FOLDER .. S(203) .. tostring(getPlaceId())
local filePath = placeFolder .. S(593)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(810) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(413) .. filePath .. S(233) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(185) .. filePath .. S(63) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(467)] = function()
return function(import)
    local getPlaceId = import(S(261))
    local registry = import(S(34))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(237))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(290)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(183))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(662)] = function()
local Players = game:GetService(S(667))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(530))
    local backpack = player:WaitForChild(S(260))
    return player, humanoid, backpack
end
return getMe
end
modules[S(483)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(738)] = function()
local Players = game:GetService(S(709))
local RunService = game:GetService(S(125))
local UserInputService = game:GetService(S(767))
local Workspace = game:GetService(S(585))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(434))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(3))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(873))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(693)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(74)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(597))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(353)] = function()
local Players = game:GetService(S(205))
local STRINGS = {
    en = {
        search_placeholder = S(868),
        open_menu_hint = S(388),
    },
    th = {
        search_placeholder = S(750),
        open_menu_hint = S(863),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(725)
    if locale:sub(1, 2):lower() == S(791) then
        return S(848)
    end
    return S(269)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(825) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(243)] = function()
local LOG_FILE = S(292)
local function log(message)
    local line = os.date(S(221)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(390))
            end
            appendfile(LOG_FILE, line .. S(66))
        elseif writefile then
            local existing = S(368)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(360))
        else
            error(S(753))
        end
        if not (isfile and readfile) then
            error(S(606))
        end
        if not isfile(LOG_FILE) then
            error(S(690) .. LOG_FILE .. S(425))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(510) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(152) .. LOG_FILE .. S(486) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(780)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(194) then
        return nil
    end
    local number, suffix = text:match(S(608))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(363), S(59))))
    if not value then
        return nil
    end
    if suffix ~= S(62) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(452)] = function()
return function(import)
local log = import(S(222))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(595) .. name .. S(87) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(477)] = function()
return function(import)
local RunService = game:GetService(S(311))
local log = import(S(188))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(159) .. task_.name .. S(871) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(9) .. task_.name .. S(326)
                    .. string.format(S(342), now - task_.busySince)
                    .. S(2))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(8) .. starved.name .. S(432)
            .. string.format(S(23), now - starved.waitingSince)
            .. S(313))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(393) .. (currentTask and currentTask.name or S(688)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(784)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(644))
local policy = {}
local probes = {}
local order = {}
local mode = S(163)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(358) .. (mode == S(199)
        and S(11)
        or (S(476) .. mode .. S(21))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(258) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(835) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(61) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(462) .. name .. S(620)
            .. string.format(S(533), now - blockedSince[name])
            .. S(287))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(772)] = function()
local Players = game:GetService(S(769))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(72))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(123)] = function()
return function(import)
local i18n = import(S(50))
local UserInputService = game:GetService(S(875))
local GuiService = game:GetService(S(336))
local THEME = {
    BrandName = S(249),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(837))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(65))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(700))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(488))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(634)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(806))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(786))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(681)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(843))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(189)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(557))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(70))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(600))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(459))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(146))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(878))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(611))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(663))
            if not valueLbl then
                valueLbl = Instance.new(S(359))
                valueLbl.Name = S(639)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(202))
            if not badge then
                badge = Instance.new(S(506))
                badge.Name = S(282)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(762))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(474))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(509))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(428))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(85)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(299)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(251))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(520))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(427))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(705)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(357))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(238))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(602))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(404)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(842))
    screenGui.Name = S(747)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(138))
    local main = Instance.new(S(618))
    main.Name = S(19)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(285))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(666))
    tabBar.Name = S(297)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(116))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(572))
    header.Name = S(720)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(209))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(345))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(278))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(216)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(525))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(660)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(179))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(686)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(526))
    searchBox.Name = S(790)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(215))
    searchBox.Text = S(331)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(496))
    content.Name = S(100)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(149))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(356))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(112))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(104))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(60)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(471) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(643) .. (item.Subtitle or S(849))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(713))
        footerLabel.Text = i18n.T(S(93))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(701)] = function()
return function(import)
local gameApi = import(S(430))(import)
local log = import(S(160))
local runGuarded = import(S(819))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(671) .. minItemsToCollect .. S(490))
    else
        log(S(808))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(820) then
            log(S(144))
            lastReported = S(468)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(22) then
            log(S(151))
            lastReported = S(161)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(309) then
            local capacity = gameApi.ChestCapacity()
            log(S(487) .. count .. S(139) .. want .. S(577)
                .. (capacity and (S(401) .. capacity .. S(115)) or S(77)))
            lastReported = S(795)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(46), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(749) .. count .. S(75) .. after .. S(727))
        else
            log(S(642) .. count
                .. S(495) .. tostring(after) .. S(807))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(775)] = function()
return function(import)
local gameApi = import(S(457))(import)
local log = import(S(852))
local runGuarded = import(S(140))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(394) .. (enabled and S(655) or S(682)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(226) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(348) .. reward.category .. S(482)
                .. tostring(reward.tierKey) .. S(679) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(446) .. #pending .. S(814))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(763), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(802)] = function()
return function(import)
local getCurrentPlot = import(S(765))
local getPlotParts = import(S(543))
local log = import(S(724))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(130), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(610) .. (enabled and S(265) or S(127)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(469)] = function()
return function(import)
local Players = game:GetService(S(343))
local getCurrentPlot = import(S(587))
local getPlotParts = import(S(407))
local getMinerSlots = import(S(405))(import)
local teleport = import(S(328))
local parseMoney = import(S(813))
local gameApi = import(S(854))(import)
local log = import(S(498))
local runGuarded = import(S(101))(import)
local spendPolicy = import(S(502))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(654)
local LOCKED_SLOT_TEXT = S(134)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(408))
    return pad and pad:FindFirstChildWhichIsA(S(245), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(646)) then
            local digits = descendant.Text:match(S(262))
            if digits then
                return tonumber((digits:gsub(S(707), S(314))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(669)) and descendant.Text:match(S(17)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(711) .. holder.Name .. S(88)
            .. gameApi.MinerSlots() .. S(295) .. gameApi.MaxMinerSlots() .. S(451)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(378)
    end
    if not prompt.Enabled then
        return true, S(455)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(124) .. price
    end
    local action = prompt.ActionText or S(256)
    if action:match(S(480)) or action:match(S(546)) or action:match(S(210)) then
        return true, S(826) .. action .. S(827)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(107))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(318) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(659) .. why .. S(157))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(578))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(375) .. prompt.ObjectText
                    .. S(279) .. shown .. S(581) .. math.floor(base) .. S(861))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(801))
    if line ~= lastSurvey then
        log(S(529) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(418), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(79), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(839)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(317), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(458) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(71))
    else
        lastAttempt = 0
        log(S(735))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(98),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(677)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(212)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(768)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(736))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(395) .. holder.Name .. S(120))
            return false
        end
        log(S(475) .. candidate.slot .. S(867) .. candidate.name
            .. S(379) .. tostring(why))
    end
    if not miner then
        log(S(14))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(307) .. holder.Name .. S(824) .. miner.name)
        return false
    end
    log(S(281) .. miner.name .. S(255) .. miner.power
        .. S(367) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(402) .. entry.name .. S(704) .. entry.holder.Name
            .. S(493) .. tostring(entry.prompt.ActionText) .. S(870))
        return false
    end
    log(S(675) .. entry.name .. S(267) .. entry.power
        .. S(435) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(719) .. pad.holder.Name .. S(338)
                .. tostring(pad.priceText))
        else
            log(S(211) .. pad.holder.Name
                .. S(347) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(574)
            .. string.format(S(683), bestBefore) .. S(136)
            .. string.format(S(128), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(860), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(603))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(381) then
        runGuarded(S(650), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(794) then
        log(S(869) .. target.holder.Name
            .. S(323) .. tostring(target.priceText))
        runGuarded(S(661), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(562) .. target.holder.Name .. S(664) .. target.name
        .. S(540) .. target.power .. S(246) .. best.name .. S(230) .. best.power .. S(102))
    runGuarded(S(596), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(844)] = function()
return function(import)
local gameApi = import(S(174))(import)
local parseMoney = import(S(840))
local log = import(S(614))
local runGuarded = import(S(757))(import)
local spendPolicy = import(S(706))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(722) .. (enabled and S(698) or S(623)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(537)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(674) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(796) .. deepestKey .. S(645))
        return true
    end
    log(S(564) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(847)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(51) .. gameApi.ZoneDisplayName(zoneKey) .. S(411)
                .. math.floor(price) .. S(47) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(504) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(374) .. zoneKey .. S(13))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(582) then
            reportedLocked = S(746)
            log(S(612) .. zoneKey .. S(703)
                .. gameApi.ZoneDisplayName(currentKey) .. S(40)
                .. done .. S(721) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(207)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(438), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(257)] = function()
return function(import)
local gameApi = import(S(846))(import)
local log = import(S(175))
local runGuarded = import(S(167))(import)
local spendPolicy = import(S(168))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(133) .. (enabled and S(821) or S(204)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(470) .. node.zoneKey .. S(436) .. node.id
                    .. S(177) .. node.family .. S(198) .. node.level .. S(561) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(244) .. node.id
                    .. S(695) .. node.family .. S(805) .. node.level .. S(392) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(575) .. blocked
                .. S(851))
        elseif powerBlocked then
            log(S(817) .. powerBlocked.zoneKey .. S(92) .. powerBlocked.id
                .. S(545) .. powerBlocked.family .. S(38)
                .. math.floor(powerBlocked.powerReq) .. S(815)
                .. math.floor(powerBlocked.power)
                .. S(617))
        else
            log(S(450)
                .. S(403) .. math.floor(gameApi.GetMoney()) .. S(137))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(689) .. nearest.zoneKey
                    .. S(568) .. nearest.id .. S(83) .. nearest.family .. S(400)
                    .. nearest.level .. S(80) .. nearest.max
                    .. S(788) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(485)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(497),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(284))
        end
        if not buyCore then
            table.insert(parts, S(559))
        end
        if #parts == 0 then
            return S(665)
        end
        return S(220) .. table.concat(parts, S(553)) .. S(573)
    end
    local why
    if nearest.cost > budget then
        why = S(105) .. string.format(S(829), nearest.cost)
            .. S(519) .. string.format(S(571), money)
    elseif nearest.powerOk == false then
        why = S(638) .. string.format(S(340), nearest.powerReq) .. S(544)
    else
        why = S(235)
    end
    return S(266) .. nearest.zoneKey .. S(33) .. tostring(nearest.id)
        .. S(838) .. tostring(nearest.family) .. S(613) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(196) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(591) and cost > best)
            or (mode ~= S(456) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(53)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(16), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(156)] = function()
return function(import)
local Workspace = game:GetService(S(556))
local gameApi = import(S(731))(import)
local teleport = import(S(858))
local log = import(S(148))
local runGuarded = import(S(787))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(58))
    else
        lastAttempt = 0
        log(S(248))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(680))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(15), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(629)
                .. (sold and S(538)
                         or S(633)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(630) .. (sold and (S(676) .. #keys .. S(431)) or S(241)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(387), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(479)] = function()
return function(import)
local Workspace = game:GetService(S(242))
local gameApi = import(S(433))(import)
local teleport = import(S(740))
local log = import(S(635))
local runGuarded = import(S(296))(import)
local spendPolicy = import(S(473))(import)
local STATS = { S(6), S(528), S(696) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(605)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(201) .. (enabled and S(590) or S(26)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(584) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(566) or S(7)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(739) .. holderKey .. S(803) .. choice.stat .. S(5)
                        .. choice.info.level .. S(264) .. after.level
                        .. S(304) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(742) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(508))
    local shop = stand and stand:FindFirstChild(S(521))
    if not shop then
        return nil
    end
    if shop:IsA(S(594)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(782), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(876)
                .. (bought and S(117)
                           or S(170)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(441))
        end
    end
    if bought then
        log(S(589) .. pickaxe.index .. S(743) .. pickaxe.name
            .. S(592) .. pickaxe.multiplier .. S(373))
        return true
    end
    log(S(184) .. pickaxe.index .. S(354) .. pickaxe.name
        .. S(737))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(567) .. holderKey .. S(621)
                    .. tostring(worn or S(190)) .. S(647) .. wanted.name)
            else
                log(S(409) .. holderKey .. S(28)
                    .. wanted.name .. S(73) .. tostring(nowWorn or S(91)) .. S(396))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(640) .. holderKey .. S(141) .. variant
                .. S(760) .. after .. S(512) .. cost .. S(728))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(445))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(173) .. gameApi.OwnedPickaxeCount()
                    .. S(658) .. gameApi.MaxMinerSlots()
                    .. S(500))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(752))
        elseif maxed > 0 then
            log(S(389))
        else
            log(S(131))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(383) and price > best)
            or (mode ~= S(346) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(524))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(454), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(745)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(691))
local Workspace = game:GetService(S(276))
local log = import(S(108))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(748), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(628) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(417))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(563) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(110) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(550) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(252) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(714) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(274))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(312) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(195))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(547), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(778) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(4) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(236) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(55), keys)
end
function api.EquipSlot(slot)
    return send(S(758), slot == S(472) and S(186) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(828), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(344), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(609), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(447))
end
function api.BuySkill(zoneKey, family)
    return send(S(554), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(423)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(180)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(812))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(766) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(733) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(773), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(601))
    plots = plots and plots:FindFirstChild(S(811))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(461)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(412)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(366))
    local gui = board and board:FindFirstChild(S(440))
    local root = gui and gui:FindFirstChild(S(744))
    local list = root and root:FindFirstChild(S(516))
    local row = list and list:FindFirstChild(S(800) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(823)) and descendant.Text ~= S(785) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(415) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(154), characterKey, stat == S(324) and S(31) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(391), max = S(672), cost = S(89) },
    Speed = { level = S(97), max = S(522), cost = S(855) },
    MineSpeed = { level = S(754), max = S(514), cost = S(218) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(37) or charName == S(106) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(126) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(181) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(321) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(103) and type(holder.CharName) == S(588) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(651), S(169) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(777) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(301)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(857)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(229)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(371) then
        return {}, S(362)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(303)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(350) or type(state.Track) ~= S(382) then
                table.insert(notes, tabKey .. S(535) .. tostring(category) .. S(122))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(865) .. waiting
                        .. S(422))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(636) .. total .. S(499)
            .. (#notes > 0 and (S(652) .. table.concat(notes, S(119))) or S(52))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(569) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(774)] = function()
return function(import)
local Players = game:GetService(S(132))
local gameApi = import(S(228))(import)
local log = import(S(48))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(841))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(90), S(832))))
    if not value then
        return nil
    end
    if suffix ~= S(271) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(329))
    local mineGui = playerGui and playerGui:FindFirstChild(S(552))
    local hotbar = mineGui and mineGui:FindFirstChild(S(99))
    local content = hotbar and hotbar:FindFirstChild(S(54))
    return content and content:FindFirstChild(S(694))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(78)) then
            local count = descendant.Text:match(S(302))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(193))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(809))
    local nameLabel = content and content:FindFirstChild(S(327))
    local damageLabel = content and content:FindFirstChild(S(308))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(834) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(150) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(856) .. slot .. S(710) .. info.name
                        .. S(776) .. info.power .. S(619) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(648)] = function()
local Players = game:GetService(S(25))
local Workspace = game:GetService(S(49))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(723))
    local plots = mineWorld and mineWorld:FindFirstChild(S(872))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(507)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(57)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(793)),
        Nodes = plot:FindFirstChild(S(376)),
        Chest = plot:FindFirstChild(S(717)),
        Zones = plot:FindFirstChild(S(20)),
        Crew = plot:FindFirstChild(S(398)),
        Loot = plot:FindFirstChild(S(332)),
        PlotIndex = plot:GetAttribute(S(726)),
        ChestCapacity = plot:GetAttribute(S(539)),
    }
end
return getPlotParts
end
modules[S(380)] = function()
return function(import)
    local createWindow = import(S(783))(import)
    local i18n = import(S(816))
    local antiAfk = import(S(280))(import)
    local buildEnv = import(S(759))
    local scheduler = import(S(770))(import)
    local spendPolicy = import(S(377))(import)
    local config = import(S(109))(import)
    local log = import(S(164))
    log(S(527) .. buildEnv.Commit .. S(142) .. buildEnv.Env
        .. S(670) .. buildEnv.BuiltAt .. S(673))
    local window = createWindow({
        Subtitle = S(531),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(239))(import)
    local autoPlace = import(S(685))(import)
    local autoChest = import(S(460))(import)
    local autoSell = import(S(81))(import)
    local autoUpgrade = import(S(598))(import)
    local autoResearch = import(S(56))(import)
    local autoClaim = import(S(27))(import)
    local autoPlot = import(S(678))(import)
    local gameApi = import(S(729))(import)
    scheduler.RegisterStandalone(S(147), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(836), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(43), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(764), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(191), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(448), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(625), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(536), 20)
    scheduler.RegisterExclusive(S(532), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(250), autoResearch.NextPrice)
    spendPolicy.Register(S(259), autoUpgrade.NextPrice)
    spendPolicy.Register(S(576), autoPlot.NextPrice)
    spendPolicy.Register(S(30), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(555), S(322))
    farmTab:AddSectionHeader(S(489))
    persistentSegmented(
        farmTab,
        { S(39), S(206), S(491) },
        { S(518), S(859), S(716) },
        S(792), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(607),
        S(624)
    ), S(732), autoMine.SetEnabled)
    persistentSlider(farmTab, S(384), S(143),
        1, 30, S(254), 5, autoMine.SetCyclesPerSecond, S(334))
    persistentSlider(farmTab, S(444), S(253),
        1, 10, S(517), 1, autoMine.SetClicksPerCycle, S(294))
    persistentToggle(farmTab:AddCard(
        S(153),
        S(325)
    ), S(421), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(626), S(277),
        1, 60, S(708), 3, autoPlace.SetCheckInterval, S(657))
    persistentToggle(
        farmTab:AddCard(S(320),
            S(171)),
        S(397), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(95),
            S(492)),
        S(649), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(84), S(18),
        5, 300, S(96), 60, autoPlace.SetEquipBestInterval, S(231))
    persistentToggle(
        farmTab:AddCard(S(697),
            S(586)),
        S(437), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(114), S(755),
        0, 500, S(330), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(67))
    persistentToggle(farmTab:AddCard(
        S(352),
        S(372)
    ), S(275), autoChest.SetEnabled)
    persistentSlider(farmTab, S(501), S(523),
        50, 3000, S(818), 100, autoChest.SetMinItems, S(369))
    persistentSlider(farmTab, S(351), S(453),
        1, 60, S(223), 3, autoChest.SetCheckInterval, S(24))
    persistentToggle(farmTab:AddCard(
        S(333),
        S(478)
    ), S(289), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(622),
            S(797)),
        S(877), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(570), S(771),
        5, 300, S(364), 20, autoSell.SetCheckInterval, S(604))
    farmTab:AddSlider(S(240),
        S(414),
        5, 120, sellMaxWait, function(value)
            config.Set(S(542), value)
        end, S(751))
    persistentToggle(farmTab:AddCard(
        S(439),
        S(443)
    ), S(789), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(32), S(426),
        5, 300, S(288), 20, autoUpgrade.SetInterval, S(579))
    persistentSlider(farmTab, S(29), S(560),
        1, 20, S(862), 5, autoUpgrade.SetMaxPerPass, S(386))
    persistentSegmented(
        farmTab,
        { S(41), S(361), S(270), S(86) },
        { S(300), S(234), S(551), S(224) },
        S(365), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(874), S(118),
        0, 500, S(580), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(172))
    persistentToggle(
        farmTab:AddCard(S(761),
            S(424)),
        S(82), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(111) .. pickaxe.multiplier .. S(305)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(319)
            end
            return S(466) .. index .. S(232) .. (pickaxeLabels[index] or S(494))
        end
        local savedPick = config.Get(S(464), 0)
        pickaxeCard = farmTab:AddSlider(
            S(420),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(45), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(410)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(779),
            S(756)),
        S(481), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(850),
            S(291)),
        S(712), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(565),
            S(558)),
        S(419), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(833), S(64),
        1, 20, S(182), 2, autoUpgrade.SetMutationTarget, S(339))
    persistentSlider(farmTab, S(272), S(94),
        0, 500, S(145), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(247))
    persistentToggle(farmTab:AddCard(
        S(293),
        S(730)
    ), S(718), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(76), S(263),
        10, 600, S(341), 30, autoResearch.SetCheckInterval, S(219))
    persistentSlider(farmTab, S(306), S(315),
        1, 20, S(734), 5, autoResearch.SetMaxPerPass, S(449))
    persistentSlider(farmTab, S(741), S(631),
        0, 500, S(484), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(853))
    persistentToggle(
        farmTab:AddCard(S(511),
            S(548)),
        S(798), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(286),
            S(583)),
        S(513), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(165),
            S(845)),
        S(42), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(158), S(113) },
        { true, false },
        S(273), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(864),
        S(316)
    ), S(208), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(162), S(225),
        5, 300, S(12), 30, autoClaim.SetCheckInterval, S(699))
    persistentSlider(farmTab, S(599), S(831),
        1, 30, S(515), 10, autoClaim.SetMaxPerPass, S(505))
    persistentToggle(farmTab:AddCard(
        S(549),
        S(68)
    ), S(668), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(627), S(268),
        5, 300, S(135), 30, autoPlot.SetCheckInterval, S(615))
    persistentToggle(
        farmTab:AddCard(S(335),
            S(822)),
        S(399), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(1),
            S(283)),
        S(337), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(349), S(192),
        0, 500, S(44), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(616))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(429))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(463), S(702))
    persistentToggle(
        settingsTab:AddCard(S(684), S(804)),
        S(653),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(637), 100)
    settingsTab:AddSlider(S(213), S(227), 50, 150, savedScale, function(value)
        config.Set(S(465), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(155), 1)
    settingsTab:AddSegmented({ S(406), S(178), S(214) }, savedLanguage, function(index, value)
        local code = ({ S(36), S(687), S(830) })[index]
        config.Set(S(385), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(799), S(416), S(176) })[savedLanguage] or S(534))
    log(S(355) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(632)] = function()
return {
    [79389059854988] = S(370),
}
end
import(S(866))(import)
