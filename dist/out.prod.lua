-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoPlace] slot ", "UIListLayout", "Chest", "games/my_anime_mine/auto_upgrade", "", "nothing to place or upgrade", " waited ", "s", "core/i18n", "", "TextLabel", "core/ui", "none", "s", "Mining", "TextButton", " ", "core/scheduler", "[AutoChest] disabled", "games/my_anime_mine/auto_research", "AutoUpgrade", "[AutoPlace] couldn't pick up ", "auto", "unlock", "ฟาร์ม", " still empty after placing ", "autoSell", "autoPlotLatest", "games/my_anime_mine/main", "\n", "M", " reward(s) looked claimable but none went through", "Anti-AFK", "games/my_anime_mine/auto_sell", "", "[AutoClaim] claimed ", "", ": ", "[AutoPlace] ", "ClickDetector", "TextLabel", "Frame", ", built ", "x", "index rewards are disabled in this game build", "[AutoUpgrade] every stat on every miner is maxed", "ItemFrame", "อ่านป้ายบน chest ทุกกี่วินาที", "Frame", "enabled", "games/my_anime_mine/get_current_plot", "auto", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "(nothing to do)", "core/logger", " node(s) looked buyable but the server refused", "core/teleport", "nothing claimable", "number", "games/my_anime_mine/auto_place", "ProximityPrompt", "Players", "Auto Mine: ความเร็ว", "Frame", ")", " for ", ": ", "games/my_anime_mine/get_current_plot", "Frame", "UserInputService", "TextLabel", " ", ")", "games/my_anime_mine/get_plot_parts", "Root", " (chest holds ", "lowest", "core/config", "games/my_anime_mine/auto_mine", "AutoSell", "sold ", "empty", "VirtualUser", "TextLabel", "search_placeholder", "%.3g", "GetCharacterUpgradeCost", "[", "TextLabel", "[AutoUpgrade] bought pickaxe #", "BasePart", "CoreGui", ") ", "[AutoPlace] disabled", " ชิ้น", "ReplicatedStorage", "Auto Research: ซื้อสาย Core", "[Bb]uy", "core/anti_afk", "core/i18n", "autoResearch", " error: ", "table", "Players", "Loot", " costs ", "TextLabel", "Workspace", "PlayerGui", " ", ",", "core/logger", "TextButton", "TextButton", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "[AutoClaim] ", "table", "'", " x", ")", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "autoPlaceUnlockBudget", "autoSellInterval", "needs ", "เช็คโซนทุกกี่วินาที", "core/logger", "Holders", "Core buying is off", ",", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "games/my_anime_mine/game_api", "MineEquipSlot", " items (now ", "languageIndex", "slot not in the hotbar", "[AutoUpgrade] ", "table", " ", "=", "AutoResearch", "[AutoSell] MineSellItems ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "Humanoid", "autoResearchCore", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "Mining ก่อน", "autoMineSpeed", "HumanoidRootPart", "TextLabel", "UIPadding", "", "HttpService", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "Modules", " ", "auto", "/", " ", ": badge says ", "TextButton", "[AutoPlot] ", "/", " ", "Auto Claim: เก็บสูงสุดต่อรอบ", "", " (", "", " from ", "UIScale", "/", "Crew", " team power, you have ", "upgrade", "core/run_job", "fill", "games/my_anime_mine/game_api", "autoUpgrade", "autoChestMinItems", "[AutoPlace] unlocking ", "[AutoChest] enabled (collects once >= ", "SellStand", "Auto Upgrade: Pickaxe ที่จะซื้อ", "core/teleport", "autoUpgradeRollMutation", "core/run_job", "^M(%d+)$", " costs ", "[AutoPlace] fireproximityprompt error: ", " ", "MineSpeed", "GetHolderLevel", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "Badge", "%.3g", " pickaxe(s) for ", "autoUpgradeMaxPerPass", "Header", "Players", " - ", "games/my_anime_mine/get_current_plot", "autoPlotKeepMoney", "🏠", "AutoPlot", "games/my_anime_mine/auto_chest", "games/my_anime_mine/auto_research", "[Scheduler] turn: ", "auto", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "[AutoPlot] ", "fill", "no isfile/readfile available to verify the write", "maxed", " (money ", "[AutoResearch] ", "0 = ไม่จำกัด", "Speed ก่อน", "open_menu_hint", "core/click", "Players", "button has no size on screen", ")", "ถูกสุดก่อน", ")", "core/dispatch", " ", "games/registry", "MineWorld", "Damage", ", base ", "autoUpgradePriority", " claimable but none could be resolved", "enabled", "core/anti_afk", "[AntiAFK] idle detected, simulating input", " ", "Damage", "disabled", "ปุ่มเปิดเมนู", "%.3g", "%", " stack(s)", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "table", "core/run_job", "server didn't equip it", "core/i18n", "action '", "games/my_anime_mine/game_api", "[AutoClaim] ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", ": ", "games/my_anime_mine/auto_chest", "string", "Workspace", "HotbarFrame", "autoUpgradeEquipPickaxe", "X", "?", " -> ", "languageIndex", "Content", "MineSpeed", "", "core/logger", "s - jumping the queue", "lowest", "-", "core/run_job", " -> ", "", "AutoClaim", "List", "GetCharacterMineSpeedUpgradeCost", "⚙", "AutoChest", " (", "Auto Research (Plot)", " pickaxe ", "This is not a supported place.", "Plots", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "Mutation: เพดานราคาต่อครั้ง", "Auto Research: ซื้อสูงสุดต่อรอบ", "table", "BasePart", "Auto Research: กันเงินไว้", " for ", "table", "antiAfk", "Damage ก่อน", ", ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "CharMaxLevel", "core/parse_money", "Auto Plot: ซื้อโซนถัดไป", "ต่ำสุดก่อน", "", "MineBuyPickaxe", "table", "Pad", "th", "function", "table", "แพงสุดก่อน", "core/run_job", "UICorner", "neither appendfile nor writefile is available", "core/logger", "games/my_anime_mine/get_best_miner", "unreadable", "Frame", "Players", "ขายทุกกี่วินาที", "games/my_anime_mine/game_api", "ScreenGui", "research", "autoUpgradeKeepMoney", "autoResearchCoreLast", "[Config] couldn't write ", "core/get_place_id", "=locked(", "prod", "autoUpgradeMutationTarget", "[Scheduler] ", "Workspace", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "[AutoPlace] placed ", "autoResearchCheapestFirst", " - upgrade the miners first", "ค้นหาฟังก์ชัน...", "nothing left to buy (", "Container", "Open menu", "AutoMine", "", "Speed", " possible holder(s) - not buying more", "ItemName", "Auto Place Miner", "Auto Plot: กันเงินไว้", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "may need us at the stand - trying that", "en", "MekPro.log", "M", "TextButton", "autoClaimMaxPerPass", "enabled", ") didn't take: ", "ตั้งค่า", "Empty miner slot", "th", "GetHolderSpeedLevel", "sale didn't go through", "MineRollMutation", " | ", "AutoPlace", "x", "table", "core/run_job", "[AutoUpgrade] MineBuyPickaxe ", "games/my_anime_mine/get_plot_parts", "TextLabel", "#", "AutoClaim", "autoMine", "en-us", "search_placeholder", "[AutoUpgrade] pickaxe #", "empty", "none", "/config.json", "; ", "[%X] ", "^([%d,]+)$", "autoResearchInterval", "core/get_leaderstat", " items)", " (power ", "BasePart", "ไทย", "TextLabel", "[AutoChest] MineTakeChest had no effect (", "CoreUI", "UIStroke", "games/my_anime_mine/game_api", "disabled", "Auto Place: เพดานราคาปลดช่อง", ")", " (", "games/my_anime_mine/auto_claim", "Auto Research: รอบเช็ค", "games/my_anime_mine/get_plot_parts", "MineWorld", "table", "core/logger", "All", "waiting", "ValueLabel", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", " didn't go through (out of stock or too expensive)", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "M", "[AutoPlot] ", "[AutoResearch] nothing open and affordable right now", "ClaimableTiers failed", "Limited", "Search", "ProximityPrompt", "TextLabel", "core/teleport", ", have ", " has been busy for ", "autoUpgradePickaxe", "VirtualInputManager", "[MekPro] build ", "[AutoMine] ", "autoPlaceUnlock", "\n", "GetCharacterSpeedUpgradeCost", "", "BasePart", "[AutoPlace] ", " research is ", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " not bought yet (", "core/dispatch", "[AutoPlot] now mining ", "", "[AutoClaim] idle: ", ")", " -> ", "MineEquipPickaxe", "Frame", "write reported success but the line isn't in ", "AutoResearch", "Auto Plot: รอบเช็ค", ") -> ", "] job failed: ", "prompt disabled", ") needs ", "/s", "autoUpgradeLimitPickaxes", " team power", "Shop Stand", "autoClaim", "AutoPlot", "Workspace", "UIListLayout", "write reported success but ", ": ", "MineGui", "Auto Place: เปลี่ยนตัวอ่อนออก", "Zone buying is off", "core/get_place_id", "autoResearchZones", "Players", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "table", "autoPlaceUpgradeWeak", "games/my_anime_mine/auto_plot", "MineSellItems", "core/logger", "Players", "works from anywhere - no teleport needed", "%.0f", "OwnerUserId", "disabled", "[AutoPlot] unlocked ", ")", "core/humanoid", "TextLabel", "ValueLabel", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "[Uu]nlock", "function", "MineUpgradeCharacter", "ไล่ซื้อทุกกี่วินาที", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "[AutoResearch] idle: ", " refused - ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "M", "/", "[AutoUpgrade] ", "core/logger", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "[AutoChest] collected ", "autoUpgradePickaxeIndex", " -> ", "Mutation: หยุดเมื่อได้ x", " still locked after paying ", "string", "อังกฤษ", "TextLabel", "autoPlotBuy", "autoPlaceInterval", "table", "[AntiAFK] disabled", "[AutoPlace] drifted out of range of ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "no mine data", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "TextButton", "AutoUpgrade", "autoChest", "Auto Claim: รอบเช็ค", "AutoChest", ")", "", "Auto Chest: รอบเช็ค", "core/logger", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "Auto Upgrade: สุ่ม Mutation", "enabled", "Auto Upgrade Plot (Zone)", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "Players", "s - carrying on without it", "", "%.0f", "cheapest open node ", "Exclusive", "0 = ไม่จำกัด", ") ", " (x", "RunService", "[AutoUpgrade] ", "needs ", "autoMineClicks", "MinePower", "ZoneBoardGui", "[GameAPI] couldn't load the game's modules: ", "Hauler", "GuiService", "Content", "core/scheduler", "games/my_anime_mine/auto_claim", "ย่อขยายเมนูทั้งชุด", "MekPro", "Text", "unlock", "MineSpeed", "([%d,%.]+)%s*(%a*)", " (x", "autoSellMaxWait", "[AutoUpgrade] ", "core/teleport", "MineSelectZone", "core/build_env", "disabled", "autoPlace", "0fb5d9b+", "menuScale", "[AutoUpgrade] ", "en", "Frame", "leaderstats", "TextLabel", "^x(%d+)$", "string", " wouldn't take ", "[MekPro] settings file: ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "Auto Mine: คลิกต่อรอบ", ")", "Search functions...", "/", " (", "Locked miner slot", ") on ", "[AntiAFK] enabled - watching Player.Idled", "[AutoPlace] upgrading ", " (", "AutoSell", "%$%s*([%d,%.]+)%s*(%a*)", "th", "Backpack", "core/ui", "", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", ") gave nothing", "%$", "games/my_anime_mine/game_api", "autoPlot", "th", "", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "cost", "ตามเครื่อง", "autoSellProtectUnits", "games/my_anime_mine/game_api", "Nodes", "autoClaimInterval", "Character", "games/my_anime_mine/auto_place", "[AutoPlace] unlocked ", "[AutoResearch] ", "autoUpgradeInterval", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "ไล่อัปเกรด miner ทุกกี่วินาที", "core/logger", " (", "PlotIndex", "Frame", "", "[AutoPlace] nothing in the hotbar to place", "M", "PickaxesStand", "core/parse_money", "core/logger", "every open node is maxed", "MineBuySkill", "core/debug_tab", "RunService", "disabled", "Humanoid", ")", "autoChestInterval", "Auto Upgrade: กันเงินไว้", "Auto Chest: เกณฑ์เก็บ", "ZoneRow", "Speed", "[AutoSell] disabled", "[AutoPlace] ", "TextBox", "", "s", "Players", "%$", "games/my_anime_mine/game_api", "MineTakeChest", "Core", "table", "research", "TextButton", "locked slot", "s", "core/logger", "Auto Sell", "CharMineSpeedMaxLevel", ")", "games/my_anime_mine/auto_plot", "TabBar", ",", "UserInputService", "", "Frame", "games/my_anime_mine/auto_upgrade", "[AutoSell] enabled", "autoUpgradePickaxeIndex", "2026-09-21 12:49:31", "games/registry", "slot ", "games/my_anime_mine/auto_sell", "[GameAPI] SendEvent failed: ", "(", "[AutoChest] chest is empty", "[AutoPlace] holders: ", "Content", " (power ", ": State(", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", " (", "TextLabel", "[AutoPlace] couldn't hold anything - not pressing E", "Frame", " ", "", "slot past the usable cap", "Auto Research: ซื้อสาย Zone", "Auto Plot: ใช้โซนล่าสุดเสมอ", "ChestCapacity", "s", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "ScrollingFrame", "OwnerUserId", "TextButton", "", " (", " (", "UIListLayout", "Auto Sell: รอบขาย", "TextLabel", "games/my_anime_mine/main", "UICorner", "needs us at the stand - keeping the teleport", " mutation x", "x", "both Zone and Core buying are switched off", "Auto Upgrade: แจก Pickaxe ให้ miner", "open_menu_hint", "menuScale", " but no unclaimed reward in its Track", " (", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "s", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "Plots", "ZoneBoard", "[AutoPlot] couldn't switch to ", "autoPlotInterval", "/", "[AutoResearch] cheapest open node: ", "[AutoResearch] ", "MekPro", ") from ", "Auto Place: รอบเช็ค", "", "Main", "Auto Upgrade: รอบเช็ค", "Zones", "CharSpeedMaxLevel", "[BestMiner] slot ", "", "core/logger", "=empty", "Text", "core/run_job", " power ", ": ", "AutoPlace", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "works from anywhere - dropping the teleport", "autoResearchMaxPerPass", "[AutoUpgrade] ", ")", "HumanoidRootPart", ") ", "s", "Frame", "')", "BodyPosition", "Hauler", "HumanoidRootPart", "autoUpgradeMutationBudget", "[AutoResearch] Core ", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "Auto Sell: รอคิวได้นานสุด", "My Anime Mine · Mining Hub", "Damage", "Auto Upgrade Unit", "Workspace", "core/get_place_id", "table", "en", "core/get_me", "games/my_anime_mine/game_api", "s", " (", "autoResearchKeepMoney", ", you have ", "[Config] couldn't read ", " #", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "games/my_anime_mine/auto_mine", "Auto Claim (Index)", ") ", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "[AutoChest] waiting: ", "table", "BodyVelocity", "[AutoSell] ", "costs ", "string", "modules not loaded", "core/config", "AutoPlace", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "cost", "x", "ขนาดเมนู", ")", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "core/logger", "Auto Chest", "string", "[AutoPlace] picked up ", "[AutoResearch] ", " items", "core/build_env", "[Scheduler] ", " (still ", ")", "waiting", "table", "games/my_anime_mine/get_best_miner", " owned)", "^%$", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "TextLabel", "GetHolderMineSpeedLevel", " while selecting", " -> x", "Auto Place: ปลดล็อกช่อง miner", "enabled", "core/logger", "AutoPlace", " -> ", "[AutoChest] can't read the chest yet", "games/my_anime_mine/game_api", "Auto Mine", "unreadable", "Badge", "core/parse_money", " doesn't exist", "[Scheduler] ", " (prompt says '", "Players", "/", "core/logger", "[Logger] failed to write ", "affordable - waiting for a turn", "badge says ", "x", " (cost ", "core/run_job", "autoSellMaxWait"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(233)] = function()
return function(import)
local log = import(S(397))
local Players = game:GetService(S(458))
local VirtualUser = game:GetService(S(83))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(574))
        connection = player.Idled:Connect(function()
            log(S(234))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(501))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(785)] = function()
return { Env = S(323), IsDev = false, Commit = S(555), BuiltAt = S(654) }
end
modules[S(218)] = function()
local VirtualInputManager = game:GetService(S(416))
local GuiService = game:GetService(S(537))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(220)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(770)] = function()
return function(import)
local HttpService = game:GetService(S(152))
local getPlaceId = import(S(746))
local log = import(S(641))
local ROOT_FOLDER = S(708)
local placeFolder = ROOT_FOLDER .. S(570) .. tostring(getPlaceId())
local filePath = placeFolder .. S(373)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(360) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(755) .. filePath .. S(38) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(320) .. filePath .. S(723) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(224)] = function()
return function(import)
    local getPlaceId = import(S(321))
    local registry = import(S(655))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(280))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(378)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(560))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(749)] = function()
local Players = game:GetService(S(198))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(143))
    local backpack = player:WaitForChild(S(580))
    return player, humanoid, backpack
end
return getMe
end
modules[S(456)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(472)] = function()
local Players = game:GetService(S(813))
local RunService = game:GetService(S(529))
local UserInputService = game:GetService(S(70))
local Workspace = game:GetService(S(255))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(619))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(730))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(765))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(91)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(423)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(735))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(9)] = function()
local Players = game:GetService(S(104))
local STRINGS = {
    en = {
        search_placeholder = S(569),
        open_menu_hint = S(334),
    },
    th = {
        search_placeholder = S(331),
        open_menu_hint = S(238),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(368)
    if locale:sub(1, 2):lower() == S(353) then
        return S(588)
    end
    return S(748)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(52) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(488)] = function()
local LOG_FILE = S(345)
local function log(message)
    local line = os.date(S(375)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(151))
            end
            appendfile(LOG_FILE, line .. S(420))
        elseif writefile then
            local existing = S(711)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(30))
        else
            error(S(308))
        end
        if not (isfile and readfile) then
            error(S(211))
        end
        if not isfile(LOG_FILE) then
            error(S(451) .. LOG_FILE .. S(810))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(436) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(816) .. LOG_FILE .. S(252) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(612)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(768) then
        return nil
    end
    local number, suffix = text:match(S(578))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(111), S(629))))
    if not value then
        return nil
    end
    if suffix ~= S(671) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(245)] = function()
return function(import)
local log = import(S(801))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(88) .. name .. S(440) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(539)] = function()
return function(import)
local RunService = game:GetService(S(617))
local log = import(S(604))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(325) .. task_.name .. S(102) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(811) .. task_.name .. S(414)
                    .. string.format(S(523), now - task_.busySince)
                    .. S(521))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(786) .. starved.name .. S(7)
            .. string.format(S(467), now - starved.waitingSince)
            .. S(266))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(206) .. (currentTask and currentTask.name or S(54)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(183)] = function()
local Players = game:GetService(S(62))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(737))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(12)] = function()
return function(import)
local i18n = import(S(247))
local UserInputService = game:GetService(S(648))
local THEME = {
    BrandName = S(542),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(307))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(688))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(150))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(114))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(264)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(386))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(497))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(259)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(686))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(5)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(678))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(684))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(71))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(650))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(149))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(11))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(89))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(400))
            if not valueLbl then
                valueLbl = Instance.new(S(84))
                valueLbl.Name = S(474)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(808))
            if not badge then
                badge = Instance.new(S(41))
                badge.Name = S(193)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(347))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(69))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(42))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(113))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(717)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(240)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(607))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(49))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(160))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(649)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(435))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(450))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(638))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(720)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(316))
    screenGui.Name = S(385)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(92))
    local main = Instance.new(S(669))
    main.Name = S(712)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(169))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(559))
    tabBar.Name = S(646)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(2))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(733))
    header.Name = S(197)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(16))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(364))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(561))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(582)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(680))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(258)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(506))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(268)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(628))
    searchBox.Name = S(409)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(85))
    searchBox.Text = S(430)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(312))
    content.Name = S(662)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(64))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(107))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(694))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(473))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(543)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(589) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(158) .. (item.Subtitle or S(165))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(369))
        footerLabel.Text = i18n.T(S(217))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(253)] = function()
return function(import)
local gameApi = import(S(315))(import)
local log = import(S(55))
local runGuarded = import(S(361))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(180) .. minItemsToCollect .. S(379))
    else
        log(S(19))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(311) then
            log(S(804))
            lastReported = S(807)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(371) then
            log(S(660))
            lastReported = S(82)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(399) then
            local capacity = gameApi.ChestCapacity()
            log(S(763) .. count .. S(162) .. want .. S(784)
                .. (capacity and (S(76) .. capacity .. S(221)) or S(35)))
            lastReported = S(789)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(510), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(490) .. count .. S(133) .. after .. S(568))
        else
            log(S(384) .. count
                .. S(260) .. tostring(after) .. S(120))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(540)] = function()
return function(import)
local gameApi = import(S(633))(import)
local log = import(S(126))
local runGuarded = import(S(721))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(116) .. (enabled and S(517) or S(553)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(431) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(36) .. reward.category .. S(670)
                .. tostring(reward.tierKey) .. S(756) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(250) .. #pending .. S(32))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(272), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(79)] = function()
return function(import)
local getCurrentPlot = import(S(68))
local getPlotParts = import(S(363))
local log = import(S(779))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(40), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(418) .. (enabled and S(232) or S(237)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(598)] = function()
return function(import)
local Players = game:GetService(S(520))
local getCurrentPlot = import(S(51))
local getPlotParts = import(S(74))
local getMinerSlots = import(S(310))(import)
local teleport = import(S(412))
local parseMoney = import(S(809))
local gameApi = import(S(387))(import)
local log = import(S(265))
local runGuarded = import(S(185))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(352)
local LOCKED_SLOT_TEXT = S(572)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(301))
    return pad and pad:FindFirstChildWhichIsA(S(61), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(383)) then
            local digits = descendant.Text:match(S(376))
            if digits then
                return tonumber((digits:gsub(S(129), S(298))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(411)) and descendant.Text:match(S(793)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(656) .. holder.Name .. S(427)
            .. gameApi.MinerSlots() .. S(157) .. gameApi.MaxMinerSlots() .. S(792)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(639)
    end
    if not prompt.Enabled then
        return true, S(441)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(767) .. price
    end
    local action = prompt.ActionText or S(10)
    if action:match(S(476)) or action:match(S(98)) or action:match(S(585)) then
        return true, S(248) .. action .. S(118)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(148))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(188) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(322) .. why .. S(432))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(719))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(139) .. prompt.ObjectText
                    .. S(659) .. shown .. S(229) .. math.floor(base) .. S(223))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(357))
    if line ~= lastSurvey then
        log(S(661) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(175), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(173), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(24), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(424) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(94))
    else
        lastAttempt = 0
        log(S(762))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(243),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(135)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(672)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(246)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(609))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(502) .. holder.Name .. S(797))
            return false
        end
        log(S(1) .. candidate.slot .. S(683) .. candidate.name
            .. S(350) .. tostring(why))
    end
    if not miner then
        log(S(668))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(39) .. holder.Name .. S(26) .. miner.name)
        return false
    end
    log(S(328) .. miner.name .. S(663) .. miner.power
        .. S(573) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(22) .. entry.name .. S(168) .. entry.holder.Name
            .. S(812) .. tostring(entry.prompt.ActionText) .. S(734))
        return false
    end
    log(S(782) .. entry.name .. S(380) .. entry.power
        .. S(709) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(627) .. pad.holder.Name .. S(494)
                .. tostring(pad.priceText))
        else
            log(S(599) .. pad.holder.Name
                .. S(66) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(6))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(210) then
        runGuarded(S(771), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(544) then
        log(S(179) .. target.holder.Name
            .. S(288) .. tostring(target.priceText))
        runGuarded(S(802), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(575) .. target.holder.Name .. S(67) .. target.name
        .. S(576) .. target.power .. S(439) .. best.name .. S(697) .. best.power .. S(65))
    runGuarded(S(358), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(645)] = function()
return function(import)
local gameApi = import(S(176))(import)
local parseMoney = import(S(295))
local log = import(S(112))
local runGuarded = import(S(306))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(161) .. (enabled and S(50) or S(388)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(632)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(429) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(571) .. deepestKey .. S(777))
        return true
    end
    log(S(703) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(405) .. gameApi.ZoneDisplayName(zoneKey) .. S(187)
                .. math.floor(price) .. S(754) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(470) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(605) .. zoneKey .. S(729))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(637) then
            reportedLocked = S(317)
            log(S(209) .. zoneKey .. S(482)
                .. gameApi.ZoneDisplayName(currentKey) .. S(425)
                .. done .. S(814) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(448), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(205)] = function()
return function(import)
local gameApi = import(S(805))(import)
local log = import(S(815))
local runGuarded = import(S(821))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(783) .. (enabled and S(349) or S(469)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(600) .. node.zoneKey .. S(155) .. node.id
                    .. S(277) .. node.family .. S(731) .. node.level .. S(803) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(739) .. node.id
                    .. S(391) .. node.family .. S(760) .. node.level .. S(270) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(214) .. blocked
                .. S(56))
        elseif powerBlocked then
            log(S(707) .. powerBlocked.zoneKey .. S(235) .. powerBlocked.id
                .. S(166) .. powerBlocked.family .. S(442)
                .. math.floor(powerBlocked.powerReq) .. S(172)
                .. math.floor(powerBlocked.power)
                .. S(330))
        else
            log(S(406)
                .. S(213) .. math.floor(gameApi.GetMoney()) .. S(390))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(706) .. nearest.zoneKey
                    .. S(72) .. nearest.id .. S(752) .. nearest.family .. S(527)
                    .. nearest.level .. S(170) .. nearest.max
                    .. S(106) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(692)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(635),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(455))
        end
        if not buyCore then
            table.insert(parts, S(128))
        end
        if #parts == 0 then
            return S(614)
        end
        return S(332) .. table.concat(parts, S(292)) .. S(620)
    end
    local why
    if nearest.cost > budget then
        why = S(124) .. string.format(S(194), nearest.cost)
            .. S(413) .. string.format(S(86), money)
    elseif nearest.powerOk == false then
        why = S(531) .. string.format(S(239), nearest.powerReq) .. S(445)
    else
        why = S(817)
    end
    return S(524) .. nearest.zoneKey .. S(163) .. tostring(nearest.id)
        .. S(666) .. tostring(nearest.family) .. S(93) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(481) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(140), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(657)] = function()
return function(import)
local Workspace = game:GetService(S(745))
local gameApi = import(S(249))(import)
local teleport = import(S(550))
local log = import(S(309))
local runGuarded = import(S(269))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(626))
    else
        lastAttempt = 0
        log(S(652))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(181))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(410), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(141)
                .. (sold and S(726)
                         or S(689)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(766) .. (sold and (S(81) .. #keys .. S(241)) or S(355)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(80), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(651)] = function()
return function(import)
local Workspace = game:GetService(S(108))
local gameApi = import(S(594))(import)
local teleport = import(S(57))
local log = import(S(613))
local runGuarded = import(S(174))(import)
local STATS = { S(228), S(337), S(190) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(267)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(549) .. (enabled and S(800) or S(618)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(137) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(591) or S(212)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(728) .. holderKey .. S(189) .. choice.stat .. S(17)
                        .. choice.info.level .. S(433) .. after.level
                        .. S(486) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(774) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(611))
    local shop = stand and stand:FindFirstChild(S(446))
    if not shop then
        return nil
    end
    if shop:IsA(S(381)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(286), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(362)
                .. (bought and S(466)
                           or S(343)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(475))
        end
    end
    if bought then
        log(S(90) .. pickaxe.index .. S(138) .. pickaxe.name
            .. S(547) .. pickaxe.multiplier .. S(511))
        return true
    end
    log(S(370) .. pickaxe.index .. S(225) .. pickaxe.name
        .. S(402))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(487) .. holderKey .. S(279)
                    .. tostring(worn or S(372)) .. S(492) .. wanted.name)
            else
                log(S(136) .. holderKey .. S(564)
                    .. wanted.name .. S(787) .. tostring(nowWorn or S(13)) .. S(644))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(557) .. holderKey .. S(690) .. variant
                .. S(798) .. after .. S(820) .. cost .. S(471))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(530) .. gameApi.OwnedPickaxeCount()
                    .. S(195) .. gameApi.MaxMinerSlots()
                    .. S(338))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(677))
        elseif maxed > 0 then
            log(S(46))
        else
            log(S(142))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(21), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(131)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(96))
local Workspace = game:GetService(S(449))
local log = import(S(718))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(154), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(535) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(460))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(500) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(536) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(747) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(396) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(736) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(186))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(658) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(634))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(398), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(117) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(103) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(597) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(463), keys)
end
function api.EquipSlot(slot)
    return send(S(132), slot == S(522) and S(422) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(299), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(434), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(356), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(615), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(300)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(465)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(533))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(303) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(289) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(551), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(227))
    plots = plots and plots:FindFirstChild(S(701))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(219)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(679)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(702))
    local gui = board and board:FindFirstChild(S(534))
    local root = gui and gui:FindFirstChild(S(75))
    local list = root and root:FindFirstChild(S(273))
    local row = list and list:FindFirstChild(S(624) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(795)) and descendant.Text ~= S(681) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(477) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(478), characterKey, stat == S(15) and S(545) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(191), max = S(294), cost = S(87) },
    Speed = { level = S(354), max = S(715), cost = S(421) },
    MineSpeed = { level = S(796), max = S(643), cost = S(274) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(563) or charName == S(167) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(285) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(59) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(495) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(244) and type(holder.CharName) == S(781) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(408), S(525) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(636) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(769)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(504)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(45)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(764) then
        return {}, S(407)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(58)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(790) or type(state.Track) ~= S(304) then
                table.insert(notes, tabKey .. S(664) .. tostring(category) .. S(584))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(159) .. waiting
                        .. S(696))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(818) .. total .. S(231)
            .. (#notes > 0 and (S(199) .. table.concat(notes, S(374))) or S(336))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(254) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(791)] = function()
return function(import)
local Players = game:GetService(S(313))
local gameApi = import(S(586))(import)
local log = import(S(464))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(546))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(647), S(271)))) or 0
    if suffix ~= S(512) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(109))
    local mineGui = playerGui and playerGui:FindFirstChild(S(453))
    local hotbar = mineGui and mineGui:FindFirstChild(S(256))
    local content = hotbar and hotbar:FindFirstChild(S(538))
    return content and content:FindFirstChild(S(333))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(667)) then
            local count = descendant.Text:match(S(562))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(47))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(262))
    local nameLabel = content and content:FindFirstChild(S(339))
    local damageLabel = content and content:FindFirstChild(S(236))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(608) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(705) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(716) .. slot .. S(452) .. info.name
                        .. S(722) .. info.power .. S(119) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(200)] = function()
local Players = game:GetService(S(631))
local Workspace = game:GetService(S(326))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(395))
    local plots = mineWorld and mineWorld:FindFirstChild(S(281))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(468)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(394)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(127)),
        Nodes = plot:FindFirstChild(S(595)),
        Chest = plot:FindFirstChild(S(3)),
        Zones = plot:FindFirstChild(S(714)),
        Crew = plot:FindFirstChild(S(171)),
        Loot = plot:FindFirstChild(S(105)),
        PlotIndex = plot:GetAttribute(S(606)),
        ChestCapacity = plot:GetAttribute(S(675)),
    }
end
return getPlotParts
end
modules[S(687)] = function()
return function(import)
    local createWindow = import(S(581))(import)
    local i18n = import(S(100))
    local antiAfk = import(S(99))(import)
    local buildEnv = import(S(552))
    local scheduler = import(S(18))(import)
    local config = import(S(78))(import)
    local log = import(S(514))
    log(S(417) .. buildEnv.Commit .. S(682) .. buildEnv.Env
        .. S(43) .. buildEnv.BuiltAt .. S(73))
    local window = createWindow({
        Subtitle = S(742),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(758))(import)
    local autoPlace = import(S(60))(import)
    local autoChest = import(S(204))(import)
    local autoSell = import(S(34))(import)
    local autoUpgrade = import(S(4))(import)
    local autoResearch = import(S(20))(import)
    local autoClaim = import(S(392))(import)
    local autoPlot = import(S(462))(import)
    local gameApi = import(S(750))(import)
    scheduler.RegisterStandalone(S(335), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(276), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(724), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(507), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(437), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(366), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(203), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(822), 20)
    scheduler.RegisterExclusive(S(577), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(25), S(202))
    persistentToggle(farmTab:AddCard(
        S(806),
        S(403)
    ), S(367), autoMine.SetEnabled)
    persistentSlider(farmTab, S(63), S(700),
        1, 30, S(147), 5, autoMine.SetCyclesPerSecond, S(443))
    persistentSlider(farmTab, S(567), S(483),
        1, 10, S(532), 1, autoMine.SetClicksPerCycle, S(691))
    persistentToggle(farmTab:AddCard(
        S(340),
        S(242)
    ), S(554), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(710), S(590),
        1, 60, S(499), 3, autoPlace.SetCheckInterval, S(751))
    persistentToggle(
        farmTab:AddCard(S(454),
            S(794)),
        S(461), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(799),
            S(665)),
        S(419), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(389), S(526),
        0, 500, S(122), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(404))
    persistentToggle(farmTab:AddCard(
        S(780),
        S(426)
    ), S(508), autoChest.SetEnabled)
    persistentSlider(farmTab, S(623), S(327),
        50, 3000, S(178), 100, autoChest.SetMinItems, S(95))
    persistentSlider(farmTab, S(513), S(48),
        1, 60, S(621), 3, autoChest.SetCheckInterval, S(699))
    persistentToggle(farmTab:AddCard(
        S(642),
        S(401)
    ), S(27), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(130),
            S(698)),
        S(593), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(685), S(314),
        5, 300, S(123), 20, autoSell.SetCheckInterval, S(676))
    farmTab:AddSlider(S(741),
        S(484),
        5, 120, sellMaxWait, function(value)
            config.Set(S(548), value)
        end, S(8))
    persistentToggle(farmTab:AddCard(
        S(744),
        S(115)
    ), S(177), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(713), S(603),
        5, 300, S(601), 20, autoUpgrade.SetInterval, S(14))
    persistentSlider(farmTab, S(583), S(459),
        1, 20, S(196), 5, autoUpgrade.SetMaxPerPass, S(819))
    persistentSegmented(
        farmTab,
        { S(297), S(291), S(216), S(146) },
        { S(77), S(743), S(625), S(263) },
        S(230), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(622), S(725),
        0, 500, S(318), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(31))
    persistentToggle(
        farmTab:AddCard(S(778),
            S(251)),
        S(415), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(528) .. pickaxe.multiplier .. S(788)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(503)
            end
            return S(365) .. index .. S(110) .. (pickaxeLabels[index] or S(505))
        end
        local savedPick = config.Get(S(653), 0)
        pickaxeCard = farmTab:AddSlider(
            S(182),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(491), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(37)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(489),
            S(519)),
        S(444), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(693),
            S(757)),
        S(257), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(516),
            S(761)),
        S(184), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(493), S(282),
        1, 20, S(324), 2, autoUpgrade.SetMutationTarget, S(44))
    persistentSlider(farmTab, S(283), S(215),
        0, 500, S(738), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(346))
    persistentToggle(farmTab:AddCard(
        S(278),
        S(192)
    ), S(101), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(393), S(479),
        10, 600, S(377), 30, autoResearch.SetCheckInterval, S(732))
    persistentSlider(farmTab, S(284), S(772),
        1, 20, S(727), 5, autoResearch.SetMaxPerPass, S(359))
    persistentSlider(farmTab, S(287), S(53),
        0, 500, S(753), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(485))
    persistentToggle(
        farmTab:AddCard(S(673),
            S(773)),
        S(457), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(97),
            S(153)),
        S(144), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(208),
            S(121)),
        S(319), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(222), S(305) },
        { true, false },
        S(329), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(759),
        S(293)
    ), S(447), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(509), S(602),
        5, 300, S(596), 30, autoClaim.SetCheckInterval, S(640))
    persistentSlider(farmTab, S(164), S(740),
        1, 30, S(348), 10, autoClaim.SetMaxPerPass, S(775))
    persistentToggle(farmTab:AddCard(
        S(518),
        S(145)
    ), S(587), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(438), S(125),
        5, 300, S(704), 30, autoPlot.SetCheckInterval, S(630))
    persistentToggle(
        farmTab:AddCard(S(296),
            S(480)),
        S(498), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(674),
            S(515)),
        S(28), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(341), S(566),
        0, 500, S(201), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(610))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(616))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(351), S(275))
    persistentToggle(
        settingsTab:AddCard(S(33), S(342)),
        S(290),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(556), 100)
    settingsTab:AddSlider(S(776), S(541), 50, 150, savedScale, function(value)
        config.Set(S(695), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(261), 1)
    settingsTab:AddSegmented({ S(592), S(496), S(382) }, savedLanguage, function(index, value)
        local code = ({ S(23), S(558), S(579) })[index]
        config.Set(S(134), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(207), S(344), S(302) })[savedLanguage] or S(156))
    log(S(565) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(226)] = function()
return {
    [79389059854988] = S(29),
}
end
import(S(428))(import)
