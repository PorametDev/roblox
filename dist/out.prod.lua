-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"search_placeholder", "costs ", "Search functions...", "Auto Chest", "[GameAPI] shapes: no mine data yet", "Auto Research: กันเงินไว้", "UserInputService", "Auto Mine: คลิกต่อรอบ", "My Anime Mine · Mining Hub", "TextLabel", "table", "/", "GetCharacterMineSpeedUpgradeCost", "🏠", "prompt disabled", ",", " wouldn't take ", "TextLabel", " ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "no isfile/readfile available to verify the write", "Humanoid", "OwnerUserId", "ReplicatedStorage", "core/logger", "autoResearch", "CoreUI", "badge says ", "[GameAPI] shapes: no Character entries in the inventory", "Mutation: เพดานราคาต่อครั้ง", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", ")", "[AutoClaim] ", "Mutation: หยุดเมื่อได้ x", " - ", "[AutoPlot] ", " -> ", "", " -> ", ", built ", "dearest", "s", "Auto Plot: รอบเช็ค", "[AutoPlace] upgrading ", "Content", ")", "Auto Research: รอบเช็ค", "nothing to place or upgrade", "table", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "en", " (x", "Players", " still locked after paying ", " owned)", "autoChest", "HttpService", "[AutoChest] chest is empty", "table", "core/teleport", "", "{", "MineSpeed", "M", "GetCharacterUpgradeCost", "List", " GetHolderLevel=", "Badge", "Zones", "TextLabel", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "Frame", "([%d,%.]+)%s*(%a*)", "AutoUpgrade", "enabled", "HotbarFrame", "core/anti_afk", "CoreGui", "unreadable", " -> ", "AutoPlot", "%.0f", "M", "Players", "TextButton", "en", "string", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "AutoResearch", "th", "Backpack", "autoMineClicks", " possible holder(s) - not buying more", "table", "autoUpgradeMaxPerPass", "spendOrder", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "core/click", "games/my_anime_mine/auto_sell", "core/get_leaderstat", "Auto Plot: ใช้โซนล่าสุดเสมอ", "TextButton", "[AutoChest] enabled (collects once >= ", "SellStand", "none", " not bought yet (", "games/my_anime_mine/game_api", " while selecting", "[", " -> ", " (", ")", "Players", "PickaxesStand", " ", "ไล่ซื้อทุกกี่วินาที", "AutoMine", "[AutoChest] can't read the chest yet", "[AutoResearch] Core ", "AutoPlace", "PlotIndex", "s", "Auto Mine", "Damage", "open_menu_hint", ",", "^([%d,%.]+)%s*(%a*)$", "/", "unlock", "games/my_anime_mine/auto_plot", "disabled", "[BestMiner] slot ", "research", "autoResearchCore", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "CharMineSpeedMaxLevel", "M", "Modules", "core/run_job", "TextButton", "[AutoUpgrade] every stat on every miner is maxed", "games/my_anime_mine/game_api", "M", ", you have ", "table", "Players", "[AutoUpgrade] MineBuyPickaxe ", "th", "autoPlotKeepMoney", ")", "nothing left to buy (", " (power ", " still empty after placing ", "autoSellInterval", "function", "Loot", "[AutoClaim] ", "waiting", "autoUpgradeRollMutation", "core/spend_policy", "core/logger", "Header", "TextLabel", "MineBuySkill", " thing available first", "autoClaimInterval", "[Uu]nlock", "autoResearchInterval", "TextButton", ") ", "อังกฤษ", "autoUpgradePriority", "games/my_anime_mine/auto_place", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "core/logger", "CharMaxLevel", "dearest", "core/spend_policy", " ชิ้น", "Auto Sell: รอบขาย", "0 = ไม่จำกัด", "AutoPlot", "", "neither appendfile nor writefile is available", " -> x", "%.3g", "disabled", "[AutoPlace] ", "s", "Auto Claim (Index)", "MekPro", "auto", "[AutoMine] ", " for ", "TextLabel", " pickaxe ", "[AutoClaim] idle: ", "d9948b6+", "MineSellItems", " items (now ", "MineSpeed", "table", "Auto Research (Plot)", "AutoClaim", "⚙", "core/run_job", "Mining ก่อน", "core/build_env", "=", ": ", "disabled", "Locked miner slot", "", "string", "%.3g", "Auto Sell", " has been busy for ", "')", "enabled", "off", "write reported success but the line isn't in ", "ItemName", "This is not a supported place.", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "enabled", "", "no mine data", "ClaimableTiers failed", "core/get_place_id", "AutoResearch", "table", "TextButton", "ค้นหาฟังก์ชัน...", "Holders", " (x", ": ", "[AutoPlace] unlocking ", "en-us", "cost", "", ": badge says ", "dearest", " ", " (still ", "Container", "unreadable", "ขนาดเมนู", ") ", "off", "x", "ZoneBoardGui", "string", "autoPlaceEquipBest", "[AutoSell] enabled", "HumanoidRootPart", "", " for ", " (prompt says '", "core/spend_policy", "core/parse_money", "VirtualInputManager", "2026-09-21 13:11:57", " (cost ", "ClickDetector", "AutoSell", "OwnerUserId", "[AutoPlot] unlocked ", "AutoPlace", "/s", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " ", "[GameAPI] holder ", "HumanoidRootPart", "GuiService", "autoResearchMaxPerPass", ") from ", "games/registry", "[AutoPlace] ", "[AutoPlace] placed ", "Text", " items", "core/ui", "s", " costs ", "search_placeholder", "[AutoResearch] ", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "TextBox", "Auto Upgrade Unit", "Character", "AutoPlot", "[AntiAFK] idle detected, simulating input", "[GameAPI] SendEvent failed: ", "empty", "/", "[AutoChest] disabled", " costs ", "Frame", "[SpendPolicy] ", "TextLabel", "autoChestMinItems", "Chest", "table", "Core buying is off", "TextButton", ")", "core/debug_tab", "[GameAPI] shapes:   GetHolderVariant=", "TextLabel", "core/scheduler", "MineWorld", " x", "TextLabel", "%.3g", "AutoPlace", "core/parse_money", "AutoUpgrade", "autoUpgradeMutationBudget", "fill", "[AutoPlace] couldn't pick up ", "ขายทุกกี่วินาที", "Speed", ")", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "Workspace", "ScreenGui", "[Logger] failed to write ", "TextLabel", "Mining", "", "Frame", "Damage", "(nothing to do)", "core/logger", "AutoPlace", "[AutoPlace] nothing in the hotbar to place", "write reported success but ", "autoPlaceUnlock", " (", "MineTakeChest", " waited ", " research is ", " (", "core/scheduler", "core/logger", "core/logger", "autoClaim", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "Frame", "", " power ", "^x(%d+)$", ",", ": State(", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "ลำดับการใช้เงินรวมทุกระบบ", " ", " from ", "Root", "table", "autoClaimMaxPerPass", "slot ", "AutoPlace", "Auto Upgrade: สุ่ม Mutation", "function", "core/logger", "games/my_anime_mine/auto_plot", "[Scheduler] ", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/auto_claim", "GetHolderMineSpeedLevel", "Frame", "autoUpgradeKeepMoney", "core/i18n", "auto", "Players", "games/my_anime_mine/get_plot_parts", "[AutoUpgrade] ", " ", "[AutoUpgrade] bought pickaxe #", "languageIndex", "core/get_place_id", "games/my_anime_mine/game_api", "Hauler", "Auto Place Miner", "[AutoPlace] ", "MineGui", " node(s) looked buyable but the server refused", "Main", "AutoResearch", "Auto Chest: เกณฑ์เก็บ", "autoResearchKeepMoney", "UIListLayout", "Frame", "^M(%d+)$", "Plots", "antiAfk", "games/my_anime_mine/main", "UIListLayout", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "table", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", " (", "[AutoPlot] ", "ตั้งค่า", "ChestCapacity", "core/parse_money", "autoPlaceUpgradeWeak", "games/my_anime_mine/get_plot_parts", "Auto Claim: เก็บสูงสุดต่อรอบ", "s", "slot not in the hotbar", "[SpendPolicy] ", ")", "GetCharacterSpeedUpgradeCost", "games/registry", "autoPlaceEquipBestInterval", "core/config", "core/humanoid", "TextLabel", "'", "autoPlotLatest", "UIPadding", ")", "/", "core/spend_policy", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", " mutation x", "[AutoUpgrade] ", "s", "[AutoResearch] idle: ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Zone buying is off", "fill", "Damage ก่อน", "Frame", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "lowest", "games/my_anime_mine/get_plot_parts", "", "Auto Upgrade: กันเงินไว้", "AutoClaim", "[AutoResearch] ", "Auto Place: รอบเช็ค", "Core", "UIStroke", "core/logger", "AutoPlot", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " (", "Workspace", "/", "[AutoUpgrade] ", " team power", "ไล่อัปเกรด miner ทุกกี่วินาที", "core/dispatch", " ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "ต่างคนต่างซื้อ", "s - jumping the queue", "AutoPlace", "", "Auto Place: ปลดล็อกช่อง miner", "BasePart", "works from anywhere - no teleport needed", "[AutoPlace] Equip Best: hotbar's strongest ", "games/my_anime_mine/get_current_plot", "[AutoResearch] cheapest open node: ", "[Bb]uy", " ", "s", "games/my_anime_mine/auto_mine", "none", "games/my_anime_mine/auto_claim", "^%$", ")", "[AutoChest] waiting: ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "", ": ", ")", " fields: ", "core/logger", "autoPlot", "Damage", "cost", "", "dearest", " ", "MineEquipSlot", "TextLabel", "AutoChest", "Players", " (money ", "[Config] couldn't read ", "[GameAPI] shapes: Hotbar[", "core/run_job", " doesn't exist", "AutoUpgrade", "RunService", "All", "MekPro.log", "ValueLabel", "core/logger", "%$", "nothing claimable", "Limited", "", " GetHolderDamage=", "[AutoSell] MineSellItems ", "works from anywhere - dropping the teleport", "games/my_anime_mine/get_current_plot", "x", "games/my_anime_mine/auto_place", "Auto Sell: รอคิวได้นานสุด", "locked slot", "Frame", "Hauler", "[GameAPI] couldn't load the game's modules: ", "core/i18n", "[AutoResearch] ", "ถูกสุดก่อน", " (", "needs us at the stand - keeping the teleport", "", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "TextLabel", "core/anti_afk", "Content", "server didn't equip it", "%$", "MekPro", "off", "[AutoPlace] picked up ", "Badge", "x", "[AutoPlot] ", "TextButton", "M", "[AutoPlace] ", "core/run_job", " team power, you have ", "MineSpeed", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "%$%s*([%d,%.]+)%s*(%a*)", "every open node is maxed", "แพงสุดก่อน", "core/logger", "UICorner", " ParseCharStack=", "dearest", "core/config", "Auto Research: ซื้อสาย Core", "games/my_anime_mine/auto_upgrade", "ต่ำสุดก่อน", "ย่อขยายเมนูทั้งชุด", ") -> ", "autoUpgradePickaxeIndex", "games/my_anime_mine/auto_upgrade", "Auto Place: เพดานราคาปลดช่อง", "AutoChest", "core/spend_policy", "Empty miner slot", "th", "th", "", "Players", "table", "", "languageIndex", ")", "/config.json", "Players", "unlock", "", "table", ")", "BasePart", "Players", "games/my_anime_mine/auto_research", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "games/my_anime_mine/get_current_plot", " error: ", "core/spend_policy", "GetHolderLevel", "/", "Auto Upgrade: Pickaxe ที่จะซื้อ", "[AutoUpgrade] ", "menuScale", " ", " reward(s) looked claimable but none went through", "BodyVelocity", "number", "[AntiAFK] disabled", "[MekPro] settings file: ", "x", "(", "table", "[AutoPlace] holders: ", "\n", ") on ", "/", "[AutoPlace] unlocked ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "All", "Search", "auto", " (", "TabBar", "cheapest", " ", "core/dispatch", "[AutoPlace] disabled", "[AutoChest] MineTakeChest had no effect (", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "GetHolderSpeedLevel", "[AutoPlace] fireproximityprompt error: ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "", ") ", "ไทย", "games/my_anime_mine/game_api", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "autoUpgradeMutationTarget", "RunService", "core/build_env", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "[GameAPI] shapes: Hotbar is empty", "disabled", "[AntiAFK] enabled - watching Player.Idled", "Auto Research: ซื้อสูงสุดต่อรอบ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Open menu", " items)", "autoUpgrade", "upgrade", "games/my_anime_mine/get_best_miner", "[AutoResearch] nothing open and affordable right now", "[AutoPlace] couldn't hold anything - not pressing E", "MineRollMutation", "table", "AutoPlot", "[Config] couldn't write ", "ValueLabel", "ScrollingFrame", "[AutoUpgrade] pickaxe #", " #", "table", "table", "action '", "needs ", "sale didn't go through", "[AutoResearch] ", "[MekPro] build ", "autoUpgradePickaxe", "BasePart", ") - leaving it alone for ", "core/run_job", "maxed", "Auto Mine: ความเร็ว", "Auto Chest: รอบเช็ค", "] job failed: ", "[%X] ", "s behind a purchase that never happened - letting it spend", "MinePower", "[AutoChest] collected ", "games/my_anime_mine/auto_chest", "Auto Research: ซื้อสาย Zone", "buying the ", "core/run_job", " (", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "Character", "[Scheduler] ", "Humanoid", "=empty", "autoSellMaxWait", "Frame", "[AutoPlace] drifted out of range of ", "", ", ", "empty", " -> ", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ", base ", "Shop Stand", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "Workspace", "MineBuyPickaxe", ": ", "core/ui", "MineUpgradeCharacter", "[GameAPI] shapes: Holders.", "games/my_anime_mine/auto_mine", "MineEquipPickaxe", "Nodes", "UserInputService", " refused - ", "autoSellMaxWait", " | ", "UIListLayout", "core/i18n", "Auto Upgrade: รอบเช็ค", ") ", "disabled", "Players", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "s", "Auto Place: รอบดึงตัวเก่ง", "core/logger", "X", " GetVariantMult=", "TextLabel", "Frame", "ItemFrame", "อ่านป้ายบน chest ทุกกี่วินาที", "core/logger", "Workspace", "sold ", " didn't go through (out of stock or too expensive)", "en", "core/run_job", "autoSellProtectUnits", "string", ")", "ZoneRow", "table", "Crew", "", "[AutoSell] ", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "Auto Place: เปลี่ยนตัวอ่อนออก", "may need us at the stand - trying that", " ", "autoResearchZones", "autoResearchCheapestFirst", "}", "core/teleport", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "autoChestInterval", "%.3g", "autoMine", "AutoPlace", "games/my_anime_mine/game_api", " (", " ", "[AutoPlace] slot ", "Speed", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "Content", "games/my_anime_mine/auto_research", "prod", "Anti-AFK", "research", "0 = ไม่จำกัด", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "cheapest open node ", "Auto Upgrade: แจก Pickaxe ให้ miner", "Auto Plot: ซื้อโซนถัดไป", "auto", ") needs ", "UIScale", "Plots", ": ", "=", ", have ", "enabled", "[AutoSell] disabled", " -> ", "%", "VirtualUser", "games/my_anime_mine/auto_chest", "แพงสุดก่อน", "Exclusive", "core/get_place_id", "\n", "autoUpgradePickaxeIndex", "s - carrying on without it", "s", "autoPlotBuy", "^Swap out ", "BasePart", "Pad", "dearest", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "both Zone and Core buying are switched off", "[Scheduler] turn: ", "ZoneBoard", "UICorner", " (chest holds ", "%.3g", ") didn't take: ", " is no better after the swap (", "core/logger", "autoUpgradeLimitPickaxes", " (", "games/my_anime_mine/game_api", "games/my_anime_mine/game_api", ") gave nothing", "", "%.0f", "enabled", "games/my_anime_mine/auto_sell", " stack(s)", "cheapest", "=locked(", "CharSpeedMaxLevel", "autoResearchCoreLast", "core/teleport", "ถูกสุดก่อน", "autoMineSpeed", "MineWorld", "%.3g", "off - every automation spends on its own", "[AutoPlot] now mining ", "TextLabel", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "ปุ่มเปิดเมนู", " claimable but none could be resolved", "[AutoPlot] couldn't switch to ", "Auto Claim: รอบเช็ค", "%.0f", "Auto Plot: กันเงินไว้", "=", "core/get_me", "games/my_anime_mine/game_api", "", "core/teleport", "", "Auto Upgrade Plot (Zone)", "Speed ก่อน", " ", "-", "[AutoClaim] claimed ", "Workspace", "AutoUpgrade", " (power ", "lowest", "off", " waited ", "AutoResearch", "autoUpgradeEquipPickaxe", "core/logger", "autoSell", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "modules not loaded", ")", "games/my_anime_mine/game_api", "waiting", "MineSelectZone", "HumanoidRootPart", "autoPlaceInterval", "#", "string", "core/run_job", "Text", "AutoUpgrade", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "ProximityPrompt", "TextLabel", "leaderstats", "autoUpgradeInterval", "slot past the usable cap", "BodyPosition", "] = ", "autoPlotInterval", " pickaxe(s) for ", "core/logger", "MineEquipBest", "s", "ยิง Equip Best ทุกกี่วินาที", "", "autoPlace", "ProximityPrompt", " - upgrade the miners first", "autoPlaceUnlockBudget", "affordable - waiting for a turn", "open_menu_hint", "TextLabel", "button has no size on screen", " but no unclaimed reward in its Track", "เช็คโซนทุกกี่วินาที", "Frame", "?", "ฟาร์ม", "TextButton", "[AutoUpgrade] ", "; ", "[GameAPI] shapes: inventory entry ", "ตามเครื่อง", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "[AutoUpgrade] ", "core/logger", " -> ", "games/my_anime_mine/main", " (", "PlayerGui", "menuScale", ")", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "[Scheduler] ", "AutoSell", "table", "index rewards are disabled in this game build", "x", "games/my_anime_mine/game_api", "%.3g", "", "needs "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(77)] = function()
return function(import)
local log = import(S(872))
local Players = game:GetService(S(710))
local VirtualUser = game:GetService(S(775))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(633))
        connection = player.Idled:Connect(function()
            log(S(293))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(597))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(629)] = function()
return { Env = S(756), IsDev = false, Commit = S(198), BuiltAt = S(263) }
end
modules[S(98)] = function()
local VirtualInputManager = game:GetService(S(262))
local GuiService = game:GetService(S(275))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(884)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(555)] = function()
return function(import)
local HttpService = game:GetService(S(57))
local getPlaceId = import(S(385))
local log = import(S(161))
local ROOT_FOLDER = S(191)
local placeFolder = ROOT_FOLDER .. S(428) .. tostring(getPlaceId())
local filePath = placeFolder .. S(575)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(202) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(498) .. filePath .. S(483) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(646) .. filePath .. S(210) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(615)] = function()
return function(import)
    local getPlaceId = import(S(230))
    local registry = import(S(278))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(223))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(100)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(865))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(829)] = function()
local Players = game:GetService(S(146))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(678))
    local backpack = player:WaitForChild(S(91))
    return player, humanoid, backpack
end
return getMe
end
modules[S(779)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(422)] = function()
local Players = game:GetService(S(570))
local RunService = game:GetService(S(628))
local UserInputService = game:GetService(S(7))
local Workspace = game:GetService(S(722))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(22))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(274))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(595))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(786)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(581)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(868))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(706)] = function()
local Players = game:GetService(S(379))
local STRINGS = {
    en = {
        search_placeholder = S(3),
        open_menu_hint = S(636),
    },
    th = {
        search_placeholder = S(234),
        open_menu_hint = S(822),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(239)
    if locale:sub(1, 2):lower() == S(568) then
        return S(567)
    end
    return S(86)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(378) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(714)] = function()
local LOG_FILE = S(505)
local function log(message)
    local line = os.date(S(666)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(482))
            end
            appendfile(LOG_FILE, line .. S(603))
        elseif writefile then
            local existing = S(257)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(780))
        else
            error(S(184))
        end
        if not (isfile and readfile) then
            error(S(21))
        end
        if not isfile(LOG_FILE) then
            error(S(340) .. LOG_FILE .. S(501))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(221) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(330) .. LOG_FILE .. S(237) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(261)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(858) then
        return nil
    end
    local number, suffix = text:match(S(548))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(126), S(213))))
    if not value then
        return nil
    end
    if suffix ~= S(353) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(544)] = function()
return function(import)
local log = import(S(847))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(109) .. name .. S(665) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(311)] = function()
return function(import)
local RunService = game:GetService(S(503))
local log = import(S(486))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(371) .. task_.name .. S(586) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(677) .. task_.name .. S(217)
                    .. string.format(S(82), now - task_.busySince)
                    .. S(782))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(905) .. starved.name .. S(344)
            .. string.format(S(826), now - starved.waitingSince)
            .. S(463))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(791) .. (currentTask and currentTask.name or S(336)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(429)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(25))
local policy = {}
local probes = {}
local order = {}
local mode = S(536)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(300) .. (mode == S(843)
        and S(818)
        or (S(672) .. mode .. S(165))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(250) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(809) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(554) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(416) .. name .. S(844)
            .. string.format(S(805), now - blockedSince[name])
            .. S(667))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(832)] = function()
local Players = game:GetService(S(113))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(855))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(695)] = function()
return function(import)
local i18n = import(S(377))
local UserInputService = game:GetService(S(701))
local THEME = {
    BrandName = S(535),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(793))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(552))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(426))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target)
    local dragging, dragInput, dragStart, startPos
    local state = { moved = false }
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            state.moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                state.moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections, state
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(233))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(465)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(449))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(331))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(888)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(314))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(38)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(648))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(396))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(530))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(887))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(864))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(301))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(820))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(506))
            if not valueLbl then
                valueLbl = Instance.new(S(423))
                valueLbl.Name = S(647)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(68))
            if not badge then
                badge = Instance.new(S(195))
                badge.Name = S(538)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(890))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(375))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(334))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(140))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(528)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(774)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(352))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(397))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(85))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(804)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(718))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(705))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(541))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(860)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(329))
    screenGui.Name = S(27)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(78))
    local main = Instance.new(S(299))
    main.Name = S(392)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(766))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(439))
    tabBar.Name = S(612)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(402))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(681))
    header.Name = S(162)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(102))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(70))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(163))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(333)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(306))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(715)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(169))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(837)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(289))
    searchBox.Name = S(609)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(286))
    searchBox.Text = S(490)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(520))
    content.Name = S(45)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(72))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(10))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(125))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(310))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    local avatarConnections, avatarDrag = makeDraggable(avatar, main)
    for _, connection in ipairs(avatarConnections) do
        table.insert(connections, connection)
    end
    table.insert(connections, avatar.Activated:Connect(function()
        if minimized and not avatarDrag.moved then
            setMinimized(false)
        end
    end))
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(281)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(831) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(750) .. (item.Subtitle or S(733))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(1))
        footerLabel.Text = i18n.T(S(882))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(776)] = function()
return function(import)
local gameApi = import(S(625))(import)
local log = import(S(175))
local runGuarded = import(S(500))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(103) .. minItemsToCollect .. S(637))
    else
        log(S(297))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(247) then
            log(S(118))
            lastReported = S(79)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(295) then
            log(S(58))
            lastReported = S(685)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(853) then
            local capacity = gameApi.ChestCapacity()
            log(S(480) .. count .. S(605) .. want .. S(282)
                .. (capacity and (S(794) .. capacity .. S(150)) or S(511)))
            lastReported = S(158)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(564), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(669) .. count .. S(200) .. after .. S(307))
        else
            log(S(617) .. count
                .. S(80) .. tostring(after) .. S(46))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(373)] = function()
return function(import)
local gameApi = import(S(802))(import)
local log = import(S(369))
local runGuarded = import(S(661))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(157) .. (enabled and S(806) or S(211)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(197) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(838) .. reward.category .. S(614)
                .. tostring(reward.tierKey) .. S(650) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(33) .. #pending .. S(594))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(445), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(698)] = function()
return function(import)
local getCurrentPlot = import(S(515))
local getPlotParts = import(S(380))
local log = import(S(798))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(265), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(193) .. (enabled and S(226) or S(632)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(517)] = function()
return function(import)
local Players = game:GetService(S(84))
local getCurrentPlot = import(S(470))
local getPlotParts = import(S(412))
local getMinerSlots = import(S(372))(import)
local teleport = import(S(60))
local parseMoney = import(S(410))
local gameApi = import(S(830))(import)
local log = import(S(507))
local runGuarded = import(S(859))(import)
local spendPolicy = import(S(260))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(566)
local LOCKED_SLOT_TEXT = S(212)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(787))
    return pad and pad:FindFirstChildWhichIsA(S(878), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(883)) then
            local digits, suffix = descendant.Text:match(S(127))
            if digits then
                local value = tonumber((digits:gsub(S(16), S(572))))
                if value then
                    if suffix ~= S(876) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(183)
    return (text:gsub(S(785), S(443)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(494)) and descendant.Text:match(S(478)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(365) .. holder.Name .. S(106)
            .. gameApi.MinerSlots() .. S(128) .. gameApi.MaxMinerSlots() .. S(55)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(519)
    end
    if not prompt.Enabled then
        return true, S(15)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(2) .. price
    end
    local action = prompt.ActionText or S(61)
    if action:match(S(167)) or action:match(S(472)) or action:match(S(534)) then
        return true, S(653) .. action .. S(424)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(256))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(620) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(810) .. why .. S(324))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(679))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(209) .. prompt.ObjectText
                    .. S(600) .. shown .. S(689) .. math.floor(base) .. S(851))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(704))
    if line ~= lastSurvey then
        log(S(602) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(320), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(639), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(338)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(577), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(389) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(616))
    else
        lastAttempt = 0
        log(S(224))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(50),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(415)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(867)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(533)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(339))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(682) .. holder.Name .. S(108))
            return false
        end
        log(S(751) .. candidate.slot .. S(674) .. candidate.name
            .. S(796) .. tostring(why))
    end
    if not miner then
        log(S(642))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(279) .. holder.Name .. S(153) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(280) .. miner.name .. S(841) .. miner.power
        .. S(604) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(321) .. entry.name .. S(361) .. entry.holder.Name
            .. S(259) .. tostring(entry.prompt.ActionText) .. S(218))
        return false
    end
    log(S(537) .. entry.name .. S(152) .. entry.power
        .. S(277) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(543) .. pad.holder.Name .. S(54)
                .. tostring(pad.priceText))
        else
            log(S(606) .. pad.holder.Name
                .. S(258) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(188) .. holderName .. S(797)
            .. string.format(S(795), before) .. S(686) .. string.format(S(315), after)
            .. S(660) .. SWAP_COOLDOWN .. S(783))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(469)
            .. string.format(S(215), bestBefore) .. S(773)
            .. string.format(S(745), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(269), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(48))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(437) then
        runGuarded(S(747), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(129) then
        log(S(238) .. target.holder.Name
            .. S(194) .. tostring(target.priceText))
        runGuarded(S(120), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(44) .. target.holder.Name .. S(694) .. target.name
        .. S(900) .. target.power .. S(560) .. best.name .. S(342) .. best.power .. S(427))
    runGuarded(S(366), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(130)] = function()
return function(import)
local gameApi = import(S(107))(import)
local parseMoney = import(S(317))
local log = import(S(551))
local runGuarded = import(S(206))(import)
local spendPolicy = import(S(160))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(407) .. (enabled and S(75) or S(131)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(508)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(819) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(611) .. deepestKey .. S(580))
        return true
    end
    log(S(824) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(292)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(540) .. gameApi.ZoneDisplayName(zoneKey) .. S(285)
                .. math.floor(price) .. S(144) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(268) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(111) .. zoneKey .. S(903))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(758) then
            reportedLocked = S(133)
            log(S(36) .. zoneKey .. S(702)
                .. gameApi.ZoneDisplayName(currentKey) .. S(345)
                .. done .. S(455) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(645)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(81), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(583)] = function()
return function(import)
local gameApi = import(S(748))(import)
local log = import(S(450))
local runGuarded = import(S(139))(import)
local spendPolicy = import(S(565))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(287) .. (enabled and S(219) or S(709)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(656) .. node.zoneKey .. S(115) .. node.id
                    .. S(346) .. node.family .. S(170) .. node.level .. S(37) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(119) .. node.id
                    .. S(800) .. node.family .. S(249) .. node.level .. S(110) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(524) .. blocked
                .. S(391))
        elseif powerBlocked then
            log(S(446) .. powerBlocked.zoneKey .. S(460) .. powerBlocked.id
                .. S(406) .. powerBlocked.family .. S(765)
                .. math.floor(powerBlocked.powerReq) .. S(545)
                .. math.floor(powerBlocked.power)
                .. S(879))
        else
            log(S(641)
                .. S(497) .. math.floor(gameApi.GetMoney()) .. S(479))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(471) .. nearest.zoneKey
                    .. S(836) .. nearest.id .. S(749) .. nearest.family .. S(623)
                    .. nearest.level .. S(12) .. nearest.max
                    .. S(298) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(790)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(448),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(436))
        end
        if not buyCore then
            table.insert(parts, S(305))
        end
        if #parts == 0 then
            return S(549)
        end
        return S(151) .. table.concat(parts, S(684)) .. S(484)
    end
    local why
    if nearest.cost > budget then
        why = S(654) .. string.format(S(186), nearest.cost)
            .. S(770) .. string.format(S(911), money)
    elseif nearest.powerOk == false then
        why = S(913) .. string.format(S(817), nearest.powerReq) .. S(457)
    else
        why = S(881)
    end
    return S(761) .. nearest.zoneKey .. S(382) .. tostring(nearest.id)
        .. S(453) .. tostring(nearest.family) .. S(708) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(434) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(41) and cost > best)
            or (mode ~= S(243) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(845)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(231), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(99)] = function()
return function(import)
local Workspace = game:GetService(S(839))
local gameApi = import(S(386))(import)
local teleport = import(S(813))
local log = import(S(337))
local runGuarded = import(S(726))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(772))
    else
        lastAttempt = 0
        log(S(255))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(104))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(863), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(513)
                .. (sold and S(514)
                         or S(527)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(734) .. (sold and (S(723) .. #keys .. S(808)) or S(655)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(266), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(562)] = function()
return function(import)
local Workspace = game:GetService(S(454))
local gameApi = import(S(142))(import)
local teleport = import(S(742))
local log = import(S(349))
local runGuarded = import(S(673))(import)
local spendPolicy = import(S(178))(import)
local STATS = { S(335), S(323), S(201) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(441)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(381) .. (enabled and S(771) or S(187)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(94) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(489) or S(662)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(896) .. holderKey .. S(360) .. choice.stat .. S(272)
                        .. choice.info.level .. S(898) .. after.level
                        .. S(589) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(240) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(114))
    local shop = stand and stand:FindFirstChild(S(690))
    if not shop then
        return nil
    end
    if shop:IsA(S(659)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(467), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(147)
                .. (bought and S(468)
                           or S(737)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(325))
        end
    end
    if bought then
        log(S(383) .. pickaxe.index .. S(19) .. pickaxe.name
            .. S(52) .. pickaxe.multiplier .. S(32))
        return true
    end
    log(S(649) .. pickaxe.index .. S(738) .. pickaxe.name
        .. S(724))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(891) .. holderKey .. S(196)
                    .. tostring(worn or S(476)) .. S(39) .. wanted.name)
            else
                log(S(432) .. holderKey .. S(17)
                    .. wanted.name .. S(245) .. tostring(nowWorn or S(105)) .. S(417))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(456) .. holderKey .. S(431) .. variant
                .. S(185) .. after .. S(264) .. cost .. S(574))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(840))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(591) .. gameApi.OwnedPickaxeCount()
                    .. S(871) .. gameApi.MaxMinerSlots()
                    .. S(93))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(821))
        elseif maxed > 0 then
            log(S(141))
        else
            log(S(20))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(491) and price > best)
            or (mode ~= S(788) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(318))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(74), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(801)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(24))
local Workspace = game:GetService(S(328))
local log = import(S(897))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(138), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(522) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(59))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(907) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(387) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(651) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(304) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(521) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(398))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(294) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(343))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(608), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(145) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(601) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(291) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(199), keys)
end
function api.EquipSlot(slot)
    return send(S(493), slot == S(326) and S(241) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(693), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(699), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(643), holderKey)
end
local describedShapes = false
function api.DescribeShapes()
    if describedShapes or not modules() then
        return
    end
    describedShapes = true
    local mine = api.GetMineData()
    if not mine then
        log(S(5))
        return
    end
    local function describe(value)
        if type(value) ~= S(644) then
            return tostring(value)
        end
        local parts = {}
        for key, item in pairs(value) do
            table.insert(parts, tostring(key) .. S(769) .. tostring(item))
        end
        table.sort(parts)
        return S(62) .. table.concat(parts, S(593)) .. S(741)
    end
    pcall(function()
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(363) and holder.CharName then
                log(S(697) .. holderKey .. S(492) .. describe(holder))
                log(S(309) ..
                    tostring(mineConfig.GetHolderVariant(holder))
                    .. S(716) ..
                    tostring(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
                    .. S(512) .. tostring(mineConfig.GetHolderDamage(holder))
                    .. S(67) .. tostring(mineConfig.GetHolderLevel(holder)))
                break
            end
        end
    end)
    pcall(function()
        local shown = 0
        for slot, key in pairs(mine.Hotbar or {}) do
            log(S(499) .. tostring(slot) .. S(869) .. tostring(key)
                .. S(553) .. tostring(mineConfig.ParseCharStack(key)))
            shown = shown + 1
            if shown >= 3 then
                break
            end
        end
        if shown == 0 then
            log(S(631))
        end
    end)
    pcall(function()
        local entries = mineInventory.Collect(mine, S(504), mineInventory.KindOrder.OreFirst)
        local shown = 0
        for _, entry in pairs(entries or {}) do
            if entry.Kind == S(676) then
                log(S(893) .. describe(entry))
                shown = shown + 1
                if shown >= 2 then
                    break
                end
            end
        end
        if shown == 0 then
            log(S(29))
        end
    end)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(873))
end
function api.BuySkill(zoneKey, family)
    return send(S(164), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(731)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(53)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(668))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(368) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(404) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(854), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(312))
    plots = plots and plots:FindFirstChild(S(399))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(496)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(267)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(792))
    local gui = board and board:FindFirstChild(S(252))
    local root = gui and gui:FindFirstChild(S(362))
    local list = root and root:FindFirstChild(S(66))
    local row = list and list:FindFirstChild(S(730) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(717)) and descendant.Text ~= S(622) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(155) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(696), characterKey, stat == S(332) and S(63) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(588), max = S(176), cost = S(65) },
    Speed = { level = S(619), max = S(811), cost = S(418) },
    MineSpeed = { level = S(374), max = S(136), cost = S(13) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(87) or charName == S(569) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(828) .. tostring(value))
        end
        table.sort(fields)
        log(S(273) .. holderKey .. S(485) .. table.concat(fields, S(473)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(571) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(596) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(214) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(11) and type(holder.CharName) == S(728) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(510), S(778) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(652) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(850)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(228)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(908)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(49) then
        return {}, S(229)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(509)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(232) or type(state.Track) ~= S(579) then
                table.insert(notes, tabKey .. S(357) .. tostring(category) .. S(803))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(242) .. waiting
                        .. S(885))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(28) .. total .. S(823)
            .. (#notes > 0 and (S(35) .. table.concat(notes, S(892))) or S(578))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(253) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(640)] = function()
return function(import)
local Players = game:GetService(S(582))
local gameApi = import(S(910))(import)
local log = import(S(721))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(73))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(356), S(912))))
    if not value then
        return nil
    end
    if suffix ~= S(227) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(901))
    local mineGui = playerGui and playerGui:FindFirstChild(S(390))
    local hotbar = mineGui and mineGui:FindFirstChild(S(76))
    local content = hotbar and hotbar:FindFirstChild(S(532))
    return content and content:FindFirstChild(S(246))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(18)) then
            local count = descendant.Text:match(S(355))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(719))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(754))
    local nameLabel = content and content:FindFirstChild(S(222))
    local damageLabel = content and content:FindFirstChild(S(488))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(833) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(296) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(132) .. slot .. S(768) .. info.name
                        .. S(354) .. info.power .. S(313) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(585)] = function()
local Players = game:GetService(S(576))
local Workspace = game:GetService(S(692))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(816))
    local plots = mineWorld and mineWorld:FindFirstChild(S(767))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(23)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(442)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(235)),
        Nodes = plot:FindFirstChild(S(700)),
        Chest = plot:FindFirstChild(S(303)),
        Zones = plot:FindFirstChild(S(69)),
        Crew = plot:FindFirstChild(S(732)),
        Loot = plot:FindFirstChild(S(156)),
        PlotIndex = plot:GetAttribute(S(121)),
        ChestCapacity = plot:GetAttribute(S(409)),
    }
end
return getPlotParts
end
modules[S(401)] = function()
return function(import)
    local createWindow = import(S(283))(import)
    local i18n = import(S(523))
    local antiAfk = import(S(531))(import)
    local buildEnv = import(S(208))
    local scheduler = import(S(347))(import)
    local spendPolicy = import(S(587))(import)
    local config = import(S(421))(import)
    local log = import(S(348))
    log(S(657) .. buildEnv.Commit .. S(526) .. buildEnv.Env
        .. S(40) .. buildEnv.BuiltAt .. S(729))
    local window = createWindow({
        Subtitle = S(9),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(475))(import)
    local autoPlace = import(S(173))(import)
    local autoChest = import(S(670))(import)
    local autoSell = import(S(807))(import)
    local autoUpgrade = import(S(557))(import)
    local autoResearch = import(S(755))(import)
    local autoClaim = import(S(477))(import)
    local autoPlot = import(S(370))(import)
    local gameApi = import(S(852))(import)
    scheduler.RegisterStandalone(S(117), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(495), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(316), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(861), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(89), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(204), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(451), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(703), 20)
    scheduler.RegisterExclusive(S(906), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(393), autoResearch.NextPrice)
    spendPolicy.Register(S(502), autoUpgrade.NextPrice)
    spendPolicy.Register(S(182), autoPlot.NextPrice)
    spendPolicy.Register(S(464), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(889), S(14))
    farmTab:AddSectionHeader(S(359))
    persistentSegmented(
        farmTab,
        { S(462), S(525), S(550) },
        { S(220), S(613), S(177) },
        S(96), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(123),
        S(862)
    ), S(746), autoMine.SetEnabled)
    persistentSlider(farmTab, S(663), S(621),
        1, 30, S(815), 5, autoMine.SetCyclesPerSecond, S(270))
    persistentSlider(farmTab, S(8), S(97),
        1, 10, S(92), 1, autoMine.SetClicksPerCycle, S(539))
    persistentToggle(farmTab:AddCard(
        S(388),
        S(88)
    ), S(877), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(447), S(904),
        1, 60, S(856), 3, autoPlace.SetCheckInterval, S(42))
    persistentToggle(
        farmTab:AddCard(S(736),
            S(895)),
        S(411), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(753),
            S(789)),
        S(254), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(713), S(875),
        5, 300, S(420), 60, autoPlace.SetEquipBestInterval, S(414))
    persistentToggle(
        farmTab:AddCard(S(466),
            S(618)),
        S(341), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(563), S(181),
        0, 500, S(880), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(64))
    persistentToggle(farmTab:AddCard(
        S(4),
        S(760)
    ), S(56), autoChest.SetEnabled)
    persistentSlider(farmTab, S(394), S(711),
        50, 3000, S(302), 100, autoChest.SetMinItems, S(179))
    persistentSlider(farmTab, S(664), S(720),
        1, 60, S(744), 3, autoChest.SetCheckInterval, S(433))
    persistentToggle(farmTab:AddCard(
        S(216),
        S(435)
    ), S(848), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(626),
            S(691)),
        S(727), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(180), S(322),
        5, 300, S(154), 20, autoSell.SetCheckInterval, S(874))
    farmTab:AddSlider(S(518),
        S(31),
        5, 120, sellMaxWait, function(value)
            config.Set(S(680), value)
        end, S(712))
    persistentToggle(farmTab:AddCard(
        S(290),
        S(403)
    ), S(638), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(707), S(458),
        5, 300, S(866), 20, autoUpgrade.SetInterval, S(284))
    persistentSlider(farmTab, S(743), S(271),
        1, 20, S(95), 5, autoUpgrade.SetMaxPerPass, S(516))
    persistentSegmented(
        farmTab,
        { S(558), S(438), S(835), S(207) },
        { S(842), S(124), S(752), S(546) },
        S(172), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(444), S(688),
        0, 500, S(376), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(542))
    persistentToggle(
        farmTab:AddCard(S(687),
            S(849)),
        S(658), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(236) .. pickaxe.multiplier .. S(112)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(675)
            end
            return S(857) .. index .. S(244) .. (pickaxeLabels[index] or S(481))
        end
        local savedPick = config.Get(S(781), 0)
        pickaxeCard = farmTab:AddSlider(
            S(590),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(561), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(683)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(735),
            S(174)),
        S(799), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(762),
            S(288)),
        S(846), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(367),
            S(584)),
        S(159), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(34), S(71),
        1, 20, S(627), 2, autoUpgrade.SetMutationTarget, S(909))
    persistentSlider(farmTab, S(30), S(759),
        0, 500, S(319), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(143))
    persistentToggle(farmTab:AddCard(
        S(203),
        S(461)
    ), S(26), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(47), S(116),
        10, 600, S(168), 30, autoResearch.SetCheckInterval, S(122))
    persistentSlider(farmTab, S(634), S(635),
        1, 20, S(276), 5, autoResearch.SetMaxPerPass, S(599))
    persistentSlider(farmTab, S(6), S(358),
        0, 500, S(395), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(83))
    persistentToggle(
        farmTab:AddCard(S(671),
            S(430)),
        S(739), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(556),
            S(529)),
        S(134), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(405),
            S(327)),
        S(812), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(814), S(777) },
        { true, false },
        S(740), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(190),
        S(225)
    ), S(350), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(825), S(547),
        5, 300, S(166), 30, autoClaim.SetCheckInterval, S(189))
    persistentSlider(farmTab, S(413), S(452),
        1, 30, S(364), 10, autoClaim.SetMaxPerPass, S(251))
    persistentToggle(farmTab:AddCard(
        S(834),
        S(630)
    ), S(487), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(43), S(886),
        5, 300, S(870), 30, autoPlot.SetCheckInterval, S(474))
    persistentToggle(
        farmTab:AddCard(S(763),
            S(440)),
        S(784), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(101),
            S(351)),
        S(425), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(827), S(607),
        0, 500, S(149), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(137))
    if buildEnv.IsDev then
        task.delay(8, gameApi.DescribeShapes)
    end
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(308))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(408), S(205))
    persistentToggle(
        settingsTab:AddCard(S(757), S(135)),
        S(400),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(592), 100)
    settingsTab:AddSlider(S(248), S(559), 50, 150, savedScale, function(value)
        config.Set(S(902), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(573), 1)
    settingsTab:AddSegmented({ S(894), S(171), S(624) }, savedLanguage, function(index, value)
        local code = ({ S(764), S(725), S(148) })[index]
        config.Set(S(384), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(192), S(51), S(90) })[savedLanguage] or S(610))
    log(S(598) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(419)] = function()
return {
    [79389059854988] = S(899),
}
end
import(S(459))(import)
