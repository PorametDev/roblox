-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoUpgrade] ", "; ", "core/logger", "[AutoChest] collected ", "TextLabel", "อ่านป้ายบน chest ทุกกี่วินาที", "%.3g", "may need us at the stand - trying that", "AutoPlace", "disabled", ")", "ไทย", "needs ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", " costs ", "[AutoPlace] ", "/", "autoPlaceInterval", "autoUpgradeMaxPerPass", "Auto Plot: กันเงินไว้", "write reported success but the line isn't in ", "", "OwnerUserId", "Auto Sell: รอบขาย", "AutoResearch", "x", "autoResearchCore", ": ", "games/my_anime_mine/auto_chest", "[AutoPlot] now mining ", "Empty miner slot", "ย่อขยายเมนูทั้งชุด", ": ", "PlotIndex", "en", "/", "[AutoResearch] ", "number", "core/teleport", "core/run_job", "UIListLayout", "nothing claimable", " owned)", "Auto Upgrade: Pickaxe ที่จะซื้อ", "My Anime Mine · Mining Hub", "[AutoResearch] ", "disabled", "table", "AutoPlot", "UIListLayout", "[AutoResearch] nothing open and affordable right now", "autoPlaceUpgradeWeak", "string", "nothing left to buy (", "^x(%d+)$", "UIScale", "fill", "table", ",", " (power ", "disabled", "Auto Place Miner", ") gave nothing", " from ", ")", "Character", "[AutoPlace] picked up ", "games/my_anime_mine/get_plot_parts", "Players", "core/run_job", "Core buying is off", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Frame", "TextLabel", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "[AntiAFK] disabled", "[Bb]uy", "^%$", "VirtualInputManager", "Shop Stand", "[AutoUpgrade] ", "x", "none", "Auto Research: ซื้อสาย Core", " (x", "[AutoResearch] Core ", "Speed", "[AutoClaim] ", "Frame", "Badge", "[AutoPlot] ", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "", "ZoneBoard", "write reported success but ", "search_placeholder", "disabled", "[AutoChest] waiting: ", "Holders", " has been busy for ", "MineSellItems", "Nodes", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/teleport", "", "core/teleport", "[AutoUpgrade] ", "core/build_env", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Frame", "[AutoUpgrade] ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", " (cost ", "⚙", " -> ", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "core/logger", "SellStand", "autoChest", " -> ", "upgrade", " wouldn't take ", "AutoPlace", " (", "Damage", "[AutoUpgrade] ", " team power", ", ", " (", "core/humanoid", "AutoUpgrade", "affordable - waiting for a turn", "Main", "autoPlaceUnlockBudget", "[AntiAFK] idle detected, simulating input", "[AutoPlot] couldn't switch to ", "[AutoPlot] ", "en", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "core/logger", "ตามเครื่อง", "ZoneRow", "อังกฤษ", "", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "autoUpgradeMutationTarget", "CoreGui", ")", "autoPlotBuy", "empty", "Auto Research: รอบเช็ค", "ReplicatedStorage", "games/my_anime_mine/get_best_miner", "M", "core/i18n", "prod", "CharSpeedMaxLevel", "autoMineSpeed", "UserInputService", "autoPlace", "ขนาดเมนู", "AutoPlot", "core/run_job", "[AutoPlace] couldn't hold anything - not pressing E", "X", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "Auto Research (Plot)", ") from ", "GetCharacterUpgradeCost", "GetHolderSpeedLevel", "autoUpgradePickaxeIndex", "Auto Upgrade: กันเงินไว้", "Auto Plot: ใช้โซนล่าสุดเสมอ", "autoResearchInterval", "autoUpgradeKeepMoney", ") ", "Auto Chest: เกณฑ์เก็บ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", ", you have ", "%.3g", "MekPro.log", "TextButton", "Damage", ",", "core/logger", "Auto Plot: รอบเช็ค", "Players", "GuiService", "TextButton", ")", "core/dispatch", "core/logger", "[AutoChest] can't read the chest yet", "table", "autoUpgradeMutationBudget", " still locked after paying ", "Header", "table", "Auto Mine", "([%d,%.]+)%s*(%a*)", "AutoClaim", "lowest", "ต่ำสุดก่อน", "needs us at the stand - keeping the teleport", "M", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "[AutoPlace] ", "[AutoClaim] claimed ", "games/my_anime_mine/game_api", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "core/logger", "core/anti_afk", ": ", "[AutoSell] enabled", "TextButton", "[Logger] failed to write ", "BodyPosition", "s - jumping the queue", "games/my_anime_mine/auto_research", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "autoUpgradePriority", "Damage", "Auto Upgrade Unit", " items (now ", " mutation x", ") needs ", "MineTakeChest", "", " didn't go through (out of stock or too expensive)", "Frame", "core/run_job", "core/logger", "core/ui", "This is not a supported place.", "table", "core/i18n", "Players", "🏠", " ", "HumanoidRootPart", " (", "MineUpgradeCharacter", "Limited", "autoMineClicks", " -> ", "[AutoResearch] idle: ", "Auto Sell", "MekPro", "UIStroke", "open_menu_hint", "Auto Upgrade: รอบเช็ค", "UIListLayout", "games/my_anime_mine/auto_plot", "AutoChest", "TextLabel", "both Zone and Core buying are switched off", "string", " for ", "", "Content", "enabled", " #", "core/logger", "Players", "core/get_place_id", "[Uu]nlock", "th", "MineWorld", "games/my_anime_mine/auto_claim", "[Config] couldn't read ", "core/get_place_id", "Loot", "UIPadding", "HumanoidRootPart", "[AutoPlace] drifted out of range of ", "autoPlotKeepMoney", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", " items)", "s", "games/my_anime_mine/game_api", "/", "lowest", "[AutoPlace] couldn't pick up ", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "ZoneBoardGui", "Core", "OwnerUserId", "/config.json", " but no unclaimed reward in its Track", "MineSelectZone", "Auto Mine: คลิกต่อรอบ", "autoSellMaxWait", " possible holder(s) - not buying more", "TextLabel", "Locked miner slot", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " ", "Frame", " ชิ้น", "[%X] ", "MineBuyPickaxe", "[MekPro] build ", " stack(s)", "games/my_anime_mine/game_api", "[AutoSell] disabled", "autoUpgradeRollMutation", "function", "TextLabel", "] job failed: ", "nothing to place or upgrade", "games/my_anime_mine/auto_upgrade", "s", ")", "games/my_anime_mine/game_api", "TextButton", "", "Workspace", "CoreUI", "GetCharacterSpeedUpgradeCost", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "%$%s*([%d,%.]+)%s*(%a*)", "Speed", "Badge", "PickaxesStand", "", " waited ", "", "core/config", "2026-09-21 12:42:30", "RunService", "table", "UICorner", "works from anywhere - no teleport needed", ": ", "core/scheduler", "TextLabel", "[GameAPI] couldn't load the game's modules: ", "table", "open_menu_hint", "enabled", ", base ", "disabled", "[AutoClaim] ", "[AutoUpgrade] ", " claimable but none could be resolved", "", "modules not loaded", "AutoSell", "autoResearchMaxPerPass", "Auto Claim: เก็บสูงสุดต่อรอบ", "Auto Mine: ความเร็ว", "AutoUpgrade", "table", " ", "[AutoChest] chest is empty", "games/my_anime_mine/game_api", ") ", "GetHolderLevel", "autoPlaceUnlock", "M", "core/logger", "games/my_anime_mine/auto_sell", "core/logger", ") ", "auto", "(nothing to do)", "no mine data", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "ไล่อัปเกรด miner ทุกกี่วินาที", "Auto Sell: รอคิวได้นานสุด", "core/run_job", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "core/parse_money", "TextButton", "Search", "ถูกสุดก่อน", "autoResearchZones", "autoResearchCheapestFirst", "autoResearchCoreLast", "\n", "works from anywhere - dropping the teleport", "costs ", "Auto Research: กันเงินไว้", "cost", "=empty", "ไล่ซื้อทุกกี่วินาที", "s", "Auto Upgrade Plot (Zone)", "index rewards are disabled in this game build", "en-us", "Text", "[AutoUpgrade] bought pickaxe #", "[Scheduler] turn: ", " refused - ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "%.0f", " | ", "[AutoPlace] ", "Players", "sold ", "autoResearchKeepMoney", "games/my_anime_mine/auto_plot", "games/my_anime_mine/auto_sell", " (", "0 = ไม่จำกัด", "games/my_anime_mine/get_plot_parts", "lowest", "Hauler", "[AutoSell] MineSellItems ", "core/ui", "[AutoPlace] unlocked ", "%", "ProximityPrompt", "autoChestInterval", "neither appendfile nor writefile is available", " ", " - ", "autoClaimInterval", "s", "core/config", "ปุ่มเปิดเมนู", "/", "Anti-AFK", "[AutoPlace] nothing in the hotbar to place", "s", "no isfile/readfile available to verify the write", "/", "MineRollMutation", ") ", "Mutation: เพดานราคาต่อครั้ง", "games/my_anime_mine/get_best_miner", "autoSellMaxWait", "BasePart", "Search functions...", " (chest holds ", "", "%.0f", "autoMine", "Players", "Workspace", "%$", "Exclusive", "ScreenGui", "M", ": badge says ", "Auto Chest: รอบเช็ค", "Frame", "autoUpgradePickaxeIndex", "", " ", "games/my_anime_mine/auto_claim", "games/my_anime_mine/get_current_plot", ")", "AutoPlace", "All", "[AutoPlace] fireproximityprompt error: ", "core/i18n", "games/my_anime_mine/game_api", "s", "Workspace", "Auto Upgrade: สุ่ม Mutation", "TextLabel", "unlock", "x", "auto", "autoPlot", "[AutoPlace] holders: ", "TextLabel", "MineSpeed", "enabled", "autoUpgradeEquipPickaxe", "[AutoPlace] slot ", " (still ", "x", "autoUpgradePickaxe", "autoSellProtectUnits", "MineEquipSlot", "Auto Chest", "auto", "autoUpgradeInterval", "core/get_place_id", ")", "[AutoPlot] ", "autoClaim", "0 = ไม่จำกัด", " (", "research", "Auto Place: เปลี่ยนตัวอ่อนออก", ")", " ", "ItemName", "Frame", "core/logger", "ClickDetector", "Text", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", " costs ", "AutoResearch", "MineEquipPickaxe", " (x", "unlock", " (prompt says '", ")", "[AutoResearch] ", "empty", "\n", " (", "games/my_anime_mine/get_current_plot", ") -> ", "(", "ขายทุกกี่วินาที", "BasePart", "/s", "เช็คโซนทุกกี่วินาที", "MineGui", "ฟาร์ม", "autoPlotLatest", "table", "games/registry", " x", "games/my_anime_mine/auto_place", "Chest", "%.3g", "", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "BodyVelocity", "Crew", "games/my_anime_mine/main", "core/logger", "[GameAPI] SendEvent failed: ", "[AutoChest] disabled", "", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "enabled", "core/parse_money", " (", "TextLabel", "games/my_anime_mine/game_api", "Plots", "Auto Claim (Index)", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "", "prompt disabled", "Players", "Pad", "research", "core/logger", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Modules", "[AutoPlace] unlocking ", "x", "server didn't equip it", "th", "", "TextLabel", "Mining", "s", "core/build_env", "games/my_anime_mine/game_api", "fill", "^([%d,]+)$", "slot not in the hotbar", "autoUpgrade", "table", "none", "BasePart", "แพงสุดก่อน", " -> ", "Frame", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", ")", "core/dispatch", "ProximityPrompt", "table", "core/run_job", "[AutoUpgrade] pickaxe #", "Zone buying is off", "HttpService", "autoUpgradeLimitPickaxes", "Auto Claim: รอบเช็ค", "[AutoUpgrade] every stat on every miner is maxed", "function", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "AutoPlace", "ค้นหาฟังก์ชัน...", "games/registry", "ValueLabel", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "TextLabel", ": ", ")", "", ")", "TextLabel", " research is ", "auto", "?", "en", "waiting", "M", "Players", "MineSpeed", " (power ", "", "th", "games/my_anime_mine/main", "ItemFrame", "AutoClaim", "core/parse_money", "maxed", "TextButton", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Mining", "TextLabel", "TextButton", "", "ClaimableTiers failed", "MineWorld", "Container", "[AntiAFK] enabled - watching Player.Idled", "Workspace", "games/my_anime_mine/game_api", "[AutoResearch] ", "core/get_me", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "[AutoMine] ", "^M(%d+)$", "Zones", "BasePart", "Workspace", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "", "TextLabel", "%$", "", "[Scheduler] ", "[AutoResearch] cheapest open node: ", " for ", "core/logger", "languageIndex", " power ", "autoClaimMaxPerPass", " (", "core/logger", "autoPlotInterval", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", " ", "Damage", " pickaxe ", " items", "=locked(", "games/my_anime_mine/auto_place", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", " - upgrade the miners first", "sale didn't go through", "", "games/my_anime_mine/auto_mine", "core/get_leaderstat", "[MekPro] settings file: ", ")", ")", "core/teleport", "[AutoSell] ", "enabled", "autoChestMinItems", "MinePower", "action '", "leaderstats", "ตั้งค่า", "AutoSell", "slot ", "GetHolderMineSpeedLevel", "'", "Backpack", "VirtualUser", "-", "[AutoPlace] disabled", "[", "games/my_anime_mine/get_current_plot", "5571fad+", "core/run_job", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "autoResearch", "Players", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "[AutoClaim] idle: ", "Frame", "[BestMiner] slot ", "s", "[Config] couldn't write ", "Root", "=", " ", "core/debug_tab", ", built ", "AutoMine", "core/scheduler", " (", "AutoChest", "Speed", "needs ", " node(s) looked buyable but the server refused", "UserInputService", "", " reward(s) looked claimable but none went through", "locked slot", "table", "games/my_anime_mine/auto_research", "/", " error: ", "ChestCapacity", "MekPro", " -> ", ") didn't take: ", "table", "ValueLabel", "TextBox", " pickaxe(s) for ", ") on ", "GetCharacterMineSpeedUpgradeCost", " (", "List", "table", "TextLabel", "table", "PlayerGui", "RunService", "CharMineSpeedMaxLevel", " ", "Auto Upgrade: แจก Pickaxe ให้ miner", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "HumanoidRootPart", "unreadable", "Hauler", "Auto Place: ปลดล็อกช่อง miner", " team power, you have ", "badge says ", "antiAfk", "games/my_anime_mine/auto_upgrade", "table", "string", " ", "string", ": State(", "Auto Research: ซื้อสูงสุดต่อรอบ", "[AutoChest] MineTakeChest had no effect (", "languageIndex", "Mutation: หยุดเมื่อได้ x", "th", " while selecting", " doesn't exist", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "search_placeholder", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "ScrollingFrame", "[AutoUpgrade] MineBuyPickaxe ", "unreadable", "Humanoid", "[AutoPlot] unlocked ", " ", "Content", "Frame", "every open node is maxed", "[Scheduler] ", ",", "games/my_anime_mine/game_api", "TextButton", ")", "[AutoChest] enabled (collects once >= ", " (money ", "cheapest open node ", "HotbarFrame", "core/run_job", "waiting", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "core/click", "string", "games/my_anime_mine/auto_chest", "CharMaxLevel", "[AutoPlace] upgrading ", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", " (", "Auto Place: เพดานราคาปลดช่อง", "[Scheduler] ", "menuScale", "Auto Place: รอบเช็ค", "games/my_anime_mine/auto_mine", "')", "games/my_anime_mine/get_plot_parts", ", have ", "core/logger", "core/anti_afk", "Content", " still empty after placing ", "button has no size on screen", "MineBuySkill", "slot past the usable cap", "MineSpeed", "", "cost", "autoSellInterval", "menuScale", "Players", "autoSell", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", " not bought yet (", "s - carrying on without it", "Auto Plot: ซื้อโซนถัดไป", "Plots", "/", "Humanoid", "Auto Research: ซื้อสาย Zone", " -> x", "#", "Open menu", "TextLabel", "UICorner", "TabBar", "Frame", "[AutoPlace] placed "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(212)] = function()
return function(import)
local log = import(S(232))
local Players = game:GetService(S(264))
local VirtualUser = game:GetService(S(676))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(621))
        connection = player.Idled:Connect(function()
            log(S(135))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(76))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(108)] = function()
return { Env = S(156), IsDev = false, Commit = S(681), BuiltAt = S(329) }
end
modules[S(778)] = function()
local VirtualInputManager = game:GetService(S(79))
local GuiService = game:GetService(S(188))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(798)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(421)] = function()
return function(import)
local HttpService = game:GetService(S(579))
local getPlaceId = import(S(482))
local log = import(S(794))
local ROOT_FOLDER = S(713)
local placeFolder = ROOT_FOLDER .. S(423) .. tostring(getPlaceId())
local filePath = placeFolder .. S(288)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(235) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(270) .. filePath .. S(28) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(691) .. filePath .. S(213) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(573)] = function()
return function(import)
    local getPlaceId = import(S(271))
    local registry = import(S(520))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(234))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(659)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(669))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(625)] = function()
local Players = game:GetService(S(69))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(814))
    local backpack = player:WaitForChild(S(675))
    return player, humanoid, backpack
end
return getMe
end
modules[S(265)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(130)] = function()
local Players = game:GetService(S(602))
local RunService = game:GetService(S(728))
local UserInputService = game:GetService(S(159))
local Workspace = game:GetService(S(441))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(759))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(240))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(527))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(434)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(513)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(217))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(458)] = function()
local Players = game:GetService(S(440))
local STRINGS = {
    en = {
        search_placeholder = S(435),
        open_menu_hint = S(818),
    },
    th = {
        search_placeholder = S(586),
        open_menu_hint = S(422),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(391)
    if locale:sub(1, 2):lower() == S(267) then
        return S(554)
    end
    return S(35)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(480) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(494)] = function()
local LOG_FILE = S(181)
local function log(message)
    local line = os.date(S(300)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(543))
            end
            appendfile(LOG_FILE, line .. S(381))
        elseif writefile then
            local existing = S(437)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(507))
        else
            error(S(416))
        end
        if not (isfile and readfile) then
            error(S(427))
        end
        if not isfile(LOG_FILE) then
            error(S(95) .. LOG_FILE .. S(752))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(21) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(216) .. LOG_FILE .. S(334) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(536)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(779) then
        return nil
    end
    local number, suffix = text:match(S(321))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(766), S(144))))
    if not value then
        return nil
    end
    if suffix ~= S(802) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(163)] = function()
return function(import)
local log = import(S(117))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(679) .. name .. S(309) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(335)] = function()
return function(import)
local RunService = game:GetService(S(330))
local log = import(S(640))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(765) .. task_.name .. S(711) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(637) .. task_.name .. S(100)
                    .. string.format(S(438), now - task_.busySince)
                    .. S(810))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(787) .. starved.name .. S(326)
            .. string.format(S(397), now - starved.waitingSince)
            .. S(218))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(394) .. (currentTask and currentTask.name or S(366)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(39)] = function()
local Players = game:GetService(S(237))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(274))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(411)] = function()
return function(import)
local i18n = import(S(155))
local UserInputService = game:GetService(S(704))
local THEME = {
    BrandName = S(248),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(332))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(820))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(273))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(182))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(105)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(249))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(819))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(598)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(336))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(228)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(756))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(50))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(463))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(298))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(615))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(556))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(294))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(588))
            if not valueLbl then
                valueLbl = Instance.new(S(308))
                valueLbl.Name = S(717)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(323))
            if not badge then
                badge = Instance.new(S(725))
                badge.Name = S(90)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(189))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(448))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(822))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(616))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(327)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(413)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(230))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(688))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(315))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(93)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(570))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(252))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(612))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(392)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(444))
    screenGui.Name = S(318)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(147))
    local main = Instance.new(S(110))
    main.Name = S(133)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(56))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(73))
    tabBar.Name = S(821)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(41))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(763))
    header.Name = S(197)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(768))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(634))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(255))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(657)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(215))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(165)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(375))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(677)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(718))
    searchBox.Name = S(376)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(96))
    searchBox.Text = S(705)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(493))
    content.Name = S(762)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(89))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(74))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(339))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(469))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(496)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(346) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(491) .. (item.Subtitle or S(633))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(754))
        footerLabel.Text = i18n.T(S(250))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(29)] = function()
return function(import)
local gameApi = import(S(304))(import)
local log = import(S(140))
local runGuarded = import(S(40))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(770) .. minItemsToCollect .. S(278))
    else
        log(S(532))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(734) then
            log(S(193))
            lastReported = S(758)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(506) then
            log(S(355))
            lastReported = S(150)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(775) then
            local capacity = gameApi.ChestCapacity()
            log(S(98) .. count .. S(710) .. want .. S(651)
                .. (capacity and (S(436) .. capacity .. S(490)) or S(533)))
            lastReported = S(600)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(700), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(4) .. count .. S(224) .. after .. S(769))
        else
            log(S(747) .. count
                .. S(714) .. tostring(after) .. S(594))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(452)] = function()
return function(import)
local gameApi = import(S(539))(import)
local log = import(S(363))
local runGuarded = import(S(682))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(88) .. (enabled and S(261) or S(10)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(687) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(208) .. reward.category .. S(451)
                .. tostring(reward.tierKey) .. S(262) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(343) .. #pending .. S(706))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(201), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(658)] = function()
return function(import)
local getCurrentPlot = import(S(680))
local getPlotParts = import(S(407))
local log = import(S(530))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(495), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(627) .. (enabled and S(471) or S(47)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(522)] = function()
return function(import)
local Players = game:GetService(S(545))
local getCurrentPlot = import(S(453))
local getPlotParts = import(S(68))
local getMinerSlots = import(S(432))(import)
local teleport = import(S(104))
local parseMoney = import(S(610))
local gameApi = import(S(314))(import)
local log = import(S(3))
local runGuarded = import(S(371))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(31)
local LOCKED_SLOT_TEXT = S(295)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(546))
    return pad and pad:FindFirstChildWhichIsA(S(414), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(538)) then
            local digits = descendant.Text:match(S(562))
            if digits then
                return tonumber((digits:gsub(S(59), S(617))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(590)) and descendant.Text:match(S(78)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(672) .. holder.Name .. S(809)
            .. gameApi.MinerSlots() .. S(36) .. gameApi.MaxMinerSlots() .. S(43)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(707)
    end
    if not prompt.Enabled then
        return true, S(544)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(383) .. price
    end
    local action = prompt.ActionText or S(450)
    if action:match(S(266)) or action:match(S(77)) or action:match(S(635)) then
        return true, S(668) .. action .. S(674)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(733))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(457) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(652) .. why .. S(661))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(386))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(693) .. prompt.ObjectText
                    .. S(511) .. shown .. S(341) .. math.floor(base) .. S(454))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(398))
    if line ~= lastSurvey then
        log(S(468) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(561), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(121), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(502), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(207) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(678))
    else
        lastAttempt = 0
        log(S(632))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(784),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(563)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(800)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(553)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(425))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(275) .. holder.Name .. S(751))
            return false
        end
        log(S(473) .. candidate.slot .. S(124) .. candidate.name
            .. S(715) .. tostring(why))
    end
    if not miner then
        log(S(164))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(16) .. holder.Name .. S(797) .. miner.name)
        return false
    end
    log(S(823) .. miner.name .. S(604) .. miner.power
        .. S(720) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(283) .. entry.name .. S(64) .. entry.holder.Name
            .. S(503) .. tostring(entry.prompt.ActionText) .. S(791))
        return false
    end
    log(S(67) .. entry.name .. S(60) .. entry.power
        .. S(168) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(399) .. pad.holder.Name .. S(196)
                .. tostring(pad.priceText))
        else
            log(S(412) .. pad.holder.Name
                .. S(258) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(310))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(57) then
        runGuarded(S(455), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(464) then
        log(S(551) .. target.holder.Name
            .. S(639) .. tostring(target.priceText))
        runGuarded(S(585), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(782) .. target.holder.Name .. S(33) .. target.name
        .. S(722) .. target.power .. S(510) .. best.name .. S(487) .. best.power .. S(662))
    runGuarded(S(123), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(253)] = function()
return function(import)
local gameApi = import(S(459))(import)
local parseMoney = import(S(374))
local log = import(S(263))
local runGuarded = import(S(576))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(91) .. (enabled and S(665) or S(97)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(442)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(30) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(785) .. deepestKey .. S(572))
        return true
    end
    log(S(136) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(484) .. gameApi.ZoneDisplayName(zoneKey) .. S(15)
                .. math.floor(price) .. S(179) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(760) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(508) .. zoneKey .. S(148))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(488) then
            reportedLocked = S(547)
            log(S(137) .. zoneKey .. S(395)
                .. gameApi.ZoneDisplayName(currentKey) .. S(596)
                .. done .. S(17) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(49), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(219)] = function()
return function(import)
local gameApi = import(S(209))(import)
local log = import(S(548))
local runGuarded = import(S(231))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(46) .. (enabled and S(535) or S(342)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(505) .. node.zoneKey .. S(730) .. node.id
                    .. S(644) .. node.family .. S(364) .. node.level .. S(245) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(86) .. node.id
                    .. S(405) .. node.family .. S(357) .. node.level .. S(569) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(37) .. blocked
                .. S(703))
        elseif powerBlocked then
            log(S(624) .. powerBlocked.zoneKey .. S(239) .. powerBlocked.id
                .. S(699) .. powerBlocked.family .. S(226)
                .. math.floor(powerBlocked.powerReq) .. S(737)
                .. math.floor(powerBlocked.power)
                .. S(655))
        else
            log(S(51)
                .. S(771) .. math.floor(gameApi.GetMoney()) .. S(483))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(638) .. nearest.zoneKey
                    .. S(743) .. nearest.id .. S(129) .. nearest.family .. S(176)
                    .. nearest.level .. S(813) .. nearest.max
                    .. S(498) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(256)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(286),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(578))
        end
        if not buyCore then
            table.insert(parts, S(71))
        end
        if #parts == 0 then
            return S(764)
        end
        return S(54) .. table.concat(parts, S(128)) .. S(313)
    end
    local why
    if nearest.cost > budget then
        why = S(702) .. string.format(S(180), nearest.cost)
            .. S(793) .. string.format(S(7), money)
    elseif nearest.powerOk == false then
        why = S(13) .. string.format(S(524), nearest.powerReq) .. S(127)
    else
        why = S(132)
    end
    return S(772) .. nearest.zoneKey .. S(354) .. tostring(nearest.id)
        .. S(241) .. tostring(nearest.family) .. S(430) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(246) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(499), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(362)] = function()
return function(import)
local Workspace = game:GetService(S(317))
local gameApi = import(S(280))(import)
local teleport = import(S(663))
local log = import(S(185))
local runGuarded = import(S(70))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(305))
    else
        lastAttempt = 0
        log(S(214))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(118))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(574), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(410)
                .. (sold and S(382)
                         or S(204)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(664) .. (sold and (S(401) .. #keys .. S(303)) or S(656)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(671), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(311)] = function()
return function(import)
local Workspace = game:GetService(S(622))
local gameApi = import(S(767))(import)
local teleport = import(S(106))
local log = import(S(211))
local runGuarded = import(S(774))(import)
local STATS = { S(125), S(322), S(470) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(408)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(81) .. (enabled and S(340) or S(61)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(716) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(202) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(385) or S(611)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(1) .. holderKey .. S(297) .. choice.stat .. S(761)
                        .. choice.info.level .. S(120) .. after.level
                        .. S(281) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(803) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(324))
    local shop = stand and stand:FindFirstChild(S(80))
    if not shop then
        return nil
    end
    if shop:IsA(S(630)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(567), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(757)
                .. (bought and S(333)
                           or S(8)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(220))
        end
    end
    if bought then
        log(S(393) .. pickaxe.index .. S(648) .. pickaxe.name
            .. S(85) .. pickaxe.multiplier .. S(592))
        return true
    end
    log(S(577) .. pickaxe.index .. S(417) .. pickaxe.name
        .. S(229))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(111) .. holderKey .. S(650)
                    .. tostring(worn or S(566)) .. S(115) .. wanted.name)
            else
                log(S(344) .. holderKey .. S(122)
                    .. wanted.name .. S(474) .. tostring(nowWorn or S(83)) .. S(190))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(126) .. holderKey .. S(225) .. variant
                .. S(816) .. after .. S(113) .. cost .. S(65))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(107) .. gameApi.OwnedPickaxeCount()
                    .. S(719) .. gameApi.MaxMinerSlots()
                    .. S(293))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(534))
        elseif maxed > 0 then
            log(S(582))
        else
            log(S(584))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(352), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(560)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(152))
local Workspace = game:GetService(S(461))
local log = import(S(645))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(550), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(337) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(726))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(724) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(409) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(565) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(353) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(735) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(628))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(531) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(227))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(456), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(575) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(194) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(66) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(101), keys)
end
function api.EquipSlot(slot)
    return send(S(478), slot == S(325) and S(636) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(301), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(500), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(429), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(799), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(519)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(400)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(667))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(583) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(338) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(290), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(268))
    plots = plots and plots:FindFirstChild(S(540))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(806)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(287)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(94))
    local gui = board and board:FindFirstChild(S(285))
    local root = gui and gui:FindFirstChild(S(692))
    local list = root and root:FindFirstChild(S(723))
    local row = list and list:FindFirstChild(S(142) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(595)) and descendant.Text ~= S(259) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(307) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(242), characterKey, stat == S(614) and S(603) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(358), max = S(781), cost = S(169) },
    Speed = { level = S(170), max = S(157), cost = S(319) },
    MineSpeed = { level = S(673), max = S(729), cost = S(721) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(742) or charName == S(316) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(741) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(38) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(744) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(331) and type(holder.CharName) == S(257) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(243), S(443) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(48) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(347)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(367)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(390)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(58) then
        return {}, S(618)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(42)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(708) or type(state.Track) ~= S(198) then
                table.insert(notes, tabKey .. S(745) .. tostring(category) .. S(63))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(446) .. waiting
                        .. S(289))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(738) .. total .. S(345)
            .. (#notes > 0 and (S(418) .. table.concat(notes, S(2))) or S(525))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(53) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(153)] = function()
return function(import)
local Players = game:GetService(S(685))
local gameApi = import(S(356))(import)
local log = import(S(361))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(200))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(184), S(605)))) or 0
    if suffix ~= S(555) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(727))
    local mineGui = playerGui and playerGui:FindFirstChild(S(516))
    local hotbar = mineGui and mineGui:FindFirstChild(S(773))
    local content = hotbar and hotbar:FindFirstChild(S(260))
    return content and content:FindFirstChild(S(620))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(5)) then
            local count = descendant.Text:match(S(55))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(608))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(796))
    local nameLabel = content and content:FindFirstChild(S(492))
    local damageLabel = content and content:FindFirstChild(S(222))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(593) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(428) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(689) .. slot .. S(591) .. info.name
                        .. S(642) .. info.power .. S(521) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(509)] = function()
local Players = game:GetService(S(187))
local Workspace = game:GetService(S(631))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(619))
    local plots = mineWorld and mineWorld:FindFirstChild(S(812))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(23)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(792)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(99)),
        Nodes = plot:FindFirstChild(S(102)),
        Chest = plot:FindFirstChild(S(523)),
        Zones = plot:FindFirstChild(S(629)),
        Crew = plot:FindFirstChild(S(528)),
        Loot = plot:FindFirstChild(S(272)),
        PlotIndex = plot:GetAttribute(S(34)),
        ChestCapacity = plot:GetAttribute(S(712)),
    }
end
return getPlotParts
end
modules[S(607)] = function()
return function(import)
    local createWindow = import(S(233))(import)
    local i18n = import(S(236))
    local antiAfk = import(S(795))(import)
    local buildEnv = import(S(559))
    local scheduler = import(S(698))(import)
    local config = import(S(328))(import)
    local log = import(S(192))
    log(S(302) .. buildEnv.Commit .. S(537) .. buildEnv.Env
        .. S(696) .. buildEnv.BuiltAt .. S(11))
    local window = createWindow({
        Subtitle = S(45),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(790))(import)
    local autoPlace = import(S(653))(import)
    local autoChest = import(S(780))(import)
    local autoSell = import(S(404))(import)
    local autoUpgrade = import(S(740))(import)
    local autoResearch = import(S(709))(import)
    local autoClaim = import(S(269))(import)
    local autoPlot = import(S(403))(import)
    local gameApi = import(S(623))(import)
    scheduler.RegisterStandalone(S(697), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(254), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(9), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(131), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(25), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(609), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(162), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(433), 20)
    scheduler.RegisterExclusive(S(348), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(517), S(238))
    persistentToggle(farmTab:AddCard(
        S(199),
        S(320)
    ), S(439), autoMine.SetEnabled)
    persistentSlider(farmTab, S(351), S(626),
        1, 30, S(158), 5, autoMine.SetCyclesPerSecond, S(514))
    persistentSlider(farmTab, S(291), S(497),
        1, 10, S(244), 1, autoMine.SetClicksPerCycle, S(475))
    persistentToggle(farmTab:AddCard(
        S(62),
        S(613)
    ), S(160), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(789), S(145),
        1, 60, S(18), 3, autoPlace.SetCheckInterval, S(690))
    persistentToggle(
        farmTab:AddCard(S(489),
            S(92)),
        S(52), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(736),
            S(277)),
        S(359), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(786), S(406),
        0, 500, S(134), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(445))
    persistentToggle(farmTab:AddCard(
        S(479),
        S(571)
    ), S(119), autoChest.SetEnabled)
    persistentSlider(farmTab, S(177), S(808),
        50, 3000, S(666), 100, autoChest.SetMinItems, S(299))
    persistentSlider(farmTab, S(447), S(6),
        1, 60, S(415), 3, autoChest.SetCheckInterval, S(420))
    persistentToggle(farmTab:AddCard(
        S(247),
        S(732)
    ), S(807), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(210),
            S(647)),
        S(477), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(24), S(512),
        5, 300, S(804), 20, autoSell.SetCheckInterval, S(312))
    farmTab:AddSlider(S(370),
        S(112),
        5, 120, sellMaxWait, function(value)
            config.Set(S(292), value)
        end, S(460))
    persistentToggle(farmTab:AddCard(
        S(223),
        S(753)
    ), S(564), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(251), S(369),
        5, 300, S(481), 20, autoUpgrade.SetInterval, S(388))
    persistentSlider(farmTab, S(14), S(72),
        1, 20, S(19), 5, autoUpgrade.SetMaxPerPass, S(465))
    persistentSegmented(
        farmTab,
        { S(203), S(183), S(701), S(557) },
        { S(282), S(649), S(87), S(801) },
        S(221), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(172), S(549),
        0, 500, S(175), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(154))
    persistentToggle(
        farmTab:AddCard(S(683),
            S(686)),
        S(476), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(501) .. pickaxe.multiplier .. S(504)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(178)
            end
            return S(817) .. index .. S(694) .. (pickaxeLabels[index] or S(75))
        end
        local savedPick = config.Get(S(449), 0)
        pickaxeCard = farmTab:AddSlider(
            S(44),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(171), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(22)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(206),
            S(783)),
        S(580), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(731),
            S(654)),
        S(472), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(462),
            S(109)),
        S(306), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(749), S(589),
        1, 20, S(146), 2, autoUpgrade.SetMutationTarget, S(552))
    persistentSlider(farmTab, S(431), S(486),
        0, 500, S(195), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(601))
    persistentToggle(farmTab:AddCard(
        S(167),
        S(396)
    ), S(684), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(151), S(387),
        10, 600, S(174), 30, autoResearch.SetCheckInterval, S(426))
    persistentSlider(farmTab, S(746), S(373),
        1, 20, S(349), 5, autoResearch.SetMaxPerPass, S(26))
    persistentSlider(farmTab, S(384), S(755),
        0, 500, S(402), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(360))
    persistentToggle(
        farmTab:AddCard(S(815),
            S(103)),
        S(378), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(84),
            S(526)),
        S(27), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(139),
            S(166)),
        S(380), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(377), S(568) },
        { true, false },
        S(379), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(541),
        S(296)
    ), S(485), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(581), S(368),
        5, 300, S(419), 30, autoClaim.SetCheckInterval, S(279))
    persistentSlider(farmTab, S(350), S(284),
        1, 30, S(643), 10, autoClaim.SetMaxPerPass, S(82))
    persistentToggle(farmTab:AddCard(
        S(389),
        S(542)
    ), S(467), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(186), S(515),
        5, 300, S(646), 30, autoPlot.SetCheckInterval, S(558))
    persistentToggle(
        farmTab:AddCard(S(811),
            S(776)),
        S(149), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(173),
            S(116)),
        S(518), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(20), S(372),
        0, 500, S(276), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(205))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(695))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(670), S(114))
    persistentToggle(
        settingsTab:AddCard(S(424), S(777)),
        S(739),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(805), 100)
    settingsTab:AddSlider(S(161), S(32), 50, 150, savedScale, function(value)
        config.Set(S(788), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(748), 1)
    settingsTab:AddSegmented({ S(141), S(143), S(12) }, savedLanguage, function(index, value)
        local code = ({ S(365), S(599), S(606) })[index]
        config.Set(S(641), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(597), S(138), S(750) })[savedLanguage] or S(466))
    log(S(660) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(587)] = function()
return {
    [79389059854988] = S(529),
}
end
import(S(191))(import)
