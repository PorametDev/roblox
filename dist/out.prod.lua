-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoResearch] cheapest open node: ", "no isfile/readfile available to verify the write", "ขนาดเมนู", "ขายทุกกี่วินาที", " (cost ", ")", "locked slot", "autoPlot", "autoUpgradePriority", "GetCharacterSpeedUpgradeCost", "ต่ำสุดก่อน", "PlayerGui", "Auto Research: ซื้อสาย Core", ")", " holder(s) - not buying more", "[AutoChest] collected ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "[AutoResearch] ", "autoUpgradePickaxe", "Players", "Hauler", "Mining", "Auto Claim: รอบเช็ค", "table", "cost", "TextLabel", "core/click", "table", "GetHolderMineSpeedLevel", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", ")", "core/logger", "AutoPlace", "Auto Upgrade Plot (Zone)", "s - carrying on without it", "MineBuyPickaxe", "games/my_anime_mine/auto_plot", " ", "fill", " (", " -> ", "s", "core/logger", "[AutoPlot] couldn't switch to ", "core/get_place_id", "search_placeholder", "autoPlaceUpgradeWeak", ")", "sold ", "%.0f", "Auto Research: ซื้อสาย Zone", "button has no size on screen", " zones=", "ปุ่มเปิดเมนู", "AutoUpgrade", " (chest holds ", "TextButton", "games/my_anime_mine/auto_research", "TextLabel", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "core/parse_money", "Backpack", "[AntiAFK] idle detected, simulating input", "games/my_anime_mine/auto_claim", "core/logger", "=locked(", "HttpService", "games/my_anime_mine/get_current_plot", " ", "MineSelectZone", "TextLabel", "costs ", "games/my_anime_mine/game_api", ") ", "", "unreadable", " (x", "Frame", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "games/my_anime_mine/auto_claim", "core/config", "[Scheduler] ", "Shop Stand", "ItemName", "[AutoPlace] picked up ", "autoResearchMaxPerPass", "th", "List", "Place", "HumanoidRootPart", "autoSellMaxWait", "autoPlotInterval", " has been busy for ", "core/logger", "TextLabel", "games/my_anime_mine/get_best_miner", "ScreenGui", "table", "TextLabel", "#", ": ", "Frame", "TextButton", "", "Players", " refused - ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "ค้นหาฟังก์ชัน...", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "disabled", "prompt disabled", "OwnerUserId", "Content", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "games/my_anime_mine/game_api", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "UIStroke", "neither appendfile nor writefile is available", "Frame", "?", "MekPro", "s", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Auto Sell", "VirtualInputManager", "ย่อขยายเมนูทั้งชุด", "%$", "PlotIndex", "Frame", "-", "CoreGui", "GetCharacterMineSpeedUpgradeCost", "ZoneBoard", "Workspace", "auto", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "x", "UIListLayout", "autoChestMinItems", "en", "UIPadding", "open_menu_hint", "TextButton", "CoreUI", "[AutoChest] MineTakeChest had no effect (", "[AutoPlace] ", " -> ", "Hauler", "^([%d,]+)$", " (money ", "MineGui", "unreadable", "enabled", "autoUpgrade", "ClickDetector", "[Uu]nlock", "Root", "games/my_anime_mine/auto_upgrade", "[AutoPlot] now mining ", "[AutoUpgrade] bought pickaxe #", ") ", "games/my_anime_mine/auto_place", "ไล่ซื้อทุกกี่วินาที", "waiting", "core/teleport", "autoPlotKeepMoney", "[BestMiner] slot ", "AutoClaim", "autoChestInterval", " (x", "[AutoChest] waiting: ", "upgrade", ", you have ", "[AutoUpgrade] ", " research is ", "AutoPlace", "%$%s*([%d,%.]+)%s*(%a*)", "games/registry", "Auto Research: กันเงินไว้", "autoPlaceInterval", "HotbarFrame", "Zones", "games/my_anime_mine/game_api", " (power ", "Auto Research (Plot)", "games/my_anime_mine/get_current_plot", "nothing to place or upgrade", "MinePower", "=", "/s", " req=", "none", " node(s), ", "open_menu_hint", "games/my_anime_mine/auto_chest", "Frame", "[AntiAFK] enabled - watching Player.Idled", " doesn't exist", "TextLabel", "string", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", ": ", " costs ", "autoClaim", "AutoMine", " ชิ้น", "[Bb]uy", "core/run_job", "[AutoPlace] placed ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", " node(s), ", "MineRollMutation", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "empty", "Speed", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[AntiAFK] disabled", "auto", "MineSpeed", "Pad", ") on ", "/", ")", " team power, you have ", "Crew", "number", " (prompt says '", ")", "Mutation: เพดานราคาต่อครั้ง", "UICorner", "autoUpgradeRollMutation", "autoUpgradeInterval", "TabBar", "[AutoResearch] Core ", "core/logger", "เช็คโซนทุกกี่วินาที", "=empty", "[AutoUpgrade] ", "core/i18n", "[AutoResearch]   ", "core/get_place_id", "Mining", "UIListLayout", "ตั้งค่า", "[MekPro] build ", "autoSellMaxWait", "ChestCapacity", "[AutoUpgrade] ", "core/run_job", "Humanoid", "Search", "enabled", " ", "disabled", "CharSpeedMaxLevel", "core/logger", "(", "x", "Auto Upgrade: รอบเช็ค", "", "th", "Workspace", "[AutoPlot] unlocked ", ")", "GetCharacterUpgradeCost", ") -> ", "TextButton", "core/logger", " pickaxe(s) for ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " powerOk=", "games/my_anime_mine/auto_mine", "[Config] couldn't write ", "core/run_job", "Auto Upgrade Unit", " (", "autoResearch", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "research", "MineTakeChest", "ItemFrame", "core/logger", "core/teleport", "[AutoPlace] couldn't pick up ", "games/my_anime_mine/auto_chest", "Search functions...", " (", ")", "%$", " | ", "Damage", "My Anime Mine · Mining Hub", "Frame", "[AutoResearch] nothing open and affordable right now", "Auto Upgrade: แจก Pickaxe ให้ miner", "Modules", "autoResearchZones", "en", "Main", "core/i18n", ") needs ", ") from ", "", "TextBox", "", "M", "", "^x(%d+)$", "GuiService", "UIListLayout", "maxed", " reward(s) looked claimable but none went through", "core/teleport", "s - jumping the queue", " (", "(nothing to do)", "core/run_job", "AutoResearch", "Header", " cost=", "AutoPlot", "GetHolderLevel", "BasePart", "AutoClaim", " while selecting", "", "UICorner", "", "ไล่อัปเกรด miner ทุกกี่วินาที", "M", " buyZones=", "core/teleport", "core/get_place_id", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "", "OwnerUserId", "disabled", "th", "MineWorld", "BodyPosition", "This is not a supported place.", "Players", "[AutoUpgrade] MineBuyPickaxe ", "^M(%d+)$", "0131623+", "VirtualUser", "may need us at the stand - trying that", "autoUpgradeMutationTarget", "disabled", " -> ", "games/registry", " -> ", "x", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "M", ") ", "autoResearchCheapestFirst", "table", "AutoChest", "MineUpgradeCharacter", "Text", "[AutoUpgrade] every stat on every miner is maxed", "[", "[AutoUpgrade] pickaxe #", "[AutoChest] chest is empty", "Auto Plot: ซื้อโซนถัดไป", " -> x", "[Scheduler] ", "games/my_anime_mine/game_api", " ", "')", "Frame", "table", " still empty after placing ", "TextLabel", " (", " (", "core/logger", "core/logger", "🏠", "autoSell", "core/run_job", "UserInputService", "Auto Plot: ใช้โซนล่าสุดเสมอ", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "core/scheduler", ": ", "Auto Place: รอบเช็ค", "Auto Research: รอบเช็ค", "Players", "autoResearchCoreLast", "search_placeholder", "games/my_anime_mine/auto_mine", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " owned)", "Auto Place Miner", "core/logger", "games/my_anime_mine/get_plot_parts", "[AutoPlace] drifted out of range of ", "ScrollingFrame", "core/debug_tab", "Container", "menuScale", "table", " (", "[AutoChest] can't read the chest yet", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "autoSellInterval", "^%$", "action '", "MineSpeed", "", "\n", "autoChest", "[GameAPI] SendEvent failed: ", "Open menu", " open=", "/", "core/ui", "core/get_me", "string", " power=", "", "", "%", "Damage", "[AutoResearch] idle: money=", ": ", "[AutoPlace] disabled", ") didn't take, trying the next one", "core/anti_afk", "(none)", "s", "Auto Chest: รอบเช็ค", "autoResearchInterval", "ProximityPrompt", " waited ", "ฟาร์ม", "Auto Plot: กันเงินไว้", "waiting", "en-us", "Auto Research: ซื้อสูงสุดต่อรอบ", "2026-09-21 05:07:26", "slot ", ": ", "TextButton", "Holders", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "games/my_anime_mine/auto_sell", "0 = ไม่จำกัด", "Damage", "autoUpgradeMutationBudget", "[AutoMine] ", "works from anywhere - no teleport needed", " mutation x", "works from anywhere - dropping the teleport", "[AutoPlace] holders: ", "[AutoResearch]   ", "core/ui", "x", "Damage", "autoMineSpeed", " power ", "auto", " buyCore=", "M", " from ", "core/run_job", "Auto Place: เปลี่ยนตัวอ่อนออก", "[AutoResearch] ", "[Logger] failed to write ", "core/scheduler", "Auto Chest", "ZoneBoardGui", "TextLabel", "research", "Text", "Auto Mine: ความเร็ว", "PickaxesStand", ": ", ",", "x", "Auto Upgrade: Pickaxe ที่จะซื้อ", "BasePart", "SellStand", ")", "string", "lowest", "Loot", "cost", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "CharMineSpeedMaxLevel", " ", " open and unmaxed", "TextLabel", "core/logger", "autoMineClicks", "table", "Anti-AFK", " ", "/", "core/run_job", "th", "games/my_anime_mine/game_api", " not bought yet (", "/", "AutoPlot", "BodyVelocity", " ", ",", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "AutoUpgrade", "[AutoPlace] upgrading ", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "AutoPlace", "MekPro", "Players", "games/my_anime_mine/auto_upgrade", "All", "Workspace", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "MineSpeed", "Auto Chest: เกณฑ์เก็บ", " - upgrade the miners first", " error: ", "Frame", "UIScale", "", "[AutoPlace] slot ", "Content", "[AutoSell] MineSellItems ", "Auto Mine", "table", ")", "TextLabel", "games/my_anime_mine/auto_place", "", "Workspace", "menuScale", "X", "อ่านป้ายบน chest ทุกกี่วินาที", " (", "Frame", "table", "auto", "RunService", "disabled", " (power ", "", "Speed", "Plots", "TextButton", " not maxed", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " items)", "TextLabel", "ProximityPrompt", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "[AutoResearch] ", "Players", "[AutoPlace] ", "[AutoClaim] ", "ValueLabel", " ", "games/my_anime_mine/main", "TextLabel", ", built ", "Players", "autoUpgradePickaxeIndex", "leaderstats", "Auto Mine: คลิกต่อรอบ", "autoClaimMaxPerPass", "[%X] ", "autoUpgradeLimitPickaxes", "", "Empty miner slot", "RunService", ")", "core/parse_money", "core/i18n", "อังกฤษ", "core/dispatch", "[AutoClaim] claimed ", " node(s) looked buyable but the server refused", "needs us at the stand - keeping the teleport", " (", "[GameAPI] couldn't load the game's modules: ", "core/build_env", "HumanoidRootPart", "TextLabel", "/", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", ")", "Chest", "/", "MineEquipSlot", "[AutoSell] disabled", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "autoClaimInterval", "core/dispatch", "games/my_anime_mine/get_best_miner", " ", "core/config", "Auto Plot: รอบเช็ค", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "Players", "[AutoChest] disabled", "core/anti_afk", "MineBuySkill", "autoPlace", "HumanoidRootPart", "games/my_anime_mine/game_api", "MineWorld", "autoResearchCore", " ", "] job failed: ", "sale didn't go through", " (", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "core/run_job", "Humanoid", " -> ", "Auto Sell: รอบขาย", "", "[Config] couldn't read ", "แพงสุดก่อน", "fill", "function", "Auto Upgrade: สุ่ม Mutation", " x", "TextButton", "[AutoSell] ", "empty", "GetHolderSpeedLevel", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[AutoPlace] nothing in the hotbar to place", ")", "AutoSell", "core/logger", "antiAfk", "en", "games/my_anime_mine/get_plot_parts", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "Content", "Auto Claim: เก็บสูงสุดต่อรอบ", "ตามเครื่อง", "[AutoUpgrade] ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "AutoChest", "table", "core/logger", "TextButton", "ถูกสุดก่อน", "table", "CharMaxLevel", "[AutoPlace] fireproximityprompt error: ", "write reported success but the line isn't in ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Frame", "ZoneRow", "prod", "MekPro.log", "games/my_anime_mine/get_current_plot", "function", "Auto Upgrade: กันเงินไว้", "ไทย", " budget=", "games/my_anime_mine/game_api", "table", "Mutation: หยุดเมื่อได้ x", "MineSellItems", "Players", "autoMine", "s", "lowest", "", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "lowest", "[Scheduler] turn: ", "/config.json", "BasePart", "Nodes", "table", " items", "Players", " stack(s)", "Frame", " #", "s", "[AutoResearch]   Core: ", "autoUpgradeKeepMoney", "[AutoSell] enabled", "games/my_anime_mine/game_api", "TextLabel", "'", "/", "core/build_env", "", "autoUpgradePickaxeIndex", " didn't go through (out of stock or too expensive)", "[AutoUpgrade] ", "⚙", "core/logger", "%.0f", "autoPlotLatest", "enabled", "Plots", "Auto Sell: รอคิวได้นานสุด", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Badge", "[AutoPlot] ", "[AutoClaim] ", "core/humanoid", "autoPlotBuy", "[AutoChest] enabled (collects once >= ", "AutoResearch", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "s", ",", "enabled", " costs ", "games/my_anime_mine/game_api", "core/get_leaderstat", "games/my_anime_mine/auto_plot", "/", "Badge", "ReplicatedStorage", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "\n", "languageIndex", "UserInputService", "autoResearchKeepMoney", "TextLabel", "games/my_anime_mine/main", "MineEquipPickaxe", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[MekPro] settings file: ", "[AutoPlot] ", "string", "Locked miner slot", ") ", "[^%d%.]", "Auto Claim (Index)", " items (now ", "", "s", "Workspace", "games/my_anime_mine/auto_research", "enabled", "Speed", "autoUpgradeMaxPerPass", "autoUpgradeEquipPickaxe", "[AutoPlot] ", "BasePart", "TextLabel", " pickaxe ", " ", "core/logger", "games/my_anime_mine/get_plot_parts", "languageIndex", "games/my_anime_mine/auto_sell", "[Scheduler] ", "write reported success but ", "s", "AutoSell", "[AutoResearch] ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "ValueLabel", " ("}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(432)] = function()
return function(import)
local log = import(S(398))
local Players = game:GetService(S(569))
local VirtualUser = game:GetService(S(347))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(198))
        connection = player.Idled:Connect(function()
            log(S(64))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(218))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(589)] = function()
return { Env = S(662), IsDev = false, Commit = S(346), BuiltAt = S(444) }
end
modules[S(28)] = function()
local VirtualInputManager = game:GetService(S(126))
local GuiService = game:GetService(S(310))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(53)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(604)] = function()
return function(import)
local HttpService = game:GetService(S(68))
local getPlaceId = import(S(242))
local log = import(S(95))
local ROOT_FOLDER = S(122)
local placeFolder = ROOT_FOLDER .. S(502) .. tostring(getPlaceId())
local filePath = placeFolder .. S(681)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(670) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(626) .. filePath .. S(429) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(274) .. filePath .. S(446) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(601)] = function()
return function(import)
    local getPlaceId = import(S(46))
    local registry = import(S(352))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(342))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(724)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(571))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(421)] = function()
local Players = game:GetService(S(518))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(622))
    local backpack = player:WaitForChild(S(63))
    return player, humanoid, backpack
end
return getMe
end
modules[S(334)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(714)] = function()
local Players = game:GetService(S(561))
local RunService = game:GetService(S(547))
local UserInputService = game:GetService(S(384))
local Workspace = game:GetService(S(521))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(251))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(91))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(509))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(324)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(682)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(341))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(240)] = function()
local Players = game:GetService(S(106))
local STRINGS = {
    en = {
        search_placeholder = S(287),
        open_menu_hint = S(417),
    },
    th = {
        search_placeholder = S(109),
        open_menu_hint = S(55),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(442)
    if locale:sub(1, 2):lower() == S(504) then
        return S(262)
    end
    return S(299)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(136) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(33)] = function()
local LOG_FILE = S(663)
local function log(message)
    local line = os.date(S(574)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(424))
            end
            appendfile(LOG_FILE, line .. S(414))
        elseif writefile then
            local existing = S(529)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(730))
        else
            error(S(119))
        end
        if not (isfile and readfile) then
            error(S(2))
        end
        if not isfile(LOG_FILE) then
            error(S(764) .. LOG_FILE .. S(199))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(658) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(472) .. LOG_FILE .. S(203) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(580)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(201) then
        return nil
    end
    local number, suffix = text:match(S(178))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(511), S(677))))
    if not value then
        return nil
    end
    if suffix ~= S(327) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(383)] = function()
return function(import)
local log = import(S(704))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(364) .. name .. S(617) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(387)] = function()
return function(import)
local RunService = game:GetService(S(578))
local log = import(S(759))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(83) .. task_.name .. S(526) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(369) .. task_.name .. S(94)
                    .. string.format(S(705), now - task_.busySince)
                    .. S(36))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(763) .. starved.name .. S(438)
            .. string.format(S(51), now - starved.waitingSince)
            .. S(315))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(680) .. (currentTask and currentTask.name or S(317)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(166)] = function()
local Players = game:GetService(S(21))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(612))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(420)] = function()
return function(import)
local i18n = import(S(301))
local UserInputService = game:GetService(S(732))
local THEME = {
    BrandName = S(517),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(328))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(231))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(142))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(653))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(304)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(118))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(536))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(121)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(200))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(625)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(401))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(244))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(100))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(130))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(72))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(567))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(591))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(769))
            if not valueLbl then
                valueLbl = Instance.new(S(695))
                valueLbl.Name = S(564)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(711))
            if not badge then
                badge = Instance.new(S(96))
                badge.Name = S(727)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(268))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(294))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(373))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(104))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(336)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(426)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(197))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(527))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(447))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(105)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(103))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(311))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(553))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(362)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(98))
    screenGui.Name = S(145)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(132))
    local main = Instance.new(S(688))
    main.Name = S(300)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(528))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(79))
    tabBar.Name = S(234)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(139))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(120))
    header.Name = S(320)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(144))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(476))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(27))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(746)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(632))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(541)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(58))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(131)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(305))
    searchBox.Name = S(252)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(47))
    searchBox.Text = S(261)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(544))
    content.Name = S(645)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(660))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(496))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(195))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(60))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(478)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(425) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(510) .. (item.Subtitle or S(699))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(393))
        footerLabel.Text = i18n.T(S(143))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(196)] = function()
return function(import)
local gameApi = import(S(613))(import)
local log = import(S(379))
local runGuarded = import(S(250))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(716) .. minItemsToCollect .. S(556))
    else
        log(S(608))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(153) then
            log(S(407))
            lastReported = S(77)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(634) then
            log(S(366))
            lastReported = S(215)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(165) then
            local capacity = gameApi.ChestCapacity()
            log(S(172) .. count .. S(223) .. want .. S(685)
                .. (capacity and (S(57) .. capacity .. S(229)) or S(329)))
            lastReported = S(441)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(360), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(16) .. count .. S(745) .. after .. S(638))
        else
            log(S(146) .. count
                .. S(623) .. tostring(after) .. S(594))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(65)] = function()
return function(import)
local gameApi = import(S(505))(import)
local log = import(S(269))
local runGuarded = import(S(503))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(563) .. (enabled and S(707) or S(548)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(584) .. reward.category .. S(603)
                .. tostring(reward.tierKey) .. S(689) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(713) .. #pending .. S(313))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(169), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(394)] = function()
return function(import)
local getCurrentPlot = import(S(69))
local getPlotParts = import(S(643))
local log = import(S(380))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(156), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(454) .. (enabled and S(253) or S(338)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(537)] = function()
return function(import)
local Players = game:GetService(S(673))
local getCurrentPlot = import(S(187))
local getPlotParts = import(S(399))
local getMinerSlots = import(S(97))(import)
local teleport = import(S(284))
local gameApi = import(S(184))(import)
local log = import(S(66))
local runGuarded = import(S(621))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(577)
local LOCKED_SLOT_TEXT = S(741)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(221))
    return pad and pad:FindFirstChildWhichIsA(S(437), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(376)) then
            local digits = descendant.Text:match(S(150))
            if digits then
                return tonumber((digits:gsub(S(720), S(550))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(557)) and descendant.Text:match(S(410)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(445) .. holder.Name .. S(506)
            .. gameApi.MinerSlots() .. S(726) .. gameApi.MaxMinerSlots() .. S(396)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(7)
    end
    if not prompt.Enabled then
        return true, S(112)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(73) .. price
    end
    local action = prompt.ActionText or S(413)
    if action:match(S(157)) or action:match(S(208)) or action:match(S(128)) then
        return true, S(411) .. action .. S(696)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(590))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(657) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(67) .. why .. S(224))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(238))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(190) .. prompt.ObjectText .. S(258) .. power .. S(14))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(291))
    if line ~= lastSurvey then
        log(S(458) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(40), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(173), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(147) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(430))
    else
        lastAttempt = 0
        log(S(408))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(211),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(90), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(637))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(400) .. holder.Name .. S(326))
            return false
        end
        log(S(530) .. candidate.slot .. S(316) .. candidate.name
            .. S(431))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(562) .. holder.Name .. S(375) .. miner.name)
        return false
    end
    log(S(210) .. miner.name .. S(185) .. miner.power
        .. S(222) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(285) .. entry.name .. S(468) .. entry.holder.Name
            .. S(228) .. tostring(entry.prompt.ActionText) .. S(372))
        return false
    end
    log(S(86) .. entry.name .. S(549) .. entry.power
        .. S(303) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(188))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(628) then
        runGuarded(S(34), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(514) .. target.holder.Name .. S(102) .. target.name
        .. S(406) .. target.power .. S(267) .. best.name .. S(587) .. best.power .. S(32))
    runGuarded(S(516), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(725)] = function()
return function(import)
local gameApi = import(S(694))(import)
local parseMoney = import(S(62))
local log = import(S(283))
local runGuarded = import(S(318))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(712) .. (enabled and S(750) or S(350)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(290)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(160) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(288) .. deepestKey .. S(49))
        return true
    end
    log(S(45) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(754) .. gameApi.ZoneDisplayName(zoneKey) .. S(204)
                .. math.floor(price) .. S(174) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(264) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(277) .. zoneKey .. S(6))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(477) then
            reportedLocked = S(280)
            log(S(739) .. zoneKey .. S(107)
                .. gameApi.ZoneDisplayName(currentKey) .. S(176)
                .. done .. S(697) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(508), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(59)] = function()
return function(import)
local gameApi = import(S(370))(import)
local log = import(S(497))
local runGuarded = import(S(469))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(471) .. (enabled and S(154) or S(111)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(767) .. node.zoneKey .. S(494) .. node.id
                    .. S(770) .. node.family .. S(742) .. node.level .. S(351) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(235) .. node.id
                    .. S(619) .. node.family .. S(162) .. node.level .. S(353) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(560) .. blocked
                .. S(585))
        elseif powerBlocked then
            log(S(19) .. powerBlocked.zoneKey .. S(501) .. powerBlocked.id
                .. S(378) .. powerBlocked.family .. S(302)
                .. math.floor(powerBlocked.powerReq) .. S(225)
                .. math.floor(powerBlocked.power)
                .. S(525))
        else
            log(S(295)
                .. S(151) .. math.floor(gameApi.GetMoney()) .. S(265))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(1) .. nearest.zoneKey
                    .. S(616) .. nearest.id .. S(543) .. nearest.family .. S(75)
                    .. nearest.level .. S(596) .. nearest.max
                    .. S(722) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(428) .. math.floor(money)
        .. S(668) .. math.floor(budget)
        .. S(54) .. (#zones > 0 and table.concat(zones, S(482)) or S(433))
        .. S(332) .. tostring(buyZones) .. S(466) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(241) .. zoneKey .. S(371) .. tostring(node.id)
                        .. S(377) .. tostring(node.family) .. S(357)
                        .. node.level .. S(507) .. node.max
                        .. S(418) .. tostring(node.open)
                        .. S(321) .. tostring(node.cost)
                        .. S(272) .. tostring(node.powerOk)
                        .. S(192) .. tostring(node.powerReq)
                        .. S(423) .. tostring(node.power))
                end
            end
        end
        log(S(459) .. zoneKey .. S(481) .. #nodes
            .. S(194) .. pending .. S(554))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(691) .. #coreNodes .. S(212)
        .. coreOpen .. S(495))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(319), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(762)] = function()
return function(import)
local Workspace = game:GetService(S(263))
local gameApi = import(S(723))(import)
local teleport = import(S(333))
local log = import(S(44))
local runGuarded = import(S(209))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(598))
    else
        lastAttempt = 0
        log(S(693))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(486))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(558), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(532)
                .. (sold and S(457)
                         or S(586)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(633) .. (sold and (S(50) .. #keys .. S(687)) or S(618)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(639), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(519)] = function()
return function(import)
local Workspace = game:GetService(S(748))
local gameApi = import(S(116))(import)
local teleport = import(S(314))
local log = import(S(236))
local runGuarded = import(S(275))(import)
local STATS = { S(427), S(551), S(220) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(489)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(702) .. (enabled and S(721) or S(255)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(534) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(676) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(26) or S(312)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(175) .. holderKey .. S(758) .. choice.stat .. S(70)
                        .. choice.info.level .. S(42) .. after.level
                        .. S(592) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(491) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(480))
    local shop = stand and stand:FindFirstChild(S(84))
    if not shop then
        return nil
    end
    if shop:IsA(S(485)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(755), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(344)
                .. (bought and S(455)
                           or S(348)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(492))
        end
    end
    if bought then
        log(S(161) .. pickaxe.index .. S(39) .. pickaxe.name
            .. S(171) .. pickaxe.multiplier .. S(289))
        return true
    end
    log(S(365) .. pickaxe.index .. S(565) .. pickaxe.name
        .. S(701))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(648) .. holderKey .. S(757)
                .. tostring(entry and entry.Pickaxe or S(193)) .. S(148) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(249) .. holderKey .. S(456) .. variant
                .. S(368) .. after .. S(5) .. cost .. S(487))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(239) .. gameApi.OwnedPickaxeCount()
                    .. S(270) .. gameApi.MinerSlots()
                    .. S(15))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(355))
        elseif maxed > 0 then
            log(S(363))
        else
            log(S(599))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(405) and entry.Pickaxe and entry.Pickaxe ~= S(576) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(513), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(74)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(728))
local Workspace = game:GetService(S(135))
local log = import(S(652))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(297), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(588) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(651))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(655) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(149) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(545) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(29) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(22) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(345))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(416) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(281))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(520), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(374) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(672), keys)
end
function api.EquipSlot(key)
    return send(S(597), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(37), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(736), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(213), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(610), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(99)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(391)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(189))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(665) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(359) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(71), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(614))
    plots = plots and plots:FindFirstChild(S(552))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(607)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(113)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(134))
    local gui = board and board:FindFirstChild(S(475))
    local root = gui and gui:FindFirstChild(S(158))
    local list = root and root:FindFirstChild(S(89))
    local row = list and list:FindFirstChild(S(661) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(734)) and descendant.Text ~= S(538) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(629) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(361), characterKey, stat == S(23) and S(412) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(323), max = S(656), cost = S(266) },
    Speed = { level = S(635), max = S(256), cost = S(10) },
    MineSpeed = { level = S(30), max = S(493), cost = S(133) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(25) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(227) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(740) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(684) and type(holder.CharName) == S(488) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(499) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(422) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(602)] = function()
return function(import)
local Players = game:GetService(S(686))
local log = import(S(640))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(12))
    local mineGui = playerGui and playerGui:FindFirstChild(S(152))
    local hotbar = mineGui and mineGui:FindFirstChild(S(182))
    local content = hotbar and hotbar:FindFirstChild(S(531))
    return content and content:FindFirstChild(S(403))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(756)) then
            local count = descendant.Text:match(S(309))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(282))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(114))
    local nameLabel = content and content:FindFirstChild(S(85))
    local damageLabel = content and content:FindFirstChild(S(452))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(308) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(743), S(306)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(419) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(168) .. slot .. S(388) .. info.name
                        .. S(464) .. info.power .. S(631) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(664)] = function()
local Players = game:GetService(S(343))
local Workspace = game:GetService(S(539))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(340))
    local plots = mineWorld and mineWorld:FindFirstChild(S(708))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(337)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(760)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(448)),
        Nodes = plot:FindFirstChild(S(683)),
        Chest = plot:FindFirstChild(S(595)),
        Zones = plot:FindFirstChild(S(183)),
        Crew = plot:FindFirstChild(S(226)),
        Loot = plot:FindFirstChild(S(490)),
        PlotIndex = plot:GetAttribute(S(129)),
        ChestCapacity = plot:GetAttribute(S(248)),
    }
end
return getPlotParts
end
modules[S(735)] = function()
return function(import)
    local createWindow = import(S(460))(import)
    local i18n = import(S(581))
    local antiAfk = import(S(609))(import)
    local buildEnv = import(S(698))
    local scheduler = import(S(473))(import)
    local config = import(S(82))(import)
    local log = import(S(257))
    log(S(246) .. buildEnv.Commit .. S(41) .. buildEnv.Env
        .. S(568) .. buildEnv.BuiltAt .. S(535))
    local window = createWindow({
        Subtitle = S(293),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(273))(import)
    local autoPlace = import(S(163))(import)
    local autoChest = import(S(286))(import)
    local autoSell = import(S(450))(import)
    local autoUpgrade = import(S(159))(import)
    local autoResearch = import(S(749))(import)
    local autoClaim = import(S(81))(import)
    local autoPlot = import(S(38))(import)
    local gameApi = import(S(669))(import)
    scheduler.RegisterStandalone(S(206), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(650), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(177), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(56), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(717), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(325), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(322), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(247), 20)
    scheduler.RegisterExclusive(S(766), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(439), S(381))
    persistentToggle(farmTab:AddCard(
        S(533),
        S(636)
    ), S(674), autoMine.SetEnabled)
    persistentSlider(farmTab, S(479), S(649),
        1, 30, S(463), 5, autoMine.SetCyclesPerSecond, S(191))
    persistentSlider(farmTab, S(572), S(217),
        1, 10, S(498), 1, autoMine.SetClicksPerCycle, S(354))
    persistentToggle(farmTab:AddCard(
        S(397),
        S(710)
    ), S(611), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(389), S(271),
        1, 60, S(181), 3, autoPlace.SetCheckInterval, S(123))
    persistentToggle(
        farmTab:AddCard(S(470),
            S(512)),
        S(48), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(474),
        S(593)
    ), S(415), autoChest.SetEnabled)
    persistentSlider(farmTab, S(524), S(124),
        50, 3000, S(140), 100, autoChest.SetMinItems, S(207))
    persistentSlider(farmTab, S(435), S(542),
        1, 60, S(170), 3, autoChest.SetCheckInterval, S(675))
    persistentToggle(farmTab:AddCard(
        S(125),
        S(202)
    ), S(382), autoSell.SetEnabled)
    persistentSlider(farmTab, S(624), S(4),
        5, 300, S(409), 20, autoSell.SetCheckInterval, S(690))
    farmTab:AddSlider(S(709),
        S(115),
        5, 120, sellMaxWait, function(value)
            config.Set(S(92), value)
        end, S(719))
    persistentToggle(farmTab:AddCard(
        S(276),
        S(335)
    ), S(155), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(260), S(330),
        5, 300, S(233), 20, autoUpgrade.SetInterval, S(765))
    persistentSlider(farmTab, S(279), S(737),
        1, 20, S(752), 5, autoUpgrade.SetMaxPerPass, S(138))
    persistentSegmented(
        farmTab,
        { S(11), S(292), S(751), S(243) },
        { S(679), S(462), S(216), S(523) },
        S(9), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(666), S(768),
        0, 500, S(692), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(356))
    persistentToggle(
        farmTab:AddCard(S(718),
            S(678)),
        S(20), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(78) .. pickaxe.multiplier .. S(579)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(449)
            end
            return S(101) .. index .. S(254) .. (pickaxeLabels[index] or S(395))
        end
        local savedPick = config.Get(S(700), 0)
        pickaxeCard = farmTab:AddSlider(
            S(484),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(570), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(76)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(515),
            S(386)),
        S(575), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(296),
            S(522)),
        S(753), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(630),
            S(559)),
        S(232), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(671), S(61),
        1, 20, S(349), 2, autoUpgrade.SetMutationTarget, S(461))
    persistentSlider(farmTab, S(230), S(451),
        0, 500, S(453), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(331))
    persistentToggle(farmTab:AddCard(
        S(186),
        S(31)
    ), S(278), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(390), S(164),
        10, 600, S(436), 30, autoResearch.SetCheckInterval, S(434))
    persistentSlider(farmTab, S(443), S(17),
        1, 20, S(87), 5, autoResearch.SetMaxPerPass, S(259))
    persistentSlider(farmTab, S(180), S(659),
        0, 500, S(733), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(467))
    persistentToggle(
        farmTab:AddCard(S(52),
            S(110)),
        S(298), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(13),
            S(108)),
        S(615), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(620),
            S(137)),
        S(392), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(654), S(627) },
        { true, false },
        S(358), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(744),
        S(555)
    ), S(205), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(24), S(80),
        5, 300, S(600), 30, autoClaim.SetCheckInterval, S(43))
    persistentSlider(farmTab, S(646), S(18),
        1, 30, S(573), 10, autoClaim.SetMaxPerPass, S(483))
    persistentToggle(farmTab:AddCard(
        S(35),
        S(606)
    ), S(8), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(605), S(237),
        5, 300, S(93), 30, autoPlot.SetCheckInterval, S(747))
    persistentToggle(
        farmTab:AddCard(S(367),
            S(644)),
        S(715), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(385),
            S(214)),
        S(706), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(440), S(117),
        0, 500, S(167), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(307))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(402))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(245), S(703))
    persistentToggle(
        settingsTab:AddCard(S(500), S(729)),
        S(641),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(404), 100)
    settingsTab:AddSlider(S(3), S(127), 50, 150, savedScale, function(value)
        config.Set(S(540), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(761), 1)
    settingsTab:AddSegmented({ S(647), S(582), S(667) }, savedLanguage, function(index, value)
        local code = ({ S(546), S(642), S(88) })[index]
        config.Set(S(731), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(219), S(141), S(339) })[savedLanguage] or S(465))
    log(S(738) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(179)] = function()
return {
    [79389059854988] = S(566),
}
end
import(S(583))(import)
