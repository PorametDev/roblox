-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"en", "[AutoPlace] slot ", "TextLabel", "VirtualUser", "Players", "", "", "core/build_env", " owned)", "Text", "", "languageIndex", "table", "MineBuyPickaxe", "autoUpgradeEquipPickaxe", "All", "s", "MineGui", "M", "Frame", "core/run_job", "TextButton", "Holders", ") ", " (power ", "TextLabel", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "core/scheduler", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "[AutoPlace] couldn't pick up ", " for ", "Mutation: เพดานราคาต่อครั้ง", " (", "MineSellItems", "Loot", "[AutoUpgrade] ", "/", "MineSpeed", "My Anime Mine · Mining Hub", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "⚙", "auto", "[AutoResearch] ", "core/teleport", "autoClaim", "waiting", "GetCharacterUpgradeCost", "core/parse_money", "games/my_anime_mine/auto_plot", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "games/my_anime_mine/auto_claim", "TextLabel", "[AutoUpgrade] ", "autoUpgrade", "Auto Research (Plot)", "games/my_anime_mine/main", " (", "core/build_env", "core/logger", " refused - ", "[Bb]uy", "autoUpgradeLimitPickaxes", "games/my_anime_mine/auto_upgrade", "Auto Upgrade: รอบเช็ค", "MineBuySkill", "[AutoPlot] unlocked ", "ปุ่มเปิดเมนู", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "core/run_job", "(nothing to do)", "Plots", "Plots", " (chest holds ", "games/my_anime_mine/auto_place", "[AutoChest] chest is empty", " (x", "games/my_anime_mine/game_api", "core/click", "Auto Plot: รอบเช็ค", "[AutoSell] ", "lowest", "autoResearchInterval", "games/my_anime_mine/game_api", "core/i18n", "[AutoPlace] ", "th", "GuiService", " research is ", "Auto Claim: รอบเช็ค", "[AutoUpgrade] ", "research", " doesn't exist", "TextLabel", "TextLabel", "enabled", "autoPlotInterval", "Mining", " x", " -> x", "MineTakeChest", " from ", "games/my_anime_mine/game_api", "VirtualInputManager", "[AutoResearch] nothing open and affordable right now", "TextLabel", "Open menu", "0 = ไม่จำกัด", "Auto Research: กันเงินไว้", "autoSellMaxWait", "Workspace", "languageIndex", "", "Auto Chest: รอบเช็ค", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "autoSell", "ค้นหาฟังก์ชัน...", "s", "ValueLabel", "[AutoResearch] ", "Auto Chest: เกณฑ์เก็บ", "'", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "games/my_anime_mine/auto_mine", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "lowest", "Content", ": ", "core/i18n", "core/logger", "ValueLabel", "ZoneBoard", "=locked(", "Auto Sell: รอบขาย", "Search", "core/anti_afk", "prod", "ItemFrame", "core/logger", "^%$", "TextLabel", "CoreUI", "autoResearchMaxPerPass", "Mutation: หยุดเมื่อได้ x", ", built ", "MineSpeed", "every open node is maxed", "TextButton", "needs ", "[AntiAFK] enabled - watching Player.Idled", " items)", "table", "games/my_anime_mine/get_best_miner", "auto", "TextLabel", "Core buying is off", "Frame", "[AutoPlace] couldn't hold anything - not pressing E", "UICorner", "Badge", ", base ", "List", "Auto Plot: ซื้อโซนถัดไป", "ขายทุกกี่วินาที", "[GameAPI] couldn't load the game's modules: ", "/", " while selecting", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "[", "Empty miner slot", " -> ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "HumanoidRootPart", "AutoChest", " still locked after paying ", "TextButton", "Auto Upgrade: แจก Pickaxe ให้ miner", ") ", "action '", " has been busy for ", "UICorner", "", "games/registry", "TextLabel", "autoPlotBuy", "[AutoResearch] idle: ", "BodyPosition", " node(s) looked buyable but the server refused", "autoPlotLatest", "HumanoidRootPart", ", you have ", "sold ", "Container", ") gave nothing", " claimable but none could be resolved", " (x", "core/logger", "works from anywhere - dropping the teleport", "needs us at the stand - keeping the teleport", "autoResearch", "BasePart", "TextLabel", "[AutoPlace] placed ", "Frame", ") on ", "[AutoClaim] ", "[Scheduler] turn: ", "%.3g", "x", "string", "autoPlaceUnlockBudget", "", " - ", "PlayerGui", " mutation x", "games/my_anime_mine/game_api", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "GetCharacterMineSpeedUpgradeCost", "AutoClaim", " - upgrade the miners first", "games/my_anime_mine/game_api", "games/my_anime_mine/get_current_plot", "", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "s - jumping the queue", "[AutoPlot] ", "/", "HotbarFrame", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "core/logger", "string", "Character", "BasePart", " ", "none", "core/scheduler", "ไทย", ") ", "locked slot", "autoPlot", "", "games/my_anime_mine/auto_sell", "[AutoUpgrade] ", "search_placeholder", "table", "ขนาดเมนู", "Auto Place Miner", "[MekPro] settings file: ", " stack(s)", " ", "TextLabel", "[AutoPlace] fireproximityprompt error: ", "server didn't equip it", "slot ", "autoUpgradeMutationBudget", "; ", "AutoPlace", "%.0f", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "Auto Plot: กันเงินไว้", "^([%d,]+)$", "AutoResearch", "[AutoPlace] drifted out of range of ", ")", "แพงสุดก่อน", "", "AutoUpgrade", "UIStroke", "[AutoPlace] upgrading ", "research", "disabled", "TextButton", "autoMineSpeed", "games/my_anime_mine/auto_chest", " -> ", "UIListLayout", "games/my_anime_mine/auto_mine", "/s", "disabled", "UIPadding", "/", ")", "", "auto", "core/logger", "disabled", "core/get_place_id", "core/logger", "Speed", "ต่ำสุดก่อน", "games/my_anime_mine/auto_research", "cheapest open node ", "Auto Research: ซื้อสาย Core", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "MineWorld", "[AutoUpgrade] nothing to upgrade (no miners placed?)", " #", "core/logger", "Mining ก่อน", "Auto Upgrade: Pickaxe ที่จะซื้อ", "Zone buying is off", "core/run_job", "core/teleport", "MineEquipPickaxe", "sale didn't go through", "Frame", ")", "x", "Core", " ", "M", "games/my_anime_mine/auto_plot", "[AutoUpgrade] MineBuyPickaxe ", "ตั้งค่า", "Frame", "ChestCapacity", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "autoResearchZones", "core/run_job", " (", "string", "s", "UserInputService", "[Logger] failed to write ", "en-us", "[AutoResearch] Core ", ": ", "Auto Mine", "", "table", " (still ", "x", "UIScale", "%$", "games/my_anime_mine/game_api", "Frame", "Modules", "HttpService", " ชิ้น", " (", "GetCharacterSpeedUpgradeCost", " ", "Damage ก่อน", "", "unlock", "", "\n", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "[AutoMine] ", "Main", "nothing claimable", "autoResearchCoreLast", "th", "AutoPlace", ",", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "[AutoPlace] ", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "[GameAPI] SendEvent failed: ", ")", "Players", "[AutoPlot] ", "[AutoPlot] now mining ", "no isfile/readfile available to verify the write", "[AutoClaim] idle: ", "no mine data", "[Scheduler] ", "[AutoPlot] couldn't switch to ", "[Config] couldn't write ", "Auto Place: รอบเช็ค", "games/my_anime_mine/game_api", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "core/ui", "/", "Auto Upgrade: กันเงินไว้", "CharSpeedMaxLevel", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "th", "Frame", "x", "GetHolderMineSpeedLevel", ")", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "autoUpgradeRollMutation", " team power", "%$%s*([%d,%.]+)%s*(%a*)", "Pad", "Players", "table", "games/my_anime_mine/auto_research", ": badge says ", "core/dispatch", "Auto Research: ซื้อสูงสุดต่อรอบ", "')", "[AutoResearch] cheapest open node: ", "Auto Research: ซื้อสาย Zone", "core/config", " (", "46ba61f+", " ", "table", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "ไล่ซื้อทุกกี่วินาที", "[Scheduler] ", "none", "table", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ClickDetector", "%.3g", "core/teleport", "Auto Research: รอบเช็ค", "This is not a supported place.", "Players", "[AutoPlace] disabled", "", " (", "core/get_me", "core/ui", "Search functions...", "Players", "games/my_anime_mine/auto_sell", "Badge", "PlotIndex", "core/run_job", "Auto Place: ปลดล็อกช่อง miner", "%$", "0 = ไม่จำกัด", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "TextBox", " ", "\n", "disabled", ", have ", ")", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Players", "#", "=empty", ": ", "Auto Mine: ความเร็ว", "Auto Claim: เก็บสูงสุดต่อรอบ", "[MekPro] build ", "อ่านป้ายบน chest ทุกกี่วินาที", " error: ", "table", "[AutoUpgrade] ", "[AutoChest] waiting: ", "maxed", "=", "Workspace", "GetHolderSpeedLevel", " -> ", "autoChest", "AutoMine", "M", "core/dispatch", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "autoPlotKeepMoney", "slot past the usable cap", " ", "OwnerUserId", "M", "unreadable", "AutoChest", "TextLabel", "", "X", "core/run_job", "cost", "Content", "CoreGui", "[AutoSell] enabled", " (money ", "write reported success but the line isn't in ", "ClaimableTiers failed", "Auto Mine: คลิกต่อรอบ", "Crew", "MekPro", "[Uu]nlock", "[AutoResearch] ", ")", "TextButton", " but no unclaimed reward in its Track", "empty", "MineWorld", "Anti-AFK", "[AutoChest] can't read the chest yet", "string", "games/my_anime_mine/get_plot_parts", "BodyVelocity", "CharMineSpeedMaxLevel", "] job failed: ", "menuScale", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "Speed", "core/parse_money", ") from ", "enabled", "games/my_anime_mine/auto_chest", "autoPlaceUnlock", " ", "autoUpgradePickaxeIndex", " pickaxe ", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/get_best_miner", "[AutoSell] disabled", "core/logger", " for ", "[AutoSell] MineSellItems ", "Workspace", "ฟาร์ม", "[AutoPlace] unlocked ", "OwnerUserId", "CharMaxLevel", "autoUpgradePriority", "games/my_anime_mine/auto_upgrade", ")", ")", "autoChestInterval", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "เช็คโซนทุกกี่วินาที", "slot not in the hotbar", "TextButton", "MekPro.log", " -> ", "", "en", "UIListLayout", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "MineRollMutation", "th", "TabBar", "core/logger", "SellStand", "[AutoClaim] claimed ", "", "Auto Sell: รอคิวได้นานสุด", "[BestMiner] slot ", "Auto Claim (Index)", "", "disabled", "games/my_anime_mine/auto_place", "autoUpgradePickaxe", "TextButton", "TextLabel", " (power ", "Damage", "%", "", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/game_api", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoPlace] nothing in the hotbar to place", "autoResearchCore", "%.3g", "MekPro", "Players", "^M(%d+)$", " (", "[AntiAFK] idle detected, simulating input", "fill", "auto", "^x(%d+)$", "autoResearchCheapestFirst", " (prompt says '", "core/logger", "s", "enabled", "table", "s", "Exclusive", "autoClaimInterval", "/config.json", "ย่อขยายเมนูทั้งชุด", " waited ", "MineSelectZone", "autoUpgradePickaxeIndex", "Locked miner slot", "?", "index rewards are disabled in this game build", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "", "autoMine", " ", "Humanoid", ")", "[Config] couldn't read ", "-", "nothing left to buy (", "Auto Upgrade Unit", "2026-09-21 12:49:42", "needs ", "autoResearchKeepMoney", "Text", "Auto Upgrade Plot (Zone)", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", " possible holder(s) - not buying more", "Auto Place: เปลี่ยนตัวอ่อนออก", "M", " (cost ", "[AutoResearch] ", "TextLabel", "Damage", "ตามเครื่อง", "Nodes", " (", "AutoSell", "costs ", "Hauler", "AutoSell", " still empty after placing ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "games/my_anime_mine/auto_claim", "Limited", "affordable - waiting for a turn", " items", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "autoUpgradeKeepMoney", "antiAfk", "UIListLayout", "core/anti_afk", " (", "MineUpgradeCharacter", "table", "Chest", "games/my_anime_mine/game_api", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "ProximityPrompt", "[AutoChest] collected ", "unreadable", "[%X] ", "autoSellInterval", "function", "GetHolderLevel", "[AutoPlot] ", "core/logger", "core/teleport", " items (now ", "Players", ")", " didn't go through (out of stock or too expensive)", "core/config", "works from anywhere - no teleport needed", "core/humanoid", "[AutoPlace] ", "[AutoChest] enabled (collects once >= ", "(", "[AutoChest] MineTakeChest had no effect (", " wouldn't take ", "autoClaimMaxPerPass", "autoPlaceInterval", "Zones", " costs ", "Frame", "Backpack", "neither appendfile nor writefile is available", "autoUpgradeMutationTarget", "core/logger", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "AutoPlace", "Shop Stand", " power ", ") needs ", "Auto Chest", ",", "[AntiAFK] disabled", "RunService", "number", ",", "TextLabel", "games/my_anime_mine/game_api", "ZoneRow", " pickaxe(s) for ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "AutoClaim", "AutoPlace", "open_menu_hint", "cost", "table", "s", "", "both Zone and Core buying are switched off", "Frame", "AutoUpgrade", "menuScale", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "ZoneBoardGui", "Header", "HumanoidRootPart", "[AutoPlace] picked up ", "Content", "AutoPlot", "ScreenGui", " ", "empty", "Players", ") ", "badge says ", "[AutoUpgrade] every stat on every miner is maxed", "[AutoPlace] holders: ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Hauler", "table", ") didn't take: ", " (", "[AutoUpgrade] pickaxe #", "core/logger", " reward(s) looked claimable but none went through", "s - carrying on without it", "function", ")", "autoSellProtectUnits", " costs ", "leaderstats", ") -> ", "MineSpeed", " | ", ")", "core/run_job", "MineEquipSlot", "autoSellMaxWait", ", ", "table", "core/get_place_id", "prompt disabled", "autoPlaceUpgradeWeak", "button has no size on screen", "ScrollingFrame", "UserInputService", " (", "core/debug_tab", ": State(", "core/parse_money", "ถูกสุดก่อน", "MinePower", "/", "ProximityPrompt", "autoMineClicks", "open_menu_hint", "ItemName", "Auto Plot: ใช้โซนล่าสุดเสมอ", "table", "🏠", "[AutoPlace] unlocking ", " not bought yet (", "[AutoChest] disabled", "ReplicatedStorage", "games/my_anime_mine/get_plot_parts", "", "s", "core/i18n", "table", "s", "enabled", "fill", "games/registry", "RunService", " -> ", "/", "games/my_anime_mine/get_current_plot", ": ", "table", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", " ", "write reported success but ", "autoPlace", "core/run_job", "core/logger", ")", "Frame", "Damage", "waiting", "", "autoUpgradeMaxPerPass", "string", "TextButton", "modules not loaded", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", ")", "%.0f", "en", "[AutoClaim] ", "AutoPlot", "BasePart", "enabled", "Workspace", "PickaxesStand", "[AutoUpgrade] ", "upgrade", "Auto Sell", "core/logger", "autoUpgradeInterval", "games/my_anime_mine/main", "", "autoChestMinItems", "AutoResearch", "search_placeholder", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "may need us at the stand - trying that", "Workspace", "Players", "nothing to place or upgrade", "TextLabel", "BasePart", "[Scheduler] ", "Auto Place: เพดานราคาปลดช่อง", "Humanoid", "core/get_place_id", "x", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "Frame", "core/get_leaderstat", "[AutoUpgrade] bought pickaxe #", "unlock", "Root", "Speed ก่อน", "([%d,%.]+)%s*(%a*)", "Auto Upgrade: สุ่ม Mutation", " team power, you have ", ": ", "อังกฤษ"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(135)] = function()
return function(import)
local log = import(S(229))
local Players = game:GetService(S(558))
local VirtualUser = game:GetService(S(4))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(149))
        connection = player.Idled:Connect(function()
            log(S(561))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(667))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(58)] = function()
return { Env = S(136), IsDev = false, Commit = S(399), BuiltAt = S(592) }
end
modules[S(78)] = function()
local VirtualInputManager = game:GetService(S(103))
local GuiService = game:GetService(S(87))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(728)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(397)] = function()
return function(import)
local HttpService = game:GetService(S(338))
local getPlaceId = import(S(725))
local log = import(S(196))
local ROOT_FOLDER = S(557)
local placeFolder = ROOT_FOLDER .. S(226) .. tostring(getPlaceId())
local filePath = placeFolder .. S(574)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(445) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(588) .. filePath .. S(327) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(369) .. filePath .. S(762) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(456)] = function()
return function(import)
    local getPlaceId = import(S(809))
    local registry = import(S(757))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(412))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(813)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(715))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(417)] = function()
local Players = game:GetService(S(420))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(586))
    local backpack = player:WaitForChild(S(656))
    return player, humanoid, backpack
end
return getMe
end
modules[S(287)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(645)] = function()
local Players = game:GetService(S(697))
local RunService = game:GetService(S(758))
local UserInputService = game:GetService(S(730))
local Workspace = game:GetService(S(511))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(808))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(189))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(490))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(785)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(805)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(186))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(752)] = function()
local Players = game:GetService(S(802))
local STRINGS = {
    en = {
        search_placeholder = S(419),
        open_menu_hint = S(106),
    },
    th = {
        search_placeholder = S(116),
        open_menu_hint = S(67),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(325)
    if locale:sub(1, 2):lower() == S(86) then
        return S(353)
    end
    return S(782)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(563) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(138)] = function()
local LOG_FILE = S(525)
local function log(message)
    local line = os.date(S(632)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(329))
            end
            appendfile(LOG_FILE, line .. S(431))
        elseif writefile then
            local existing = S(750)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(347))
        else
            error(S(657))
        end
        if not (isfile and readfile) then
            error(S(364))
        end
        if not isfile(LOG_FILE) then
            error(S(766) .. LOG_FILE .. S(92))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(474) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(324) .. LOG_FILE .. S(821) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(734)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(776) then
        return nil
    end
    local number, suffix = text:match(S(386))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(355), S(181))))
    if not value then
        return nil
    end
    if suffix ~= S(527) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(768)] = function()
return function(import)
local log = import(S(534))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(168) .. name .. S(492) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(28)] = function()
return function(import)
local RunService = game:GetService(S(668))
local log = import(S(659))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(806) .. task_.name .. S(444) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(404) .. task_.name .. S(179)
                    .. string.format(S(781), now - task_.busySince)
                    .. S(710))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(367) .. starved.name .. S(576)
            .. string.format(S(257), now - starved.waitingSince)
            .. S(224))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(206) .. (currentTask and currentTask.name or S(70)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(410)] = function()
local Players = game:GetService(S(640))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(690))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(418)] = function()
return function(import)
local i18n = import(S(128))
local UserInputService = game:GetService(S(323))
local THEME = {
    BrandName = S(478),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(180))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(158))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(280))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(545))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(222)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(268))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(250))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(580)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(105))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(774)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(729))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(621))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(52))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(156))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(603))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(465))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(140))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(130))
            if not valueLbl then
                valueLbl = Instance.new(S(201))
                valueLbl.Name = S(118)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(422))
            if not badge then
                badge = Instance.new(S(804))
                badge.Name = S(159)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(482))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(315))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(203))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(147))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(11)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(549)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(306))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(655))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(175))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(795)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(20))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(276))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(272))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(595)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(694))
    screenGui.Name = S(141)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(471))
    local main = Instance.new(S(771))
    main.Name = S(350)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(333))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(812))
    tabBar.Name = S(533)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(529))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(684))
    header.Name = S(689)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(22))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(93))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(671))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(583)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(777))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(467)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(524))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(589)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(429))
    searchBox.Name = S(134)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(798))
    searchBox.Text = S(7)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(379))
    content.Name = S(692)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(336))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(154))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(740))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(546))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(10)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(211) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(695) .. (item.Subtitle or S(240))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(243))
        footerLabel.Text = i18n.T(S(678))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(274)] = function()
return function(import)
local gameApi = import(S(627))(import)
local log = import(S(288))
local runGuarded = import(S(302))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(647) .. minItemsToCollect .. S(150))
    else
        log(S(747))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(631) then
            log(S(487))
            lastReported = S(463)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(484) then
            log(S(75))
            lastReported = S(696)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(46) then
            local capacity = gameApi.ChestCapacity()
            log(S(447) .. count .. S(737) .. want .. S(617)
                .. (capacity and (S(73) .. capacity .. S(719)) or S(344)))
            lastReported = S(773)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(464), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(630) .. count .. S(639) .. after .. S(360))
        else
            log(S(649) .. count
                .. S(759) .. tostring(after) .. S(587))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(614)] = function()
return function(import)
local gameApi = import(S(77))(import)
local log = import(S(637))
local runGuarded = import(S(468))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(205) .. (enabled and S(499) or S(542)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(365) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(536) .. reward.category .. S(233)
                .. tostring(reward.tierKey) .. S(297) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(783) .. #pending .. S(709))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(218), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(277)] = function()
return function(import)
local getCurrentPlot = import(S(551))
local getPlotParts = import(S(505))
local log = import(S(508))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(408), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(349) .. (enabled and S(786) or S(271)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(74)] = function()
return function(import)
local Players = game:GetService(S(361))
local getCurrentPlot = import(S(221))
local getPlotParts = import(S(749))
local getMinerSlots = import(S(152))(import)
local teleport = import(S(44))
local parseMoney = import(S(497))
local gameApi = import(S(672))(import)
local log = import(S(285))
local runGuarded = import(S(21))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(169)
local LOCKED_SLOT_TEXT = S(579)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(387))
    return pad and pad:FindFirstChildWhichIsA(S(738), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(26)) then
            local digits = descendant.Text:match(S(261))
            if digits then
                return tonumber((digits:gsub(S(666), S(6))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(183)) and descendant.Text:match(S(139)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(253) .. holder.Name .. S(746)
            .. gameApi.MinerSlots() .. S(281) .. gameApi.MaxMinerSlots() .. S(9)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(238)
    end
    if not prompt.Enabled then
        return true, S(726)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(609) .. price
    end
    local action = prompt.ActionText or S(266)
    if action:match(S(479)) or action:match(S(61)) or action:match(S(334)) then
        return true, S(178) .. action .. S(121)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(172))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(251) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(132) .. why .. S(712))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(438))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(449) .. prompt.ObjectText
                    .. S(648) .. shown .. S(160) .. math.floor(base) .. S(382))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(718))
    if line ~= lastSurvey then
        log(S(701) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(562), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(790), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(815), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(646) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(414))
    else
        lastAttempt = 0
        log(S(675))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(613),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(523)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(459)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(252)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(554))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(263) .. holder.Name .. S(166))
            return false
        end
        log(S(2) .. candidate.slot .. S(560) .. candidate.name
            .. S(705) .. tostring(why))
    end
    if not miner then
        log(S(157))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(357) .. holder.Name .. S(612) .. miner.name)
        return false
    end
    log(S(202) .. miner.name .. S(547) .. miner.power
        .. S(204) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(30) .. entry.name .. S(101) .. entry.holder.Name
            .. S(566) .. tostring(entry.prompt.ActionText) .. S(394))
        return false
    end
    log(S(691) .. entry.name .. S(25) .. entry.power
        .. S(498) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(85) .. pad.holder.Name .. S(174)
                .. tostring(pad.priceText))
        else
            log(S(513) .. pad.holder.Name
                .. S(31) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(803))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(756) then
        runGuarded(S(354), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(345) then
        log(S(745) .. target.holder.Name
            .. S(509) .. tostring(target.priceText))
        runGuarded(S(661), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(269) .. target.holder.Name .. S(439) .. target.name
        .. S(57) .. target.power .. S(716) .. best.name .. S(398) .. best.power .. S(518))
    runGuarded(S(677), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(49)] = function()
return function(import)
local gameApi = import(S(371))(import)
local parseMoney = import(S(48))
local log = import(S(129))
local runGuarded = import(S(720))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(362) .. (enabled and S(95) or S(432)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(426)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(363) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(340) .. deepestKey .. S(307))
        return true
    end
    log(S(368) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(225) .. gameApi.ZoneDisplayName(zoneKey) .. S(714)
                .. math.floor(price) .. S(190) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(66) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(623) .. zoneKey .. S(481))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(91) then
            reportedLocked = S(270)
            log(S(636) .. zoneKey .. S(60)
                .. gameApi.ZoneDisplayName(currentKey) .. S(88)
                .. done .. S(374) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(693), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(291)] = function()
return function(import)
local gameApi = import(S(215))(import)
local log = import(S(298))
local runGuarded = import(S(424))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(480) .. (enabled and S(755) or S(279)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(119) .. node.zoneKey .. S(765) .. node.id
                    .. S(731) .. node.family .. S(698) .. node.level .. S(452) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(326) .. node.id
                    .. S(706) .. node.family .. S(24) .. node.level .. S(526) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(602) .. blocked
                .. S(187))
        elseif powerBlocked then
            log(S(43) .. powerBlocked.zoneKey .. S(585) .. powerBlocked.id
                .. S(33) .. powerBlocked.family .. S(664)
                .. math.floor(powerBlocked.powerReq) .. S(820)
                .. math.floor(powerBlocked.power)
                .. S(219))
        else
            log(S(104)
                .. S(473) .. math.floor(gameApi.GetMoney()) .. S(434))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(395) .. nearest.zoneKey
                    .. S(342) .. nearest.id .. S(416) .. nearest.family .. S(177)
                    .. nearest.level .. S(760) .. nearest.max
                    .. S(654) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(683)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(309),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(301))
        end
        if not buyCore then
            table.insert(parts, S(155))
        end
        if #parts == 0 then
            return S(146)
        end
        return S(590) .. table.concat(parts, S(723)) .. S(264)
    end
    local why
    if nearest.cost > budget then
        why = S(148) .. string.format(S(556), nearest.cost)
            .. S(433) .. string.format(S(207), money)
    elseif nearest.powerOk == false then
        why = S(593) .. string.format(S(409), nearest.powerReq) .. S(385)
    else
        why = S(616)
    end
    return S(292) .. nearest.zoneKey .. S(430) .. tostring(nearest.id)
        .. S(607) .. tostring(nearest.family) .. S(237) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(185) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(797), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(241)] = function()
return function(import)
local Workspace = game:GetService(S(450))
local gameApi = import(S(83))(import)
local teleport = import(S(303))
local log = import(S(59))
local runGuarded = import(S(319))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(507))
    else
        lastAttempt = 0
        log(S(472))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(535))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(629), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(510)
                .. (sold and S(197)
                         or S(198)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(80) .. (sold and (S(191) .. #keys .. S(248)) or S(305)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(608), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(517)] = function()
return function(import)
local Workspace = game:GetService(S(801))
local gameApi = import(S(102))(import)
local teleport = import(S(638))
local log = import(S(769))
local runGuarded = import(S(69))(import)
local STATS = { S(604), S(289), S(717) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(125)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(446) .. (enabled and S(569) or S(286)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(753) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(469) or S(448)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(90) .. holderKey .. S(502) .. choice.stat .. S(310)
                        .. choice.info.level .. S(275) .. after.level
                        .. S(165) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(679) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(788))
    local shop = stand and stand:FindFirstChild(S(662))
    if not shop then
        return nil
    end
    if shop:IsA(S(200)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(232), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(313)
                .. (bought and S(644)
                           or S(800)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(29))
        end
    end
    if bought then
        log(S(814) .. pickaxe.index .. S(249) .. pickaxe.name
            .. S(76) .. pickaxe.multiplier .. S(519))
        return true
    end
    log(S(707) .. pickaxe.index .. S(400) .. pickaxe.name
        .. S(642))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(242) .. holderKey .. S(504)
                    .. tostring(worn or S(234)) .. S(170) .. wanted.name)
            else
                log(S(789) .. holderKey .. S(650)
                    .. wanted.name .. S(331) .. tostring(nowWorn or S(405)) .. S(282))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(36) .. holderKey .. S(214) .. variant
                .. S(99) .. after .. S(601) .. cost .. S(770))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(53) .. gameApi.OwnedPickaxeCount()
                    .. S(674) .. gameApi.MaxMinerSlots()
                    .. S(598))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(660))
        elseif maxed > 0 then
            log(S(700))
        else
            log(S(296))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(267), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(220)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(748))
local Workspace = game:GetService(S(787))
local log = import(S(567))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(337), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(164) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(13))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(724) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(703) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(151) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(570) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(610) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(559))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(359) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(100))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(16), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(763) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(389) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(231) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(34), keys)
end
function api.EquipSlot(slot)
    return send(S(721), slot == S(283) and S(346) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(14), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(304), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(531), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(65), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(704)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(413)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(736))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(634) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(244) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(577), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(485))
    plots = plots and plots:FindFirstChild(S(72))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(388)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(461)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(131))
    local gui = board and board:FindFirstChild(S(688))
    local root = gui and gui:FindFirstChild(S(816))
    local list = root and root:FindFirstChild(S(161))
    local row = list and list:FindFirstChild(S(673) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(94)) and descendant.Text ~= S(466) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(711) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(624), characterKey, stat == S(97) and S(145) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(635), max = S(515), cost = S(47) },
    Speed = { level = S(451), max = S(376), cost = S(341) },
    MineSpeed = { level = S(381), max = S(491), cost = S(217) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(230) or charName == S(537) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(330) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(669) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(209) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(401) and type(holder.CharName) == S(321) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(615), S(572) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(680) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(778)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(366)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(581)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(406) then
        return {}, S(475)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(351)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(743) or type(state.Track) ~= S(625) then
                table.insert(notes, tabKey .. S(733) .. tostring(category) .. S(193))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(391) .. waiting
                        .. S(483))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(699) .. total .. S(194)
            .. (#notes > 0 and (S(212) .. table.concat(notes, S(255))) or S(541))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(488) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(506)] = function()
return function(import)
local Players = game:GetService(S(5))
local gameApi = import(S(552))(import)
local log = import(S(708))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(818))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(670), S(415)))) or 0
    if suffix ~= S(112) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(213))
    local mineGui = playerGui and playerGui:FindFirstChild(S(18))
    local hotbar = mineGui and mineGui:FindFirstChild(S(227))
    local content = hotbar and hotbar:FindFirstChild(S(126))
    return content and content:FindFirstChild(S(192))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(3)) then
            local count = descendant.Text:match(S(564))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(137))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(470))
    local nameLabel = content and content:FindFirstChild(S(741))
    local damageLabel = content and content:FindFirstChild(S(772))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(550) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(37) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(539) .. slot .. S(127) .. info.name
                        .. S(663) .. info.power .. S(98) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(761)] = function()
local Players = game:GetService(S(436))
local Workspace = game:GetService(S(110))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(295))
    local plots = mineWorld and mineWorld:FindFirstChild(S(71))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(514)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(489)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(23)),
        Nodes = plot:FindFirstChild(S(606)),
        Chest = plot:FindFirstChild(S(626)),
        Zones = plot:FindFirstChild(S(653)),
        Crew = plot:FindFirstChild(S(477)),
        Loot = plot:FindFirstChild(S(35)),
        PlotIndex = plot:GetAttribute(S(423)),
        ChestCapacity = plot:GetAttribute(S(316)),
    }
end
return getPlotParts
end
modules[S(56)] = function()
return function(import)
    local createWindow = import(S(373))(import)
    local i18n = import(S(84))
    local antiAfk = import(S(622))(import)
    local buildEnv = import(S(8))
    local scheduler = import(S(235))(import)
    local config = import(S(643))(import)
    local log = import(S(792))
    log(S(442) .. buildEnv.Commit .. S(320) .. buildEnv.Env
        .. S(144) .. buildEnv.BuiltAt .. S(780))
    local window = createWindow({
        Subtitle = S(39),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(123))(import)
    local autoPlace = import(S(543))(import)
    local autoChest = import(S(500))(import)
    local autoSell = import(S(421))(import)
    local autoUpgrade = import(S(63))(import)
    local autoResearch = import(S(390))(import)
    local autoClaim = import(S(51))(import)
    local autoPlot = import(S(312))(import)
    local gameApi = import(S(335))(import)
    scheduler.RegisterStandalone(S(454), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(173), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(256), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(685), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(262), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(676), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(784), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(109), 20)
    scheduler.RegisterExclusive(S(611), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(512), S(744))
    persistentToggle(farmTab:AddCard(
        S(328),
        S(171)
    ), S(584), autoMine.SetEnabled)
    persistentSlider(farmTab, S(440), S(68),
        1, 30, S(273), 5, autoMine.SetCyclesPerSecond, S(278))
    persistentSlider(farmTab, S(476), S(27),
        1, 10, S(739), 1, autoMine.SetClicksPerCycle, S(810))
    persistentToggle(farmTab:AddCard(
        S(246),
        S(317)
    ), S(767), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(370), S(582),
        1, 60, S(652), 3, autoPlace.SetCheckInterval, S(17))
    persistentToggle(
        farmTab:AddCard(S(599),
            S(122)),
        S(727), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(425),
            S(779)),
        S(501), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(807), S(427),
        0, 500, S(210), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(462))
    persistentToggle(farmTab:AddCard(
        S(665),
        S(457)
    ), S(453), autoChest.SetEnabled)
    persistentSlider(farmTab, S(120), S(435),
        50, 3000, S(796), 100, autoChest.SetMinItems, S(339))
    persistentSlider(farmTab, S(113), S(443),
        1, 60, S(520), 3, autoChest.SetCheckInterval, S(322))
    persistentToggle(farmTab:AddCard(
        S(791),
        S(50)
    ), S(115), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(811),
            S(618)),
        S(713), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(133), S(163),
        5, 300, S(633), 20, autoSell.SetCheckInterval, S(568))
    farmTab:AddSlider(S(538),
        S(494),
        5, 120, sellMaxWait, function(value)
            config.Set(S(722), value)
        end, S(571))
    persistentToggle(farmTab:AddCard(
        S(591),
        S(687)
    ), S(54), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(64), S(553),
        5, 300, S(793), 20, autoUpgrade.SetInterval, S(751))
    persistentSlider(farmTab, S(216), S(377),
        1, 20, S(775), 5, autoUpgrade.SetMaxPerPass, S(208))
    persistentSegmented(
        farmTab,
        { S(290), S(343), S(817), S(299) },
        { S(81), S(548), S(496), S(38) },
        S(516), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(375), S(702),
        0, 500, S(619), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(19))
    persistentToggle(
        farmTab:AddCard(S(223),
            S(114)),
        S(544), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(195) .. pickaxe.multiplier .. S(641)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(259)
            end
            return S(437) .. index .. S(460) .. (pickaxeLabels[index] or S(799))
        end
        local savedPick = config.Get(S(503), 0)
        pickaxeCard = farmTab:AddSlider(
            S(300),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(578), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(682)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(372),
            S(530)),
        S(62), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(176),
            S(358)),
        S(15), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(819),
            S(521)),
        S(384), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(143), S(597),
        1, 20, S(658), 2, autoUpgrade.SetMutationTarget, S(380))
    persistentSlider(farmTab, S(32), S(107),
        0, 500, S(254), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(600))
    persistentToggle(farmTab:AddCard(
        S(55),
        S(348)
    ), S(199), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(411), S(403),
        10, 600, S(82), 30, autoResearch.SetCheckInterval, S(754))
    persistentSlider(farmTab, S(393), S(407),
        1, 20, S(142), 5, autoResearch.SetMaxPerPass, S(332))
    persistentSlider(farmTab, S(108), S(628),
        0, 500, S(594), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(311))
    persistentToggle(
        farmTab:AddCard(S(396),
            S(124)),
        S(318), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(293),
            S(383)),
        S(555), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(258),
            S(495)),
        S(352), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(735), S(265) },
        { true, false },
        S(565), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(540),
        S(402)
    ), S(45), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(89), S(428),
        5, 300, S(573), 30, autoClaim.SetCheckInterval, S(681))
    persistentSlider(farmTab, S(441), S(228),
        1, 30, S(651), 10, autoClaim.SetMaxPerPass, S(308))
    persistentToggle(farmTab:AddCard(
        S(596),
        S(167)
    ), S(239), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(79), S(522),
        5, 300, S(96), 30, autoPlot.SetCheckInterval, S(117))
    persistentToggle(
        farmTab:AddCard(S(162),
            S(294)),
        S(184), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(742),
            S(356)),
        S(188), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(260), S(764),
        0, 500, S(458), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(455))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(732))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(314), S(41))
    persistentToggle(
        settingsTab:AddCard(S(486), S(40)),
        S(620),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(493), 100)
    settingsTab:AddSlider(S(245), S(575), 50, 150, savedScale, function(value)
        config.Set(S(686), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(111), 1)
    settingsTab:AddSegmented({ S(605), S(822), S(236) }, savedLanguage, function(index, value)
        local code = ({ S(42), S(1), S(378) })[index]
        config.Set(S(12), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(153), S(528), S(532) })[savedLanguage] or S(284))
    log(S(247) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(182)] = function()
return {
    [79389059854988] = S(794),
}
end
import(S(392))(import)
