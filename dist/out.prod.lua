-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {" | ", "dearest", "Auto Claim: รอบเช็ค", "[AntiAFK] idle detected, simulating input", "AutoPlace", "Auto Mine: คลิกต่อรอบ", "Loot", "MineEquipPickaxe", "[AutoPlace] Equip Best: hotbar's strongest ", "UIStroke", "games/my_anime_mine/auto_research", "Damage ก่อน", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "autoUpgradePickaxeIndex", "Limited", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "[AntiAFK] disabled", "[Scheduler] ", "autoPlaceUnlockBudget", "Workspace", "unlock", "string", "", "core/ui", "AutoResearch", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "table", "core/dispatch", "[AutoPlace] holders: ", ": ", "[AutoClaim] ", "s", "table", " not bought yet (", "dearest", "[AutoChest] waiting: ", "autoPlotLatest", "Players", "Auto Upgrade Plot (Zone)", " ", "AutoResearch", " (", "GetHolderLevel", " -> ", "", "/", "Frame", "ไล่อัปเกรด miner ทุกกี่วินาที", "%$%s*([%d,%.]+)%s*(%a*)", "Auto Place: เปลี่ยนตัวอ่อนออก", "core/logger", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "ยิง Equip Best ทุกกี่วินาที", "autoResearchCheapestFirst", "antiAfk", " pickaxe(s) for ", "[Logger] failed to write ", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "RunService", "[AutoPlace] drifted out of range of ", "both Zone and Core buying are switched off", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "[AutoResearch] ", "[Bb]uy", "^([%d,%.]+)%s*(%a*)$", "AutoMine", "core/teleport", "auto", "core/logger", "autoSellMaxWait", "auto", "en", "autoUpgradePriority", "games/my_anime_mine/get_current_plot", "table", "disabled", "games/my_anime_mine/get_plot_parts", "HumanoidRootPart", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "core/logger", "enabled", "Workspace", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "", ": ", "games/my_anime_mine/game_api", "unlock", "core/build_env", "index rewards are disabled in this game build", "[AutoClaim] idle: ", "[AutoPlot] unlocked ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "[AutoPlot] ", "search_placeholder", ") on ", "core/logger", "[AutoUpgrade] ", "Plots", "TextButton", "เช็คโซนทุกกี่วินาที", "TextLabel", " while selecting", "Content", "Chest", "/", "อ่านป้ายบน chest ทุกกี่วินาที", "core/build_env", "games/my_anime_mine/game_api", "[AutoUpgrade] ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", ")", "", " owned)", "dearest", "leaderstats", "button has no size on screen", "ValueLabel", "AutoPlot", "off", "[AutoPlace] ", " (", "ค้นหาฟังก์ชัน...", "Search", " research is ", "UIListLayout", "x", "ปุ่มเปิดเมนู", "MineSpeed", "[AutoPlace] ", "autoResearchMaxPerPass", "autoPlace", "Workspace", " for ", "[AutoUpgrade] every stat on every miner is maxed", "autoSellProtectUnits", "Players", "", "Auto Research: ซื้อสาย Zone", "", " costs ", "MineWorld", "core/run_job", "autoUpgradeKeepMoney", "cheapest", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "AutoChest", "games/my_anime_mine/auto_sell", "core/scheduler", "SellStand", "games/my_anime_mine/get_current_plot", " (", "UICorner", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[AutoSell] MineSellItems ", "th", " ", " waited ", ")", "core/logger", "GuiService", " stack(s)", " (power ", " waited ", "sale didn't go through", "s", "MineTakeChest", "ถูกสุดก่อน", "'", "prod", " still locked after paying ", "Modules", "UIPadding", "Auto Claim: เก็บสูงสุดต่อรอบ", "core/get_leaderstat", "%.0f", "[AutoResearch] Core ", "Frame", "core/ui", "unreadable", "[AutoUpgrade] ", "s", " -> ", "MekPro", "/", "CoreGui", "core/click", "games/my_anime_mine/auto_plot", "empty", " doesn't exist", "autoResearchInterval", "UserInputService", "core/debug_tab", "autoUpgradePickaxe", "%.0f", "Frame", "core/spend_policy", "core/logger", "works from anywhere - dropping the teleport", "MineEquipSlot", "GetCharacterSpeedUpgradeCost", "TextButton", "ItemName", "Backpack", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "table", "TextButton", "ต่ำสุดก่อน", "games/my_anime_mine/auto_research", "th", "TextLabel", "s", "Mining", "Frame", "s", "autoResearchCoreLast", "Damage", "dearest", "AutoUpgrade", " fields: ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[AutoChest] can't read the chest yet", "autoPlaceInterval", "core/humanoid", "disabled", "core/run_job", "HotbarFrame", "write reported success but the line isn't in ", "autoSellMaxWait", "core/run_job", "cost", "[AutoPlot] couldn't switch to ", "", "games/my_anime_mine/auto_upgrade", "MineBuySkill", "MinePower", "Content", "", "[AutoMine] ", "TextButton", "Speed", "[AutoUpgrade] bought pickaxe #", "%", "ScrollingFrame", "OwnerUserId", "Damage", "menuScale", "[AutoPlace] ", "games/registry", "Auto Claim (Index)", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "cost", "research", "Empty miner slot", "s", "[AutoUpgrade] ", "0 = ไม่จำกัด", "AutoClaim", "TextButton", " (chest holds ", " is no better after the swap (", "Hauler", "[SpendPolicy] ", " thing available first", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", ")", "AutoPlot", "%.3g", "Auto Research: ซื้อสาย Core", "slot ", ") ", "ProximityPrompt", ") needs ", "empty", "([%d,%.]+)%s*(%a*)", "AutoPlot", ") ", "GetHolderMineSpeedLevel", ")", "games/my_anime_mine/auto_place", "List", " didn't go through (out of stock or too expensive)", "Content", "", "games/my_anime_mine/game_api", "", "Workspace", "games/my_anime_mine/main", ") from ", " wouldn't take ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "needs ", "core/config", "[Scheduler] ", "sold ", "ChestCapacity", "core/logger", " - ", "works from anywhere - no teleport needed", "autoSellInterval", "BasePart", "Open menu", "X", "CharMaxLevel", "M", "cheapest", " (", "autoChestInterval", "=", "Mutation: เพดานราคาต่อครั้ง", " refused - ", "%$", "CharSpeedMaxLevel", "fill", "Auto Research (Plot)", "TextBox", "en", "affordable - waiting for a turn", "[AutoPlace] couldn't hold anything - not pressing E", "costs ", "table", "Auto Place: รอบดึงตัวเก่ง", "Auto Upgrade: กันเงินไว้", "core/parse_money", "s - carrying on without it", "table", "Auto Upgrade Unit", "อังกฤษ", "games/my_anime_mine/auto_claim", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "off - every automation spends on its own", "\n", " -> ", "Players", "", "autoMine", " for ", "", "waiting", "autoUpgradeRollMutation", "[AutoResearch] ", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "%.3g", " items)", "core/logger", "core/spend_policy", "table", "[AutoUpgrade] MineBuyPickaxe ", "enabled", " team power", "MineBuyPickaxe", "Players", "Speed", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "core/i18n", " ", "core/spend_policy", "GetHolderSpeedLevel", "function", "core/logger", " (", "[AutoPlace] couldn't pick up ", "GetCharacterMineSpeedUpgradeCost", "string", "action '", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "Zone buying is off", "ZoneBoard", "needs us at the stand - keeping the teleport", "[AutoPlot] ", " x", "ValueLabel", "games/my_anime_mine/get_plot_parts", " (still ", "neither appendfile nor writefile is available", "no isfile/readfile available to verify the write", "ZoneRow", " -> ", "games/my_anime_mine/get_current_plot", "[AutoResearch] ", "[AutoPlace] disabled", "disabled", "every open node is maxed", "Frame", "CoreUI", "games/my_anime_mine/game_api", "HumanoidRootPart", "", "x", "may need us at the stand - trying that", "แพงสุดก่อน", "MineSpeed", " pickaxe ", "VirtualInputManager", "open_menu_hint", " ชิ้น", " ", "autoUpgrade", " costs ", "Frame", "ItemFrame", "none", ", base ", "core/logger", "", "AutoResearch", "OwnerUserId", "TextLabel", "core/i18n", "Crew", "cheapest open node ", "RunService", "M", "open_menu_hint", "TextButton", "=locked(", "autoPlotInterval", "M", "M", "Auto Upgrade: Pickaxe ที่จะซื้อ", "UIScale", " items (now ", "[Uu]nlock", "#", "s - jumping the queue", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", ")", "TextLabel", "[AutoChest] enabled (collects once >= ", " items", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "TextLabel", " (", "AutoPlace", "AutoResearch", "[AutoPlace] ", " -> ", "[AutoResearch] cheapest open node: ", "waiting", "games/my_anime_mine/get_best_miner", "Mining ก่อน", "Speed ก่อน", "(nothing to do)", "BasePart", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", ",", "modules not loaded", "[GameAPI] SendEvent failed: ", "", "no mine data", "BasePart", "", "All", "Auto Place: เพดานราคาปลดช่อง", "TextLabel", " (x", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "autoChest", "[AutoUpgrade] ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Auto Plot: รอบเช็ค", "BodyVelocity", "Auto Chest", "core/get_place_id", "autoPlotBuy", "Humanoid", "(", "core/run_job", "[AutoPlace] unlocking ", "[AutoResearch] nothing open and affordable right now", "table", "[AutoClaim] ", "AutoUpgrade", "core/anti_afk", "autoPlotKeepMoney", "Search functions...", "autoPlot", "core/anti_afk", " #", "VirtualUser", ")", ") didn't take: ", "Frame", "Auto Research: กันเงินไว้", "MineGui", "%.3g", "PickaxesStand", ")", "Zones", "", "core/run_job", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "[SpendPolicy] ", "unreadable", " (", "ถูกสุดก่อน", "Auto Mine: ความเร็ว", "2026-09-21 13:08:13", "s behind a purchase that never happened - letting it spend", "TextButton", "Core", "[AutoChest] disabled", "313921c+", "write reported success but ", "[AutoPlace] upgrading ", "", "TextLabel", "This is not a supported place.", "AutoPlace", "My Anime Mine · Mining Hub", "%.3g", " ", "autoUpgradeLimitPickaxes", "autoUpgradeMutationTarget", "table", "Container", "disabled", "TextLabel", "core/get_place_id", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "ตามเครื่อง", "Character", "games/registry", "[AutoChest] collected ", "TextLabel", " (", "slot past the usable cap", "autoResearchKeepMoney", "games/my_anime_mine/main", "core/config", "Frame", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " (x", "Text", "Players", "research", "games/my_anime_mine/game_api", "Holders", "Auto Sell: รอบขาย", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "table", "nothing claimable", "TextLabel", "Auto Place: รอบเช็ค", "needs ", "MineSpeed", "MineRollMutation", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "/config.json", "GetCharacterUpgradeCost", "games/my_anime_mine/auto_mine", "/", "fill", " -> ", "Badge", " ", "core/teleport", "Players", "games/my_anime_mine/get_plot_parts", "Hauler", "dearest", "AutoChest", "0 = ไม่จำกัด", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "%.3g", "table", "Auto Mine", "string", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", ")", "core/parse_money", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "games/my_anime_mine/auto_place", " (", "ClaimableTiers failed", " reward(s) looked claimable but none went through", "Auto Sell: รอคิวได้นานสุด", "] job failed: ", "HttpService", "games/my_anime_mine/auto_claim", "Frame", "Auto Research: รอบเช็ค", "AutoClaim", "[AutoPlace] unlocked ", "autoSell", ") -> ", " (", "[%X] ", "s", "AutoSell", "lowest", "autoPlaceUnlock", "buying the ", "Anti-AFK", "en", "autoResearch", " (prompt says '", "[AutoChest] chest is empty", "table", "core/get_me", "function", "TabBar", "แพงสุดก่อน", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "core/run_job", "%.3g", "AutoSell", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "TextLabel", ")", "autoUpgradeInterval", "^%$", ": ", "%.3g", " from ", "M", "[AutoResearch] ", " ", "MekPro", "ต่างคนต่างซื้อ", "Workspace", "autoResearchCore", "[AutoSell] disabled", ") - leaving it alone for ", "upgrade", "auto", " error: ", "autoPlaceUpgradeWeak", "languageIndex", "TextButton", "autoUpgradeMaxPerPass", "/", " has been busy for ", "games/my_anime_mine/game_api", "Players", "core/dispatch", "🏠", "Auto Place Miner", "games/my_anime_mine/game_api", "autoResearchZones", ", you have ", "[Scheduler] ", "core/logger", "^x(%d+)$", ": State(", ")", "", "enabled", "", "games/my_anime_mine/auto_mine", "ย่อขยายเมนูทั้งชุด", "[AutoSell] enabled", "Pad", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "auto", "PlotIndex", "[AutoPlace] placed ", "enabled", "core/spend_policy", "Auto Sell", "/", "games/my_anime_mine/auto_sell", "[", " (money ", "[AutoPlace] picked up ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "off", "[AntiAFK] enabled - watching Player.Idled", "table", "autoPlaceEquipBestInterval", "Badge", "[AutoPlot] ", "core/logger", "prompt disabled", "ลำดับการใช้เงินรวมทุกระบบ", "games/my_anime_mine/game_api", "ฟาร์ม", "TextLabel", " node(s) looked buyable but the server refused", "[Config] couldn't write ", "AutoPlace", "x", "lowest", "games/my_anime_mine/auto_plot", "core/logger", "off", "%.0f", "PlayerGui", "ขายทุกกี่วินาที", "AutoUpgrade", "core/logger", ",", " ", "x", "UIListLayout", "Header", "⚙", "=", "AutoUpgrade", "string", "", "spendOrder", "Locked miner slot", "UICorner", " team power, you have ", "ไล่ซื้อทุกกี่วินาที", "disabled", "languageIndex", "HumanoidRootPart", "s", "TextLabel", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "games/my_anime_mine/get_best_miner", "[BestMiner] slot ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Auto Plot: ซื้อโซนถัดไป", "CharMineSpeedMaxLevel", "games/my_anime_mine/game_api", "MineSelectZone", "Auto Place: ปลดล็อกช่อง miner", "AutoPlace", "^Swap out ", "", "ClickDetector", "; ", "core/spend_policy", "[AutoPlace] nothing in the hotbar to place", ", have ", "autoUpgradeEquipPickaxe", " (", "MineSellItems", "UIListLayout", "%$", "TextLabel", "autoUpgradeMutationBudget", "", "", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "locked slot", "Shop Stand", "^M(%d+)$", "menuScale", "ProximityPrompt", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "ไทย", "?", "table", "core/logger", "slot not in the hotbar", "[MekPro] build ", "autoMineClicks", "[Scheduler] turn: ", ")", "enabled", "Auto Plot: ใช้โซนล่าสุดเสมอ", "table", "[AutoResearch] idle: ", "core/logger", ")", "Auto Upgrade: รอบเช็ค", "Damage", "TextLabel", "AutoPlace", "Nodes", "", "autoClaim", "[AutoPlace] slot ", "AutoPlot", ")", "[AutoUpgrade] pickaxe #", "Players", "search_placeholder", "[AutoPlot] now mining ", "autoUpgradePickaxeIndex", "\n", "[AutoUpgrade] ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "core/get_place_id", "ReplicatedStorage", "core/teleport", " but no unclaimed reward in its Track", ": ", "Frame", "table", "ZoneBoardGui", ") gave nothing", "nothing to place or upgrade", "Players", "Auto Upgrade: สุ่ม Mutation", "Humanoid", "core/run_job", "nothing left to buy (", "GuiService", "[AutoChest] MineTakeChest had no effect (", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "UserInputService", "Auto Chest: รอบเช็ค", "badge says ", "", "Auto Chest: เกณฑ์เก็บ", "games/my_anime_mine/auto_chest", "[AutoClaim] claimed ", "[GameAPI] couldn't load the game's modules: ", "none", "AutoPlot", "core/scheduler", "')", "/", "[GameAPI] holder ", "s", "Plots", " ", "/s", "number", "Auto Research: ซื้อสูงสุดต่อรอบ", "Core buying is off", "games/my_anime_mine/game_api", "Root", " -> ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "dearest", "BodyPosition", "Mutation: หยุดเมื่อได้ x", "=empty", " (power ", "[AutoSell] ", "ScreenGui", "TextLabel", " possible holder(s) - not buying more", "MekPro.log", ")", "MineEquipBest", "Exclusive", "server didn't equip it", ": badge says ", "Main", " (cost ", "core/run_job", ") ", "Players", ") ", "ตั้งค่า", "core/teleport", "autoChestMinItems", "[MekPro] settings file: ", "autoClaimMaxPerPass", "core/spend_policy", " power ", "AutoPlace", "BasePart", " claimable but none could be resolved", "th", "Auto Upgrade: แจก Pickaxe ให้ miner", "Auto Plot: กันเงินไว้", "s", "core/parse_money", ", built ", "[AutoPlace] fireproximityprompt error: ", " mutation x", " ", " ", "maxed", "AutoUpgrade", "-", ": ", "string", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "autoClaimInterval", "core/i18n", "Text", " still empty after placing ", "games/my_anime_mine/auto_chest", "games/my_anime_mine/auto_upgrade", " ", "autoMineSpeed", "x", "core/logger", ",", "en-us", "ขนาดเมนู", " -> x", "MineWorld", " - upgrade the miners first", "th", "[Config] couldn't read ", "", "off", ")", "MineUpgradeCharacter", ", ", "autoPlaceEquipBest", "Frame"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(478)] = function()
return function(import)
local log = import(S(197))
local Players = game:GetService(S(635))
local VirtualUser = game:GetService(S(480))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(668))
        connection = player.Idled:Connect(function()
            log(S(4))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(17))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(88)] = function()
return { Env = S(169), IsDev = false, Commit = S(503), BuiltAt = S(498) }
end
modules[S(186)] = function()
local VirtualInputManager = game:GetService(S(394))
local GuiService = game:GetService(S(793))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(116)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(292)] = function()
return function(import)
local HttpService = game:GetService(S(579))
local getPlaceId = import(S(778))
local log = import(S(404))
local ROOT_FOLDER = S(619)
local placeFolder = ROOT_FOLDER .. S(661) .. tostring(getPlaceId())
local filePath = placeFolder .. S(549)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(515) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(886) .. filePath .. S(85) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(680) .. filePath .. S(613) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(636)] = function()
return function(import)
    local getPlaceId = import(S(464))
    local registry = import(S(523))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(508))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(174)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(115))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(600)] = function()
local Players = game:GetService(S(333))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(790))
    local backpack = player:WaitForChild(S(203))
    return player, humanoid, backpack
end
return getMe
end
modules[S(519)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(223)] = function()
local Players = game:GetService(S(352))
local RunService = game:GetService(S(59))
local UserInputService = game:GetService(S(796))
local Workspace = game:GetService(S(82))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(466))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(709))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(462))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(851)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(451)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(823))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(409)] = function()
local Players = game:GetService(S(841))
local STRINGS = {
    en = {
        search_placeholder = S(476),
        open_menu_hint = S(301),
    },
    th = {
        search_placeholder = S(122),
        open_menu_hint = S(127),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(880)
    if locale:sub(1, 2):lower() == S(155) then
        return S(885)
    end
    return S(595)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(655) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(69)] = function()
local LOG_FILE = S(831)
local function log(message)
    local line = os.date(S(588)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(736))
            end
            appendfile(LOG_FILE, line .. S(775))
        elseif writefile then
            local existing = S(45)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(331))
        else
            error(S(375))
        end
        if not (isfile and readfile) then
            error(S(376))
        end
        if not isfile(LOG_FILE) then
            error(S(504) .. LOG_FILE .. S(189))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(227) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(57) .. LOG_FILE .. S(782) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(857)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(22) then
        return nil
    end
    local number, suffix = text:match(S(49))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(446), S(452))))
    if not value then
        return nil
    end
    if suffix ~= S(388) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(468)] = function()
return function(import)
local log = import(S(685))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(663) .. name .. S(578) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(148)] = function()
return function(import)
local RunService = game:GetService(S(412))
local log = import(S(673))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(642) .. task_.name .. S(627) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(18) .. task_.name .. S(633)
                    .. string.format(S(175), now - task_.busySince)
                    .. S(324))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(293) .. starved.name .. S(157)
            .. string.format(S(687), now - starved.waitingSince)
            .. S(425))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(752) .. (currentTask and currentTask.name or S(443)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(659)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(691))
local policy = {}
local probes = {}
local order = {}
local mode = S(119)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(262) .. (mode == S(686)
        and S(330)
        or (S(593) .. mode .. S(263))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(667) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(305) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(822) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(493) .. name .. S(163)
            .. string.format(S(194), now - blockedSince[name])
            .. S(499))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(67)] = function()
local Players = game:GetService(S(771))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(387))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(24)] = function()
return function(import)
local i18n = import(S(355))
local UserInputService = game:GetService(S(191))
local GuiService = game:GetService(S(160))
local THEME = {
    BrandName = S(183),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(152))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(704))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(172))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(239))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(799)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(10))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(210))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(746)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(507))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(283)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(243))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(125))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(711))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(384))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(678))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(829))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(408))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(117))
            if not valueLbl then
                valueLbl = Instance.new(S(432))
                valueLbl.Name = S(372)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(671))
            if not badge then
                badge = Instance.new(S(101))
                badge.Name = S(555)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(258))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(483))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(195))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(201))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(737)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(242)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(177))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(531))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(500))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(723)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(213))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(695))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(206))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(534)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(828))
    screenGui.Name = S(385)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(185))
    local main = Instance.new(S(581))
    main.Name = S(837)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(421))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(47))
    tabBar.Name = S(602)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(732))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(783))
    header.Name = S(696)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(99))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(525))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(428))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(112)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(415))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(302)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(630))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(865)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(315))
    searchBox.Name = S(123)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(94))
    searchBox.Text = S(84)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(893))
    content.Name = S(236)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(400))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(609))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(395))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(762))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(871)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(337) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(512) .. (item.Subtitle or S(506))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(772))
        footerLabel.Text = i18n.T(S(414))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(801)] = function()
return function(import)
local gameApi = import(S(386))(import)
local log = import(S(296))
local runGuarded = import(S(229))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(429) .. minItemsToCollect .. S(344))
    else
        log(S(502))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(179) then
            log(S(221))
            lastReported = S(494)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(273) then
            log(S(598))
            lastReported = S(188)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(439) then
            local capacity = gameApi.ChestCapacity()
            log(S(36) .. count .. S(552) .. want .. S(430)
                .. (capacity and (S(259) .. capacity .. S(889)) or S(490)))
            lastReported = S(338)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(562), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(524) .. count .. S(422) .. after .. S(481))
        else
            log(S(794) .. count
                .. S(819) .. tostring(after) .. S(832))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(580)] = function()
return function(import)
local gameApi = import(S(284))(import)
local log = import(S(748))
local runGuarded = import(S(605))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(31) .. (enabled and S(81) or S(382)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(90) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(802) .. reward.category .. S(812)
                .. tostring(reward.tierKey) .. S(479) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(472) .. #pending .. S(576))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(583), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(650)] = function()
return function(import)
local getCurrentPlot = import(S(379))
local getPlotParts = import(S(373))
local log = import(S(878))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(724), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(238) .. (enabled and S(658) or S(517)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(279)] = function()
return function(import)
local Players = game:GetService(S(558))
local getCurrentPlot = import(S(74))
local getPlotParts = import(S(77))
local getMinerSlots = import(S(713))(import)
local teleport = import(S(780))
local parseMoney = import(S(323))
local gameApi = import(S(718))(import)
local log = import(S(643))
local runGuarded = import(S(791))(import)
local spendPolicy = import(S(357))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(253)
local LOCKED_SLOT_TEXT = S(703)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(653))
    return pad and pad:FindFirstChildWhichIsA(S(271), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(543)) then
            local digits, suffix = descendant.Text:match(S(65))
            if digits then
                local value = tonumber((digits:gsub(S(879), S(405))))
                if value then
                    if suffix ~= S(647) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(237)
    return (text:gsub(S(722), S(449)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(455)) and descendant.Text:match(S(612)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(269) .. holder.Name .. S(34)
            .. gameApi.MinerSlots() .. S(105) .. gameApi.MaxMinerSlots() .. S(113)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(739)
    end
    if not prompt.Enabled then
        return true, S(674)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(319) .. price
    end
    local action = prompt.ActionText or S(701)
    if action:match(S(423)) or action:match(S(64)) or action:match(S(733)) then
        return true, S(365) .. action .. S(168)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(78))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(859) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(416) .. why .. S(278))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(825))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(308) .. prompt.ObjectText
                    .. S(467) .. shown .. S(403) .. math.floor(base) .. S(759))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(1))
    if line ~= lastSurvey then
        log(S(29) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(553), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(625), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(509)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(87), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(436) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(381))
    else
        lastAttempt = 0
        log(S(715))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(250),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(749)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(527)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(835)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(727))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(60) .. holder.Name .. S(102))
            return false
        end
        log(S(767) .. candidate.slot .. S(151) .. candidate.name
            .. S(482) .. tostring(why))
    end
    if not miner then
        log(S(318))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(247) .. holder.Name .. S(872) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(657) .. miner.name .. S(826) .. miner.power
        .. S(95) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(362) .. entry.name .. S(615) .. entry.holder.Name
            .. S(597) .. tostring(entry.prompt.ActionText) .. S(807))
        return false
    end
    log(S(665) .. entry.name .. S(162) .. entry.power
        .. S(288) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(129) .. pad.holder.Name .. S(170)
                .. tostring(pad.priceText))
        else
            log(S(584) .. pad.holder.Name
                .. S(336) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(120) .. holderName .. S(260)
            .. string.format(S(565), before) .. S(182) .. string.format(S(606), after)
            .. S(624) .. SWAP_COOLDOWN .. S(32))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(9)
            .. string.format(S(343), bestBefore) .. S(378)
            .. string.format(S(267), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(763), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(787))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(313) then
        runGuarded(S(5), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(21) then
        log(S(469) .. target.holder.Name
            .. S(133) .. tostring(target.priceText))
        runGuarded(S(681), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(505) .. target.holder.Name .. S(866) .. target.name
        .. S(42) .. target.power .. S(586) .. best.name .. S(361) .. best.power .. S(111))
    runGuarded(S(850), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(684)] = function()
return function(import)
local gameApi = import(S(817))(import)
local parseMoney = import(S(571))
local log = import(S(360))
local runGuarded = import(S(491))(import)
local spendPolicy = import(S(196))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(672) .. (enabled and S(349) or S(224)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(311)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(773) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(587) .. deepestKey .. S(265))
        return true
    end
    log(S(231) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(266)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(93) .. gameApi.ZoneDisplayName(zoneKey) .. S(399)
                .. math.floor(price) .. S(641) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(91) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(433) .. zoneKey .. S(753))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(536) then
            reportedLocked = S(252)
            log(S(370) .. zoneKey .. S(310)
                .. gameApi.ZoneDisplayName(currentKey) .. S(124)
                .. done .. S(808) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(275)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(805), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(208)] = function()
return function(import)
local gameApi = import(S(676))(import)
local log = import(S(159))
local runGuarded = import(S(225))(import)
local spendPolicy = import(S(848))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(380) .. (enabled and S(648) or S(707)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(340) .. node.zoneKey .. S(862) .. node.id
                    .. S(730) .. node.family .. S(842) .. node.level .. S(44) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(176) .. node.id
                    .. S(526) .. node.family .. S(840) .. node.level .. S(437) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(617) .. blocked
                .. S(679))
        elseif powerBlocked then
            log(S(63) .. powerBlocked.zoneKey .. S(861) .. powerBlocked.id
                .. S(495) .. powerBlocked.family .. S(272)
                .. math.floor(powerBlocked.powerReq) .. S(705)
                .. math.floor(powerBlocked.power)
                .. S(884))
        else
            log(S(470)
                .. S(664) .. math.floor(gameApi.GetMoney()) .. S(570))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(438) .. nearest.zoneKey
                    .. S(40) .. nearest.id .. S(574) .. nearest.family .. S(270)
                    .. nearest.level .. S(632) .. nearest.max
                    .. S(140) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(61)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(501),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(367))
        end
        if not buyCore then
            table.insert(parts, S(816))
        end
        if #parts == 0 then
            return S(383)
        end
        return S(792) .. table.concat(parts, S(891)) .. S(488)
    end
    local why
    if nearest.cost > budget then
        why = S(545) .. string.format(S(614), nearest.cost)
            .. S(728) .. string.format(S(511), money)
    elseif nearest.powerOk == false then
        why = S(291) .. string.format(S(486), nearest.powerReq) .. S(350)
    else
        why = S(317)
    end
    return S(411) .. nearest.zoneKey .. S(618) .. tostring(nearest.id)
        .. S(121) .. tostring(nearest.family) .. S(276) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(757) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(217) and cost > best)
            or (mode ~= S(35) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(41)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(435), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(662)] = function()
return function(import)
local Workspace = game:GetService(S(20))
local gameApi = import(S(108))(import)
local teleport = import(S(844))
local log = import(S(80))
local runGuarded = import(S(142))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(623))
    else
        lastAttempt = 0
        log(S(652))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(149))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(743), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(154)
                .. (sold and S(198)
                         or S(369)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(827) .. (sold and (S(294) .. #keys .. S(161)) or S(164)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(590), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(874)] = function()
return function(import)
local Workspace = game:GetService(S(132))
local gameApi = import(S(537))(import)
local teleport = import(S(557))
local log = import(S(96))
local runGuarded = import(S(839))(import)
local spendPolicy = import(S(726))(import)
local STATS = { S(216), S(353), S(128) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(591)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(180) .. (enabled and S(754) or S(76)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(27) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(230) or S(863)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(97) .. holderKey .. S(156) .. choice.stat .. S(356)
                        .. choice.info.level .. S(332) .. after.level
                        .. S(46) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(251) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(487))
    local shop = stand and stand:FindFirstChild(S(740))
    if not shop then
        return nil
    end
    if shop:IsA(S(444)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(300), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(348)
                .. (bought and S(298)
                           or S(390)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(431))
        end
    end
    if bought then
        log(S(241) .. pickaxe.index .. S(693) .. pickaxe.name
            .. S(456) .. pickaxe.multiplier .. S(427))
        return true
    end
    log(S(770) .. pickaxe.index .. S(556) .. pickaxe.name
        .. S(281))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(776) .. holderKey .. S(393)
                    .. tostring(worn or S(804)) .. S(554) .. wanted.name)
            else
                log(S(109) .. holderKey .. S(289)
                    .. wanted.name .. S(374) .. tostring(nowWorn or S(402)) .. S(769))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(459) .. holderKey .. S(860) .. variant
                .. S(882) .. after .. S(838) .. cost .. S(610))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(864))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(255) .. gameApi.OwnedPickaxeCount()
                    .. S(56) .. gameApi.MaxMinerSlots()
                    .. S(830))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(83))
        elseif maxed > 0 then
            log(S(134))
        else
            log(S(13))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(2) and price > best)
            or (mode ~= S(561) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(690))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(473), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(634)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(779))
local Workspace = game:GetService(S(286))
local log = import(S(51))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(171), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(803) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(471))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(325) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(261) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(75) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(541) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(560) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(741))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(448) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(166))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(453), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(669) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(33) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(522) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(731), keys)
end
function api.EquipSlot(slot)
    return send(S(199), slot == S(334) and S(649) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(351), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(8), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(547), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(833))
end
function api.BuySkill(zoneKey, family)
    return send(S(234), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(599)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(788)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(235))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(601) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(320) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(719), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(883))
    plots = plots and plots:FindFirstChild(S(811))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(38)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(244)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(368))
    local gui = board and board:FindFirstChild(S(785))
    local root = gui and gui:FindFirstChild(S(818))
    local list = root and root:FindFirstChild(S(280))
    local row = list and list:FindFirstChild(S(377) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(734)) and descendant.Text ~= S(765) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(359) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(890), characterKey, stat == S(212) and S(546) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(43), max = S(303), cost = S(550) },
    Speed = { level = S(358), max = S(312), cost = S(200) },
    MineSpeed = { level = S(277), max = S(717), cost = S(363) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(568) or charName == S(137) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(698) .. tostring(value))
        end
        table.sort(fields)
        log(S(809) .. holderKey .. S(219) .. table.concat(fields, S(397)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(756) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(814) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(867) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(347) and type(holder.CharName) == S(364) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(15), S(834) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(566) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(447)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(450)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(89)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(205) then
        return {}, S(575)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(542)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(747) or type(state.Track) ~= S(784) then
                table.insert(notes, tabKey .. S(645) .. tostring(category) .. S(786))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(836) .. waiting
                        .. S(781))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(798) .. total .. S(852)
            .. (#notes > 0 and (S(297) .. table.concat(notes, S(725))) or S(232))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(700) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(440)] = function()
return function(import)
local Players = game:GetService(S(535))
local gameApi = import(S(86))(import)
local log = import(S(345))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(274))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(692), S(285))))
    if not value then
        return nil
    end
    if suffix ~= S(139) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(688))
    local mineGui = playerGui and playerGui:FindFirstChild(S(485))
    local hotbar = mineGui and mineGui:FindFirstChild(S(226))
    local content = hotbar and hotbar:FindFirstChild(S(103))
    return content and content:FindFirstChild(S(516))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(518)) then
            local count = descendant.Text:match(S(644))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(401))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(282))
    local nameLabel = content and content:FindFirstChild(S(202))
    local damageLabel = content and content:FindFirstChild(S(245))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(23) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(184) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(714) .. slot .. S(30) .. info.name
                        .. S(849) .. info.power .. S(371) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(150)] = function()
local Players = game:GetService(S(136))
local Workspace = game:GetService(S(621))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(141))
    local plots = mineWorld and mineWorld:FindFirstChild(S(98))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(407)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(559)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(538)),
        Nodes = plot:FindFirstChild(S(764)),
        Chest = plot:FindFirstChild(S(104)),
        Zones = plot:FindFirstChild(S(489)),
        Crew = plot:FindFirstChild(S(410)),
        Loot = plot:FindFirstChild(S(7)),
        PlotIndex = plot:GetAttribute(S(656)),
        ChestCapacity = plot:GetAttribute(S(295)),
    }
end
return getPlotParts
end
modules[S(287)] = function()
return function(import)
    local createWindow = import(S(178))(import)
    local i18n = import(S(870))
    local antiAfk = import(S(474))(import)
    local buildEnv = import(S(107))
    local scheduler = import(S(806))(import)
    local spendPolicy = import(S(346))(import)
    local config = import(S(530))(import)
    local log = import(S(758))
    log(S(750) .. buildEnv.Commit .. S(306) .. buildEnv.Env
        .. S(858) .. buildEnv.BuiltAt .. S(158))
    local window = createWindow({
        Subtitle = S(510),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(551))(import)
    local autoPlace = import(S(573))(import)
    local autoChest = import(S(873))(import)
    local autoSell = import(S(147))(import)
    local autoUpgrade = import(S(233))(import)
    local autoResearch = import(S(11))(import)
    local autoClaim = import(S(328))(import)
    local autoPlot = import(S(187))(import)
    local gameApi = import(S(639))(import)
    scheduler.RegisterStandalone(S(66), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(146), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(721), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(218), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(25), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(257), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(118), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(228), 20)
    scheduler.RegisterExclusive(S(607), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(406), autoResearch.NextPrice)
    spendPolicy.Register(S(699), autoUpgrade.NextPrice)
    spendPolicy.Register(S(768), autoPlot.NextPrice)
    spendPolicy.Register(S(434), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(677), S(637))
    farmTab:AddSectionHeader(S(675))
    persistentSegmented(
        farmTab,
        { S(620), S(167), S(603) },
        { S(888), S(144), S(114) },
        S(702), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(567),
        S(52)
    ), S(335), autoMine.SetEnabled)
    persistentSlider(farmTab, S(497), S(777),
        1, 30, S(876), 5, autoMine.SetCyclesPerSecond, S(813))
    persistentSlider(farmTab, S(6), S(220),
        1, 10, S(751), 1, autoMine.SetClicksPerCycle, S(389))
    persistentToggle(farmTab:AddCard(
        S(638),
        S(445)
    ), S(131), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(544), S(532),
        1, 60, S(222), 3, autoPlace.SetCheckInterval, S(589))
    persistentToggle(
        farmTab:AddCard(S(50),
            S(62)),
        S(628), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(341),
            S(145)),
        S(892), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(321), S(53),
        5, 300, S(670), 60, autoPlace.SetEquipBestInterval, S(181))
    persistentToggle(
        farmTab:AddCard(S(720),
            S(329)),
        S(592), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(454), S(563),
        0, 500, S(19), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(413))
    persistentToggle(farmTab:AddCard(
        S(463),
        S(16)
    ), S(458), autoChest.SetEnabled)
    persistentSlider(farmTab, S(800), S(460),
        50, 3000, S(845), 100, autoChest.SetMinItems, S(396))
    persistentSlider(farmTab, S(797), S(106),
        1, 60, S(307), 3, autoChest.SetCheckInterval, S(214))
    persistentToggle(farmTab:AddCard(
        S(660),
        S(744)
    ), S(585), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(26),
            S(604)),
        S(135), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(539), S(689),
        5, 300, S(299), 20, autoSell.SetCheckInterval, S(211))
    farmTab:AddSlider(S(577),
        S(572),
        5, 120, sellMaxWait, function(value)
            config.Set(S(70), value)
        end, S(856))
    persistentToggle(farmTab:AddCard(
        S(326),
        S(92)
    ), S(398), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(760), S(48),
        5, 300, S(611), 20, autoUpgrade.SetInterval, S(810))
    persistentSlider(farmTab, S(820), S(654),
        1, 20, S(631), 5, autoUpgrade.SetMaxPerPass, S(682))
    persistentSegmented(
        farmTab,
        { S(207), S(12), S(442), S(441) },
        { S(683), S(761), S(240), S(392) },
        S(73), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(322), S(342),
        0, 500, S(143), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(419))
    persistentToggle(
        farmTab:AddCard(S(153),
            S(110)),
        S(193), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(533) .. pickaxe.multiplier .. S(646)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(520)
            end
            return S(424) .. index .. S(875) .. (pickaxeLabels[index] or S(666))
        end
        local savedPick = config.Get(S(774), 0)
        pickaxeCard = farmTab:AddSlider(
            S(420),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(14), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(887)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(58),
            S(366)),
        S(513), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(854),
            S(426)),
        S(729), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(789),
            S(492)),
        S(339), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(824), S(540),
        1, 20, S(514), 2, autoUpgrade.SetMutationTarget, S(877))
    persistentSlider(farmTab, S(309), S(256),
        0, 500, S(735), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(304))
    persistentToggle(farmTab:AddCard(
        S(314),
        S(354)
    ), S(596), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(582), S(706),
        10, 600, S(190), 30, autoResearch.SetCheckInterval, S(710))
    persistentSlider(farmTab, S(815), S(738),
        1, 20, S(130), 5, autoResearch.SetMaxPerPass, S(126))
    persistentSlider(farmTab, S(484), S(712),
        0, 500, S(528), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(616))
    persistentToggle(
        farmTab:AddCard(S(138),
            S(569)),
        S(640), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(268),
            S(548)),
        S(622), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(795),
            S(204)),
        S(215), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(496), S(391) },
        { true, false },
        S(54), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(249),
        S(79)
    ), S(766), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(3), S(264),
        5, 300, S(869), 30, autoClaim.SetCheckInterval, S(165))
    persistentSlider(farmTab, S(173), S(868),
        1, 30, S(847), 10, autoClaim.SetMaxPerPass, S(694))
    persistentToggle(farmTab:AddCard(
        S(39),
        S(564)
    ), S(477), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(461), S(100),
        5, 300, S(417), 30, autoPlot.SetCheckInterval, S(254))
    persistentToggle(
        farmTab:AddCard(S(716),
            S(457)),
        S(465), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(755),
            S(821)),
        S(37), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(855), S(608),
        0, 500, S(475), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(418))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(192))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(843), S(697))
    persistentToggle(
        settingsTab:AddCard(S(594), S(290)),
        S(55),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(742), 100)
    settingsTab:AddSlider(S(881), S(651), 50, 150, savedScale, function(value)
        config.Set(S(246), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(629), 1)
    settingsTab:AddSegmented({ S(521), S(327), S(745) }, savedLanguage, function(index, value)
        local code = ({ S(626), S(72), S(209) })[index]
        config.Set(S(708), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(68), S(316), S(853) })[savedLanguage] or S(71))
    log(S(846) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(248)] = function()
return {
    [79389059854988] = S(529),
}
end
import(S(28))(import)
