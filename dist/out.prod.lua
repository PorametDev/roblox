-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"games/my_anime_mine/auto_chest", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "string", "core/anti_afk", "lowest", "UIListLayout", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "[Scheduler] ", "disabled", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "autoPlotKeepMoney", "Damage", " (", " open and unmaxed", "core/logger", "ChestCapacity", "autoUpgradeEquipPickaxe", "", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "Chest", "'", "PickaxesStand", "disabled", "locked slot", "games/my_anime_mine/get_current_plot", "แพงสุดก่อน", "TextBox", "[AutoClaim] ", "", "TextLabel", "ZoneBoard", "TextLabel", "Auto Plot: กันเงินไว้", "", "ProximityPrompt", "autoSellMaxWait", "MineRollMutation", ")", "enabled", "[AutoPlace] picked up ", "leaderstats", " refused - ", "ถูกสุดก่อน", "HumanoidRootPart", "TextLabel", "UICorner", "ValueLabel", "Loot", ")", ") ", "Mining", "no isfile/readfile available to verify the write", "table", "autoClaimInterval", "autoPlotBuy", "function", " (", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "AutoPlace", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/game_api", "Crew", "core/get_me", "BasePart", "Players", " -> ", " (power ", "core/i18n", "empty", "[AutoPlot] unlocked ", "Speed", " pickaxe ", "Pad", "AutoPlot", "action '", "Zones", ")", " (x", " | ", "BasePart", "[AutoResearch]   ", "BodyVelocity", "ต่ำสุดก่อน", "table", "[AutoChest] chest is empty", "Frame", "", "core/logger", "antiAfk", "MineSelectZone", "AutoClaim", "th", "ย่อขยายเมนูทั้งชุด", ": ", " not bought yet (", "ItemName", "Auto Plot: รอบเช็ค", " (cost ", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", " ", "CoreGui", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "/", "Hauler", "games/my_anime_mine/auto_research", "table", "[AutoUpgrade] ", "core/humanoid", "[AutoSell] ", "games/my_anime_mine/game_api", "TextButton", "games/my_anime_mine/get_plot_parts", "core/teleport", "🏠", ") ", "games/my_anime_mine/get_current_plot", "UserInputService", "[AutoUpgrade] MineBuyPickaxe ", ") -> ", "", "?", "string", "core/run_job", "games/my_anime_mine/auto_plot", "core/logger", "Shop Stand", "en", "lowest", "ZoneBoardGui", "[AutoUpgrade] ", "CharMineSpeedMaxLevel", "core/debug_tab", "Workspace", "table", "enabled", ",", "unreadable", "TextLabel", " not maxed", ": ", " reward(s) looked claimable but none went through", "\n", "works from anywhere - no teleport needed", "M", "[AutoUpgrade] ", ") ", "autoUpgrade", "Content", "games/my_anime_mine/get_plot_parts", "core/i18n", "0 = ไม่จำกัด", ",", "ไทย", "M", "ReplicatedStorage", "GetHolderSpeedLevel", "Open menu", "HumanoidRootPart", "MekPro", "s", "autoUpgradePriority", " node(s), ", ")", "Auto Research (Plot)", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "PlayerGui", "UIPadding", "open_menu_hint", " costs ", " -> ", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "AutoChest", "autoResearchZones", "games/my_anime_mine/auto_chest", " powerOk=", "Frame", "TextLabel", "AutoClaim", " (money ", "GetHolderLevel", "TextButton", "TextLabel", "s", "[AutoPlace] nothing in the hotbar to place", "prod", "s", "table", "(none)", "string", "[AutoClaim] ", "^M(%d+)$", "games/my_anime_mine/game_api", "[AutoUpgrade] ", "[AutoSell] enabled", "waiting", " ", "Players", " cost=", " has been busy for ", "autoPlotLatest", "Badge", "RunService", "autoMineSpeed", " zones=", ") from ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "x", "games/my_anime_mine/get_plot_parts", "GuiService", "ScrollingFrame", "List", "lowest", "", "TextButton", "core/parse_money", "core/ui", "Frame", ")", "core/scheduler", "/", "Auto Claim: รอบเช็ค", "Header", "neither appendfile nor writefile is available", " error: ", "s", "Frame", "TextButton", "enabled", "[AutoUpgrade] ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "[AutoUpgrade] pickaxe #", "[AutoChest] can't read the chest yet", "en", "", "TabBar", " node(s) looked buyable but the server refused", "", "Plots", "core/logger", "')", "autoSell", "MineBuySkill", "games/my_anime_mine/get_best_miner", "core/dispatch", " research is ", "autoUpgradePickaxeIndex", "Damage", "M", " items)", " didn't go through (out of stock or too expensive)", "อังกฤษ", "HotbarFrame", " (chest holds ", "Auto Sell: รอบขาย", " (", "core/logger", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Search", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "TextLabel", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Frame", "[AutoPlot] ", "", ")", "=", "UIListLayout", "Nodes", "[AutoChest] disabled", " ", "Workspace", "Auto Claim: เก็บสูงสุดต่อรอบ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "MineSpeed", "[AutoUpgrade] every stat on every miner is maxed", "[AutoMine] ", " ", "[AntiAFK] enabled - watching Player.Idled", "[AutoPlot] ", "Auto Upgrade Unit", " x", "Speed", "Auto Chest: เกณฑ์เก็บ", "core/logger", "AutoPlace", "%$%s*([%d,%.]+)%s*(%a*)", "slot not in the hotbar", " ", "Root", "[AutoResearch] ", "Modules", "CoreUI", "games/my_anime_mine/game_api", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "Players", "", "cost", "AutoUpgrade", "[Bb]uy", "⚙", "RunService", "games/my_anime_mine/auto_mine", "[AutoChest] enabled (collects once >= ", "[AutoResearch] ", "core/logger", "search_placeholder", " still empty after placing ", "", "auto", "autoUpgradeKeepMoney", "Damage", "[AutoSell] MineSellItems ", " costs ", "", "games/my_anime_mine/game_api", " req=", "x", "Auto Research: ซื้อสาย Zone", "[AutoUpgrade] nothing to upgrade (no miners placed?)", " (", "string", " doesn't exist", "needs us at the stand - keeping the teleport", "core/teleport", " items (now ", "MineWorld", " #", "Auto Plot: ใช้โซนล่าสุดเสมอ", "fill", " ", "sale didn't go through", "Content", "CharMaxLevel", "[AutoPlace] drifted out of range of ", "X", "core/logger", "MineSpeed", ")", "s", ": ", "core/config", "TextLabel", "[AutoResearch]   ", ")", "games/my_anime_mine/main", "fa9f5dc+", "ค้นหาฟังก์ชัน...", "disabled", "Auto Plot: ซื้อโซนถัดไป", "table", "autoPlot", "menuScale", "Auto Research: รอบเช็ค", ") needs ", ")", "VirtualUser", " (", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "cost", "HumanoidRootPart", "%", "core/ui", "%.0f", "[AutoPlace] disabled", " (", "games/my_anime_mine/auto_place", "table", "Auto Upgrade: สุ่ม Mutation", "%$", "Frame", "enabled", "core/parse_money", "[AutoChest] MineTakeChest had no effect (", " - upgrade the miners first", "autoPlaceInterval", "auto", "[AutoPlot] now mining ", "core/logger", "Auto Upgrade: แจก Pickaxe ให้ miner", "Auto Mine", " (", "BodyPosition", "Players", " ", "Workspace", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "games/my_anime_mine/auto_upgrade", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "UIListLayout", "table", "games/my_anime_mine/game_api", "", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "เช็คโซนทุกกี่วินาที", "[AutoResearch] cheapest open node: ", "", "%.0f", "core/anti_afk", "[AutoResearch] Core ", "MinePower", "(nothing to do)", "Speed", "autoClaimMaxPerPass", "UIScale", " holder(s) - not buying more", "en-us", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Players", "MineBuyPickaxe", "games/my_anime_mine/auto_claim", "s", "Backpack", "([%d,%.]+)%s*(%a*)", "autoUpgradeMaxPerPass", "games/my_anime_mine/auto_plot", "games/my_anime_mine/auto_sell", "[%X] ", "core/get_place_id", "[AutoPlot] ", "AutoChest", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "auto", "autoPlace", "AutoResearch", "Players", "core/get_place_id", "s", "[AutoPlace] ", "autoUpgradeInterval", "autoChestInterval", "core/run_job", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "", "table", "th", "research", "autoSellProtectUnits", "[AutoResearch] nothing open and affordable right now", "core/config", "[AutoSell] disabled", "Character", "core/run_job", " -> ", "MineEquipPickaxe", " (", ")", "Auto Sell", "core/run_job", " buyZones=", "GetCharacterSpeedUpgradeCost", "My Anime Mine · Mining Hub", "UICorner", "research", "MineTakeChest", "Hauler", "games/registry", "", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "prompt disabled", "core/build_env", "อ่านป้ายบน chest ทุกกี่วินาที", "AutoPlot", " (prompt says '", "table", "waiting", "TextButton", "s - jumping the queue", "auto", "autoUpgradeLimitPickaxes", "autoResearchMaxPerPass", "autoChest", "table", "All", "[Scheduler] ", "%$", "ตามเครื่อง", "autoPlotInterval", "[AutoPlace] ", "core/teleport", "Anti-AFK", "AutoResearch", "enabled", "autoUpgradeMutationBudget", "Frame", "s - carrying on without it", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "UserInputService", "core/run_job", "[AutoPlace] slot ", "autoResearchInterval", "Auto Place Miner", "nothing to place or upgrade", "", "ZoneRow", "core/logger", "games/my_anime_mine/auto_mine", "th", " from ", "autoSellMaxWait", "UIStroke", "empty", "costs ", "MineSpeed", "AutoUpgrade", "Mutation: เพดานราคาต่อครั้ง", "[AutoPlot] couldn't switch to ", "Humanoid", "Auto Chest", "core/build_env", "PlotIndex", "autoMine", "games/my_anime_mine/game_api", "Auto Place: รอบเช็ค", "disabled", "[Logger] failed to write ", " stack(s)", "", "ตั้งค่า", "Frame", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "AutoPlace", "-", "Players", "core/logger", ") ", " ชิ้น", "GetHolderMineSpeedLevel", "menuScale", "maxed", "[Config] couldn't write ", "core/logger", "search_placeholder", "slot past the usable cap", "TextLabel", "[AutoResearch] idle: money=", " while selecting", "table", "ขายทุกกี่วินาที", " ", "", "/", "/", "Text", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "Auto Research: ซื้อสูงสุดต่อรอบ", "CharSpeedMaxLevel", " ", "en", "autoPlaceUpgradeWeak", "may need us at the stand - trying that", "Empty miner slot", "Mining", "] job failed: ", "VirtualInputManager", "Auto Sell: รอคิวได้นานสุด", "Frame", "TextLabel", "^%$", "[", "Holders", "/s", "SellStand", "/", "#", "MineGui", "ItemFrame", "Auto Claim (Index)", " (x", "[AutoResearch] ", "Plots", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoPlace] holders: ", "core/logger", "Auto Mine: คลิกต่อรอบ", "core/logger", "", ") on ", "[AutoChest] waiting: ", " buyCore=", "", "Auto Place: เปลี่ยนตัวอ่อนออก", "server didn't equip it", "autoResearch", "BasePart", "=empty", "TextButton", "/", ")", "unreadable", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ",", "Auto Upgrade: Pickaxe ที่จะซื้อ", ": ", " node(s), ", "x", ")", "ValueLabel", "/", "ฟาร์ม", "write reported success but ", "games/my_anime_mine/auto_upgrade", "core/dispatch", "AutoSell", "autoResearchCoreLast", "function", "[AutoChest] collected ", "[AntiAFK] idle detected, simulating input", "MineEquipSlot", "Players", "autoClaim", ")", " budget=", "ProximityPrompt", "", "(", "Players", "[BestMiner] slot ", "[AutoUpgrade] bought pickaxe #", "games/my_anime_mine/auto_place", "core/teleport", ", base ", "open_menu_hint", " (power ", "GetCharacterMineSpeedUpgradeCost", "", "games/my_anime_mine/game_api", ": ", "GetCharacterUpgradeCost", "ClickDetector", " mutation x", "games/my_anime_mine/main", "x", "[MekPro] settings file: ", "[Uu]nlock", " power ", "languageIndex", "[AntiAFK] disabled", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "autoResearchCheapestFirst", "core/get_leaderstat", "games/my_anime_mine/game_api", "/config.json", " pickaxe(s) for ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "M", "This is not a supported place.", "TextLabel", ",", "MineWorld", "number", "autoChestMinItems", " ", "[AutoPlace] upgrading ", "games/registry", "Content", "button has no size on screen", "[AutoResearch] ", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", ", built ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " waited ", "Main", "string", " (", "Auto Upgrade Plot (Zone)", "Auto Research: กันเงินไว้", "TextLabel", " -> ", ", you have ", "[AutoClaim] claimed ", "s", " (", " open=", "games/my_anime_mine/get_best_miner", "TextButton", "MekPro", "Players", " -> x", "Container", "MineSellItems", "core/logger", "games/my_anime_mine/auto_research", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "AutoMine", "autoResearchCore", "^x(%d+)$", "Workspace", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "ปุ่มเปิดเมนู", " items", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", " -> ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "Auto Upgrade: รอบเช็ค", "Mutation: หยุดเมื่อได้ x", "games/my_anime_mine/game_api", "Humanoid", "[Scheduler] turn: ", "Badge", "OwnerUserId", "[MekPro] build ", "Auto Chest: รอบเช็ค", "games/my_anime_mine/auto_claim", "[AutoPlace] couldn't hold anything - not pressing E", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "autoUpgradePickaxe", "\n", "TextLabel", "upgrade", "autoUpgradePickaxeIndex", ": ", " owned)", "TextButton", "table", "Auto Research: ซื้อสาย Core", "MekPro.log", ") didn't take: ", "th", "autoUpgradeMutationTarget", "OwnerUserId", "core/scheduler", "x", "[Scheduler] ", "[AutoPlace] couldn't pick up ", "ขนาดเมนู", "^([%d,]+)$", "BasePart", "table", "Workspace", "games/my_anime_mine/auto_sell", "autoUpgradeRollMutation", "TextLabel", "works from anywhere - dropping the teleport", "Auto Upgrade: กันเงินไว้", "TextLabel", "core/click", "ScreenGui", "Damage", "Locked miner slot", "autoSellInterval", "[AutoPlace] fireproximityprompt error: ", "none", "", "core/logger", "autoResearchKeepMoney", "core/run_job", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "AutoSell", "[AutoResearch]   Core: ", "2026-09-21 05:15:00", "Auto Mine: ความเร็ว", "Text", "write reported success but the line isn't in ", "Frame", "[AutoPlace] placed ", " (", "[Config] couldn't read ", "autoMineClicks", "[GameAPI] SendEvent failed: ", "TextLabel", " power=", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "HttpService", "MineUpgradeCharacter", " ", "ไล่ซื้อทุกกี่วินาที", "core/i18n", "core/run_job", "/", "Frame", "core/get_place_id", "sold ", "=locked(", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "[GameAPI] couldn't load the game's modules: ", "disabled", " team power, you have ", "Search functions...", "core/run_job", "slot ", "fill", "languageIndex"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(4)] = function()
return function(import)
local log = import(S(498))
local Players = game:GetService(S(619))
local VirtualUser = game:GetService(S(356))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(278))
        connection = player.Idled:Connect(function()
            log(S(610))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(640))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(463)] = function()
return { Env = S(185), IsDev = false, Commit = S(346), BuiltAt = S(754) }
end
modules[S(740)] = function()
local VirtualInputManager = game:GetService(S(557))
local GuiService = game:GetService(S(209))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(659)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(442)] = function()
return function(import)
local HttpService = game:GetService(S(767))
local getPlaceId = import(S(775))
local log = import(S(748))
local ROOT_FOLDER = S(159)
local placeFolder = ROOT_FOLDER .. S(773) .. tostring(getPlaceId())
local filePath = placeFolder .. S(645)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(467) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(761) .. filePath .. S(630) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(533) .. filePath .. S(94) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(605)] = function()
return function(import)
    local getPlaceId = import(S(429))
    local registry = import(S(657))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(649))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(643)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(41))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(63)] = function()
local Players = game:GetService(S(385))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(701))
    local backpack = player:WaitForChild(S(415))
    return player, humanoid, backpack
end
return getMe
end
modules[S(421)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(108)] = function()
local Players = game:GetService(S(197))
local RunService = game:GetService(S(202))
local UserInputService = game:GetService(S(117))
local Workspace = game:GetService(S(133))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(510))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(362))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(82))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(64)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(587)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(384))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(771)] = function()
local Players = game:GetService(S(411))
local STRINGS = {
    en = {
        search_placeholder = S(782),
        open_menu_hint = S(157),
    },
    th = {
        search_placeholder = S(347),
        open_menu_hint = S(693),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(408)
    if locale:sub(1, 2):lower() == S(500) then
        return S(722)
    end
    return S(127)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(471) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(534)] = function()
local LOG_FILE = S(720)
local function log(message)
    local line = os.date(S(420)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(579))
            end
            appendfile(LOG_FILE, line .. S(142))
        elseif writefile then
            local existing = S(314)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(711))
        else
            error(S(223))
        end
        if not (isfile and readfile) then
            error(S(52))
        end
        if not isfile(LOG_FILE) then
            error(S(603) .. LOG_FILE .. S(322))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(757) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(518) .. LOG_FILE .. S(596) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(374)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(666) then
        return nil
    end
    local number, suffix = text:match(S(286))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(594), S(213))))
    if not value then
        return nil
    end
    if suffix ~= S(18) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(772)] = function()
return function(import)
local log = import(S(527))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(562) .. name .. S(556) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(219)] = function()
return function(import)
local RunService = game:GetService(S(301))
local log = import(S(336))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(8) .. task_.name .. S(224) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(477) .. task_.name .. S(199)
                    .. string.format(S(399), now - task_.busySince)
                    .. S(488))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(727) .. starved.name .. S(664)
            .. string.format(S(365), now - starved.waitingSince)
            .. S(470))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(702) .. (currentTask and currentTask.name or S(403)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(482)] = function()
local Players = game:GetService(S(65))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(158))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(216)] = function()
return function(import)
local i18n = import(S(150))
local UserInputService = game:GetService(S(490))
local THEME = {
    BrandName = S(679),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(455))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(46))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(167))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(214))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(543)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(503))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(739))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(121)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(260))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(628)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(210))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(267))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(537))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(559))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(650))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(30))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(560))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(47))
            if not valueLbl then
                valueLbl = Instance.new(S(45))
                valueLbl.Name = S(600)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(201))
            if not badge then
                badge = Instance.new(S(712))
                badge.Name = S(703)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(717))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(774))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(522))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(111))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(296)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(363)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(217))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(86))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(181))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(747)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(372))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(391))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(589))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(546)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(741))
    screenGui.Name = S(292)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(101))
    local main = Instance.new(S(262))
    main.Name = S(665)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(406))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(226))
    tabBar.Name = S(235)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(6))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(487))
    header.Name = S(222)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(227))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(182))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(138))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(237)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(678))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(335)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(469))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(525)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(27))
    searchBox.Name = S(258)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(306))
    searchBox.Text = S(436)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(758))
    content.Name = S(332)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(176))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(32))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(625))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(342))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(756)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(583) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(100) .. (item.Subtitle or S(496))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(535))
        footerLabel.Text = i18n.T(S(168))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(1)] = function()
return function(import)
local gameApi = import(S(110))(import)
local log = import(S(380))
local runGuarded = import(S(451))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(303) .. minItemsToCollect .. S(249))
    else
        log(S(269))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(592) then
            log(S(232))
            lastReported = S(137)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(504) then
            log(S(85))
            lastReported = S(69)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(468) then
            local capacity = gameApi.ChestCapacity()
            log(S(581) .. count .. S(590) .. want .. S(694)
                .. (capacity and (S(253) .. capacity .. S(49)) or S(520)))
            lastReported = S(195)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(172), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(609) .. count .. S(325) .. after .. S(355))
        else
            log(S(375) .. count
                .. S(66) .. tostring(after) .. S(265))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(707)] = function()
return function(import)
local gameApi = import(S(192))(import)
local log = import(S(88))
local runGuarded = import(S(123))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(28) .. (enabled and S(373) or S(517)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(673) .. reward.category .. S(288)
                .. tostring(reward.tierKey) .. S(327) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(190) .. #pending .. S(141))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(91), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(302)] = function()
return function(import)
local getCurrentPlot = import(S(116))
local getPlotParts = import(S(112))
local log = import(S(256))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(632), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(276) .. (enabled and S(485) or S(23)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(622)] = function()
return function(import)
local Players = game:GetService(S(612))
local getCurrentPlot = import(S(25))
local getPlotParts = import(S(208))
local getMinerSlots = import(S(677))(import)
local teleport = import(S(324))
local gameApi = import(S(700))(import)
local log = import(S(15))
local runGuarded = import(S(434))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(554)
local LOCKED_SLOT_TEXT = S(743)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(73))
    return pad and pad:FindFirstChildWhichIsA(S(35), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(736)) then
            local digits = descendant.Text:match(S(730))
            if digits then
                return tonumber((digits:gsub(S(152), S(120))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(177)) and descendant.Text:match(S(561)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(784) .. holder.Name .. S(95)
            .. gameApi.MinerSlots() .. S(220) .. gameApi.MaxMinerSlots() .. S(716)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(24)
    end
    if not prompt.Enabled then
        return true, S(462)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(505) .. price
    end
    local action = prompt.ActionText or S(308)
    if action:match(S(637)) or action:match(S(299)) or action:match(S(478)) then
        return true, S(75) .. action .. S(21)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(44))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(745) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(777) .. why .. S(77))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(588))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(266) .. prompt.ObjectText
                    .. S(618) .. shown .. S(624) .. math.floor(base) .. S(614))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(79))
    if line ~= lastSurvey then
        log(S(575) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(329), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(713), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(481) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(366))
    else
        lastAttempt = 0
        log(S(692))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(647),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(287)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(536)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(585)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(184))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(334) .. holder.Name .. S(539))
            return false
        end
        log(S(492) .. candidate.slot .. S(255) .. candidate.name
            .. S(721) .. tostring(why))
    end
    if not miner then
        log(S(708))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(431) .. holder.Name .. S(307) .. miner.name)
        return false
    end
    log(S(759) .. miner.name .. S(626) .. miner.power
        .. S(580) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(728) .. entry.name .. S(501) .. entry.holder.Name
            .. S(466) .. tostring(entry.prompt.ActionText) .. S(240))
        return false
    end
    log(S(40) .. entry.name .. S(67) .. entry.power
        .. S(205) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(495))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(785) then
        runGuarded(S(59), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(656) .. target.holder.Name .. S(140) .. target.name
        .. S(383) .. target.power .. S(119) .. best.name .. S(667) .. best.power .. S(344))
    runGuarded(S(524), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(418)] = function()
return function(import)
local gameApi = import(S(629))(import)
local parseMoney = import(S(215))
local log = import(S(284))
local runGuarded = import(S(783))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(279) .. (enabled and S(39) or S(348)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(371)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(379) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(357) .. deepestKey .. S(591))
        return true
    end
    log(S(509) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(422) .. gameApi.ZoneDisplayName(zoneKey) .. S(313)
                .. math.floor(price) .. S(672) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(70) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(675) .. zoneKey .. S(338))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(456) then
            reportedLocked = S(439)
            log(S(263) .. zoneKey .. S(42)
                .. gameApi.ZoneDisplayName(currentKey) .. S(245)
                .. done .. S(566) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(74), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(105)] = function()
return function(import)
local gameApi = import(S(515))(import)
local log = import(S(684))
local runGuarded = import(S(750))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(660) .. (enabled and S(228) or S(780)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(290) .. node.zoneKey .. S(769) .. node.id
                    .. S(367) .. node.family .. S(50) .. node.level .. S(671) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(401) .. node.id
                    .. S(13) .. node.family .. S(528) .. node.level .. S(170) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(572) .. blocked
                .. S(236))
        elseif powerBlocked then
            log(S(304) .. powerBlocked.zoneKey .. S(196) .. powerBlocked.id
                .. S(320) .. powerBlocked.family .. S(354)
                .. math.floor(powerBlocked.powerReq) .. S(781)
                .. math.floor(powerBlocked.power)
                .. S(376))
        else
            log(S(441)
                .. S(179) .. math.floor(gameApi.GetMoney()) .. S(38))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(397) .. nearest.zoneKey
                    .. S(386) .. nearest.id .. S(57) .. nearest.family .. S(146)
                    .. nearest.level .. S(103) .. nearest.max
                    .. S(169) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(538) .. math.floor(money)
        .. S(615) .. math.floor(budget)
        .. S(204) .. (#zones > 0 and table.concat(zones, S(651)) or S(188))
        .. S(452) .. tostring(buyZones) .. S(582) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(343) .. zoneKey .. S(330) .. tostring(node.id)
                        .. S(448) .. tostring(node.family) .. S(115)
                        .. node.level .. S(545) .. node.max
                        .. S(676) .. tostring(node.open)
                        .. S(198) .. tostring(node.cost)
                        .. S(175) .. tostring(node.powerOk)
                        .. S(316) .. tostring(node.powerReq)
                        .. S(765) .. tostring(node.power))
                end
            end
        end
        log(S(81) .. zoneKey .. S(340) .. #nodes
            .. S(162) .. pending .. S(139))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(753) .. #coreNodes .. S(597)
        .. coreOpen .. S(14))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(427), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(419)] = function()
return function(import)
local Workspace = game:GetService(S(691))
local gameApi = import(S(293))(import)
local teleport = import(S(623))
local log = import(S(239))
local runGuarded = import(S(445))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(443))
    else
        lastAttempt = 0
        log(S(194))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(565))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(616), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(312)
                .. (sold and S(737)
                         or S(323)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(109) .. (sold and (S(776) .. #keys .. S(519)) or S(331)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(752), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(389)] = function()
return function(import)
local Workspace = game:GetService(S(271))
local gameApi = import(S(644))(import)
local teleport = import(S(113))
local log = import(S(305))
local runGuarded = import(S(491))(import)
local STATS = { S(247), S(282), S(506) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(212)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(229) .. (enabled and S(135) or S(9)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(187) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(5) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(297) or S(532)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(145) .. holderKey .. S(655) .. choice.stat .. S(550)
                        .. choice.info.level .. S(446) .. after.level
                        .. S(601) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(361) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(22))
    local shop = stand and stand:FindFirstChild(S(126))
    if not shop then
        return nil
    end
    if shop:IsA(S(731)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(80), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(118)
                .. (bought and S(143)
                           or S(553)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(360))
        end
    end
    if bought then
        log(S(621) .. pickaxe.index .. S(542) .. pickaxe.name
            .. S(78) .. pickaxe.multiplier .. S(163))
        return true
    end
    log(S(231) .. pickaxe.index .. S(277) .. pickaxe.name
        .. S(250))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(130) .. holderKey .. S(72)
                .. tostring(entry and entry.Pickaxe or S(746)) .. S(696) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(193) .. holderKey .. S(633) .. variant
                .. S(681) .. after .. S(98) .. cost .. S(599))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(107) .. gameApi.OwnedPickaxeCount()
                    .. S(646) .. gameApi.MinerSlots()
                    .. S(407))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(58))
        elseif maxed > 0 then
            log(S(275))
        else
            log(S(319))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(540) and entry.Pickaxe and entry.Pickaxe ~= S(398) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(298), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(315)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(155))
local Workspace = game:GetService(S(387))
local log = import(S(125))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(291), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(779) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(350))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(718) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(458) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(53) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(475) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(104) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(191))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(763) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(457))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(476), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(106) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(392) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(444) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(683), keys)
end
function api.EquipSlot(slot)
    return send(S(611), slot == S(29) and S(460) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(412), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(447), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(37), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(242), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(732)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(428)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(402))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(56) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(369) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(90), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(326))
    plots = plots and plots:FindFirstChild(S(573))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(295)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(724)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(31))
    local gui = board and board:FindFirstChild(S(129))
    local root = gui and gui:FindFirstChild(S(289))
    local list = root and root:FindFirstChild(S(211))
    local row = list and list:FindFirstChild(S(497) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(670)) and descendant.Text ~= S(264) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(608) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(768), characterKey, stat == S(51) and S(274) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(180), max = S(333), cost = S(631) },
    Speed = { level = S(156), max = S(549), cost = S(453) },
    MineSpeed = { level = S(530), max = S(131), cost = S(627) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(122) or charName == S(34) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(437) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(653) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(321) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(134) and type(holder.CharName) == S(3) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(84) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(189) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(243)] = function()
return function(import)
local Players = game:GetService(S(680))
local gameApi = import(S(393))(import)
local log = import(S(578))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(416))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(136), S(234)))) or 0
    if suffix ~= S(87) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(166))
    local mineGui = playerGui and playerGui:FindFirstChild(S(568))
    local hotbar = mineGui and mineGui:FindFirstChild(S(252))
    local content = hotbar and hotbar:FindFirstChild(S(148))
    return content and content:FindFirstChild(S(682))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(764)) then
            local count = descendant.Text:match(S(690))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(569))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(658))
    local nameLabel = content and content:FindFirstChild(S(96))
    local damageLabel = content and content:FindFirstChild(S(12))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(617) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(544) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(620) .. slot .. S(715) .. info.name
                        .. S(638) .. info.power .. S(281) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(60)] = function()
local Players = game:GetService(S(526))
local Workspace = game:GetService(S(733))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(652))
    local plots = mineWorld and mineWorld:FindFirstChild(S(238))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(704)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(149)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(563)),
        Nodes = plot:FindFirstChild(S(268)),
        Chest = plot:FindFirstChild(S(20)),
        Zones = plot:FindFirstChild(S(76)),
        Crew = plot:FindFirstChild(S(62)),
        Loot = plot:FindFirstChild(S(48)),
        PlotIndex = plot:GetAttribute(S(513)),
        ChestCapacity = plot:GetAttribute(S(16)),
    }
end
return getPlotParts
end
modules[S(345)] = function()
return function(import)
    local createWindow = import(S(364))(import)
    local i18n = import(S(68))
    local antiAfk = import(S(400))(import)
    local buildEnv = import(S(512))
    local scheduler = import(S(725))(import)
    local config = import(S(341))(import)
    local log = import(S(576))
    log(S(705) .. buildEnv.Commit .. S(760) .. buildEnv.Env
        .. S(662) .. buildEnv.BuiltAt .. S(449))
    local window = createWindow({
        Subtitle = S(454),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(499))(import)
    local autoPlace = import(S(368))(import)
    local autoChest = import(S(174))(import)
    local autoSell = import(S(734))(import)
    local autoUpgrade = import(S(604))(import)
    local autoResearch = import(S(685))(import)
    local autoClaim = import(S(413))(import)
    local autoPlot = import(S(124))(import)
    local gameApi = import(S(61))(import)
    scheduler.RegisterStandalone(S(688), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(423), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(285), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(507), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(484), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(178), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(465), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(36), 20)
    scheduler.RegisterExclusive(S(606), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(602), S(114))
    persistentToggle(farmTab:AddCard(
        S(382),
        S(709)
    ), S(514), autoMine.SetEnabled)
    persistentSlider(farmTab, S(755), S(390),
        1, 30, S(203), 5, autoMine.SetCyclesPerSecond, S(564))
    persistentSlider(farmTab, S(577), S(697),
        1, 10, S(762), 1, autoMine.SetClicksPerCycle, S(635))
    persistentToggle(farmTab:AddCard(
        S(494),
        S(410)
    ), S(426), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(516), S(409),
        1, 60, S(377), 3, autoPlace.SetCheckInterval, S(430))
    persistentToggle(
        farmTab:AddCard(S(584),
            S(395)),
        S(552), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(511),
        S(547)
    ), S(474), autoChest.SetEnabled)
    persistentSlider(farmTab, S(283), S(695),
        50, 3000, S(654), 100, autoChest.SetMinItems, S(529))
    persistentSlider(farmTab, S(706), S(464),
        1, 60, S(433), 3, autoChest.SetCheckInterval, S(160))
    persistentToggle(farmTab:AddCard(
        S(450),
        S(261)
    ), S(241), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(171),
            S(294)),
        S(440), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(254), S(541),
        5, 300, S(744), 20, autoSell.SetCheckInterval, S(183))
    farmTab:AddSlider(S(558),
        S(230),
        5, 120, sellMaxWait, function(value)
            config.Set(S(502), value)
        end, S(186))
    persistentToggle(farmTab:AddCard(
        S(280),
        S(687)
    ), S(147), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(698), S(574),
        5, 300, S(432), 20, autoUpgrade.SetInterval, S(339))
    persistentSlider(farmTab, S(424), S(489),
        1, 20, S(417), 5, autoUpgrade.SetMaxPerPass, S(207))
    persistentSegmented(
        farmTab,
        { S(83), S(742), S(404), S(555) },
        { S(128), S(311), S(71), S(337) },
        S(161), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(738), S(593),
        0, 500, S(310), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(648))
    persistentToggle(
        farmTab:AddCard(S(165),
            S(461)),
        S(710), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(571) .. pickaxe.multiplier .. S(218)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(273)
            end
            return S(567) .. index .. S(270) .. (pickaxeLabels[index] or S(435))
        end
        local savedPick = config.Get(S(246), 0)
        pickaxeCard = farmTab:AddSlider(
            S(595),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(714), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(394)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(523),
            S(19)),
        S(472), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(381),
            S(359)),
        S(17), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(370),
            S(99)),
        S(735), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(699), S(10),
        1, 20, S(723), 2, autoUpgrade.SetMutationTarget, S(317))
    persistentSlider(farmTab, S(508), S(151),
        0, 500, S(486), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(144))
    persistentToggle(farmTab:AddCard(
        S(164),
        S(686)
    ), S(586), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(353), S(770),
        10, 600, S(493), 30, autoResearch.SetCheckInterval, S(674))
    persistentSlider(farmTab, S(548), S(751),
        1, 20, S(473), 5, autoResearch.SetMaxPerPass, S(726))
    persistentSlider(farmTab, S(669), S(778),
        0, 500, S(749), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(248))
    persistentToggle(
        farmTab:AddCard(S(318),
            S(102)),
        S(173), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(719),
            S(2)),
        S(689), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(7),
            S(661)),
        S(607), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(43), S(26) },
        { true, false },
        S(642), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(570),
        S(663)
    ), S(613), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(221), S(766),
        5, 300, S(54), 30, autoClaim.SetCheckInterval, S(414))
    persistentSlider(farmTab, S(272), S(259),
        1, 30, S(405), 10, autoClaim.SetMaxPerPass, S(598))
    persistentToggle(farmTab:AddCard(
        S(668),
        S(358)
    ), S(351), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(97), S(396),
        5, 300, S(480), 30, autoPlot.SetCheckInterval, S(225))
    persistentToggle(
        farmTab:AddCard(S(349),
            S(641)),
        S(55), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(328),
            S(388)),
        S(200), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(33), S(206),
        0, 500, S(11), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(154))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(132))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(521), S(300))
    persistentToggle(
        settingsTab:AddCard(S(483), S(257)),
        S(89),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(531), 100)
    settingsTab:AddSlider(S(729), S(93), 50, 150, savedScale, function(value)
        config.Set(S(352), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(786), 1)
    settingsTab:AddSegmented({ S(479), S(251), S(153) }, savedLanguage, function(index, value)
        local code = ({ S(309), S(551), S(438) })[index]
        config.Set(S(639), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(425), S(233), S(92) })[savedLanguage] or S(378))
    log(S(636) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(459)] = function()
return {
    [79389059854988] = S(634),
}
end
import(S(244))(import)
