-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[Scheduler] turn: ", "/", "string", "MineRollMutation", "AutoChest", "x", "s", ") needs ", "ย่อขยายเมนูทั้งชุด", "TextLabel", "off", "games/my_anime_mine/game_api", "autoUpgradeEquipPickaxe", "TextButton", "Mutation: เพดานราคาต่อครั้ง", ")", " -> ", "AutoUpgrade", " ", ")", " (", "My Anime Mine · Mining Hub", "table", "sale didn't go through", "s", "Auto Research: กันเงินไว้", "Damage ก่อน", "table", "TextLabel", "function", "games/my_anime_mine/auto_place", "autoResearch", "TextLabel", " (", "ZoneRow", "no mine data", "/s", "ถูกสุดก่อน", " thing available first", "%.3g", "autoUpgradePickaxeIndex", ")", "3ed6c26+", "", "action '", "games/my_anime_mine/game_api", "core/logger", "spendOrder", "MineEquipSlot", "AutoPlot", "AutoPlace", "Auto Upgrade Plot (Zone)", "games/registry", "x", "empty", "[AutoResearch] Core ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "%.3g", "[AutoChest] MineTakeChest had no effect (", "x", "[AutoPlace] ", "autoResearchInterval", "write reported success but ", "/config.json", "nothing claimable", ",", "core/i18n", "UIStroke", "enabled", "none", "Players", "ScreenGui", "[AutoResearch] idle: ", "autoUpgradePriority", "[AutoSell] MineSellItems ", "empty", " fields: ", "ต่างคนต่างซื้อ", "string", "games/my_anime_mine/auto_upgrade", ") on ", "TextLabel", "games/my_anime_mine/get_plot_parts", "autoSell", "ปุ่มเปิดเมนู", "table", ": ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "Frame", "RunService", "AutoUpgrade", "ไล่ซื้อทุกกี่วินาที", "VirtualUser", "[AutoPlace] fireproximityprompt error: ", "AutoResearch", "Text", "", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", " (chest holds ", " (money ", "Container", "[AntiAFK] disabled", "[AutoResearch] nothing open and affordable right now", "games/my_anime_mine/auto_sell", "every open node is maxed", "=", "^Swap out ", "'", "th", "", "[AutoUpgrade] ", ")", "autoPlaceUnlockBudget", "research", "Frame", "autoUpgradeKeepMoney", " doesn't exist", "fill", "GetHolderSpeedLevel", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "UIListLayout", "search_placeholder", "^x(%d+)$", "MinePower", "autoPlotLatest", "Damage", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Auto Place: เปลี่ยนตัวอ่อนออก", " waited ", "CoreGui", "ไทย", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "0 = ไม่จำกัด", "Frame", "core/get_place_id", "AutoSell", " (", " costs ", "games/my_anime_mine/game_api", " ", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "ไล่อัปเกรด miner ทุกกี่วินาที", "UIListLayout", "Auto Chest: รอบเช็ค", "UserInputService", "core/logger", "List", "%$", "AutoPlace", "Frame", "core/config", "ขนาดเมนู", "Auto Place Miner", "Shop Stand", "autoUpgradeRollMutation", "core/run_job", "[AutoPlace] couldn't pick up ", "[%X] ", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", " pickaxe(s) for ", "Auto Claim (Index)", "(", "%$%s*([%d,%.]+)%s*(%a*)", "⚙", "autoPlace", "dearest", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "X", "Zone buying is off", "button has no size on screen", "Auto Plot: ใช้โซนล่าสุดเสมอ", "[GameAPI] couldn't load the game's modules: ", "Players", ",", "Damage", "autoPlaceEquipBest", "#", ": ", "MineSpeed", "[AutoUpgrade] bought pickaxe #", "Exclusive", " (", "autoUpgradeMaxPerPass", "อ่านป้ายบน chest ทุกกี่วินาที", "", ") ", " but no unclaimed reward in its Track", "Auto Place: เพดานราคาปลดช่อง", "games/my_anime_mine/auto_mine", "enabled", "[AutoUpgrade] pickaxe #", "autoResearchCheapestFirst", "Auto Mine: ความเร็ว", "", " -> ", "autoUpgradePickaxe", "Main", "table", "ฟาร์ม", " items)", "table", "core/get_place_id", "table", "s", "may need us at the stand - trying that", "MineSelectZone", "2026-09-21 13:08:14", "core/scheduler", "AutoPlot", "games/my_anime_mine/auto_research", "Players", "", " ", "antiAfk", " (", "", "x", "games/my_anime_mine/auto_claim", "prompt disabled", "core/logger", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "ยิง Equip Best ทุกกี่วินาที", "[SpendPolicy] ", "This is not a supported place.", "th", "Auto Sell", "[AutoClaim] claimed ", "cost", "TextButton", "AutoPlace", "s", "Frame", "th", "s - carrying on without it", "TextBox", "server didn't equip it", "", "Modules", "locked slot", "/", " for ", "AutoPlot", "auto", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "Root", "MineSpeed", "enabled", "table", " wouldn't take ", " (x", ")", " ", "TextLabel", "CoreUI", "M", "M", "autoResearchCoreLast", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", ", you have ", " ", "core/spend_policy", "GetCharacterMineSpeedUpgradeCost", "off", "GetHolderLevel", ": ", "ProximityPrompt", "core/i18n", "", "languageIndex", " still empty after placing ", "core/logger", "dearest", " has been busy for ", "=", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "dearest", "works from anywhere - dropping the teleport", "Crew", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "UIListLayout", "%.0f", "dearest", "ProximityPrompt", "%.0f", "Humanoid", "waiting", "Players", "[Scheduler] ", "PlotIndex", "[", "auto", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "waiting", "games/my_anime_mine/game_api", "core/run_job", ")", "AutoUpgrade", "[AutoResearch] cheapest open node: ", "CharSpeedMaxLevel", "Open menu", "ReplicatedStorage", "core/logger", "TextLabel", "", "/", "autoResearchKeepMoney", "[AutoChest] waiting: ", "s - jumping the queue", "AutoUpgrade", "Content", " team power, you have ", "Speed ก่อน", "[AutoPlace] holders: ", "unreadable", "GetHolderMineSpeedLevel", "TextButton", "Plots", "[AntiAFK] enabled - watching Player.Idled", "[AutoPlace] disabled", "core/logger", "games/my_anime_mine/auto_plot", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "en", "UIScale", "Auto Upgrade: Pickaxe ที่จะซื้อ", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Header", "function", "TextLabel", "MineBuyPickaxe", "0 = ไม่จำกัด", "leaderstats", "Frame", "[AutoPlace] Equip Best: hotbar's strongest ", "disabled", "GuiService", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "Players", "autoPlaceInterval", "autoResearchCore", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "[AutoSell] disabled", "Auto Research: ซื้อสาย Core", " ", "core/spend_policy", "Auto Claim: เก็บสูงสุดต่อรอบ", "Mining ก่อน", "autoSellMaxWait", "[AutoChest] enabled (collects once >= ", "off - every automation spends on its own", "both Zone and Core buying are switched off", "Loot", "core/logger", "open_menu_hint", " (", "autoResearchZones", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "core/logger", " (cost ", "games/my_anime_mine/get_plot_parts", "TextLabel", "SellStand", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", " not bought yet (", "upgrade", "Auto Mine", "?", " #", ")", " ชิ้น", "core/get_place_id", "modules not loaded", "", ")", "Chest", "Players", "[AutoPlace] drifted out of range of ", "All", "[AutoPlace] ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "lowest", "auto", "%.3g", "TextLabel", " -> x", " owned)", "open_menu_hint", ", ", "ZoneBoard", "core/run_job", "CharMineSpeedMaxLevel", " (", "Mining", "ถูกสุดก่อน", "", "works from anywhere - no teleport needed", "[AutoClaim] ", "Content", " ", "[AutoPlace] unlocked ", "[AutoPlot] ", "AutoPlot", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "core/ui", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "", "Auto Chest: เกณฑ์เก็บ", "[AutoPlot] ", "Auto Plot: รอบเช็ค", "[AutoPlace] upgrading ", "games/my_anime_mine/auto_sell", "Auto Research: ซื้อสูงสุดต่อรอบ", "off", "Anti-AFK", "TextButton", " reward(s) looked claimable but none went through", "^M(%d+)$", "เช็คโซนทุกกี่วินาที", "disabled", "games/my_anime_mine/get_plot_parts", ")", "[AutoChest] chest is empty", "CharMaxLevel", "Hauler", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", ")", "core/logger", "games/my_anime_mine/get_current_plot", "M", "ตั้งค่า", "Auto Plot: ซื้อโซนถัดไป", "MekPro", ")", "Auto Research: ซื้อสาย Zone", "Locked miner slot", " (", "[AutoPlace] picked up ", "prod", "[AutoPlot] couldn't switch to ", "%.3g", "Auto Upgrade: รอบเช็ค", "core/teleport", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "costs ", "s", "ValueLabel", "Badge", "Players", "none", "Humanoid", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "table", "OwnerUserId", "needs ", "autoMineSpeed", ") from ", "[AutoPlace] placed ", "core/spend_policy", "[MekPro] settings file: ", "", "Frame", "core/anti_afk", "needs ", "อังกฤษ", "core/run_job", "", "Mutation: หยุดเมื่อได้ x", "en", "[Logger] failed to write ", "core/logger", "core/teleport", "lowest", "Auto Place: รอบเช็ค", "🏠", "core/get_leaderstat", "[AutoPlace] couldn't hold anything - not pressing E", "games/my_anime_mine/auto_claim", "x", "Auto Sell: รอบขาย", "autoMine", "maxed", "[GameAPI] holder ", " waited ", "games/my_anime_mine/main", "ZoneBoardGui", "[Bb]uy", "MineTakeChest", "%", "table", "disabled", "disabled", "games/my_anime_mine/get_best_miner", "[GameAPI] SendEvent failed: ", "BasePart", "core/build_env", "%.0f", "Hauler", "Auto Chest", "TextButton", " costs ", "autoUpgradeInterval", "cheapest", "MineBuySkill", "games/my_anime_mine/auto_research", ")", "ตามเครื่อง", "AutoResearch", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "HumanoidRootPart", "games/my_anime_mine/game_api", "autoPlotKeepMoney", "autoUpgradeLimitPickaxes", "autoClaimMaxPerPass", "dearest", "Core", "autoMineClicks", "ขายทุกกี่วินาที", "; ", "", " didn't go through (out of stock or too expensive)", "/", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "[AutoUpgrade] ", "autoPlot", "BasePart", "autoUpgradePickaxeIndex", "[AutoPlot] ", "slot past the usable cap", "GuiService", ") gave nothing", "MineSpeed", "fill", "/", "Auto Claim: รอบเช็ค", "table", "auto", "core/logger", "Frame", "[AutoUpgrade] ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", " -> ", "core/run_job", "Frame", "", "core/logger", "core/teleport", "core/spend_policy", "Workspace", "\n", "s", "", "BasePart", "Auto Sell: รอคิวได้นานสุด", "en", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "[AutoResearch] ", "autoUpgrade", "cheapest open node ", "unlock", ")", "Auto Upgrade: สุ่ม Mutation", "Core buying is off", "PickaxesStand", "core/run_job", "games/my_anime_mine/main", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "[AutoChest] can't read the chest yet", "[Config] couldn't write ", "[AutoPlace] slot ", "badge says ", " while selecting", "Players", "Pad", "games/my_anime_mine/game_api", "core/logger", "AutoMine", " still locked after paying ", "dearest", "[AutoPlace] nothing in the hotbar to place", " -> ", "AutoResearch", "autoResearchMaxPerPass", "core/run_job", "ItemFrame", "string", "^([%d,%.]+)%s*(%a*)$", "slot not in the hotbar", "Auto Research (Plot)", "string", "TextButton", "search_placeholder", " -> ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "AutoPlace", "core/run_job", " team power", " (", "[AutoPlot] unlocked ", "UserInputService", "[AutoUpgrade] ", "[AutoPlot] now mining ", "sold ", "autoSellInterval", "core/anti_afk", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "core/parse_money", "BasePart", "enabled", "core/dispatch", "TextLabel", "[Config] couldn't read ", " (still ", " from ", "core/teleport", "PlayerGui", "[AntiAFK] idle detected, simulating input", "[Uu]nlock", " possible holder(s) - not buying more", "autoPlaceEquipBestInterval", "core/click", "disabled", "AutoPlace", "needs us at the stand - keeping the teleport", " pickaxe ", " error: ", "[AutoClaim] idle: ", "-", "[AutoResearch] ", "games/my_anime_mine/auto_place", "Holders", "autoUpgradeMutationTarget", "AutoClaim", "s behind a purchase that never happened - letting it spend", "AutoClaim", "\n", "ValueLabel", "autoChestInterval", "nothing to place or upgrade", "Workspace", "cheapest", "no isfile/readfile available to verify the write", "core/scheduler", "core/config", "games/my_anime_mine/auto_plot", "table", "Auto Place: ปลดล็อกช่อง miner", "[Scheduler] ", " ", "[AutoChest] disabled", ") ", "Auto Mine: คลิกต่อรอบ", "games/my_anime_mine/game_api", "core/i18n", "core/parse_money", "Nodes", "AutoPlace", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "[AutoPlace] ", "=locked(", "ลำดับการใช้เงินรวมทุกระบบ", "core/build_env", ": ", "MineGui", "GetCharacterUpgradeCost", "MekPro", "autoClaim", " claimable but none could be resolved", "MineWorld", "แพงสุดก่อน", ": badge says ", "Search functions...", "write reported success but the line isn't in ", "[BestMiner] slot ", "HumanoidRootPart", ")", "] job failed: ", "Content", "AutoSell", "Search", ") didn't take: ", "AutoChest", " ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "TextButton", "TextLabel", "ItemName", " ", "research", "th", "autoSellMaxWait", "TextLabel", " - ", "games/registry", "[Scheduler] ", ", have ", "games/my_anime_mine/auto_upgrade", " ", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "", " (x", "[AutoChest] collected ", "Auto Plot: กันเงินไว้", " -> ", "table", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "Speed", "Plots", "M", "table", "ClickDetector", "[AutoUpgrade] every stat on every miner is maxed", "MineSellItems", ",", "(nothing to do)", "games/my_anime_mine/get_best_miner", " stack(s)", "menuScale", "autoPlotBuy", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " (", "s", "s", "ค้นหาฟังก์ชัน...", "MineEquipPickaxe", "core/parse_money", "Players", "TextLabel", "MineUpgradeCharacter", "core/logger", " (power ", "TextLabel", "[AutoClaim] ", "core/logger", ": State(", "autoClaimInterval", "VirtualInputManager", "Empty miner slot", "MekPro.log", "OwnerUserId", "core/spend_policy", "AutoUpgrade", "Auto Place: รอบดึงตัวเก่ง", "Auto Upgrade: กันเงินไว้", "", "Players", " (prompt says '", "autoPlaceUnlock", "=empty", ") ", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "[AutoUpgrade] ", "core/dispatch", "", ": ", "table", "ScrollingFrame", "%.3g", "", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "AutoPlace", "/", "autoChestMinItems", " (power ", "core/debug_tab", "games/my_anime_mine/auto_chest", ") ", " refused - ", ")", "[AutoResearch] ", "cost", " items", "Text", "Auto Upgrade Unit", "MineWorld", "ต่ำสุดก่อน", "Workspace", "แพงสุดก่อน", "[AutoSell] enabled", "[AutoMine] ", ") -> ", "[AutoPlace] ", "UICorner", "autoChest", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", " ", "affordable - waiting for a turn", "%.3g", "off", "Workspace", "Workspace", "autoUpgradeMutationBudget", " | ", "s", "index rewards are disabled in this game build", "^%$", "core/logger", "[MekPro] build ", "%$", "Auto Upgrade: แจก Pickaxe ให้ miner", "[SpendPolicy] ", "Auto Research: รอบเช็ค", "TextLabel", "core/spend_policy", "AutoResearch", "", "Frame", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "autoPlaceUpgradeWeak", "autoSellProtectUnits", "s", "", "", "games/my_anime_mine/game_api", "games/my_anime_mine/game_api", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " is no better after the swap (", " items (now ", "HumanoidRootPart", " -> ", "TextLabel", "menuScale", "%.3g", "", "unlock", "languageIndex", "Speed", "AutoPlot", " research is ", "en-us", "Character", " for ", "core/ui", "ClaimableTiers failed", "[AutoUpgrade] ", "HotbarFrame", "/", "BodyVelocity", "[AutoUpgrade] MineBuyPickaxe ", ") - leaving it alone for ", "Badge", "GetCharacterSpeedUpgradeCost", "", "UICorner", "BodyPosition", "MineEquipBest", "M", "slot ", "Limited", "RunService", " power ", "Frame", " mutation x", "core/get_me", "TabBar", "nothing left to buy (", "table", "[AutoSell] ", " x", "string", "ChestCapacity", "UIPadding", "unreadable", "buying the ", "core/humanoid", " - upgrade the miners first", "number", ", built ", "TextButton", "games/my_anime_mine/auto_mine", "HttpService", " node(s) looked buyable but the server refused", "([%d,%.]+)%s*(%a*)", "[AutoResearch] ", "')", "neither appendfile nor writefile is available", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "games/my_anime_mine/get_current_plot", ", base ", "Zones", "table", "enabled", "Damage", "core/logger", "games/my_anime_mine/game_api", "games/my_anime_mine/auto_chest", "Backpack", "games/my_anime_mine/get_current_plot", " (", "[AutoPlace] unlocking ", "autoPlotInterval"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(605)] = function()
return function(import)
local log = import(S(799))
local Players = game:GetService(S(448))
local VirtualUser = game:GetService(S(93))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(318))
        connection = player.Idled:Connect(function()
            log(S(618))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(102))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(495)] = function()
return { Env = S(438), IsDev = false, Commit = S(43), BuiltAt = S(207) }
end
modules[S(622)] = function()
local VirtualInputManager = game:GetService(S(738))
local GuiService = game:GetService(S(336))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(170)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(645)] = function()
return function(import)
local HttpService = game:GetService(S(873))
local getPlaceId = import(S(135))
local log = import(S(735))
local ROOT_FOLDER = S(667)
local placeFolder = ROOT_FOLDER .. S(2) .. tostring(getPlaceId())
local filePath = placeFolder .. S(64)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(28) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(613) .. filePath .. S(87) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(569) .. filePath .. S(664) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(611)] = function()
return function(import)
    local getPlaceId = import(S(202))
    local registry = import(S(695))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(224))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(475)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(332))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(856)] = function()
local Players = game:GetService(S(173))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(450))
    local backpack = player:WaitForChild(S(889))
    return player, humanoid, backpack
end
return getMe
end
modules[S(371)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(867)] = function()
local Players = game:GetService(S(747))
local RunService = game:GetService(S(852))
local UserInputService = game:GetService(S(600))
local Workspace = game:GetService(S(548))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(285))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(509))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(840))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(494)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(552)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(847))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(67)] = function()
local Players = game:GetService(S(728))
local STRINGS = {
    en = {
        search_placeholder = S(673),
        open_menu_hint = S(300),
    },
    th = {
        search_placeholder = S(725),
        open_menu_hint = S(85),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(832)
    if locale:sub(1, 2):lower() == S(225) then
        return S(109)
    end
    return S(554)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(382) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(302)] = function()
local LOG_FILE = S(740)
local function log(message)
    local line = os.date(S(158)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(268))
            end
            appendfile(LOG_FILE, line .. S(637))
        elseif writefile then
            local existing = S(815)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(549))
        else
            error(S(878))
        end
        if not (isfile and readfile) then
            error(S(643))
        end
        if not isfile(LOG_FILE) then
            error(S(63) .. LOG_FILE .. S(117))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(674) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(469) .. LOG_FILE .. S(265) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(608)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(79) then
        return nil
    end
    local number, suffix = text:match(S(163))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(174), S(466))))
    if not value then
        return nil
    end
    if suffix ~= S(212) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(565)] = function()
return function(import)
local log = import(S(271))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(290) .. name .. S(678) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(208)] = function()
return function(import)
local RunService = game:GetService(S(90))
local log = import(S(320))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(288) .. task_.name .. S(627) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(649) .. task_.name .. S(273)
                    .. string.format(S(281), now - task_.busySince)
                    .. S(234))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(696) .. starved.name .. S(483)
            .. string.format(S(284), now - starved.waitingSince)
            .. S(308))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(1) .. (currentTask and currentTask.name or S(716)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(806)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(537))
local policy = {}
local probes = {}
local order = {}
local mode = S(791)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(803) .. (mode == S(11)
        and S(350)
        or (S(866) .. mode .. S(39))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(263) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(642) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(579) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(223) .. name .. S(129)
            .. string.format(S(496), now - blockedSince[name])
            .. S(635))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(616)] = function()
local Players = game:GetService(S(338))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(676))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(404)] = function()
return function(import)
local i18n = import(S(655))
local UserInputService = game:GetService(S(145))
local GuiService = game:GetService(S(529))
local THEME = {
    BrandName = S(432),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(784))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(846))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(864))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(229))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(814)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(68))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(733))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(367)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(612))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(544)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(758))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(121))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(729))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(89))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(253))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(687))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(361))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(446))
            if not valueLbl then
                valueLbl = Instance.new(S(384))
                valueLbl.Name = S(638)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(447))
            if not badge then
                badge = Instance.new(S(693))
                badge.Name = S(843)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(415))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(333))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(134))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(686))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(185)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(488)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(461))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(809))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(499))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(395)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(854))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(143))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(591))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(774)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(72))
    screenGui.Name = S(254)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(130))
    local main = Instance.new(S(150))
    main.Name = S(197)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(324))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(543))
    tabBar.Name = S(857)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(280))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(232))
    header.Name = S(327)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(14))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(805))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(33))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(826)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(316))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(168)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(871))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(629)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(235))
    searchBox.Name = S(681)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(122))
    searchBox.Text = S(216)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(115))
    content.Name = S(310)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(538))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(329))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(387))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(303))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(96)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(755) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(252) .. (item.Subtitle or S(97))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(592))
        footerLabel.Text = i18n.T(S(354))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(767)] = function()
return function(import)
local gameApi = import(S(294))(import)
local log = import(S(47))
local runGuarded = import(S(584))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(349) .. minItemsToCollect .. S(200))
    else
        log(S(651))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(314) then
            log(S(568))
            lastReported = S(865)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(76) then
            log(S(422))
            lastReported = S(55)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(293) then
            local capacity = gameApi.ChestCapacity()
            log(S(307) .. count .. S(305) .. want .. S(773)
                .. (capacity and (S(99) .. capacity .. S(561)) or S(845)))
            lastReported = S(286)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(683), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(703) .. count .. S(820) .. after .. S(42))
        else
            log(S(59) .. count
                .. S(822) .. tostring(after) .. S(505))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(477)] = function()
return function(import)
local gameApi = import(S(887))(import)
local log = import(S(220))
local runGuarded = import(S(390))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(734) .. (enabled and S(884) or S(335)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(628) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(227) .. reward.category .. S(650)
                .. tostring(reward.tierKey) .. S(368) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(397) .. #pending .. S(416))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(634), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(872)] = function()
return function(import)
local getCurrentPlot = import(S(880))
local getPlotParts = import(S(420))
local log = import(S(427))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(712), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(781) .. (enabled and S(69) or S(490)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(31)] = function()
return function(import)
local Players = game:GetService(S(211))
local getCurrentPlot = import(S(890))
local getPlotParts = import(S(360))
local getMinerSlots = import(S(492))(import)
local teleport = import(S(471))
local parseMoney = import(S(727))
local gameApi = import(S(654))(import)
local log = import(S(470))
local runGuarded = import(S(596))(import)
local spendPolicy = import(S(458))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(739)
local LOCKED_SLOT_TEXT = S(435)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(574))
    return pad and pad:FindFirstChildWhichIsA(S(283), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(82)) then
            local digits, suffix = descendant.Text:match(S(587))
            if digits then
                local value = tonumber((digits:gsub(S(66), S(746))))
                if value then
                    if suffix ~= S(304) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(519)
    return (text:gsub(S(107), S(406)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(823)) and descendant.Text:match(S(798)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(850) .. holder.Name .. S(364)
            .. gameApi.MinerSlots() .. S(240) .. gameApi.MaxMinerSlots() .. S(386)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(239)
    end
    if not prompt.Enabled then
        return true, S(219)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(444) .. price
    end
    local action = prompt.ActionText or S(460)
    if action:match(S(619)) or action:match(S(486)) or action:match(S(148)) then
        return true, S(45) .. action .. S(108)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(821))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(94) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(661) .. why .. S(433))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(750))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(274) .. prompt.ObjectText
                    .. S(162) .. shown .. S(881) .. math.floor(base) .. S(296))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(795))
    if line ~= lastSurvey then
        log(S(313) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(118), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(365), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(230)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(827), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(61) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(319))
    else
        lastAttempt = 0
        log(S(540))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(451),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(588)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(528)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(236)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(580))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(377) .. holder.Name .. S(572))
            return false
        end
        log(S(570) .. candidate.slot .. S(891) .. candidate.name
            .. S(682) .. tostring(why))
    end
    if not miner then
        log(S(476))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(660) .. holder.Name .. S(270) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(457) .. miner.name .. S(765) .. miner.power
        .. S(81) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(157) .. entry.name .. S(615) .. entry.holder.Name
            .. S(748) .. tostring(entry.prompt.ActionText) .. S(877))
        return false
    end
    log(S(437) .. entry.name .. S(732) .. entry.power
        .. S(456) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(379) .. pad.holder.Name .. S(578)
                .. tostring(pad.priceText))
        else
            log(S(400) .. pad.holder.Name
                .. S(241) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(783) .. holderName .. S(819)
            .. string.format(S(440), before) .. S(593) .. string.format(S(825), after)
            .. S(842) .. SWAP_COOLDOWN .. S(724))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(334)
            .. string.format(S(759), bestBefore) .. S(195)
            .. string.format(S(383), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(624), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(640))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(532) then
        runGuarded(S(595), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(560) then
        log(S(892) .. target.holder.Name
            .. S(834) .. tostring(target.priceText))
        runGuarded(S(51), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(410) .. target.holder.Name .. S(178) .. target.name
        .. S(392) .. target.power .. S(782) .. best.name .. S(436) .. best.power .. S(20))
    runGuarded(S(658), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(321)] = function()
return function(import)
local gameApi = import(S(817))(import)
local parseMoney = import(S(656))
local log = import(S(576))
local runGuarded = import(S(465))(import)
local spendPolicy = import(S(547))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(527) .. (enabled and S(190) or S(491)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(801)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(602) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(355) .. deepestKey .. S(677))
        return true
    end
    log(S(439) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(402)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(408) .. gameApi.ZoneDisplayName(zoneKey) .. S(500)
                .. math.floor(price) .. S(259) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(599) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(722) .. zoneKey .. S(770))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(114) then
            reportedLocked = S(690)
            log(S(401) .. zoneKey .. S(769)
                .. gameApi.ZoneDisplayName(currentKey) .. S(831)
                .. done .. S(839) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(242)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(50), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(504)] = function()
return function(import)
local gameApi = import(S(575))(import)
local log = import(S(146))
local runGuarded = import(S(156))(import)
local spendPolicy = import(S(261))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(876) .. (enabled and S(610) or S(419)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(630) .. node.zoneKey .. S(399) .. node.id
                    .. S(215) .. node.family .. S(768) .. node.level .. S(17) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(56) .. node.id
                    .. S(598) .. node.family .. S(186) .. node.level .. S(705) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(771) .. blocked
                .. S(874))
        elseif powerBlocked then
            log(S(557) .. powerBlocked.zoneKey .. S(684) .. powerBlocked.id
                .. S(182) .. powerBlocked.family .. S(8)
                .. math.floor(powerBlocked.powerReq) .. S(311)
                .. math.floor(powerBlocked.power)
                .. S(868))
        else
            log(S(103)
                .. S(100) .. math.floor(gameApi.GetMoney()) .. S(16))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(298) .. nearest.zoneKey
                    .. S(699) .. nearest.id .. S(137) .. nearest.family .. S(751)
                    .. nearest.level .. S(763) .. nearest.max
                    .. S(138) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(351)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(515),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(169))
        end
        if not buyCore then
            table.insert(parts, S(563))
        end
        if #parts == 0 then
            return S(105)
        end
        return S(858) .. table.concat(parts, S(388)) .. S(112)
    end
    local why
    if nearest.cost > budget then
        why = S(463) .. string.format(S(58), nearest.cost)
            .. S(697) .. string.format(S(40), money)
    elseif nearest.powerOk == false then
        why = S(454) .. string.format(S(790), nearest.powerReq) .. S(597)
    else
        why = S(789)
    end
    return S(559) .. nearest.zoneKey .. S(344) .. tostring(nearest.id)
        .. S(21) .. tostring(nearest.family) .. S(652) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(73) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(514) and cost > best)
            or (mode ~= S(272) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(95)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(807), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(104)] = function()
return function(import)
local Workspace = game:GetService(S(641))
local gameApi = import(S(12))(import)
local teleport = import(S(442))
local log = import(S(731))
local runGuarded = import(S(295))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(342))
    else
        lastAttempt = 0
        log(S(780))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(362))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(266), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(75)
                .. (sold and S(277)
                         or S(625)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(860) .. (sold and (S(603) .. #keys .. S(718)) or S(24)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(136), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(80)] = function()
return function(import)
local Workspace = game:GetService(S(792))
local gameApi = import(S(510))(import)
local teleport = import(S(546))
local log = import(S(545))
local runGuarded = import(S(542))(import)
local spendPolicy = import(S(345))(import)
local STATS = { S(126), S(708), S(246) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(381)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(111) .. (enabled and S(247) or S(623)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(859) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(772) or S(481)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(523) .. holderKey .. S(213) .. choice.stat .. S(788)
                        .. choice.info.level .. S(581) .. after.level
                        .. S(521) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(228) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(564))
    local shop = stand and stand:FindFirstChild(S(154))
    if not shop then
        return nil
    end
    if shop:IsA(S(525)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(609), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(841)
                .. (bought and S(396)
                           or S(205)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(98))
        end
    end
    if bought then
        log(S(180) .. pickaxe.index .. S(689) .. pickaxe.name
            .. S(702) .. pickaxe.multiplier .. S(374))
        return true
    end
    log(S(191) .. pickaxe.index .. S(140) .. pickaxe.name
        .. S(520))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(601) .. holderKey .. S(626)
                    .. tostring(worn or S(449)) .. S(541) .. wanted.name)
            else
                log(S(753) .. holderKey .. S(249)
                    .. wanted.name .. S(614) .. tostring(nowWorn or S(70)) .. S(369))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(539) .. holderKey .. S(855) .. variant
                .. S(385) .. after .. S(359) .. cost .. S(251))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(309))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(837) .. gameApi.OwnedPickaxeCount()
                    .. S(160) .. gameApi.MaxMinerSlots()
                    .. S(620))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(341))
        elseif maxed > 0 then
            log(S(713))
        else
            log(S(567))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(276) and price > best)
            or (mode ~= S(166) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(91))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(297), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(816)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(301))
local Workspace = game:GetService(S(793))
local log = import(S(358))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(238), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(172) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(647))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(757) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(497) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(201) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(711) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(424) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(417))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(493) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(487))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(378), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(706) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(489) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(833) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(714), keys)
end
function api.EquipSlot(slot)
    return send(S(49), slot == S(701) and S(760) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(330), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(726), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(4), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(848))
end
function api.BuySkill(zoneKey, family)
    return send(S(503), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(883)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(71)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(124))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(30) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(23) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(206), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(670))
    plots = plots and plots:FindFirstChild(S(709))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(287)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(741)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(389))
    local gui = board and board:FindFirstChild(S(485))
    local root = gui and gui:FindFirstChild(S(245))
    local list = root and root:FindFirstChild(S(147))
    local row = list and list:FindFirstChild(S(35) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(29)) and descendant.Text ~= S(373) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(328) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(730), characterKey, stat == S(393) and S(531) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(264), max = S(423), cost = S(666) },
    Speed = { level = S(119), max = S(299), cost = S(844) },
    MineSpeed = { level = S(315), max = S(391), cost = S(262) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(862) or charName == S(551) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(106) .. tostring(value))
        end
        table.sort(fields)
        log(S(482) .. holderKey .. S(77) .. table.concat(fields, S(19)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(535) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(869) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(586) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(452) and type(holder.CharName) == S(590) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(851), S(181) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(198) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(372)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(36)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(797)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(248) then
        return {}, S(836)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(65)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(203) or type(state.Track) ~= S(86) then
                table.insert(notes, tabKey .. S(736) .. tostring(category) .. S(530))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(672) .. waiting
                        .. S(187))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(571) .. total .. S(669)
            .. (#notes > 0 and (S(694) .. table.concat(notes, S(518))) or S(194))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(3) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(717)] = function()
return function(import)
local Players = game:GetService(S(376))
local gameApi = import(S(139))(import)
local log = import(S(353))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(875))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(715), S(237))))
    if not value then
        return nil
    end
    if suffix ~= S(44) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(617))
    local mineGui = playerGui and playerGui:FindFirstChild(S(665))
    local hotbar = mineGui and mineGui:FindFirstChild(S(838))
    local content = hotbar and hotbar:FindFirstChild(S(679))
    return content and content:FindFirstChild(S(101))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(10)) then
            local count = descendant.Text:match(S(123))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(585))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(398))
    local nameLabel = content and content:FindFirstChild(S(688))
    local damageLabel = content and content:FindFirstChild(S(175))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(110) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(533) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(675) .. slot .. S(756) .. info.name
                        .. S(853) .. info.power .. S(861) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(428)] = function()
local Players = game:GetService(S(573))
local Workspace = game:GetService(S(778))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(776))
    local plots = mineWorld and mineWorld:FindFirstChild(S(317))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(453)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(83)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(632)),
        Nodes = plot:FindFirstChild(S(657)),
        Chest = plot:FindFirstChild(S(375)),
        Zones = plot:FindFirstChild(S(882)),
        Crew = plot:FindFirstChild(S(278)),
        Loot = plot:FindFirstChild(S(352)),
        PlotIndex = plot:GetAttribute(S(289)),
        ChestCapacity = plot:GetAttribute(S(863)),
    }
end
return getPlotParts
end
modules[S(484)] = function()
return function(import)
    local createWindow = import(S(835))(import)
    local i18n = import(S(267))
    local antiAfk = import(S(462))(import)
    local buildEnv = import(S(663))
    local scheduler = import(S(644))(import)
    local spendPolicy = import(S(742))(import)
    local config = import(S(151))(import)
    local log = import(S(886))
    log(S(800) .. buildEnv.Commit .. S(34) .. buildEnv.Env
        .. S(870) .. buildEnv.BuiltAt .. S(426))
    local window = createWindow({
        Subtitle = S(22),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(189))(import)
    local autoPlace = import(S(631))(import)
    local autoChest = import(S(888))(import)
    local autoSell = import(S(411))(import)
    local autoUpgrade = import(S(698))(import)
    local autoResearch = import(S(210))(import)
    local autoClaim = import(S(218))(import)
    local autoPlot = import(S(646))(import)
    local gameApi = import(S(46))(import)
    scheduler.RegisterStandalone(S(577), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(5), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(762), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(18), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(582), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(636), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(209), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(348), 20)
    scheduler.RegisterExclusive(S(680), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(507), autoResearch.NextPrice)
    spendPolicy.Register(S(743), autoUpgrade.NextPrice)
    spendPolicy.Register(S(830), autoPlot.NextPrice)
    spendPolicy.Register(S(149), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(199), S(474))
    farmTab:AddSectionHeader(S(662))
    persistentSegmented(
        farmTab,
        { S(78), S(38), S(671) },
        { S(413), S(502), S(282) },
        S(48), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(366),
        S(279)
    ), S(480), autoMine.SetEnabled)
    persistentSlider(farmTab, S(193), S(403),
        1, 30, S(455), 5, autoMine.SetCyclesPerSecond, S(37))
    persistentSlider(farmTab, S(653), S(57),
        1, 10, S(516), 1, autoMine.SetClicksPerCycle, S(6))
    persistentToggle(farmTab:AddCard(
        S(153),
        S(275)
    ), S(165), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(473), S(721),
        1, 60, S(339), 3, autoPlace.SetCheckInterval, S(813))
    persistentToggle(
        farmTab:AddCard(S(128),
            S(221)),
        S(811), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(120),
            S(322)),
        S(176), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(744), S(222),
        5, 300, S(621), 60, autoPlace.SetEquipBestInterval, S(796))
    persistentToggle(
        farmTab:AddCard(S(648),
            S(244)),
        S(749), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(188), S(331),
        0, 500, S(113), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(256))
    persistentToggle(farmTab:AddCard(
        S(498),
        S(818)
    ), S(785), autoChest.SetEnabled)
    persistentSlider(farmTab, S(407), S(787),
        50, 3000, S(764), 100, autoChest.SetMinItems, S(370))
    persistentSlider(farmTab, S(144), S(184),
        1, 60, S(639), 3, autoChest.SetCheckInterval, S(231))
    persistentToggle(farmTab:AddCard(
        S(226),
        S(425)
    ), S(84), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(357),
            S(337)),
        S(812), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(479), S(517),
        5, 300, S(604), 20, autoSell.SetCheckInterval, S(723))
    farmTab:AddSlider(S(553),
        S(88),
        5, 120, sellMaxWait, function(value)
            config.Set(S(692), value)
        end, S(550))
    persistentToggle(farmTab:AddCard(
        S(775),
        S(132)
    ), S(558), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(441), S(142),
        5, 300, S(501), 20, autoUpgrade.SetInterval, S(204))
    persistentSlider(farmTab, S(380), S(594),
        1, 20, S(183), 5, autoUpgrade.SetMaxPerPass, S(217))
    persistentSegmented(
        farmTab,
        { S(777), S(27), S(312), S(347) },
        { S(472), S(885), S(829), S(179) },
        S(74), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(745), S(167),
        0, 500, S(116), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(429))
    persistentToggle(
        farmTab:AddCard(S(326),
            S(659)),
        S(196), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(250) .. pickaxe.multiplier .. S(421)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(443)
            end
            return S(177) .. index .. S(260) .. (pickaxeLabels[index] or S(685))
        end
        local savedPick = config.Get(S(526), 0)
        pickaxeCard = farmTab:AddSlider(
            S(325),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(41), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(808)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(879),
            S(556)),
        S(512), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(802),
            S(606)),
        S(13), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(562),
            S(700)),
        S(155), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(467), S(405),
        1, 20, S(633), 2, autoUpgrade.SetMutationTarget, S(478))
    persistentSlider(farmTab, S(15), S(133),
        0, 500, S(794), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(849))
    persistentToggle(farmTab:AddCard(
        S(589),
        S(258)
    ), S(32), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(804), S(92),
        10, 600, S(62), 30, autoResearch.SetCheckInterval, S(25))
    persistentSlider(farmTab, S(412), S(810),
        1, 20, S(583), 5, autoResearch.SetMaxPerPass, S(60))
    persistentSlider(farmTab, S(26), S(363),
        0, 500, S(306), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(710))
    persistentToggle(
        farmTab:AddCard(S(434),
            S(522)),
        S(356), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(343),
            S(555)),
        S(340), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(786),
            S(141)),
        S(257), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(394), S(779) },
        { true, false },
        S(192), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(161),
        S(127)
    ), S(668), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(534), S(159),
        5, 300, S(737), 30, autoClaim.SetCheckInterval, S(7))
    persistentSlider(farmTab, S(346), S(508),
        1, 30, S(513), 10, autoClaim.SetMaxPerPass, S(54))
    persistentToggle(farmTab:AddCard(
        S(52),
        S(761)
    ), S(524), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(409), S(418),
        5, 300, S(893), 30, autoPlot.SetCheckInterval, S(445))
    persistentToggle(
        farmTab:AddCard(S(431),
            S(292)),
        S(720), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(171),
            S(752)),
        S(125), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(704), S(707),
        0, 500, S(511), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(255))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(766))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(430), S(164))
    persistentToggle(
        settingsTab:AddCard(S(414), S(607)),
        S(214),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(824), 100)
    settingsTab:AddSlider(S(152), S(9), 50, 150, savedScale, function(value)
        config.Set(S(719), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(269), 1)
    settingsTab:AddSegmented({ S(506), S(464), S(131) }, savedLanguage, function(index, value)
        local code = ({ S(291), S(468), S(691) })[index]
        config.Set(S(828), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(536), S(323), S(233) })[savedLanguage] or S(243))
    log(S(459) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(53)] = function()
return {
    [79389059854988] = S(566),
}
end
import(S(754))(import)
