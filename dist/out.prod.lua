-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"th", "open_menu_hint", "2026-09-21 13:09:09", "autoPlaceUnlockBudget", "[AutoPlace] picked up ", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "TextLabel", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Frame", " error: ", "GetCharacterMineSpeedUpgradeCost", "Mining", "AutoPlace", "th", "[AutoUpgrade] ", "autoResearchCore", "/", " costs ", "ไล่อัปเกรด miner ทุกกี่วินาที", "table", "[GameAPI] shapes: Hotbar is empty", "Auto Plot: ซื้อโซนถัดไป", "upgrade", "M", "AutoPlace", "CharMineSpeedMaxLevel", "", "TextLabel", "[AutoClaim] ", " (", "Frame", "All", "core/logger", "Speed", "games/my_anime_mine/game_api", "autoChestMinItems", "MineSpeed", "", "AutoResearch", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "works from anywhere - no teleport needed", "core/logger", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "core/teleport", "Frame", "core/spend_policy", "UIStroke", "function", "\n", "[AutoChest] collected ", "Modules", "Auto Mine: ความเร็ว", "MineBuyPickaxe", "AutoChest", " but no unclaimed reward in its Track", "core/debug_tab", "Workspace", "[AutoUpgrade] ", "ItemName", "ลำดับการใช้เงินรวมทุกระบบ", "waiting", "GetHolderLevel", "autoUpgradeInterval", "Main", "autoPlot", "", "disabled", "write reported success but the line isn't in ", " ", "Auto Research (Plot)", "empty", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " stack(s)", " ", "Workspace", " waited ", "[AntiAFK] enabled - watching Player.Idled", "ZoneBoardGui", "core/logger", "autoSell", "=empty", "games/my_anime_mine/auto_plot", "games/my_anime_mine/game_api", ") - leaving it alone for ", "games/my_anime_mine/game_api", "My Anime Mine · Mining Hub", "leaderstats", "autoPlotKeepMoney", " items (now ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "Auto Sell", "AutoUpgrade", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", " costs ", "Auto Upgrade: กันเงินไว้", "?", "Speed ก่อน", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "[AutoPlace] upgrading ", "s", ": badge says ", "games/my_anime_mine/auto_claim", "^Swap out ", "x", "games/my_anime_mine/auto_sell", "แพงสุดก่อน", "ClaimableTiers failed", "BasePart", ") gave nothing", "autoPlaceUpgradeWeak", "autoUpgradeMaxPerPass", "Mutation: เพดานราคาต่อครั้ง", "[AutoPlace] ", "MekPro", "[AutoResearch] ", "games/my_anime_mine/auto_plot", "disabled", "Damage", "table", "", "Core", "[Scheduler] ", "Workspace", "TextButton", "autoUpgradePickaxe", "MineUpgradeCharacter", " (", "string", "UserInputService", "core/get_place_id", ",", "[MekPro] build ", ") ", "table", "autoMineSpeed", ",", "Root", "[", "[Config] couldn't read ", "neither appendfile nor writefile is available", " waited ", "AutoMine", "games/my_anime_mine/game_api", "core/spend_policy", "games/my_anime_mine/game_api", "Content", "games/my_anime_mine/auto_place", "Auto Upgrade Plot (Zone)", "slot ", "Players", " team power", "core/run_job", "AutoResearch", "Core buying is off", "[AutoResearch] idle: ", "/", "MineSpeed", "[Uu]nlock", "", "none", "/", " (", "", "Backpack", "core/dispatch", "อังกฤษ", "locked slot", "AutoPlot", "Open menu", "CoreUI", "แพงสุดก่อน", "MineGui", "", " refused - ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "MineTakeChest", "autoMine", "ReplicatedStorage", "Frame", " thing available first", "%.3g", ") didn't take: ", "autoPlace", "unreadable", "Frame", "[AutoPlace] ", " ParseCharStack=", "Auto Place: รอบดึงตัวเก่ง", " ", " didn't go through (out of stock or too expensive)", "core/ui", "Plots", "Auto Place: รอบเช็ค", "", "UIListLayout", " (chest holds ", " ", " (x", "core/spend_policy", "[AutoPlace] unlocking ", " -> ", "cheapest open node ", "works from anywhere - dropping the teleport", "[AutoPlot] ", "function", "MineRollMutation", "core/logger", "", "autoPlotBuy", "/", "HttpService", "games/my_anime_mine/auto_claim", ", ", " (", "([%d,%.]+)%s*(%a*)", "enabled", "MineEquipBest", "core/run_job", "autoUpgradeEquipPickaxe", "This is not a supported place.", "autoResearchZones", "=locked(", " (still ", "autoSellMaxWait", "Container", "AutoSell", "cost", "[%X] ", "/config.json", "AutoPlace", "PickaxesStand", " -> ", "s", "] job failed: ", ", base ", "core/logger", "Auto Place: เปลี่ยนตัวอ่อนออก", "UserInputService", "MinePower", "TextButton", "M", "disabled", "Badge", "TextButton", "core/build_env", "string", "autoUpgradePickaxeIndex", ") on ", "PlotIndex", "games/registry", "en", "badge says ", "Header", "[AutoPlace] unlocked ", "autoUpgrade", "[AutoPlace] couldn't hold anything - not pressing E", " GetVariantMult=", "off - every automation spends on its own", " -> ", " while selecting", "TextLabel", "autoResearchCoreLast", "core/spend_policy", "BodyPosition", " (prompt says '", "HumanoidRootPart", ")", "core/config", "autoMineClicks", "s", "Anti-AFK", "ขนาดเมนู", " ", "Content", "HumanoidRootPart", " wouldn't take ", "en", "M", "Auto Research: ซื้อสูงสุดต่อรอบ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "ZoneRow", "X", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "TextLabel", " -> x", "core/logger", "CoreGui", "[AutoPlace] slot ", "=", "fill", "GuiService", "Workspace", "autoResearchMaxPerPass", "autoPlaceEquipBest", "Players", ": ", "Damage", "", "(", "Auto Plot: กันเงินไว้", "CharMaxLevel", "core/logger", "[AutoClaim] idle: ", "games/my_anime_mine/auto_mine", "s - carrying on without it", "Players", ": ", " ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "", " ", "nothing claimable", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "dc8a1d7+", "AutoPlace", "both Zone and Core buying are switched off", "TabBar", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "ValueLabel", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Auto Mine: คลิกต่อรอบ", "games/my_anime_mine/auto_research", " not bought yet (", "[AutoResearch] ", "[AutoChest] chest is empty", "[GameAPI] shapes: Holders.", ")", "enabled", "autoClaimMaxPerPass", "ไทย", "core/logger", " #", "Exclusive", "OwnerUserId", "/s", "Players", "[AutoClaim] claimed ", "core/parse_money", "games/my_anime_mine/main", "OwnerUserId", "research", "games/my_anime_mine/game_api", "[AntiAFK] disabled", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "core/get_me", "Auto Claim: เก็บสูงสุดต่อรอบ", "", "core/teleport", "] = ", " has been busy for ", "[AutoClaim] ", "Empty miner slot", "AutoUpgrade", "[AutoPlace] Equip Best: hotbar's strongest ", "[AutoSell] disabled", ")", "autoResearchInterval", "disabled", " still locked after paying ", " -> ", " possible holder(s) - not buying more", "Search functions...", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "/", "games/my_anime_mine/auto_place", "ค้นหาฟังก์ชัน...", "Players", "table", "Shop Stand", "s", " | ", "ตามเครื่อง", "(nothing to do)", "Auto Chest: เกณฑ์เก็บ", "games/my_anime_mine/get_current_plot", "Auto Sell: รอบขาย", "costs ", "[AntiAFK] idle detected, simulating input", "languageIndex", " items)", "[AutoPlot] ", "ถูกสุดก่อน", "enabled", " ", "[AutoPlace] disabled", " ", "[AutoPlace] placed ", "Auto Research: กันเงินไว้", "[AutoSell] enabled", " mutation x", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Hauler", "x", "[Bb]uy", "%$", "[AutoUpgrade] ", "ต่ำสุดก่อน", " pickaxe(s) for ", "[AutoPlace] couldn't pick up ", "[AutoResearch] ", "autoPlaceEquipBestInterval", " (cost ", "TextLabel", "/", "antiAfk", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/i18n", "ย่อขยายเมนูทั้งชุด", ": ", "VirtualUser", "table", "Speed", "", " (", "sold ", " from ", "[AutoResearch] cheapest open node: ", "AutoUpgrade", "", "%", "UIListLayout", "core/scheduler", ") ", "", "auto", "unreadable", " team power, you have ", "Character", "menuScale", "autoClaim", "[AutoPlace] ", "dearest", "off", "ไล่ซื้อทุกกี่วินาที", "core/ui", "", "autoPlotInterval", "core/logger", ")", "ปุ่มเปิดเมนู", "/", "[AutoPlace] holders: ", "[Logger] failed to write ", "Players", "core/teleport", "⚙", "[AutoPlot] ", "Locked miner slot", "games/my_anime_mine/game_api", " (x", "0 = ไม่จำกัด", "MineSellItems", "TextButton", "core/logger", "s", "BasePart", ")", "TextButton", "table", "games/registry", "Frame", "core/logger", "TextLabel", "search_placeholder", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "%$", "server didn't equip it", "[MekPro] settings file: ", "x", "disabled", "languageIndex", "%.3g", " GetHolderDamage=", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "[AutoResearch] nothing open and affordable right now", " (money ", "AutoPlot", " (power ", "unlock", "[AutoSell] MineSellItems ", "Pad", "[AutoMine] ", "List", "nothing left to buy (", "core/get_place_id", "TextLabel", "off", "Auto Upgrade: ซื้อ Pickaxe ด้วย", " reward(s) looked claimable but none went through", "AutoPlace", "[AutoPlot] couldn't switch to ", "games/my_anime_mine/auto_upgrade", "every open node is maxed", "autoUpgradeKeepMoney", "AutoChest", " owned)", "TextLabel", "[Scheduler] ", " (", "[AutoChest] disabled", "[GameAPI] shapes: inventory entry ", "ScrollingFrame", ")", "lowest", "ถูกสุดก่อน", " power ", "autoUpgradeMutationBudget", "off", "core/parse_money", "MineSpeed", "[GameAPI] SendEvent failed: ", "MineBuySkill", "MekPro.log", "modules not loaded", "#", "[AutoUpgrade] every stat on every miner is maxed", "Humanoid", "cheapest", "core/i18n", "", "games/my_anime_mine/game_api", "table", "%.0f", "none", ")", "autoResearchCheapestFirst", "Mutation: หยุดเมื่อได้ x", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", ") ", "[AutoPlace] drifted out of range of ", "TextButton", "TextLabel", "affordable - waiting for a turn", "Auto Upgrade: สุ่ม Mutation", "ต่างคนต่างซื้อ", "Auto Chest: รอบเช็ค", "[AutoUpgrade] ", "Auto Upgrade Unit", "", "needs ", ") ", "core/anti_afk", "Loot", "prod", "table", "auto", "Damage ก่อน", " pickaxe ", "games/my_anime_mine/auto_chest", "ChestCapacity", "games/my_anime_mine/get_best_miner", "[GameAPI] holder ", "waiting", "GuiService", "Auto Sell: รอคิวได้นานสุด", "Frame", "Humanoid", "may need us at the stand - trying that", "^M(%d+)$", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "UIPadding", "core/config", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "sale didn't go through", "games/my_anime_mine/get_best_miner", "Frame", "Auto Claim (Index)", "s", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "autoSellMaxWait", "games/my_anime_mine/auto_mine", "Auto Place: ปลดล็อกช่อง miner", "เช็คโซนทุกกี่วินาที", "AutoPlot", "core/get_leaderstat", "GetCharacterUpgradeCost", ") from ", "enabled", "AutoUpgrade", "[AutoPlot] unlocked ", "MineEquipSlot", "MekPro", ") -> ", "x", " ", ",", "^x(%d+)$", "table", "Frame", ")", "UICorner", ")", " ชิ้น", "en", ", you have ", "Text", "autoChestInterval", "Damage", "core/run_job", "auto", "%$%s*([%d,%.]+)%s*(%a*)", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "[AutoUpgrade] pickaxe #", "-", "BasePart", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "%.3g", "", " -> ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "PlayerGui", "unlock", "MineSelectZone", " (", "lowest", " (", "M", "TextLabel", "AutoClaim", " (", "BasePart", "s - jumping the queue", "Auto Plot: ใช้โซนล่าสุดเสมอ", "games/my_anime_mine/auto_research", "VirtualInputManager", "RunService", "en-us", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "\n", "core/click", "core/logger", "[AutoPlace] ", "UIScale", ")", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "core/scheduler", "Players", "th", "slot not in the hotbar", "off", "core/run_job", "open_menu_hint", "ClickDetector", "no mine data", "AutoSell", "[GameAPI] shapes: Hotbar[", "autoClaimInterval", "games/my_anime_mine/auto_sell", "search_placeholder", "core/humanoid", "table", "')", "", "table", "core/run_job", "UICorner", "AutoUpgrade", ": State(", "%.3g", "autoPlotLatest", "prompt disabled", "Players", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", ", built ", "buying the ", "%.0f", "no isfile/readfile available to verify the write", "RunService", "HotbarFrame", "Workspace", " still empty after placing ", "", "Auto Research: รอบเช็ค", "games/my_anime_mine/get_plot_parts", "Zones", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "[AutoPlace] nothing in the hotbar to place", " x", "Content", "auto", " GetHolderLevel=", "Crew", " ", "0 = ไม่จำกัด", "Text", "MineWorld", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "core/get_place_id", "table", "ยิง Equip Best ทุกกี่วินาที", "[AutoPlace] fireproximityprompt error: ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "🏠", "string", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "MineEquipPickaxe", "ZoneBoard", "autoSellInterval", ": ", "menuScale", "MineWorld", "index rewards are disabled in this game build", "AutoPlace", "Search", "Hauler", "research", "AutoPlot", "TextLabel", "table", " research is ", "slot past the usable cap", "nothing to place or upgrade", "core/logger", "Badge", "M", "write reported success but ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "s", "autoResearch", "AutoResearch", "Zone buying is off", "games/my_anime_mine/auto_chest", "Auto Place Miner", "[GameAPI] shapes: no Character entries in the inventory", "ProximityPrompt", "games/my_anime_mine/get_current_plot", "[Scheduler] ", "ขายทุกกี่วินาที", "dearest", "games/my_anime_mine/game_api", "[GameAPI] shapes: no mine data yet", "table", "'", "}", "needs ", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "games/my_anime_mine/main", "Auto Claim: รอบเช็ค", "GetHolderSpeedLevel", "[AutoSell] ", "games/my_anime_mine/auto_upgrade", "Auto Research: ซื้อสาย Zone", "core/anti_afk", "Auto Place: เพดานราคาปลดช่อง", "Auto Chest", "core/spend_policy", ")", " is no better after the swap (", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " fields: ", "dearest", "ItemFrame", "TextBox", "; ", "TextLabel", "autoPlaceInterval", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "x", "%.3g", "games/my_anime_mine/get_plot_parts", ")", ": ", "core/teleport", "SellStand", "UIListLayout", "needs us at the stand - keeping the teleport", "autoUpgradePriority", "cost", "Plots", "core/run_job", "[AutoChest] enabled (collects once >= ", "Auto Upgrade: แจก Pickaxe ให้ miner", " (power ", " -> ", "s", "[AutoResearch] ", "string", "[AutoUpgrade] ", "", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "CharSpeedMaxLevel", "Frame", "^([%d,%.]+)%s*(%a*)$", "Holders", "fill", "Chest", "maxed", "[AutoChest] MineTakeChest had no effect (", "s", "^%$", "autoSellProtectUnits", "empty", "autoUpgradeMutationTarget", "enabled", "s", ", have ", "[AutoPlot] now mining ", "autoResearchKeepMoney", "[AutoResearch] Core ", "AutoClaim", " for ", "core/logger", "table", " ", "", "dearest", "Auto Upgrade: Pickaxe ที่จะซื้อ", "TextButton", "[SpendPolicy] ", "core/run_job", ")", "table", " for ", "Character", "th", " -> ", "autoUpgradeLimitPickaxes", "core/logger", "autoChest", " ", "BodyVelocity", " claimable but none could be resolved", "action '", "[Scheduler] turn: ", "[AutoUpgrade] bought pickaxe #", "AutoPlot", "[Config] couldn't write ", "{", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "Mining ก่อน", "cheapest", "autoPlaceUnlock", "core/build_env", "%.3g", "Players", "อ่านป้ายบน chest ทุกกี่วินาที", "", " items", " - upgrade the miners first", "=", "dearest", "games/my_anime_mine/get_current_plot", " (", "table", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " node(s) looked buyable but the server refused", "ฟาร์ม", "TextLabel", "number", " - ", "", " doesn't exist", ")", "dearest", " ", "Nodes", "TextLabel", "[AutoChest] waiting: ", "", "s behind a purchase that never happened - letting it spend", "%.0f", "%.3g", "core/dispatch", "", "autoUpgradeRollMutation", "games/my_anime_mine/get_plot_parts", "Auto Upgrade: รอบเช็ค", "AutoPlace", "HumanoidRootPart", " (", "Players", "GetCharacterSpeedUpgradeCost", "TextLabel", "core/spend_policy", "ตั้งค่า", "TextLabel", "Auto Mine", "core/i18n", "GetHolderMineSpeedLevel", "=", "button has no size on screen", "spendOrder", "Auto Plot: รอบเช็ค", "ValueLabel", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "core/logger", ") needs ", "[SpendPolicy] ", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "autoUpgradePickaxeIndex", "ScreenGui", "Auto Research: ซื้อสาย Core", "core/parse_money", ")", "[AutoUpgrade] MineBuyPickaxe ", "All", "core/logger", "[GameAPI] couldn't load the game's modules: ", "Limited", "ProximityPrompt", "table", "AutoResearch", "core/run_job", "[GameAPI] shapes:   GetHolderVariant=", "TextButton", "Frame", "[BestMiner] slot ", "string", "[AutoChest] can't read the chest yet"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(748)] = function()
return function(import)
local log = import(S(210))
local Players = game:GetService(S(339))
local VirtualUser = game:GetService(S(413))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(79))
        connection = player.Idled:Connect(function()
            log(S(381))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(346))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(248)] = function()
return { Env = S(548), IsDev = false, Commit = S(317), BuiltAt = S(3) }
end
modules[S(635)] = function()
local VirtualInputManager = game:GetService(S(630))
local GuiService = game:GetService(S(294))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(886)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(566)] = function()
return function(import)
local HttpService = game:GetService(S(214))
local getPlaceId = import(S(489))
local log = import(S(891))
local ROOT_FOLDER = S(587)
local placeFolder = ROOT_FOLDER .. S(444) .. tostring(getPlaceId())
local filePath = placeFolder .. S(232)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(22) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(141) .. filePath .. S(704) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(832) .. filePath .. S(412) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(167)] = function()
return function(import)
    local getPlaceId = import(S(693))
    local registry = import(S(463))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(223))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(580)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(89))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(348)] = function()
local Players = game:GetService(S(309))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(521))
    local backpack = player:WaitForChild(S(166))
    return player, humanoid, backpack
end
return getMe
end
modules[S(132)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(655)] = function()
local Players = game:GetService(S(667))
local RunService = game:GetService(S(673))
local UserInputService = game:GetService(S(131))
local Workspace = game:GetService(S(59))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(561))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(269))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(826))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(626)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(459)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(267))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(883)] = function()
local Players = game:GetService(S(876))
local STRINGS = {
    en = {
        search_placeholder = S(365),
        open_menu_hint = S(171),
    },
    th = {
        search_placeholder = S(369),
        open_menu_hint = S(443),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(632)
    if locale:sub(1, 2):lower() == S(16) then
        return S(1)
    end
    return S(599)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(428) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(81)] = function()
local LOG_FILE = S(517)
local function log(message)
    local line = os.date(S(231)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(122))
            end
            appendfile(LOG_FILE, line .. S(634))
        elseif writefile then
            local existing = S(301)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(51))
        else
            error(S(142))
        end
        if not (isfile and readfile) then
            error(S(672))
        end
        if not isfile(LOG_FILE) then
            error(S(721) .. LOG_FILE .. S(857))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(70) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(446) .. LOG_FILE .. S(299) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(513)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(130) then
        return nil
    end
    local number, suffix = text:match(S(606))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(133), S(864))))
    if not value then
        return nil
    end
    if suffix ~= S(29) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(646)] = function()
return function(import)
local log = import(S(823))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(140) .. name .. S(237) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(641)] = function()
return function(import)
local RunService = game:GetService(S(631))
local log = import(S(807))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(124) .. task_.name .. S(12) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(732) .. task_.name .. S(353)
                    .. string.format(S(671), now - task_.busySince)
                    .. S(308))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(502) .. starved.name .. S(143)
            .. string.format(S(866), now - starved.waitingSince)
            .. S(627))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(829) .. (currentTask and currentTask.name or S(376)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(879)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(902))
local policy = {}
local probes = {}
local order = {}
local mode = S(491)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(893) .. (mode == S(645)
        and S(261)
        or (S(670) .. mode .. S(183))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(512) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(522) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(435) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(814) .. name .. S(78)
            .. string.format(S(527), now - blockedSince[name])
            .. S(865))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(46)] = function()
local Players = game:GetService(S(152))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(278))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(438)] = function()
return function(import)
local i18n = import(S(523))
local UserInputService = game:GetService(S(241))
local GuiService = game:GetService(S(558))
local THEME = {
    BrandName = S(116),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(596))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(661))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(565))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(243))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(439)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(49))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(406))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(98)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(760))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(543)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(506))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(770))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(878))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(11))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(466))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(623))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(536))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(889))
            if not valueLbl then
                valueLbl = Instance.new(S(264))
                valueLbl.Name = S(322)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(719))
            if not badge then
                badge = Instance.new(S(7))
                badge.Name = S(246)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(813))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(787))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(911))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(456))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(613)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(423)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(33))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(182))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(535))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(810)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(571))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(198))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(461))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(601)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(896))
    screenGui.Name = S(172)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(290))
    local main = Instance.new(S(47))
    main.Name = S(66)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(638))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(464))
    tabBar.Name = S(320)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(424))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(594))
    header.Name = S(256)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(247))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(881))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(501))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(427)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(910))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(285)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(126))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(609)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(758))
    searchBox.Name = S(709)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(467))
    searchBox.Text = S(422)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(560))
    content.Name = S(684)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(188))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(862))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(647))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(287))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(690)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(677) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(809) .. (item.Subtitle or S(175))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(654))
        footerLabel.Text = i18n.T(S(2))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(727)] = function()
return function(import)
local gameApi = import(S(469))(import)
local log = import(S(35))
local runGuarded = import(S(775))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(776) .. minItemsToCollect .. S(383))
    else
        log(S(504))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(429) then
            log(S(914))
            lastReported = S(187)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(797) then
            log(S(328))
            lastReported = S(73)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(63) then
            local capacity = gameApi.ChestCapacity()
            log(S(863) .. count .. S(407) .. want .. S(843)
                .. (capacity and (S(199) .. capacity .. S(529)) or S(658)))
            lastReported = S(557)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(56), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(52) .. count .. S(91) .. after .. S(442))
        else
            log(S(793) .. count
                .. S(779) .. tostring(after) .. S(507))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(215)] = function()
return function(import)
local gameApi = import(S(87))(import)
local log = import(S(305))
local runGuarded = import(S(604))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(354) .. (enabled and S(219) or S(474)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(306) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(340) .. reward.category .. S(688)
                .. tostring(reward.tierKey) .. S(335) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(31) .. #pending .. S(493))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(805), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(576)] = function()
return function(import)
local getCurrentPlot = import(S(731))
local getPlotParts = import(S(679))
local log = import(S(441))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(648), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(486) .. (enabled and S(583) or S(361)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(368)] = function()
return function(import)
local Players = game:GetService(S(642))
local getCurrentPlot = import(S(378))
local getPlotParts = import(S(765))
local getMinerSlots = import(S(555))(import)
local teleport = import(S(351))
local parseMoney = import(S(898))
local gameApi = import(S(147))(import)
local log = import(S(239))
local runGuarded = import(S(815))(import)
local spendPolicy = import(S(202))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(355)
local LOCKED_SLOT_TEXT = S(451)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(485))
    return pad and pad:FindFirstChildWhichIsA(S(905), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(30)) then
            local digits, suffix = descendant.Text:match(S(788))
            if digits then
                local value = tonumber((digits:gsub(S(591), S(784))))
                if value then
                    if suffix ~= S(40) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(197)
    return (text:gsub(S(105), S(869)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(713)) and descendant.Text:match(S(795)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(151) .. holder.Name .. S(326)
            .. gameApi.MinerSlots() .. S(158) .. gameApi.MaxMinerSlots() .. S(500)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(169)
    end
    if not prompt.Enabled then
        return true, S(666)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(380) .. price
    end
    local action = prompt.ActionText or S(842)
    if action:match(S(160)) or action:match(S(397)) or action:match(S(470)) then
        return true, S(828) .. action .. S(738)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(874))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(696) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(225) .. why .. S(359))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(83))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(885) .. prompt.ObjectText
                    .. S(302) .. shown .. S(238) .. math.floor(base) .. S(270))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(374))
    if line ~= lastSurvey then
        log(S(445) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(790), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(25), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(708)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(483), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(115) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(388))
    else
        lastAttempt = 0
        log(S(668))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(700),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(644)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(716)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(471)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(682))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(534) .. holder.Name .. S(263))
            return false
        end
        log(S(291) .. candidate.slot .. S(217) .. candidate.name
            .. S(185) .. tostring(why))
    end
    if not miner then
        log(S(259))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(637) .. holder.Name .. S(676) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(390) .. miner.name .. S(778) .. miner.power
        .. S(251) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(402) .. entry.name .. S(419) .. entry.holder.Name
            .. S(268) .. tostring(entry.prompt.ActionText) .. S(657))
        return false
    end
    log(S(5) .. entry.name .. S(482) .. entry.power
        .. S(582) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(434) .. pad.holder.Name .. S(362)
                .. tostring(pad.priceText))
        else
            log(S(257) .. pad.holder.Name
                .. S(806) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(189) .. holderName .. S(753)
            .. string.format(S(867), before) .. S(262) .. string.format(S(764), after)
            .. S(86) .. SWAP_COOLDOWN .. S(102))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(357)
            .. string.format(S(476), bestBefore) .. S(614)
            .. string.format(S(839), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(873), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(717))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(293) then
        runGuarded(S(233), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(617) then
        log(S(203) .. target.holder.Name
            .. S(818) .. tostring(target.priceText))
        runGuarded(S(494), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(101) .. target.holder.Name .. S(767) .. target.name
        .. S(503) .. target.power .. S(588) .. best.name .. S(619) .. best.power .. S(460))
    runGuarded(S(15), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(118)] = function()
return function(import)
local gameApi = import(S(85))(import)
local parseMoney = import(S(341))
local log = import(S(457))
local runGuarded = import(S(908))(import)
local spendPolicy = import(S(48))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(450) .. (enabled and S(331) or S(245)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(398)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(802) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(875) .. deepestKey .. S(766))
        return true
    end
    log(S(495) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(579)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(207) .. gameApi.ZoneDisplayName(zoneKey) .. S(20)
                .. math.floor(price) .. S(600) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(585) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(129) .. zoneKey .. S(597))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(344) then
            reportedLocked = S(711)
            log(S(384) .. zoneKey .. S(176)
                .. gameApi.ZoneDisplayName(currentKey) .. S(715)
                .. done .. S(163) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(712)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(831), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(325)] = function()
return function(import)
local gameApi = import(S(345))(import)
local log = import(S(289))
local runGuarded = import(S(660))(import)
local spendPolicy = import(S(266))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(781) .. (enabled and S(386) or S(119)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(327) .. node.zoneKey .. S(192) .. node.id
                    .. S(164) .. node.family .. S(533) .. node.level .. S(363) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(804) .. node.id
                    .. S(417) .. node.family .. S(545) .. node.level .. S(235) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(403) .. blocked
                .. S(851))
        elseif powerBlocked then
            log(S(117) .. powerBlocked.zoneKey .. S(200) .. powerBlocked.id
                .. S(848) .. powerBlocked.family .. S(892)
                .. math.floor(powerBlocked.powerReq) .. S(430)
                .. math.floor(powerBlocked.power)
                .. S(844))
        else
            log(S(479)
                .. S(480) .. math.floor(gameApi.GetMoney()) .. S(899))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(420) .. nearest.zoneKey
                    .. S(590) .. nearest.id .. S(621) .. nearest.family .. S(135)
                    .. nearest.level .. S(19) .. nearest.max
                    .. S(96) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(319)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(123),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(726))
        end
        if not buyCore then
            table.insert(parts, S(156))
        end
        if #parts == 0 then
            return S(497)
        end
        return S(488) .. table.concat(parts, S(216)) .. S(639)
    end
    local why
    if nearest.cost > budget then
        why = S(544) .. string.format(S(612), nearest.cost)
            .. S(801) .. string.format(S(184), money)
    elseif nearest.powerOk == false then
        why = S(740) .. string.format(S(664), nearest.powerReq) .. S(153)
    else
        why = S(537)
    end
    return S(205) .. nearest.zoneKey .. S(311) .. tostring(nearest.id)
        .. S(625) .. tostring(nearest.family) .. S(426) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(157) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(734) and cost > best)
            or (mode ~= S(846) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(155)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(725), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(107)] = function()
return function(import)
local Workspace = game:GetService(S(77))
local gameApi = import(S(735))(import)
local teleport = import(S(768))
local log = import(S(636))
local runGuarded = import(S(154))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(358))
    else
        lastAttempt = 0
        log(S(392))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(769))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(730), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(484)
                .. (sold and S(206)
                         or S(771)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(745) .. (sold and (S(418) .. #keys .. S(75)) or S(569)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(229), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(496)] = function()
return function(import)
local Workspace = game:GetService(S(295))
local gameApi = import(S(525))(import)
local teleport = import(S(448))
local log = import(S(44))
local runGuarded = import(S(221))(import)
local spendPolicy = import(S(146))(import)
local STATS = { S(300), S(36), S(159) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(620)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(541) .. (enabled and S(799) or S(69)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(849) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(773) or S(792)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(783) .. holderKey .. S(860) .. choice.stat .. S(825)
                        .. choice.info.level .. S(204) .. after.level
                        .. S(367) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(230) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(234))
    local shop = stand and stand:FindFirstChild(S(372))
    if not shop then
        return nil
    end
    if shop:IsA(S(610)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(110), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(900)
                .. (bought and S(43)
                           or S(562)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(347))
        end
    end
    if bought then
        log(S(830) .. pickaxe.index .. S(76) .. pickaxe.name
            .. S(201) .. pickaxe.multiplier .. S(752))
        return true
    end
    log(S(608) .. pickaxe.index .. S(314) .. pickaxe.name
        .. S(193))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(60) .. holderKey .. S(552)
                    .. tostring(worn or S(528)) .. S(821) .. wanted.name)
            else
                log(S(468) .. holderKey .. S(279)
                    .. wanted.name .. S(226) .. tostring(nowWorn or S(162)) .. S(816))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(17) .. holderKey .. S(393) .. variant
                .. S(288) .. after .. S(405) .. cost .. S(595))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(356))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(399) .. gameApi.OwnedPickaxeCount()
                    .. S(401) .. gameApi.MaxMinerSlots()
                    .. S(364))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(785))
        elseif maxed > 0 then
            log(S(520))
        else
            log(S(697))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(811) and price > best)
            or (mode ~= S(859) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(421))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(584), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(145)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(181))
local Workspace = game:GetService(S(675))
local log = import(S(465))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(53), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(903) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(462))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(808) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(710) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(817) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(714) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(395) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(563))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(515) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(179))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(901), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(737) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(659) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(431) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(455), keys)
end
function api.EquipSlot(slot)
    return send(S(586), slot == S(161) and S(211) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(55), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(701), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(209), holderKey)
end
local describedShapes = false
function api.DescribeShapes()
    if describedShapes or not modules() then
        return
    end
    describedShapes = true
    local mine = api.GetMineData()
    if not mine then
        log(S(736))
        return
    end
    local function describe(value)
        if type(value) ~= S(371) then
            return tostring(value)
        end
        local parts = {}
        for key, item in pairs(value) do
            table.insert(parts, tostring(key) .. S(845) .. tostring(item))
        end
        table.sort(parts)
        return S(833) .. table.concat(parts, S(387)) .. S(739)
    end
    pcall(function()
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(593) and holder.CharName then
                log(S(329) .. holderKey .. S(389) .. describe(holder))
                log(S(909) ..
                    tostring(mineConfig.GetHolderVariant(holder))
                    .. S(260) ..
                    tostring(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
                    .. S(477) .. tostring(mineConfig.GetHolderDamage(holder))
                    .. S(686) .. tostring(mineConfig.GetHolderLevel(holder)))
                break
            end
        end
    end)
    pcall(function()
        local shown = 0
        for slot, key in pairs(mine.Hotbar or {}) do
            log(S(651) .. tostring(slot) .. S(352) .. tostring(key)
                .. S(190) .. tostring(mineConfig.ParseCharStack(key)))
            shown = shown + 1
            if shown >= 3 then
                break
            end
        end
        if shown == 0 then
            log(S(23))
        end
    end)
    pcall(function()
        local entries = mineInventory.Collect(mine, S(34), mineInventory.KindOrder.OreFirst)
        local shown = 0
        for _, entry in pairs(entries or {}) do
            if entry.Kind == S(819) then
                log(S(505) .. describe(entry))
                shown = shown + 1
                if shown >= 2 then
                    break
                end
            end
        end
        if shown == 0 then
            log(S(729))
        end
    end)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(220))
end
function api.BuySkill(zoneKey, family)
    return send(S(516), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(694)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(370)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(242))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(208) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(526) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(618), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(691))
    plots = plots and plots:FindFirstChild(S(774))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(447)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(337)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(702))
    local gui = board and board:FindFirstChild(S(80))
    local root = gui and gui:FindFirstChild(S(139))
    local list = root and root:FindFirstChild(S(487))
    local row = list and list:FindFirstChild(S(284) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(490)) and descendant.Text ~= S(68) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(50) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(128), characterKey, stat == S(14) and S(39) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(64), max = S(304), cost = S(581) },
    Speed = { level = S(744), max = S(786), cost = S(877) },
    MineSpeed = { level = S(884), max = S(28), cost = S(13) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(249) or charName == S(856) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(292) .. tostring(value))
        end
        table.sort(fields)
        log(S(556) .. holderKey .. S(755) .. table.concat(fields, S(276)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(414) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(854) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(699) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(136) and type(holder.CharName) == S(913) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(904), S(336) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(121) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(518)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(649)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(707)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(549) then
        return {}, S(109)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(315)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(656) or type(state.Track) ~= S(906) then
                table.insert(notes, tabKey .. S(663) .. tostring(category) .. S(111))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(103) .. waiting
                        .. S(57))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(255) .. total .. S(827)
            .. (#notes > 0 and (S(855) .. table.concat(notes, S(759))) or S(350))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(782) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(570)] = function()
return function(import)
local Players = game:GetService(S(840))
local gameApi = import(S(37))(import)
local log = import(S(334))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(218))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(138), S(165))))
    if not value then
        return nil
    end
    if suffix ~= S(416) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(616))
    local mineGui = playerGui and playerGui:FindFirstChild(S(174))
    local hotbar = mineGui and mineGui:FindFirstChild(S(674))
    local content = hotbar and hotbar:FindFirstChild(S(148))
    return content and content:FindFirstChild(S(228))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(853)) then
            local count = descendant.Text:match(S(592))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(757))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(277))
    local nameLabel = content and content:FindFirstChild(S(61))
    local damageLabel = content and content:FindFirstChild(S(120))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(313) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(213) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(912) .. slot .. S(310) .. info.name
                        .. S(510) .. info.power .. S(683) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(847)] = function()
local Players = game:GetService(S(298))
local Workspace = game:GetService(S(125))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(706))
    local plots = mineWorld and mineWorld:FindFirstChild(S(195))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(343)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(871)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(789)),
        Nodes = plot:FindFirstChild(S(861)),
        Chest = plot:FindFirstChild(S(791)),
        Zones = plot:FindFirstChild(S(680)),
        Crew = plot:FindFirstChild(S(687)),
        Loot = plot:FindFirstChild(S(547)),
        PlotIndex = plot:GetAttribute(S(252)),
        ChestCapacity = plot:GetAttribute(S(554)),
    }
end
return getPlotParts
end
modules[S(742)] = function()
return function(import)
    local createWindow = import(S(194))(import)
    local i18n = import(S(410))
    local antiAfk = import(S(546))(import)
    local buildEnv = import(S(838))
    local scheduler = import(S(425))(import)
    local spendPolicy = import(S(751))(import)
    local config = import(S(271))(import)
    local log = import(S(718))
    log(S(134) .. buildEnv.Commit .. S(32) .. buildEnv.Env
        .. S(669) .. buildEnv.BuiltAt .. S(858))
    local window = createWindow({
        Subtitle = S(88),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(307))(import)
    local autoPlace = import(S(149))(import)
    local autoChest = import(S(553))(import)
    local autoSell = import(S(653))(import)
    local autoUpgrade = import(S(746))(import)
    local autoResearch = import(S(629))(import)
    local autoClaim = import(S(104))(import)
    local autoPlot = import(S(84))(import)
    local gameApi = import(S(452))(import)
    scheduler.RegisterStandalone(S(144), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(499), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(318), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(94), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(907), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(624), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(170), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(575), 20)
    scheduler.RegisterExclusive(S(650), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(41), autoResearch.NextPrice)
    spendPolicy.Register(S(662), autoUpgrade.NextPrice)
    spendPolicy.Register(S(481), autoPlot.NextPrice)
    spendPolicy.Register(S(27), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(852), S(698))
    farmTab:AddSectionHeader(S(62))
    persistentSegmented(
        farmTab,
        { S(539), S(509), S(173) },
        { S(436), S(836), S(756) },
        S(887), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(882),
        S(10)
    ), S(180), autoMine.SetEnabled)
    persistentSlider(farmTab, S(54), S(574),
        1, 30, S(137), 5, autoMine.SetCyclesPerSecond, S(338))
    persistentSlider(farmTab, S(324), S(92),
        1, 10, S(272), 1, autoMine.SetClicksPerCycle, S(106))
    persistentToggle(farmTab:AddCard(
        S(728),
        S(722)
    ), S(186), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(196), S(321),
        1, 60, S(761), 3, autoPlace.SetCheckInterval, S(794))
    persistentToggle(
        farmTab:AddCard(S(240),
            S(95)),
        S(112), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(316),
            S(894)),
        S(297), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(191), S(695),
        5, 300, S(404), 60, autoPlace.SetEquipBestInterval, S(273))
    persistentToggle(
        farmTab:AddCard(S(577),
            S(178)),
        S(837), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(749), S(689),
        0, 500, S(4), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(622))
    persistentToggle(farmTab:AddCard(
        S(750),
        S(8)
    ), S(824), autoChest.SetEnabled)
    persistentSlider(farmTab, S(377), S(323),
        50, 3000, S(38), 100, autoChest.SetMinItems, S(598))
    persistentSlider(farmTab, S(540), S(841),
        1, 60, S(602), 3, autoChest.SetCheckInterval, S(573))
    persistentToggle(farmTab:AddCard(
        S(93),
        S(890)
    ), S(82), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(681),
            S(478)),
        S(796), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(379), S(733),
        5, 300, S(703), 20, autoSell.SetCheckInterval, S(723))
    farmTab:AddSlider(S(559),
        S(177),
        5, 120, sellMaxWait, function(value)
            config.Set(S(227), value)
        end, S(800))
    persistentToggle(farmTab:AddCard(
        S(542),
        S(564)
    ), S(258), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(872), S(21),
        5, 300, S(65), 20, autoUpgrade.SetInterval, S(780))
    persistentSlider(farmTab, S(9), S(754),
        1, 20, S(113), 5, autoUpgrade.SetMaxPerPass, S(763))
    persistentSegmented(
        farmTab,
        { S(400), S(551), S(99), S(835) },
        { S(508), S(603), S(415), S(514) },
        S(772), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(97), S(394),
        0, 500, S(498), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(281))
    persistentToggle(
        farmTab:AddCard(S(492),
            S(409)),
        S(127), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(453) .. pickaxe.multiplier .. S(330)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(532)
            end
            return S(519) .. index .. S(71) .. (pickaxeLabels[index] or S(640))
        end
        local savedPick = config.Get(S(250), 0)
        pickaxeCard = farmTab:AddSlider(
            S(812),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(895), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(524)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(42),
            S(6)),
        S(822), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(777),
            S(45)),
        S(222), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(538),
            S(100)),
        S(870), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(531), S(366),
        1, 20, S(798), 2, autoUpgrade.SetMutationTarget, S(473))
    persistentSlider(farmTab, S(114), S(454),
        0, 500, S(511), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(26))
    persistentToggle(farmTab:AddCard(
        S(72),
        S(568)
    ), S(724), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(678), S(437),
        10, 600, S(360), 30, autoResearch.SetCheckInterval, S(373))
    persistentSlider(farmTab, S(282), S(567),
        1, 20, S(296), 5, autoResearch.SetMaxPerPass, S(396))
    persistentSlider(farmTab, S(391), S(762),
        0, 500, S(803), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(720))
    persistentToggle(
        farmTab:AddCard(S(747),
            S(633)),
        S(224), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(897),
            S(312)),
        S(18), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(741),
            S(286)),
        S(265), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(385), S(108) },
        { true, false },
        S(530), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(572),
        S(615)
    ), S(433), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(743), S(834),
        5, 300, S(652), 30, autoClaim.SetCheckInterval, S(458))
    persistentSlider(farmTab, S(349), S(850),
        1, 30, S(332), 10, autoClaim.SetMaxPerPass, S(589))
    persistentToggle(farmTab:AddCard(
        S(150),
        S(611)
    ), S(67), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(888), S(578),
        5, 300, S(440), 30, autoPlot.SetCheckInterval, S(236))
    persistentToggle(
        farmTab:AddCard(S(24),
            S(607)),
        S(212), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(628),
            S(692)),
        S(665), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(303), S(283),
        0, 500, S(90), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(244))
    if buildEnv.IsDev then
        task.delay(8, gameApi.DescribeShapes)
    end
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(58))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(880), S(449))
    persistentToggle(
        settingsTab:AddCard(S(274), S(74)),
        S(408),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(705), 100)
    settingsTab:AddSlider(S(275), S(411), 50, 150, savedScale, function(value)
        config.Set(S(432), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(475), 1)
    settingsTab:AddSegmented({ S(375), S(168), S(333) }, savedLanguage, function(index, value)
        local code = ({ S(685), S(254), S(643) })[index]
        config.Set(S(382), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(550), S(280), S(820) })[savedLanguage] or S(605))
    log(S(472) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(253)] = function()
return {
    [79389059854988] = S(342),
}
end
import(S(868))(import)
