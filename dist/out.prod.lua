-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"0 = ไม่จำกัด", "Chest", "Players", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "BasePart", "core/logger", "ZoneBoard", "autoPlotKeepMoney", "autoClaimMaxPerPass", "GetCharacterSpeedUpgradeCost", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", " node(s), ", "Damage", "[AutoResearch] ", "MineSpeed", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", " (prompt says '", " req=", "autoResearchCheapestFirst", "?", "core/logger", "Speed", "Frame", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "autoSellMaxWait", "GetCharacterUpgradeCost", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "number", "disabled", "none", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "core/dispatch", "table", "auto", "autoClaim", "Mining", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " pickaxe ", " items)", "[AutoUpgrade] ", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "TextLabel", "autoChestInterval", " -> ", "ClickDetector", " not maxed", "BasePart", "string", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "autoSellMaxWait", "s", "Auto Research: กันเงินไว้", ")", "core/run_job", " costs ", "Auto Upgrade: กันเงินไว้", "core/get_place_id", "autoResearchKeepMoney", "Open menu", "[AutoClaim] claimed ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " refused - ", "/s", "TabBar", "games/my_anime_mine/game_api", "enabled", "This is not a supported place.", "Shop Stand", "table", "CharMineSpeedMaxLevel", " ", "games/my_anime_mine/main", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "", "ไล่อัปเกรด miner ทุกกี่วินาที", "core/teleport", "[AutoUpgrade] pickaxe #", "AutoSell", "s - jumping the queue", "core/teleport", "Nodes", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "ขนาดเมนู", "Players", "maxed", "", "TextLabel", "ย่อขยายเมนูทั้งชุด", "[AutoPlace] fireproximityprompt error: ", ") ", "TextButton", "Place", ")", "core/get_place_id", "M", "[AutoUpgrade] ", "table", " ", "core/run_job", "ตามเครื่อง", "core/run_job", "[AutoResearch]   ", "HumanoidRootPart", "[AutoChest] disabled", "needs us at the stand - keeping the teleport", "Search functions...", "[AutoPlace] slot ", "core/get_place_id", "AutoResearch", "Hauler", "Zones", " costs ", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "Loot", "[AutoResearch] ", "%.0f", " (", " ", "Speed", "Auto Plot: กันเงินไว้", "/", "Frame", "[AutoResearch] ", "[AutoUpgrade] every stat on every miner is maxed", "table", "ตั้งค่า", "s", "ZoneBoardGui", " power=", "", "MineWorld", "^x(%d+)$", "games/my_anime_mine/auto_chest", "core/debug_tab", "Pad", "autoUpgradePickaxe", "autoMine", " waited ", "Players", "autoResearchCore", "may need us at the stand - trying that", "autoUpgradeMaxPerPass", "autoPlotInterval", "string", "[AutoResearch] ", ",", "/", "TextLabel", " ", " pickaxe(s) for ", "core/get_leaderstat", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", ",", "[AutoResearch] idle: money=", "AutoPlace", "", "[AutoPlace] placed ", "neither appendfile nor writefile is available", " still empty after placing ", "Auto Chest: รอบเช็ค", "Empty miner slot", "Auto Place: รอบเช็ค", "HumanoidRootPart", "([%d,%.]+)%s*(%a*)", "games/my_anime_mine/get_current_plot", " (", " ", ") on ", "th", "BasePart", "AutoUpgrade", "TextButton", "UIPadding", ") -> ", "[AutoMine] ", "core/parse_money", "[MekPro] settings file: ", "core/logger", "games/my_anime_mine/game_api", "Main", "games/my_anime_mine/game_api", "VirtualUser", " mutation x", "Crew", " -> x", "Damage", "MineSpeed", "GuiService", " | ", "auto", "", "X", "/", "[Uu]nlock", "games/my_anime_mine/auto_place", " ", "[Scheduler] turn: ", "VirtualInputManager", "RunService", "TextButton", "Frame", "Search", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "autoPlaceUpgradeWeak", ", you have ", "[MekPro] build ", "search_placeholder", "Auto Mine: คลิกต่อรอบ", "table", "core/click", "[AntiAFK] idle detected, simulating input", "Frame", "Players", "Auto Mine", " buyZones=", "[Config] couldn't write ", "AutoPlace", "PickaxesStand", "ค้นหาฟังก์ชัน...", "d820662+", "core/logger", "[AutoChest] waiting: ", "autoUpgradeLimitPickaxes", "[AutoPlot] ", " from ", "/", "languageIndex", "Workspace", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "Workspace", "TextLabel", "lowest", "table", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "CharMaxLevel", "[GameAPI] SendEvent failed: ", "Humanoid", "ถูกสุดก่อน", "autoChestMinItems", "PlayerGui", "autoUpgradeMutationTarget", "[AutoResearch] cheapest open node: ", "=locked(", "string", "x", "Plots", "[AutoPlot] now mining ", " (x", " open=", ": ", "core/logger", "menuScale", "[AutoClaim] ", "MineSellItems", "core/ui", "=empty", "auto", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "enabled", "M", "games/my_anime_mine/auto_sell", "AutoMine", "ZoneRow", "TextLabel", "autoUpgrade", "search_placeholder", "Auto Claim: รอบเช็ค", "GetHolderMineSpeedLevel", "[AutoSell] MineSellItems ", "MineUpgradeCharacter", " items (now ", "CharSpeedMaxLevel", "function", "Workspace", "BasePart", "Hauler", "", "table", "autoChest", "auto", "[AntiAFK] enabled - watching Player.Idled", ")", "Auto Plot: ซื้อโซนถัดไป", "games/my_anime_mine/get_best_miner", "core/run_job", "core/logger", "games/my_anime_mine/get_plot_parts", "prompt disabled", "ItemFrame", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", " has been busy for ", "[AutoSell] ", "button has no size on screen", "Frame", ")", "Auto Research: ซื้อสาย Zone", "[AutoResearch]   ", "/", "ปุ่มเปิดเมนู", "HotbarFrame", " powerOk=", "Content", "[AutoPlace] ", ": ", "games/my_anime_mine/auto_mine", "TextLabel", "Content", " (power ", "Mining", "", " (x", "Players", "AutoSell", " not bought yet (", "[AutoUpgrade] MineBuyPickaxe ", "^([%d,]+)$", "Auto Plot: ใช้โซนล่าสุดเสมอ", "Text", "AutoClaim", " (", " didn't go through (out of stock or too expensive)", "core/logger", "[AutoSell] disabled", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "open_menu_hint", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "autoMineClicks", "table", "%$%s*([%d,%.]+)%s*(%a*)", "Container", " -> ", "autoUpgradePriority", "core/i18n", "List", "MekPro", " open and unmaxed", "games/my_anime_mine/auto_place", "[AutoPlot] ", "core/i18n", "prod", "Auto Upgrade: รอบเช็ค", "sale didn't go through", "Badge", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "games/my_anime_mine/auto_research", "autoPlace", "menuScale", "[GameAPI] couldn't load the game's modules: ", "", "MineSpeed", " #", "SellStand", "AutoPlot", ")", " -> ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", " - upgrade the miners first", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "autoResearch", "", "(none)", "table", ") from ", "[AutoPlace] nothing in the hotbar to place", "AutoChest", "", ") ", "core/logger", "locked slot", "[Logger] failed to write ", "autoResearchMaxPerPass", " x", "MekPro.log", "Damage", "games/my_anime_mine/get_current_plot", " (money ", "ProximityPrompt", "ChestCapacity", "RunService", "", "[AutoChest] MineTakeChest had no effect (", "Anti-AFK", "x", "games/my_anime_mine/game_api", "TextLabel", "%", "Locked miner slot", "core/parse_money", ")", "autoPlotBuy", "core/logger", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "Backpack", "ScrollingFrame", " research is ", "games/my_anime_mine/game_api", "autoUpgradeEquipPickaxe", "[AutoPlot] couldn't switch to ", "M", "fill", "GetHolderLevel", "", " power ", "core/logger", "%$", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "x", "ต่ำสุดก่อน", "no isfile/readfile available to verify the write", "/", "Auto Sell: รอบขาย", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "games/my_anime_mine/auto_upgrade", "^M(%d+)$", "autoSellInterval", ") ", "ValueLabel", "Auto Chest", "works from anywhere - no teleport needed", "Auto Mine: ความเร็ว", "🏠", " (", "TextLabel", " while selecting", "autoUpgradeInterval", "Frame", "TextButton", "autoUpgradeKeepMoney", "OwnerUserId", "AutoUpgrade", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "%.0f", "core/logger", " buyCore=", "autoSell", "ProximityPrompt", "UserInputService", "[Scheduler] ", "languageIndex", "s", "table", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Holders", "^%$", "[AutoUpgrade] bought pickaxe #", "core/logger", "PlotIndex", "core/config", "core/ui", "", "MineWorld", "[BestMiner] slot ", "Auto Chest: เกณฑ์เก็บ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "[AutoPlace] picked up ", "ReplicatedStorage", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "", "Workspace", "Content", "Frame", "string", "research", "BodyVelocity", "Auto Upgrade: Pickaxe ที่จะซื้อ", "lowest", "Auto Place Miner", "enabled", "[AntiAFK] disabled", "HumanoidRootPart", "[AutoUpgrade] ", "MineRollMutation", "TextButton", "MineTakeChest", "\n", ": ", " node(s) looked buyable but the server refused", "Humanoid", ")", "[AutoResearch] Core ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "MineBuyPickaxe", "/", " (", "[AutoChest] chest is empty", "antiAfk", " stack(s)", " holder(s) - not buying more", "ScreenGui", "nothing to place or upgrade", "Players", "Players", "Frame", "TextButton", "ValueLabel", " ", " (", "GetCharacterMineSpeedUpgradeCost", "games/my_anime_mine/auto_mine", "Header", "[AutoClaim] ", "leaderstats", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "TextLabel", "games/my_anime_mine/game_api", "Auto Place: เปลี่ยนตัวอ่อนออก", "TextButton", "autoPlotLatest", "Auto Research: ซื้อสูงสุดต่อรอบ", "AutoPlace", "MinePower", "-", "MineBuySkill", "s - carrying on without it", "unreadable", "⚙", "Auto Sell", " zones=", "games/my_anime_mine/get_current_plot", "ItemName", "MineEquipPickaxe", "[AutoPlace] ", "fill", "Mutation: เพดานราคาต่อครั้ง", "core/run_job", "games/my_anime_mine/auto_sell", "table", "games/my_anime_mine/auto_claim", "", "games/registry", "AutoClaim", "slot ", "table", "sold ", "ไล่ซื้อทุกกี่วินาที", "MineGui", "Character", "core/teleport", "[AutoUpgrade] ", " -> ", "TextLabel", "Text", "core/anti_afk", "] job failed: ", "M", "[AutoChest] collected ", "disabled", "en", "TextLabel", "core/build_env", "core/config", "function", "TextLabel", "AutoResearch", ",", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", " (", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "MineEquipSlot", "Badge", "games/registry", "[AutoSell] enabled", "core/anti_afk", "action '", "games/my_anime_mine/game_api", "HttpService", "TextLabel", "[AutoPlace] drifted out of range of ", ") ", "core/run_job", " (", "ฟาร์ม", "AutoPlot", "core/logger", "x", "Auto Research: รอบเช็ค", "TextLabel", "core/humanoid", "s", " (", "Auto Upgrade Unit", "OwnerUserId", ")", "[AutoChest] can't read the chest yet", "s", " node(s), ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "", "games/my_anime_mine/game_api", " items", "core/get_me", "core/logger", "games/my_anime_mine/get_plot_parts", "enabled", "2026-09-21 05:12:18", "Frame", "", "TextLabel", "games/my_anime_mine/auto_chest", " error: ", "%$", "GetHolderSpeedLevel", "[Bb]uy", "ขายทุกกี่วินาที", "Workspace", "อ่านป้ายบน chest ทุกกี่วินาที", "[AutoChest] enabled (collects once >= ", "UICorner", "/", " ", "[Scheduler] ", "Auto Claim: เก็บสูงสุดต่อรอบ", "en", " ", " (cost ", "th", "core/teleport", "Frame", " cost=", "games/my_anime_mine/get_best_miner", "UIListLayout", "x", "disabled", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/run_job", "th", ")", ")", "waiting", "Auto Upgrade Plot (Zone)", "Players", "", " (", "[AutoPlace] upgrading ", " (power ", "autoResearchInterval", " doesn't exist", "games/my_anime_mine/main", " ชิ้น", "research", "games/my_anime_mine/get_plot_parts", ": ", "empty", "MineSelectZone", "Damage", " -> ", "'", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "Frame", "string", " team power, you have ", ", built ", " owned)", "cost", ") didn't take, trying the next one", "CoreUI", "[AutoResearch] nothing open and affordable right now", ")", "AutoChest", "Players", "[AutoPlace] holders: ", "write reported success but ", "table", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "games/my_anime_mine/auto_claim", "upgrade", "#", "TextLabel", "autoUpgradePickaxeIndex", ": ", ")", "games/my_anime_mine/auto_plot", " ", "Auto Sell: รอคิวได้นานสุด", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "", ")", "TextButton", "core/logger", "autoUpgradePickaxeIndex", "core/scheduler", " reward(s) looked claimable but none went through", " ", "core/build_env", "UIListLayout", "CoreGui", "Modules", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "autoUpgradeMutationBudget", "[%X] ", "works from anywhere - dropping the teleport", "ไทย", "BodyPosition", "empty", "disabled", "Speed", "=", "[AutoPlot] unlocked ", "(nothing to do)", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "th", "games/my_anime_mine/auto_plot", "s", "UIStroke", "UIScale", "waiting", "autoPlaceInterval", "(", "Auto Claim (Index)", "", "/config.json", "Plots", "Auto Upgrade: แจก Pickaxe ให้ miner", "autoResearchZones", ", base ", "s", "unreadable", "disabled", "games/my_anime_mine/auto_upgrade", "')", "Root", "\n", "enabled", "MekPro", "games/my_anime_mine/auto_research", "Auto Upgrade: สุ่ม Mutation", "core/logger", "แพงสุดก่อน", "en-us", "Auto Plot: รอบเช็ค", "autoResearchCoreLast", "[AutoPlace] couldn't pick up ", "[Config] couldn't read ", "[Scheduler] ", "", " budget=", "games/my_anime_mine/game_api", "My Anime Mine · Mining Hub", "core/scheduler", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "TextBox", "อังกฤษ", "UIListLayout", "All", "lowest", ") needs ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "autoUpgradeRollMutation", "table", "autoPlot", "UICorner", "Mutation: หยุดเมื่อได้ x", "[AutoResearch]   Core: ", "Players", "UserInputService", "costs ", "core/i18n", "[", "core/run_job", ",", "Auto Research (Plot)", "write reported success but the line isn't in ", "เช็คโซนทุกกี่วินาที", "[AutoPlot] ", "autoSellProtectUnits", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "autoClaimInterval", "en", ": ", "s", "core/dispatch", "[AutoPlace] disabled", "cost", " (chest holds ", "", "open_menu_hint", "autoMineSpeed", "Auto Research: ซื้อสาย Core", " ("}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(544)] = function()
return function(import)
local log = import(S(575))
local Players = game:GetService(S(313))
local VirtualUser = game:GetService(S(183))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(282))
        connection = player.Idled:Connect(function()
            log(S(212))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(470))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(685)] = function()
return { Env = S(341), IsDev = false, Commit = S(221), BuiltAt = S(596) }
end
modules[S(211)] = function()
local VirtualInputManager = game:GetService(S(199))
local GuiService = game:GetService(S(189))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(294)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(449)] = function()
return function(import)
local HttpService = game:GetService(S(567))
local getPlaceId = import(S(58))
local log = import(S(6))
local ROOT_FOLDER = S(726)
local placeFolder = ROOT_FOLDER .. S(484) .. tostring(getPlaceId())
local filePath = placeFolder .. S(713)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(126) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(735) .. filePath .. S(305) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(217) .. filePath .. S(643) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(773)] = function()
return function(import)
    local getPlaceId = import(S(95))
    local registry = import(S(531))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(68))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(152)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(503))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(592)] = function()
local Players = game:GetService(S(632))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(479))
    local backpack = player:WaitForChild(S(394))
    return player, humanoid, backpack
end
return getMe
end
modules[S(109)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(579)] = function()
local Players = game:GetService(S(85))
local RunService = game:GetService(S(380))
local UserInputService = game:GetService(S(757))
local Workspace = game:GetService(S(275))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(238))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(104))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(465))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(171)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(5)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(694))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(334)] = function()
local Players = game:GetService(S(756))
local STRINGS = {
    en = {
        search_placeholder = S(107),
        open_menu_hint = S(60),
    },
    th = {
        search_placeholder = S(220),
        open_menu_hint = S(300),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(731)
    if locale:sub(1, 2):lower() == S(627) then
        return S(617)
    end
    return S(770)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(191) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(21)] = function()
local LOG_FILE = S(374)
local function log(message)
    local line = os.date(S(691)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(403))
            end
            appendfile(LOG_FILE, line .. S(724))
        elseif writefile then
            local existing = S(367)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(476))
        else
            error(S(159))
        end
        if not (isfile and readfile) then
            error(S(410))
        end
        if not isfile(LOG_FILE) then
            error(S(663) .. LOG_FILE .. S(638))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(764) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(371) .. LOG_FILE .. S(671) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(177)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(245) then
        return nil
    end
    local number, suffix = text:match(S(330))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(147), S(589))))
    if not value then
        return nil
    end
    if suffix ~= S(712) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(55)] = function()
return function(import)
local log = import(S(179))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(760) .. name .. S(545) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(741)] = function()
return function(import)
local RunService = game:GetService(S(200))
local log = import(S(323))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(736) .. task_.name .. S(601) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(612) .. task_.name .. S(292)
                    .. string.format(S(433), now - task_.busySince)
                    .. S(515))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(439) .. starved.name .. S(139)
            .. string.format(S(117), now - starved.waitingSince)
            .. S(80))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(198) .. (currentTask and currentTask.name or S(700)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(81)] = function()
local Players = game:GetService(S(493))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(164))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(450)] = function()
return function(import)
local i18n = import(S(759))
local UserInputService = game:GetService(S(438))
local THEME = {
    BrandName = S(336),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(753))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(609))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(174))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(201))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(677)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(706))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(386))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(20)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(424))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(350)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(395))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(745))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(88))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(427))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(307))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(505))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(554))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(496))
            if not valueLbl then
                valueLbl = Instance.new(S(232))
                valueLbl.Name = S(418)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(561))
            if not badge then
                badge = Instance.new(S(542))
                badge.Name = S(344)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(173))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(650))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(213))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(679))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(777)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(387)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(123))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(202))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(474))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(598)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(23))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(686))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(495))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(543)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(490))
    screenGui.Name = S(657)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(687))
    local main = Instance.new(S(597))
    main.Name = S(181)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(707))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(295))
    tabBar.Name = S(65)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(622))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(462))
    header.Name = S(501)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(508))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(149))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(669))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(311)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(428))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(193)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(92))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(513)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(743))
    searchBox.Name = S(203)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(208))
    searchBox.Text = S(451)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(619))
    content.Name = S(303)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(494))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(550))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(778))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(578))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(319)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(131) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(497) .. (item.Subtitle or S(361))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(267))
        footerLabel.Text = i18n.T(S(326))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(600)] = function()
return function(import)
local gameApi = import(S(66))(import)
local log = import(S(369))
local runGuarded = import(S(571))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(608) .. minItemsToCollect .. S(40))
    else
        log(S(105))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(516) then
            log(S(585))
            lastReported = S(719)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(695) then
            log(S(486))
            lastReported = S(644)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(630) then
            local capacity = gameApi.ChestCapacity()
            log(S(223) .. count .. S(610) .. want .. S(591)
                .. (capacity and (S(776) .. capacity .. S(584)) or S(459)))
            lastReported = S(708)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(366), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(547) .. count .. S(272) .. after .. S(672))
        else
            log(S(382) .. count
                .. S(45) .. tostring(after) .. S(296))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(529)] = function()
return function(import)
local gameApi = import(S(590))(import)
local log = import(S(447))
local runGuarded = import(S(102))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(502) .. (enabled and S(260) or S(30)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(61) .. reward.category .. S(197)
                .. tostring(reward.tierKey) .. S(352) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(254) .. #pending .. S(683))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(532), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(306)] = function()
return function(import)
local getCurrentPlot = import(S(376))
local getPlotParts = import(S(288))
local log = import(S(680))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(46), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(176) .. (enabled and S(725) or S(720)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(196)] = function()
return function(import)
local Players = game:GetService(S(492))
local getCurrentPlot = import(S(520))
local getPlotParts = import(S(642))
local getMinerSlots = import(S(621))(import)
local teleport = import(S(618))
local gameApi = import(S(506))(import)
local log = import(S(593))
local runGuarded = import(S(286))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(162)
local LOCKED_SLOT_TEXT = S(388)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(136))
    return pad and pad:FindFirstChildWhichIsA(S(378), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(265)) then
            local digits = descendant.Text:match(S(317))
            if digits then
                return tonumber((digits:gsub(S(154), S(633))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(599)) and descendant.Text:match(S(445)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(533) .. holder.Name .. S(315)
            .. gameApi.MinerSlots() .. S(227) .. gameApi.MaxMinerSlots() .. S(654)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(370)
    end
    if not prompt.Enabled then
        return true, S(289)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(758) .. price
    end
    local action = prompt.ActionText or S(737)
    if action:match(S(195)) or action:match(S(604)) or action:match(S(602)) then
        return true, S(565) .. action .. S(648)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(471))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(90) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(244) .. why .. S(355))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(257))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(698) .. prompt.ObjectText
                    .. S(710) .. shown .. S(717) .. math.floor(base) .. S(283))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(190))
    if line ~= lastSurvey then
        log(S(662) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(401), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(667), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(523) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(774))
    else
        lastAttempt = 0
        log(S(325))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(665),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(93), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(365))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(569) .. holder.Name .. S(425))
            return false
        end
        log(S(108) .. candidate.slot .. S(634) .. candidate.name
            .. S(656))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(304) .. holder.Name .. S(160) .. miner.name)
        return false
    end
    log(S(158) .. miner.name .. S(309) .. miner.power
        .. S(169) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(734) .. entry.name .. S(226) .. entry.holder.Name
            .. S(17) .. tostring(entry.prompt.ActionText) .. S(722))
        return false
    end
    log(S(456) .. entry.name .. S(636) .. entry.power
        .. S(364) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(491))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(524) then
        runGuarded(S(156), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(635) .. target.holder.Name .. S(771) .. target.name
        .. S(423) .. target.power .. S(175) .. best.name .. S(781) .. best.power .. S(94))
    runGuarded(S(218), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(704)] = function()
return function(import)
local gameApi = import(S(182))(import)
local parseMoney = import(S(389))
local log = import(S(392))
local runGuarded = import(S(526))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(225) .. (enabled and S(595) or S(624)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(406)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(248) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(321) .. deepestKey .. S(659))
        return true
    end
    log(S(399) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(339) .. gameApi.ZoneDisplayName(zoneKey) .. S(56)
                .. math.floor(price) .. S(206) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(699) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(572) .. zoneKey .. S(54))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(641) then
            reportedLocked = S(464)
            log(S(766) .. zoneKey .. S(63)
                .. gameApi.ZoneDisplayName(currentKey) .. S(396)
                .. done .. S(299) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(574), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(727)] = function()
return function(import)
local gameApi = import(S(739))(import)
local log = import(S(287))
local runGuarded = import(S(100))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(14) .. (enabled and S(469) or S(548)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(146) .. node.zoneKey .. S(684) .. node.id
                    .. S(558) .. node.family .. S(570) .. node.level .. S(541) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(481) .. node.id
                    .. S(167) .. node.family .. S(368) .. node.level .. S(647) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(116) .. blocked
                .. S(478))
        elseif powerBlocked then
            log(S(124) .. powerBlocked.zoneKey .. S(119) .. powerBlocked.id
                .. S(581) .. powerBlocked.family .. S(748)
                .. math.floor(powerBlocked.powerReq) .. S(652)
                .. math.floor(powerBlocked.power)
                .. S(358))
        else
            log(S(658)
                .. S(377) .. math.floor(gameApi.GetMoney()) .. S(390))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(243) .. nearest.zoneKey
                    .. S(168) .. nearest.id .. S(485) .. nearest.family .. S(91)
                    .. nearest.level .. S(411) .. nearest.max
                    .. S(113) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(155) .. math.floor(money)
        .. S(738) .. math.floor(budget)
        .. S(519) .. (#zones > 0 and table.concat(zones, S(556)) or S(362))
        .. S(216) .. tostring(buyZones) .. S(435) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(103) .. zoneKey .. S(611) .. tostring(node.id)
                        .. S(118) .. tostring(node.family) .. S(417)
                        .. node.level .. S(122) .. node.max
                        .. S(250) .. tostring(node.open)
                        .. S(620) .. tostring(node.cost)
                        .. S(302) .. tostring(node.powerOk)
                        .. S(18) .. tostring(node.powerReq)
                        .. S(130) .. tostring(node.power))
                end
            end
        end
        log(S(298) .. zoneKey .. S(251) .. #nodes
            .. S(12) .. pending .. S(47))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(755) .. #coreNodes .. S(587)
        .. coreOpen .. S(337))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(110), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(527)] = function()
return function(import)
local Workspace = game:GetService(S(606))
local gameApi = import(S(180))(import)
local teleport = import(S(539))
local log = import(S(222))
local runGuarded = import(S(761))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(324))
    else
        lastAttempt = 0
        log(S(563))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(353))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(437), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(270)
                .. (sold and S(692)
                         or S(106)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(293) .. (sold and (S(535) .. #keys .. S(488)) or S(343)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(79), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(721)] = function()
return function(import)
local Workspace = game:GetService(S(460))
local gameApi = import(S(25))(import)
local teleport = import(S(77))
local log = import(S(729))
local runGuarded = import(S(626))(import)
local STATS = { S(13), S(697), S(15) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(233)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(24) .. (enabled and S(67) or S(696)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(279) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(467) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(775) or S(86)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(540) .. holderKey .. S(615) .. choice.stat .. S(674)
                        .. choice.info.level .. S(356) .. after.level
                        .. S(148) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(655) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(219))
    local shop = stand and stand:FindFirstChild(S(69))
    if not shop then
        return nil
    end
    if shop:IsA(S(276)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(48), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(316)
                .. (bought and S(420)
                           or S(142)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(114))
        end
    end
    if bought then
        log(S(446) .. pickaxe.index .. S(72) .. pickaxe.name
            .. S(312) .. pickaxe.multiplier .. S(678))
        return true
    end
    log(S(78) .. pickaxe.index .. S(150) .. pickaxe.name
        .. S(322))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(97) .. holderKey .. S(39)
                .. tostring(entry and entry.Pickaxe or S(31)) .. S(332) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(41) .. holderKey .. S(184) .. variant
                .. S(186) .. after .. S(616) .. cost .. S(628))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(472) .. gameApi.OwnedPickaxeCount()
                    .. S(151) .. gameApi.MinerSlots()
                    .. S(489))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(345))
        elseif maxed > 0 then
            log(S(125))
        else
            log(S(588))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(528) and entry.Pickaxe and entry.Pickaxe ~= S(278) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(431), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(566)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(457))
local Workspace = game:GetService(S(231))
local log = import(S(252))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(688), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(349) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(363))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(234) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(277) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(534) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(70) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(111) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(415))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(237) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(475))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(746), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(664) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(329) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(538) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(255), keys)
end
function api.EquipSlot(key)
    return send(S(560), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(483), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(522), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(473), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(514), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(442)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(3)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(512))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(274) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(98) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(645), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(132))
    plots = plots and plots:FindFirstChild(S(714))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(140)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(583)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(7))
    local gui = board and board:FindFirstChild(S(129))
    local root = gui and gui:FindFirstChild(S(723))
    local list = root and root:FindFirstChild(S(335))
    local row = list and list:FindFirstChild(S(264) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(43)) and descendant.Text ~= S(192) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(553) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(271), characterKey, stat == S(310) and S(351) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(402), max = S(236), cost = S(27) },
    Speed = { level = S(603), max = S(273), cost = S(10) },
    MineSpeed = { level = S(269), max = S(71), cost = S(499) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(651) or charName == S(157) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(34) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(29) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(463) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(210) and type(holder.CharName) == S(49) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(751) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(145) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(285)] = function()
return function(import)
local Players = game:GetService(S(214))
local gameApi = import(S(397))(import)
local log = import(S(434))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(165))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(762), S(75)))) or 0
    if suffix ~= S(381) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(241))
    local mineGui = playerGui and playerGui:FindFirstChild(S(537))
    local hotbar = mineGui and mineGui:FindFirstChild(S(301))
    local content = hotbar and hotbar:FindFirstChild(S(308))
    return content and content:FindFirstChild(S(331))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(568)) then
            local count = descendant.Text:match(S(133))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(290))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(461))
    local nameLabel = content and content:FindFirstChild(S(521))
    local damageLabel = content and content:FindFirstChild(S(375))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(87) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(194) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(453) .. slot .. S(477) .. info.name
                        .. S(404) .. info.power .. S(373) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(166)] = function()
local Players = game:GetService(S(661))
local Workspace = game:GetService(S(229))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(452))
    local plots = mineWorld and mineWorld:FindFirstChild(S(247))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(430)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(594)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(444)),
        Nodes = plot:FindFirstChild(S(82)),
        Chest = plot:FindFirstChild(S(2)),
        Zones = plot:FindFirstChild(S(112)),
        Crew = plot:FindFirstChild(S(185)),
        Loot = plot:FindFirstChild(S(115)),
        PlotIndex = plot:GetAttribute(S(448)),
        ChestCapacity = plot:GetAttribute(S(379)),
    }
end
return getPlotParts
end
modules[S(73)] = function()
return function(import)
    local createWindow = import(S(256))(import)
    local i18n = import(S(340))
    local antiAfk = import(S(564))(import)
    local buildEnv = import(S(551))
    local scheduler = import(S(682))(import)
    local config = import(S(552))(import)
    local log = import(S(405))
    log(S(207) .. buildEnv.Commit .. S(498) .. buildEnv.Env
        .. S(653) .. buildEnv.BuiltAt .. S(480))
    local window = createWindow({
        Subtitle = S(740),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(500))(import)
    local autoPlace = import(S(338))(import)
    local autoChest = import(S(134))(import)
    local autoSell = import(S(262))(import)
    local autoUpgrade = import(S(414))(import)
    local autoResearch = import(S(346))(import)
    local autoClaim = import(S(666))(import)
    local autoPlot = import(S(673))(import)
    local gameApi = import(S(385))(import)
    scheduler.RegisterStandalone(S(263), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(660), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(511), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(172), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(555), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(320), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(354), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(26), 20)
    scheduler.RegisterExclusive(S(314), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(573), S(422))
    persistentToggle(farmTab:AddCard(
        S(215),
        S(749)
    ), S(138), autoMine.SetEnabled)
    persistentSlider(farmTab, S(421), S(327),
        1, 30, S(779), 5, autoMine.SetCyclesPerSecond, S(64))
    persistentSlider(farmTab, S(209), S(83),
        1, 10, S(328), 1, autoMine.SetClicksPerCycle, S(623))
    persistentToggle(farmTab:AddCard(
        S(468),
        S(359)
    ), S(347), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(163), S(407),
        1, 60, S(709), 3, autoPlace.SetCheckInterval, S(441))
    persistentToggle(
        farmTab:AddCard(S(507),
            S(204)),
        S(205), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(419),
        S(701)
    ), S(280), autoChest.SetEnabled)
    persistentSlider(farmTab, S(454), S(28),
        50, 3000, S(240), 100, autoChest.SetMinItems, S(640))
    persistentSlider(farmTab, S(161), S(607),
        1, 60, S(44), 3, autoChest.SetCheckInterval, S(705))
    persistentToggle(farmTab:AddCard(
        S(518),
        S(357)
    ), S(436), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(291),
            S(32)),
        S(767), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(412), S(605),
        5, 300, S(416), 20, autoSell.SetCheckInterval, S(718))
    farmTab:AddSlider(S(675),
        S(676),
        5, 120, sellMaxWait, function(value)
            config.Set(S(51), value)
        end, S(586))
    persistentToggle(farmTab:AddCard(
        S(582),
        S(153)
    ), S(266), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(342), S(76),
        5, 300, S(426), 20, autoUpgrade.SetInterval, S(580))
    persistentSlider(farmTab, S(559), S(50),
        1, 20, S(143), 5, autoUpgrade.SetMaxPerPass, S(408))
    persistentSegmented(
        farmTab,
        { S(409), S(646), S(22), S(37) },
        { S(747), S(187), S(120), S(188) },
        S(333), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(57), S(557),
        0, 500, S(429), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(546))
    persistentToggle(
        farmTab:AddCard(S(742),
            S(504)),
        S(137), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(249) .. pickaxe.multiplier .. S(629)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(482)
            end
            return S(668) .. index .. S(99) .. (pickaxeLabels[index] or S(768))
        end
        local savedPick = config.Get(S(670), 0)
        pickaxeCard = farmTab:AddSlider(
            S(466),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(681), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(530)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(259),
            S(74)),
        S(224), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(715),
            S(16)),
        S(398), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(728),
            S(702)),
        S(750), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(754), S(393),
        1, 20, S(242), 2, autoUpgrade.SetMutationTarget, S(576))
    persistentSlider(farmTab, S(525), S(1),
        0, 500, S(690), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(261))
    persistentToggle(farmTab:AddCard(
        S(763),
        S(455)
    ), S(360), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(577), S(536),
        10, 600, S(637), 30, autoResearch.SetCheckInterval, S(52))
    persistentSlider(farmTab, S(510), S(62),
        1, 20, S(372), 5, autoResearch.SetMaxPerPass, S(384))
    persistentSlider(farmTab, S(53), S(432),
        0, 500, S(59), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(96))
    persistentToggle(
        farmTab:AddCard(S(297),
            S(625)),
        S(716), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(780),
            S(458)),
        S(141), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(235),
            S(4)),
        S(733), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(239), S(730) },
        { true, false },
        S(19), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(711),
        S(38)
    ), S(36), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(268), S(230),
        5, 300, S(769), 30, autoClaim.SetCheckInterval, S(772))
    persistentSlider(farmTab, S(613), S(689),
        1, 30, S(9), 10, autoClaim.SetMaxPerPass, S(246))
    persistentToggle(farmTab:AddCard(
        S(631),
        S(11)
    ), S(752), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(732), S(765),
        5, 300, S(144), 30, autoPlot.SetCheckInterval, S(128))
    persistentToggle(
        farmTab:AddCard(S(284),
            S(42)),
        S(391), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(318),
            S(649)),
        S(509), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(121), S(413),
        0, 500, S(8), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(400))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(135))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(127), S(517))
    persistentToggle(
        settingsTab:AddCard(S(383), S(443)),
        S(487),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(348), 100)
    settingsTab:AddSlider(S(84), S(89), 50, 150, savedScale, function(value)
        config.Set(S(253), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(440), 1)
    settingsTab:AddSegmented({ S(101), S(744), S(693) }, savedLanguage, function(index, value)
        local code = ({ S(258), S(614), S(703) })[index]
        config.Set(S(228), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(281), S(549), S(170) })[savedLanguage] or S(35))
    log(S(178) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(562)] = function()
return {
    [79389059854988] = S(639),
}
end
import(S(33))(import)
