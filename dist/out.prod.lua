-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "[AutoUpgrade] ", "core/get_place_id", "SellStand", "games/my_anime_mine/auto_research", "MineGui", "GetHolderMineSpeedLevel", "core/logger", "Open menu", "/", "[AutoChest] disabled", "ValueLabel", "autoUpgradePickaxe", "ตามเครื่อง", "Content", "All", "games/my_anime_mine/game_api", "TextButton", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "Frame", ": ", "autoPlaceUpgradeWeak", "Main", "[AutoPlace] nothing in the hotbar to place", "waiting", "disabled", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Players", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/get_best_miner", "sold ", "[%X] ", "Auto Research: กันเงินไว้", "AutoSell", "", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "nothing to place or upgrade", "th", " (cost ", "autoMineSpeed", "action '", " ", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "cost", "fill", "Auto Place: รอบเช็ค", "games/my_anime_mine/auto_place", "costs ", " items", " items)", "games/my_anime_mine/get_current_plot", "[AutoUpgrade] ", "MekPro", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "", "UICorner", "th", "search_placeholder", "autoChest", "RunService", "CoreUI", "autoUpgradeEquipPickaxe", "TextButton", " stack(s)", ": ", " -> ", "autoResearchKeepMoney", "[AutoPlace] upgrading ", "Frame", "[AutoUpgrade] pickaxe #", " doesn't exist", "s", "unreadable", "autoResearch", "MekPro", " while selecting", "auto", "[AutoSell] ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Players", "table", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "", "AutoMine", "s", "auto", ") didn't take, trying the next one", "Frame", "TextButton", "Hauler", ")", "antiAfk", "HumanoidRootPart", "autoUpgradeMaxPerPass", " (prompt says '", "enabled", "ScreenGui", "sale didn't go through", "[AutoResearch] ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "unreadable", "core/teleport", "Frame", ")", "TextButton", "BasePart", "games/my_anime_mine/game_api", "ClickDetector", "PlotIndex", "Humanoid", "')", "PlayerGui", " (", "[^%d%.]", "autoUpgradePickaxeIndex", "ProximityPrompt", "TextButton", "Auto Research: ซื้อสูงสุดต่อรอบ", "[AutoResearch] ", "[AutoUpgrade] bought pickaxe #", "s", "core/build_env", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "This is not a supported place.", "works from anywhere - dropping the teleport", "TextLabel", "Auto Chest: รอบเช็ค", "[AntiAFK] idle detected, simulating input", "(", "GetCharacterUpgradeCost", "auto", "Auto Research: ซื้อสาย Zone", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "table", "UIListLayout", "TextLabel", "games/my_anime_mine/game_api", "table", "[AutoUpgrade] ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "=locked(", "string", "^([%d,]+)$", "Holders", "[AutoResearch] nothing open and affordable right now", "TextLabel", "VirtualUser", "", "ไล่อัปเกรด miner ทุกกี่วินาที", " ", "Auto Research: รอบเช็ค", "Speed", "TextLabel", "games/my_anime_mine/auto_research", "write reported success but ", "Players", "Crew", "menuScale", "ScrollingFrame", "[AutoChest] can't read the chest yet", "core/i18n", "Frame", "%$%s*([%d,%.]+)%s*(%a*)", "ถูกสุดก่อน", "MekPro.log", " ", "UICorner", "Frame", "core/scheduler", "Auto Place Miner", "Auto Place: เปลี่ยนตัวอ่อนออก", "core/ui", "TextLabel", ") from ", "core/logger", " -> ", "[Uu]nlock", "core/dispatch", "core/teleport", "TextLabel", "Auto Research: ซื้อสาย Core", "autoMine", "x", "core/humanoid", "", "x", "TextLabel", "Frame", "Humanoid", "search_placeholder", "languageIndex", "[AutoChest] MineTakeChest had no effect (", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "needs us at the stand - keeping the teleport", "Header", "core/anti_afk", "My Anime Mine · Mining Hub", " error: ", "games/my_anime_mine/get_plot_parts", "TextButton", "MineEquipSlot", "Mutation: เพดานราคาต่อครั้ง", "core/config", "OwnerUserId", "leaderstats", "[Config] couldn't write ", "BasePart", "", " still empty after placing ", "[Bb]uy", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "en-us", "games/my_anime_mine/auto_chest", "Auto Upgrade: สุ่ม Mutation", "core/debug_tab", "CoreGui", " (", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "MineRollMutation", "'", "ตั้งค่า", "BodyPosition", "RunService", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/ui", "ไล่ซื้อทุกกี่วินาที", "Search functions...", "Pad", "[AutoChest] waiting: ", "lowest", "ไทย", "Frame", "Auto Mine: คลิกต่อรอบ", "#", "TextBox", "GetHolderSpeedLevel", "", "x", "HumanoidRootPart", "Speed", "Locked miner slot", "Zones", "TextLabel", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Frame", "Badge", "ค้นหาฟังก์ชัน...", "core/logger", "core/i18n", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "x", " x", "Auto Sell: รอบขาย", ",", "X", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "ProximityPrompt", "MineSpeed", "%", ")", "ItemName", "Mutation: หยุดเมื่อได้ x", ": ", ")", "[AutoResearch] ", "ต่ำสุดก่อน", "table", "TextLabel", "[AutoPlace] holders: ", "UIListLayout", "", "core/logger", "core/logger", ") on ", "maxed", "lowest", "core/logger", "MineTakeChest", " pickaxe ", "GetCharacterSpeedUpgradeCost", "core/dispatch", "cost", "[AutoPlace] ", "", "autoPlace", "-", "[GameAPI] couldn't load the game's modules: ", "games/my_anime_mine/auto_mine", "MineUpgradeCharacter", "(nothing to do)", "core/anti_afk", "Plots", "อังกฤษ", "/config.json", "Speed", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "CharSpeedMaxLevel", "table", "[AutoResearch] Core ", "en", "[AutoChest] collected ", "none", " -> ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "[AntiAFK] enabled - watching Player.Idled", "[AutoPlace] fireproximityprompt error: ", "autoChestMinItems", "core/scheduler", "UserInputService", "M", "autoResearchMaxPerPass", "core/get_leaderstat", "core/parse_money", "Chest", "TextLabel", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "autoMineClicks", "", "HotbarFrame", "UIPadding", "core/logger", "=", "[Config] couldn't read ", "", "core/get_me", "neither appendfile nor writefile is available", "Damage", "games/my_anime_mine/auto_place", "enabled", "en", "Mining", "games/my_anime_mine/auto_sell", "", "[AutoPlace] picked up ", "games/my_anime_mine/main", "Anti-AFK", "autoUpgradeRollMutation", "MineBuySkill", "[Logger] failed to write ", "Workspace", "[AutoPlace] ", "Frame", "no isfile/readfile available to verify the write", "[AutoPlace] slot ", "autoResearchCore", "ขนาดเมนู", "autoPlaceInterval", "", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "core/logger", "AutoChest", "[AutoPlace] drifted out of range of ", "Backpack", "ย่อขยายเมนูทั้งชุด", "TabBar", "Search", "table", "Auto Sell", "autoChestInterval", "games/my_anime_mine/main", "th", "s", "^x(%d+)$", "[AutoPlace] disabled", "[AutoPlace] couldn't pick up ", "AutoResearch", "autoUpgradeKeepMoney", "[BestMiner] slot ", "waiting", "ItemFrame", "ValueLabel", "[Scheduler] turn: ", "core/build_env", "Loot", "Modules", "CharMaxLevel", "th", "TextLabel", "[AutoSell] MineSellItems ", ")", "autoUpgrade", " (", "Frame", "Auto Mine: ความเร็ว", "games/my_anime_mine/auto_upgrade", "Auto Research (Plot)", "CharMineSpeedMaxLevel", "[AutoSell] enabled", "languageIndex", "autoResearchInterval", " | ", "M", "core/logger", "BodyVelocity", "core/get_place_id", "core/get_place_id", "Workspace", "UserInputService", " ", "autoUpgradeMutationBudget", "core/config", "Auto Chest", "Damage", "ChestCapacity", "🏠", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "autoUpgradeMutationTarget", "M", "games/my_anime_mine/game_api", "lowest", "แพงสุดก่อน", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " ", "write reported success but the line isn't in ", "disabled", "0 = ไม่จำกัด", "s", "Badge", "prod", "Damage", "games/my_anime_mine/game_api", "locked slot", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Damage", "autoUpgradePriority", "Auto Upgrade: แจก Pickaxe ให้ miner", "MineSpeed", "auto", "GuiService", "Auto Upgrade: รอบเช็ค", "[AutoUpgrade] every stat on every miner is maxed", "[AntiAFK] disabled", "Empty miner slot", "autoSell", "[AutoPlace] placed ", "Players", " -> x", " (power ", "fill", "Container", "[AutoSell] disabled", " items (now ", "Auto Upgrade: Pickaxe ที่จะซื้อ", "upgrade", "Content", "Text", "function", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "core/logger", "button has no size on screen", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "ปุ่มเปิดเมนู", "[AutoUpgrade] ", ": ", " (x", "HumanoidRootPart", "MineWorld", "TextLabel", "ReplicatedStorage", "/", ")", "\n", "%$", " power ", "VirtualInputManager", " didn't go through (out of stock or too expensive)", "core/logger", "TextLabel", "prompt disabled", "Text", "\n", "Players", "", "core/logger", "", ": ", ") ", "", "/", "[GameAPI] SendEvent failed: ", "MineSpeed", "games/my_anime_mine/get_best_miner", " node(s) looked buyable but the server refused", "table", "Auto Mine", "MineSellItems", "⚙", "games/my_anime_mine/get_plot_parts", "TextButton", "UIListLayout", "games/my_anime_mine/game_api", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "AutoPlace", "games/my_anime_mine/auto_sell", "Place", "UIScale", " mutation x", "open_menu_hint", "core/teleport", "autoUpgradePickaxeIndex", "menuScale", ") ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "autoSellInterval", "[AutoChest] enabled (collects once >= ", "GetHolderLevel", "AutoUpgrade", "core/click", "disabled", "Nodes", " -> ", " (", "Content", "MineBuyPickaxe", "enabled", "", ",", "games/my_anime_mine/auto_upgrade", "อ่านป้ายบน chest ทุกกี่วินาที", "autoResearchZones", "table", "TextButton", "^%$", "games/my_anime_mine/get_current_plot", "core/logger", " (x", "/s", "Players", "[AutoMine] ", "autoResearchCheapestFirst", " -> ", "Players", "Players", "UIStroke", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "core/i18n", "open_menu_hint", "[Scheduler] ", "games/registry", " (", "games/my_anime_mine/auto_mine", "HttpService", " ", " ", "games/registry", "games/my_anime_mine/auto_chest", " ชิ้น", " from ", "Auto Upgrade Unit", "Auto Chest: เกณฑ์เก็บ", "Auto Upgrade: กันเงินไว้", "games/my_anime_mine/get_current_plot", "?", "autoUpgradeInterval", "ฟาร์ม", "", "[MekPro] settings file: ", ")", "TextLabel", "GetCharacterMineSpeedUpgradeCost", ")", "Workspace", "Mining", "games/my_anime_mine/game_api", "TextLabel", "/", "en", "=empty", " (power ", "MineEquipPickaxe", ") -> "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(196)] = function()
return function(import)
local log = import(S(350))
local Players = game:GetService(S(532))
local VirtualUser = game:GetService(S(147))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(305))
        connection = player.Idled:Connect(function()
            log(S(128))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(432))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(373)] = function()
return { Env = S(419), IsDev = false }
end
modules[S(508)] = function()
local VirtualInputManager = game:GetService(S(465))
local GuiService = game:GetService(S(429))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(450)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(203)] = function()
return function(import)
local HttpService = game:GetService(S(542))
local getPlaceId = import(S(3))
local log = import(S(467))
local ROOT_FOLDER = S(75)
local placeFolder = ROOT_FOLDER .. S(479) .. tostring(getPlaceId())
local filePath = placeFolder .. S(294)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(267) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(323) .. filePath .. S(454) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(206) .. filePath .. S(263) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(281)] = function()
return function(import)
    local getPlaceId = import(S(395))
    local registry = import(S(539))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(124))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(312)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(205))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(325)] = function()
local Players = game:GetService(S(533))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(189))
    local backpack = player:WaitForChild(S(353))
    return player, humanoid, backpack
end
return getMe
end
modules[S(396)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(184)] = function()
local Players = game:GetService(S(28))
local RunService = game:GetService(S(223))
local UserInputService = game:GetService(S(309))
local Workspace = game:GetService(S(340))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(110))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(93))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(394))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(106)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(207)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(222))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(536)] = function()
local Players = game:GetService(S(528))
local STRINGS = {
    en = {
        search_placeholder = S(227),
        open_menu_hint = S(9),
    },
    th = {
        search_placeholder = S(247),
        open_menu_hint = S(452),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(212)
    if locale:sub(1, 2):lower() == S(361) then
        return S(377)
    end
    return S(567)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(428) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(175)] = function()
local LOG_FILE = S(165)
local function log(message)
    local line = os.date(S(32)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(237))
            end
            appendfile(LOG_FILE, line .. S(471))
        elseif writefile then
            local existing = S(148)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(462))
        else
            error(S(326))
        end
        if not (isfile and readfile) then
            error(S(343))
        end
        if not isfile(LOG_FILE) then
            error(S(155) .. LOG_FILE .. S(71))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(414) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(339) .. LOG_FILE .. S(21) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(313)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(142) then
        return nil
    end
    local number, suffix = text:match(S(163))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(254), S(475))))
    if not value then
        return nil
    end
    if suffix ~= S(516) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(169)] = function()
return function(import)
local RunService = game:GetService(S(60))
local log = import(S(321))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(538) .. task_.name .. S(198) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(372) .. (currentTask and currentTask.name or S(290)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(499)] = function()
local Players = game:GetService(S(80))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(239))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(225)] = function()
return function(import)
local i18n = import(S(249))
local UserInputService = game:GetService(S(398))
local THEME = {
    BrandName = S(53),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(167))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(56))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(320))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(489))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(318)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(534))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(243))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(553)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(468))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(348)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(159))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(135))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(458))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(342))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(268))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(126))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(315))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(371))
            if not valueLbl then
                valueLbl = Instance.new(S(146))
                valueLbl.Name = S(12)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(246))
            if not badge then
                badge = Instance.new(S(136))
                badge.Name = S(418)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(117))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(188))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(383))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(18))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(473)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(259)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(69))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(168))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(522))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(208)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(88))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(490))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(105))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(470)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(97))
    screenGui.Name = S(61)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(216))
    local main = Instance.new(S(20))
    main.Name = S(23)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(496))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(245))
    tabBar.Name = S(355)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(270))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(103))
    header.Name = S(195)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(89))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(565))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(378))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(556)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(63))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(255)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(200))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(286)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(235))
    searchBox.Name = S(356)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(58))
    searchBox.Text = S(271)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(232))
    content.Name = S(15)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(162))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(173))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(498))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(187))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(446)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(333) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(150) .. (item.Subtitle or S(478))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(190))
        footerLabel.Text = i18n.T(S(537))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(213)] = function()
return function(import)
local gameApi = import(S(137))(import)
local log = import(S(272))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(505) .. minItemsToCollect .. S(50))
    else
        log(S(11))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(73) then
            log(S(160))
            lastReported = S(101)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(369) then
            log(S(229) .. count .. S(460) .. minItemsToCollect .. S(49))
            lastReported = S(25)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(301) .. count .. S(442) .. after .. S(91))
        else
            log(S(192) .. count
                .. S(531) .. tostring(after) .. S(461))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(288)] = function()
return function(import)
local getCurrentPlot = import(S(524))
local getPlotParts = import(S(29))
local log = import(S(248))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(108), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(529) .. (enabled and S(515) or S(509)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(328)] = function()
return function(import)
local Players = game:GetService(S(472))
local getCurrentPlot = import(S(51))
local getPlotParts = import(S(488))
local getMinerSlots = import(S(30))(import)
local teleport = import(S(179))
local gameApi = import(S(107))(import)
local log = import(S(393))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(433)
local LOCKED_SLOT_TEXT = S(241)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(228))
    return pad and pad:FindFirstChildWhichIsA(S(257), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(559)) then
            local digits = descendant.Text:match(S(143))
            if digits then
                return tonumber((digits:gsub(S(517), S(185))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(153)) and descendant.Text:match(S(523)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(422)
    end
    if not prompt.Enabled then
        return true, S(469)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(48) .. price
    end
    local action = prompt.ActionText or S(324)
    if action:match(S(177)) or action:match(S(210)) or action:match(S(463)) then
        return true, S(41) .. action .. S(220)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(456))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(306) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(141) .. why .. S(104))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(568))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(322) .. prompt.ObjectText .. S(129) .. power .. S(264))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(391))
    if line ~= lastSurvey then
        log(S(269) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(439), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(444), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(341) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(364))
    else
        lastAttempt = 0
        log(S(244))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(503),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(495), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(24))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(352) .. holder.Name .. S(76))
            return false
        end
        log(S(344) .. candidate.slot .. S(512) .. candidate.name
            .. S(87))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(283) .. holder.Name .. S(209) .. miner.name)
        return false
    end
    log(S(435) .. miner.name .. S(438) .. miner.power
        .. S(274) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(365) .. entry.name .. S(548) .. entry.holder.Name
            .. S(95) .. tostring(entry.prompt.ActionText) .. S(111))
        return false
    end
    log(S(334) .. entry.name .. S(569) .. entry.power
        .. S(174) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(37))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(45) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(68) .. target.holder.Name .. S(65) .. target.name
        .. S(113) .. target.power .. S(571) .. best.name .. S(382) .. best.power .. S(558))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(5)] = function()
return function(import)
local gameApi = import(S(491))(import)
local log = import(S(273))
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(265) .. (enabled and S(96) or S(415)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
            end
        end
    end
    return list
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    if buyZones then
        local nodes = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(99) .. node.zoneKey .. S(413) .. node.id
                    .. S(217) .. node.family .. S(477) .. node.level .. S(176) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if buyCore then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(299) .. node.id
                    .. S(540) .. node.family .. S(502) .. node.level .. S(511) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(119) .. blocked
                .. S(483))
        else
            log(S(145))
        end
    end
    busy = false
end
function autoResearch.HasWork()
    if buyZones and #buyableZoneNodes() > 0 then
        return true
    end
    return buyCore and #buyableCoreNodes() > 0
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(494)] = function()
return function(import)
local Workspace = game:GetService(S(397))
local gameApi = import(S(564))(import)
local teleport = import(S(102))
local log = import(S(525))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(441))
    else
        lastAttempt = 0
        log(S(388))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(4))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(116), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(379)
                .. (sold and S(125)
                         or S(194)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(78) .. (sold and (S(31) .. #keys .. S(64)) or S(98)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(385)] = function()
return function(import)
local gameApi = import(S(409))(import)
local log = import(S(277))
local STATS = { S(420), S(240), S(258) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(276)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(139) .. (enabled and S(329) or S(26)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(298) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(230) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(44) or S(275)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(2) .. holderKey .. S(543) .. choice.stat .. S(544)
                        .. choice.info.level .. S(303) .. after.level
                        .. S(566) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(282) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function findPickaxe()
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local before = pickaxe.ownedCount
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    if ownedCountOf(pickaxe.name) > before then
        log(S(120) .. pickaxe.index .. S(399) .. pickaxe.name
            .. S(455) .. pickaxe.multiplier .. S(380))
        return true
    end
    log(S(70) .. pickaxe.index .. S(166) .. pickaxe.name
        .. S(466))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(52) .. holderKey .. S(279)
                .. tostring(entry and entry.Pickaxe or S(302)) .. S(66) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(453) .. holderKey .. S(497) .. variant
                .. S(437) .. after .. S(39) .. cost .. S(260))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass and tryPickaxe() then
        bought = bought + 1
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(250))
        elseif maxed > 0 then
            log(S(431))
        else
            log(S(492))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(138) and entry.Pickaxe and entry.Pickaxe ~= S(284) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(17)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(459))
local log = import(S(474))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(375), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(287) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(134))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(357) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(90) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(480) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(278))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(16), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(81) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(486), keys)
end
function api.EquipSlot(key)
    return send(S(201), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(514), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(570), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(219), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(338), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(521)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.GetSkillNodes(zoneKey) or {}) do
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            table.insert(list, {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            })
        end
    end)
    return list
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(447) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(289), characterKey, stat == S(331) and S(481) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(506), max = S(376), cost = S(130) },
    Speed = { level = S(236), max = S(297), cost = S(280) },
    MineSpeed = { level = S(7), max = S(387), cost = S(560) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(484) then
        return 0
    end
    return tonumber(result.Total) or 0
end
return api
end
end
modules[S(482)] = function()
return function(import)
local Players = game:GetService(S(436))
local log = import(S(449))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(112))
    local mineGui = playerGui and playerGui:FindFirstChild(S(6))
    local hotbar = mineGui and mineGui:FindFirstChild(S(319))
    local content = hotbar and hotbar:FindFirstChild(S(513))
    return content and content:FindFirstChild(S(440))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(180)) then
            local count = descendant.Text:match(S(363))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(370))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(445))
    local nameLabel = content and content:FindFirstChild(S(261))
    local damageLabel = content and content:FindFirstChild(S(424))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(55) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(114), S(35)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(10) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(368) .. slot .. S(476) .. info.name
                        .. S(464) .. info.power .. S(252) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(552)] = function()
local Players = game:GetService(S(156))
local Workspace = game:GetService(S(562))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(457))
    local plots = mineWorld and mineWorld:FindFirstChild(S(292))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(204)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(199)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(144)),
        Nodes = plot:FindFirstChild(S(510)),
        Chest = plot:FindFirstChild(S(314)),
        Zones = plot:FindFirstChild(S(242)),
        Crew = plot:FindFirstChild(S(157)),
        Loot = plot:FindFirstChild(S(374)),
        PlotIndex = plot:GetAttribute(S(109)),
        ChestCapacity = plot:GetAttribute(S(404)),
    }
end
return getPlotParts
end
modules[S(360)] = function()
return function(import)
    local createWindow = import(S(172))(import)
    local i18n = import(S(161))
    local antiAfk = import(S(291))(import)
    local buildEnv = import(S(122))
    local scheduler = import(S(308))(import)
    local config = import(S(401))(import)
    local log = import(S(8))
    local window = createWindow({
        Subtitle = S(197),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(541))(import)
    local autoPlace = import(S(47))(import)
    local autoChest = import(S(546))(import)
    local autoSell = import(S(332))(import)
    local autoUpgrade = import(S(518))(import)
    local autoResearch = import(S(154))(import)
    local gameApi = import(S(421))(import)
    scheduler.RegisterStandalone(S(84), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(351), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(493), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(507), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(366), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(34), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork)
    scheduler.Start()
    local farmTab = window:AddTab(S(555), S(405))
    persistentToggle(farmTab:AddCard(
        S(485),
        S(36)
    ), S(182), autoMine.SetEnabled)
    persistentSlider(farmTab, S(384), S(140),
        1, 30, S(40), 5, autoMine.SetCyclesPerSecond, S(527))
    persistentSlider(farmTab, S(233), S(54),
        1, 10, S(317), 1, autoMine.SetClicksPerCycle, S(183))
    persistentToggle(farmTab:AddCard(
        S(170),
        S(296)
    ), S(285), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(46), S(133),
        1, 60, S(347), 3, autoPlace.SetCheckInterval, S(72))
    persistentToggle(
        farmTab:AddCard(S(171),
            S(1)),
        S(22), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(402),
        S(82)
    ), S(59), autoChest.SetEnabled)
    persistentSlider(farmTab, S(550), S(218),
        50, 3000, S(307), 100, autoChest.SetMinItems, S(547))
    persistentSlider(farmTab, S(127), S(519),
        1, 60, S(359), 3, autoChest.SetCheckInterval, S(417))
    persistentToggle(farmTab:AddCard(
        S(358),
        S(349)
    ), S(434), autoSell.SetEnabled)
    persistentSlider(farmTab, S(253), S(19),
        10, 300, S(504), 45, autoSell.SetCheckInterval, S(362))
    persistentToggle(farmTab:AddCard(
        S(549),
        S(100)
    ), S(381), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(430), S(149),
        5, 300, S(554), 20, autoUpgrade.SetInterval, S(121))
    persistentSlider(farmTab, S(316), S(79),
        1, 20, S(94), 5, autoUpgrade.SetMaxPerPass, S(186))
    persistentSegmented(
        farmTab,
        { S(266), S(403), S(152), S(563) },
        { S(410), S(327), S(295), S(427) },
        S(425), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(551), S(304),
        0, 500, S(367), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(310))
    persistentToggle(
        farmTab:AddCard(S(211),
            S(451)),
        S(13), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(526) .. pickaxe.multiplier .. S(561)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(256)
            end
            return S(234) .. index .. S(42) .. (pickaxeLabels[index] or S(412))
        end
        local savedPick = config.Get(S(115), 0)
        pickaxeCard = farmTab:AddSlider(
            S(443),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(500), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(83)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(426),
            S(43)),
        S(62), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(214),
            S(448)),
        S(337), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(262), S(123),
        1, 20, S(407), 2, autoUpgrade.SetMutationTarget, S(251))
    persistentSlider(farmTab, S(202), S(416),
        0, 500, S(400), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(392))
    persistentToggle(farmTab:AddCard(
        S(386),
        S(406)
    ), S(74), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(151), S(226),
        10, 600, S(390), 30, autoResearch.SetCheckInterval, S(85))
    persistentSlider(farmTab, S(118), S(193),
        1, 20, S(311), 5, autoResearch.SetMaxPerPass, S(238))
    persistentSlider(farmTab, S(33), S(423),
        0, 500, S(67), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(408))
    persistentToggle(
        farmTab:AddCard(S(132),
            S(224)),
        S(520), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(181),
            S(535)),
        S(345), autoResearch.SetBuyCore, true
    )
    persistentSegmented(
        farmTab,
        { S(164), S(411) },
        { true, false },
        S(530), 1, autoResearch.SetCheapestFirst
    )
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(215))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(221), S(487))
    persistentToggle(
        settingsTab:AddCard(S(336), S(27)),
        S(92),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(501), 100)
    settingsTab:AddSlider(S(346), S(354), 50, 150, savedScale, function(value)
        config.Set(S(158), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(191), 1)
    settingsTab:AddSegmented({ S(14), S(293), S(231) }, savedLanguage, function(index, value)
        local code = ({ S(131), S(300), S(38) })[index]
        config.Set(S(389), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(86), S(330), S(57) })[savedLanguage] or S(77))
    log(S(557) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(545)] = function()
return {
    [79389059854988] = S(335),
}
end
import(S(178))(import)
