-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/debug_tab", "research", "Dump", "[AutoPlot] ", "TextButton", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "AutoPlace", "core/build_env", "core/config", "MineWorld", "TextButton", "autoSellProtectUnits", "empty", "TextLabel", " is no better after the swap (", ": ", "ClaimableTiers failed", " items)", "[AutoUpgrade] bought pickaxe #", "unreadable", "'", "[AutoUpgrade] every stat on every miner is maxed", "TextLabel", "[Bb]uy", "[AutoPlace] nothing in the hotbar to place", "ขายทุกกี่วินาที", "lowest", "", "Players", " (", "[AutoPlace] fireproximityprompt error: ", "Badge", " didn't go through (out of stock or too expensive)", "s", "([%d,%.]+)%s*(%a*)", "PickaxesStand", " ", "autoPlaceUnlock", "[AutoPlace] disabled", "Auto Research (Plot)", " (x", "empty", "Auto Place: รอบดึงตัวเก่ง", "ตามเครื่อง", ", ", " ", "waiting", "core/run_job", "core/ui", "[AutoSell] MineSellItems ", "List", "MekPro", "getnilinstances", "TextLabel", "/", "dearest", "core/parse_money", "en-us", "[AutoUpgrade] ", "AutoUpgrade", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Zones", "ZoneBoardGui", "Auto Upgrade: Pickaxe ที่จะซื้อ", "[GameAPI] holder ", "s - jumping the queue", "games/my_anime_mine/auto_place", "table", "", "games/my_anime_mine/get_best_miner", "fill", "[^%w]+", "[AutoPlace] holders: ", "x", "autoResearchMaxPerPass", "Auto Upgrade: รอบเช็ค", " (", "Loot", "OwnerUserId", "function", "sale didn't go through", "Auto Place: เพดานราคาปลดช่อง", "autoClaim", "core/teleport", "no mine data", "[Decompile] === end ===", "games/my_anime_mine/auto_mine", "open_menu_hint", "%.3g", " (", "autoClaimMaxPerPass", "PlayerGui", "MineSelectZone", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Mining ก่อน", "] job failed: ", "Humanoid", "enabled", "[%X] ", " ", "UIListLayout", "Content", "games/my_anime_mine/game_api", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ไทย", "autoPlotInterval", "neither appendfile nor writefile is available", "[MekPro] settings file: ", " claimable but none could be resolved", "getscriptbytecode", "^%$", "autoResearchCore", "  ", "Damage", "Auto Plot: ซื้อโซนถัดไป", " while selecting", "Shop Stand", "Plots", "[AutoChest] can't read the chest yet", "enabled", "CharMaxLevel", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", " for ", "WalkSpeed (ปกติ 16)", ".lua", "core/logger", "games/my_anime_mine/get_plot_parts", "dev", "AutoUpgrade", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "MineSpeed", "[AutoClaim] ", "ฟาร์ม", "core/scheduler", "en", "AutoPlot", "core/logger", "Auto Claim: รอบเช็ค", "[AutoPlace] ", "[AutoPlace] unlocking ", "games/registry", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "Frame", "2026-09-21 13:08:13", "อ่านป้ายบน chest ทุกกี่วินาที", "แพงสุดก่อน", "AutoResearch", "core/logger", "off", "Auto Upgrade: สุ่ม Mutation", "Players", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "string", "[AutoClaim] idle: ", "%.3g", "This is not a supported place.", "0 = ไม่จำกัด", "แพงสุดก่อน", "Workspace", " -> ", "disabled", "autoUpgradePickaxe", "autoChestInterval", "", "Auto Research: ซื้อสาย Core", "table", "autoClaimInterval", "no", "[^%.]+", "[Decompile] === executor capabilities ===", "Container", "AutoUpgrade", "TextLabel", "Pad", "TextButton", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "TextLabel", " script(s) to ", "[Decompile] wrote ", "core/logger", "/", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "[AutoUpgrade] ", "MineEquipBest", "Mutation: เพดานราคาต่อครั้ง", "games/my_anime_mine/auto_claim", ")", "[Scheduler] ", " -> ", "maxed", "ClickDetector", "enabled", "[AutoPlot] unlocked ", "GetHolderMineSpeedLevel", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "[SpendPolicy] ", "disabled", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "TextButton", ")", "[AutoUpgrade] MineBuyPickaxe ", "s behind a purchase that never happened - letting it spend", "auto", "s", "M", "", "AutoUpgrade", "search_placeholder", "[AutoResearch] cheapest open node: ", "ProximityPrompt", "table", "ChestCapacity", " (Text=", "[Interaction] logger enabled - go click/interact with things now", "LayerCollector", "3ed6c26", "[Logger] failed to write ", "table", "", "MineGui", "core/spend_policy", "upgrade", "games/my_anime_mine/game_api", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", ")", " ชิ้น", "autoUpgradeEquipPickaxe", "cost", "MinePower", "table", "autoUpgradeMaxPerPass", "มุดแมพ (Noclip)", "autoResearch", "th", "games/my_anime_mine/auto_mine", "AutoChest", "autoSellMaxWait", " x", ") - leaving it alone for ", "[AutoUpgrade] pickaxe #", " (prompt says '", "ถูกสุดก่อน", "games/my_anime_mine/auto_sell", "Search", "ค้นหาฟังก์ชัน...", "%$%s*([%d,%.]+)%s*(%a*)", "Damage", "", "core/logger", "", "AutoSell", " (", "TextLabel", " still locked after paying ", "Content", "ZoneBoard", "getsenv", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "search_placeholder", "core/logger", "Workspace", "MekPro.log", ")", "jumpPower", "AutoPlot", " owned)", "games/my_anime_mine/auto_upgrade", "[GuiDump] === end (", "table", "Auto Sell: รอบขาย", ")", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "AutoUpgrade", " remotes", "Workspace", "games/my_anime_mine/game_api", "core/teleport", "games/registry", "fill", "autoMine", "Hauler", "core/spend_policy", "Auto Place: ปลดล็อกช่อง miner", "core/run_job", "Speed", ", have ", "Frame", "VirtualInputManager", "[Scheduler] turn: ", "Damage ก่อน", "core/logger", "", " still empty after placing ", "enabled", "TextLabel", "Players", "Speed", "core/dispatch", "autoResearchZones", "[AntiAFK] disabled", "%", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "autoResearchCheapestFirst", "Players", "function", " but no unclaimed reward in its Track", "HumanoidRootPart", "TextBox", "autoPlot", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "slot not in the hotbar", "AutoClaim", ": ", "X", " possible holder(s) - not buying more", "core/logger", "core/run_job", "ไล่ซื้อทุกกี่วินาที", "MineSpeed", "", "core/anti_afk", "UIScale", "unreadable", "AutoPlot", "TextButton", "  [attr] ", "[AutoPlot] ", "", "Players", "Auto Upgrade: แจก Pickaxe ให้ miner", "float", "disabled", "HumanoidRootPart", "Players", "core/i18n", "Auto Plot: รอบเช็ค", "Frame", "autoPlaceEquipBestInterval", "games/my_anime_mine/get_current_plot", "nothing claimable", "Header", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "table", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "autoPlaceUnlockBudget", "off", " ", "needs us at the stand - keeping the teleport", "HotbarFrame", "no isfile/readfile available to verify the write", "Auto Chest", " wouldn't take ", "disabled", "auto", "both Zone and Core buying are switched off", "Auto Research: ซื้อสาย Zone", "Limited", "", "GetHolderLevel", "JumpPower (ปกติ 50)", "TextButton", "core/run_job", "languageIndex", "ReplicatedStorage", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "AutoPlot", "games/my_anime_mine/game_api", " node(s) looked buyable but the server refused", "[MekPro] build ", "[AutoPlace] ", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", ": State(", "[AutoPlot] ", " ", "unlock", ",", "=", "^([%d,%.]+)%s*(%a*)$", "core/logger", "/", " power ", "Character", "[AutoUpgrade] ", "TextLabel", "affordable - waiting for a turn", "th", "[Uu]nlock", "CoreUI", " waited ", "x", "ลำดับการใช้เงินรวมทุกระบบ", "AutoPlace", "Players", "[AutoResearch] ", "MineUpgradeCharacter", " items (now ", "[AutoClaim] ", "%.3g", "string", "[AutoPlace] upgrading ", "TextLabel", "s - carrying on without it", "needs ", "none", "core/ui", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "autoUpgradePriority", "core/run_job", "Auto Sell: รอคิวได้นานสุด", "", "Decompile Client Scripts", "table", "TextLabel", "table", "\n", "auto", "[AutoPlace] couldn't hold anything - not pressing E", "; ", "games/my_anime_mine/game_api", "/", "MekPro/decompiled", "-", "dearest", "VirtualUser", "autoPlaceInterval", "games/my_anime_mine/auto_place", "Auto Plot: ใช้โซนล่าสุดเสมอ", "Auto Place Miner", ", base ", " fields: ", "works from anywhere - no teleport needed", "')", "Nodes", "works from anywhere - dropping the teleport", ": ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "GuiButton", " ", "cheapest", "core/scheduler", "function", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "getscripts", "getcallbackvalue", ")", "GuiButton", "games/my_anime_mine/auto_chest", "Text", "core/logger", "[AutoPlace] picked up ", "ดีบัก", "games/my_anime_mine/get_plot_parts", "[GameAPI] SendEvent failed: ", " files", "autoUpgradeInterval", "badge says ", "MineEquipPickaxe", "(", " -> ", ", built ", ") ", "Players", ") gave nothing", "UICorner", "[AutoPlace] Equip Best: hotbar's strongest ", ") ", "core/spend_policy", "MekPro", "Frame", "UserInputService", ")", "autoPlaceEquipBest", "autoUpgradeKeepMoney", "autoPlotLatest", "(nothing to do)", "Players", "write reported success but the line isn't in ", "/config.json", "ย่อขยายเมนูทั้งชุด", "table", "Dump Visible GUI", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "fly", "%.0f", "UserInputService", "🏠", "GetCharacterSpeedUpgradeCost", "menuScale", "Auto Chest: รอบเช็ค", "modules not loaded", " for ", "[AutoPlace] unlocked ", "cost", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "ProximityPrompt", "Badge", "Auto Place: เปลี่ยนตัวอ่อนออก", "core/debug_dump_remotes", "core/parse_money", " = ", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "", "[AutoUpgrade] ", "Crew", "GuiService", "Text", "MekPro", "[AutoChest] waiting: ", " pickaxe ", "Zone buying is off", "autoUpgradePickaxeIndex", "table", "[AutoPlace] couldn't pick up ", "TextLabel", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "BodyPosition", "BasePart", "core/get_leaderstat", "Auto Mine: ความเร็ว", "Hauler", "GuiObject", ") didn't take: ", "Holders", "=", "AutoPlace", " research is ", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", ") from ", "เดินทะลุกำแพงและพื้น", "[Decompile] === start ===", "debug.getupvalues", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/get_best_miner", "Auto Mine: คลิกต่อรอบ", "MineBuySkill", "[AutoSell] ", " from ", "core/i18n", "[AutoResearch] ", ",", "OwnerUserId", "autoUpgradeLimitPickaxes", "HttpService", "", " screens", "table", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "#", "[AutoChest] chest is empty", "games/my_anime_mine/auto_plot", " (", "slot ", "Speed ก่อน", " (", "ProximityPrompt", " ", "[AutoPlace] ", "games/my_anime_mine/auto_research", "x", " waited ", "none", " refused - ", "string", "Search functions...", "th", "All", "[GuiDump] === visible PlayerGui tree ===", "Auto Upgrade: กันเงินไว้", "AutoResearch", "games/my_anime_mine/get_current_plot", "s", "languageIndex", "Auto Chest: เกณฑ์เก็บ", "TextLabel", "^Swap out ", " (power ", ") ", "Frame", "UIListLayout", "'", "Auto Research: ซื้อสูงสุดต่อรอบ", ")", "บิน (Fly)", "core/run_job", "costs ", "BasePart", "Open menu", "may need us at the stand - trying that", "AutoResearch", "Players", "autoMineSpeed", "AutoChest", "games/my_anime_mine/auto_research", "ปุ่มเปิดเมนู", "[GuiDump] ", "Exclusive", "[Remotes] ", " error: ", "PlayerScripts", "TextButton", "TextLabel", ") ", "noclip", "autoResearchCoreLast", " #", "lowest", "", ")", "cheapest open node ", "Auto Research: กันเงินไว้", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Core buying is off", "core/spend_policy", "Players", "ต่างคนต่างซื้อ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", " reward(s) looked claimable but none went through", "core/debug_dump_gui", "MineEquipSlot", "_", "[Config] couldn't read ", "autoPlotKeepMoney", "string", "", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "x", "เช็คโซนทุกกี่วินาที", "[AutoSell] enabled", "index rewards are disabled in this game build", "auto", "Main", "core/logger", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "core/config", "GuiService", "core/debug_tab", "core/get_me", "games/my_anime_mine/auto_sell", "core/logger", "ValueLabel", "getrenv", "[AutoResearch] idle: ", "Auto Mine", " doesn't exist", "", "[AutoPlace] placed ", "[AutoPlace] ", "Locked miner slot", "TextLabel", "Auto Research: รอบเช็ค", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "UserInputService", "M", "[AntiAFK] idle detected, simulating input", "Auto Place: รอบเช็ค", "s", "action '", "[Interaction] Key pressed: %s (gameProcessed=%s)", "Frame", "[AutoChest] collected ", "games/my_anime_mine/auto_plot", "autoResearchKeepMoney", "Auto Upgrade Unit", "core/debug_decompile", "dearest", "  ", "core/logger", "%.3g", " '", " (", "Auto Claim: เก็บสูงสุดต่อรอบ", "core/get_place_id", "0 = ไม่จำกัด", "off", "PlayerGui", "leaderstats", "Workspace", "GetCharacterMineSpeedUpgradeCost", "autoPlotBuy", "[AutoChest] enabled (collects once >= ", "core/logger", "games/my_anime_mine/main", "ValueLabel", "UIListLayout", "s", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "games/my_anime_mine/game_api", "needs ", "AutoPlot", "getgc", "[AutoMine] ", "write reported success but ", "[AutoChest] disabled", "[AutoPlace] slot ", "core/anti_afk", "[Config] couldn't write ", "TextButton", ")", "yes", ")", "", " ", "Workspace", "", "BodyVelocity", ")", "s", " (cost ", "table", "core/debug_decompile", "TextLabel", "games/my_anime_mine/get_plot_parts", "Interaction Logger", "disabled", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "ถูกสุดก่อน", " -> ", "research", "M", "core/spend_policy", "core/humanoid", "CharMineSpeedMaxLevel", "debug.getconstants", "AutoResearch", " | ", "LocalScript", "core/debug_dump_remotes", "RemoteFunction", "ความเร็ววิ่ง", "Frame", ") -> ", "core/logger", "อังกฤษ", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", " - ", "MekPro", "hookfunction", "unlock", "MineSpeed", "[AutoSell] disabled", " costs ", " not bought yet (", "[AutoResearch] Core ", "string", " (ActionText=", "ต่ำสุดก่อน", "games/my_anime_mine/main", "BasePart", "Frame", "[AntiAFK] enabled - watching Player.Idled", "core/teleport", "Mutation: หยุดเมื่อได้ x", " (", "[", "/", "off - every automation spends on its own", "autoPlace", "Damage", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "core/debug_dump_gui", "games/my_anime_mine/game_api", "walkSpeed", "cheapest", "core/logger", "autoUpgradeRollMutation", "", "MineBuyPickaxe", "Auto Upgrade Plot (Zone)", " team power", " = ", "My Anime Mine · Mining Hub", "[AutoPlot] couldn't switch to ", "ขนาดเมนู", "[SpendPolicy] ", "number", "ItemName", "autoResearchInterval", " -> x", "autoChestMinItems", ")", "🐛", "แรงกระโดด", "[AutoUpgrade] ", "ReplicatedStorage", "", "", "AutoPlace", "autoSell", "MineWorld", "[Interaction] ProximityPrompt triggered: ", "server didn't equip it", "ยิง Equip Best ทุกกี่วินาที", " mutation x", ") needs ", "games/my_anime_mine/auto_chest", "%.3g", "games/my_anime_mine/game_api", "prompt disabled", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "games/my_anime_mine/game_api", "%$", "AutoSell", ")", "", "autoSellInterval", "Frame", "off", "core/logger", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "=empty", "AutoPlace", "[AutoClaim] claimed ", "MineSellItems", "AutoPlace", "Modules", "Backpack", "Frame", "decompile", "[Scheduler] ", "button has no size on screen", "MineRollMutation", "core/logger", "core/get_place_id", "[Interaction] logger disabled", "core/i18n", "getloadedmodules", "autoMineClicks", "Anti-AFK", "sold ", "dearest", "M", " has been busy for ", "Humanoid", " (chest holds ", "debug.getproto", "[Interaction] Button activated: ", "'", "", " '", "PlotIndex", "Players", "%.3g", "CharSpeedMaxLevel", " (still ", ": badge says ", " -> ", "ตั้งค่า", "core/parse_money", "RunService", "s", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[Decompile] decompile unavailable in this executor", "table", "%.0f", " ", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "dearest", "[Decompile] PlayerScripts not found", " (", "Run", ")", "TextLabel", " (Text=", "autoUpgradeMutationBudget", "en", ", you have ", "RunService", "nothing to place or upgrade", "/", "autoSellMaxWait", "ClickDetector", "ลอย (Float)", " costs ", "[AutoUpgrade] ", " screen(s)) ===", "/", "ItemFrame", "core/humanoid", "^M(%d+)$", "\n", "[AutoPlot] now mining ", "", "core/debug_interaction_logger", " failed)", " ===", " thing available first", "GetCharacterUpgradeCost", "Players", "%.3g", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " team power, you have ", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "MineTakeChest", " (", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "ScreenGui", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", " - upgrade the miners first", "Dump", " stack(s)", "core/logger", "autoUpgradePickaxeIndex", "interactionLogger", "every open node is maxed", "core/debug_interaction_logger", "?", "getconnections", " ", "x", "AutoClaim", "=locked(", "SellStand", ",", " pickaxe(s) for ", "[Interaction] ClickDetector clicked: ", " ", "", ": ", "[GuiDump] PlayerGui not found", "Content", "%$", ") on ", "TabBar", "ScrollingFrame", "ClickDetector", " -> ", "core/run_job", ")", "enabled", "buying the ", "[BestMiner] slot ", "/", "Empty miner slot", "table", "/s", "Core", "Plots", "GetHolderSpeedLevel", "games/my_anime_mine/game_api", "ModuleScript", "HumanoidRootPart", "TextLabel", ")", "ProximityPrompt", "UIPadding", "core/get_place_id", "Auto Sell", "[AutoResearch] ", " (money ", "core/dispatch", "Root", "autoUpgrade", "M", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[GameAPI] couldn't load the game's modules: ", " (", "th", "AutoPlace", " ", "", "s", "UICorner", "dearest", ": ", " (", "en", "slot past the usable cap", "^x(%d+)$", "", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "core/run_job", "table", "Chest", "core/logger", "nothing left to buy (", "Auto Plot: กันเงินไว้", " -> ", "%.0f", "⚙", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", " (power ", "table", "s", "[AutoPlace] drifted out of range of ", "[Remotes] === total: ", "CoreGui", "PlayerGui", "core/logger", "open_menu_hint", "games/my_anime_mine/auto_claim", "s", "AutoMine", "spendOrder", "Dump All Remotes", "UIStroke", "Workspace", "Frame", ")", "[Decompile] ", " items", "autoChest", "games/my_anime_mine/get_current_plot", "autoPlaceUpgradeWeak", ": ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "", "ZoneRow", "ไล่อัปเกรด miner ทุกกี่วินาที", " (x", "antiAfk", "Mining", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "[Scheduler] ", "autoUpgradeMutationTarget", "core/build_env", "[AutoResearch] ", "[AutoChest] MineTakeChest had no effect (", "Auto Claim (Index)", "TextButton", "menuScale", "waiting", "core/teleport", "locked slot", "core/spend_policy", "[AutoResearch] nothing open and affordable right now", "", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "BasePart", "string", "core/click", "RemoteEvent"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(321)] = function()
return function(import)
local log = import(S(902))
local Players = game:GetService(S(334))
local VirtualUser = game:GetService(S(424))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(750))
        connection = player.Idled:Connect(function()
            log(S(654))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(300))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(1016)] = function()
return { Env = S(128), IsDev = true, Commit = S(216), BuiltAt = S(144) }
end
modules[S(1031)] = function()
local VirtualInputManager = game:GetService(S(288))
local GuiService = game:GetService(S(635))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(820)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(9)] = function()
return function(import)
local HttpService = game:GetService(S(543))
local getPlaceId = import(S(823))
local log = import(S(632))
local ROOT_FOLDER = S(736)
local placeFolder = ROOT_FOLDER .. S(55) .. tostring(getPlaceId())
local filePath = placeFolder .. S(478)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(546) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(621) .. filePath .. S(313) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(696) .. filePath .. S(1005) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(710)] = function()
return function(import)
local Players = game:GetService(S(296))
local log = import(S(291))
local CAPABILITIES = {
    S(818), S(110), S(443), S(690), S(826),
    S(908), S(444), S(257), S(641), S(723),
    S(531), S(835), S(737), S(53),
}
local OUT_FOLDER = S(421)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(169)) do
        if type(target) ~= S(983) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(170))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(1000) .. name .. S(770) .. ((ok and type(value) == S(441)) and S(699) or S(168)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(852))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(599))
    if not playerScripts then
        log(S(858))
        return 0
    end
    pcall(makefolder, S(468))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(726)) or target:IsA(S(941)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(623) and #source > 0 then
                local name = target:GetFullName():gsub(S(72), S(620))
                if pcall(writefile, OUT_FOLDER .. S(876) .. name .. S(125), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(179) .. written .. S(178) .. OUT_FOLDER
        .. S(894) .. failed .. S(884))
    return written
end
local function run()
    log(S(530))
    reportCapabilities()
    local count = dumpAll()
    log(S(86))
    return count
end
return run
end
end
modules[S(760)] = function()
return function(import)
local Players = game:GetService(S(476))
local log = import(S(822))
local function dumpVisible(root, indent)
    indent = indent or S(164)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(521)) and child.Visible then
            local line = indent .. child.ClassName .. S(669) .. child.Name .. S(21)
            if child:IsA(S(23)) or child:IsA(S(325)) then
                line = line .. S(863) .. tostring(child.Text) .. S(700)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(326) .. attrName .. S(500) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(113))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(988))
    if not playerGui then
        log(S(920))
        return 0
    end
    local screens = 0
    log(S(567))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(215)) and screen.Enabled and screen.Name ~= S(52) then
            screens = screens + 1
            log(S(595) .. screen.ClassName .. S(839) .. screen.Name .. S(837))
            dumpVisible(screen, S(666))
        end
    end
    log(S(268) .. screens .. S(875))
    return screens
end
return dumpGui
end
end
modules[S(498)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(784))
local log = import(S(764))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(1032)) or descendant:IsA(S(728)) then
            count = count + 1
            log(S(597) .. descendant.ClassName .. S(435) .. descendant:GetFullName())
        end
    end
    log(S(986) .. count .. S(885))
    return count
end
return dumpRemotes
end
end
modules[S(906)] = function()
return function(import)
local Players = game:GetService(S(304))
local Workspace = game:GetService(S(677))
local UserInputService = game:GetService(S(485))
local log = import(S(667))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(790) .. describe(prompt)
            .. S(745) .. prompt.ActionText .. S(187))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(916) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(836) .. describe(button)
            .. S(213) .. tostring(button.Text) .. S(929))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(210)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(926)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(437)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(824))
        return
    end
    connections = {}
    log(S(214))
    local playerGui = Players.LocalPlayer:WaitForChild(S(675))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(945)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(871)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(446)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(658),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(636)] = function()
return function(import)
local interactionLogger = import(S(883))(import)
local dumpRemotes = import(S(727))(import)
local dumpGui = import(S(618))(import)
local decompileScripts = import(S(664))(import)
local humanoid = import(S(721))
local function build(window, persist)
    local tab = window:AddTab(S(451), S(781))
    persist.Toggle(
        tab:AddCard(
            S(713),
            S(527)
        ),
        S(904),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(995),
        S(892)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(900), function()
        remoteBtn.Text = dumpRemotes() .. S(274)
    end)
    local guiCard = tab:AddCard(
        S(481),
        S(898)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(3), function()
        guiBtn.Text = dumpGui() .. S(545)
    end)
    local decompileCard = tab:AddCard(
        S(411),
        S(94)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(860), function()
        decompileBtn.Text = decompileScripts() .. S(454)
    end)
    humanoid.Start()
    persist.Slider(tab, S(729), S(124),
        8, 200, S(762), 16, humanoid.SetWalkSpeed, S(766))
    persist.Slider(tab, S(782), S(360),
        30, 300, S(264), 50, humanoid.SetJumpPower, S(320))
    persist.Toggle(
        tab:AddCard(S(583), S(547)),
        S(483), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(232), S(529)),
        S(603), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(872), S(759)),
        S(331), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(951)] = function()
return function(import)
    local getPlaceId = import(S(672))
    local registry = import(S(278))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(156))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(518)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(676))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(637)] = function()
local Players = game:GetService(S(888))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(97))
    local backpack = player:WaitForChild(S(816))
    return player, humanoid, backpack
end
return getMe
end
modules[S(947)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(878)] = function()
local Players = game:GetService(S(841))
local RunService = game:GetService(S(849))
local UserInputService = game:GetService(S(652))
local Workspace = game:GetService(S(261))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(833))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(942))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(705))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(586)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(517)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(516))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(538)] = function()
local Players = game:GetService(S(393))
local STRINGS = {
    en = {
        search_placeholder = S(564),
        open_menu_hint = S(587),
    },
    th = {
        search_placeholder = S(245),
        open_menu_hint = S(594),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(58)
    if locale:sub(1, 2):lower() == S(565) then
        return S(958)
    end
    return S(135)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(416) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(249)] = function()
local LOG_FILE = S(262)
local function log(message)
    local line = os.date(S(99)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(704))
            end
            appendfile(LOG_FILE, line .. S(880))
        elseif writefile then
            local existing = S(961)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(415))
        else
            error(S(107))
        end
        if not (isfile and readfile) then
            error(S(350))
        end
        if not isfile(LOG_FILE) then
            error(S(692) .. LOG_FILE .. S(644))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(477) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(217) .. LOG_FILE .. S(965) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(57)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(563) then
        return nil
    end
    local number, suffix = text:match(S(246))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(914), S(607))))
    if not value then
        return nil
    end
    if suffix ~= S(219) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(284)] = function()
return function(import)
local log = import(S(379))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(754) .. name .. S(96) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(134)] = function()
return function(import)
local RunService = game:GetService(S(867))
local log = import(S(681))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(819) .. task_.name .. S(598) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(1014) .. task_.name .. S(832)
                    .. string.format(S(979), now - task_.busySince)
                    .. S(402))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(188) .. starved.name .. S(389)
            .. string.format(S(854), now - starved.waitingSince)
            .. S(66))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(289) .. (currentTask and currentTask.name or S(475)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(221)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(975))
local policy = {}
local probes = {}
local order = {}
local mode = S(346)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(196) .. (mode == S(807)
        and S(756)
        or (S(931) .. mode .. S(886))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(149) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(763) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(857) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(774) .. name .. S(560)
            .. string.format(S(484), now - blockedSince[name])
            .. S(202))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(84)] = function()
local Players = game:GetService(S(329))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(307))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(405)] = function()
return function(import)
local i18n = import(S(335))
local UserInputService = game:GetService(S(470))
local GuiService = game:GetService(S(505))
local THEME = {
    BrandName = S(507),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(963))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(464))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(946))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(1020))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(206)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(996))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(649))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(907)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(253))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(502)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(925))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(101))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(574))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(998))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(177))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(173))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(384))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(683))
            if not valueLbl then
                valueLbl = Instance.new(S(54))
                valueLbl.Name = S(640)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(32))
            if not badge then
                badge = Instance.new(S(943))
                badge.Name = S(496)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(199))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(817))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(659))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(361))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(624)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(301)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(806))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(578))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(697))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(69)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(469))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(579))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(600))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(506)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(897))
    screenGui.Name = S(388)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(987))
    local main = Instance.new(S(749))
    main.Name = S(631)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(322))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(287))
    tabBar.Name = S(924)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(684))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(143))
    header.Name = S(341)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(5))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(514))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(711))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(785)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(11))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(314)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(175))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(422)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(308))
    searchBox.Name = S(244)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(208))
    searchBox.Text = S(544)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(730))
    content.Name = S(102)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(337))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(14))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(88))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(862))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(448)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(248) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(374) .. (item.Subtitle or S(292))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(259))
        footerLabel.Text = i18n.T(S(990))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(795)] = function()
return function(import)
local gameApi = import(S(419))(import)
local log = import(S(137))
local runGuarded = import(S(48))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(680) .. minItemsToCollect .. S(18))
    else
        log(S(693))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(20) then
            log(S(119))
            lastReported = S(323)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(42) then
            log(S(549))
            lastReported = S(13)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(1022) then
            local capacity = gameApi.ChestCapacity()
            log(S(508) .. count .. S(755) .. want .. S(1001)
                .. (capacity and (S(834) .. capacity .. S(944)) or S(838)))
            lastReported = S(47)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(236), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(660) .. count .. S(396) .. after .. S(698))
        else
            log(S(1018) .. count
                .. S(459) .. tostring(after) .. S(445))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(186)] = function()
return function(import)
local gameApi = import(S(797))(import)
local log = import(S(989))
local runGuarded = import(S(362))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(132) .. (enabled and S(294) or S(197)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(154) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(812) .. reward.category .. S(702)
                .. tostring(reward.tierKey) .. S(605) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(397) .. #pending .. S(617))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(312), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(87)] = function()
return function(import)
local getCurrentPlot = import(S(570))
local getPlotParts = import(S(127))
local log = import(S(808))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(191), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(691) .. (enabled and S(98) or S(161)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(67)] = function()
return function(import)
local Players = game:GetService(S(614))
local getCurrentPlot = import(S(1003))
local getPlotParts = import(S(712))
local getMinerSlots = import(S(533))(import)
local teleport = import(S(1023))
local parseMoney = import(S(848))
local gameApi = import(S(276))(import)
local log = import(S(148))
local runGuarded = import(S(972))(import)
local spendPolicy = import(S(613))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(934)
local LOCKED_SLOT_TEXT = S(648)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(174))
    return pad and pad:FindFirstChildWhichIsA(S(555), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(413)) then
            local digits, suffix = descendant.Text:match(S(378))
            if digits then
                local value = tonumber((digits:gsub(S(376), S(882))))
                if value then
                    if suffix ~= S(701) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(358)
    return (text:gsub(S(575), S(410)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(295)) and descendant.Text:match(S(111)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(552) .. holder.Name .. S(742)
            .. gameApi.MinerSlots() .. S(380) .. gameApi.MaxMinerSlots() .. S(266)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(1024)
    end
    if not prompt.Enabled then
        return true, S(798)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(585) .. price
    end
    local action = prompt.ActionText or S(1027)
    if action:match(S(387)) or action:match(S(24)) or action:match(S(922)) then
        return true, S(657) .. action .. S(580)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(333))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(31) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(912) .. why .. S(200))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(810))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(524) .. prompt.ObjectText
                    .. S(458) .. shown .. S(429) .. math.floor(base) .. S(706))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(725))
    if line ~= lastSurvey then
        log(S(73) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(279), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(222), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(959)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(738), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(557) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(39))
    else
        lastAttempt = 0
        log(S(494))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(971),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(311)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(968)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(791)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(25))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(985) .. holder.Name .. S(116))
            return false
        end
        log(S(694) .. candidate.slot .. S(753) .. candidate.name
            .. S(522) .. tostring(why))
    end
    if not miner then
        log(S(417))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(370) .. holder.Name .. S(293) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(646) .. miner.name .. S(982) .. miner.power
        .. S(923) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(513) .. entry.name .. S(537) .. entry.holder.Name
            .. S(241) .. tostring(entry.prompt.ActionText) .. S(432))
        return false
    end
    log(S(450) .. entry.name .. S(576) .. entry.power
        .. S(528) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(647) .. pad.holder.Name .. S(254)
                .. tostring(pad.priceText))
        else
            log(S(492) .. pad.holder.Name
                .. S(123) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(139) .. holderName .. S(15)
            .. string.format(S(668), before) .. S(978) .. string.format(S(89), after)
            .. S(239) .. SWAP_COOLDOWN .. S(656))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(465)
            .. string.format(S(398), bestBefore) .. S(927)
            .. string.format(S(842), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(525), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(868))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(71) then
        runGuarded(S(814), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(375) then
        log(S(140) .. target.holder.Name
            .. S(491) .. tostring(target.priceText))
        runGuarded(S(7), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(400) .. target.holder.Name .. S(919) .. target.name
        .. S(551) .. target.power .. S(731) .. best.name .. S(77) .. best.power .. S(471))
    runGuarded(S(392), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(661)] = function()
return function(import)
local gameApi = import(S(687))(import)
local parseMoney = import(S(499))
local log = import(S(316))
local runGuarded = import(S(408))(import)
local spendPolicy = import(S(720))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(327) .. (enabled and S(192) or S(353)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(801)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(881) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(90) .. deepestKey .. S(780))
        return true
    end
    log(S(772) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(136)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(373) .. gameApi.ZoneDisplayName(zoneKey) .. S(873)
                .. math.floor(price) .. S(866) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(193) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(252) .. zoneKey .. S(861))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(718) then
            reportedLocked = S(2)
            log(S(4) .. zoneKey .. S(562)
                .. gameApi.ZoneDisplayName(currentKey) .. S(526)
                .. done .. S(933) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(689)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(265), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(593)] = function()
return function(import)
local gameApi = import(S(367))(import)
local log = import(S(449))
local runGuarded = import(S(317))(import)
local spendPolicy = import(S(467))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(539) .. (enabled and S(120) or S(714)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(949) .. node.zoneKey .. S(347) .. node.id
                    .. S(670) .. node.family .. S(577) .. node.level .. S(160) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(743) .. node.id
                    .. S(859) .. node.family .. S(461) .. node.level .. S(846) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(394) .. blocked
                .. S(368))
        elseif powerBlocked then
            log(S(1017) .. powerBlocked.zoneKey .. S(556) .. powerBlocked.id
                .. S(554) .. powerBlocked.family .. S(794)
                .. math.floor(powerBlocked.powerReq) .. S(891)
                .. math.floor(powerBlocked.power)
                .. S(899))
        else
            log(S(1026)
                .. S(950) .. math.floor(gameApi.GetMoney()) .. S(999))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(209) .. nearest.zoneKey
                    .. S(37) .. nearest.id .. S(957) .. nearest.family .. S(466)
                    .. nearest.level .. S(869) .. nearest.max
                    .. S(741) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(355)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(937),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(510))
        end
        if not buyCore then
            table.insert(parts, S(612))
        end
        if #parts == 0 then
            return S(905)
        end
        return S(976) .. table.concat(parts, S(45)) .. S(271)
    end
    local why
    if nearest.cost > budget then
        why = S(688) .. string.format(S(155), nearest.cost)
            .. S(286) .. string.format(S(796), money)
    elseif nearest.powerOk == false then
        why = S(403) .. string.format(S(889), nearest.powerReq) .. S(769)
    else
        why = S(385)
    end
    return S(609) .. nearest.zoneKey .. S(438) .. tostring(nearest.id)
        .. S(30) .. tostring(nearest.family) .. S(602) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(642) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(56) and cost > best)
            or (mode ~= S(964) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(724)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(569), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(638)] = function()
return function(import)
local Workspace = game:GetService(S(159))
local gameApi = import(S(940))(import)
local teleport = import(S(751))
local log = import(S(639))
local runGuarded = import(S(928))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(740))
    else
        lastAttempt = 0
        log(S(628))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(913))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(495), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(50)
                .. (sold and S(434)
                         or S(348)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(536) .. (sold and (S(829) .. #keys .. S(901)) or S(81)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(802), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(532)] = function()
return function(import)
local Workspace = game:GetService(S(997))
local gameApi = import(S(223))(import)
local teleport = import(S(277))
local log = import(S(126))
local runGuarded = import(S(584))(import)
local spendPolicy = import(S(1025))(import)
local STATS = { S(247), S(297), S(739) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(606)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(874) .. (enabled and S(930) or S(332)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(218) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(493) or S(190)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(183) .. holderKey .. S(960) .. choice.stat .. S(46)
                        .. choice.info.level .. S(189) .. after.level
                        .. S(181) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(228) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(36))
    local shop = stand and stand:FindFirstChild(S(117))
    if not shop then
        return nil
    end
    if shop:IsA(S(1029)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(748), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(201)
                .. (bought and S(431)
                           or S(588)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(302))
        end
    end
    if bought then
        log(S(19) .. pickaxe.index .. S(917) .. pickaxe.name
            .. S(1010) .. pickaxe.multiplier .. S(263))
        return true
    end
    log(S(240) .. pickaxe.index .. S(909) .. pickaxe.name
        .. S(33))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(383) .. holderKey .. S(509)
                    .. tostring(worn or S(404)) .. S(717) .. wanted.name)
            else
                log(S(59) .. holderKey .. S(352)
                    .. wanted.name .. S(844) .. tostring(nowWorn or S(561)) .. S(608))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(503) .. holderKey .. S(793) .. variant
                .. S(778) .. after .. S(708) .. cost .. S(803))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(273))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(783) .. gameApi.OwnedPickaxeCount()
                    .. S(915) .. gameApi.MaxMinerSlots()
                    .. S(315))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(122))
        elseif maxed > 0 then
            log(S(22))
        else
            log(S(436))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(665) and price > best)
            or (mode ~= S(423) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(60))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(207), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(800)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(364))
local Workspace = game:GetService(S(275))
local log = import(S(260))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(815), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(956) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(211))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(343) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(520) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(68) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(935) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(281) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(879))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(453) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(893))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(566), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(973) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(269) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(382) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(813), keys)
end
function api.EquipSlot(slot)
    return send(S(619), slot == S(28) and S(1007) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(767), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(457), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(821), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(184))
end
function api.BuySkill(zoneKey, family)
    return send(S(535), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(709)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(151)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(229))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(305) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(166) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(93), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(10))
    plots = plots and plots:FindFirstChild(S(118))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(29)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(79)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(256))
    local gui = board and board:FindFirstChild(S(63))
    local root = gui and gui:FindFirstChild(S(952))
    local list = root and root:FindFirstChild(S(51))
    local row = list and list:FindFirstChild(S(1008) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(401)) and descendant.Text ~= S(804) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(80) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(395), characterKey, stat == S(1012) and S(319) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(359), max = S(121), cost = S(887) },
    Speed = { level = S(939), max = S(843), cost = S(487) },
    MineSpeed = { level = S(194), max = S(722), cost = S(678) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(1030) or charName == S(786) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(377) .. tostring(value))
        end
        table.sort(fields)
        log(S(65) .. holderKey .. S(430) .. table.concat(fields, S(100)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(414) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(775) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(744) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(480) and type(holder.CharName) == S(153) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(357), S(596) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(512) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(490)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(85)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(629)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(853) then
        return {}, S(17)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(340)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(412) or type(state.Track) ~= S(230) then
                table.insert(notes, tabKey .. S(372) .. tostring(category) .. S(463))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(845) .. waiting
                        .. S(306))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(456) .. total .. S(109)
            .. (#notes > 0 and (S(735) .. table.concat(notes, S(418))) or S(970))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(399) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(70)] = function()
return function(import)
local Players = game:GetService(S(590))
local gameApi = import(S(761))(import)
local log = import(S(732))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(35))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(540), S(918))))
    if not value then
        return nil
    end
    if suffix ~= S(250) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(92))
    local mineGui = playerGui and playerGui:FindFirstChild(S(220))
    local hotbar = mineGui and mineGui:FindFirstChild(S(349))
    local content = hotbar and hotbar:FindFirstChild(S(921))
    return content and content:FindFirstChild(S(171))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(601)) then
            local count = descendant.Text:match(S(969))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(877))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(255))
    local nameLabel = content and content:FindFirstChild(S(776))
    local damageLabel = content and content:FindFirstChild(S(758))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(645) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(420) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(932) .. slot .. S(16) .. info.name
                        .. S(381) .. info.power .. S(238) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(339)] = function()
local Players = game:GetService(S(462))
local Workspace = game:GetService(S(703))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(789))
    local plots = mineWorld and mineWorld:FindFirstChild(S(938))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(541)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(452)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(523)),
        Nodes = plot:FindFirstChild(S(433)),
        Chest = plot:FindFirstChild(S(974)),
        Zones = plot:FindFirstChild(S(62)),
        Crew = plot:FindFirstChild(S(504)),
        Loot = plot:FindFirstChild(S(78)),
        PlotIndex = plot:GetAttribute(S(840)),
        ChestCapacity = plot:GetAttribute(S(212)),
    }
end
return getPlotParts
end
modules[S(682)] = function()
return function(import)
    local createWindow = import(S(49))(import)
    local i18n = import(S(825))
    local antiAfk = import(S(695))(import)
    local buildEnv = import(S(8))
    local scheduler = import(S(440))(import)
    local spendPolicy = import(S(282))(import)
    local config = import(S(634))(import)
    local log = import(S(180))
    log(S(369) .. buildEnv.Commit .. S(966) .. buildEnv.Env
        .. S(460) .. buildEnv.BuiltAt .. S(582))
    local window = createWindow({
        Subtitle = S(771),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(235))(import)
    local autoPlace = import(S(426))(import)
    local autoChest = import(S(447))(import)
    local autoSell = import(S(243))(import)
    local autoUpgrade = import(S(267))(import)
    local autoResearch = import(S(558))(import)
    local autoClaim = import(S(991))(import)
    local autoPlot = import(S(550))(import)
    local gameApi = import(S(103))(import)
    scheduler.RegisterStandalone(S(993), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(592), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(787), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(172), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(589), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(911), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(324), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(237), 20)
    scheduler.RegisterExclusive(S(251), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(147), autoResearch.NextPrice)
    spendPolicy.Register(S(129), autoUpgrade.NextPrice)
    spendPolicy.Register(S(366), autoPlot.NextPrice)
    spendPolicy.Register(S(811), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(133), S(486))
    farmTab:AddSectionHeader(S(391))
    persistentSegmented(
        farmTab,
        { S(615), S(242), S(146) },
        { S(674), S(439), S(830) },
        S(994), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(643),
        S(61)
    ), S(280), autoMine.SetEnabled)
    persistentSlider(farmTab, S(519), S(715),
        1, 30, S(591), 5, autoMine.SetCyclesPerSecond, S(936))
    persistentSlider(farmTab, S(534), S(198),
        1, 10, S(827), 1, autoMine.SetClicksPerCycle, S(626))
    persistentToggle(farmTab:AddCard(
        S(428),
        S(896)
    ), S(757), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(655), S(342),
        1, 60, S(425), 3, autoPlace.SetCheckInterval, S(571))
    persistentToggle(
        farmTab:AddCard(S(497),
            S(1013)),
        S(1004), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(856),
            S(501)),
        S(472), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(43), S(792),
        5, 300, S(338), 60, autoPlace.SetEquipBestInterval, S(850))
    persistentToggle(
        farmTab:AddCard(S(283),
            S(152)),
        S(38), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(82), S(673),
        0, 500, S(345), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(205))
    persistentToggle(farmTab:AddCard(
        S(351),
        S(195)
    ), S(1002), autoChest.SetEnabled)
    persistentSlider(farmTab, S(573), S(633),
        50, 3000, S(779), 100, autoChest.SetMinItems, S(226))
    persistentSlider(farmTab, S(489), S(145),
        1, 60, S(163), 3, autoChest.SetCheckInterval, S(992))
    persistentToggle(farmTab:AddCard(
        S(948),
        S(611)
    ), S(788), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(799),
            S(442)),
        S(12), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(270), S(26),
        5, 300, S(805), 20, autoSell.SetCheckInterval, S(34))
    farmTab:AddSlider(S(409),
        S(182),
        5, 120, sellMaxWait, function(value)
            config.Set(S(870), value)
        end, S(962))
    persistentToggle(farmTab:AddCard(
        S(663),
        S(890)
    ), S(953), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(76), S(1009),
        5, 300, S(455), 20, autoUpgrade.SetInterval, S(685))
    persistentSlider(farmTab, S(142), S(104),
        1, 20, S(231), 5, autoUpgrade.SetMaxPerPass, S(910))
    persistentSegmented(
        farmTab,
        { S(746), S(290), S(553), S(95) },
        { S(27), S(114), S(285), S(131) },
        S(407), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(568), S(515),
        0, 500, S(473), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(831))
    persistentToggle(
        farmTab:AddCard(S(955),
            S(1006)),
        S(162), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(41) .. pickaxe.multiplier .. S(225)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(310)
            end
            return S(548) .. index .. S(855) .. (pickaxeLabels[index] or S(365))
        end
        local savedPick = config.Get(S(903), 0)
        pickaxeCard = farmTab:AddSlider(
            S(64),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(511), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(328)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(176),
            S(981)),
        S(542), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(330),
            S(686)),
        S(227), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(150),
            S(895)),
        S(765), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(752), S(406),
        1, 20, S(1015), 2, autoUpgrade.SetMutationTarget, S(74))
    persistentSlider(farmTab, S(185), S(157),
        0, 500, S(864), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(719))
    persistentToggle(farmTab:AddCard(
        S(40),
        S(625)
    ), S(233), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(650), S(318),
        10, 600, S(777), 30, autoResearch.SetCheckInterval, S(204))
    persistentSlider(farmTab, S(581), S(851),
        1, 20, S(75), 5, autoResearch.SetMaxPerPass, S(390))
    persistentSlider(farmTab, S(610), S(482),
        0, 500, S(662), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(653))
    persistentToggle(
        farmTab:AddCard(S(356),
            S(734)),
        S(299), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(165),
            S(130)),
        S(112), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(1028),
            S(6)),
        S(604), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(716), S(158) },
        { true, false },
        S(303), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(1019),
        S(344)
    ), S(83), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(138), S(258),
        5, 300, S(167), 30, autoClaim.SetCheckInterval, S(707))
    persistentSlider(farmTab, S(671), S(224),
        1, 30, S(91), 10, autoClaim.SetMaxPerPass, S(559))
    persistentToggle(farmTab:AddCard(
        S(768),
        S(651)
    ), S(309), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(336), S(627),
        5, 300, S(106), 30, autoPlot.SetCheckInterval, S(984))
    persistentToggle(
        farmTab:AddCard(S(115),
            S(371)),
        S(679), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(427),
            S(272)),
        S(474), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(977), S(616),
        0, 500, S(622), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(954))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(1))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(847), S(980))
    persistentToggle(
        settingsTab:AddCard(S(828), S(809)),
        S(1011),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(1021), 100)
    settingsTab:AddSlider(S(773), S(479), 50, 150, savedScale, function(value)
        config.Set(S(488), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(363), 1)
    settingsTab:AddSegmented({ S(44), S(733), S(105) }, savedLanguage, function(index, value)
        local code = ({ S(354), S(967), S(386) })[index]
        config.Set(S(572), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(203), S(865), S(234) })[savedLanguage] or S(630))
    log(S(108) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(141)] = function()
return {
    [79389059854988] = S(747),
}
end
import(S(298))(import)
