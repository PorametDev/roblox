-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Error: ", "Frame", "neither appendfile nor writefile is available", "  [attr] ", "ProximityPrompt", "Anti-AFK", " (MaxActivationDistance=%s)", "games/my_anime_mine/auto_upgrade", "[Interaction] ProximityPrompt triggered: ", "[AutoMine] enabled", "GuiButton", "[Interaction] Button activated: ", " doesn't exist", "[AutoSell] enabled", "games/my_anime_mine/get_current_plot", "Auto Upgrade", "Frame", "games/my_anime_mine/auto_chest", "[MyAnimeMine] === Tool: ", "  [attr] ", "ScrollingFrame", "Crew", "?", "Auto Chest", "RunService", "core/get_me", "core/get_place_id", ")", "  [attr] ", "[AutoSell] clicked SelectAllButton", "", "RunService", "ChestFrame", "Backpack", "'", "CoreGui", "TextLabel", "games/my_anime_mine/auto_chest", "core/ui", "core/dispatch", "  [attr] ", "[GuiDump] MineGui not found", "ReplicatedStorage", "'", "en", "TextButton", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "PlayerGui", "Debug: Dump Node/Holder", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "RunService", "Debug: Dump Sell Cards", " = ", "TextLabel", "games/my_anime_mine/auto_mine", "Dump", "[AutoSell] clicked SellButton", " (ActionText=", "-", "Done", "Holders", " = ", "games/my_anime_mine/get_plot_parts", "\n", "games/my_anime_mine/main", "MineGui", "ProximityPrompt", "PlayerGui", "Frame", "games/my_anime_mine/get_plot_parts", "[AutoSell] SelectAllButton click error: ", "ClickDetector", "TextLabel", "PlayerGui", "Header", "ClickDetector", "Badge", ": ", "games/my_anime_mine/auto_place", "", "[AutoUpgrade] enabled", "TextLabel", "Content", "ClickDetector", "VirtualUser", "[AutoMine] disabled", "Dump", "RemoteEvent", "th", "[Remotes] ", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/debug_dump", "MineGui", "Content", "Players", "games/my_anime_mine/auto_place", "games/registry", "[AutoChest] TakeButton click error: ", "SellButton", "TextButton", "[Logger] failed to write ", "UIScale", ")", "Loot", "TextLabel", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "core/logger", "[AutoSell] disabled", "open_menu_hint", "MekPro.log", "Auto Sell", "Search", "core/anti_afk", "Empty miner slot", "Plots", "Dump", "Debug: Dump All Remotes", "core/get_place_id", "ตามเครื่อง", "ValueLabel", "ตั้งค่า", "MineGui", "UIStroke", "Players", "Label", "Backpack", "en", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "core/logger", "auto", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "TextLabel", "games/my_anime_mine/get_plot_parts", "Tool", "ProximityPrompt", "Players", "games/my_anime_mine/auto_sell", " (Text=", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "  ", "Content", "ค้นหาฟังก์ชัน...", "Select All", "no tool", "", "write reported success but ", "MineGui", "ฟาร์ม", "core/logger", "Backpack", "games/my_anime_mine/auto_sell", "", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "Pad", "core/logger", "games/my_anime_mine/get_current_plot", "Players", "Workspace", "Content", "games/my_anime_mine/debug_dump_sell_cards", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "games/my_anime_mine/get_current_plot", "log failed", "TextBox", "Frame", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Zones", "", "RemoteFunction", "[GuiDump] === visible MineGui tree ===", "", "ClickDetector", "ValueBase", "Chest", "[AntiAFK] idle detected, simulating input", "[MyAnimeMine] not standing in any plot", "RunService", " = ", "core/anti_afk", "games/my_anime_mine/get_plot_parts", "[AutoSell] SellButton click error: ", "games/registry", "Done", "Players", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "core/ui", "core/logger", " ", "%", "MineWorld", "core/logger", "TextButton", " = ", " remotes", "UIListLayout", "[AutoPlace] placed ", " ===", " (Text=", "TextLabel", "Text", "  ", "UpgradePart", "ไทย", "TextButton", ")", "core/logger", "core/dispatch", "games/my_anime_mine/debug_dump_sell_cards", "games/my_anime_mine/debug_dump_remotes", "", "MekPro", "[AutoPlace] disabled", "TextButton", "[AntiAFK] enabled - watching Player.Idled", "Players", "ValueLabel", "UIPadding", "Content", "TextLabel", "TextLabel", "Frame", "TextButton", "TabBar", "This is not a supported place.", "", "not found", "Debug: Current Plot", "TakeButton", "Debug: Dump Visible GUI", "no plot", "'", "games/my_anime_mine/debug_dump_gui", "ย่อขยายเมนูทั้งชุด", "[AntiAFK] disabled", "ClickDetector", "[MyAnimeMine] no nodes present right now", "games/my_anime_mine/get_current_plot", "GuiButton", "Players", " ===", ": ", "PlayerGui", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "games/my_anime_mine/auto_mine", "Frame", "Workspace", "", "[Interaction] logger enabled - go click/interact with things now", "TextLabel", "[AutoChest] disabled", " ===", "Nodes", "SellFrame", "[MyAnimeMine] === Node: ", "Check", "RemoteEvent", "PlotIndex", "games/my_anime_mine/get_current_plot", "[Interaction] Key pressed: %s (gameProcessed=%s)", "Players", "Humanoid", "write reported success but the line isn't in ", "games/my_anime_mine/debug_dump", "", "core/logger", "TextLabel", "core/get_leaderstat", "UserInputService", "Main", "ProximityPrompt", "RunService", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", " ===", "%", "core/logger", "ProximityPrompt", "[MyAnimeMine] === Dump start ===", " '", "Tool", "[AutoUpgrade] disabled", "core/interaction_logger", "Dump", "Auto Mine", "", ": ", " '", "[AutoChest] enabled", "core/logger", "Players", "core/i18n", "X", "no isfile/readfile available to verify the write", "Search functions...", "ProximityPrompt", " '", "core/i18n", "Auto Place Miner", " ===", "games/my_anime_mine/auto_upgrade", "🏠", "OwnerUserId", "Scroll", "Frame", "TextButton", "open_menu_hint", "[Interaction] logger disabled", "UIListLayout", "[MyAnimeMine] no holders found", "Players", "games/my_anime_mine/debug_dump_gui", "games/my_anime_mine/main", "TextLabel", " <-- remote, likely fired by the real interaction", "\n", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "อังกฤษ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "auto", " '", "[Interaction] ClickDetector clicked: ", "games/my_anime_mine/get_current_plot", "TextButton", "ขนาดเมนู", "TextButton", "Humanoid", "UIListLayout", "'", "[MyAnimeMine] === Holder: ", "Players", "games/my_anime_mine/debug_dump_remotes", "UserInputService", "[GuiDump] === end ===", "My Anime Mine · Mining Hub", " on ", "Dump", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "[AutoChest] clicked TakeButton", "Card", "  ", "games/my_anime_mine/get_plot_parts", "Frame", "core/logger", "SelectAllButton", "Text", "core/interaction_logger", "Sell for", "search_placeholder", "Debug: Dump Backpack Tool", "Badge", "Workspace", "SellStand", "leaderstats", "ScreenGui", " = ", "UICorner", "Frame", "Frame", "core/logger", "", " cards", "", "Actions", "ChestCapacity", "Open menu", "GuiObject", "PlayerGui", "GuiObject", "⚙", "core/i18n", "[Remotes] === total: ", "SellFrame", "en-us", "[SellCards] === total cards: ", "th", "[AutoPlace] enabled", "[%X] ", "[SellCards] Card #", "CoreUI", "TextLabel", "Frame", "Interaction Logger (debug)", "search_placeholder", "th", "Done", "UICorner", "ปุ่มเปิดเมนู", "RemoteFunction"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(179)] = function()
return function(import)
local log = import(S(155))
local Players = game:GetService(S(136))
local VirtualUser = game:GetService(S(85))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(214))
        connection = player.Idled:Connect(function()
            log(S(175))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(234))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(207)] = function()
return function(import)
    local getPlaceId = import(S(118))
    local registry = import(S(97))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(224))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(267)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(353))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(26)] = function()
local Players = game:GetService(S(239))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(261))
    local backpack = player:WaitForChild(S(126))
    return player, humanoid, backpack
end
return getMe
end
modules[S(27)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(296)] = function()
local Players = game:GetService(S(215))
local STRINGS = {
    en = {
        search_placeholder = S(293),
        open_menu_hint = S(365),
    },
    th = {
        search_placeholder = S(142),
        open_menu_hint = S(387),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(373)
    if locale:sub(1, 2):lower() == S(375) then
        return S(89)
    end
    return S(127)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(318) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(281)] = function()
return function(import)
local Players = game:GetService(S(260))
local Workspace = game:GetService(S(351))
local UserInputService = game:GetService(S(268))
local log = import(S(206))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(9) .. describe(prompt)
            .. S(58) .. prompt.ActionText .. S(28))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(320) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(12) .. describe(button)
            .. S(138) .. tostring(button.Text) .. S(205))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(276)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(84)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(11)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(306))
        return
    end
    connections = {}
    log(S(248))
    local playerGui = Players.LocalPlayer:WaitForChild(S(48))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(67)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(172)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(238)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(259),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(107)] = function()
local LOG_FILE = S(110)
local function log(message)
    local line = os.date(S(377)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(31))
            end
            appendfile(LOG_FILE, line .. S(64))
        elseif writefile then
            local existing = S(264)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(314))
        else
            error(S(3))
        end
        if not (isfile and readfile) then
            error(S(292))
        end
        if not isfile(LOG_FILE) then
            error(S(146) .. LOG_FILE .. S(13))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(262) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(101) .. LOG_FILE .. S(241) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(39)] = function()
return function(import)
local i18n = import(S(370))
local UserInputService = game:GetService(S(331))
local THEME = {
    BrandName = S(211),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(356))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(386))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(217))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(204))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(168)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(123))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(132))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(23)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(73))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(225)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(21))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(307))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(219))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(2))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(312))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(105))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(220))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(216))
            if not valueLbl then
                valueLbl = Instance.new(S(37))
                valueLbl.Name = S(120)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(77))
            if not badge then
                badge = Instance.new(S(266))
                badge.Name = S(350)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(213))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(358))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(17))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(222))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(360)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(274))
        local track = Instance.new(S(221))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(303))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(192))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(152)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(189)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(69))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(326))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(324))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(200)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(354))
    screenGui.Name = S(379)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(36))
    local main = Instance.new(S(165))
    main.Name = S(269)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(102))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(342))
    tabBar.Name = S(223)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(195))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(357))
    header.Name = S(75)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(322))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(249))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(199))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(247)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(100))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(291)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(46))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(59)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(164))
    searchBox.Name = S(112)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(383))
    searchBox.Text = S(284)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(381))
    content.Name = S(94)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(245))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(54))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(305))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(380))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(345)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(362) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(188) .. (item.Subtitle or S(80))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(348))
        footerLabel.Text = i18n.T(S(109))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(18)] = function()
return function(import)
local Players = game:GetService(S(329))
local RunService = game:GetService(S(51))
local getCurrentPlot = import(S(156))
local getPlotParts = import(S(91))
local log = import(S(187))
local FIRE_INTERVAL = 1
local function getTakeButton()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(74))
    local mineGui = playerGui and playerGui:FindFirstChild(S(147))
    local chestFrame = mineGui and mineGui:FindFirstChild(S(33))
    local content = chestFrame and chestFrame:FindFirstChild(S(141))
    local actions = content and content:FindFirstChild(S(363))
    return actions and actions:FindFirstChild(S(228))
end
local function clickButton(button)
    local ok, err = pcall(function()
        firesignal(button.MouseButton1Click)
    end)
    return ok, err
end
local autoChest = {}
local connection = nil
function autoChest.SetEnabled(enabled)
    if enabled == autoChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(250))
        return
    end
    log(S(287))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        local parts = plot and getPlotParts(plot)
        local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(294), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        local takeButton = getTakeButton()
        if takeButton then
            local ok, err = clickButton(takeButton)
            if ok then
                log(S(338))
            else
                log(S(98) .. tostring(err))
            end
        end
    end)
end
function autoChest.IsEnabled()
    return connection ~= nil
end
return autoChest
end
end
modules[S(244)] = function()
return function(import)
local RunService = game:GetService(S(32))
local getCurrentPlot = import(S(321))
local getPlotParts = import(S(63))
local log = import(S(191))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(235), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(86))
        return
    end
    log(S(10))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(96)] = function()
return function(import)
local Players = game:GetService(S(124))
local RunService = game:GetService(S(177))
local getCurrentPlot = import(S(162))
local getPlotParts = import(S(133))
local log = import(S(288))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(154))
    return pad and pad:FindFirstChildWhichIsA(S(270), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(114)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(212))
        return
    end
    log(S(376))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(150))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(325))
        local tool = backpack:FindFirstChildWhichIsA(S(134))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(196) .. tool.Name .. S(334) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(151)] = function()
return function(import)
local Players = game:GetService(S(309))
local Workspace = game:GetService(S(158))
local RunService = game:GetService(S(25))
local log = import(S(129))
local FIRE_INTERVAL = 3
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(367))
    local mineGui = playerGui and playerGui:FindFirstChild(S(66))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(253))
    return sellFrame and sellFrame:FindFirstChild(S(83))
end
local function labelText(button)
    local content = button:FindFirstChild(S(218))
    local label = content and content:FindFirstChild(S(125))
    return label and label.Text or S(210)
end
local function clickButton(button)
    local ok, err = pcall(function()
        firesignal(button.MouseButton1Click)
    end)
    return ok, err
end
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(108))
        return
    end
    log(S(14))
    local lastFire = 0
    local busy = false
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL or busy then
            return
        end
        lastFire = now
        local sellStand = Workspace:FindFirstChild(S(352))
        local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(5), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        busy = true
        task.spawn(function()
            task.wait(0.3)
            local content = getSellFrame()
            local selectAllButton = content and content:FindFirstChild(S(344))
            local sellButton = content and content:FindFirstChild(S(99))
            if selectAllButton and labelText(selectAllButton) == S(143) then
                local ok, err = clickButton(selectAllButton)
                if ok then
                    log(S(30))
                else
                    log(S(71) .. tostring(err))
                end
                task.wait(0.3)
            end
            if sellButton and labelText(sellButton):find(S(347), 1, true) then
                local ok, err = clickButton(sellButton)
                if ok then
                    log(S(57))
                else
                    log(S(181) .. tostring(err))
                end
            end
            busy = false
        end)
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(299)] = function()
return function(import)
local RunService = game:GetService(S(271))
local getCurrentPlot = import(S(237))
local getPlotParts = import(S(180))
local log = import(S(275))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(202))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(76))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(280))
        return
    end
    log(S(81))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(92)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(171)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(295) .. child.Name .. S(44)
        if child:IsA(S(135)) then
            line = line .. string.format(
                S(139),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(72)) then
            line = line .. string.format(S(7), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(88)) or child:IsA(S(169)) then
            line = line .. S(313)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(41) .. attrName .. S(62) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(201))
    end
end
return dumpInstance
end
modules[S(310)] = function()
return function(import)
local Players = game:GetService(S(157))
local log = import(S(265))
local function dumpVisible(root, indent)
    indent = indent or S(145)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(366)) and child.Visible then
            local line = indent .. child.ClassName .. S(319) .. child.Name .. S(327)
            if child:IsA(S(82)) or child:IsA(S(304)) then
                line = line .. S(198) .. tostring(child.Text) .. S(103)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(20) .. attrName .. S(178) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(340))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(242))
    local mineGui = playerGui and playerGui:FindFirstChild(S(93))
    if not mineGui then
        log(S(42))
        return
    end
    log(S(170))
    dumpVisible(mineGui)
    log(S(332))
end
return dumpMineGui
end
end
modules[S(209)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(43))
local log = import(S(359))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(256)) or descendant:IsA(S(388)) then
            count = count + 1
            log(S(90) .. descendant.ClassName .. S(78) .. descendant:GetFullName())
        end
    end
    log(S(371) .. count .. S(251))
    return count
end
return dumpRemotes
end
end
modules[S(160)] = function()
return function(import)
local Players = game:GetService(S(289))
local log = import(S(343))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(68))
    local mineGui = playerGui and playerGui:FindFirstChild(S(122))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(372))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(159)) and sellFrame.Content:FindFirstChild(S(302))
    if not scroll then
        log(S(185))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(339) or card:IsA(S(368)) then
            count = count + 1
            log(S(378) .. count .. S(285) .. card.ClassName .. S(286) .. card.Name .. S(35))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(4) .. attrName .. S(193) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(140) .. child.ClassName .. S(278) .. child.Name .. S(231)
                if child:IsA(S(173)) then
                    line = line .. S(53) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(374) .. count .. S(298))
    return count
end
return dumpSellCards
end
end
modules[S(258)] = function()
local Players = game:GetService(S(95))
local Workspace = game:GetService(S(246))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(190))
    local plots = mineWorld and mineWorld:FindFirstChild(S(115))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(301)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(341)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(61)),
        Nodes = plot:FindFirstChild(S(252)),
        Chest = plot:FindFirstChild(S(174)),
        Zones = plot:FindFirstChild(S(167)),
        Crew = plot:FindFirstChild(S(22)),
        Loot = plot:FindFirstChild(S(104)),
        PlotIndex = plot:GetAttribute(S(257)),
        ChestCapacity = plot:GetAttribute(S(364)),
    }
end
return getPlotParts
end
modules[S(311)] = function()
return function(import)
    local Players = game:GetService(S(184))
    local createWindow = import(S(186))(import)
    local i18n = import(S(290))
    local antiAfk = import(S(113))(import)
    local interactionLogger = import(S(346))(import)
    local log = import(S(149))
    local window = createWindow({
        Subtitle = S(333),
    })
    local getCurrentPlot = import(S(15))
    local getPlotParts = import(S(70))
    local dumpInstance = import(S(263))
    local dumpRemotes = import(S(330))(import)
    local dumpSellCards = import(S(208))(import)
    local dumpGui = import(S(232))(import)
    local autoMine = import(S(55))(import)
    local autoPlace = import(S(79))(import)
    local autoChest = import(S(38))(import)
    local autoSell = import(S(137))(import)
    local autoUpgrade = import(S(8))(import)
    local farmTab = window:AddTab(S(148), S(300))
    farmTab:AddCard(
        S(283),
        S(131)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(297),
        S(50)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(24),
        S(161)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(111),
        S(243)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(16),
        S(47)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(227), S(272))
    local checkBtn
    checkBtn = debugCard:AddButton(S(255), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(226)
            log(S(176))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(166),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(49),
        S(315)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(335), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(230)
            return
        end
        local ok, err = log(S(277))
        if not ok then
            dumpBtn.Text = S(163)
            dumpCard:SetSubtitle(S(1) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(254) .. firstNode.Name .. S(240))
            dumpInstance(firstNode, log)
        else
            log(S(236))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(328) .. firstHolder.Name .. S(197))
            dumpInstance(firstHolder, log)
        else
            log(S(308))
        end
        dumpBtn.Text = S(60)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(349),
        S(153)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(116), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(34))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(279))
        if not tool then
            toolDumpBtn.Text = S(144)
            return
        end
        log(S(19) .. tool.Name .. S(273))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(29) .. attrName .. S(355) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(385)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(117),
        S(128)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(282), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(194)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(52),
        S(336)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(56), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(361)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(229),
        S(337)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(87), function()
        dumpGui()
        guiDumpBtn.Text = S(183)
    end)
    local settingsTab = window:AddTab(S(121), S(369))
    settingsTab:AddCard(S(6), S(317)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(382),
        S(106)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(323), S(233), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(119), S(316), S(203) }, 1, function(index, value)
        local code = ({ S(130), S(45), S(384) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(182)] = function()
return {
    [79389059854988] = S(65),
}
end
import(S(40))(import)
