-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"autoPlotInterval", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "\n", "Dump Visible GUI", "", "Container", ") ", "getconnections", "unlock", "UserInputService", "needs ", "core/debug_tab", "TextLabel", "0 = ไม่จำกัด", "([%d,%.]+)%s*(%a*)", "Auto Upgrade: รอบเช็ค", "[AutoChest] collected ", "games/my_anime_mine/get_current_plot", "x", "games/my_anime_mine/game_api", " (power ", "ClickDetector", "WalkSpeed (ปกติ 16)", "getscriptbytecode", "core/ui", "^%$", "ดีบัก", "Speed", "Search functions...", "lowest", "cheapest open node ", "?", "autoPlaceUnlock", "autoResearchCore", "jumpPower", "Nodes", "AutoPlace", "/", "Core buying is off", " (", "core/i18n", "string", "[Interaction] logger enabled - go click/interact with things now", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "ValueLabel", "[AutoResearch] Core ", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "core/logger", "s", "core/humanoid", "ไทย", "Auto Plot: กันเงินไว้", "ProximityPrompt", "ScreenGui", "/", "Open menu", "s - jumping the queue", "core/logger", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "decompile", "Auto Plot: ใช้โซนล่าสุดเสมอ", "Character", " items)", "autoResearchCoreLast", ": ", "63721c6", "Players", "PlayerGui", "upgrade", "[AutoPlace] picked up ", "autoPlaceInterval", "unreadable", "core/config", "core/debug_interaction_logger", "autoPlotKeepMoney", " (cost ", "autoClaimInterval", "Auto Claim: รอบเช็ค", "core/scheduler", " (prompt says '", "UICorner", "Auto Research: ซื้อสาย Core", "[%X] ", "Speed", "Frame", "", "MineEquipSlot", "core/logger", "RemoteFunction", "search_placeholder", "Frame", " for ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "Auto Research (Plot)", "interactionLogger", "🐛", ") didn't take: ", " ", " has been busy for ", "RemoteEvent", "UICorner", "[Scheduler] ", " ", "works from anywhere - dropping the teleport", "Auto Research: รอบเช็ค", "MineSelectZone", "GetCharacterMineSpeedUpgradeCost", "Frame", "games/my_anime_mine/auto_place", "Auto Upgrade: กันเงินไว้", "autoPlaceUnlockBudget", " possible holder(s) - not buying more", "research", "debug.getconstants", "nothing left to buy (", "autoUpgradeKeepMoney", "CoreUI", "table", "[AutoSell] MineSellItems ", "[AutoPlace] fireproximityprompt error: ", "Players", "[AutoPlace] holders: ", " from ", "no isfile/readfile available to verify the write", "TextLabel", "autoUpgradePriority", "[Uu]nlock", "waiting", "games/my_anime_mine/auto_mine", "s", "Auto Chest: รอบเช็ค", "AutoPlace", "core/dispatch", ")", "UIScale", "Speed", ")", "server didn't equip it", "Auto Place: เพดานราคาปลดช่อง", "enabled", "MineBuySkill", "ไล่ซื้อทุกกี่วินาที", "yes", "[AutoResearch] ", "  ", "[AntiAFK] enabled - watching Player.Idled", "string", " (", " ชิ้น", "getscripts", "core/logger", "[AutoClaim] ", "VirtualInputManager", "slot not in the hotbar", "[Decompile] === executor capabilities ===", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "string", "table", "Players", "TextButton", "UIListLayout", "Loot", "ReplicatedStorage", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "MineUpgradeCharacter", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "[GuiDump] PlayerGui not found", " (", ") -> ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", " stack(s)", "%.0f", "autoUpgradeMaxPerPass", "debug.getupvalues", "", "Anti-AFK", "ตั้งค่า", " (", ") ", "[AutoUpgrade] ", "[AutoPlot] couldn't switch to ", "Backpack", "PlayerScripts", " (Text=", "UserInputService", "core/logger", "Text", " not bought yet (", "UIPadding", "%$", "[GuiDump] ", "core/debug_decompile", "core/click", "core/debug_interaction_logger", "Badge", "may need us at the stand - trying that", "maxed", "AutoSell", "LocalScript", "core/parse_money", "games/my_anime_mine/auto_mine", "Frame", "VirtualUser", "TextLabel", "[AutoResearch] nothing open and affordable right now", "auto", " ", "BasePart", "ลอย (Float)", "[AutoPlace] placed ", "prompt disabled", "[AutoPlace] slot ", ",", "th", "th", "TextLabel", "TextButton", "TextLabel", "Dump", "GetCharacterUpgradeCost", "ClickDetector", "search_placeholder", "Content", "table", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", ") from ", ")", "needs us at the stand - keeping the teleport", "[Remotes] === total: ", "AutoChest", "CharSpeedMaxLevel", " (", "core/run_job", "autoUpgradeMutationTarget", "core/debug_dump_gui", " files", "Frame", ")", "'", "games/my_anime_mine/game_api", "'", "ValueLabel", "s", "M", "autoUpgradeLimitPickaxes", "Modules", "Auto Place: ปลดล็อกช่อง miner", "core/teleport", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "Players", "open_menu_hint", "Mining", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/humanoid", "auto", "(", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "enabled", "⚙", "This is not a supported place.", "ZoneRow", "every open node is maxed", "Plots", ")", "en", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ไล่อัปเกรด miner ทุกกี่วินาที", "M", "core/debug_tab", "[Scheduler] ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", " screen(s)) ===", "TextButton", "", "fill", "TextButton", "enabled", "core/logger", "Main", " script(s) to ", "games/registry", "TextBox", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "disabled", "Auto Sell: รอคิวได้นานสุด", " = ", "", "Auto Upgrade Unit", "number", ")", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "autoSellMaxWait", "debug.getproto", "[Decompile] ", "[AutoUpgrade] pickaxe #", "UserInputService", "games/my_anime_mine/game_api", ",", "menuScale", "[AutoUpgrade] ", "games/my_anime_mine/auto_plot", " #", "slot ", "ProximityPrompt", "Text", "autoResearchInterval", "", "", "[Decompile] === start ===", "[AutoPlace] ", "Players", "[AutoPlace] unlocked ", "PlotIndex", " | ", "core/parse_money", "TextLabel", " ", "games/my_anime_mine/auto_sell", "OwnerUserId", "Auto Upgrade: Pickaxe ที่จะซื้อ", "[Interaction] Button activated: ", "", " pickaxe ", "Workspace", "/config.json", "core/logger", "[AutoResearch] cheapest open node: ", "games/my_anime_mine/auto_upgrade", "')", " -> ", "autoResearchKeepMoney", "core/teleport", "games/my_anime_mine/auto_chest", "ReplicatedStorage", "ProximityPrompt", "sale didn't go through", "autoResearchMaxPerPass", "AutoPlot", "", "0 = ไม่จำกัด", "needs ", "x", "[AutoPlot] ", "[Remotes] ", "", "games/my_anime_mine/game_api", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", " ", "[Interaction] ProximityPrompt triggered: ", " (", "Auto Claim: เก็บสูงสุดต่อรอบ", " team power, you have ", "autoSell", "float", "Workspace", "ItemFrame", "games/my_anime_mine/get_plot_parts", "MineEquipPickaxe", ")", "core/run_job", "อังกฤษ", "TextLabel", "Mining", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "Plots", "PickaxesStand", "write reported success but the line isn't in ", "both Zone and Core buying are switched off", "games/my_anime_mine/get_current_plot", "languageIndex", "Hauler", "", "[AntiAFK] disabled", "none", "autoResearch", "core/logger", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "", "", "[GameAPI] SendEvent failed: ", "nothing to place or upgrade", "autoMineClicks", "[Decompile] === end ===", "CharMaxLevel", "Frame", " (x", "MineSpeed", ")", "/", ": ", "UIStroke", "autoPlotBuy", "autoSellMaxWait", " owned)", "Dump", "function", "enabled", "[AutoResearch] ", "Damage", "autoUpgradePickaxe", "autoChestMinItems", "[Decompile] decompile unavailable in this executor", "core/run_job", "s", "Auto Upgrade Plot (Zone)", "Workspace", "getloadedmodules", "'", "core/debug_dump_remotes", ".lua", "Run", "[Logger] failed to write ", "[Interaction] ClickDetector clicked: ", "core/get_me", "Frame", "OwnerUserId", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", " items (now ", "core/logger", "autoMine", "Players", "[AutoPlot] now mining ", "core/logger", "MinePower", "string", "[GameAPI] couldn't load the game's modules: ", " (chest holds ", "", "Search", "HttpService", "[GuiDump] === end (", "2026-09-21 12:32:06", "en-us", "table", "", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", " power ", "table", "My Anime Mine · Mining Hub", "  [attr] ", "disabled", "autoUpgrade", " '", "Frame", "[AutoChest] chest is empty", "", "cost", "Holders", "antiAfk", "Frame", "[AutoPlot] ", "[Config] couldn't write ", "function", "%$%s*([%d,%.]+)%s*(%a*)", " screens", "[AutoResearch] idle: ", "games/my_anime_mine/game_api", " ", "disabled", " doesn't exist", "Zone buying is off", "autoMineSpeed", "HumanoidRootPart", ")", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "HotbarFrame", "affordable - waiting for a turn", "GuiObject", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "core/logger", "lowest", "TextButton", "[Decompile] wrote ", ", you have ", "waiting", "RunService", "x", "games/my_anime_mine/auto_research", " ", "core/build_env", "Auto Upgrade: สุ่ม Mutation", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", " items", "autoClaim", "[AutoChest] MineTakeChest had no effect (", "ProximityPrompt", "Root", "TextLabel", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", ")", "works from anywhere - no teleport needed", ")", "core/logger", "games/my_anime_mine/main", "HumanoidRootPart", "core/logger", ": ", "empty", "ถูกสุดก่อน", "[AutoClaim] claimed ", "Badge", "no", "[AutoSell] enabled", "GetHolderSpeedLevel", "Auto Plot: ซื้อโซนถัดไป", "ClickDetector", "AutoPlot", "fly", "%.3g", "", "button has no size on screen", "string", "TextLabel", "Core", "table", "(nothing to do)", ")", " didn't go through (out of stock or too expensive)", " (", "core/build_env", "AutoResearch", "Auto Claim (Index)", "/", "ZoneBoardGui", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "MekPro.log", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/game_api", "walkSpeed", " research is ", "menuScale", "Workspace", "core/logger", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "LayerCollector", "table", "BodyVelocity", "autoPlot", "[AutoUpgrade] MineBuyPickaxe ", "All", "Auto Research: ซื้อสูงสุดต่อรอบ", "^([%d,]+)$", "Frame", "TextLabel", ") needs ", "TextLabel", "th", "[AutoChest] waiting: ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "[AutoUpgrade] ", "ZoneBoard", "core/get_place_id", " (", "Auto Place Miner", "/s", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "MineSpeed", "AutoClaim", "core/run_job", "ModuleScript", "Damage", "MineTakeChest", "write reported success but ", "games/my_anime_mine/game_api", "=", "ต่ำสุดก่อน", "games/my_anime_mine/get_best_miner", "[AutoMine] ", ", ", "/", "^x(%d+)$", "lowest", "MineWorld", "[AutoUpgrade] ", "open_menu_hint", "autoPlace", "AutoChest", "[Interaction] Key pressed: %s (gameProcessed=%s)", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", ")", "core/i18n", "core/config", "Decompile Client Scripts", "Players", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "s", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "leaderstats", "GetCharacterSpeedUpgradeCost", "core/teleport", "[AutoSell] disabled", "", "TextLabel", "[AutoPlace] ", "  ", "disabled", "core/anti_afk", "locked slot", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "MineRollMutation", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "core/scheduler", "", "autoSellProtectUnits", "[AutoChest] disabled", "autoChestInterval", " -> ", "GetHolderLevel", "Interaction Logger", " '", "BodyPosition", "แรงกระโดด", "_", "UIListLayout", " ", "SellStand", "core/logger", "games/my_anime_mine/auto_place", "[AutoResearch] ", "unreadable", "", "GuiButton", "ปุ่มเปิดเมนู", "[AutoPlace] unlocking ", "Content", " mutation x", "PlayerGui", "ScrollingFrame", "function", "Header", "[Config] couldn't read ", "[^%.]+", "[GuiDump] === visible PlayerGui tree ===", "ค้นหาฟังก์ชัน...", "s - carrying on without it", "games/my_anime_mine/game_api", " costs ", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "auto", "th", "[AutoPlace] ", "core/dispatch", "AutoMine", ") ", "getnilinstances", ": ", "table", "[Bb]uy", "[AutoPlace] couldn't pick up ", "UIListLayout", "autoUpgradePickaxeIndex", " failed)", "getcallbackvalue", "Auto Mine: คลิกต่อรอบ", "^M(%d+)$", "อ่านป้ายบน chest ทุกกี่วินาที", "เช็คโซนทุกกี่วินาที", " (money ", "table", ",", "research", "เดินทะลุกำแพงและพื้น", "AutoPlace", "core/debug_decompile", "[BestMiner] slot ", "table", " ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "%.3g", "core/logger", "ฟาร์ม", " (Text=", "CharMineSpeedMaxLevel", " (", " while selecting", "Chest", "GuiButton", "[Scheduler] turn: ", "Auto Chest: เกณฑ์เก็บ", "games/my_anime_mine/auto_claim", "Players", "autoPlotLatest", "TextLabel", "[AutoChest] can't read the chest yet", "core/run_job", "table", "autoResearchCheapestFirst", "=empty", "AutoUpgrade", "Auto Upgrade: แจก Pickaxe ให้ miner", "มุดแมพ (Noclip)", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Auto Sell", "Auto Place: เปลี่ยนตัวอ่อนออก", "-", "🏠", "ItemName", "core/get_place_id", "games/my_anime_mine/auto_chest", "autoUpgradePickaxeIndex", "", "[", "TextButton", "[AutoClaim] ", "Zones", "x", "MineWorld", "[AutoSell] ", "TextLabel", "games/my_anime_mine/auto_upgrade", "X", "en", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "[AutoResearch] ", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "[AutoUpgrade] bought pickaxe #", "games/my_anime_mine/auto_claim", "core/run_job", "TextLabel", "getrenv", "Shop Stand", " (", "unlock", "MineBuyPickaxe", "", "autoPlaceUpgradeWeak", "autoChest", "%$", "แพงสุดก่อน", "s", "core/get_leaderstat", "TextLabel", "Frame", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "costs ", "Players", " node(s) looked buyable but the server refused", "HumanoidRootPart", "[AutoPlace] nothing in the hotbar to place", "autoUpgradeRollMutation", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "ChestCapacity", "Hauler", "Auto Chest", ") ", "GetHolderMineSpeedLevel", "BasePart", "autoUpgradeMutationBudget", "Auto Research: กันเงินไว้", "Auto Research: ซื้อสาย Zone", "Workspace", "games/registry", "Auto Place: รอบเช็ค", "Pad", "[AutoPlace] drifted out of range of ", "ขนาดเมนู", " reward(s) looked claimable but none went through", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "[AutoPlace] couldn't hold anything - not pressing E", "Players", ": ", " refused - ", "en", "games/my_anime_mine/auto_sell", "core/anti_afk", " still empty after placing ", "บิน (Fly)", " pickaxe(s) for ", "string", "games/my_anime_mine/game_api", "core/get_place_id", "x", " for ", ", built ", "[AutoPlot] unlocked ", " (", " ===", "[AutoPlace] disabled", "Content", "action '", "Mutation: หยุดเมื่อได้ x", ", have ", "\n", "core/teleport", "/", "] job failed: ", "ตามเครื่อง", "Humanoid", "slot past the usable cap", "autoSellInterval", "[Interaction] logger disabled", ", base ", "AutoUpgrade", "CoreGui", "[AutoUpgrade] every stat on every miner is maxed", " x", "autoResearchZones", "BasePart", "table", " - upgrade the miners first", "MekPro/decompiled", "Auto Mine", " error: ", "MekPro", "core/run_job", " = ", "Players", "ย่อขยายเมนูทั้งชุด", "MekPro", "s", "core/debug_dump_remotes", "Empty miner slot", "AutoResearch", "M", "Workspace", "TextButton", "Players", ")", "sold ", "", "empty", ") on ", "", "games/my_anime_mine/game_api", "M", " (ActionText=", "neither appendfile nor writefile is available", "noclip", "games/my_anime_mine/main", "games/my_anime_mine/auto_plot", "disabled", "table", "languageIndex", "PlayerGui", "=locked(", "core/ui", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", " costs ", " (power ", "TextButton", "", "Auto Mine: ความเร็ว", "TabBar", "core/logger", "AutoPlace", ")", "Damage", "MineSellItems", "/", " (", "%.0f", " still locked after paying ", "[AutoUpgrade] ", "getsenv", "%.3g", " team power", " ", "[AutoChest] enabled (collects once >= ", " waited ", "ขายทุกกี่วินาที", "games/my_anime_mine/get_plot_parts", "[^%w]+", "TextButton", "[AutoPlace] upgrading ", ")", "MineSpeed", "table", "hookfunction", "games/my_anime_mine/auto_research", "Players", "auto", "[AntiAFK] idle detected, simulating input", "fill", "JumpPower (ปกติ 50)", "Auto Plot: รอบเช็ค", "", "autoUpgradeInterval", "dev", "enabled", "autoClaimMaxPerPass", "#", "Humanoid", " -> ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", ")", "core/parse_money", "core/logger", "%", "games/my_anime_mine/get_best_miner", "Mutation: เพดานราคาต่อครั้ง", "BasePart", "core/i18n", "ความเร็ววิ่ง", "[MekPro] settings file: ", " -> ", "GuiService", "autoUpgradeEquipPickaxe", "MekPro", "[MekPro] build ", "getgc", " ", ": ", "Crew", "Locked miner slot", " -> x", "core/logger", " -> ", "table", "[Scheduler] ", "List", "cost", "/", "core/logger", "Dump All Remotes", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "[AutoPlot] ", "TextLabel", "", "Damage", "AutoSell", "games/my_anime_mine/get_current_plot", "M", " remotes", "RunService", "MineGui", "[Decompile] PlayerScripts not found", "s", "core/debug_dump_gui", "core/run_job", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "MekPro", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", " (x", "Auto Sell: รอบขาย", "AutoClaim"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(768)] = function()
return function(import)
local log = import(S(48))
local Players = game:GetService(S(252))
local VirtualUser = game:GetService(S(204))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(147))
        connection = player.Idled:Connect(function()
            log(S(876))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(375))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(522)] = function()
return { Env = S(882), IsDev = true, Commit = S(67), BuiltAt = S(434) }
end
modules[S(194)] = function()
local VirtualInputManager = game:GetService(S(154))
local GuiService = game:GetService(S(900))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(513)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(74)] = function()
return function(import)
local HttpService = game:GetService(S(432))
local getPlaceId = import(S(774))
local log = import(S(673))
local ROOT_FOLDER = S(812)
local placeFolder = ROOT_FOLDER .. S(916) .. tostring(getPlaceId())
local filePath = placeFolder .. S(327)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(835) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(634) .. filePath .. S(66) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(454) .. filePath .. S(649) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(193)] = function()
return function(import)
local Players = game:GetService(S(820))
local log = import(S(498))
local CAPABILITIES = {
    S(61), S(24), S(151), S(904), S(409),
    S(8), S(656), S(858), S(723), S(115),
    S(175), S(295), S(872), S(648),
}
local OUT_FOLDER = S(804)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(635)) do
        if type(target) ~= S(871) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(156))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(296) .. name .. S(288) .. ((ok and type(value) == S(398)) and S(144) or S(504)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(404))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(184))
    if not playerScripts then
        log(S(930))
        return 0
    end
    pcall(makefolder, S(935))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(200)) or target:IsA(S(563)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(148) and #source > 0 then
                local name = target:GetFullName():gsub(S(866), S(616))
                if pcall(writefile, OUT_FOLDER .. S(525) .. name .. S(412), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(475) .. written .. S(282) .. OUT_FOLDER
        .. S(556) .. failed .. S(655))
    return written
end
local function run()
    log(S(311))
    reportCapabilities()
    local count = dumpAll()
    log(S(385))
    return count
end
return run
end
end
modules[S(932)] = function()
return function(import)
local Players = game:GetService(S(763))
local log = import(S(58))
local function dumpVisible(root, indent)
    indent = indent or S(5)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(470)) and child.Visible then
            local line = indent .. child.ClassName .. S(445) .. child.Name .. S(410)
            if child:IsA(S(318)) or child:IsA(S(819)) then
                line = line .. S(675) .. tostring(child.Text) .. S(494)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(442) .. attrName .. S(809) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(146))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(837))
    if not playerGui then
        log(S(168))
        return 0
    end
    local screens = 0
    log(S(636))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(538)) and screen.Enabled and screen.Name ~= S(902) then
            screens = screens + 1
            log(S(192) .. screen.ClassName .. S(613) .. screen.Name .. S(243))
            dumpVisible(screen, S(598))
        end
    end
    log(S(433) .. screens .. S(274))
    return screens
end
return dumpGui
end
end
modules[S(814)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(164))
local log = import(S(328))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(101)) or descendant:IsA(S(90)) then
            count = count + 1
            log(S(346) .. descendant.ClassName .. S(764) .. descendant:GetFullName())
        end
    end
    log(S(231) .. count .. S(780))
    return count
end
return dumpRemotes
end
end
modules[S(75)] = function()
return function(import)
local Players = game:GetService(S(313))
local Workspace = game:GetService(S(326))
local UserInputService = game:GetService(S(298))
local log = import(S(917))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(351) .. describe(prompt)
            .. S(829) .. prompt.ActionText .. S(466))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(415) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(323) .. describe(button)
            .. S(185) .. tostring(button.Text) .. S(869))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(306)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(508)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(680)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(794))
        return
    end
    connections = {}
    log(S(43))
    local playerGui = Players.LocalPlayer:WaitForChild(S(630))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(53)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(22)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(625)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(581),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(271)] = function()
return function(import)
local interactionLogger = import(S(195))(import)
local dumpRemotes = import(S(411))(import)
local dumpGui = import(S(237))(import)
local decompileScripts = import(S(667))(import)
local humanoid = import(S(50))
local function build(window, persist)
    local tab = window:AddTab(S(27), S(97))
    persist.Toggle(
        tab:AddCard(
            S(612),
            S(582)
        ),
        S(96),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(918),
        S(47)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(220), function()
        remoteBtn.Text = dumpRemotes() .. S(927)
    end)
    local guiCard = tab:AddCard(
        S(4),
        S(165)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(397), function()
        guiBtn.Text = dumpGui() .. S(457)
    end)
    local decompileCard = tab:AddCard(
        S(586),
        S(537)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(413), function()
        decompileBtn.Text = decompileScripts() .. S(238)
    end)
    humanoid.Start()
    persist.Slider(tab, S(897), S(23),
        8, 200, S(532), 16, humanoid.SetWalkSpeed, S(728))
    persist.Slider(tab, S(615), S(878),
        30, 300, S(35), 50, humanoid.SetJumpPower, S(437))
    persist.Toggle(
        tab:AddCard(S(770), S(737)),
        S(510), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(694), S(665)),
        S(831), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(210), S(641)),
        S(356), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(645)] = function()
return function(import)
    local getPlaceId = import(S(701))
    local registry = import(S(755))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(262))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(734)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(591))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(416)] = function()
local Players = game:GetService(S(810))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(791))
    local backpack = player:WaitForChild(S(183))
    return player, humanoid, backpack
end
return getMe
end
modules[S(555)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(256)] = function()
local Players = game:GetService(S(122))
local RunService = game:GetService(S(478))
local UserInputService = game:GetService(S(186))
local Workspace = game:GetService(S(818))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(886))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(465))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(540))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(895)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(209)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(614))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(896)] = function()
local Players = game:GetService(S(684))
local STRINGS = {
    en = {
        search_placeholder = S(29),
        open_menu_hint = S(56),
    },
    th = {
        search_placeholder = S(637),
        open_menu_hint = S(626),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(435)
    if locale:sub(1, 2):lower() == S(550) then
        return S(216)
    end
    return S(766)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(257) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(421)] = function()
local LOG_FILE = S(528)
local function log(message)
    local line = os.date(S(84)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(512))
            end
            appendfile(LOG_FILE, line .. S(3))
        elseif writefile then
            local existing = S(430)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(786))
        else
            error(S(830))
        end
        if not (isfile and readfile) then
            error(S(125))
        end
        if not isfile(LOG_FILE) then
            error(S(566) .. LOG_FILE .. S(462))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(369) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(414) .. LOG_FILE .. S(906) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(317)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(427) then
        return nil
    end
    local number, suffix = text:match(S(456))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(214), S(87))))
    if not value then
        return nil
    end
    if suffix ~= S(823) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(688)] = function()
return function(import)
local log = import(S(425))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(705) .. name .. S(789) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(80)] = function()
return function(import)
local RunService = game:GetService(S(928))
local log = import(S(89))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(272) .. task_.name .. S(806) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(913) .. task_.name .. S(100)
                    .. string.format(S(173), now - task_.busySince)
                    .. S(638))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(103) .. starved.name .. S(863)
            .. string.format(S(855), now - starved.waitingSince)
            .. S(57))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(681) .. (currentTask and currentTask.name or S(518)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(787)] = function()
local Players = game:GetService(S(160))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(497))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(839)] = function()
return function(import)
local i18n = import(S(584))
local UserInputService = game:GetService(S(10))
local THEME = {
    BrandName = S(807),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(82))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(102))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(190))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(218))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(606)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(393))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(490))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(32)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(205))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(595)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(631))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(162))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(126))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(446))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(549))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(547))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(219))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(244))
            if not valueLbl then
                valueLbl = Instance.new(S(364))
                valueLbl.Name = S(45)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(196))
            if not badge then
                badge = Instance.new(S(13))
                badge.Name = S(503)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(161))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(203))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(417))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(706))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(704)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(892)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(387))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(452))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(474))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(276)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(92))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(653))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(278))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(307)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(54))
    screenGui.Name = S(118)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(797))
    local main = Instance.new(S(239))
    main.Name = S(281)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(136))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(736))
    tabBar.Name = S(847)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(617))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(546))
    header.Name = S(633)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(867))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(921))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(217))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(448)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(275))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(714)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(844))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(698)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(284))
    searchBox.Name = S(431)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(223))
    searchBox.Text = S(381)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(109))
    content.Name = S(224)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(86))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(735))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(253))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(712))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(188)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(374) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(319) .. (item.Subtitle or S(624))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(91))
        footerLabel.Text = i18n.T(S(578))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(335)] = function()
return function(import)
local gameApi = import(S(299))(import)
local log = import(S(910))
local runGuarded = import(S(235))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(862) .. minItemsToCollect .. S(64))
    else
        log(S(608))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(623) then
            log(S(687))
            lastReported = S(73)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(500) then
            log(S(447))
            lastReported = S(824)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(477) then
            local capacity = gameApi.ChestCapacity()
            log(S(551) .. count .. S(573) .. want .. S(485)
                .. (capacity and (S(429) .. capacity .. S(519)) or S(341)))
            lastReported = S(129)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(580), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(17) .. count .. S(420) .. after .. S(138))
        else
            log(S(487) .. count
                .. S(332) .. tostring(after) .. S(821))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(683)] = function()
return function(import)
local gameApi = import(S(20))(import)
local log = import(S(495))
local runGuarded = import(S(362))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(707) .. (enabled and S(141) or S(443)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(502) .. reward.category .. S(460)
                .. tostring(reward.tierKey) .. S(304) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(153) .. #pending .. S(760))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(561), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(202)] = function()
return function(import)
local getCurrentPlot = import(S(371))
local getPlotParts = import(S(359))
local log = import(S(536))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(222), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(571) .. (enabled and S(399) or S(286)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(621)] = function()
return function(import)
local Players = game:GetService(S(423))
local getCurrentPlot = import(S(925))
local getPlotParts = import(S(865))
local getMinerSlots = import(S(570))(import)
local teleport = import(S(593))
local parseMoney = import(S(201))
local gameApi = import(S(242))(import)
local log = import(S(891))
local runGuarded = import(S(721))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(815)
local LOCKED_SLOT_TEXT = S(908)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(757))
    return pad and pad:FindFirstChildWhichIsA(S(337), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(722)) then
            local digits = descendant.Text:match(S(545))
            if digits then
                return tonumber((digits:gsub(S(663), S(845))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(686)) and descendant.Text:match(S(26)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(305) .. holder.Name .. S(189)
            .. gameApi.MinerSlots() .. S(788) .. gameApi.MaxMinerSlots() .. S(396)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(601)
    end
    if not prompt.Enabled then
        return true, S(212)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(738) .. price
    end
    local action = prompt.ActionText or S(826)
    if action:match(S(128)) or action:match(S(651)) or action:match(S(731)) then
        return true, S(783) .. action .. S(241)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(741))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(121) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(838) .. why .. S(390))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(691))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(568) .. prompt.ObjectText
                    .. S(258) .. shown .. S(795) .. math.floor(base) .. S(850))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(316))
    if line ~= lastSurvey then
        log(S(123) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(277), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(70), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(726), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(644) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(781))
    else
        lastAttempt = 0
        log(S(226))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(529),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(155)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(792)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(139)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(742))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(758) .. holder.Name .. S(678))
            return false
        end
        log(S(213) .. candidate.slot .. S(725) .. candidate.name
            .. S(98) .. tostring(why))
    end
    if not miner then
        log(S(762))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(312) .. holder.Name .. S(769) .. miner.name)
        return false
    end
    log(S(211) .. miner.name .. S(21) .. miner.power
        .. S(825) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(652) .. entry.name .. S(124) .. entry.holder.Name
            .. S(81) .. tostring(entry.prompt.ActionText) .. S(331))
        return false
    end
    log(S(71) .. entry.name .. S(843) .. entry.power
        .. S(228) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(597) .. pad.holder.Name .. S(856)
                .. tostring(pad.priceText))
        else
            log(S(314) .. pad.holder.Name
                .. S(776) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(383))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(877) then
        runGuarded(S(666), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(9) then
        log(S(627) .. target.holder.Name
            .. S(93) .. tostring(target.priceText))
        runGuarded(S(37), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(868) .. target.holder.Name .. S(499) .. target.name
        .. S(521) .. target.power .. S(170) .. best.name .. S(779) .. best.power .. S(361))
    runGuarded(S(849), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(833)] = function()
return function(import)
local gameApi = import(S(348))(import)
local parseMoney = import(S(890))
local log = import(S(848))
local runGuarded = import(S(405))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(453) .. (enabled and S(279) or S(834)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(191)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(424) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(677) .. deepestKey .. S(583))
        return true
    end
    log(S(182) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(345) .. gameApi.ZoneDisplayName(zoneKey) .. S(842)
                .. math.floor(price) .. S(476) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(778) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(40) .. zoneKey .. S(266))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(664) then
            reportedLocked = S(114)
            log(S(920) .. zoneKey .. S(765)
                .. gameApi.ZoneDisplayName(currentKey) .. S(533)
                .. done .. S(391) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(509), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(873)] = function()
return function(import)
local gameApi = import(S(639))(import)
local log = import(S(472))
local runGuarded = import(S(562))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(622) .. (enabled and S(883) or S(461)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(717) .. node.zoneKey .. S(104) .. node.id
                    .. S(149) .. node.family .. S(7) .. node.level .. S(899) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(46) .. node.id
                    .. S(169) .. node.family .. S(748) .. node.level .. S(887) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(400) .. blocked
                .. S(740))
        elseif powerBlocked then
            log(S(145) .. powerBlocked.zoneKey .. S(208) .. powerBlocked.id
                .. S(854) .. powerBlocked.family .. S(548)
                .. math.floor(powerBlocked.powerReq) .. S(354)
                .. math.floor(powerBlocked.power)
                .. S(803))
        else
            log(S(206)
                .. S(661) .. math.floor(gameApi.GetMoney()) .. S(240))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(329) .. nearest.zoneKey
                    .. S(350) .. nearest.id .. S(352) .. nearest.family .. S(647)
                    .. nearest.level .. S(853) .. nearest.max
                    .. S(640) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(370)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(516),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(463))
        end
        if not buyCore then
            table.insert(parts, S(39))
        end
        if #parts == 0 then
            return S(264)
        end
        return S(116) .. table.concat(parts, S(572)) .. S(292)
    end
    local why
    if nearest.cost > budget then
        why = S(11) .. string.format(S(672), nearest.cost)
            .. S(785) .. string.format(S(511), money)
    elseif nearest.powerOk == false then
        why = S(343) .. string.format(S(859), nearest.powerReq) .. S(860)
    else
        why = S(469)
    end
    return S(31) .. nearest.zoneKey .. S(670) .. tostring(nearest.id)
        .. S(179) .. tostring(nearest.family) .. S(180) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(458) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(816), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(320)] = function()
return function(import)
local Workspace = game:GetService(S(535))
local gameApi = import(S(567))(import)
local teleport = import(S(250))
local log = import(S(152))
local runGuarded = import(S(808))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(594))
    else
        lastAttempt = 0
        log(S(505))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(619))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(488), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(120)
                .. (sold and S(105)
                         or S(230)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(711) .. (sold and (S(822) .. #keys .. S(172)) or S(338)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(199), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(330)] = function()
return function(import)
local Workspace = game:GetService(S(408))
local gameApi = import(S(827))(import)
local teleport = import(S(334))
local log = import(S(280))
local runGuarded = import(S(933))(import)
local STATS = { S(401), S(85), S(389) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(473)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(577) .. (enabled and S(260) or S(599)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(119) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(30) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(449) or S(198)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(553) .. holderKey .. S(618) .. choice.stat .. S(481)
                        .. choice.info.level .. S(911) .. after.level
                        .. S(38) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(915) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(368))
    local shop = stand and stand:FindFirstChild(S(724))
    if not shop then
        return nil
    end
    if shop:IsA(S(801)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(750), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(542)
                .. (bought and S(493)
                           or S(197)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(59))
        end
    end
    if bought then
        log(S(719) .. pickaxe.index .. S(905) .. pickaxe.name
            .. S(937) .. pickaxe.multiplier .. S(889))
        return true
    end
    log(S(297) .. pickaxe.index .. S(99) .. pickaxe.name
        .. S(520))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(857) .. holderKey .. S(325)
                .. tostring(entry and entry.Pickaxe or S(376)) .. S(610) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(302) .. holderKey .. S(629) .. variant
                .. S(909) .. after .. S(77) .. cost .. S(492))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(181) .. gameApi.OwnedPickaxeCount()
                    .. S(771) .. gameApi.MaxMinerSlots()
                    .. S(113))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(919))
        elseif maxed > 0 then
            log(S(798))
        else
            log(S(559))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(802) and entry.Pickaxe and entry.Pickaxe ~= S(176) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(692), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(531)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(336))
local Workspace = game:GetService(S(357))
local log = import(S(187))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(248), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(428) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(689))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(650) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(373) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(912) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(159) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(746) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(658))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(382) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(565))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(543), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(436) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(539) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(63) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(852), keys)
end
function api.EquipSlot(slot)
    return send(S(88), slot == S(347) and S(380) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(727), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(360), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(603), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(142), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(662)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(587)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(426))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(455) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(440) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(107), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(576))
    plots = plots and plots:FindFirstChild(S(367))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(68)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(418)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(554))
    local gui = board and board:FindFirstChild(S(526))
    local root = gui and gui:FindFirstChild(S(489))
    local list = root and root:FindFirstChild(S(914))
    local row = list and list:FindFirstChild(S(263) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(596)) and descendant.Text ~= S(324) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(632) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(166), characterKey, stat == S(254) and S(560) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(611), max = S(386), cost = S(221) },
    Speed = { level = S(506), max = S(233), cost = S(592) },
    MineSpeed = { level = S(749), max = S(676), cost = S(108) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(772) or charName == S(309) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(517) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(291) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(42) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(669) and type(holder.CharName) == S(158) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(225) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(514) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(893)] = function()
return function(import)
local Players = game:GetService(S(739))
local gameApi = import(S(773))(import)
local log = import(S(378))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(15))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(300), S(880)))) or 0
    if suffix ~= S(289) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(69))
    local mineGui = playerGui and playerGui:FindFirstChild(S(929))
    local hotbar = mineGui and mineGui:FindFirstChild(S(468))
    local content = hotbar and hotbar:FindFirstChild(S(628))
    return content and content:FindFirstChild(S(6))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(515)) then
            local count = descendant.Text:match(S(574))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(358))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(782))
    local nameLabel = content and content:FindFirstChild(S(700))
    local damageLabel = content and content:FindFirstChild(S(564))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(922) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(55) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(668) .. slot .. S(392) .. info.name
                        .. S(439) .. info.power .. S(799) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(18)] = function()
local Players = game:GetService(S(874))
local Workspace = game:GetService(S(754))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(710))
    local plots = mineWorld and mineWorld:FindFirstChild(S(265))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(321)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(530)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(450)),
        Nodes = plot:FindFirstChild(S(36)),
        Chest = plot:FindFirstChild(S(679)),
        Zones = plot:FindFirstChild(S(708)),
        Crew = plot:FindFirstChild(S(907)),
        Loot = plot:FindFirstChild(S(163)),
        PlotIndex = plot:GetAttribute(S(315)),
        ChestCapacity = plot:GetAttribute(S(745)),
    }
end
return getPlotParts
end
modules[S(496)] = function()
return function(import)
    local createWindow = import(S(25))(import)
    local i18n = import(S(41))
    local antiAfk = import(S(600))(import)
    local buildEnv = import(S(482))
    local scheduler = import(S(605))(import)
    local config = import(S(585))(import)
    local log = import(S(620))
    log(S(903) .. buildEnv.Commit .. S(234) .. buildEnv.Env
        .. S(777) .. buildEnv.BuiltAt .. S(229))
    local window = createWindow({
        Subtitle = S(441),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(130))(import)
    local autoPlace = import(S(110))(import)
    local autoChest = import(S(702))(import)
    local autoSell = import(S(767))(import)
    local autoUpgrade = import(S(713))(import)
    local autoResearch = import(S(480))(import)
    local autoClaim = import(S(720))(import)
    local autoPlot = import(S(303))(import)
    local gameApi = import(S(459))(import)
    scheduler.RegisterStandalone(S(646), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(232), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(133), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(796), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(523), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(939), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(340), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(294), 20)
    scheduler.RegisterExclusive(S(924), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(674), S(699))
    persistentToggle(farmTab:AddCard(
        S(805),
        S(157)
    ), S(422), autoMine.SetEnabled)
    persistentSlider(farmTab, S(846), S(841),
        1, 30, S(464), 5, autoMine.SetCyclesPerSecond, S(558))
    persistentSlider(farmTab, S(657), S(259),
        1, 10, S(384), 1, autoMine.SetClicksPerCycle, S(479))
    persistentToggle(farmTab:AddCard(
        S(557),
        S(379)
    ), S(579), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(756), S(94),
        1, 60, S(72), 3, autoPlace.SetCheckInterval, S(406))
    persistentToggle(
        farmTab:AddCard(S(697),
            S(484)),
        S(729), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(249),
            S(716)),
        S(33), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(140), S(342),
        0, 500, S(112), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(270))
    persistentToggle(farmTab:AddCard(
        S(747),
        S(761)
    ), S(730), autoChest.SetEnabled)
    persistentSlider(farmTab, S(682), S(552),
        50, 3000, S(403), 100, autoChest.SetMinItems, S(150))
    persistentSlider(farmTab, S(132), S(659),
        1, 60, S(609), 3, autoChest.SetCheckInterval, S(49))
    persistentToggle(farmTab:AddCard(
        S(696),
        S(671)
    ), S(355), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(60),
            S(718)),
        S(607), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(938), S(864),
        5, 300, S(793), 20, autoSell.SetCheckInterval, S(131))
    farmTab:AddSlider(S(287),
        S(227),
        5, 120, sellMaxWait, function(value)
            config.Set(S(395), value)
        end, S(733))
    persistentToggle(farmTab:AddCard(
        S(290),
        S(366)
    ), S(444), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(16), S(269),
        5, 300, S(881), 20, autoUpgrade.SetInterval, S(813))
    persistentSlider(farmTab, S(419), S(888),
        1, 20, S(174), 5, autoUpgrade.SetMaxPerPass, S(775))
    persistentSegmented(
        farmTab,
        { S(569), S(923), S(137), S(365) },
        { S(575), S(851), S(28), S(870) },
        S(127), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(111), S(471),
        0, 500, S(117), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(926))
    persistentToggle(
        farmTab:AddCard(S(695),
            S(167)),
        S(402), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(388) .. pickaxe.multiplier .. S(135)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(349)
            end
            return S(885) .. index .. S(861) .. (pickaxeLabels[index] or S(744))
        end
        local savedPick = config.Get(S(703), 0)
        pickaxeCard = farmTab:AddSlider(
            S(322),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(654), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(310)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(840),
            S(527)),
        S(247), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(693),
            S(293)),
        S(901), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(483),
            S(438)),
        S(743), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(784), S(590),
        1, 20, S(236), 2, autoUpgrade.SetMutationTarget, S(709))
    persistentSlider(farmTab, S(894), S(14),
        0, 500, S(751), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(246))
    persistentToggle(farmTab:AddCard(
        S(95),
        S(171)
    ), S(377), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(106), S(143),
        10, 600, S(308), 30, autoResearch.SetCheckInterval, S(245))
    persistentSlider(farmTab, S(544), S(268),
        1, 20, S(339), 5, autoResearch.SetMaxPerPass, S(344))
    persistentSlider(farmTab, S(752), S(2),
        0, 500, S(333), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(817))
    persistentToggle(
        farmTab:AddCard(S(753),
            S(255)),
        S(800), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(83),
            S(936)),
        S(34), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(467),
            S(251)),
        S(65), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(501), S(732) },
        { true, false },
        S(690), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(524),
        S(285)
    ), S(486), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(79), S(588),
        5, 300, S(78), 30, autoClaim.SetCheckInterval, S(589))
    persistentSlider(farmTab, S(353), S(44),
        1, 30, S(884), 10, autoClaim.SetMaxPerPass, S(19))
    persistentToggle(farmTab:AddCard(
        S(407),
        S(602)
    ), S(541), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(879), S(660),
        5, 300, S(1), 30, autoPlot.SetCheckInterval, S(931))
    persistentToggle(
        farmTab:AddCard(S(507),
            S(491)),
        S(394), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(62),
            S(934)),
        S(685), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(52), S(273),
        0, 500, S(76), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(828))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(12))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(178), S(261))
    persistentToggle(
        settingsTab:AddCard(S(177), S(604)),
        S(451),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(301), 100)
    settingsTab:AddSlider(S(759), S(811), 50, 150, savedScale, function(value)
        config.Set(S(534), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(372), 1)
    settingsTab:AddSegmented({ S(790), S(363), S(51) }, savedLanguage, function(index, value)
        local code = ({ S(875), S(267), S(215) })[index]
        config.Set(S(836), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(207), S(715), S(643) })[savedLanguage] or S(642))
    log(S(898) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(283)] = function()
return {
    [79389059854988] = S(832),
}
end
import(S(134))(import)
