-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Dump", "  [attr] ", "ไทย", "Players", "[Bb]uy", "games/my_anime_mine/auto_upgrade", "search_placeholder", "string", "TextButton", "MekPro", "TextLabel", "Players", "MekPro", "Auto Chest", "[^%w]+", "-", "games/my_anime_mine/auto_claim", "core/logger", "^M(%d+)$", "Auto Place Miner", "sold ", " items (now ", "enabled", "BodyPosition", "Damage", "none", ".lua", "ScrollingFrame", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "Plots", "games/my_anime_mine/auto_mine", "VirtualUser", " (power ", "Backpack", "Players", "Players", "GetCharacterSpeedUpgradeCost", " (", "UIListLayout", "en-us", " remotes", "core/debug_dump_gui", "/", ")", "table", "ตามเครื่อง", "", "Players", "Auto Research: กันเงินไว้", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "yes", "autoUpgradePickaxeIndex", "jumpPower", "GetCharacterMineSpeedUpgradeCost", ") on ", "HumanoidRootPart", "GetHolderLevel", "Container", "x", "CharMineSpeedMaxLevel", "[AntiAFK] enabled - watching Player.Idled", " while selecting", "write reported success but the line isn't in ", "no", "Run", "'", " ", " -> ", "Workspace", "PickaxesStand", "Players", " ", " didn't go through (out of stock or too expensive)", "CoreGui", "Players", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "UIListLayout", "getsenv", "[AutoPlace] disabled", "[Decompile] wrote ", "cost", ") ", "Humanoid", "getloadedmodules", " screen(s)) ===", "[Decompile] === end ===", "Auto Upgrade: Pickaxe ที่จะซื้อ", "games/my_anime_mine/auto_research", "[AutoUpgrade] ", "Players", "[AutoPlace] holders: ", "[AutoPlace] couldn't pick up ", "table", "auto", "lowest", "M", "GuiButton", "autoResearchInterval", "[GameAPI] couldn't load the game's modules: ", "core/config", "may need us at the stand - trying that", "TabBar", "string", "number", " -> ", "Frame", "MineSpeed", "/", "games/my_anime_mine/auto_upgrade", "core/dispatch", " still empty after placing ", "getcallbackvalue", "ProximityPrompt", "[AutoPlace] drifted out of range of ", "games/my_anime_mine/get_plot_parts", "MineEquipPickaxe", "autoPlace", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Nodes", "Auto Upgrade: แจก Pickaxe ให้ miner", "th", "", "autoUpgradeRollMutation", " not bought yet (", ")", "%", "core/dispatch", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "core/parse_money", "core/i18n", "🐛", " pickaxe ", "[AutoPlace] fireproximityprompt error: ", "slot ", "debug.getconstants", "autoPlaceInterval", "CharSpeedMaxLevel", " ", "", "upgrade", "HumanoidRootPart", "autoSellMaxWait", "?", "core/humanoid", " -> ", "table", "AutoSell", "autoMineClicks", "[^%d%.]", "ClickDetector", "core/click", "ModuleScript", "TextLabel", "core/logger", "table", "UICorner", " mutation x", "debug.getproto", "BasePart", "Hauler", "games/my_anime_mine/game_api", "Search functions...", "AutoChest", "table", "core/ui", "[Interaction] logger disabled", "Damage", "/config.json", "games/my_anime_mine/get_current_plot", "", "Auto Place: เปลี่ยนตัวอ่อนออก", "Dump All Remotes", "core/logger", "TextButton", "[AutoUpgrade] pickaxe #", "core/debug_interaction_logger", "Decompile Client Scripts", "UICorner", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/debug_tab", "getscriptbytecode", "MineUpgradeCharacter", "ItemName", ",", "All", " -> x", "core/humanoid", "TextLabel", "autoUpgradeInterval", "[Interaction] logger enabled - go click/interact with things now", " ชิ้น", " ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "[AutoChest] waiting: ", " | ", "MinePower", "[Config] couldn't write ", "Content", "games/my_anime_mine/auto_chest", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "[AutoSell] disabled", "=empty", "[GuiDump] PlayerGui not found", "[AutoMine] ", "Auto Research: ซื้อสูงสุดต่อรอบ", "/", "[Decompile] === start ===", "^x(%d+)$", "Workspace", "autoResearchCheapestFirst", "games/my_anime_mine/auto_research", "Auto Mine: ความเร็ว", "[Interaction] ProximityPrompt triggered: ", "", "games/my_anime_mine/game_api", "menuScale", "Locked miner slot", " doesn't exist", "(nothing to do)", "ต่ำสุดก่อน", "Auto Mine", "ขายทุกกี่วินาที", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "Mutation: เพดานราคาต่อครั้ง", "=", "ตั้งค่า", ": ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "M", "Auto Chest: เกณฑ์เก็บ", "open_menu_hint", " '", "core/logger", "OwnerUserId", "TextLabel", "VirtualInputManager", "s", "Players", "[AutoSell] enabled", "core/config", "core/ui", "MekPro", "", "LayerCollector", "My Anime Mine · Mining Hub", " node(s) looked buyable but the server refused", "Shop Stand", "MineSpeed", "autoResearchKeepMoney", "th", "MineTakeChest", ": ", "Frame", "MineSpeed", "\n", " error: ", " (Text=", "[AutoResearch] Core ", "autoChestInterval", "GuiButton", "ValueLabel", "=locked(", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ": ", "autoSellMaxWait", "ScreenGui", "ProximityPrompt", "core/logger", "fly", " #", "[^%.]+", "core/build_env", "AutoClaim", "%.0f", "[%X] ", "table", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "UIStroke", "Frame", "autoUpgrade", "TextLabel", "Speed", " (x", "GuiService", "Auto Claim: รอบเช็ค", " (chest holds ", " = ", "noclip", "Frame", "[Remotes] === total: ", "s", "games/my_anime_mine/game_api", "BasePart", "sale didn't go through", " (prompt says '", "Frame", " items)", "Modules", "#", "x", "", "ดีบัก", "RunService", "ความเร็ววิ่ง", "Humanoid", "", "getgc", "TextLabel", "", "table", "games/my_anime_mine/auto_chest", "[AutoPlace] nothing in the hotbar to place", "  ", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "RemoteFunction", "core/get_place_id", "Text", "core/debug_dump_remotes", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "core/build_env", "ลอย (Float)", "Auto Research (Plot)", "TextButton", "games/my_anime_mine/game_api", "WalkSpeed (ปกติ 16)", "disabled", "core/get_place_id", "[AutoChest] collected ", "TextButton", "Players", "[Interaction] Button activated: ", "ReplicatedStorage", "/", "core/logger", "games/my_anime_mine/get_plot_parts", "ค้นหาฟังก์ชัน...", "ปุ่มเปิดเมนู", "s - jumping the queue", "Frame", "TextLabel", "เดินทะลุกำแพงและพื้น", "prompt disabled", "Auto Research: ซื้อสาย Core", " ", "s", "string", "M", "TextLabel", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "แรงกระโดด", "MekPro", "core/debug_interaction_logger", "AutoPlace", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "waiting", "en", "disabled", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "autoChestMinItems", "/", "')", "TextButton", "unreadable", "games/my_anime_mine/get_plot_parts", "[AutoPlace] picked up ", " (", "PlotIndex", " x", "Loot", " screens", "[Logger] failed to write ", "Auto Mine: คลิกต่อรอบ", "autoUpgradeKeepMoney", "[AutoPlace] ", "[AutoUpgrade] MineBuyPickaxe ", "core/scheduler", " stack(s)", "TextBox", " ", "core/logger", "[AutoChest] MineTakeChest had no effect (", "autoUpgradeMutationBudget", " (x", "MekPro.log", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "GuiObject", "Auto Chest: รอบเช็ค", ")", "TextLabel", "Dump", "Search", ": ", "Empty miner slot", "languageIndex", "", "games/my_anime_mine/game_api", "Frame", "Anti-AFK", "\n", "s", "[GuiDump] === visible PlayerGui tree ===", "core/debug_decompile", " power ", "X", "[Config] couldn't read ", "%$%s*([%d,%.]+)%s*(%a*)", "[GameAPI] SendEvent failed: ", "core/logger", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "TextButton", ": ", "", "x", "Pad", "menuScale", "action '", "Players", "[AutoSell] MineSellItems ", "debug.getupvalues", "Workspace", "SellStand", "enabled", "^([%d,]+)$", " failed)", "games/my_anime_mine/get_current_plot", "autoUpgradeEquipPickaxe", " (", "autoUpgradeMaxPerPass", " owned)", "Auto Research: ซื้อสาย Zone", ")", "[AutoPlace] placed ", " script(s) to ", "MineGui", "button has no size on screen", "s", "core/logger", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "antiAfk", "[MekPro] settings file: ", "MineBuyPickaxe", "TextLabel", "auto", "UIScale", "autoResearchCore", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "ItemFrame", "[AntiAFK] idle detected, simulating input", "CharMaxLevel", "[AutoResearch] ", "Content", "Chest", "", "autoResearchMaxPerPass", "", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Frame", " '", "ReplicatedStorage", "[AutoChest] can't read the chest yet", " (power ", "Workspace", "walkSpeed", "BasePart", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "Open menu", " -> ", ")", "0 = ไม่จำกัด", "core/get_me", "", "autoUpgradeMutationTarget", "games/my_anime_mine/get_best_miner", "TextLabel", "[Remotes] ", "core/debug_decompile", "Place", ") didn't take, trying the next one", "function", "games/my_anime_mine/auto_sell", "MineSellItems", "needs us at the stand - keeping the teleport", "TextButton", "Crew", "[AutoChest] enabled (collects once >= ", "Workspace", "core/logger", "Auto Sell", "ValueLabel", "MineEquipSlot", "AutoUpgrade", "games/my_anime_mine/get_best_miner", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "empty", "getrenv", "UIPadding", " from ", "core/scheduler", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "table", "x", "TextLabel", "[Decompile] PlayerScripts not found", "getnilinstances", "ไล่ซื้อทุกกี่วินาที", "MekPro/decompiled", "Players", ")", "float", ")", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Speed", "", "Badge", "[Interaction] ClickDetector clicked: ", "ฟาร์ม", "[AutoUpgrade] ", "languageIndex", "games/registry", "nothing to place or upgrade", "Holders", "JumpPower (ปกติ 50)", "waiting", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " (ActionText=", "x", "autoMine", "search_placeholder", "ไล่อัปเกรด miner ทุกกี่วินาที", "Text", "leaderstats", "autoClaimMaxPerPass", "function", "core/debug_tab", " (", "[Scheduler] turn: ", "core/logger", "costs ", "core/logger", "UIListLayout", "PlayerGui", "Damage", ") -> ", "autoSell", "getscripts", "", "autoUpgradePickaxe", "core/i18n", "works from anywhere - no teleport needed", "TextButton", "table", "[AutoPlace] upgrading ", "Frame", "[Interaction] Key pressed: %s (gameProcessed=%s)", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "lowest", "ขนาดเมนู", "[AutoPlace] slot ", "[AutoClaim] ", "RemoteEvent", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "auto", "MineWorld", "Auto Upgrade Unit", "autoResearch", "", "Frame", "TextButton", "core/get_leaderstat", "[AutoUpgrade] ", "[BestMiner] slot ", "games/my_anime_mine/game_api", " (", "[AutoResearch] ", "unreadable", "Auto Place: รอบเช็ค", "Auto Claim (Index)", "ClickDetector", "autoSellInterval", "PlayerGui", "core/anti_afk", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "%$", "function", "open_menu_hint", "locked slot", "TextLabel", "UserInputService", "ย่อขยายเมนูทั้งชุด", "core/i18n", "interactionLogger", "core/teleport", "Speed", "th", "This is not a supported place.", "", "LocalScript", " -> ", " team power, you have ", "lowest", "AutoMine", ")", " reward(s) looked claimable but none went through", "games/registry", "HotbarFrame", "autoPlaceUpgradeWeak", "ProximityPrompt", "maxed", "[Uu]nlock", "CoreUI", "string", "write reported success but ", "มุดแมพ (Noclip)", "บิน (Fly)", "GetCharacterUpgradeCost", "[AutoClaim] ", "", ": ", "[AutoSell] ", ",", "autoUpgradePriority", " (cost ", "core/teleport", "[GuiDump] === end (", "/s", "MineBuySkill", "core/logger", "games/my_anime_mine/main", "", "[Scheduler] ", "core/logger", "'", "table", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "autoClaimInterval", "s", "core/logger", "'", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "[AutoResearch] ", "cost", "ChestCapacity", " ", " files", "[GuiDump] ", "Header", "Content", "Auto Upgrade: สุ่ม Mutation", "MineRollMutation", " = ", "Auto Sell: รอคิวได้นานสุด", "[Decompile] ", "autoUpgradePickaxeIndex", "core/teleport", "Frame", "[AutoChest] disabled", "HttpService", "/", "core/logger", "games/my_anime_mine/game_api", "ClickDetector", "hookfunction", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "autoResearchZones", "GetHolderSpeedLevel", "[AutoResearch] nothing open and affordable right now", "games/my_anime_mine/game_api", "Auto Upgrade: กันเงินไว้", "แพงสุดก่อน", "[AutoChest] chest is empty", "[Scheduler] ", "table", "core/debug_dump_remotes", "games/my_anime_mine/auto_place", "[Decompile] decompile unavailable in this executor", "works from anywhere - dropping the teleport", "no isfile/readfile available to verify the write", "fill", "UserInputService", "Dump Visible GUI", "games/my_anime_mine/auto_sell", "PlayerScripts", "empty", "TextLabel", " waited ", "games/my_anime_mine/auto_claim", "core/logger", "enabled", ") ", "HumanoidRootPart", "s", "[AutoClaim] claimed ", "UserInputService", "GetHolderMineSpeedLevel", ")", " - upgrade the miners first", "Auto Research: รอบเช็ค", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "Mutation: หยุดเมื่อได้ x", "[AutoPlace] ", "fill", "games/my_anime_mine/get_current_plot", "[AntiAFK] disabled", ")", "games/my_anime_mine/auto_mine", "Damage", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "autoClaim", "[AutoUpgrade] bought pickaxe #", "AutoResearch", " (", "autoMineSpeed", "Mining", "[AutoUpgrade] ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "_", "  ", "Auto Sell: รอบขาย", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "(", "TextLabel", "BasePart", "decompile", "auto", ") from ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "getconnections", "en", "อ่านป้ายบน chest ทุกกี่วินาที", "enabled", "[AutoResearch] ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "TextLabel", "neither appendfile nor writefile is available", "ProximityPrompt", "dev", "th", "core/get_place_id", "games/my_anime_mine/auto_place", "Interaction Logger", "RunService", "Auto Claim: เก็บสูงสุดต่อรอบ", " items", "en", "ถูกสุดก่อน", "Zones", " ", " (Text=", "🏠", "Badge", "disabled", " ", "disabled", "autoChest", "core/logger", "", "อังกฤษ", ") needs ", " ===", "string", "PlayerGui", "Mining", "core/debug_dump_gui", "[Decompile] === executor capabilities ===", "games/my_anime_mine/main", ")", "Main", "^%$", "table", "core/teleport", "BodyVelocity", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", ")", "[AutoUpgrade] every stat on every miner is maxed", "⚙", " (", "core/anti_afk", "", "Auto Upgrade: รอบเช็ค"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(778)] = function()
return function(import)
local log = import(S(638))
local Players = game:GetService(S(36))
local VirtualUser = game:GetService(S(32))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(61))
        connection = player.Idled:Connect(function()
            log(S(449))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(704))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(272)] = function()
return { Env = S(737), IsDev = true }
end
modules[S(151)] = function()
local VirtualInputManager = game:GetService(S(236))
local GuiService = game:GetService(S(284))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(435)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(240)] = function()
return function(import)
local HttpService = game:GetService(S(658))
local getPlaceId = import(S(739))
local log = import(S(268))
local ROOT_FOLDER = S(13)
local placeFolder = ROOT_FOLDER .. S(360) .. tostring(getPlaceId())
local filePath = placeFolder .. S(168)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(276) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(405) .. filePath .. S(411) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(197) .. filePath .. S(227) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(402)] = function()
return function(import)
local Players = game:GetService(S(90))
local log = import(S(380))
local CAPABILITIES = {
    S(724), S(181), S(546), S(307), S(84),
    S(728), S(112), S(78), S(496), S(135),
    S(419), S(158), S(663), S(505),
}
local OUT_FOLDER = S(507)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(271)) do
        if type(target) ~= S(45) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(765))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(653) .. name .. S(651) .. ((ok and type(value) == S(480)) and S(51) or S(64)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(676))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(683))
    if not playerScripts then
        log(S(504))
        return 0
    end
    pcall(makefolder, S(351))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(598)) or target:IsA(S(152)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(103) and #source > 0 then
                local name = target:GetFullName():gsub(S(15), S(717))
                if pcall(writefile, OUT_FOLDER .. S(43) .. name .. S(27), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(80) .. written .. S(433) .. OUT_FOLDER
        .. S(427) .. failed .. S(424))
    return written
end
local function run()
    log(S(207))
    reportCapabilities()
    local count = dumpAll()
    log(S(86))
    return count
end
return run
end
end
modules[S(42)] = function()
return function(import)
local Players = game:GetService(S(48))
local log = import(S(660))
local function dumpVisible(root, indent)
    indent = indent or S(779)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(386)) and child.Visible then
            local line = indent .. child.ClassName .. S(232) .. child.Name .. S(633)
            if child:IsA(S(722)) or child:IsA(S(484)) then
                line = line .. S(257) .. tostring(child.Text) .. S(511)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(2) .. attrName .. S(287) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(313))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(542))
    if not playerGui then
        log(S(203))
        return 0
    end
    local screens = 0
    log(S(401))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(244)) and screen.Enabled and screen.Name ~= S(10) then
            screens = screens + 1
            log(S(646) .. screen.ClassName .. S(459) .. screen.Name .. S(639))
            dumpVisible(screen, S(718))
        end
    end
    log(S(625) .. screens .. S(85))
    return screens
end
return dumpGui
end
end
modules[S(674)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(460))
local log = import(S(18))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(561)) or descendant:IsA(S(315)) then
            count = count + 1
            log(S(476) .. descendant.ClassName .. S(264) .. descendant:GetFullName())
        end
    end
    log(S(290) .. count .. S(760))
    return count
end
return dumpRemotes
end
end
modules[S(352)] = function()
return function(import)
local Players = game:GetService(S(330))
local Workspace = game:GetService(S(209))
local UserInputService = game:GetService(S(694))
local log = import(S(538))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(213) .. describe(prompt)
            .. S(526) .. prompt.ActionText .. S(774))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(516) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(331) .. describe(button)
            .. S(749) .. tostring(button.Text) .. S(125))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(267)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(150)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(260)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(166))
        return
    end
    connections = {}
    log(S(190))
    local playerGui = Players.LocalPlayer:WaitForChild(S(762))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(736)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(662)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(97)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(555),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(180)] = function()
return function(import)
local interactionLogger = import(S(176))(import)
local dumpRemotes = import(S(318))(import)
local dumpGui = import(S(764))(import)
local decompileScripts = import(S(477))(import)
local humanoid = import(S(144))
local function build(window, persist)
    local tab = window:AddTab(S(302), S(131))
    persist.Toggle(
        tab:AddCard(
            S(741),
            S(640)
        ),
        S(592),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(172),
        S(525)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(1), function()
        remoteBtn.Text = dumpRemotes() .. S(41)
    end)
    local guiCard = tab:AddCard(
        S(681),
        S(466)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(390), function()
        guiBtn.Text = dumpGui() .. S(370)
    end)
    local decompileCard = tab:AddCard(
        S(177),
        S(716)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(65), function()
        decompileBtn.Text = decompileScripts() .. S(645)
    end)
    humanoid.Start()
    persist.Slider(tab, S(304), S(325),
        8, 200, S(464), 16, humanoid.SetWalkSpeed, S(454))
    persist.Slider(tab, S(350), S(523),
        30, 300, S(53), 50, humanoid.SetJumpPower, S(412))
    persist.Toggle(
        tab:AddCard(S(615), S(385)),
        S(269), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(614), S(341)),
        S(288), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(321), S(500)),
        S(510), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(110)] = function()
return function(import)
    local getPlaceId = import(S(316))
    local registry = import(S(520))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(596))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(570)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(532))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(471)] = function()
local Players = game:GetService(S(35))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(83))
    local backpack = player:WaitForChild(S(34))
    return player, humanoid, backpack
end
return getMe
end
modules[S(327)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(187)] = function()
local Players = game:GetService(S(417))
local RunService = game:GetService(S(303))
local UserInputService = game:GetService(S(589))
local Workspace = game:GetService(S(487))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(305))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(141))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(772))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(159)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(723)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(24))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(591)] = function()
local Players = game:GetService(S(238))
local STRINGS = {
    en = {
        search_placeholder = S(162),
        open_menu_hint = S(467),
    },
    th = {
        search_placeholder = S(336),
        open_menu_hint = S(337),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(40)
    if locale:sub(1, 2):lower() == S(595) then
        return S(738)
    end
    return S(729)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(443) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(488)] = function()
local LOG_FILE = S(384)
local function log(message)
    local line = os.date(S(275)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(306))
            end
            appendfile(LOG_FILE, line .. S(399))
        elseif writefile then
            local existing = S(456)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(255))
        else
            error(S(735))
        end
        if not (isfile and readfile) then
            error(S(678))
        end
        if not isfile(LOG_FILE) then
            error(S(613) .. LOG_FILE .. S(218))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(63) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(371) .. LOG_FILE .. S(392) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(129)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(346) then
        return nil
    end
    local number, suffix = text:match(S(406))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(184), S(170))))
    if not value then
        return nil
    end
    if suffix ~= S(301) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(499)] = function()
return function(import)
local RunService = game:GetService(S(742))
local log = import(S(437))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(672) .. task_.name .. S(256) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(631) .. starved.name .. S(686)
            .. string.format(S(274), now - starved.waitingSince)
            .. S(338))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(537) .. (currentTask and currentTask.name or S(219)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(655)] = function()
local Players = game:GetService(S(75))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(56))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(241)] = function()
return function(import)
local i18n = import(S(549))
local UserInputService = game:GetService(S(680))
local THEME = {
    BrandName = S(242),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(156))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(178))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(497))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(410))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(395)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(278))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(153))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(143)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(503))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(757)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(28))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(541))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(475))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(289))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(734))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(235))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(442))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(261))
            if not valueLbl then
                valueLbl = Instance.new(S(389))
                valueLbl.Name = S(490)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(751))
            if not badge then
                badge = Instance.new(S(281))
                badge.Name = S(515)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(362))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(397))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(339))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(323))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(630)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(126)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(568))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(458))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(329))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(243)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(296))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(39))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(551))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(317)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(266))
    screenGui.Name = S(611)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(74))
    local main = Instance.new(S(253))
    main.Name = S(768)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(444))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(656))
    tabBar.Name = S(102)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(77))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(279))
    header.Name = S(647)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(174))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(340))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(308))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(214)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(569))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(404)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(9))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(16)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(378))
    searchBox.Name = S(391)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(7))
    searchBox.Text = S(139)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(106))
    content.Name = S(648)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(554))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(685))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(231))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(348))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(531)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(514) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(67) .. (item.Subtitle or S(472))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(529))
        footerLabel.Text = i18n.T(S(586))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(311)] = function()
return function(import)
local gameApi = import(S(661))(import)
local log = import(S(233))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(486) .. minItemsToCollect .. S(297))
    else
        log(S(657))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(363) then
            log(S(461))
            lastReported = S(576)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(684) then
            log(S(671))
            lastReported = S(495)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(355) then
            local capacity = gameApi.ChestCapacity()
            log(S(194) .. count .. S(206) .. want .. S(744)
                .. (capacity and (S(286) .. capacity .. S(388)) or S(309)))
            lastReported = S(524)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(328) .. count .. S(22) .. after .. S(705))
        else
            log(S(381) .. count
                .. S(468) .. tostring(after) .. S(603))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(17)] = function()
return function(import)
local gameApi = import(S(396))(import)
local log = import(S(540))
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(560) .. (enabled and S(689) or S(326)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(693) .. reward.category .. S(72)
                .. tostring(reward.tierKey) .. S(270) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(617) .. #pending .. S(604))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    task.spawn(runJob)
end
return autoClaim
end
end
modules[S(706)] = function()
return function(import)
local getCurrentPlot = import(S(425))
local getPlotParts = import(S(335))
local log = import(S(334))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(579), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(204) .. (enabled and S(23) or S(357)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(740)] = function()
return function(import)
local Players = game:GetService(S(508))
local getCurrentPlot = import(S(169))
local getPlotParts = import(S(364))
local getMinerSlots = import(S(493))(import)
local teleport = import(S(624))
local gameApi = import(S(161))(import)
local log = import(S(628))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(393)
local LOCKED_SLOT_TEXT = S(217)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(414))
    return pad and pad:FindFirstChildWhichIsA(S(608), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(188)) then
            local digits = descendant.Text:match(S(423))
            if digits then
                return tonumber((digits:gsub(S(621), S(122))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(588)) and descendant.Text:match(S(769)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(134) .. holder.Name .. S(124)
            .. gameApi.MinerSlots() .. S(659) .. gameApi.MaxMinerSlots() .. S(429)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(587)
    end
    if not prompt.Enabled then
        return true, S(342)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(539) .. price
    end
    local action = prompt.ActionText or S(47)
    if action:match(S(610)) or action:match(S(5)) or action:match(S(584)) then
        return true, S(416) .. action .. S(66)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(691))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(133) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(262) .. why .. S(44))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(202))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(225) .. prompt.ObjectText .. S(721) .. power .. S(509))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(195))
    if line ~= lastSurvey then
        log(S(91) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(702), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(140), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(374) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(79))
    else
        lastAttempt = 0
        log(S(727))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(409),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(478), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(312))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(114) .. holder.Name .. S(62))
            return false
        end
        log(S(559) .. candidate.slot .. S(366) .. candidate.name
            .. S(479))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(701) .. holder.Name .. S(111) .. miner.name)
        return false
    end
    log(S(432) .. miner.name .. S(462) .. miner.power
        .. S(55) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(92) .. entry.name .. S(498) .. entry.holder.Name
            .. S(295) .. tostring(entry.prompt.ActionText) .. S(361))
        return false
    end
    log(S(365) .. entry.name .. S(33) .. entry.power
        .. S(726) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(521))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(679) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(553) .. target.holder.Name .. S(252) .. target.name
        .. S(777) .. target.power .. S(544) .. best.name .. S(536) .. best.power .. S(469))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(211)] = function()
return function(import)
local gameApi = import(S(668))(import)
local log = import(S(173))
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(451) .. (enabled and S(731) or S(752)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                if node.powerOk then
                    table.insert(list, node)
                elseif not powerBlocked or node.powerReq < powerBlocked.powerReq then
                    powerBlocked = node
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(575) .. node.zoneKey .. S(138) .. node.id
                    .. S(712) .. node.family .. S(82) .. node.level .. S(145) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if buyCore then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(258) .. node.id
                    .. S(574) .. node.family .. S(690) .. node.level .. S(68) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(641) .. blocked
                .. S(246))
        elseif powerBlocked then
            log(S(732) .. powerBlocked.zoneKey .. S(644) .. powerBlocked.id
                .. S(38) .. powerBlocked.family .. S(759)
                .. math.floor(powerBlocked.powerReq) .. S(600)
                .. math.floor(powerBlocked.power)
                .. S(697))
        else
            log(S(667))
        end
    end
    busy = false
end
function autoResearch.HasWork()
    if buyZones then
        local nodes = buyableZoneNodes()
        if #nodes > 0 then
            return true
        end
    end
    return buyCore and #buyableCoreNodes() > 0
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(481)] = function()
return function(import)
local Workspace = game:GetService(S(69))
local gameApi = import(S(573))(import)
local teleport = import(S(771))
local log = import(S(154))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(201))
    else
        lastAttempt = 0
        log(S(239))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(421))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(113), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(418)
                .. (sold and S(677)
                         or S(483)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(620) .. (sold and (S(21) .. #keys .. S(377)) or S(294)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(6)] = function()
return function(import)
local Workspace = game:GetService(S(420))
local gameApi = import(S(292))(import)
local teleport = import(S(593))
local log = import(S(756))
local STATS = { S(707), S(594), S(248) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(557)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(715) .. (enabled and S(422) or S(754)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(673) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(95) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(81) or S(609)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(518) .. holderKey .. S(748) .. choice.stat .. S(379)
                        .. choice.info.level .. S(599) .. after.level
                        .. S(333) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(642) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function findPickaxe()
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(70))
    local shop = stand and stand:FindFirstChild(S(247))
    if not shop then
        return nil
    end
    if shop:IsA(S(293)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(465), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(375)
                .. (bought and S(550)
                           or S(101)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(438))
        end
    end
    if bought then
        log(S(710) .. pickaxe.index .. S(192) .. pickaxe.name
            .. S(383) .. pickaxe.multiplier .. S(767))
        return true
    end
    log(S(175) .. pickaxe.index .. S(753) .. pickaxe.name
        .. S(73))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(89) .. holderKey .. S(132)
                .. tostring(entry and entry.Pickaxe or S(26)) .. S(105) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(571) .. holderKey .. S(157) .. variant
                .. S(186) .. after .. S(623) .. cost .. S(431))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass and tryPickaxe() then
        bought = bought + 1
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(635))
        elseif maxed > 0 then
            log(S(775))
        else
            log(S(223))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(310) and entry.Pickaxe and entry.Pickaxe ~= S(567) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(215)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(332))
local log = import(S(408))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(298), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(99) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(770))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(634) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(160) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(155) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(19))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(407) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(251))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(185), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(146) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(482), keys)
end
function api.EquipSlot(key)
    return send(S(491), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(441), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(116), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(650), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(627), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(501)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(12)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(196))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(585) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.GetSkillNodes(zoneKey) or {}) do
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            table.insert(list, {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            })
        end
    end)
    return list
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(534) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(182), characterKey, stat == S(714) and S(107) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(57), max = S(450), cost = S(616) },
    Speed = { level = S(666), max = S(137), cost = S(37) },
    MineSpeed = { level = S(695), max = S(60), cost = S(54) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(552) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(104) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(761) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(93) and type(holder.CharName) == S(8) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(164) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(612) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(474)] = function()
return function(import)
local Players = game:GetService(S(4))
local log = import(S(688))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(581))
    local mineGui = playerGui and playerGui:FindFirstChild(S(434))
    local hotbar = mineGui and mineGui:FindFirstChild(S(606))
    local content = hotbar and hotbar:FindFirstChild(S(198))
    return content and content:FindFirstChild(S(58))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(11)) then
            local count = descendant.Text:match(S(208))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(448))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(452))
    local nameLabel = content and content:FindFirstChild(S(183))
    local damageLabel = content and content:FindFirstChild(S(543))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(618) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(149), S(547)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(108) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(572) .. slot .. S(619) .. info.name
                        .. S(403) .. info.power .. S(368) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(703)] = function()
local Players = game:GetService(S(71))
local Workspace = game:GetService(S(463))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(564))
    local plots = mineWorld and mineWorld:FindFirstChild(S(30))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(234)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(115)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(522)),
        Nodes = plot:FindFirstChild(S(119)),
        Chest = plot:FindFirstChild(S(453)),
        Zones = plot:FindFirstChild(S(747)),
        Crew = plot:FindFirstChild(S(485)),
        Loot = plot:FindFirstChild(S(369)),
        PlotIndex = plot:GetAttribute(S(367)),
        ChestCapacity = plot:GetAttribute(S(643)),
    }
end
return getPlotParts
end
modules[S(766)] = function()
return function(import)
    local createWindow = import(S(165))(import)
    local i18n = import(S(130))
    local antiAfk = import(S(582))(import)
    local buildEnv = import(S(320))
    local scheduler = import(S(376))(import)
    local config = import(S(100))(import)
    local log = import(S(632))
    local window = createWindow({
        Subtitle = S(245),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(31))(import)
    local autoPlace = import(S(675))(import)
    local autoChest = import(S(199))(import)
    local autoSell = import(S(682))(import)
    local autoUpgrade = import(S(109))(import)
    local autoResearch = import(S(88))(import)
    local autoClaim = import(S(687))(import)
    local gameApi = import(S(324))(import)
    scheduler.RegisterStandalone(S(602), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(163), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(353), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(492), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(711), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(273), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(142), 20)
    scheduler.RegisterExclusive(S(147), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(517), S(750))
    persistentToggle(farmTab:AddCard(
        S(221),
        S(512)
    ), S(528), autoMine.SetEnabled)
    persistentSlider(farmTab, S(212), S(228),
        1, 30, S(713), 5, autoMine.SetCyclesPerSecond, S(626))
    persistentSlider(farmTab, S(372), S(128),
        1, 10, S(148), 1, autoMine.SetClicksPerCycle, S(413))
    persistentToggle(farmTab:AddCard(
        S(20),
        S(50)
    ), S(117), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(577), S(193),
        1, 60, S(136), 3, autoPlace.SetCheckInterval, S(436))
    persistentToggle(
        farmTab:AddCard(S(171),
            S(118)),
        S(607), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(14),
        S(314)
    ), S(755), autoChest.SetEnabled)
    persistentSlider(farmTab, S(230), S(447),
        50, 3000, S(359), 100, autoChest.SetMinItems, S(191))
    persistentSlider(farmTab, S(387), S(730),
        1, 60, S(259), 3, autoChest.SetCheckInterval, S(345))
    persistentToggle(farmTab:AddCard(
        S(489),
        S(562)
    ), S(545), autoSell.SetEnabled)
    persistentSlider(farmTab, S(719), S(222),
        5, 300, S(580), 20, autoSell.SetCheckInterval, S(237))
    farmTab:AddSlider(S(652),
        S(354),
        5, 120, sellMaxWait, function(value)
            config.Set(S(265), value)
        end, S(400))
    persistentToggle(farmTab:AddCard(
        S(565),
        S(29)
    ), S(280), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(780), S(530),
        5, 300, S(189), 20, autoUpgrade.SetInterval, S(637))
    persistentSlider(farmTab, S(699), S(664),
        1, 20, S(428), 5, autoUpgrade.SetMaxPerPass, S(59))
    persistentSegmented(
        farmTab,
        { S(220), S(25), S(282), S(763) },
        { S(601), S(167), S(513), S(254) },
        S(622), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(669), S(457),
        0, 500, S(373), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(229))
    persistentToggle(
        farmTab:AddCard(S(556),
            S(179)),
        S(548), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(283) .. pickaxe.multiplier .. S(696)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(277)
            end
            return S(299) .. index .. S(344) .. (pickaxeLabels[index] or S(773))
        end
        local savedPick = config.Get(S(654), 0)
        pickaxeCard = farmTab:AddSlider(
            S(87),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(52), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(597)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(120),
            S(446)),
        S(426), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(649),
            S(358)),
        S(123), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(700), S(349),
        1, 20, S(473), 2, autoUpgrade.SetMutationTarget, S(300))
    persistentSlider(farmTab, S(224), S(470),
        0, 500, S(382), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(347))
    persistentToggle(farmTab:AddCard(
        S(322),
        S(76)
    ), S(566), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(698), S(506),
        10, 600, S(98), 30, autoResearch.SetCheckInterval, S(291))
    persistentSlider(farmTab, S(205), S(733),
        1, 20, S(455), 5, autoResearch.SetMaxPerPass, S(527))
    persistentSlider(farmTab, S(49), S(263),
        0, 500, S(249), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(96))
    persistentToggle(
        farmTab:AddCard(S(430),
            S(720)),
        S(665), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(343),
            S(494)),
        S(445), autoResearch.SetBuyCore, true
    )
    persistentSegmented(
        farmTab,
        { S(746), S(670) },
        { true, false },
        S(210), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(578),
        S(708)
    ), S(709), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(285), S(319),
        5, 300, S(636), 30, autoClaim.SetCheckInterval, S(692))
    persistentSlider(farmTab, S(743), S(200),
        1, 30, S(533), 10, autoClaim.SetMaxPerPass, S(502))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(535))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(226), S(776))
    persistentToggle(
        settingsTab:AddCard(S(398), S(583)),
        S(439),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(216), 100)
    settingsTab:AddSlider(S(558), S(590), 50, 150, savedScale, function(value)
        config.Set(S(415), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(394), 1)
    settingsTab:AddSegmented({ S(46), S(758), S(3) }, savedLanguage, function(index, value)
        local code = ({ S(563), S(356), S(250) })[index]
        config.Set(S(519), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(725), S(745), S(121) })[savedLanguage] or S(94))
    log(S(440) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(605)] = function()
return {
    [79389059854988] = S(629),
}
end
import(S(127))(import)
