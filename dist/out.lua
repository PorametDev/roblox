-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Interaction Logger (debug)", "UIListLayout", "[Interaction] Button activated: ", " (MaxActivationDistance=%s)", "th", "no plot", "[Logger] failed to write ", "'", "not found", "ClickDetector", "Players", "Text", "[Interaction] ProximityPrompt triggered: ", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Frame", " ===", "games/my_anime_mine/debug_dump_remotes", "TextButton", "ClaimOre", "Loot", "", "Debug: Dump All Remotes", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Players", "[AutoChest] CollectAll:FireServer() error: ", "?", "ChestCapacity", "games/my_anime_mine/main", "[Interaction] Key pressed: %s (gameProcessed=%s)", "UIScale", "[MyAnimeMine] not standing in any plot", "Debug: Current Plot", "[AutoChest] enabled (ClaimOre=%s, CollectAll=%s)", "Content", "TextButton", "UserInputService", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "[MyAnimeMine] no nodes present right now", "Remotes", "th", "ReplicatedStorage", "Check", " = ", "Error: ", "ClickDetector", "", "TextLabel", "TextLabel", "[AutoChest] disabled", "UIListLayout", "leaderstats", "[MyAnimeMine] === Node: ", "[AutoMine] disabled", "[AutoSell] disabled", "[AutoPlace] disabled", "Dump", "open_menu_hint", "Workspace", "TextLabel", "MekPro.log", "ProximityPrompt", "SellStand", "core/get_place_id", "GuiButton", "Humanoid", "ClickDetector", ": ", " (Text=", "games/my_anime_mine/get_plot_parts", " = ", "Badge", "core/logger", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "ตั้งค่า", "Players", "MekPro", "[AntiAFK] enabled - watching Player.Idled", "RunService", "Frame", "write reported success but the line isn't in ", "", "TextLabel", "ฟาร์ม", "games/my_anime_mine/main", "core/logger", "Players", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "ScreenGui", "Frame", "[MyAnimeMine] no holders found", "core/anti_afk", "Plots", "เปิด chest แล้วเก็บของอัตโนมัติในตัวเดียว (ยิง ChestPrompt + ClaimOre/CollectAll)", "", "ReplicatedStorage", "games/my_anime_mine/auto_chest", "", "[Remotes] === total: ", " ", "games/my_anime_mine/auto_place", "TextLabel", "core/logger", "ProximityPrompt", "Tool", "RunService", "core/dispatch", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "[AntiAFK] disabled", "games/my_anime_mine/auto_place", "games/my_anime_mine/auto_mine", "games/registry", "core/anti_afk", "Remotes", "ย่อขยายเมนูทั้งชุด", "Frame", "", "games/my_anime_mine/get_current_plot", "Players", "core/i18n", " (ActionText=", "ProximityPrompt", "ValueLabel", "Tool", "no tool", "CoreGui", "[AutoUpgrade] disabled", "games/my_anime_mine/auto_upgrade", "open_menu_hint", "Humanoid", "ไทย", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/debug_dump", "Workspace", "games/my_anime_mine/get_current_plot", "[AutoSell] enabled (SellItems=", "Zones", "en", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "OwnerUserId", "core/interaction_logger", "Debug: Dump Backpack Tool", "Text", "[AutoSell] SellItems:FireServer() error: ", "UIStroke", " '", " remotes", "Frame", "Auto Upgrade", "Auto Mine", "Done", "ScrollingFrame", "games/my_anime_mine/get_current_plot", "Frame", "games/my_anime_mine/get_plot_parts", "core/logger", "RunService", "TextLabel", "core/interaction_logger", "RunService", "ProximityPrompt", "games/my_anime_mine/auto_sell", "TextLabel", "อังกฤษ", "Auto Chest", "Workspace", "TextButton", "", "", "Frame", "no isfile/readfile available to verify the write", "games/my_anime_mine/get_current_plot", "Empty miner slot", "  [attr] ", "games/my_anime_mine/get_plot_parts", "Header", "TextButton", "search_placeholder", ": ", "games/my_anime_mine/get_plot_parts", "ClickDetector", "Frame", "[AutoPlace] enabled", "TextLabel", "Players", "core/get_place_id", "ProximityPrompt", "[AutoPlace] placed ", "TextLabel", ")", "⚙", "Nodes", "[AntiAFK] idle detected, simulating input", "", "core/dispatch", " ===", "TextLabel", "Holders", "[AutoMine] enabled", "core/logger", "Backpack", ")", "[Interaction] logger disabled", "ProximityPrompt", "UserInputService", "Badge", "Frame", "ค้นหาฟังก์ชัน...", "UICorner", "This is not a supported place.", "PlotIndex", "core/i18n", "core/logger", "Auto Sell", "core/logger", "TextButton", "[Interaction] logger enabled - go click/interact with things now", "[MyAnimeMine] === Dump start ===", "Search functions...", "ขนาดเมนู", "write reported success but ", "[AutoUpgrade] enabled", "ReplicatedStorage", "games/registry", "\n", "core/logger", "🏠", "[MyAnimeMine] === Tool: ", "%", "auto", "TextBox", " <-- remote, likely fired by the real interaction", "Frame", "TabBar", "Backpack", "search_placeholder", ")", "ปุ่มเปิดเมนู", "  [attr] ", "core/get_me", "neither appendfile nor writefile is available", "Open menu", "core/get_leaderstat", "games/my_anime_mine/get_plot_parts", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "Dump", "", "Anti-AFK", "RunService", "core/ui", "[Remotes] ", "TextButton", "RemoteFunction", "RemoteFunction", "Chest", "VirtualUser", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "en", "games/my_anime_mine/auto_sell", " on ", "\n", "[MyAnimeMine] === Holder: ", "games/my_anime_mine/auto_chest", "PlayerGui", "X", "RemoteEvent", "TextLabel", "Done", "UIListLayout", "Debug: Dump Node/Holder", "en-us", " ===", "", "Backpack", "games/my_anime_mine/get_plot_parts", "TextLabel", "games/my_anime_mine/debug_dump", "games/my_anime_mine/auto_upgrade", "Frame", "Crew", "เดินไปกด SellStand แล้วกด SelectAllButton อัตโนมัติ (ยังไม่ยืนยันว่าขายจริงหรือแค่เลือก)", "Auto Place Miner", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "[Interaction] ClickDetector clicked: ", "CollectAll", "core/logger", "games/my_anime_mine/auto_mine", "Main", "Pad", "th", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "UpgradePart", "TextButton", "core/logger", "%", " ===", "  ", "My Anime Mine · Mining Hub", "auto", " doesn't exist", "MineWorld", "core/i18n", "Search", "core/ui", "[AutoChest] ClaimOre:FireServer() error: ", "UIPadding", "UICorner", "RemoteEvent", "ClickDetector", "log failed", "Dump", "Players", "games/my_anime_mine/get_current_plot", "SellItems", "ValueLabel", "GuiButton", "CoreUI", "-", "TextButton", "games/my_anime_mine/debug_dump_remotes", "ตามเครื่อง", "[%X] "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(91)] = function()
return function(import)
local log = import(S(285))
local Players = game:GetService(S(184))
local VirtualUser = game:GetService(S(255))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(77))
        connection = player.Idled:Connect(function()
            log(S(192))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(108))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(194)] = function()
return function(import)
    local getPlaceId = import(S(63))
    local registry = import(S(223))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(209))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(242)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(51))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(239)] = function()
local Players = game:GetService(S(118))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(65))
    local backpack = player:WaitForChild(S(200))
    return player, humanoid, backpack
end
return getMe
end
modules[S(185)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(211)] = function()
local Players = game:GetService(S(311))
local STRINGS = {
    en = {
        search_placeholder = S(218),
        open_menu_hint = S(241),
    },
    th = {
        search_placeholder = S(207),
        open_menu_hint = S(237),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(270)
    if locale:sub(1, 2):lower() == S(289) then
        return S(40)
    end
    return S(137)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(298) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(158)] = function()
return function(import)
local Players = game:GetService(S(75))
local Workspace = game:GetService(S(133))
local UserInputService = game:GetService(S(204))
local log = import(S(199))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(13) .. describe(prompt)
            .. S(120) .. prompt.ActionText .. S(201))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(283) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(3) .. describe(button)
            .. S(68) .. tostring(button.Text) .. S(236))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(203)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(180)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(64)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(202))
        return
    end
    connections = {}
    log(S(216))
    local playerGui = Players.LocalPlayer:WaitForChild(S(263))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(160)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(45)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(315)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(29),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(225)] = function()
local LOG_FILE = S(60)
local function log(message)
    local line = os.date(S(321)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(168))
            end
            appendfile(LOG_FILE, line .. S(224))
        elseif writefile then
            local existing = S(94)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(260))
        else
            error(S(240))
        end
        if not (isfile and readfile) then
            error(S(170))
        end
        if not isfile(LOG_FILE) then
            error(S(220) .. LOG_FILE .. S(299))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(80) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(7) .. LOG_FILE .. S(67) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(303)] = function()
return function(import)
local i18n = import(S(301))
local UserInputService = game:GetService(S(36))
local THEME = {
    BrandName = S(76),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(306))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(208))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(305))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(176))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(97)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(144))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(196))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(26)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(266))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(116)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(151))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(268))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(47))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(89))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(101))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(188))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(82))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(122))
            if not valueLbl then
                valueLbl = Instance.new(S(48))
                valueLbl.Name = S(314)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(71))
            if not badge then
                badge = Instance.new(S(162))
                badge.Name = S(205)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(35))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(181))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(278))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(18))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(46)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(294))
        local track = Instance.new(S(79))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(232))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(318))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(167)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(228)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(15))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(50))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(166))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(12)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(88))
    screenGui.Name = S(316)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(125))
    local main = Instance.new(S(153))
    main.Name = S(287)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(30))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(147))
    tabBar.Name = S(233)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(2))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(115))
    header.Name = S(175)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(215))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(59))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(183))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(246)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(251))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(264)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(292))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(317)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(230))
    searchBox.Name = S(302)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(177))
    searchBox.Text = S(272)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(169))
    content.Name = S(34)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(206))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(275))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(57))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(157))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(142)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(81) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(99) .. (item.Subtitle or S(21))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(235))
        footerLabel.Text = i18n.T(S(128))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(96)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(41))
local RunService = game:GetService(S(159))
local getCurrentPlot = import(S(134))
local getPlotParts = import(S(174))
local log = import(S(155))
local FIRE_INTERVAL = 1
local function getRemote(name)
    local remotes = ReplicatedStorage:FindFirstChild(S(113))
    return remotes and remotes:FindFirstChild(name)
end
local autoChest = {}
local connection = nil
function autoChest.SetEnabled(enabled)
    if enabled == autoChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(49))
        return
    end
    local claimOre = getRemote(S(19))
    local collectAll = getRemote(S(284))
    log(string.format(
        S(33),
        tostring(claimOre ~= nil), tostring(collectAll ~= nil)
    ))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        local parts = plot and getPlotParts(plot)
        local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(121), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        if claimOre then
            local ok, err = pcall(function()
                claimOre:FireServer()
            end)
            if not ok then
                log(S(304) .. tostring(err))
            end
        end
        if collectAll then
            local ok, err = pcall(function()
                collectAll:FireServer()
            end)
            if not ok then
                log(S(25) .. tostring(err))
            end
        end
    end)
end
function autoChest.IsEnabled()
    return connection ~= nil
end
return autoChest
end
end
modules[S(286)] = function()
return function(import)
local RunService = game:GetService(S(105))
local getCurrentPlot = import(S(152))
local getPlotParts = import(S(69))
local log = import(S(102))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(308), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(53))
        return
    end
    log(S(198))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(109)] = function()
return function(import)
local Players = game:GetService(S(86))
local RunService = game:GetService(S(156))
local getCurrentPlot = import(S(171))
local getPlotParts = import(S(243))
local log = import(S(72))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(288))
    return pad and pad:FindFirstChildWhichIsA(S(61), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(172)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(55))
        return
    end
    log(S(182))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(273))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(129))
        local tool = backpack:FindFirstChildWhichIsA(S(123))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(187) .. tool.Name .. S(259) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(161)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(95))
local Workspace = game:GetService(S(165))
local RunService = game:GetService(S(78))
local log = import(S(85))
local FIRE_INTERVAL = 2
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(54))
        return
    end
    local remotes = ReplicatedStorage:FindFirstChild(S(39))
    local sellItems = remotes and remotes:FindFirstChild(S(313))
    log(S(135) .. tostring(sellItems ~= nil) .. S(189))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local sellStand = Workspace:FindFirstChild(S(62))
        local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(103), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        if sellItems then
            local ok, err = pcall(function()
                sellItems:FireServer()
            end)
            if not ok then
                log(S(143) .. tostring(err))
            end
        end
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(127)] = function()
return function(import)
local RunService = game:GetService(S(248))
local getCurrentPlot = import(S(312))
local getPlotParts = import(S(179))
local log = import(S(212))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(291))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(66))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(126))
        return
    end
    log(S(221))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(276)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(193)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(145) .. child.Name .. S(8)
        if child:IsA(S(186)) then
            line = line .. string.format(
                S(87),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(10)) then
            line = line .. string.format(S(4), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(265)) or child:IsA(S(253)) then
            line = line .. S(231)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(173) .. attrName .. S(43) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(296))
    end
end
return dumpInstance
end
modules[S(319)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(222))
local log = import(S(293))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(307)) or descendant:IsA(S(252)) then
            count = count + 1
            log(S(250) .. descendant.ClassName .. S(178) .. descendant:GetFullName())
        end
    end
    log(S(98) .. count .. S(295))
    return count
end
return dumpRemotes
end
end
modules[S(117)] = function()
local Players = game:GetService(S(11))
local Workspace = game:GetService(S(58))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(300))
    local plots = mineWorld and mineWorld:FindFirstChild(S(92))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(139)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(274)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(197)),
        Nodes = plot:FindFirstChild(S(191)),
        Chest = plot:FindFirstChild(S(254)),
        Zones = plot:FindFirstChild(S(136)),
        Crew = plot:FindFirstChild(S(279)),
        Loot = plot:FindFirstChild(S(20)),
        PlotIndex = plot:GetAttribute(S(210)),
        ChestCapacity = plot:GetAttribute(S(27)),
    }
end
return getPlotParts
end
modules[S(84)] = function()
return function(import)
    local Players = game:GetService(S(24))
    local createWindow = import(S(249))(import)
    local i18n = import(S(119))
    local antiAfk = import(S(112))(import)
    local interactionLogger = import(S(140))(import)
    local log = import(S(214))
    local window = createWindow({
        Subtitle = S(297),
    })
    local getCurrentPlot = import(S(131))
    local getPlotParts = import(S(154))
    local dumpInstance = import(S(132))
    local dumpRemotes = import(S(17))(import)
    local autoMine = import(S(110))(import)
    local autoPlace = import(S(100))(import)
    local autoChest = import(S(262))(import)
    local autoSell = import(S(258))(import)
    local autoUpgrade = import(S(277))(import)
    local farmTab = window:AddTab(S(83), S(226))
    farmTab:AddCard(
        S(149),
        S(256)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(281),
        S(282)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(164),
        S(93)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(213),
        S(280)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(148),
        S(290)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(32), S(107))
    local checkBtn
    checkBtn = debugCard:AddButton(S(42), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(9)
            log(S(31))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(37),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(269),
        S(138)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(310), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(6)
            return
        end
        local ok, err = log(S(217))
        if not ok then
            dumpBtn.Text = S(309)
            dumpCard:SetSubtitle(S(44) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(52) .. firstNode.Name .. S(195))
            dumpInstance(firstNode, log)
        else
            log(S(38))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(261) .. firstHolder.Name .. S(16))
            dumpInstance(firstHolder, log)
        else
            log(S(90))
        end
        dumpBtn.Text = S(150)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(141),
        S(73)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(245), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(234))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(104))
        if not tool then
            toolDumpBtn.Text = S(124)
            return
        end
        log(S(227) .. tool.Name .. S(271))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(238) .. attrName .. S(70) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(267)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(22),
        S(244)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(56), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(146)
    end)
    local settingsTab = window:AddTab(S(74), S(190))
    settingsTab:AddCard(S(247), S(23)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(1),
        S(14)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(219), S(114), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(320), S(163), S(130) }, 1, function(index, value)
        local code = ({ S(229), S(257), S(5) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(111)] = function()
return {
    [79389059854988] = S(28),
}
end
import(S(106))(import)
