-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {" (x", ")", ")", ")", "autoPlaceEquipBest", "", "ValueLabel", "games/my_anime_mine/auto_plot", "Search", "ขายทุกกี่วินาที", "core/dispatch", "MineTakeChest", " = ", "ถูกสุดก่อน", "core/teleport", ",", "may need us at the stand - trying that", "MekPro", "enabled", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "=locked(", "[^%.]+", " ", "write reported success but the line isn't in ", "RemoteFunction", "[AutoPlot] ", "BodyPosition", "autoPlaceEquipBestInterval", "dearest", "[AutoPlace] picked up ", "core/i18n", "_", "autoUpgradePickaxeIndex", "TextButton", "action '", "Dump All Remotes", " (prompt says '", "Auto Upgrade: รอบเช็ค", "UICorner", "string", "Auto Sell: รอบขาย", "[AutoPlot] now mining ", "/", "yes", "table", " (still ", "ItemFrame", " ", " mutation x", "autoUpgradeMutationBudget", "core/anti_afk", "[Decompile] decompile unavailable in this executor", "modules not loaded", "-", "autoUpgradeEquipPickaxe", "[AutoChest] waiting: ", "[Interaction] ProximityPrompt triggered: ", "TextButton", "Auto Mine", "Players", "games/my_anime_mine/main", " (chest holds ", ")", "core/get_leaderstat", "AutoPlot", ", base ", ": ", "Workspace", "BasePart", "core/logger", "ModuleScript", "Auto Chest", "M", "(nothing to do)", "number", "games/my_anime_mine/game_api", "GetCharacterMineSpeedUpgradeCost", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "AutoUpgrade", "AutoUpgrade", "th", "[Decompile] === end ===", "TextButton", " files", "Chest", "fill", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "GuiService", "core/i18n", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "enabled", "[AutoResearch] ", "AutoResearch", "Decompile Client Scripts", "ClickDetector", "=empty", "Players", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "", "x", "[Config] couldn't read ", "open_menu_hint", "ChestCapacity", " refused - ", "[GameAPI] shapes: no Character entries in the inventory", "autoClaimMaxPerPass", "[AutoPlace] ", "core/logger", "autoChestInterval", "  ", "autoUpgradeMaxPerPass", "Modules", "BasePart", "autoChest", "cheapest", "autoPlaceUnlock", " (", "Run", " -> ", "table", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "table", " failed)", "Crew", "Frame", "MekPro/decompiled", "%.3g", "table", "x", "core/logger", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "", "cost", "unlock", "TextBox", "table", "UserInputService", "ไล่ซื้อทุกกี่วินาที", "core/teleport", "LocalScript", "games/my_anime_mine/auto_research", " = ", "/", " (power ", "MekPro", "All", " thing available first", "core/humanoid", "games/my_anime_mine/auto_plot", "disabled", "autoUpgradeRollMutation", "[AutoPlot] ", "autoUpgradePickaxe", "games/my_anime_mine/auto_sell", ")", "UIPadding", "slot not in the hotbar", ")", "MinePower", "TextLabel", "X", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "core/spend_policy", "AutoUpgrade", "unreadable", "core/debug_interaction_logger", "core/run_job", " (", "TextLabel", "cheapest", "Auto Plot: ซื้อโซนถัดไป", "ค้นหาฟังก์ชัน...", "search_placeholder", "debug.getproto", "%.3g", "Auto Chest: รอบเช็ค", " ", "MekPro", " -> ", "] job failed: ", "[", "0 = ไม่จำกัด", "core/debug_dump_gui", "PlayerScripts", " - upgrade the miners first", ", have ", "MineSpeed", "off - every automation spends on its own", " (", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "ลำดับการใช้เงินรวมทุกระบบ", "table", "AutoPlace", "core/dispatch", " ", "'", "enabled", "Auto Research (Plot)", "en", "Root", "index rewards are disabled in this game build", "Players", "AutoChest", "TextLabel", " is no better after the swap (", "ZoneBoard", "Speed ก่อน", "en", "core/spend_policy", "Workspace", "[Interaction] ClickDetector clicked: ", "getgc", "core/get_place_id", "[AutoChest] can't read the chest yet", "s", "Dump", "[Decompile] === executor capabilities ===", " still empty after placing ", "[AutoResearch] ", "UIStroke", "WalkSpeed (ปกติ 16)", " #", ")", "", "works from anywhere - dropping the teleport", "UIScale", "GuiButton", "TextLabel", "ItemName", "string", " (", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "แรงกระโดด", "core/run_job", "none", "", "ขนาดเมนู", "getnilinstances", "autoUpgradeInterval", "/", "GetHolderLevel", "[AutoUpgrade] ", ")", "Frame", " ", "autoSellInterval", "languageIndex", "core/debug_tab", "autoResearchCoreLast", " ", "getconnections", "autoPlotBuy", "table", "Search functions...", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "Mining", "function", "PlayerGui", " possible holder(s) - not buying more", " (", " ParseCharStack=", "autoResearch", "dearest", "?", "auto", " (x", ", you have ", " not bought yet (", "games/my_anime_mine/get_plot_parts", "Auto Place: รอบดึงตัวเก่ง", "[Decompile] PlayerScripts not found", "table", "[BestMiner] slot ", " -> ", "GuiObject", "Damage", " (", "This is not a supported place.", "List", "[AutoPlace] Equip Best: hotbar's strongest ", ": ", "getloadedmodules", "Players", "auto", " ", "autoPlace", "[GameAPI] shapes: Hotbar[", "UICorner", " node(s) looked buyable but the server refused", "ClickDetector", "none", "Damage", "ตามเครื่อง", "", "games/my_anime_mine/get_plot_parts", "[Scheduler] ", " ", " '", "ย่อขยายเมนูทั้งชุด", " stack(s)", "s", "core/teleport", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Auto Claim (Index)", "Zones", "string", "every open node is maxed", "^M(%d+)$", "[Interaction] Button activated: ", "lowest", "=", "Speed", "leaderstats", "ProximityPrompt", "Damage", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "อ่านป้ายบน chest ทุกกี่วินาที", "spendOrder", "TextLabel", "/", " (", " ", ") ", "[AntiAFK] enabled - watching Player.Idled", "Auto Mine: ความเร็ว", "AutoPlot", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "[Scheduler] ", "%$%s*([%d,%.]+)%s*(%a*)", "", "nothing left to buy (", "/s", " -> ", "/", "ScreenGui", "games/my_anime_mine/get_current_plot", "220e191+", "[AutoUpgrade] every stat on every miner is maxed", "Workspace", "[MekPro] settings file: ", "Workspace", "buying the ", "{", "games/my_anime_mine/auto_claim", " (Text=", "TextButton", "games/my_anime_mine/auto_mine", "[AutoResearch] nothing open and affordable right now", "Exclusive", "แพงสุดก่อน", "; ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Hauler", "AutoPlot", "Pad", "TextLabel", "[AutoSell] ", "ValueLabel", "core/debug_dump_gui", "Content", "Frame", "[AutoPlace] ", ") on ", "Auto Plot: รอบเช็ค", "Auto Mine: คลิกต่อรอบ", "Header", "[AutoResearch] ", "autoUpgrade", "Auto Upgrade Unit", "[AutoPlace] couldn't pick up ", "string", "s behind a purchase that never happened - letting it spend", "Auto Plot: ใช้โซนล่าสุดเสมอ", "Auto Plot: กันเงินไว้", "Auto Sell", "fill", "AutoPlace", "core/debug_interaction_logger", "AutoResearch", "[AutoPlace] unlocked ", "off", "s", "decompile", "=", "", ") ", "[GameAPI] couldn't load the game's modules: ", "TextLabel", " GetHolderDamage=", "Content", "core/logger", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "Frame", "ClaimableTiers failed", ") from ", "[AutoResearch] ", "interactionLogger", " costs ", "%.0f", "GuiButton", "[GuiDump] ", "AutoSell", "[AutoMine] ", "disabled", "th", ": ", "MineSpeed", "core/humanoid", "", "AutoPlot", "games/registry", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "Auto Upgrade: Pickaxe ที่จะซื้อ", "[AutoChest] MineTakeChest had no effect (", "games/my_anime_mine/game_api", "", "Auto Upgrade: แจก Pickaxe ให้ miner", " GetVariantMult=", "autoMineClicks", "[GuiDump] === visible PlayerGui tree ===", "core/parse_money", "core/spend_policy", "Humanoid", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "ดีบัก", "TabBar", "เดินทะลุกำแพงและพื้น", "MineSpeed", "nothing to place or upgrade", "enabled", "GetCharacterSpeedUpgradeCost", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "games/my_anime_mine/game_api", "Players", "Text", "ZoneBoardGui", "table", "^Swap out ", " still locked after paying ", " has been busy for ", "Holders", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "autoPlotInterval", "CoreUI", "Auto Place: เปลี่ยนตัวอ่อนออก", "autoChestMinItems", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "AutoPlace", "PlayerGui", "", "", "s", "autoResearchCore", "[GameAPI] shapes: no mine data yet", "", "OwnerUserId", "HumanoidRootPart", "games/my_anime_mine/auto_chest", "#", "games/my_anime_mine/auto_mine", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "autoSellMaxWait", "", "[AutoUpgrade] ", "My Anime Mine · Mining Hub", "Empty miner slot", "autoMine", "Content", "[AutoUpgrade] ", "[AutoPlot] unlocked ", "", "GuiService", "Core", "core/spend_policy", "[AutoChest] chest is empty", "[AutoClaim] ", "write reported success but ", "needs ", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "[AutoPlace] slot ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "Limited", ": ", " (money ", "antiAfk", "Auto Place Miner", "'", "games/registry", ".lua", "  ", "MineSelectZone", "%", "%.0f", "Speed", "PlayerGui", "slot ", "core/debug_decompile", "empty", "[AutoPlot] ", "core/i18n", "dev", "ProximityPrompt", "HumanoidRootPart", "games/my_anime_mine/game_api", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Frame", "slot past the usable cap", "fly", "function", "autoUpgradeLimitPickaxes", "Auto Research: ซื้อสูงสุดต่อรอบ", "MineRollMutation", "getsenv", "waiting", "th", "AutoClaim", "games/my_anime_mine/main", "[AutoSell] MineSellItems ", "(", "GetHolderMineSpeedLevel", "UIListLayout", "[AutoResearch] cheapest open node: ", "Players", "table", "dearest", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "[Decompile] wrote ", "[AutoUpgrade] bought pickaxe #", ")", "MineBuyPickaxe", "MineUpgradeCharacter", "[AutoUpgrade] MineBuyPickaxe ", "core/run_job", "core/scheduler", "core/scheduler", ") ", " (ActionText=", "Auto Research: กันเงินไว้", "TextLabel", " | ", "[Config] couldn't write ", " (", "core/logger", "Auto Place: เพดานราคาปลดช่อง", "\n", "SellStand", "AutoUpgrade", " team power", "AutoChest", " for ", "core/run_job", "games/my_anime_mine/game_api", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "dearest", "[AutoPlace] fireproximityprompt error: ", "autoUpgradePriority", "Text", "[MekPro] build ", "Workspace", "🏠", ") needs ", " ", " (cost ", "ClickDetector", "games/my_anime_mine/auto_chest", ": ", "search_placeholder", " reward(s) looked claimable but none went through", "games/my_anime_mine/auto_sell", "[Logger] failed to write ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "Auto Research: ซื้อสาย Zone", "games/my_anime_mine/get_best_miner", ",", "core/parse_money", " claimable but none could be resolved", ")", "ต่ำสุดก่อน", "core/debug_tab", "Players", "no isfile/readfile available to verify the write", "core/config", "^%$", "core/run_job", "autoUpgradeKeepMoney", "ลอย (Float)", "Auto Upgrade: สุ่ม Mutation", " research is ", "no", "games/my_anime_mine/auto_upgrade", "\n", "ยิง Equip Best ทุกกี่วินาที", " wouldn't take ", "needs us at the stand - keeping the teleport", "M", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "ตั้งค่า", "[Decompile] === start ===", "games/my_anime_mine/get_current_plot", "core/teleport", "TextLabel", "Mining ก่อน", "core/run_job", "cost", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Interaction Logger", ", built ", "M", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", ", ", "core/logger", "บิน (Fly)", "%$", "TextLabel", "autoPlotLatest", "ReplicatedStorage", "maxed", "s", "Dump Visible GUI", "jumpPower", "Anti-AFK", " ===", "ความเร็ววิ่ง", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", ")", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "BodyVelocity", "core/debug_dump_remotes", "[Bb]uy", "core/logger", "ProximityPrompt", "core/get_place_id", "research", "function", "MineWorld", "", "autoMineSpeed", "disabled", "Auto Upgrade Plot (Zone)", "⚙", "/", "TextLabel", "games/my_anime_mine/auto_claim", "", "ไล่อัปเกรด miner ทุกกี่วินาที", ") ", " (", "Players", "M", ") -> ", "[Remotes] === total: ", "OwnerUserId", "[Scheduler] turn: ", "HttpService", "debug.getupvalues", "[AutoSell] enabled", "TextButton", "TextLabel", " (Text=", "lowest", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "Badge", "อังกฤษ", "off", "Mutation: เพดานราคาต่อครั้ง", "[GameAPI] SendEvent failed: ", "GetHolderSpeedLevel", "Players", "s", "[GuiDump] PlayerGui not found", "Players", "[Interaction] logger disabled", "Frame", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", ",", " screens", "games/my_anime_mine/game_api", "[AutoPlace] ", "nothing claimable", "autoPlotKeepMoney", "M", "", "dearest", "works from anywhere - no teleport needed", "", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "locked slot", "autoResearchZones", "table", " screen(s)) ===", "core/logger", "", "Open menu", "disabled", "[AutoPlace] ", "off", "}", " fields: ", " -> ", "waiting", "Auto Research: ซื้อสาย Core", " (", "open_menu_hint", "AutoPlot", "Auto Place: ปลดล็อกช่อง miner", "%.0f", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "autoClaim", "badge says ", "AutoPlace", "s", "core/logger", " script(s) to ", "core/ui", "x", "sold ", "=", "table", "autoClaimInterval", "[GuiDump] === end (", "table", "", "core/build_env", "[Interaction] Key pressed: %s (gameProcessed=%s)", " (power ", "[AutoPlace] unlocking ", "button has no size on screen", "[AutoPlace] drifted out of range of ", "[AutoPlace] holders: ", " for ", "", "RunService", "[AutoUpgrade] ", " (", "[AutoResearch] idle: ", "games/my_anime_mine/auto_upgrade", ": ", "", "core/logger", " (", "[%X] ", ": badge says ", "[AutoPlace] disabled", "unreadable", "", " -> ", "ProximityPrompt", "server didn't equip it", "Loot", "AutoMine", "core/logger", "] = ", "UIListLayout", "hookfunction", "table", "TextLabel", "games/my_anime_mine/auto_place", "[AutoPlace] upgrading ", "Badge", "Character", "Container", " ", "Frame", "Frame", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "[AutoChest] disabled", "ฟาร์ม", " owned)", "Shop Stand", "^x(%d+)$", ")", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "games/my_anime_mine/game_api", "auto", "Workspace", "([%d,%.]+)%s*(%a*)", "AutoResearch", " costs ", "games/my_anime_mine/game_api", "Auto Upgrade: ซื้อ Pickaxe ด้วย", " team power, you have ", "Locked miner slot", "neither appendfile nor writefile is available", "AutoResearch", "autoUpgradePickaxeIndex", "TextButton", "languageIndex", " error: ", "Plots", "[AutoChest] collected ", "Auto Claim: รอบเช็ค", "[SpendPolicy] ", "Backpack", "CharMaxLevel", "[GameAPI] shapes: Holders.", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", " pickaxe ", "s", "Auto Place: ดึงตัวเก่งขึ้น hotbar", " waited ", "  [attr] ", "TextLabel", "off", "core/logger", "autoResearchMaxPerPass", "[SpendPolicy] ", "cheapest open node ", "ถูกสุดก่อน", " x", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "both Zone and Core buying are switched off", "Zone buying is off", "BasePart", "[AutoClaim] idle: ", "core/click", "Players", ")", "Auto Place: รอบเช็ค", "needs ", "[GameAPI] holder ", "core/spend_policy", "Mutation: หยุดเมื่อได้ x", "s - jumping the queue", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", " while selecting", "%.3g", "%.3g", " but no unclaimed reward in its Track", "x", "[Decompile] ", "TextButton", "/", "TextLabel", "th", "games/my_anime_mine/get_current_plot", "เช็คโซนทุกกี่วินาที", "MekPro.log", "noclip", "MineSellItems", ")", "auto", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Players", "RemoteEvent", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "unlock", "%$", "2026-09-21 13:11:32", "[Remotes] ", "core/debug_dump_remotes", "[AutoPlace] nothing in the hotbar to place", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "UserInputService", "core/logger", ") didn't take: ", "[AutoPlot] couldn't switch to ", "core/logger", "table", "UserInputService", "MineBuySkill", "HotbarFrame", "games/my_anime_mine/auto_research", "core/config", "autoPlaceUpgradeWeak", "[GameAPI] shapes: Hotbar is empty", "autoResearchInterval", "Auto Chest: เกณฑ์เก็บ", "[AutoUpgrade] pickaxe #", "", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", " -> ", "Character", "s", "autoResearchCheapestFirst", "string", "table", "TextButton", "", "upgrade", "core/logger", "getscriptbytecode", "walkSpeed", "[AntiAFK] disabled", ") - leaving it alone for ", "[AutoPlace] placed ", "ScrollingFrame", "core/run_job", "en-us", " from ", "[^%w]+", " items", "autoSellMaxWait", " ", "core/ui", "core/run_job", "[AutoPlace] couldn't hold anything - not pressing E", "games/my_anime_mine/game_api", " ชิ้น", "VirtualUser", "[AutoClaim] ", "core/logger", "GetCharacterUpgradeCost", " power ", "%.3g", "LayerCollector", "Damage ก่อน", " didn't go through (out of stock or too expensive)", "ReplicatedStorage", "autoPlaceUnlockBudget", "TextButton", "AutoUpgrade", "", "MekPro", "table", "core/logger", "enabled", ")", "core/logger", ": State(", "[Uu]nlock", "Frame", "autoPlot", "", "sale didn't go through", "core/anti_afk", "Auto Upgrade: กันเงินไว้", "debug.getconstants", "autoPlaceInterval", "core/debug_decompile", "float", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "autoUpgradeMutationTarget", ")", "CharMineSpeedMaxLevel", "MineEquipSlot", "core/logger", ") gave nothing", "ปุ่มเปิดเมนู", "VirtualInputManager", "HumanoidRootPart", "getscripts", "autoSellProtectUnits", "getrenv", "🐛", "costs ", "dearest", "Humanoid", "JumpPower (ปกติ 50)", "TextLabel", "TextLabel", "ต่างคนต่างซื้อ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "AutoSell", "AutoPlace", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "0 = ไม่จำกัด", "%.3g", "PickaxesStand", "[AutoResearch] Core ", "Auto Research: รอบเช็ค", "en", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "Frame", "Hauler", "core/get_me", "Auto Claim: เก็บสูงสุดต่อรอบ", "CharSpeedMaxLevel", "", "CoreGui", "s", "มุดแมพ (Noclip)", "[AutoChest] enabled (collects once >= ", "[Interaction] logger enabled - go click/interact with things now", "s - carrying on without it", " remotes", " GetHolderLevel=", "[AutoClaim] claimed ", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "Nodes", " waited ", "BasePart", "games/my_anime_mine/get_plot_parts", "/", " items)", "Frame", ")", "Players", "core/logger", "Auto Sell: รอคิวได้นานสุด", "core/logger", "affordable - waiting for a turn", "no mine data", "MineEquipBest", "'", "menuScale", " ", "[GameAPI] shapes:   GetHolderVariant=", " ", "AutoPlace", "string", " -> x", " - ", " '", "PlotIndex", "getcallbackvalue", "research", "Main", "Dump", "games/my_anime_mine/get_best_miner", "core/parse_money", "autoSell", "RunService", "[AutoUpgrade] ", "autoResearchKeepMoney", "table", "%.3g", " pickaxe(s) for ", "MineEquipPickaxe", "empty", "^([%d,%.]+)%s*(%a*)$", "menuScale", "ไทย", "ZoneRow", "[Scheduler] ", "[AutoSell] disabled", "core/build_env", "AutoClaim", "UIListLayout", "disabled", ")", "แพงสุดก่อน", "games/my_anime_mine/auto_place", "", "core/get_place_id", "Plots", "')", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " items (now ", "[GameAPI] shapes: inventory entry ", "MineWorld", "x", "Core buying is off", "prompt disabled", "table", "core/spend_policy", " doesn't exist", "/config.json", "MineGui", "All", "[AntiAFK] idle detected, simulating input", "AutoPlace"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(926)] = function()
return function(import)
local log = import(S(858))
local Players = game:GetService(S(817))
local VirtualUser = game:GetService(S(900))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(325))
        connection = player.Idled:Connect(function()
            log(S(1052))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(884))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(724)] = function()
return { Env = S(502), IsDev = true, Commit = S(338), BuiltAt = S(849) }
end
modules[S(816)] = function()
local VirtualInputManager = game:GetService(S(940))
local GuiService = game:GetService(S(473))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(728)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(864)] = function()
return function(import)
local HttpService = game:GetService(S(655))
local getPlaceId = import(S(633))
local log = import(S(392))
local ROOT_FOLDER = S(145)
local placeFolder = ROOT_FOLDER .. S(985) .. tostring(getPlaceId())
local filePath = placeFolder .. S(1049)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(122) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(101) .. filePath .. S(407) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(542) .. filePath .. S(567) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(498)] = function()
return function(import)
local Players = game:GetService(S(284))
local log = import(S(805))
local CAPABILITIES = {
    S(384), S(882), S(942), S(212), S(283),
    S(251), S(1007), S(514), S(944), S(928),
    S(656), S(174), S(755), S(238),
}
local OUT_FOLDER = S(126)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(22)) do
        if type(target) ~= S(915) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(217))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(831) .. name .. S(142) .. ((ok and type(value) == S(258)) and S(44) or S(590)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(52))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(184))
    if not playerScripts then
        log(S(272))
        return 0
    end
    pcall(makefolder, S(18))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(140)) or target:IsA(S(71)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(40) and #source > 0 then
                local name = target:GetFullName():gsub(S(891), S(32))
                if pcall(writefile, OUT_FOLDER .. S(335) .. name .. S(490), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(528) .. written .. S(714) .. OUT_FOLDER
        .. S(168) .. failed .. S(123))
    return written
end
local function run()
    log(S(599))
    reportCapabilities()
    local count = dumpAll()
    log(S(82))
    return count
end
return run
end
end
modules[S(360)] = function()
return function(import)
local Players = game:GetService(S(649))
local log = import(S(855))
local function dumpVisible(root, indent)
    indent = indent or S(732)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(276)) and child.Visible then
            local line = indent .. child.ClassName .. S(299) .. child.Name .. S(996)
            if child:IsA(S(615)) or child:IsA(S(34)) then
                line = line .. S(346) .. tostring(child.Text) .. S(1032)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(802) .. attrName .. S(13) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(491))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(450))
    if not playerGui then
        log(S(671))
        return 0
    end
    local screens = 0
    log(S(421))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(906)) and screen.Enabled and screen.Name ~= S(914) then
            screens = screens + 1
            log(S(402) .. screen.ClassName .. S(1005) .. screen.Name .. S(488))
            dumpVisible(screen, S(110))
        end
    end
    log(S(721) .. screens .. S(691))
    return screens
end
return dumpGui
end
end
modules[S(851)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(617))
local log = import(S(990))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(845)) or descendant:IsA(S(25)) then
            count = count + 1
            log(S(850) .. descendant.ClassName .. S(67) .. descendant:GetFullName())
        end
    end
    log(S(652) .. count .. S(623))
    return count
end
return dumpRemotes
end
end
modules[S(379)] = function()
return function(import)
local Players = game:GetService(S(435))
local Workspace = game:GetService(S(68))
local UserInputService = game:GetService(S(854))
local log = import(S(919))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(57) .. describe(prompt)
            .. S(538) .. prompt.ActionText .. S(2))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(211) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(310) .. describe(button)
            .. S(660) .. tostring(button.Text) .. S(243))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(748)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(95)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(227)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(673))
        return
    end
    connections = {}
    log(S(975))
    local playerGui = Players.LocalPlayer:WaitForChild(S(259))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(632)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(565)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(401)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(725),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(580)] = function()
return function(import)
local interactionLogger = import(S(166))(import)
local dumpRemotes = import(S(629))(import)
local dumpGui = import(S(183))(import)
local decompileScripts = import(S(930))(import)
local humanoid = import(S(148))
local function build(window, persist)
    local tab = window:AddTab(S(426), S(945))
    persist.Toggle(
        tab:AddCard(
            S(607),
            S(462)
        ),
        S(398),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(36),
        S(1039)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(1010), function()
        remoteBtn.Text = dumpRemotes() .. S(977)
    end)
    local guiCard = tab:AddCard(
        S(620),
        S(413)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(216), function()
        guiBtn.Text = dumpGui() .. S(677)
    end)
    local decompileCard = tab:AddCard(
        S(94),
        S(606)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(118), function()
        decompileBtn.Text = decompileScripts() .. S(84)
    end)
    humanoid.Start()
    persist.Slider(tab, S(624), S(221),
        8, 200, S(883), 16, humanoid.SetWalkSpeed, S(1035))
    persist.Slider(tab, S(233), S(949),
        30, 300, S(621), 50, humanoid.SetJumpPower, S(456))
    persist.Toggle(
        tab:AddCard(S(613), S(964)),
        S(509), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(973), S(428)),
        S(839), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(587), S(625)),
        S(931), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(11)] = function()
return function(import)
    local getPlaceId = import(S(1036))
    local registry = import(S(412))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(279))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(64)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(314))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(967)] = function()
local Players = game:GetService(S(989))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(424))
    local backpack = player:WaitForChild(S(794))
    return player, humanoid, backpack
end
return getMe
end
modules[S(213)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(409)] = function()
local Players = game:GetService(S(60))
local RunService = game:GetService(S(1014))
local UserInputService = game:GetService(S(137))
local Workspace = game:GetService(S(210))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(948))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(458))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(628))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(69)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(113)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(27))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(89)] = function()
local Players = game:GetService(S(581))
local STRINGS = {
    en = {
        search_placeholder = S(254),
        open_menu_hint = S(694),
    },
    th = {
        search_placeholder = S(172),
        open_menu_hint = S(939),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(889)
    if locale:sub(1, 2):lower() == S(835) then
        return S(81)
    end
    return S(963)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(266) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(108)] = function()
local LOG_FILE = S(838)
local function log(message)
    local line = os.date(S(742)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(645))
            end
            appendfile(LOG_FILE, line .. S(592))
        elseif writefile then
            local existing = S(386)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(546))
        else
            error(S(784))
        end
        if not (isfile and readfile) then
            error(S(582))
        end
        if not isfile(LOG_FILE) then
            error(S(478) .. LOG_FILE .. S(1048))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(24) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(571) .. LOG_FILE .. S(738) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(1012)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(876) then
        return nil
    end
    local number, suffix = text:match(S(330))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(575), S(913))))
    if not value then
        return nil
    end
    if suffix ~= S(637) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(585)] = function()
return function(import)
local log = import(S(130))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(181) .. name .. S(180) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(536)] = function()
return function(import)
local RunService = game:GetService(S(733))
local log = import(S(752))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(329) .. task_.name .. S(789) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(1026) .. task_.name .. S(441)
                    .. string.format(S(707), now - task_.busySince)
                    .. S(976))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(297) .. starved.name .. S(982)
            .. string.format(S(400), now - starved.waitingSince)
            .. S(824))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(654) .. (currentTask and currentTask.name or S(74)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(423)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(740))
local policy = {}
local probes = {}
local order = {}
local mode = S(665)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(807) .. (mode == S(804)
        and S(188)
        or (S(343) .. mode .. S(147))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(382) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(115) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(526) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(793) .. name .. S(801)
            .. string.format(S(494), now - blockedSince[name])
            .. S(373))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(601)] = function()
local Players = game:GetService(S(524))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(941))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(895)] = function()
return function(import)
local i18n = import(S(31))
local UserInputService = game:GetService(S(860))
local GuiService = game:GetService(S(88))
local THEME = {
    BrandName = S(178),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(39))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(289))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(156))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target)
    local dragging, dragInput, dragStart, startPos
    local state = { moved = false }
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            state.moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                state.moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections, state
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(787))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(295)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(220))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(540))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(265)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(357))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(683)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(887))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(1030))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(389))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(965))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(757))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(320))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(951))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(7))
            if not valueLbl then
                valueLbl = Instance.new(S(659))
                valueLbl.Name = S(359)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(663))
            if not badge then
                badge = Instance.new(S(228))
                badge.Name = S(760)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(83))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(922))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(764))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(878))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(236)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(493)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(674))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(125))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(658))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(739)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(507))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(754))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(58))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(436)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(336))
    screenGui.Name = S(445)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(971))
    local main = Instance.new(S(987))
    main.Name = S(1009)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(226))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(394))
    tabBar.Name = S(427)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(522))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(244))
    header.Name = S(367)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(347))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(643))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(834))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(464)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(832))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(161)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(911))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(54)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(135))
    searchBox.Name = S(9)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(568))
    searchBox.Text = S(723)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(362))
    content.Name = S(469)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(765))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(950))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(102))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(160))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    local avatarConnections, avatarDrag = makeDraggable(avatar, main)
    for _, connection in ipairs(avatarConnections) do
        table.insert(connections, connection)
    end
    table.insert(connections, avatar.Activated:Connect(function()
        if minimized and not avatarDrag.moved then
            setMinimized(false)
        end
    end))
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(558)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(451) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(323) .. (item.Subtitle or S(132))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(173))
        footerLabel.Text = i18n.T(S(704))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(566)] = function()
return function(import)
local gameApi = import(S(898))(import)
local log = import(S(902))
local runGuarded = import(S(888))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(974) .. minItemsToCollect .. S(986))
    else
        log(S(767))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(165) then
            log(S(214))
            lastReported = S(745)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(499) then
            log(S(476))
            lastReported = S(1021)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(515) then
            local capacity = gameApi.ChestCapacity()
            log(S(56) .. count .. S(240) .. want .. S(892)
                .. (capacity and (S(62) .. capacity .. S(158)) or S(693)))
            lastReported = S(701)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(203), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(791) .. count .. S(1040) .. after .. S(626))
        else
            log(S(415) .. count
                .. S(334) .. tostring(after) .. S(772))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(345)] = function()
return function(import)
local gameApi = import(S(678))(import)
local log = import(S(916))
local runGuarded = import(S(604))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(901) .. (enabled and S(431) or S(150)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(815) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(979) .. reward.category .. S(563)
                .. tostring(reward.tierKey) .. S(222) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(477) .. #pending .. S(569))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(1029), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(348)] = function()
return function(import)
local getCurrentPlot = import(S(600))
local getPlotParts = import(S(296))
local log = import(S(544))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(291), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(404) .. (enabled and S(197) or S(695)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(1034)] = function()
return function(import)
local Players = game:GetService(S(669))
local getCurrentPlot = import(S(337))
local getPlotParts = import(S(984))
local getMinerSlots = import(S(1011))(import)
local teleport = import(S(139))
local parseMoney = import(S(576))
local gameApi = import(S(416))(import)
local log = import(S(881))
local runGuarded = import(S(552))(import)
local spendPolicy = import(S(822))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(467)
local LOCKED_SLOT_TEXT = S(783)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(356))
    return pad and pad:FindFirstChildWhichIsA(S(315), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(803)) then
            local digits, suffix = descendant.Text:match(S(1022))
            if digits then
                local value = tonumber((digits:gsub(S(676), S(410))))
                if value then
                    if suffix ~= S(224) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(686)
    return (text:gsub(S(439), S(452)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(169)) and descendant.Text:match(S(584)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(497) .. holder.Name .. S(269)
            .. gameApi.MinerSlots() .. S(321) .. gameApi.MaxMinerSlots() .. S(769)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(688)
    end
    if not prompt.Enabled then
        return true, S(1045)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(946) .. price
    end
    local action = prompt.ActionText or S(924)
    if action:match(S(921)) or action:match(S(630)) or action:match(S(848)) then
        return true, S(35) .. action .. S(196)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(504))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(556) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(21) .. why .. S(223))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(96))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(312) .. prompt.ObjectText
                    .. S(520) .. shown .. S(66) .. math.floor(base) .. S(918))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(541))
    if line ~= lastSurvey then
        log(S(730) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(86), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(880), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(1053)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(847), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(696) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(744))
    else
        lastAttempt = 0
        log(S(506))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(825),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(157)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(508)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(749)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(852))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(729) .. holder.Name .. S(826))
            return false
        end
        log(S(481) .. candidate.slot .. S(648) .. candidate.name
            .. S(856) .. tostring(why))
    end
    if not miner then
        log(S(897))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(363) .. holder.Name .. S(218) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(886) .. miner.name .. S(144) .. miner.power
        .. S(364) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(371) .. entry.name .. S(890) .. entry.holder.Name
            .. S(37) .. tostring(entry.prompt.ActionText) .. S(1038))
        return false
    end
    log(S(30) .. entry.name .. S(726) .. entry.power
        .. S(396) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(107) .. pad.holder.Name .. S(440)
                .. tostring(pad.priceText))
        else
            log(S(381) .. pad.holder.Name
                .. S(551) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(679) .. holderName .. S(205)
            .. string.format(S(1018), before) .. S(179) .. string.format(S(175), after)
            .. S(885) .. SWAP_COOLDOWN .. S(670))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(281)
            .. string.format(S(827), bestBefore) .. S(700)
            .. string.format(S(959), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(1001), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(430))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(377) then
        runGuarded(S(378), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(134) then
        log(S(727) .. target.holder.Name
            .. S(731) .. tostring(target.priceText))
        runGuarded(S(711), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(759) .. target.holder.Name .. S(282) .. target.name
        .. S(231) .. target.power .. S(651) .. best.name .. S(261) .. best.power .. S(4))
    runGuarded(S(193), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(149)] = function()
return function(import)
local gameApi = import(S(434))(import)
local parseMoney = import(S(422))
local log = import(S(692))
local runGuarded = import(S(534))(import)
local spendPolicy = import(S(1047))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(500) .. (enabled and S(917) or S(405)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(614)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(42) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(703) .. deepestKey .. S(578))
        return true
    end
    log(S(857) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(411)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(26) .. gameApi.ZoneDisplayName(zoneKey) .. S(779)
                .. math.floor(price) .. S(268) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(471) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(322) .. zoneKey .. S(155))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(1008) then
            reportedLocked = S(634)
            log(S(152) .. zoneKey .. S(104)
                .. gameApi.ZoneDisplayName(currentKey) .. S(589)
                .. done .. S(642) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(705)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(327), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(141)] = function()
return function(import)
local gameApi = import(S(553))(import)
local log = import(S(631))
local runGuarded = import(S(234))(import)
local spendPolicy = import(S(163))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(219) .. (enabled and S(19) or S(639)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(368) .. node.zoneKey .. S(23) .. node.id
                    .. S(543) .. node.family .. S(324) .. node.level .. S(872) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(961) .. node.id
                    .. S(189) .. node.family .. S(387) .. node.level .. S(747) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(397) .. blocked
                .. S(290))
        elseif powerBlocked then
            log(S(92) .. powerBlocked.zoneKey .. S(763) .. powerBlocked.id
                .. S(735) .. powerBlocked.family .. S(562)
                .. math.floor(powerBlocked.powerReq) .. S(782)
                .. math.floor(powerBlocked.power)
                .. S(185))
        else
            log(S(349)
                .. S(485) .. math.floor(gameApi.GetMoney()) .. S(818))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(523) .. nearest.zoneKey
                    .. S(1000) .. nearest.id .. S(278) .. nearest.family .. S(537)
                    .. nearest.level .. S(833) .. nearest.max
                    .. S(399) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(812)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(474),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(813))
        end
        if not buyCore then
            table.insert(parts, S(1044))
        end
        if #parts == 0 then
            return S(308)
        end
        return S(332) .. table.concat(parts, S(611)) .. S(530)
    end
    local why
    if nearest.cost > budget then
        why = S(479) .. string.format(S(905), nearest.cost)
            .. S(186) .. string.format(S(127), money)
    elseif nearest.powerOk == false then
        why = S(820) .. string.format(S(828), nearest.powerReq) .. S(549)
    else
        why = S(993)
    end
    return S(808) .. nearest.zoneKey .. S(894) .. tostring(nearest.id)
        .. S(117) .. tostring(nearest.family) .. S(647) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(736) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(555) and cost > best)
            or (mode ~= S(684) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(778)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(785), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(154)] = function()
return function(import)
local Workspace = game:GetService(S(342))
local gameApi = import(S(957))(import)
local teleport = import(S(15))
local log = import(S(937))
local runGuarded = import(S(896))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(1027))
    else
        lastAttempt = 0
        log(S(657))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(547))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(503), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(519)
                .. (sold and S(225)
                         or S(595)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(358) .. (sold and (S(717) .. #keys .. S(301)) or S(925)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(403), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(737)] = function()
return function(import)
local Workspace = game:GetService(S(340))
local gameApi = import(S(76))(import)
local teleport = import(S(303))
local log = import(S(992))
local runGuarded = import(S(167))(import)
local spendPolicy = import(S(209))(import)
local STATS = { S(277), S(495), S(408) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(661)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(242) .. (enabled and S(91) or S(1031)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(690) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(605) or S(618)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(1015) .. holderKey .. S(298) .. choice.stat .. S(48)
                        .. choice.info.level .. S(275) .. after.level
                        .. S(43) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(133) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(960))
    local shop = stand and stand:FindFirstChild(S(770))
    if not shop then
        return nil
    end
    if shop:IsA(S(983)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(814), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(533)
                .. (bought and S(685)
                           or S(17)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(797))
        end
    end
    if bought then
        log(S(529) .. pickaxe.index .. S(286) .. pickaxe.name
            .. S(1) .. pickaxe.multiplier .. S(63))
        return true
    end
    log(S(869) .. pickaxe.index .. S(250) .. pickaxe.name
        .. S(908))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(465) .. holderKey .. S(798)
                    .. tostring(worn or S(292)) .. S(119) .. wanted.name)
            else
                log(S(470) .. holderKey .. S(594)
                    .. wanted.name .. S(46) .. tostring(nowWorn or S(235)) .. S(841))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(956) .. holderKey .. S(49) .. variant
                .. S(1003) .. after .. S(564) .. cost .. S(934))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(80))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(734) .. gameApi.OwnedPickaxeCount()
                    .. S(1019) .. gameApi.MaxMinerSlots()
                    .. S(260))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(627))
        elseif maxed > 0 then
            log(S(339))
        else
            log(S(482))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(947) and price > best)
            or (mode ~= S(264) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(164))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(912), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(780)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(909))
local Workspace = game:GetService(S(560))
local log = import(S(612))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(112), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(388) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(253))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(719) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(966) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(45) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(273) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(354) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(309))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(667) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(12))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(146), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(438) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(859) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(761) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(840), keys)
end
function api.EquipSlot(slot)
    return send(S(936), slot == S(879) and S(6) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(531), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(1020), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(513), holderKey)
end
local describedShapes = false
function api.DescribeShapes()
    if describedShapes or not modules() then
        return
    end
    describedShapes = true
    local mine = api.GetMineData()
    if not mine then
        log(S(455))
        return
    end
    local function describe(value)
        if type(value) ~= S(192) then
            return tostring(value)
        end
        local parts = {}
        for key, item in pairs(value) do
            table.insert(parts, tostring(key) .. S(385) .. tostring(item))
        end
        table.sort(parts)
        return S(344) .. table.concat(parts, S(195)) .. S(698)
    end
    pcall(function()
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(128) and holder.CharName then
                log(S(796) .. holderKey .. S(245) .. describe(holder))
                log(S(999) ..
                    tostring(mineConfig.GetHolderVariant(holder))
                    .. S(419) ..
                    tostring(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
                    .. S(390) .. tostring(mineConfig.GetHolderDamage(holder))
                    .. S(978) .. tostring(mineConfig.GetHolderLevel(holder)))
                break
            end
        end
    end)
    pcall(function()
        local shown = 0
        for slot, key in pairs(mine.Hotbar or {}) do
            log(S(288) .. tostring(slot) .. S(753) .. tostring(key)
                .. S(262) .. tostring(mineConfig.ParseCharStack(key)))
            shown = shown + 1
            if shown >= 3 then
                break
            end
        end
        if shown == 0 then
            log(S(866))
        end
    end)
    pcall(function()
        local entries = mineInventory.Collect(mine, S(1051), mineInventory.KindOrder.OreFirst)
        local shown = 0
        for _, entry in pairs(entries or {}) do
            if entry.Kind == S(873) then
                log(S(1041) .. describe(entry))
                shown = shown + 1
                if shown >= 2 then
                    break
                end
            end
        end
        if shown == 0 then
            log(S(105))
        end
    end)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(995))
end
function api.BuySkill(zoneKey, family)
    return send(S(861), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(136)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(97)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(159))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(635) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(525) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(492), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(636))
    plots = plots and plots:FindFirstChild(S(790))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(202)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(653)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(206))
    local gui = board and board:FindFirstChild(S(437))
    local root = gui and gui:FindFirstChild(S(200))
    local list = root and root:FindFirstChild(S(280))
    local row = list and list:FindFirstChild(S(1025) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(602)) and descendant.Text ~= S(472) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(510) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(532), characterKey, stat == S(257) and S(187) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(241), max = S(795), cost = S(903) },
    Speed = { level = S(668), max = S(969), cost = S(432) },
    MineSpeed = { level = S(521), max = S(935), cost = S(77) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(307) or charName == S(970) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(718) .. tostring(value))
        end
        table.sort(fields)
        log(S(821) .. holderKey .. S(699) .. table.concat(fields, S(998)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(1017) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(75) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(372) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(722) and type(holder.CharName) == S(1002) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(483), S(350) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(1046) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(53)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(994)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(201)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(120) then
        return {}, S(395)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(680)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(756) or type(state.Track) ~= S(877) then
                table.insert(notes, tabKey .. S(920) .. tostring(category) .. S(938))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(743) .. waiting
                        .. S(829))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(710) .. total .. S(577)
            .. (#notes > 0 and (S(1004) .. table.concat(notes, S(352))) or S(331))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(230) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(574)] = function()
return function(import)
local Players = game:GetService(S(844))
local gameApi = import(S(774))(import)
local log = import(S(713))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(777))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(16), S(870))))
    if not value then
        return nil
    end
    if suffix ~= S(417) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(496))
    local mineGui = playerGui and playerGui:FindFirstChild(S(1050))
    local hotbar = mineGui and mineGui:FindFirstChild(S(862))
    local content = hotbar and hotbar:FindFirstChild(S(391))
    return content and content:FindFirstChild(S(762))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(204)) then
            local count = descendant.Text:match(S(771))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(47))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(361))
    local nameLabel = content and content:FindFirstChild(S(229))
    local damageLabel = content and content:FindFirstChild(S(293))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(99) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(143) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(274) .. slot .. S(484) .. info.name
                        .. S(904) .. info.power .. S(810) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(836)] = function()
local Players = game:GetService(S(672))
local Workspace = game:GetService(S(776))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(1042))
    local plots = mineWorld and mineWorld:FindFirstChild(S(1037))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(457)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(270)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(442)),
        Nodes = plot:FindFirstChild(S(981)),
        Chest = plot:FindFirstChild(S(85)),
        Zones = plot:FindFirstChild(S(306)),
        Crew = plot:FindFirstChild(S(124)),
        Loot = plot:FindFirstChild(S(750)),
        PlotIndex = plot:GetAttribute(S(1006)),
        ChestCapacity = plot:GetAttribute(S(103)),
    }
end
return getPlotParts
end
modules[S(518)] = function()
return function(import)
    local createWindow = import(S(715))(import)
    local i18n = import(S(501))
    local antiAfk = import(S(51))(import)
    local buildEnv = import(S(1028))
    local scheduler = import(S(535))(import)
    local spendPolicy = import(S(475))(import)
    local config = import(S(583))(import)
    local log = import(S(70))
    log(S(559) .. buildEnv.Commit .. S(741) .. buildEnv.Env
        .. S(608) .. buildEnv.BuiltAt .. S(3))
    local window = createWindow({
        Subtitle = S(466),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(461))(import)
    local autoPlace = import(S(758))(import)
    local autoChest = import(S(459))(import)
    local autoSell = import(S(570))(import)
    local autoUpgrade = import(S(591))(import)
    local autoResearch = import(S(863))(import)
    local autoClaim = import(S(644))(import)
    local autoPlot = import(S(8))(import)
    local gameApi = import(S(505))(import)
    scheduler.RegisterStandalone(S(751), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(550), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(955), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(79), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(380), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(517), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(355), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(463), 20)
    scheduler.RegisterExclusive(S(954), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(93), autoResearch.NextPrice)
    spendPolicy.Register(S(548), autoUpgrade.NextPrice)
    spendPolicy.Register(S(65), autoPlot.NextPrice)
    spendPolicy.Register(S(449), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(768), S(561))
    farmTab:AddSectionHeader(S(191))
    persistentSegmented(
        farmTab,
        { S(952), S(809), S(1033) },
        { S(697), S(170), S(29) },
        S(319), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(59),
        S(843)
    ), S(468), autoMine.SetEnabled)
    persistentSlider(farmTab, S(326), S(255),
        1, 30, S(638), 5, autoMine.SetCyclesPerSecond, S(333))
    persistentSlider(farmTab, S(366), S(871),
        1, 10, S(420), 1, autoMine.SetClicksPerCycle, S(716))
    persistentToggle(farmTab:AddCard(
        S(487),
        S(610)
    ), S(287), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(819), S(853),
        1, 60, S(929), 3, autoPlace.SetCheckInterval, S(453))
    persistentToggle(
        farmTab:AddCard(S(446),
            S(90)),
        S(865), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(800),
            S(980)),
        S(5), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(271), S(593),
        5, 300, S(28), 60, autoPlace.SetEquipBestInterval, S(215))
    persistentToggle(
        farmTab:AddCard(S(706),
            S(121)),
        S(116), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(545), S(958),
        0, 500, S(910), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(609))
    persistentToggle(farmTab:AddCard(
        S(72),
        S(317)
    ), S(114), autoChest.SetEnabled)
    persistentSlider(farmTab, S(868), S(232),
        50, 3000, S(447), 100, autoChest.SetMinItems, S(899))
    persistentSlider(farmTab, S(176), S(318),
        1, 60, S(109), 3, autoChest.SetCheckInterval, S(383))
    persistentToggle(farmTab:AddCard(
        S(376),
        S(20)
    ), S(1013), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(773),
            S(328)),
        S(943), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(41), S(10),
        5, 300, S(246), 20, autoSell.SetCheckInterval, S(619))
    farmTab:AddSlider(S(991),
        S(448),
        5, 120, sellMaxWait, function(value)
            config.Set(S(893), value)
        end, S(972))
    persistentToggle(farmTab:AddCard(
        S(370),
        S(572)
    ), S(369), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(38), S(646),
        5, 300, S(239), 20, autoUpgrade.SetInterval, S(874))
    persistentSlider(farmTab, S(256), S(162),
        1, 20, S(111), 5, autoUpgrade.SetMaxPerPass, S(129))
    persistentSegmented(
        farmTab,
        { S(579), S(907), S(207), S(603) },
        { S(311), S(316), S(313), S(429) },
        S(557), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(927), S(932),
        0, 500, S(586), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(73))
    persistentToggle(
        farmTab:AddCard(S(781),
            S(597)),
        S(153), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(267) .. pickaxe.multiplier .. S(988)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(87)
            end
            return S(460) .. index .. S(177) .. (pickaxeLabels[index] or S(662))
        end
        local savedPick = config.Get(S(33), 0)
        pickaxeCard = farmTab:AddSlider(
            S(414),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(786), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(746)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(393),
            S(708)),
        S(511), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(418),
            S(527)),
        S(55), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(588),
            S(554)),
        S(151), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(823), S(443),
        1, 20, S(933), 2, autoUpgrade.SetMutationTarget, S(830))
    persistentSlider(farmTab, S(666), S(182),
        0, 500, S(50), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(596))
    persistentToggle(farmTab:AddCard(
        S(198),
        S(953)
    ), S(263), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(962), S(138),
        10, 600, S(867), 30, autoResearch.SetCheckInterval, S(302))
    persistentSlider(farmTab, S(512), S(131),
        1, 20, S(806), 5, autoResearch.SetMaxPerPass, S(100))
    persistentSlider(farmTab, S(539), S(846),
        0, 500, S(1016), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(682))
    persistentToggle(
        farmTab:AddCard(S(573),
            S(687)),
        S(689), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(702),
            S(433)),
        S(454), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(98),
            S(811)),
        S(249), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(14), S(351) },
        { true, false },
        S(875), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(305),
        S(304)
    ), S(709), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(792), S(480),
        5, 300, S(720), 30, autoClaim.SetCheckInterval, S(799))
    persistentSlider(farmTab, S(968), S(190),
        1, 30, S(106), 10, autoClaim.SetMaxPerPass, S(1043))
    persistentToggle(farmTab:AddCard(
        S(640),
        S(766)
    ), S(923), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(365), S(837),
        5, 300, S(444), 30, autoPlot.SetCheckInterval, S(712))
    persistentToggle(
        farmTab:AddCard(S(171),
            S(675)),
        S(252), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(374),
            S(425)),
        S(616), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(375), S(78),
        0, 500, S(681), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(650))
    if buildEnv.IsDev then
        task.delay(8, gameApi.DescribeShapes)
    end
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(248))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(598), S(641))
    persistentToggle(
        settingsTab:AddCard(S(622), S(353)),
        S(486),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(1023), 100)
    settingsTab:AddSlider(S(237), S(300), 50, 150, savedScale, function(value)
        config.Set(S(997), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(788), 1)
    settingsTab:AddSegmented({ S(294), S(664), S(1024) }, savedLanguage, function(index, value)
        local code = ({ S(285), S(208), S(406) })[index]
        config.Set(S(247), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(842), S(199), S(516) })[savedLanguage] or S(775))
    log(S(341) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(489)] = function()
return {
    [79389059854988] = S(61),
}
end
import(S(194))(import)
