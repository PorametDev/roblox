-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"PlayerGui", "[Remotes] === total: ", "Dump Visible GUI", "GetHolderSpeedLevel", "Frame", "games/my_anime_mine/auto_claim", "[Interaction] ProximityPrompt triggered: ", "\n", ")", "games/my_anime_mine/game_api", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "TextLabel", "research", " ชิ้น", "core/logger", "autoResearchKeepMoney", "core/run_job", "table", "Auto Research: กันเงินไว้", "AutoUpgrade", " (power ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "UIPadding", "Text", "PickaxesStand", "Loot", "autoClaimInterval", "core/anti_afk", "TextLabel", "games/my_anime_mine/auto_upgrade", "leaderstats", "%.0f", "games/my_anime_mine/get_plot_parts", "[AutoResearch] nothing open and affordable right now", " - upgrade the miners first", ": ", "TextLabel", "0 = ไม่จำกัด", "may need us at the stand - trying that", "[GameAPI] holder ", "Auto Sell", "ClickDetector", "M", "table", " still empty after placing ", "table", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "[GuiDump] PlayerGui not found", "unlock", ") ", "ขนาดเมนู", ") ", "AutoClaim", "Frame", " but no unclaimed reward in its Track", "RemoteEvent", "TextButton", "Workspace", ": ", "core/teleport", " owned)", "[AutoResearch] Core ", "#", "[Decompile] ", "", "games/my_anime_mine/auto_mine", "unreadable", "no", "Players", "/", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "Auto Chest: รอบเช็ค", "getsenv", "autoUpgradeKeepMoney", "s", "disabled", "[AutoUpgrade] ", "Auto Place: ปลดล็อกช่อง miner", "[AutoUpgrade] ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "dearest", " (x", "enabled", "UICorner", "autoUpgradeMutationBudget", "TextLabel", "nothing claimable", "UserInputService", "autoChest", "GuiService", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "ไล่ซื้อทุกกี่วินาที", "CharMineSpeedMaxLevel", ")", " (Text=", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "games/my_anime_mine/main", "Players", "Frame", "games/my_anime_mine/get_current_plot", "lowest", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "AutoResearch", "table", "TextLabel", "Open menu", " items (now ", " items)", "", "UICorner", "menuScale", "[AutoPlace] upgrading ", "autoPlot", "/", "none", "^x(%d+)$", "core/humanoid", " (cost ", "Zone buying is off", "sold ", "Frame", "search_placeholder", "/", "Workspace", "games/my_anime_mine/auto_sell", " waited ", "needs ", "languageIndex", " -> ", " while selecting", "autoPlaceInterval", "Auto Research: ซื้อสาย Zone", "Character", "buying the ", "autoPlotBuy", " screens", "core/logger", "core/get_place_id", "CharMaxLevel", "[Interaction] Button activated: ", "walkSpeed", "enabled", "", "", "server didn't equip it", "no mine data", "core/logger", "cost", ": ", "MineWorld", " still locked after paying ", " '", "table", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "core/get_place_id", "games/my_anime_mine/auto_plot", "[AutoUpgrade] ", " -> ", "Frame", "research", " ", "List", "[AutoChest] enabled (collects once >= ", "TextLabel", "ตามเครื่อง", ")", "table", "disabled", "MineEquipPickaxe", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "disabled", "Auto Plot: ใช้โซนล่าสุดเสมอ", "มุดแมพ (Noclip)", "Mutation: หยุดเมื่อได้ x", "Auto Sell: รอบขาย", "GuiButton", "ModuleScript", "x", "AutoResearch", "_", "TextLabel", "autoResearchZones", " (x", " ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "auto", " (", "waiting", "AutoPlace", "x", "BodyPosition", "empty", "[BestMiner] slot ", "x", "^Swap out ", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "core/run_job", "AutoPlot", " refused - ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Dump", "MineSpeed", " pickaxe ", "แรงกระโดด", " -> ", "TextButton", " ", " research is ", "%.3g", ") - leaving it alone for ", "auto", "autoResearchCoreLast", "string", ": badge says ", "Auto Research: ซื้อสาย Core", "autoPlotLatest", ", ", "games/my_anime_mine/game_api", "[AutoPlace] nothing in the hotbar to place", "LayerCollector", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "HttpService", "/", "PlayerGui", " ParseCharStack=", "core/logger", "games/my_anime_mine/game_api", " wouldn't take ", "%.3g", "Frame", "function", "Auto Sell: รอคิวได้นานสุด", "[AutoSell] MineSellItems ", "Hauler", ") didn't take: ", "Text", "s", "[AutoPlot] ", "locked slot", "games/my_anime_mine/auto_mine", "ความเร็ววิ่ง", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "autoUpgradeMaxPerPass", "ProximityPrompt", "MekPro", "[AutoPlace] unlocked ", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "autoUpgradePriority", " failed)", "UIListLayout", "autoClaimMaxPerPass", "AutoChest", "AutoUpgrade", "[Remotes] ", "=", "Decompile Client Scripts", "games/my_anime_mine/game_api", "core/logger", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", " (", "'", "costs ", "%.0f", "autoUpgradeInterval", "Auto Upgrade: รอบเช็ค", ",", "Players", ", you have ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", ",", "MineRollMutation", "[AutoUpgrade] ", " costs ", "[AutoUpgrade] MineBuyPickaxe ", "GetCharacterMineSpeedUpgradeCost", "Core buying is off", "Mining", "[AutoMine] ", "string", "VirtualUser", "Auto Upgrade: Pickaxe ที่จะซื้อ", " for ", "core/build_env", "[GameAPI] shapes: Hotbar[", "autoMineSpeed", "table", "HotbarFrame", "PlayerGui", "X", "[AutoUpgrade] pickaxe #", "/config.json", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "[AutoPlace] unlocking ", "[AutoPlot] ", "table", "AutoPlot", "AutoSell", "OwnerUserId", " didn't go through (out of stock or too expensive)", "MekPro.log", " screen(s)) ===", " (", "ReplicatedStorage", "dearest", "games/my_anime_mine/game_api", "MineSpeed", "autoUpgradePickaxeIndex", " reward(s) looked claimable but none went through", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "", "BodyVelocity", " -> ", "core/debug_interaction_logger", "%.3g", "TextButton", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "  [attr] ", " team power, you have ", "Auto Research (Plot)", "neither appendfile nor writefile is available", "[AutoPlace] ", "GuiObject", "affordable - waiting for a turn", "[AutoSell] enabled", " -> x", "[MekPro] settings file: ", "float", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "table", "Root", "Damage", "แพงสุดก่อน", "", "string", "[AutoChest] disabled", ", have ", "Auto Upgrade Unit", "core/debug_dump_remotes", "interactionLogger", "needs us at the stand - keeping the teleport", "[GameAPI] couldn't load the game's modules: ", "[^%w]+", ")", "AutoPlot", "OwnerUserId", "/", "core/run_job", "off", "Plots", "getscripts", "[AutoPlace] ", "[AutoChest] chest is empty", "[Decompile] decompile unavailable in this executor", "games/my_anime_mine/game_api", "[Scheduler] turn: ", "core/spend_policy", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "UserInputService", "Core", "autoResearch", " doesn't exist", " files", "core/run_job", "BasePart", "Dump All Remotes", "string", ",", "[Interaction] logger disabled", "MineEquipSlot", "Exclusive", "ปุ่มเปิดเมนู", " (", "[AutoClaim] idle: ", "core/logger", "Backpack", "🐛", "autoSell", "core/dispatch", "Frame", "Content", " ===", "open_menu_hint", "function", "MekPro", "autoUpgradeEquipPickaxe", ") needs ", "Content", "[%X] ", "table", "games/my_anime_mine/auto_plot", "[Decompile] wrote ", "en", "Auto Chest: เกณฑ์เก็บ", "unlock", "core/scheduler", "ไทย", "Pad", "games/my_anime_mine/auto_place", "AutoClaim", "x", "[AntiAFK] idle detected, simulating input", "", "Shop Stand", "core/logger", "core/parse_money", "HumanoidRootPart", "", "none", "disabled", "TextLabel", "core/i18n", "ItemName", "ValueLabel", "[AutoResearch] ", " = ", "[AutoPlot] couldn't switch to ", "core/i18n", "autoPlotKeepMoney", "[AutoPlot] unlocked ", "[AutoPlace] drifted out of range of ", "เช็คโซนทุกกี่วินาที", "AutoPlace", "[GameAPI] shapes:   GetHolderVariant=", "Auto Claim (Index)", "MineSellItems", "Auto Claim: รอบเช็ค", "[MekPro] build ", "Auto Plot: กันเงินไว้", "MinePower", "core/config", "yes", "antiAfk", "เดินทะลุกำแพงและพื้น", "core/config", "cheapest", "[AutoPlot] ", "[SpendPolicy] ", "th", "GetHolderLevel", " GetHolderLevel=", "[", "[GameAPI] SendEvent failed: ", "[GuiDump] === end (", "ZoneBoardGui", "[AutoUpgrade] bought pickaxe #", "ValueLabel", "Character", "[GameAPI] shapes: Holders.", "", "games/my_anime_mine/game_api", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "TextButton", "every open node is maxed", "core/logger", "Frame", "Auto Mine: ความเร็ว", "upgrade", "%.3g", "AutoPlace", "%$", "[AutoPlace] picked up ", "[Decompile] PlayerScripts not found", "([%d,%.]+)%s*(%a*)", "GuiButton", ") on ", "TextBox", "/", "autoChestMinItems", " | ", "[AutoChest] can't read the chest yet", "", "ItemFrame", "s", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "off - every automation spends on its own", "MekPro", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "/s", "no isfile/readfile available to verify the write", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "", "'", "%.0f", "cheapest", "games/registry", "games/my_anime_mine/auto_claim", "] = ", "string", "?", "[AutoPlace] couldn't hold anything - not pressing E", "[AutoUpgrade] every stat on every miner is maxed", "] job failed: ", "%.3g", "core/logger", "core/logger", "Humanoid", "[Scheduler] ", "", "[Interaction] ClickDetector clicked: ", "spendOrder", "ScreenGui", "sale didn't go through", "core/logger", "fill", "TextButton", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "number", "debug.getconstants", "Players", "MineTakeChest", " (ActionText=", "core/logger", "empty", ")", "Run", "Workspace", "ZoneRow", "core/debug_decompile", "My Anime Mine · Mining Hub", "core/logger", " pickaxe(s) for ", "AutoPlace", "action '", "ProximityPrompt", "AutoUpgrade", "core/debug_dump_gui", "", "core/build_env", ", base ", "enabled", "Zones", "Speed", "core/spend_policy", "works from anywhere - no teleport needed", "core/i18n", "ClickDetector", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "", ": State(", "s", "Auto Upgrade: สุ่ม Mutation", "Auto Mine", "Locked miner slot", "languageIndex", "open_menu_hint", "[AutoResearch] ", ")", "TextButton", "RunService", "[SpendPolicy] ", "s", "Auto Mine: คลิกต่อรอบ", "[Bb]uy", " (", "Speed ก่อน", "TextLabel", "", "games/my_anime_mine/get_current_plot", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "table", "TextButton", "Plots", "works from anywhere - dropping the teleport", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "-", ")", "0 = ไม่จำกัด", "getscriptbytecode", "[Decompile] === start ===", "dev", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Badge", "fill", "อังกฤษ", " (", "ไล่อัปเกรด miner ทุกกี่วินาที", "Auto Place: เพดานราคาปลดช่อง", "TextLabel", "core/logger", "dearest", "core/run_job", "Container", " claimable but none could be resolved", "GetCharacterUpgradeCost", "^%$", "UIListLayout", " possible holder(s) - not buying more", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "games/my_anime_mine/auto_chest", "All", "fly", "table", "Players", "[Decompile] === end ===", "core/debug_interaction_logger", "[AutoPlace] ", "autoResearchCheapestFirst", "[AutoClaim] claimed ", "[AutoChest] MineTakeChest had no effect (", "enabled", "", ".lua", "Frame", "AutoPlace", "Content", "d9948b6+", "[Interaction] Key pressed: %s (gameProcessed=%s)", "BasePart", " -> ", "[GameAPI] shapes: no Character entries in the inventory", "autoSellMaxWait", "Damage", " node(s) looked buyable but the server refused", "AutoResearch", "", "table", "core/logger", "(nothing to do)", "%.3g", "menuScale", "[AutoSell] ", "cheapest open node ", "AutoMine", " (", "อ่านป้ายบน chest ทุกกี่วินาที", "autoClaim", "autoMineClicks", "s", "TextButton", "slot past the usable cap", "Frame", "(", "'", "button has no size on screen", "บิน (Fly)", ") -> ", "table", "Crew", "ตั้งค่า", "MekPro", "M", "TextLabel", "Auto Chest", "dearest", "Workspace", "BasePart", "^M(%d+)$", "th", ") ", "jumpPower", "Chest", "UserInputService", "games/my_anime_mine/get_best_miner", "unreadable", "needs ", ")", "auto", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "core/parse_money", "s", "x", "[GameAPI] shapes: Hotbar is empty", "nothing left to buy (", "🏠", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "autoUpgradePickaxeIndex", ") gave nothing", "both Zone and Core buying are switched off", "=locked(", "MineBuySkill", "core/logger", "2026-09-21 13:11:57", "en", "core/teleport", "autoPlaceUpgradeWeak", "slot not in the hotbar", "[Config] couldn't write ", "core/spend_policy", "Auto Claim: เก็บสูงสุดต่อรอบ", "core/spend_policy", " '", "[Scheduler] ", "ZoneBoard", "", "WalkSpeed (ปกติ 16)", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "Auto Place Miner", " power ", "Players", "Header", "HumanoidRootPart", "AutoPlot", ": ", " = ", "Auto Place: รอบเช็ค", " #", "core/get_place_id", "Humanoid", "[AutoPlot] now mining ", " (", "PlotIndex", " ", "UIStroke", "", "M", "[GuiDump] ", "[GameAPI] shapes: inventory entry ", "; ", "/", "ดีบัก", "games/my_anime_mine/auto_research", "[AutoPlace] holders: ", " GetVariantMult=", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", ")", "[AutoPlace] placed ", "MineSpeed", "  ", "GetHolderMineSpeedLevel", "UIScale", "games/my_anime_mine/game_api", "AutoPlace", "=empty", "", "search_placeholder", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "games/registry", "", "Players", " (money ", "[GuiDump] === visible PlayerGui tree ===", "%$%s*([%d,%.]+)%s*(%a*)", "MineBuyPickaxe", "/", " ", "games/my_anime_mine/game_api", "Mutation: เพดานราคาต่อครั้ง", "string", "TextLabel", "[AutoClaim] ", " has been busy for ", " is no better after the swap (", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "{", " script(s) to ", " (", "core/debug_tab", "AutoSell", ")", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "UIListLayout", "%$", "[AutoChest] waiting: ", "MineUpgradeCharacter", "games/my_anime_mine/auto_sell", "cost", ")", "AutoUpgrade", "s", "Players", "[Interaction] logger enabled - go click/interact with things now", "autoPlotInterval", "Search functions...", "badge says ", "TextLabel", "PlayerScripts", "", " mutation x", " fields: ", "[AutoResearch] ", "Hauler", " - ", "modules not loaded", " -> ", "ต่ำสุดก่อน", "core/debug_decompile", "core/ui", "Main", "", "[AutoPlace] couldn't pick up ", "All", "[AutoResearch] ", "[AutoSell] disabled", "getgc", "BasePart", "lowest", " ", ")", "autoUpgradeRollMutation", "games/my_anime_mine/auto_place", "%", "[Scheduler] ", " (still ", "LocalScript", "Auto Upgrade Plot (Zone)", "table", "core/run_job", " ", "ClaimableTiers failed", "Empty miner slot", "\n", "core/dispatch", " ", "ยิง Equip Best ทุกกี่วินาที", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "", "Badge", " team power", "TextLabel", "s", "[AutoChest] collected ", "CoreGui", "dearest", "[AutoPlace] Equip Best: hotbar's strongest ", "th", "index rewards are disabled in this game build", " thing available first", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", " (power ", "games/my_anime_mine/get_plot_parts", "ClickDetector", "core/ui", "debug.getproto", "slot ", "AutoUpgrade", "nothing to place or upgrade", "MineWorld", "getcallbackvalue", "CharSpeedMaxLevel", "core/debug_tab", "core/click", "core/humanoid", "Players", "M", "}", " ", "Limited", " GetHolderDamage=", "=", "autoSellMaxWait", "Auto Research: รอบเช็ค", "maxed", "", "", "waiting", "s - jumping the queue", "Workspace", "This is not a supported place.", "[AutoPlace] slot ", "autoUpgradePickaxe", "getconnections", " ", "core/spend_policy", "RunService", "Auto Plot: รอบเช็ค", "core/logger", "%.3g", "Damage ก่อน", "ChestCapacity", "games/my_anime_mine/get_best_miner", "en", "s - carrying on without it", "[AutoUpgrade] ", " waited ", "')", ")", " (chest holds ", "", "Frame", "core/scheduler", "AutoResearch", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "games/my_anime_mine/get_plot_parts", "RemoteFunction", "ต่างคนต่างซื้อ", ")", "[AutoResearch] idle: ", "table", "ถูกสุดก่อน", "s behind a purchase that never happened - letting it spend", "getloadedmodules", "แพงสุดก่อน", " ", "getnilinstances", "[Decompile] === executor capabilities ===", ") from ", "[AutoPlace] ", "[AutoClaim] ", "ProximityPrompt", " -> ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "autoUpgradeMutationTarget", "ขายทุกกี่วินาที", "AutoChest", "Nodes", "[Logger] failed to write ", " (", "table", "Mining ก่อน", "noclip", "Dump", "core/run_job", "core/anti_afk", " ", "autoPlace", "MekPro/decompiled", "Auto Plot: ซื้อโซนถัดไป", "getrenv", "core/logger", "ProximityPrompt", "TextLabel", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "", "debug.getupvalues", "ลอย (Float)", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "games/my_anime_mine/auto_upgrade", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "off", "off", "Workspace", " for ", "s", "core/teleport", "", "GetCharacterSpeedUpgradeCost", "write reported success but the line isn't in ", "JumpPower (ปกติ 50)", " (prompt says '", "Auto Upgrade: แจก Pickaxe ให้ miner", "Damage", "⚙", " items", "[AntiAFK] disabled", "CoreUI", "core/logger", "ถูกสุดก่อน", "hookfunction", " stack(s)", "M", ")", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "autoResearchMaxPerPass", "[AutoResearch] cheapest open node: ", " (", " (", " remotes", "ฟาร์ม", ")", "autoResearchInterval", "table", "TextLabel", " error: ", "games/my_anime_mine/auto_research", "core/spend_policy", "core/debug_dump_remotes", "core/teleport", "autoPlaceEquipBestInterval", "[GameAPI] shapes: no mine data yet", "[AntiAFK] enabled - watching Player.Idled", ": ", "^([%d,%.]+)%s*(%a*)$", " from ", "games/my_anime_mine/get_current_plot", "autoResearchCore", "core/run_job", "ค้นหาฟังก์ชัน...", "auto", "AutoPlot", "autoUpgradeLimitPickaxes", "Modules", "en-us", "Players", "[^%.]+", "autoUpgrade", "[AutoPlace] fireproximityprompt error: ", "=", "games/my_anime_mine/main", "Anti-AFK", " x", "MineEquipBest", " not bought yet (", "games/my_anime_mine/game_api", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "th", "TabBar", ") ", "write reported success but ", "ScrollingFrame", "games/my_anime_mine/auto_chest", "AutoPlace", "[AutoPlace] disabled", "Players", "[Config] couldn't read ", "MineGui", "function", "[Uu]nlock", "autoMine", "Players", "enabled", "core/logger", "TextLabel", ")", "TextButton", "core/parse_money", " (Text=", "core/debug_dump_gui", "Auto Place: รอบดึงตัวเก่ง", "ลำดับการใช้เงินรวมทุกระบบ", "VirtualInputManager", "ย่อขยายเมนูทั้งชุด", "prompt disabled", "[AutoUpgrade] ", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", ", built ", "decompile", " ", "", "core/get_leaderstat", "HumanoidRootPart", "Speed", "core/get_me", "Players", "autoPlaceUnlock", "ReplicatedStorage", "Auto Upgrade: กันเงินไว้", "autoChestInterval", "dearest", "Interaction Logger", "disabled", "autoSellInterval", "autoPlaceEquipBest", "Auto Place: เปลี่ยนตัวอ่อนออก", "Search", ": ", "autoSellProtectUnits", "SellStand", ")", " ", "autoPlaceUnlockBudget", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "table", "Holders", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "  ", " costs ", "core/logger", "MineSelectZone", "off", "Auto Research: ซื้อสูงสุดต่อรอบ"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(905)] = function()
return function(import)
local log = import(S(226))
local Players = game:GetService(S(975))
local VirtualUser = game:GetService(S(280))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(962))
        connection = player.Idled:Connect(function()
            log(S(401))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(936))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(283)] = function()
return { Env = S(570), IsDev = true, Commit = S(607), BuiltAt = S(673) }
end
modules[S(833)] = function()
local VirtualInputManager = game:GetService(S(1012))
local GuiService = game:GetService(S(90))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(635)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(430)] = function()
return function(import)
local HttpService = game:GetService(S(222))
local getPlaceId = import(S(699))
local log = import(S(404))
local ROOT_FOLDER = S(384)
local placeFolder = ROOT_FOLDER .. S(223) .. tostring(getPlaceId())
local filePath = placeFolder .. S(291)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(638) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(996) .. filePath .. S(695) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(678) .. filePath .. S(963) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(518)] = function()
return function(import)
local Players = game:GetService(S(267))
local log = import(S(1003))
local CAPABILITIES = {
    S(1018), S(568), S(350), S(786), S(883),
    S(853), S(830), S(73), S(910), S(508),
    S(916), S(825), S(940), S(886),
}
local OUT_FOLDER = S(908)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(976)) do
        if type(target) ~= S(617) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(887))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(64) .. name .. S(696) .. ((ok and type(value) == S(998)) and S(431) or S(68)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(353))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(768))
    if not playerScripts then
        log(S(462))
        return 0
    end
    pcall(makefolder, S(245))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(796)) or target:IsA(S(177)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(334) and #source > 0 then
                local name = target:GetFullName():gsub(S(342), S(180))
                if pcall(writefile, OUT_FOLDER .. S(711) .. name .. S(603), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(391) .. written .. S(747) .. OUT_FOLDER
        .. S(260) .. failed .. S(249))
    return written
end
local function run()
    log(S(569))
    reportCapabilities()
    local count = dumpAll()
    log(S(595))
    return count
end
return run
end
end
modules[S(526)] = function()
return function(import)
local Players = game:GetService(S(691))
local log = import(S(503))
local function dumpVisible(root, indent)
    indent = indent or S(310)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(322)) and child.Visible then
            local line = indent .. child.ClassName .. S(152) .. child.Name .. S(482)
            if child:IsA(S(579)) or child:IsA(S(315)) then
                line = line .. S(95) .. tostring(child.Text) .. S(943)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(317) .. attrName .. S(415) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(720))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(288))
    if not playerGui then
        log(S(48))
        return 0
    end
    local screens = 0
    log(S(733))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(220)) and screen.Enabled and screen.Name ~= S(641) then
            screens = screens + 1
            log(S(708) .. screen.ClassName .. S(682) .. screen.Name .. S(634))
            dumpVisible(screen, S(1047))
        end
    end
    log(S(443) .. screens .. S(301))
    return screens
end
return dumpGui
end
end
modules[S(958)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(303))
local log = import(S(258))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(56)) or descendant:IsA(S(876)) then
            count = count + 1
            log(S(254) .. descendant.ClassName .. S(1037) .. descendant:GetFullName())
        end
    end
    log(S(2) .. count .. S(381))
    return count
end
return dumpRemotes
end
end
modules[S(596)] = function()
return function(import)
local Players = game:GetService(S(995))
local Workspace = game:GetService(S(849))
local UserInputService = game:GetService(S(358))
local log = import(S(580))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(7) .. describe(prompt)
            .. S(511) .. prompt.ActionText .. S(717))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(499) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(140) .. describe(button)
            .. S(1008) .. tostring(button.Text) .. S(657))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(912)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(823)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(176)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(368))
        return
    end
    connections = {}
    log(S(763))
    local playerGui = Players.LocalPlayer:WaitForChild(S(1))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(244)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(536)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(464)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(608),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(749)] = function()
return function(import)
local interactionLogger = import(S(313))(import)
local dumpRemotes = import(S(338))(import)
local dumpGui = import(S(1009))(import)
local decompileScripts = import(S(778))(import)
local humanoid = import(S(834))
local function build(window, persist)
    local tab = window:AddTab(S(712), S(376))
    persist.Toggle(
        tab:AddCard(
            S(1031),
            S(292)
        ),
        S(339),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(365),
        S(170)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(201), function()
        remoteBtn.Text = dumpRemotes() .. S(949)
    end)
    local guiCard = tab:AddCard(
        S(3),
        S(47)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(903), function()
        guiBtn.Text = dumpGui() .. S(136)
    end)
    local decompileCard = tab:AddCard(
        S(256),
        S(571)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(515), function()
        decompileBtn.Text = decompileScripts() .. S(362)
    end)
    humanoid.Start()
    persist.Slider(tab, S(241), S(686),
        8, 200, S(141), 16, humanoid.SetWalkSpeed, S(481))
    persist.Slider(tab, S(204), S(930),
        30, 300, S(651), 50, humanoid.SetJumpPower, S(706))
    persist.Toggle(
        tab:AddCard(S(636), S(316)),
        S(592), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(173), S(433)),
        S(902), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(917), S(71)),
        S(327), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(804)] = function()
return function(import)
    local getPlaceId = import(S(138))
    local registry = import(S(729))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(850))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(1021)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(31))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(1024)] = function()
local Players = game:GetService(S(1025))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(496))
    local backpack = player:WaitForChild(S(375))
    return player, humanoid, backpack
end
return getMe
end
modules[S(155)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(117)] = function()
local Players = game:GetService(S(98))
local RunService = game:GetService(S(549))
local UserInputService = game:GetService(S(653))
local Workspace = game:GetService(S(923))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(700))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(406))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(311))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(787)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(609)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(191))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(417)] = function()
local Players = game:GetService(S(69))
local STRINGS = {
    en = {
        search_placeholder = S(765),
        open_menu_hint = S(106),
    },
    th = {
        search_placeholder = S(969),
        open_menu_hint = S(371),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(974)
    if locale:sub(1, 2):lower() == S(649) then
        return S(438)
    end
    return S(863)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(970) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(618)] = function()
local LOG_FILE = S(300)
local function log(message)
    local line = os.date(S(388)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(407))
            end
            appendfile(LOG_FILE, line .. S(8))
        elseif writefile then
            local existing = S(402)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(803))
        else
            error(S(320))
        end
        if not (isfile and readfile) then
            error(S(479))
        end
        if not isfile(LOG_FILE) then
            error(S(990) .. LOG_FILE .. S(361))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(929) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(898) .. LOG_FILE .. S(59) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(660)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(366) then
        return nil
    end
    local number, suffix = text:match(S(734))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(266), S(616))))
    if not value then
        return nil
    end
    if suffix ~= S(927) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(17)] = function()
return function(import)
local log = import(S(137))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(441) .. name .. S(492) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(872)] = function()
return function(import)
local RunService = game:GetService(S(856))
local log = import(S(495))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(683) .. task_.name .. S(955) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(497) .. task_.name .. S(743)
                    .. string.format(S(263), now - task_.busySince)
                    .. S(864))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(794) .. starved.name .. S(866)
            .. string.format(S(483), now - starved.waitingSince)
            .. S(848))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(355) .. (currentTask and currentTask.name or S(619)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(533)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(454))
local policy = {}
local probes = {}
local order = {}
local mode = S(922)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(550) .. (mode == S(1051)
        and S(475)
        or (S(134) .. mode .. S(819))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(348) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(435) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(81) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(437) .. name .. S(126)
            .. string.format(S(32), now - blockedSince[name])
            .. S(882))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(959)] = function()
local Players = game:GetService(S(594))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(1022))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(824)] = function()
return function(import)
local i18n = import(S(411))
local UserInputService = game:GetService(S(88))
local THEME = {
    BrandName = S(476),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(110))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(84))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(23))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target)
    local dragging, dragInput, dragStart, startPos
    local state = { moved = false }
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            state.moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                state.moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections, state
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(548))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(527)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(705))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(767))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(489)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(913))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(538)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(991))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(250))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(811))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(871))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(643))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(164))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(181))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(413))
            if not valueLbl then
                valueLbl = Instance.new(S(1004))
                valueLbl.Name = S(446)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(809))
            if not badge then
                badge = Instance.new(S(954))
                badge.Name = S(573)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(505))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(99))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(455))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(57))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(870)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(793)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(159))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(121))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(561))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(730)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(230))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(587))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(452))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(236)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(501))
    screenGui.Name = S(937)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(814))
    local main = Instance.new(S(54))
    main.Name = S(780)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(722))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(5))
    tabBar.Name = S(988)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(753))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(632))
    header.Name = S(692)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(206))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(410))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(37))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(65)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(1006))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(289)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(630))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(565)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(466))
    searchBox.Name = S(1036)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(727))
    searchBox.Text = S(498)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(379))
    content.Name = S(387)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(604))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(556))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(545))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(105))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    local avatarConnections, avatarDrag = makeDraggable(avatar, main)
    for _, connection in ipairs(avatarConnections) do
        table.insert(connections, connection)
    end
    table.insert(connections, avatar.Activated:Connect(function()
        if minimized and not avatarDrag.moved then
            setMinimized(false)
        end
    end))
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(24)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(726) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(161) .. (item.Subtitle or S(769))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(122))
        footerLabel.Text = i18n.T(S(382))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(992)] = function()
return function(import)
local gameApi = import(S(985))(import)
local log = import(S(858))
local runGuarded = import(S(799))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(163) .. minItemsToCollect .. S(108))
    else
        log(S(335))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(655) then
            log(S(470))
            lastReported = S(67)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(513) then
            log(S(352))
            lastReported = S(192)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(188) then
            local capacity = gameApi.ChestCapacity()
            log(S(755) .. count .. S(346) .. want .. S(935)
                .. (capacity and (S(869) .. capacity .. S(759)) or S(144)))
            lastReported = S(847)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(252), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(813) .. count .. S(107) .. after .. S(1040))
        else
            log(S(600) .. count
                .. S(205) .. tostring(after) .. S(166))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(6)] = function()
return function(import)
local gameApi = import(S(738))(import)
local log = import(S(938))
local runGuarded = import(S(363))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(890) .. (enabled and S(1002) or S(76)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(373) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(599) .. reward.category .. S(207)
                .. tostring(reward.tierKey) .. S(698) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(742) .. #pending .. S(308))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(53), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(240)] = function()
return function(import)
local getCurrentPlot = import(S(100))
local getPlotParts = import(S(33))
local log = import(S(1049))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(42), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(278) .. (enabled and S(142) or S(171)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(398)] = function()
return function(import)
local Players = game:GetService(S(835))
local getCurrentPlot = import(S(966))
local getPlotParts = import(S(875))
local getMinerSlots = import(S(654))(import)
local teleport = import(S(60))
local parseMoney = import(S(405))
local gameApi = import(S(354))(import)
local log = import(S(672))
local runGuarded = import(S(904))(import)
local spendPolicy = import(S(957))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(802)
local LOCKED_SLOT_TEXT = S(543)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(397))
    return pad and pad:FindFirstChildWhichIsA(S(524), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(12)) then
            local digits, suffix = descendant.Text:match(S(964))
            if digits then
                local value = tonumber((digits:gsub(S(270), S(557))))
                if value then
                    if suffix ~= S(449) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(143)
    return (text:gsub(S(195), S(845)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(29)) and descendant.Text:match(S(586)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(826) .. holder.Name .. S(984)
            .. gameApi.MinerSlots() .. S(114) .. gameApi.MaxMinerSlots() .. S(61)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(239)
    end
    if not prompt.Enabled then
        return true, S(1014)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(262) .. price
    end
    local action = prompt.ActionText or S(1020)
    if action:match(S(999)) or action:match(S(553)) or action:match(S(754)) then
        return true, S(523) .. action .. S(261)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(693))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(978) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(670) .. why .. S(514))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(725))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(841) .. prompt.ObjectText
                    .. S(633) .. shown .. S(529) .. math.floor(base) .. S(951))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(469))
    if line ~= lastSurvey then
        log(S(714) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(504), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(457), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(993)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(394), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(351) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(994))
    else
        lastAttempt = 0
        log(S(559))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(687),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(677)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(631)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(145)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(219))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(420) .. holder.Name .. S(130))
            return false
        end
        log(S(851) .. candidate.slot .. S(899) .. candidate.name
            .. S(235) .. tostring(why))
    end
    if not miner then
        log(S(490))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(321) .. holder.Name .. S(45) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(718) .. miner.name .. S(21) .. miner.power
        .. S(465) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(782) .. entry.name .. S(965) .. entry.holder.Name
            .. S(931) .. tostring(entry.prompt.ActionText) .. S(867))
        return false
    end
    log(S(461) .. entry.name .. S(821) .. entry.power
        .. S(888) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(597) .. pad.holder.Name .. S(151)
                .. tostring(pad.priceText))
        else
            log(S(246) .. pad.holder.Name
                .. S(282) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(889) .. holderName .. S(744)
            .. string.format(S(458), before) .. S(312) .. string.format(S(314), after)
            .. S(210) .. SWAP_COOLDOWN .. S(473))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(816)
            .. string.format(S(493), bestBefore) .. S(129)
            .. string.format(S(229), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(459), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(828))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(574) then
        runGuarded(S(422), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(49) then
        log(S(293) .. target.holder.Name
            .. S(924) .. tostring(target.priceText))
        runGuarded(S(189), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(112) .. target.holder.Name .. S(149) .. target.name
        .. S(554) .. target.power .. S(637) .. best.name .. S(625) .. best.power .. S(343))
    runGuarded(S(522), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(390)] = function()
return function(import)
local gameApi = import(S(227))(import)
local parseMoney = import(S(1007))
local log = import(S(374))
local runGuarded = import(S(968))(import)
local spendPolicy = import(S(356))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(294) .. (enabled and S(601) or S(168)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(460)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(701) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(948) .. deepestKey .. S(566))
        return true
    end
    log(S(416) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(344)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(436) .. gameApi.ZoneDisplayName(zoneKey) .. S(273)
                .. math.floor(price) .. S(268) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(419) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(372) .. zoneKey .. S(1005))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(160) then
            reportedLocked = S(13)
            log(S(238) .. zoneKey .. S(199)
                .. gameApi.ZoneDisplayName(currentKey) .. S(208)
                .. done .. S(736) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(296)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(694), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(713)] = function()
return function(import)
local gameApi = import(S(257))(import)
local log = import(S(15))
local runGuarded = import(S(582))(import)
local spendPolicy = import(S(855))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(772) .. (enabled and S(530) or S(409)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(414) .. node.zoneKey .. S(854) .. node.id
                    .. S(702) .. node.family .. S(52) .. node.level .. S(158) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(62) .. node.id
                    .. S(187) .. node.family .. S(50) .. node.level .. S(776) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(546) .. blocked
                .. S(614))
        elseif powerBlocked then
            log(S(784) .. powerBlocked.zoneKey .. S(1041) .. powerBlocked.id
                .. S(947) .. powerBlocked.family .. S(386)
                .. math.floor(powerBlocked.powerReq) .. S(318)
                .. math.floor(powerBlocked.power)
                .. S(35))
        else
            log(S(34)
                .. S(732) .. math.floor(gameApi.GetMoney()) .. S(547))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(946) .. nearest.zoneKey
                    .. S(184) .. nearest.id .. S(576) .. nearest.family .. S(989)
                    .. nearest.level .. S(467) .. nearest.max
                    .. S(1048) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(669)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(359),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(119))
        end
        if not buyCore then
            table.insert(parts, S(276))
        end
        if #parts == 0 then
            return S(453)
        end
        return S(664) .. table.concat(parts, S(217)) .. S(878)
    end
    local why
    if nearest.cost > budget then
        why = S(127) .. string.format(S(859), nearest.cost)
            .. S(336) .. string.format(S(620), money)
    elseif nearest.powerOk == false then
        why = S(656) .. string.format(S(209), nearest.powerReq) .. S(810)
    else
        why = S(323)
    end
    return S(623) .. nearest.zoneKey .. S(906) .. tostring(nearest.id)
        .. S(302) .. tostring(nearest.family) .. S(650) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(879) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(815) and cost > best)
            or (mode ~= S(304) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(103)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(615), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(757)] = function()
return function(import)
local Workspace = game:GetService(S(58))
local gameApi = import(S(218))(import)
local teleport = import(S(926))
local log = import(S(520))
local runGuarded = import(S(197))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(785))
    else
        lastAttempt = 0
        log(S(324))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(1039))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(891), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(233)
                .. (sold and S(563)
                         or S(340)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(622) .. (sold and (S(120) .. #keys .. S(941)) or S(502)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(750), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(30)] = function()
return function(import)
local Workspace = game:GetService(S(646))
local gameApi = import(S(305))(import)
local teleport = import(S(675))
local log = import(S(494))
local runGuarded = import(S(347))(import)
local spendPolicy = import(S(679))(import)
local STATS = { S(933), S(1023), S(719) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(101)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(272) .. (enabled and S(83) or S(1032)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(44) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(758) or S(844)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(79) .. holderKey .. S(885) .. choice.stat .. S(737)
                        .. choice.info.level .. S(610) .. after.level
                        .. S(70) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(148) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(25))
    local shop = stand and stand:FindFirstChild(S(403))
    if not shop then
        return nil
    end
    if shop:IsA(S(364)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(647), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(274)
                .. (bought and S(534)
                           or S(39)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(986))
        end
    end
    if bought then
        log(S(445) .. pickaxe.index .. S(800) .. pickaxe.name
            .. S(183) .. pickaxe.multiplier .. S(94))
        return true
    end
    log(S(290) .. pickaxe.index .. S(704) .. pickaxe.name
        .. S(299))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(1015) .. holderKey .. S(203)
                    .. tostring(worn or S(408)) .. S(892) .. wanted.name)
            else
                log(S(157) .. holderKey .. S(228)
                    .. wanted.name .. S(795) .. tostring(nowWorn or S(115)) .. S(9))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(77) .. holderKey .. S(770) .. variant
                .. S(325) .. after .. S(118) .. cost .. S(751))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(253))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(865) .. gameApi.OwnedPickaxeCount()
                    .. S(521) .. gameApi.MaxMinerSlots()
                    .. S(588))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(91))
        elseif maxed > 0 then
            log(S(491))
        else
            log(S(269))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(1030) and price > best)
            or (mode ~= S(645) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(760))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(20), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(723)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(1027))
local Workspace = game:GetService(S(516))
local log = import(S(911))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(973), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(341) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(560))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(46) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(234) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(593) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(104) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(773) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(648))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(442) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(510))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(591), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(880) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(1044) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(133) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(425), keys)
end
function api.EquipSlot(slot)
    return send(S(369), slot == S(846) and S(333) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(735), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(169), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(271), holderKey)
end
local describedShapes = false
function api.DescribeShapes()
    if describedShapes or not modules() then
        return
    end
    describedShapes = true
    local mine = api.GetMineData()
    if not mine then
        log(S(961))
        return
    end
    local function describe(value)
        if type(value) ~= S(389) then
            return tostring(value)
        end
        local parts = {}
        for key, item in pairs(value) do
            table.insert(parts, tostring(key) .. S(979) .. tostring(item))
        end
        table.sort(parts)
        return S(746) .. table.concat(parts, S(805)) .. S(837)
    end
    pcall(function()
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(295) and holder.CharName then
                log(S(448) .. holderKey .. S(1019) .. describe(holder))
                log(S(423) ..
                    tostring(mineConfig.GetHolderVariant(holder))
                    .. S(715) ..
                    tostring(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
                    .. S(840) .. tostring(mineConfig.GetHolderDamage(holder))
                    .. S(440) .. tostring(mineConfig.GetHolderLevel(holder)))
                break
            end
        end
    end)
    pcall(function()
        local shown = 0
        for slot, key in pairs(mine.Hotbar or {}) do
            log(S(284) .. tostring(slot) .. S(487) .. tostring(key)
                .. S(225) .. tostring(mineConfig.ParseCharStack(key)))
            shown = shown + 1
            if shown >= 3 then
                break
            end
        end
        if shown == 0 then
            log(S(663))
        end
    end)
    pcall(function()
        local entries = mineInventory.Collect(mine, S(783), mineInventory.KindOrder.OreFirst)
        local shown = 0
        for _, entry in pairs(entries or {}) do
            if entry.Kind == S(447) then
                log(S(709) .. describe(entry))
                shown = shown + 1
                if shown >= 2 then
                    break
                end
            end
        end
        if shown == 0 then
            log(S(611))
        end
    end)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(983))
end
function api.BuySkill(zoneKey, family)
    return send(S(671), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(900)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(762)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(429))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(383) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(329) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(1050), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(150))
    plots = plots and plots:FindFirstChild(S(349))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(1001)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(298)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(684))
    local gui = board and board:FindFirstChild(S(444))
    local root = gui and gui:FindFirstChild(S(330))
    local list = root and root:FindFirstChild(S(162))
    local row = list and list:FindFirstChild(S(517) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(741)) and descendant.Text ~= S(109) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(231) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(756), characterKey, stat == S(277) and S(202) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(439), max = S(139), cost = S(585) },
    Speed = { level = S(4), max = S(831), cost = S(928) },
    MineSpeed = { level = S(721), max = S(93), cost = S(275) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(488) or charName == S(602) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(255) .. tostring(value))
        end
        table.sort(fields)
        log(S(40) .. holderKey .. S(771) .. table.concat(fields, S(789)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(953) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(507) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(213) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(153) and type(holder.CharName) == S(740) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(839), S(370) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(286) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(775)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(146)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(818)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(798) then
        return {}, S(801)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(87)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(167) or type(state.Track) ~= S(18) then
                table.insert(notes, tabKey .. S(539) .. tostring(category) .. S(668))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(214) .. waiting
                        .. S(55))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(766) .. total .. S(584)
            .. (#notes > 0 and (S(774) .. table.concat(notes, S(710))) or S(781))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(279) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(862)] = function()
return function(import)
local Players = game:GetService(S(509))
local gameApi = import(S(10))(import)
local log = import(S(147))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(463))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(367), S(915))))
    if not value then
        return nil
    end
    if suffix ~= S(471) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(224))
    local mineGui = playerGui and playerGui:FindFirstChild(S(997))
    local hotbar = mineGui and mineGui:FindFirstChild(S(287))
    local content = hotbar and hotbar:FindFirstChild(S(380))
    return content and content:FindFirstChild(S(583))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(86)) then
            local count = descendant.Text:match(S(116))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(472))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(606))
    local nameLabel = content and content:FindFirstChild(S(412))
    local damageLabel = content and content:FindFirstChild(S(613))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(808) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(123) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(193) .. slot .. S(36) .. info.name
                        .. S(690) .. info.power .. S(982) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(558)] = function()
local Players = game:GetService(S(731))
local Workspace = game:GetService(S(124))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(829))
    local plots = mineWorld and mineWorld:FindFirstChild(S(562))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(345)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(822)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(1045)),
        Nodes = plot:FindFirstChild(S(897)),
        Chest = plot:FindFirstChild(S(652)),
        Zones = plot:FindFirstChild(S(531)),
        Crew = plot:FindFirstChild(S(639)),
        Loot = plot:FindFirstChild(S(26)),
        PlotIndex = plot:GetAttribute(S(703)),
        ChestCapacity = plot:GetAttribute(S(861)),
    }
end
return getPlotParts
end
modules[S(980)] = function()
return function(import)
    local createWindow = import(S(779))(import)
    local i18n = import(S(535))
    local antiAfk = import(S(28))(import)
    local buildEnv = import(S(528))
    local scheduler = import(S(395))(import)
    local spendPolicy = import(S(681))(import)
    local config = import(S(434))(import)
    local log = import(S(512))
    log(S(427) .. buildEnv.Commit .. S(748) .. buildEnv.Env
        .. S(1017) .. buildEnv.BuiltAt .. S(790))
    local window = createWindow({
        Subtitle = S(519),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(66))(import)
    local autoPlace = import(S(792))(import)
    local autoChest = import(S(590))(import)
    local autoSell = import(S(125))(import)
    local autoUpgrade = import(S(919))(import)
    local autoResearch = import(S(956))(import)
    local autoClaim = import(S(486))(import)
    local autoPlot = import(S(156))(import)
    local gameApi = import(S(450))(import)
    scheduler.RegisterStandalone(S(624), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(896), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(724), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(525), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(179), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(399), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(971), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(842), 20)
    scheduler.RegisterExclusive(S(297), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(873), autoResearch.NextPrice)
    spendPolicy.Register(S(827), autoUpgrade.NextPrice)
    spendPolicy.Register(S(198), autoPlot.NextPrice)
    spendPolicy.Register(S(605), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(950), S(665))
    farmTab:AddSectionHeader(S(1011))
    persistentSegmented(
        farmTab,
        { S(877), S(881), S(884) },
        { S(921), S(484), S(581) },
        S(500), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(542),
        S(185)
    ), S(1000), autoMine.SetEnabled)
    persistentSlider(farmTab, S(456), S(1043),
        1, 30, S(285), 5, autoMine.SetCyclesPerSecond, S(478))
    persistentSlider(farmTab, S(552), S(328),
        1, 10, S(628), 1, autoMine.SetClicksPerCycle, S(662))
    persistentToggle(farmTab:AddCard(
        S(689),
        S(200)
    ), S(907), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(697), S(1046),
        1, 60, S(131), 3, autoPlace.SetCheckInterval, S(629))
    persistentToggle(
        farmTab:AddCard(S(1035),
            S(247)),
        S(676), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(589),
            S(357)),
        S(1034), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(1010), S(806),
        5, 300, S(960), 60, autoPlace.SetEquipBestInterval, S(75))
    persistentToggle(
        farmTab:AddCard(S(78),
            S(1016)),
        S(1026), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(578), S(567),
        0, 500, S(1042), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(942))
    persistentToggle(farmTab:AddCard(
        S(644),
        S(716)
    ), S(89), autoChest.SetEnabled)
    persistentSlider(farmTab, S(393), S(537),
        50, 3000, S(468), 100, autoChest.SetMinItems, S(14))
    persistentSlider(farmTab, S(72), S(626),
        1, 60, S(1029), 3, autoChest.SetCheckInterval, S(661))
    persistentToggle(farmTab:AddCard(
        S(41),
        S(221)
    ), S(377), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(96),
            S(196)),
        S(1038), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(175), S(895),
        5, 300, S(1033), 20, autoSell.SetCheckInterval, S(761))
    farmTab:AddSlider(S(232),
        S(745),
        5, 120, sellMaxWait, function(value)
            config.Set(S(612), value)
        end, S(812))
    persistentToggle(farmTab:AddCard(
        S(337),
        S(874)
    ), S(977), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(265), S(577),
        5, 300, S(264), 20, autoUpgrade.SetInterval, S(540))
    persistentSlider(farmTab, S(451), S(242),
        1, 20, S(243), 5, autoUpgrade.SetMaxPerPass, S(190))
    persistentSegmented(
        farmTab,
        { S(777), S(860), S(555), S(901) },
        { S(788), S(331), S(532), S(306) },
        S(248), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(1028), S(22),
        0, 500, S(74), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(836))
    persistentToggle(
        farmTab:AddCard(S(666),
            S(506)),
        S(852), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(82) .. pickaxe.multiplier .. S(868)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(893)
            end
            return S(63) .. index .. S(838) .. (pickaxeLabels[index] or S(154))
        end
        local savedPick = config.Get(S(307), 0)
        pickaxeCard = farmTab:AddSlider(
            S(281),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(667), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(685)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(807),
            S(752)),
        S(972), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(932),
            S(659)),
        S(385), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(541),
            S(564)),
        S(791), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(174), S(480),
        1, 20, S(894), 2, autoUpgrade.SetMutationTarget, S(400))
    persistentSlider(farmTab, S(739), S(38),
        0, 500, S(85), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(43))
    persistentToggle(farmTab:AddCard(
        S(319),
        S(920)
    ), S(360), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(843), S(92),
        10, 600, S(952), 30, autoResearch.SetCheckInterval, S(925))
    persistentSlider(farmTab, S(1052), S(102),
        1, 20, S(945), 5, autoResearch.SetMaxPerPass, S(178))
    persistentSlider(farmTab, S(19), S(80),
        0, 500, S(16), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(707))
    persistentToggle(
        farmTab:AddCard(S(132),
            S(477)),
        S(182), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(215),
            S(474)),
        S(967), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(11),
            S(914)),
        S(212), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(939), S(332) },
        { true, false },
        S(598), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(424),
        S(572)
    ), S(627), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(426), S(309),
        5, 300, S(27), 30, autoClaim.SetCheckInterval, S(237))
    persistentSlider(farmTab, S(680), S(688),
        1, 30, S(251), 10, autoClaim.SetMaxPerPass, S(194))
    persistentToggle(farmTab:AddCard(
        S(797),
        S(944)
    ), S(113), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(857), S(421),
        5, 300, S(764), 30, autoPlot.SetCheckInterval, S(551))
    persistentToggle(
        farmTab:AddCard(S(909),
            S(918)),
        S(135), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(172),
            S(259)),
        S(216), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(428), S(820),
        0, 500, S(418), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(642))
    if buildEnv.IsDev then
        task.delay(8, gameApi.DescribeShapes)
    end
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(832))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(640), S(934))
    persistentToggle(
        settingsTab:AddCard(S(981), S(728)),
        S(432),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(621), 100)
    settingsTab:AddSlider(S(51), S(1013), 50, 150, savedScale, function(value)
        config.Set(S(111), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(544), 1)
    settingsTab:AddSegmented({ S(165), S(575), S(396) }, savedLanguage, function(index, value)
        local code = ({ S(186), S(674), S(987) })[index]
        config.Set(S(128), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(211), S(392), S(817) })[savedLanguage] or S(658))
    log(S(326) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(485)] = function()
return {
    [79389059854988] = S(97),
}
end
import(S(378))(import)
