-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Auto Mine", "SelectAllButton", "TextButton", "core/i18n", "PlotIndex", "MineGui", " '", "UIPadding", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Select All", "ฟาร์ม", "[AutoChest] fireproximityprompt error: ", " (Text=", "games/my_anime_mine/auto_sell", "Plots", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "core/logger", "games/my_anime_mine/debug_dump_remotes", "core/logger", ")", "RemoteFunction", " (MaxActivationDistance=%s)", " ===", "[Interaction] logger enabled - go click/interact with things now", "Dump", "", "  [attr] ", "[AutoChest] enabled", "⚙", "", "core/anti_afk", "core/logger", "write reported success but the line isn't in ", "games/my_anime_mine/auto_mine", "Crew", "games/my_anime_mine/get_current_plot", "SellFrame", "Players", "core/dispatch", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "TakeButton", "games/registry", "'", ")", "UICorner", "Debug: Dump All Remotes", "[AntiAFK] enabled - watching Player.Idled", "RunService", "Scroll", "TextButton", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "th", "games/my_anime_mine/debug_dump_gui", "Sell for", " doesn't exist", "ReplicatedStorage", "ClickDetector", "Debug: Dump Visible GUI", "UICorner", "", "Holders", "Zones", " '", "games/my_anime_mine/get_plot_parts", "Workspace", "Auto Place Miner", "  ", "games/my_anime_mine/get_current_plot", "MouseButton1Up", "Frame", "PlayerGui", "games/my_anime_mine/main", " <-- remote, likely fired by the real interaction", "  [attr] ", "[AutoSell] clicked SellButton", "ChestCapacity", "อังกฤษ", "Tool", "ClickDetector", "RunService", "[Remotes] ", "Frame", "Auto Sell", "core/logger", "Dump", "Players", ")", "Text", "MineGui", "'", "RunService", "Content", "Badge", "core/logger", "Players", "core/ui", "core/anti_afk", "Frame", "not found", "core/get_leaderstat", "Content", "Label", "en", "My Anime Mine · Mining Hub", "TextButton", "TextButton", "TextLabel", "ScrollingFrame", "[MyAnimeMine] === Node: ", "games/my_anime_mine/auto_chest", "ClickDetector", "ค้นหาฟังก์ชัน...", "RemoteEvent", "PlayerGui", "[AutoChest] clicked TakeButton", "MineGui", "SellFrame", "[Logger] failed to write ", "Check", "PlayerGui", "search_placeholder", "UIListLayout", "leaderstats", "MouseButton1Up", "Anti-AFK", "no plot", "CoreUI", "games/my_anime_mine/debug_dump_sell_cards", "Error: ", "Activated", "games/my_anime_mine/auto_place", "games/my_anime_mine/debug_dump_gui", "TextLabel", "Badge", ": ", "Content", "PlayerGui", "RunService", "RunService", "Pad", "core/i18n", "[GuiDump] === visible MineGui tree ===", " ===", "ตามเครื่อง", "core/interaction_logger", " remotes", "open_menu_hint", "games/my_anime_mine/get_current_plot", "GuiObject", "ProximityPrompt", "Content", "UserInputService", "Backpack", "Tool", "Frame", "Interaction Logger (debug)", "Search functions...", "games/my_anime_mine/auto_mine", "Debug: Current Plot", "[AutoSell] clicked SelectAllButton", "This is not a supported place.", "core/i18n", " = ", "🏠", "ChestFrame", "ClickDetector", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " = ", "MekPro.log", "TextLabel", "Players", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "TextLabel", "games/registry", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "[MyAnimeMine] === Dump start ===", "[Interaction] Key pressed: %s (gameProcessed=%s)", "?", "", "Frame", " = ", "core/dispatch", "Main", "[Interaction] ClickDetector clicked: ", "Players", "TextButton", "games/my_anime_mine/auto_sell", "OwnerUserId", "\n", "UIScale", "Backpack", "ProximityPrompt", "games/my_anime_mine/auto_upgrade", "TextLabel", "Players", "TextLabel", "core/ui", "log failed", "[AutoUpgrade] disabled", "Humanoid", "", "SellButton", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "MouseButton1Down", "core/logger", "[Interaction] Button activated: ", "Done", "core/logger", "no tool", "ValueLabel", "[Remotes] === total: ", "games/my_anime_mine/auto_place", "write reported success but ", "UIStroke", "RemoteFunction", "MineWorld", "en-us", "Chest", "CoreGui", "[Interaction] logger disabled", "UIListLayout", "Workspace", "games/my_anime_mine/get_plot_parts", " '", "%", "auto", "[AutoPlace] enabled", "SellStand", "neither appendfile nor writefile is available", " ===", "-", "core/get_place_id", "Dump", "ValueBase", "[SellCards] === total cards: ", "Frame", ": ", "games/my_anime_mine/get_plot_parts", "[AutoSell] SellFrame.Visible = ", "Frame", "MouseButton1Click", "[AutoMine] disabled", "Frame", "games/my_anime_mine/get_current_plot", "UIListLayout", "[MyAnimeMine] no nodes present right now", "  [attr] ", "[AutoPlace] disabled", "Players", "VirtualUser", "Actions", "TextLabel", "", " = ", "[MyAnimeMine] === Tool: ", "Workspace", "  [attr] ", "MouseButton1Down", "open_menu_hint", "Done", "", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Nodes", "games/my_anime_mine/get_plot_parts", "TextBox", "Text", " (ActionText=", "MekPro", "games/my_anime_mine/debug_dump_remotes", "ProximityPrompt", " ===", "ValueLabel", "Activated", "core/logger", "search_placeholder", "core/logger", "[SellCards] Card #", "Humanoid", "ProximityPrompt", "Debug: Dump Backpack Tool", " (Text=", "Players", "RemoteEvent", "ClickDetector", "games/my_anime_mine/auto_chest", "GuiButton", "TextLabel", "games/my_anime_mine/get_current_plot", " cards", "'", "GuiObject", "PlayerGui", "core/logger", "Open menu", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "th", "MouseButton1Click", "[MyAnimeMine] not standing in any plot", "Dump", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "[AutoChest] ChestFrame.Visible = ", "UserInputService", "games/my_anime_mine/get_plot_parts", "Debug: Dump Node/Holder", "games/my_anime_mine/main", "Empty miner slot", "Dump", "games/my_anime_mine/debug_dump_sell_cards", "TextLabel", "", "TextLabel", "UpgradePart", "games/my_anime_mine/get_plot_parts", "X", "games/my_anime_mine/debug_dump", "ขนาดเมนู", "ScreenGui", "Frame", "Players", "[MyAnimeMine] === Holder: ", "", "", " ===", "core/interaction_logger", "core/logger", "  ", "TextButton", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Auto Chest", "no isfile/readfile available to verify the write", "games/my_anime_mine/debug_dump", "", "Card", "[%X] ", "Players", "en", "[AutoChest] disabled", "Content", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "ProximityPrompt", ": ", "Search", "Done", "Auto Upgrade", "Debug: Dump Sell Cards", "  ", "'", "[AntiAFK] idle detected, simulating input", "core/logger", "auto", "[AutoUpgrade] enabled", "core/get_place_id", "[MyAnimeMine] no holders found", "[AntiAFK] disabled", "[AutoMine] enabled", "TextLabel", "[AutoSell] disabled", "Frame", "[AutoSell] enabled", "[AutoSell] fireproximityprompt error: ", "Frame", "[GuiDump] MineGui not found", " '", " on ", "TextButton", "core/get_me", "ย่อขยายเมนูทั้งชุด", "th", "Players", "[GuiDump] === end ===", "GuiButton", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "MineGui", " = ", " ", "Header", "\n", "TextButton", "TabBar", "[Interaction] ProximityPrompt triggered: ", "TextButton", "ProximityPrompt", "games/my_anime_mine/get_current_plot", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "ไทย", "ปุ่มเปิดเมนู", "TextLabel", "games/my_anime_mine/auto_upgrade", "[AutoPlace] placed ", "%", "TextLabel", "Loot", "ตั้งค่า", "Backpack", ""}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(33)] = function()
return function(import)
local log = import(S(21))
local Players = game:GetService(S(321))
local VirtualUser = game:GetService(S(252))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(49))
        connection = player.Idled:Connect(function()
            log(S(350))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(356))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(184)] = function()
return function(import)
    local getPlaceId = import(S(234))
    local registry = import(S(44))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(163))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(102)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(125))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(368)] = function()
local Players = game:GetService(S(371))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(202))
    local backpack = player:WaitForChild(S(155))
    return player, humanoid, backpack
end
return getMe
end
modules[S(354)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(164)] = function()
local Players = game:GetService(S(40))
local STRINGS = {
    en = {
        search_placeholder = S(159),
        open_menu_hint = S(296),
    },
    th = {
        search_placeholder = S(114),
        open_menu_hint = S(388),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(219)
    if locale:sub(1, 2):lower() == S(298) then
        return S(54)
    end
    return S(338)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(352) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(326)] = function()
return function(import)
local Players = game:GetService(S(337))
local Workspace = game:GetService(S(67))
local UserInputService = game:GetService(S(154))
local log = import(S(207))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(382) .. describe(prompt)
            .. S(269) .. prompt.ActionText .. S(22))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(186) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(208) .. describe(button)
            .. S(283) .. tostring(button.Text) .. S(89))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(342)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(113)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(288)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(222))
        return
    end
    connections = {}
    log(S(26))
    local playerGui = Players.LocalPlayer:WaitForChild(S(122))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(281)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(81)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(373)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(179),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(34)] = function()
local LOG_FILE = S(171)
local function log(message)
    local line = os.date(S(336)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(32))
            end
            appendfile(LOG_FILE, line .. S(191))
        elseif writefile then
            local existing = S(334)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(379))
        else
            error(S(231))
        end
        if not (isfile and readfile) then
            error(S(332))
        end
        if not isfile(LOG_FILE) then
            error(S(215) .. LOG_FILE .. S(57))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(35) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(120) .. LOG_FILE .. S(137) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(199)] = function()
return function(import)
local i18n = import(S(143))
local UserInputService = game:GetService(S(304))
local THEME = {
    BrandName = S(270),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(47))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(61))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(8))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(367))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(324)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(216))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(254))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(180)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(109))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(28)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(110))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(247))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(172))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(182))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(313))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(358))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(311))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(274))
            if not valueLbl then
                valueLbl = Instance.new(S(289))
                valueLbl.Name = S(212)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(95))
            if not badge then
                badge = Instance.new(S(135))
                badge.Name = S(136)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(107))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(157))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(242))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(108))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(397)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(392))
        local track = Instance.new(S(72))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(363))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(188))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(263)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(227)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(360))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(223))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(383))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(90)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(319))
    screenGui.Name = S(129)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(221))
    local main = Instance.new(S(245))
    main.Name = S(185)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(192))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(238))
    tabBar.Name = S(381)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(124))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(320))
    header.Name = S(378)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(52))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(196))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(198))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(181)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(329))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(316)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(3))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(233)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(267))
    searchBox.Name = S(344)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(123))
    searchBox.Text = S(323)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(84))
    content.Name = S(94)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(100))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(389))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(149))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(175))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(268)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(203) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(377) .. (item.Subtitle or S(62))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(277))
        footerLabel.Text = i18n.T(S(261))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(287)] = function()
return function(import)
local Players = game:GetService(S(187))
local RunService = game:GetService(S(50))
local getCurrentPlot = import(S(150))
local getPlotParts = import(S(305))
local log = import(S(96))
local FIRE_INTERVAL = 0.5
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(294))
    local mineGui = playerGui and playerGui:FindFirstChild(S(6))
    return mineGui and mineGui:FindFirstChild(S(167))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(153))
    local actions = content and content:FindFirstChild(S(253))
    return actions and actions:FindFirstChild(S(43))
end
local function clickButton(button)
    for _, signalName in ipairs({ S(299), S(132), S(206), S(126) }) do
        local signal = button[signalName]
        if signal then
            pcall(function()
                firesignal(signal)
            end)
        end
    end
end
local autoChest = {}
local connection = nil
function autoChest.SetEnabled(enabled)
    if enabled == autoChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(339))
        return
    end
    log(S(30))
    local lastFire = 0
    local lastVisibleState = nil
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local chestFrame = getChestFrame()
        local visible = chestFrame ~= nil and chestFrame.Visible
        if visible ~= lastVisibleState then
            log(S(303) .. tostring(visible))
            lastVisibleState = visible
        end
        if visible then
            local takeButton = getTakeButton(chestFrame)
            if takeButton then
                clickButton(takeButton)
                log(S(117))
            end
        else
            local plot = getCurrentPlot()
            local parts = plot and getPlotParts(plot)
            local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(152), true)
            if prompt then
                local ok, err = pcall(function()
                    fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
                end)
                if not ok then
                    log(S(14) .. tostring(err))
                end
            end
        end
    end)
end
function autoChest.IsEnabled()
    return connection ~= nil
end
return autoChest
end
end
modules[S(160)] = function()
return function(import)
local RunService = game:GetService(S(141))
local getCurrentPlot = import(S(38))
local getPlotParts = import(S(66))
local log = import(S(295))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(286), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(244))
        return
    end
    log(S(357))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(133)] = function()
return function(import)
local Players = game:GetService(S(97))
local RunService = game:GetService(S(93))
local getCurrentPlot = import(S(385))
local getPlotParts = import(S(225))
local log = import(S(210))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(142))
    return pad and pad:FindFirstChildWhichIsA(S(272), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(308)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(250))
        return
    end
    log(S(229))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(396))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(280))
        local tool = backpack:FindFirstChildWhichIsA(S(80))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(391) .. tool.Name .. S(366) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(189)] = function()
return function(import)
local Players = game:GetService(S(197))
local Workspace = game:GetService(S(258))
local RunService = game:GetService(S(82))
local log = import(S(86))
local FIRE_INTERVAL = 0.5
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(116))
    local mineGui = playerGui and playerGui:FindFirstChild(S(375))
    return mineGui and mineGui:FindFirstChild(S(39))
end
local function labelText(button)
    local content = button:FindFirstChild(S(340))
    local label = content and content:FindFirstChild(S(104))
    return label and label.Text or S(255)
end
local function clickButton(button)
    for _, signalName in ipairs({ S(243), S(275), S(260), S(71) }) do
        local signal = button[signalName]
        if signal then
            pcall(function()
                firesignal(signal)
            end)
        end
    end
end
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(359))
        return
    end
    log(S(361))
    local lastFire = 0
    local lastVisibleState = nil
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local sellFrame = getSellFrame()
        local visible = sellFrame ~= nil and sellFrame.Visible
        if visible ~= lastVisibleState then
            log(S(241) .. tostring(visible))
            lastVisibleState = visible
        end
        if visible then
            local content = sellFrame:FindFirstChild(S(103))
            local selectAllButton = content and content:FindFirstChild(S(2))
            local sellButton = content and content:FindFirstChild(S(204))
            if selectAllButton and labelText(selectAllButton) == S(12) then
                clickButton(selectAllButton)
                log(S(162))
            elseif sellButton and labelText(sellButton):find(S(56), 1, true) then
                clickButton(sellButton)
                log(S(77))
            end
        else
            local sellStand = Workspace:FindFirstChild(S(230))
            local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(384), true)
            if prompt then
                local ok, err = pcall(function()
                    fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
                end)
                if not ok then
                    log(S(362) .. tostring(err))
                end
            end
        end
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(390)] = function()
return function(import)
local RunService = game:GetService(S(140))
local getCurrentPlot = import(S(70))
local getPlotParts = import(S(315))
local log = import(S(19))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(314))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(168))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(201))
        return
    end
    log(S(353))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(333)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(10)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(7) .. child.Name .. S(292)
        if child:IsA(S(194)) then
            line = line .. string.format(
                S(264),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(59)) then
            line = line .. string.format(S(24), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(285)) or child:IsA(S(23)) then
            line = line .. S(75)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(76) .. attrName .. S(183) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(69))
    end
end
return dumpInstance
end
modules[S(134)] = function()
return function(import)
local Players = game:GetService(S(284))
local log = import(S(278))
local function dumpVisible(root, indent)
    indent = indent or S(312)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(293)) and child.Visible then
            local line = indent .. child.ClassName .. S(65) .. child.Name .. S(349)
            if child:IsA(S(393)) or child:IsA(S(380)) then
                line = line .. S(15) .. tostring(child.Text) .. S(46)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(29) .. attrName .. S(256) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(348))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(139))
    local mineGui = playerGui and playerGui:FindFirstChild(S(91))
    if not mineGui then
        log(S(364))
        return
    end
    log(S(144))
    dumpVisible(mineGui)
    log(S(372))
end
return dumpMineGui
end
end
modules[S(271)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(58))
local log = import(S(276))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(115)) or descendant:IsA(S(217)) then
            count = count + 1
            log(S(83) .. descendant.ClassName .. S(239) .. descendant:GetFullName())
        end
    end
    log(S(213) .. count .. S(325))
    return count
end
return dumpRemotes
end
end
modules[S(310)] = function()
return function(import)
local Players = game:GetService(S(251))
local log = import(S(351))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(73))
    local mineGui = playerGui and playerGui:FindFirstChild(S(118))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(119))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(138)) and sellFrame.Content:FindFirstChild(S(51))
    if not scroll then
        log(S(177))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(335) or card:IsA(S(151)) then
            count = count + 1
            log(S(279) .. count .. S(343) .. card.ClassName .. S(226) .. card.Name .. S(92))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(249) .. attrName .. S(165) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(328) .. child.ClassName .. S(365) .. child.Name .. S(45)
                if child:IsA(S(236)) then
                    line = line .. S(170) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(237) .. count .. S(25))
    return count
end
return dumpSellCards
end
end
modules[S(290)] = function()
local Players = game:GetService(S(88))
local Workspace = game:GetService(S(224))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(218))
    local plots = mineWorld and mineWorld:FindFirstChild(S(17))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(190)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(240)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(63)),
        Nodes = plot:FindFirstChild(S(265)),
        Chest = plot:FindFirstChild(S(220)),
        Zones = plot:FindFirstChild(S(64)),
        Crew = plot:FindFirstChild(S(37)),
        Loot = plot:FindFirstChild(S(394)),
        PlotIndex = plot:GetAttribute(S(5)),
        ChestCapacity = plot:GetAttribute(S(78)),
    }
end
return getPlotParts
end
modules[S(74)] = function()
return function(import)
    local Players = game:GetService(S(173))
    local createWindow = import(S(98))(import)
    local i18n = import(S(4))
    local antiAfk = import(S(99))(import)
    local interactionLogger = import(S(147))(import)
    local log = import(S(327))
    local window = createWindow({
        Subtitle = S(106),
    })
    local getCurrentPlot = import(S(246))
    local getPlotParts = import(S(266))
    local dumpInstance = import(S(317))
    local dumpRemotes = import(S(20))(import)
    local dumpSellCards = import(S(130))(import)
    local dumpGui = import(S(55))(import)
    local autoMine = import(S(36))(import)
    local autoPlace = import(S(214))(import)
    local autoChest = import(S(112))(import)
    local autoSell = import(S(16))(import)
    local autoUpgrade = import(S(195))(import)
    local farmTab = window:AddTab(S(13), S(166))
    farmTab:AddCard(
        S(1),
        S(53)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(68),
        S(169)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(331),
        S(302)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(85),
        S(9)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(346),
        S(341)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(161), S(297))
    local checkBtn
    checkBtn = debugCard:AddButton(S(121), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(101)
            log(S(300))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(330),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(306),
        S(386)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(87), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(128)
            return
        end
        local ok, err = log(S(178))
        if not ok then
            dumpBtn.Text = S(200)
            dumpCard:SetSubtitle(S(131) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(111) .. firstNode.Name .. S(273))
            dumpInstance(firstNode, log)
        else
            log(S(248))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(322) .. firstHolder.Name .. S(145))
            dumpInstance(firstHolder, log)
        else
            log(S(355))
        end
        dumpBtn.Text = S(345)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(282),
        S(18)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(309), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(193))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(156))
        if not tool then
            toolDumpBtn.Text = S(211)
            return
        end
        log(S(257) .. tool.Name .. S(232))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(259) .. attrName .. S(376) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(209)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(48),
        S(374)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(301), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(148)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(347),
        S(174)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(235), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(291)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(60),
        S(205)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(27), function()
        dumpGui()
        guiDumpBtn.Text = S(262)
    end)
    local settingsTab = window:AddTab(S(395), S(31))
    settingsTab:AddCard(S(127), S(42)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(158),
        S(11)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(318), S(369), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(146), S(79), S(387) }, 1, function(index, value)
        local code = ({ S(228), S(105), S(370) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(176)] = function()
return {
    [79389059854988] = S(307),
}
end
import(S(41))(import)
