-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoChest] waiting: ", "Auto Mine", "TakeButton", "Nodes", "อังกฤษ", " = ", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/debug_dump_gui", "Progress", "ValueBase", "Header", "Interaction Logger (debug)", "[AutoUpgrade] ", "autoPlace", "core/logger", "TabBar", "Crew", "core/ui", "Auto Sell", "MekPro", "ProximityPrompt", "[AutoChest] fireproximityprompt error: ", "", "table", "ProximityPrompt", "[Remotes] === total: ", "UIListLayout", "X", "Frame", "Frame", " '", "\n", " ===", "TextLabel", "Frame", "Frame", "[Config] couldn't write ", "", "Tool", "[AntiAFK] disabled", "", "[AutoChest] enabled (collects once >= ", "core/config", "Search functions...", "disabled", "ตามเครื่อง", "Scroll", "TextButton", "Text", "Players", " = ", ": ", "RemoteEvent", " parts", "Loot", "^(%d+)", "TextButton", "Humanoid", "ScrollingFrame", "TextLabel", "[ChestDump] === end ===", "%", "[Scheduler] ", "Dump", "Debug: Dump Backpack Tool", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", " = ", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "games/my_anime_mine/auto_mine", "games/my_anime_mine/debug_dump_chest", "TextButton", "Frame", " ", ",", "games/my_anime_mine/get_plot_parts", "TextButton", "TextLabel", "[AutoSell] clicked SelectAllButton (", "TextLabel", "TextLabel", ")", "plot has no Holders folder", "core/click", "[MyAnimeMine] no holders found", "Frame", "games/my_anime_mine/get_plot_parts", "unreadable", "games/my_anime_mine/auto_sell", "th", "[AutoSell] nothing selectable (", " '", "core/logger", "games/my_anime_mine/get_current_plot", "en", "", "core/logger", "core/logger", "neither appendfile nor writefile is available", "games/my_anime_mine/get_current_plot", "ProximityPrompt", "Players", "MineWorld", "languageIndex", "UICorner", "RunService", "no humanoid", ")", "/config.json", "core/logger", "", "[AutoPlace] ", "RemoteEvent", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "games/my_anime_mine/get_plot_parts", "ScreenGui", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "ย่อขยายเมนูทั้งชุด", "Holders", "Workspace", "ValueLabel", "games/my_anime_mine/auto_mine", "'", "ChestFrame", "Auto Chest", "[GuiDump] === end ===", "UIPadding", ": ", "  ", "Backpack", "[Interaction] Button activated: ", "Humanoid", "🏠", "enabled", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "languageIndex", "Players", "[MekPro] settings file: ", "Players", "[Logger] failed to write ", "TextLabel", "button has no size on screen", " error: ", "ค้นหาฟังก์ชัน...", "Content", "", "open_menu_hint", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/main", "en-us", "Text", "autoChest", "TextBox", "SellFrame", "[AutoChest] click failed on ", "Players", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "ตั้งค่า", "AutoUpgrade", "Sell for", "core/anti_afk", "Content", "UpgradePart", "Content", "[Scheduler] turn: ", "games/my_anime_mine/debug_dump_sell_cards", "SellFrame", "s", "Content", " items, going to collect", "SelectAllButton", "Dump", "[Config] couldn't read ", "", "[SellCards] Card #", " ===", "core/turn_scheduler", "?", "'", "Error: ", "Auto Place Miner", "Select All", "'", "ปุ่มเปิดเมนู", "games/my_anime_mine/debug_dump_chest", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "[Interaction] logger disabled", "OwnerUserId", "core/logger", "TextLabel", "/", "ClickDetector", "[GuiDump] === visible MineGui tree ===", "games/my_anime_mine/get_plot_parts", "ClickDetector", "ไทย", "core/click", "PlotIndex", ")", "core/logger", "core/interaction_logger", "write reported success but ", "Workspace", "", "  [attr] ", "SellButton", "[AutoSell] click failed on ", "TextLabel", "ChestCapacity", "This is not a supported place.", "[Interaction] ProximityPrompt triggered: ", "games/my_anime_mine/get_current_plot", "Badge", "Open menu", " holders checked)", "[AutoPlace] placed ", "'", "no tool in Backpack (all miners already placed?)", "games/registry", ": ", "[AutoSell] disabled", "games/my_anime_mine/get_plot_parts", "", "[AutoSell] fireproximityprompt error: ", "Chest", "th", "UICorner", "CoreGui", " (Text=", "PlayerGui", "[%X] ", "Players", "AutoSell", "autoUpgrade", "GuiObject", "core/i18n", "Debug: Dump Visible GUI", "en", "RemoteFunction", "Search", "core/teleport", "core/interaction_logger", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "MineGui", "Content", "games/my_anime_mine/debug_dump", "TextLabel", "'), backing off ", "GuiObject", "[Interaction] logger enabled - go click/interact with things now", "-", "core/get_place_id", "TextLabel", "GuiButton", "", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " cards", "  ", "[GuiDump] MineGui not found", "PlayerGui", "disabled", "AutoMine", "TextButton", "core/anti_afk", " (MaxActivationDistance=%s)", "CoreUI", "menuScale", "TextLabel", "[ChestDump] === plot ", "Label", "Close", " items", "auto", "log failed", "Badge", " attributes ===", "interactionLogger", ": ", "[SellCards] === total cards: ", "[ChestDump] === Chest subtree ===", "[AutoChest] Take All worked: ", "[AntiAFK] enabled - watching Player.Idled", "GuiButton", "SellStand", "core/logger", "write reported success but the line isn't in ", "Plots", "ClickDetector", "autoSell", "[AutoSell] enabled (only sells when there's something in the scroll)", "Players", "games/my_anime_mine/auto_chest", "[AutoSell] nothing to sell, closing", "core/get_leaderstat", "MekPro", "Frame", " -> ", ": ", "auto", "open_menu_hint", "[AutoPlace] ", "search_placeholder", "games/my_anime_mine/debug_dump", "Pad", "leaderstats", "  [attr] ", "ฟาร์ม", "Content", "Debug: Dump All Remotes", "Anti-AFK", "disabled", "  [attr] ", " items)", "no tool", " on ", "games/my_anime_mine/debug_dump_gui", "Card", " (Text=", "games/my_anime_mine/auto_place", "no empty holder found (", "Frame", "ValueLabel", "Close", "auto", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "", "th", "menuScale", "%", "[MyAnimeMine] === Holder: ", "[MyAnimeMine] no nodes present right now", "search_placeholder", "Players", "games/my_anime_mine/debug_dump_sell_cards", "⚙", "Check", "HttpService", ": ", " cards)", "[ChestDump] plot has no Chest", "Scroll", "Label", "Frame", "[ChestDump] === Player subtree ===", "[AutoChest] can't read the chest billboard, is the plot loaded?", "no isfile/readfile available to verify the write", "PlayerGui", "core/logger", "HumanoidRootPart", "[ChestDump] not standing in any plot", "games/my_anime_mine/auto_chest", "Backpack", "Backpack", "core/click", "", " '", " '", "not found", " (Text=", "core/logger", "unreadable", "Dump", ")", " remotes", "games/my_anime_mine/auto_upgrade", "ClickDetector", "[Interaction] ClickDetector clicked: ", "games/my_anime_mine/debug_dump_remotes", "games/my_anime_mine/get_plot_parts", "core/logger", "games/my_anime_mine/get_plot_parts", "ReplicatedStorage", "RemoteFunction", "Debug: Dump Chest Data", "[AutoSell] sell menu didn't open, aborting", "Main", "core/logger", "Dump", "ClickDetector", "Auto Upgrade", "autoMine", "games/my_anime_mine/get_current_plot", "[Interaction] Key pressed: %s (gameProcessed=%s)", "TextLabel", "", "core/config", "UIScale", "(none enabled)", "Done", "enabled", "PlayerGui", "[MyAnimeMine] === Tool: ", "th", "games/my_anime_mine/auto_place", "[ChestDump] === ReplicatedStorage.", "ValueBase", "Done", "[AntiAFK] idle detected, simulating input", "core/ui", "Sell for", "MineGui", "", "Frame", "[AutoSell] clicked SellButton", "ProximityPrompt", " -> ", "PlayerGui", "[AutoChest] chest didn't open, aborting", "core/i18n", "Debug: Dump Sell Cards", "[AutoChest] disabled", " '", "  ", "TextButton", "games/my_anime_mine/get_current_plot", " (ActionText=", "ขนาดเมนู", "ProximityPrompt", " = ", ": ", "Workspace", "games/registry", "", "UserInputService", "Debug: Dump Node/Holder", "Players", "no plot", "Debug: Current Plot", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", " ===", "[MyAnimeMine] === Node: ", " cards left, label = '", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "UIListLayout", "games/my_anime_mine/auto_upgrade", "MekPro.log", "Empty miner slot", "My Anime Mine · Mining Hub", "core/logger", "UIStroke", "Frame", "core/logger", "TextLabel", "core/get_place_id", "games/my_anime_mine/main", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "enabled", "ReplicatedStorage", "Content", "MineGui", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[AutoPlace] fireproximityprompt error: ", "VirtualInputManager", "TextButton", "Dump", "waiting", "Amount", "core/teleport", "Dump", "games/my_anime_mine/debug_dump_remotes", "  ", "  ", "core/teleport", "en", "core/get_me", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", " items", "TextButton", "[attr] ", "TextLabel", "core/i18n", "TextButton", "/", "ProximityPrompt", "Players", "  [attr] ", "GuiService", "VirtualUser", " <-- remote, likely fired by the real interaction", "antiAfk", "", "games/my_anime_mine/auto_sell", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Zones", "[Remotes] ", "auto", "[MyAnimeMine] not standing in any plot", "no character/backpack", "UIListLayout", " items)", "core/turn_scheduler", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "waiting", "AutoPlace", "Players", " ===", "Players", " doesn't exist", "AutoChest", "", "", "MineGui", " ===", "[AutoChest] Take All had NO effect (", "not standing in any plot", "'", "UserInputService", " ===", "Actions", "Players", "core/logger", "Card", " = ", "core/dispatch", "\n", "[MyAnimeMine] === Dump start ===", "core/dispatch", "TextButton", "  ", "[AutoMine] ", "Done", " = ", "[AutoChest] ", " = ", "core/get_place_id", "Tool"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(160)] = function()
return function(import)
local log = import(S(15))
local Players = game:GetService(S(494))
local VirtualUser = game:GetService(S(477))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(281))
        connection = player.Idled:Connect(function()
            log(S(397))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(40))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(353)] = function()
local VirtualInputManager = game:GetService(S(452))
local GuiService = game:GetService(S(476))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(141)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(43)] = function()
return function(import)
local HttpService = game:GetService(S(336))
local getPlaceId = import(S(443))
local log = import(S(441))
local ROOT_FOLDER = S(294)
local placeFolder = ROOT_FOLDER .. S(190) .. tostring(getPlaceId())
local filePath = placeFolder .. S(108)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(24) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(172) .. filePath .. S(52) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(37) .. filePath .. S(219) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(513)] = function()
return function(import)
    local getPlaceId = import(S(251))
    local registry = import(S(218))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(209))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(293)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(304))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(464)] = function()
local Players = game:GetService(S(509))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(58))
    local backpack = player:WaitForChild(S(352))
    return player, humanoid, backpack
end
return getMe
end
modules[S(524)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(470)] = function()
local Players = game:GetService(S(155))
local STRINGS = {
    en = {
        search_placeholder = S(44),
        open_menu_hint = S(213),
    },
    th = {
        search_placeholder = S(143),
        open_menu_hint = S(183),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(149)
    if locale:sub(1, 2):lower() == S(392) then
        return S(326)
    end
    return S(463)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(272) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(241)] = function()
return function(import)
local Players = game:GetService(S(332))
local Workspace = game:GetService(S(119))
local UserInputService = game:GetService(S(506))
local log = import(S(347))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(210) .. describe(prompt)
            .. S(415) .. prompt.ActionText .. S(107))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(366) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(130) .. describe(button)
            .. S(358) .. tostring(button.Text) .. S(362))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(100)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(191)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(253)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(186))
        return
    end
    connections = {}
    log(S(249))
    local playerGui = Players.LocalPlayer:WaitForChild(S(346))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(473)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(365)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(282)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(382),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(359)] = function()
local LOG_FILE = S(435)
local function log(message)
    local line = os.date(S(230)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(422))
            end
            appendfile(LOG_FILE, line .. S(514))
        elseif writefile then
            local existing = S(354)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(32))
        else
            error(S(98))
        end
        if not (isfile and readfile) then
            error(S(345))
        end
        if not isfile(LOG_FILE) then
            error(S(201) .. LOG_FILE .. S(497))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(285) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(139) .. LOG_FILE .. S(297) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(240)] = function()
local Players = game:GetService(S(425))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(348))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(490)] = function()
return function(import)
local RunService = game:GetService(S(105))
local log = import(S(369))
local SLICE_SECONDS = 3
local scheduler = {}
local tasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.Register(name, step, isEnabled, isBusy)
    table.insert(tasks, { name = name, step = step, isEnabled = isEnabled, isBusy = isBusy })
end
local function anyBusy()
    for _, task_ in ipairs(tasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickNextEnabled()
    if #tasks == 0 then
        return nil
    end
    for _ = 1, #tasks do
        rrIndex = (rrIndex % #tasks) + 1
        local candidate = tasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        if anyBusy() then
            return
        end
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(164) .. (currentTask and currentTask.name or S(387)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            local ok, err = pcall(currentTask.step)
            if not ok then
                log(S(63) .. currentTask.name .. S(142) .. tostring(err))
            end
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(398)] = function()
return function(import)
local i18n = import(S(235))
local UserInputService = game:GetService(S(423))
local THEME = {
    BrandName = S(20),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(226))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(104))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(126))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(413))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(173)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(439))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(442))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(177)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(246))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(41)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(59))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(433))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(469))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(402))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(140))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(252))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(77))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(321))
            if not valueLbl then
                valueLbl = Instance.new(S(207))
                valueLbl.Name = S(120)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(212))
            if not badge then
                badge = Instance.new(S(267))
                badge.Name = S(274)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(517))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(36))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(440))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(48))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(500)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(328))
        local track = Instance.new(S(320))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(72))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(71))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(499)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(62)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(295))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(27))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(467))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(49)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(115))
    screenGui.Name = S(265)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(227))
    local main = Instance.new(S(85))
    main.Name = S(375)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(386))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(29))
    tabBar.Name = S(16)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(488))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(30))
    header.Name = S(11)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(76))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(34))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(60))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(222)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(57))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(28)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(471))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(250)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(152))
    searchBox.Name = S(239)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(331))
    searchBox.Text = S(145)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(35))
    content.Name = S(161)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(342))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(383))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(299))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(80))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(150)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(38) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(73) .. (item.Subtitle or S(110))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(301))
        footerLabel.Text = i18n.T(S(146))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(350)] = function()
return function(import)
local Players = game:GetService(S(136))
local getCurrentPlot = import(S(381))
local getPlotParts = import(S(86))
local teleport = import(S(457))
local click = import(S(196))
local log = import(S(199))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(390))
    local mineGui = playerGui and playerGui:FindFirstChild(S(501))
    return mineGui and mineGui:FindFirstChild(S(123))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(168))
    local actions = content and content:FindFirstChild(S(508))
    return actions and actions:FindFirstChild(S(3))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(74), S(254)):match(S(56))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(456), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(448))
    local progress = content and content:FindFirstChild(S(9))
    local label = progress and progress:FindFirstChild(S(341))
    return parseCount(label and label.Text or S(480))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(154) .. button.Name .. S(277) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(404), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(410))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(42) .. MIN_ITEMS_TO_COLLECT .. S(489))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(22) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(407))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(280) .. count .. S(405) .. after .. S(466))
        else
            log(S(503) .. tostring(count)
                .. S(296) .. tostring(after) .. S(312))
        end
    end
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(87) then
            log(S(344))
            lastReportedCount = S(360)
        end
        return
    end
    if count < MIN_ITEMS_TO_COLLECT then
        if lastReportedCount ~= S(455) then
            log(S(1) .. count .. S(472) .. MIN_ITEMS_TO_COLLECT .. S(271))
            lastReportedCount = S(492)
        end
        return
    end
    lastReportedCount = nil
    log(S(522) .. count .. S(169))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(121)] = function()
return function(import)
local getCurrentPlot = import(S(7))
local getPlotParts = import(S(368))
local log = import(S(96))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(194), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(519) .. (enabled and S(133) or S(260)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(393)] = function()
return function(import)
local Players = game:GetService(S(496))
local getCurrentPlot = import(S(99))
local getPlotParts = import(S(370))
local log = import(S(284))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(303))
    return pad and pad:FindFirstChildWhichIsA(S(21), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(436)
end
local autoPlace = {}
local enabled = false
local lastFire = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(300) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    log(S(111) .. (enabled and S(446) or S(310)))
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local player = Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild(S(351))
    if not character or not backpack then
        reason(S(487))
        return
    end
    local humanoid = character:FindFirstChildOfClass(S(131))
    local tool = backpack:FindFirstChildWhichIsA(S(39))
    if not humanoid then
        reason(S(106))
        return
    end
    if not tool then
        reason(S(217))
        return
    end
    local plot = getCurrentPlot()
    if not plot then
        reason(S(504))
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        reason(S(82))
        return
    end
    local placed = false
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderEmpty(prompt) then
            humanoid:EquipTool(tool)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if ok then
                log(S(215) .. tool.Name .. S(314) .. holder.Name)
            else
                log(S(451) .. tostring(err))
            end
            placed = true
            break
        end
    end
    if not placed then
        reason(S(319) .. #parts.Holders:GetChildren() .. S(214))
    else
        lastReason = nil
    end
end
return autoPlace
end
end
modules[S(88)] = function()
return function(import)
local Players = game:GetService(S(231))
local Workspace = game:GetService(S(420))
local teleport = import(S(462))
local click = import(S(83))
local log = import(S(438))
local CHECK_INTERVAL = 45
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(229))
    local mineGui = playerGui and playerGui:FindFirstChild(S(400))
    return mineGui and mineGui:FindFirstChild(S(166))
end
local function labelText(button)
    local content = button:FindFirstChild(S(307))
    local label = content and content:FindFirstChild(S(269))
    return label and label.Text or S(203)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(144))
    local scroll = content and content:FindFirstChild(S(47))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(316) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(206) .. button.Name .. S(419) .. tostring(err))
    end
    return ok
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(283))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(25), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(220))
    else
        lastAttempt = 0
        log(S(289))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(223) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(374))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(292))
        local closeButton = sellFrame:FindFirstChild(S(322))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(163))
    local selectAllButton = content and content:FindFirstChild(S(170))
    local sellButton = content and content:FindFirstChild(S(205))
    if selectAllButton and labelText(selectAllButton) == S(181) then
        clickButton(selectAllButton)
        log(S(78) .. cardCount .. S(338))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(399), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(159), 1, true) then
        clickButton(sellButton)
        log(S(403))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(270))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(90) .. cardCount .. S(431)
            .. tostring(sawLabel) .. S(247) .. NOTHING_SELLABLE_COOLDOWN .. S(167))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(364)] = function()
return function(import)
local getCurrentPlot = import(S(211))
local getPlotParts = import(S(114))
local log = import(S(97))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(162))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(378))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(13) .. (enabled and S(389) or S(45)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(302)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(23)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(91) .. child.Name .. S(122)
        if child:IsA(S(417)) then
            line = line .. string.format(
                S(324),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(287)) then
            line = line .. string.format(S(264), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(53)) or child:IsA(S(372)) then
            line = line .. S(478)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(204) .. attrName .. S(51) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(128))
    end
end
return dumpInstance
end
modules[S(70)] = function()
return function(import)
local Players = game:GetService(S(101))
local ReplicatedStorage = game:GetService(S(447))
local getCurrentPlot = import(S(414))
local getPlotParts = import(S(75))
local log = import(S(188))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(468) .. name .. S(67) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(412))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(356) .. child.Name .. S(178)
            if child:IsA(S(395)) then
                line = line .. S(418) .. tostring(child.Value)
            elseif child:IsA(S(189)) or child:IsA(S(453)) then
                line = line .. S(228) .. tostring(child.Text) .. S(81)
            end
            log(line)
            dumpSubtree(child, indent .. S(518))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(349))
        return 0
    end
    log(S(268) .. plot.Name .. S(275))
    dumpAttributes(plot, S(257))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(339))
        return 0
    end
    log(S(279))
    dumpSubtree(parts.Chest, S(384))
    local player = Players.LocalPlayer
    log(S(343))
    dumpSubtree(player, S(325))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(394) .. player.Name .. S(495))
        dumpSubtree(mine, S(95))
    end
    log(S(61))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(8)] = function()
return function(import)
local Players = game:GetService(S(290))
local log = import(S(92))
local function dumpVisible(root, indent)
    indent = indent or S(401)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(248)) and child.Visible then
            local line = indent .. child.ClassName .. S(31) .. child.Name .. S(216)
            if child:IsA(S(79)) or child:IsA(S(262)) then
                line = line .. S(317) .. tostring(child.Text) .. S(198)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(475) .. attrName .. S(521) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(461))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(406))
    local mineGui = playerGui and playerGui:FindFirstChild(S(243))
    if not mineGui then
        log(S(258))
        return
    end
    log(S(192))
    dumpVisible(mineGui)
    log(S(125))
end
return dumpMineGui
end
end
modules[S(367)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(371))
local log = import(S(376))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(112)) or descendant:IsA(S(238)) then
            count = count + 1
            log(S(484) .. descendant.ClassName .. S(127) .. descendant:GetFullName())
        end
    end
    log(S(26) .. count .. S(33))
    return count
end
return dumpRemotes
end
end
modules[S(333)] = function()
return function(import)
local Players = game:GetService(S(474))
local log = import(S(510))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(259))
    local mineGui = playerGui and playerGui:FindFirstChild(S(449))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(153))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(244)) and sellFrame.Content:FindFirstChild(S(340))
    if not scroll then
        log(S(482))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(511) or card:IsA(S(234)) then
            count = count + 1
            log(S(174) .. count .. S(337) .. card.ClassName .. S(411) .. card.Name .. S(182))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(311) .. attrName .. S(512) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(460) .. child.ClassName .. S(355) .. child.Name .. S(505)
                if child:IsA(S(10)) then
                    line = line .. S(523) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(278) .. count .. S(502))
    return count
end
return dumpSellCards
end
end
modules[S(93)] = function()
local Players = game:GetService(S(138))
local Workspace = game:GetService(S(202))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(102))
    local plots = mineWorld and mineWorld:FindFirstChild(S(286))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(187)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(221)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(118)),
        Nodes = plot:FindFirstChild(S(4)),
        Chest = plot:FindFirstChild(S(224)),
        Zones = plot:FindFirstChild(S(483)),
        Crew = plot:FindFirstChild(S(17)),
        Loot = plot:FindFirstChild(S(55)),
        PlotIndex = plot:GetAttribute(S(197)),
        ChestCapacity = plot:GetAttribute(S(208)),
    }
end
return getPlotParts
end
modules[S(148)] = function()
return function(import)
    local Players = game:GetService(S(50))
    local createWindow = import(S(18))(import)
    local i18n = import(S(408))
    local antiAfk = import(S(263))(import)
    local interactionLogger = import(S(200))(import)
    local scheduler = import(S(176))(import)
    local config = import(S(385))(import)
    local log = import(S(109))
    local window = createWindow({
        Subtitle = S(437),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local getCurrentPlot = import(S(147))
    local getPlotParts = import(S(193))
    local dumpInstance = import(S(245))
    local dumpRemotes = import(S(459))(import)
    local dumpSellCards = import(S(165))(import)
    local dumpGui = import(S(315))(import)
    local dumpChest = import(S(184))(import)
    local autoMine = import(S(69))(import)
    local autoPlace = import(S(318))(import)
    local autoChest = import(S(291))(import)
    local autoSell = import(S(481))(import)
    local autoUpgrade = import(S(434))(import)
    scheduler.Register(S(261), autoMine.Step, autoMine.IsEnabled)
    scheduler.Register(S(493), autoPlace.Step, autoPlace.IsEnabled)
    scheduler.Register(S(498), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy)
    scheduler.Register(S(232), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Register(S(158), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.Start()
    local farmTab = window:AddTab(S(306), S(132))
    persistentToggle(farmTab:AddCard(
        S(2),
        S(134)
    ), S(380), autoMine.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(180),
        S(255)
    ), S(14), autoPlace.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(124),
        S(491)
    ), S(151), autoChest.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(19),
        S(450)
    ), S(288), autoSell.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(379),
        S(445)
    ), S(233), autoUpgrade.SetEnabled)
    local debugCard = farmTab:AddCard(S(427), S(66))
    local checkBtn
    checkBtn = debugCard:AddButton(S(335), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(357)
            log(S(486))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(68),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(424),
        S(432)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(377), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(426)
            return
        end
        local ok, err = log(S(515))
        if not ok then
            dumpBtn.Text = S(273)
            dumpCard:SetSubtitle(S(179) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(430) .. firstNode.Name .. S(507))
            dumpInstance(firstNode, log)
        else
            log(S(330))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(329) .. firstHolder.Name .. S(429))
            dumpInstance(firstHolder, log)
        else
            log(S(84))
        end
        dumpBtn.Text = S(396)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(65),
        S(156)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(458), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(129))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(525))
        if not tool then
            toolDumpBtn.Text = S(313)
            return
        end
        log(S(391) .. tool.Name .. S(175))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(305) .. attrName .. S(6) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(520)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(308),
        S(116)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(361), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(363)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(409),
        S(113)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(64), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(256)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(236),
        S(465)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(454), function()
        dumpGui()
        guiDumpBtn.Text = S(388)
    end)
    local chestDumpCard = farmTab:AddCard(
        S(373),
        S(185)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(171), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(54)
    end)
    local settingsTab = window:AddTab(S(157), S(334))
    persistentToggle(
        settingsTab:AddCard(S(309), S(242)),
        S(479),
        antiAfk.SetEnabled
    )
    persistentToggle(
        settingsTab:AddCard(
            S(12),
            S(428)
        ),
        S(276),
        interactionLogger.SetEnabled
    )
    local savedScale = config.Get(S(266), 100)
    settingsTab:AddSlider(S(416), S(117), 50, 150, savedScale, function(value)
        config.Set(S(327), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(135), 1)
    settingsTab:AddSegmented({ S(46), S(5), S(195) }, savedLanguage, function(index, value)
        local code = ({ S(485), S(94), S(89) })[index]
        config.Set(S(103), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(298), S(237), S(225) })[savedLanguage] or S(323))
    log(S(137) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(421)] = function()
return {
    [79389059854988] = S(444),
}
end
import(S(516))(import)
