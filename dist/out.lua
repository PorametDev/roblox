-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"games/my_anime_mine/debug_dump_sell_cards", "GuiButton", "games/my_anime_mine/debug_dump_remotes", "Backpack", "games/my_anime_mine/auto_mine", "UIListLayout", "Chest", "TextLabel", "", " = ", "Scroll", " cards", "games/my_anime_mine/auto_place", "[GuiDump] === end ===", "Main", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "core/logger", "", "Players", "games/my_anime_mine/debug_dump_remotes", " (Text=", "Humanoid", "Frame", "X", "ตามเครื่อง", "Players", "Check", "core/logger", "[AutoSell] SelectAllButton click error: ", "games/my_anime_mine/debug_dump_gui", "ChestCapacity", "TextLabel", "Debug: Dump All Remotes", "Zones", "[MyAnimeMine] no holders found", "open_menu_hint", "ClickDetector", "Dump", "TextButton", "core/get_place_id", "core/i18n", "'", "games/my_anime_mine/auto_place", "UpgradePart", "en", "Holders", "search_placeholder", "VirtualUser", "Done", "Badge", "ScrollingFrame", " (MaxActivationDistance=%s)", " = ", "%", "ProximityPrompt", "games/my_anime_mine/auto_sell", "[AntiAFK] enabled - watching Player.Idled", "games/my_anime_mine/main", "", "core/logger", "[AutoSell] clicked SelectAllButton", "core/logger", "ย่อขยายเมนูทั้งชุด", "Debug: Dump Sell Cards", "Players", "MineGui", "MineGui", "core/logger", "th", "TextButton", "Empty miner slot", "UIListLayout", "Debug: Dump Node/Holder", "Players", "Frame", "games/my_anime_mine/auto_upgrade", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Dump", "[AutoSell] disabled", "auto", "Frame", "no isfile/readfile available to verify the write", "Tool", "Content", "", "Auto Place Miner", "TextButton", "SellStand", " '", "Card", "Actions", "Plots", "", "TextLabel", "PlayerGui", "Frame", "Anti-AFK", "ValueBase", "core/logger", "Search functions...", "ClickDetector", "", "Debug: Dump Visible GUI", "TextLabel", "PlayerGui", "core/logger", " (Text=", "[Remotes] === total: ", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "Auto Upgrade", "ScreenGui", "en", "Frame", "Crew", "UIListLayout", "SellButton", "MekPro.log", "Debug: Current Plot", "Frame", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "[AutoSell] fireproximityprompt error: ", "games/my_anime_mine/get_plot_parts", "Frame", "ValueLabel", "[AutoPlace] disabled", "Loot", ": ", "ClickDetector", "Search", "Content", "games/registry", "[AntiAFK] idle detected, simulating input", "TabBar", " ===", "Workspace", "games/my_anime_mine/auto_chest", "core/i18n", "[SellCards] Card #", "ProximityPrompt", "  [attr] ", "TextLabel", " <-- remote, likely fired by the real interaction", "games/my_anime_mine/debug_dump", "games/my_anime_mine/get_current_plot", ")", "core/dispatch", "", "games/my_anime_mine/get_plot_parts", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "TextLabel", "games/my_anime_mine/debug_dump_gui", "core/logger", "PlayerGui", " ===", "", "UserInputService", "games/my_anime_mine/get_current_plot", "Players", "UICorner", "Debug: Dump Backpack Tool", "PlayerGui", "core/ui", " = ", "My Anime Mine · Mining Hub", "TextLabel", "TextButton", "en-us", "Workspace", "Frame", "TextLabel", "Content", "-", "Dump", "Select All", "Interaction Logger (debug)", "  ", "no plot", " ===", "write reported success but the line isn't in ", "games/my_anime_mine/auto_sell", "  ", "Sell for", "Backpack", "log failed", "core/interaction_logger", "[AutoSell] SellButton click error: ", "TakeButton", "RunService", "TextButton", "Backpack", " = ", "games/my_anime_mine/auto_upgrade", "[AutoChest] fireproximityprompt error: ", "[Interaction] ClickDetector clicked: ", "[MyAnimeMine] no nodes present right now", ")", "[AutoSell] enabled", "ClickDetector", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "UIPadding", "'", "Dump", "'", "Humanoid", "RemoteEvent", "games/my_anime_mine/auto_chest", "GuiButton", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/interaction_logger", "RunService", "TextLabel", "core/get_place_id", "[AutoSell] SellFrame.Visible = ", "RunService", "games/my_anime_mine/get_plot_parts", "[AutoMine] disabled", "MineWorld", "TextButton", "Players", "PlayerGui", "'", "Done", "Players", "core/get_leaderstat", "TextButton", " '", "[MyAnimeMine] === Dump start ===", "th", "ChestFrame", "UIStroke", "🏠", ")", "ReplicatedStorage", "  [attr] ", "Workspace", "UserInputService", "Label", "Done", "SellFrame", "games/my_anime_mine/get_plot_parts", "core/logger", "[AutoMine] enabled", "games/registry", "[AntiAFK] disabled", "games/my_anime_mine/debug_dump_sell_cards", "", "RemoteEvent", "games/my_anime_mine/get_plot_parts", "core/anti_afk", "leaderstats", " ", " ===", " remotes", "ปุ่มเปิดเมนู", "[Logger] failed to write ", "Frame", "?", "Dump", "  [attr] ", "TextButton", "ค้นหาฟังก์ชัน...", "Frame", "TextLabel", "Tool", "[AutoPlace] enabled", "Auto Sell", "[AutoChest] ChestFrame.Visible = ", "อังกฤษ", "RunService", "[AutoSell] clicked SellButton", "[MyAnimeMine] === Node: ", "RemoteFunction", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Players", "[AutoChest] disabled", "SelectAllButton", "TextLabel", "TextLabel", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "[MyAnimeMine] === Holder: ", "[Remotes] ", "Open menu", "core/dispatch", "  ", "games/my_anime_mine/get_current_plot", "GuiObject", "ClickDetector", "RunService", " = ", "auto", " (ActionText=", "ProximityPrompt", "games/my_anime_mine/get_current_plot", " ===", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "[GuiDump] MineGui not found", "Content", "ProximityPrompt", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "core/logger", "", "[%X] ", "open_menu_hint", "Auto Mine", " '", "", ": ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "write reported success but ", "MineGui", "Nodes", "ProximityPrompt", "GuiObject", " on ", "[MyAnimeMine] not standing in any plot", "%", "games/my_anime_mine/get_current_plot", "Players", "Players", "[AutoChest] clicked TakeButton", " '", "[GuiDump] === visible MineGui tree ===", "games/my_anime_mine/auto_mine", "[Interaction] Key pressed: %s (gameProcessed=%s)", "[Interaction] Button activated: ", "", "[Interaction] logger enabled - go click/interact with things now", "Text", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "\n", "core/anti_afk", "RemoteFunction", "  [attr] ", "ขนาดเมนู", "Frame", "SellFrame", "core/i18n", "ฟาร์ม", "Header", "OwnerUserId", "games/my_anime_mine/main", "Auto Chest", "games/my_anime_mine/get_plot_parts", "⚙", "[AutoChest] TakeButton click error: ", "TextLabel", "[AutoUpgrade] disabled", "Error: ", "[AutoPlace] placed ", "ValueLabel", "[AutoChest] enabled", "search_placeholder", "Badge", "not found", "[Interaction] logger disabled", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Pad", ": ", "TextButton", "Players", "CoreGui", "This is not a supported place.", "[AutoUpgrade] enabled", " doesn't exist", "ไทย", "neither appendfile nor writefile is available", "\n", "UIScale", "[MyAnimeMine] === Tool: ", "TextBox", "[SellCards] === total cards: ", "core/logger", "Text", "ตั้งค่า", "Content", "core/logger", "core/get_me", "CoreUI", "games/my_anime_mine/get_current_plot", "no tool", "[Interaction] ProximityPrompt triggered: ", "ProximityPrompt", "MineGui", "core/ui", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "th", "MekPro", "", "UICorner", "games/my_anime_mine/debug_dump", "PlotIndex"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(250)] = function()
return function(import)
local log = import(S(100))
local Players = game:GetService(S(319))
local VirtualUser = game:GetService(S(48))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(57))
        connection = player.Idled:Connect(function()
            log(S(133))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(245))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(284)] = function()
return function(import)
    local getPlaceId = import(S(213))
    local registry = import(S(244))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(363))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(225)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(251))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(378)] = function()
local Players = game:GetService(S(224))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(22))
    local backpack = player:WaitForChild(S(4))
    return player, humanoid, backpack
end
return getMe
end
modules[S(40)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(138)] = function()
local Players = game:GetService(S(26))
local STRINGS = {
    en = {
        search_placeholder = S(101),
        open_menu_hint = S(283),
    },
    th = {
        search_placeholder = S(262),
        open_menu_hint = S(255),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(168)
    if locale:sub(1, 2):lower() == S(229) then
        return S(387)
    end
    return S(113)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(291) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(210)] = function()
return function(import)
local Players = game:GetService(S(159))
local Workspace = game:GetService(S(236))
local UserInputService = game:GetService(S(237))
local log = import(S(17))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(382) .. describe(prompt)
            .. S(292) .. prompt.ActionText .. S(197))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(195) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(326) .. describe(button)
            .. S(21) .. tostring(button.Text) .. S(233))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(140)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(102)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(208)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(356))
        return
    end
    connections = {}
    log(S(328))
    local playerGui = Players.LocalPlayer:WaitForChild(S(162))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(313)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(199)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(2)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(325),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(68)] = function()
local LOG_FILE = S(118)
local function log(message)
    local line = os.date(S(303)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(86))
            end
            appendfile(LOG_FILE, line .. S(368))
        elseif writefile then
            local existing = S(59)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(331))
        else
            error(S(367))
        end
        if not (isfile and readfile) then
            error(S(83))
        end
        if not isfile(LOG_FILE) then
            error(S(310) .. LOG_FILE .. S(365))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(180) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(256) .. LOG_FILE .. S(128) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(385)] = function()
return function(import)
local i18n = import(S(41))
local UserInputService = game:GetService(S(157))
local THEME = {
    BrandName = S(388),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(160))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(390))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(201))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(190))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(9)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(231))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(264))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(258)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(171))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(148)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(51))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(72))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(105))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(336))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(142))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(95))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(8))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(125))
            if not valueLbl then
                valueLbl = Instance.new(S(212))
                valueLbl.Name = S(351)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(50))
            if not badge then
                badge = Instance.new(S(347))
                badge.Name = S(354)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(360))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(75))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(82))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(226))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(103)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(317))
        local track = Instance.new(S(263))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(114))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(261))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(247)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(54)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(97))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(116))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(70))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(374)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(112))
    screenGui.Name = S(379)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(362))
    local main = Instance.new(S(170))
    main.Name = S(15)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(369))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(124))
    tabBar.Name = S(134)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(6))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(257))
    header.Name = S(340)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(88))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(166))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(278))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(389)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(39))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(24)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(167))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(173)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(371))
    searchBox.Name = S(130)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(353))
    searchBox.Text = S(307)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(120))
    content.Name = S(376)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(23))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(279))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(36))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(32))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(329)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(327) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(252) .. (item.Subtitle or S(302))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(47))
        footerLabel.Text = i18n.T(S(304))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(137)] = function()
return function(import)
local Players = game:GetService(S(275))
local RunService = game:GetService(S(289))
local getCurrentPlot = import(S(318))
local getPlotParts = import(S(249))
local log = import(S(373))
local FIRE_INTERVAL = 1
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(221))
    local mineGui = playerGui and playerGui:FindFirstChild(S(384))
    return mineGui and mineGui:FindFirstChild(S(230))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(131))
    local actions = content and content:FindFirstChild(S(92))
    return actions and actions:FindFirstChild(S(188))
end
local function clickButton(button)
    local ok, err = pcall(function()
        firesignal(button.MouseButton1Click)
    end)
    return ok, err
end
local autoChest = {}
local connection = nil
function autoChest.SetEnabled(enabled)
    if enabled == autoChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(276))
        return
    end
    log(S(352))
    local lastFire = 0
    local lastVisibleState = nil
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        local parts = plot and getPlotParts(plot)
        local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(383), true)
        if prompt then
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if not ok then
                log(S(194) .. tostring(err))
            end
        end
        local chestFrame = getChestFrame()
        local visible = chestFrame ~= nil and chestFrame.Visible
        if visible ~= lastVisibleState then
            log(S(268) .. tostring(visible))
            lastVisibleState = visible
        end
        if visible then
            local takeButton = getTakeButton(chestFrame)
            if takeButton then
                local ok, err = clickButton(takeButton)
                if ok then
                    log(S(321))
                else
                    log(S(346) .. tostring(err))
                end
            end
        end
    end)
end
function autoChest.IsEnabled()
    return connection ~= nil
end
return autoChest
end
end
modules[S(324)] = function()
return function(import)
local RunService = game:GetService(S(215))
local getCurrentPlot = import(S(294))
local getPlotParts = import(S(216))
local log = import(S(301))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(288), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(217))
        return
    end
    log(S(243))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(13)] = function()
return function(import)
local Players = game:GetService(S(320))
local RunService = game:GetService(S(211))
local getCurrentPlot = import(S(158))
local getPlotParts = import(S(241))
local log = import(S(28))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(358))
    return pad and pad:FindFirstChildWhichIsA(S(55), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(71)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(126))
        return
    end
    log(S(266))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(184))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(205))
        local tool = backpack:FindFirstChildWhichIsA(S(84))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(350) .. tool.Name .. S(315) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(181)] = function()
return function(import)
local Players = game:GetService(S(361))
local Workspace = game:GetService(S(169))
local RunService = game:GetService(S(189))
local log = import(S(107))
local FIRE_INTERVAL = 3
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(154))
    local mineGui = playerGui and playerGui:FindFirstChild(S(311))
    return mineGui and mineGui:FindFirstChild(S(240))
end
local function labelText(button)
    local content = button:FindFirstChild(S(172))
    local label = content and content:FindFirstChild(S(238))
    return label and label.Text or S(156)
end
local function clickButton(button)
    local ok, err = pcall(function()
        firesignal(button.MouseButton1Click)
    end)
    return ok, err
end
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(80))
        return
    end
    log(S(198))
    local lastFire = 0
    local busy = false
    local lastVisibleState = nil
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL or busy then
            return
        end
        lastFire = now
        local sellStand = Workspace:FindFirstChild(S(89))
        local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(293), true)
        if prompt then
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if not ok then
                log(S(122) .. tostring(err))
            end
        end
        busy = true
        task.spawn(function()
            task.wait(0.3)
            local sellFrame = getSellFrame()
            local visible = sellFrame ~= nil and sellFrame.Visible
            if visible ~= lastVisibleState then
                log(S(214) .. tostring(visible))
                lastVisibleState = visible
            end
            if visible then
                local content = sellFrame:FindFirstChild(S(85))
                local selectAllButton = content and content:FindFirstChild(S(277))
                local sellButton = content and content:FindFirstChild(S(117))
                if selectAllButton and labelText(selectAllButton) == S(175) then
                    local ok, err = clickButton(selectAllButton)
                    if ok then
                        log(S(61))
                    else
                        log(S(29) .. tostring(err))
                    end
                    task.wait(0.3)
                end
                if sellButton and labelText(sellButton):find(S(183), 1, true) then
                    local ok, err = clickButton(sellButton)
                    if ok then
                        log(S(271))
                    else
                        log(S(187) .. tostring(err))
                    end
                end
            end
            busy = false
        end)
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(76)] = function()
return function(import)
local RunService = game:GetService(S(270))
local getCurrentPlot = import(S(145))
local getPlotParts = import(S(123))
local log = import(S(60))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(44))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(129))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(348))
        return
    end
    log(S(364))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(391)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(18)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(227) .. child.Name .. S(42)
        if child:IsA(S(299)) then
            line = line .. string.format(
                S(357),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(37)) then
            line = line .. string.format(S(52), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(206)) or child:IsA(S(273)) then
            line = line .. S(143)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(235) .. attrName .. S(53) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(285))
    end
end
return dumpInstance
end
modules[S(30)] = function()
return function(import)
local Players = game:GetService(S(65))
local log = import(S(62))
local function dumpVisible(root, indent)
    indent = indent or S(94)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(287)) and child.Visible then
            local line = indent .. child.ClassName .. S(306) .. child.Name .. S(222)
            if child:IsA(S(151)) or child:IsA(S(219)) then
                line = line .. S(108) .. tostring(child.Text) .. S(146)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(334) .. attrName .. S(164) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(177))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(106))
    local mineGui = playerGui and playerGui:FindFirstChild(S(66))
    if not mineGui then
        log(S(297))
        return
    end
    log(S(323))
    dumpVisible(mineGui)
    log(S(14))
end
return dumpMineGui
end
end
modules[S(20)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(234))
local log = import(S(242))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(248)) or descendant:IsA(S(333)) then
            count = count + 1
            log(S(282) .. descendant.ClassName .. S(359) .. descendant:GetFullName())
        end
    end
    log(S(109) .. count .. S(135))
    return count
end
return dumpRemotes
end
end
modules[S(1)] = function()
return function(import)
local Players = game:GetService(S(19))
local log = import(S(377))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(96))
    local mineGui = playerGui and playerGui:FindFirstChild(S(67))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(337))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(298)) and sellFrame.Content:FindFirstChild(S(11))
    if not scroll then
        log(S(330))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(91) or card:IsA(S(314)) then
            count = count + 1
            log(S(139) .. count .. S(308) .. card.ClassName .. S(322) .. card.Name .. S(202))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(141) .. attrName .. S(290) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(182) .. child.ClassName .. S(90) .. child.Name .. S(204)
                if child:IsA(S(99)) then
                    line = line .. S(192) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(372) .. count .. S(155))
    return count
end
return dumpSellCards
end
end
modules[S(380)] = function()
local Players = game:GetService(S(74))
local Workspace = game:GetService(S(136))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(218))
    local plots = mineWorld and mineWorld:FindFirstChild(S(93))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(341)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(344)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(46)),
        Nodes = plot:FindFirstChild(S(312)),
        Chest = plot:FindFirstChild(S(7)),
        Zones = plot:FindFirstChild(S(34)),
        Crew = plot:FindFirstChild(S(115)),
        Loot = plot:FindFirstChild(S(127)),
        PlotIndex = plot:GetAttribute(S(392)),
        ChestCapacity = plot:GetAttribute(S(31)),
    }
end
return getPlotParts
end
modules[S(342)] = function()
return function(import)
    local Players = game:GetService(S(220))
    local createWindow = import(S(163))(import)
    local i18n = import(S(338))
    local antiAfk = import(S(332))(import)
    local interactionLogger = import(S(186))(import)
    local log = import(S(153))
    local window = createWindow({
        Subtitle = S(165),
    })
    local getCurrentPlot = import(S(286))
    local getPlotParts = import(S(149))
    local dumpInstance = import(S(144))
    local dumpRemotes = import(S(3))(import)
    local dumpSellCards = import(S(246))(import)
    local dumpGui = import(S(152))(import)
    local autoMine = import(S(5))(import)
    local autoPlace = import(S(43))(import)
    local autoChest = import(S(207))(import)
    local autoSell = import(S(56))(import)
    local autoUpgrade = import(S(193))(import)
    local farmTab = window:AddTab(S(339), S(232))
    farmTab:AddCard(
        S(305),
        S(274)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(87),
        S(209)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(343),
        S(300)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(267),
        S(121)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(111),
        S(150)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(119), S(280))
    local checkBtn
    checkBtn = debugCard:AddButton(S(27), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(355)
            log(S(316))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(77),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(73),
        S(16)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(79), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(178)
            return
        end
        local ok, err = log(S(228))
        if not ok then
            dumpBtn.Text = S(185)
            dumpCard:SetSubtitle(S(349) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(272) .. firstNode.Name .. S(253))
            dumpInstance(firstNode, log)
        else
            log(S(196))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(281) .. firstHolder.Name .. S(295))
            dumpInstance(firstHolder, log)
        else
            log(S(35))
        end
        dumpBtn.Text = S(239)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(161),
        S(296)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(203), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(191))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(265))
        if not tool then
            toolDumpBtn.Text = S(381)
            return
        end
        log(S(370) .. tool.Name .. S(179))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(260) .. attrName .. S(10) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(223)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(33),
        S(110)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(38), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(254)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(64),
        S(200)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(259), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(12)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(104),
        S(78)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(174), function()
        dumpGui()
        guiDumpBtn.Text = S(49)
    end)
    local settingsTab = window:AddTab(S(375), S(345))
    settingsTab:AddCard(S(98), S(309)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(176),
        S(386)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(335), S(63), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(25), S(269), S(366) }, 1, function(index, value)
        local code = ({ S(81), S(45), S(69) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(132)] = function()
return {
    [79389059854988] = S(58),
}
end
import(S(147))(import)
