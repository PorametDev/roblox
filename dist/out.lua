-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoResearch] ", "M", "TextLabel", "M", "M", "neither appendfile nor writefile is available", "Speed", "TextLabel", "UserInputService", "getcallbackvalue", "[AutoResearch] idle: ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", " ", "Plots", "[AutoUpgrade] pickaxe #", "table", "getsenv", "[AutoResearch] ", ") ", "games/my_anime_mine/game_api", "core/dispatch", "[MekPro] settings file: ", " -> ", "autoUpgradePriority", "TextButton", " (x", "Players", "JumpPower (ปกติ 50)", "autoUpgradeRollMutation", " (", "UICorner", "เดินทะลุกำแพงและพื้น", " (prompt says '", "slot past the usable cap", "MineSelectZone", " pickaxe(s) for ", "Plots", "Damage", " wouldn't take ", "[MekPro] build ", "แรงกระโดด", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "[AutoPlot] ", " ", "Frame", " research is ", "GetHolderSpeedLevel", "core/get_me", "ReplicatedStorage", "autoPlaceUnlockBudget", "Humanoid", " (", "MineSpeed", "[AutoPlot] now mining ", ")", "Workspace", "core/logger", "action '", ": ", "Chest", "TextLabel", "AutoClaim", "auto", " ===", "games/my_anime_mine/auto_upgrade", "TextButton", "Auto Place: เปลี่ยนตัวอ่อนออก", "ต่ำสุดก่อน", "UIListLayout", ".lua", "ไทย", "Players", "function", "Empty miner slot", "", "[AutoPlace] disabled", "slot ", "core/parse_money", "[AutoPlace] drifted out of range of ", "[AutoChest] enabled (collects once >= ", ")", "AutoResearch", "Auto Sell: รอบขาย", "[AutoUpgrade] MineBuyPickaxe ", "HttpService", "autoMineClicks", ") on ", "autoUpgradeMutationBudget", "autoUpgradeLimitPickaxes", "", "fill", "autoMineSpeed", "core/debug_dump_gui", "[^%w]+", "getscripts", "open_menu_hint", "Players", "ItemName", "AutoPlace", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "UIListLayout", "[Interaction] Button activated: ", "BodyPosition", "unreadable", "", "table", "table", "ตามเครื่อง", " (chest holds ", "core/click", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", ")", "[GuiDump] PlayerGui not found", "core/run_job", " -> ", " (", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", " = ", "auto", "TextLabel", "อ่านป้ายบน chest ทุกกี่วินาที", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "OwnerUserId", "unlock", "Mutation: หยุดเมื่อได้ x", "", "RemoteFunction", "AutoPlot", "[AutoPlace] ", " -> ", "autoUpgradeInterval", "'", "ไล่ซื้อทุกกี่วินาที", "ModuleScript", "Locked miner slot", "core/humanoid", "')", "  ", "%$%s*([%d,%.]+)%s*(%a*)", "s", " #", " -> ", "autoResearch", "lowest", "ค้นหาฟังก์ชัน...", "menuScale", "ValueLabel", "CoreUI", "HumanoidRootPart", "UserInputService", "[Uu]nlock", "maxed", "Zone buying is off", "Damage", " ", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", ", have ", "CharMaxLevel", "[Decompile] === start ===", "Frame", "games/my_anime_mine/auto_claim", "^%$", "games/my_anime_mine/auto_mine", " ", "Holders", "Auto Chest: รอบเช็ค", "getrenv", "[Scheduler] ", "", "[GameAPI] SendEvent failed: ", "x", " (", "แพงสุดก่อน", "prompt disabled", "Auto Upgrade Unit", "autoPlaceUnlock", "games/registry", " ", "languageIndex", "core/ui", "TextLabel", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "MineTakeChest", "Main", "", "PlayerGui", "Players", "core/logger", "Content", "disabled", "Root", "autoUpgradeMutationTarget", "MineGui", " x", ")", "no isfile/readfile available to verify the write", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Auto Sell", "[AutoClaim] claimed ", "Auto Plot: กันเงินไว้", "ลอย (Float)", "fill", "Auto Place Miner", "MinePower", "autoSellMaxWait", "autoClaimMaxPerPass", "'", "X", "[AutoPlace] upgrading ", "Search", " stack(s)", "research", "%.3g", "2026-09-21 12:37:47", "[%X] ", "MineUpgradeCharacter", "[AutoClaim] ", "dev", "=locked(", "TextLabel", "[AutoPlace] holders: ", "autoResearchMaxPerPass", "Loot", "PlayerScripts", "[Interaction] logger enabled - go click/interact with things now", "SellStand", "OwnerUserId", "s", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " '", "Mining", "table", ": ", "My Anime Mine · Mining Hub", "Dump", "Workspace", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "core/teleport", "Auto Sell: รอคิวได้นานสุด", "getconnections", "games/my_anime_mine/auto_mine", "MineWorld", ", built ", "Damage", "", "/", "core/scheduler", "Run", "?", "ClickDetector", "0 = ไม่จำกัด", "", "core/logger", "Frame", "[GuiDump] === visible PlayerGui tree ===", "needs us at the stand - keeping the teleport", ")", "^M(%d+)$", "[AutoSell] ", "TextLabel", "jumpPower", "enabled", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "works from anywhere - no teleport needed", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "", "ChestCapacity", "TextButton", "ปุ่มเปิดเมนู", "no", "enabled", "", "ValueLabel", " screen(s)) ===", "This is not a supported place.", " for ", " reward(s) looked claimable but none went through", "autoResearchZones", "[AutoPlace] slot ", "autoSellInterval", "UIListLayout", "[AutoPlace] ", "[AutoChest] waiting: ", ", ", "MineSpeed", "AutoChest", "AutoClaim", "UIStroke", "Crew", "เช็คโซนทุกกี่วินาที", "AutoSell", ") ", "open_menu_hint", "table", "Mutation: เพดานราคาต่อครั้ง", "Auto Upgrade: Pickaxe ที่จะซื้อ", "autoUpgradeKeepMoney", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "[AutoPlot] couldn't switch to ", "disabled", "[AutoClaim] ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "Hauler", "string", ",", "M", "[GameAPI] couldn't load the game's modules: ", "(nothing to do)", "[AutoUpgrade] every stat on every miner is maxed", "[Interaction] ProximityPrompt triggered: ", "core/logger", "games/my_anime_mine/game_api", "getloadedmodules", "core/logger", "core/scheduler", "en", "UserInputService", "autoResearchCheapestFirst", "MineWorld", "core/logger", "ProximityPrompt", "core/run_job", "GetHolderLevel", "Search functions...", "games/my_anime_mine/get_current_plot", "  ", " -> ", " ", "Workspace", " from ", " (power ", "Dump All Remotes", "[AntiAFK] idle detected, simulating input", "WalkSpeed (ปกติ 16)", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "/", "games/my_anime_mine/auto_chest", "/config.json", "costs ", "x", "games/my_anime_mine/get_best_miner", "x", "unreadable", "s", "[Decompile] wrote ", " for ", "none", "lowest", "TextButton", "games/my_anime_mine/get_current_plot", "disabled", "[AutoPlot] ", "[Decompile] ", "en", "debug.getconstants", "", "[Scheduler] turn: ", "Interaction Logger", "Players", "core/logger", "th", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "Auto Upgrade: สุ่ม Mutation", "TextLabel", "core/run_job", "RunService", "needs ", "Auto Claim: รอบเช็ค", "AutoChest", "games/my_anime_mine/main", "(", "table", " team power", "Auto Research (Plot)", "TextLabel", "locked slot", "[AutoPlace] placed ", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", " ชิ้น", "AutoUpgrade", "([%d,%.]+)%s*(%a*)", "ฟาร์ม", "core/logger", "research", "games/my_anime_mine/auto_upgrade", "[Logger] failed to write ", "[AutoUpgrade] ", " ", "autoPlotKeepMoney", ")", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "MekPro/decompiled", "0e6a1f3+", "core/run_job", "[AutoPlace] picked up ", "[Decompile] PlayerScripts not found", "GuiButton", "^x(%d+)$", "Auto Claim: เก็บสูงสุดต่อรอบ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "antiAfk", "fly", "Frame", " files", " = ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ดีบัก", "core/debug_decompile", "[AutoResearch] ", "server didn't equip it", "TextLabel", "LocalScript", "[AutoUpgrade] ", "Decompile Client Scripts", "games/my_anime_mine/auto_place", "BasePart", "Auto Plot: รอบเช็ค", "upgrade", "TextLabel", "core/logger", ")", "%.0f", "Speed", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", ")", "TextButton", "ProximityPrompt", "M", "AutoUpgrade", "autoPlaceUpgradeWeak", "[AutoPlace] couldn't pick up ", " not bought yet (", "", "ย่อขยายเมนูทั้งชุด", "noclip", "core/run_job", "Players", " power ", " (", "Auto Chest", "core/parse_money", "BodyVelocity", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ") ", "ถูกสุดก่อน", "hookfunction", "core/config", "Workspace", "search_placeholder", "PlotIndex", "Humanoid", "autoMine", " items (now ", "มุดแมพ (Noclip)", ": ", "core/get_place_id", "slot not in the hotbar", "s", "MekPro", "GetHolderMineSpeedLevel", " ", "ความเร็ววิ่ง", "autoResearchKeepMoney", " ", "Auto Place: รอบเช็ค", "[AutoChest] collected ", "Auto Claim (Index)", "[AutoPlot] unlocked ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "menuScale", "", "Players", " mutation x", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "", " pickaxe ", "Core", "table", "none", "core/logger", "  [attr] ", ", base ", "core/ui", "\n", "[GuiDump] === end (", "/", "core/humanoid", "%$", "games/my_anime_mine/get_best_miner", "[AutoMine] ", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " (Text=", "Pad", "TextButton", "[AutoUpgrade] ", "0 = ไม่จำกัด", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "empty", "s", "[AutoPlace] unlocked ", "MineBuySkill", "Hauler", "[AutoPlace] ", "UIScale", "ZoneBoardGui", "HotbarFrame", "] job failed: ", "search_placeholder", "", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "Mining", "Damage", "Workspace", "[AutoChest] chest is empty", "autoSellProtectUnits", "games/my_anime_mine/auto_plot", "TextBox", " doesn't exist", "autoPlace", "games/my_anime_mine/game_api", "core/logger", "number", " while selecting", "autoResearchCoreLast", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", ")", "autoChestMinItems", "Nodes", ") needs ", "PickaxesStand", "core/logger", "core/anti_afk", "GetCharacterSpeedUpgradeCost", "empty", "MineRollMutation", "nothing to place or upgrade", "Auto Chest: เกณฑ์เก็บ", "MineEquipSlot", "Auto Research: รอบเช็ค", " still locked after paying ", "%.3g", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "%.3g", ")", "games/my_anime_mine/game_api", "", "[Interaction] ClickDetector clicked: ", "", ")", "table", "autoResearchCore", "List", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "[AutoUpgrade] ", ")", "core/teleport", "[AutoResearch] nothing open and affordable right now", "core/get_place_id", "float", "", "ขนาดเมนู", "GetCharacterMineSpeedUpgradeCost", "cost", "cheapest open node ", "_", "[AutoUpgrade] ", "ClickDetector", ")", " (", "Auto Plot: ใช้โซนล่าสุดเสมอ", "s", "Workspace", "button has no size on screen", "Frame", " failed)", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "autoPlaceInterval", "games/my_anime_mine/auto_place", ", you have ", "[Config] couldn't read ", "/", " script(s) to ", "PlayerGui", "core/anti_afk", "[AutoPlace] couldn't hold anything - not pressing E", ") -> ", "works from anywhere - dropping the teleport", "core/logger", "ตั้งค่า", "autoUpgrade", ")", "[AutoSell] disabled", "[", "Auto Mine: คลิกต่อรอบ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " costs ", "string", "[Remotes] === total: ", "core/debug_interaction_logger", "games/my_anime_mine/get_plot_parts", "Content", ") from ", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "core/parse_money", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "CharSpeedMaxLevel", "games/my_anime_mine/main", "MineEquipPickaxe", "x", "", "sale didn't go through", "Auto Research: ซื้อสาย Zone", "enabled", "games/my_anime_mine/auto_research", "Players", "th", "Speed", "บิน (Fly)", " | ", "cost", "debug.getproto", " (", "en", "table", "AutoResearch", "core/run_job", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Text", "[GuiDump] ", " (power ", "autoSell", " didn't go through (out of stock or too expensive)", "string", " screens", "TextButton", "function", "core/logger", "MineBuyPickaxe", "GuiService", "autoChestInterval", "core/dispatch", "ClickDetector", " waited ", ": ", "string", "ZoneBoard", "table", "Core buying is off", "BasePart", "[AntiAFK] disabled", "Character", "affordable - waiting for a turn", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "", "core/teleport", "VirtualInputManager", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "BasePart", "Anti-AFK", "MekPro", "core/build_env", "TextLabel", "[Interaction] Key pressed: %s (gameProcessed=%s)", "[^%.]+", "nothing left to buy (", "lowest", "CoreGui", "core/config", "string", "PlayerGui", "core/debug_interaction_logger", "AutoPlace", "decompile", "games/my_anime_mine/get_plot_parts", "[AutoPlace] fireproximityprompt error: ", "games/my_anime_mine/game_api", "", "table", "Container", "th", "Auto Place: ปลดล็อกช่อง miner", "TabBar", "TextButton", "MineSpeed", " owned)", "core/get_leaderstat", "[Interaction] logger disabled", "core/debug_decompile", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " remotes", "core/logger", "'", "interactionLogger", "[AutoPlot] ", "walkSpeed", "AutoMine", "/", "/", "games/my_anime_mine/auto_chest", "/", "-", "core/logger", "autoPlotInterval", " team power, you have ", "autoPlotLatest", " items", "[Scheduler] ", "ขายทุกกี่วินาที", "Auto Plot: ซื้อโซนถัดไป", "Players", "table", "Zones", " (cost ", "=empty", " (", "autoUpgradePickaxeIndex", "Auto Upgrade: กันเงินไว้", "core/run_job", "core/get_place_id", "autoUpgradeMaxPerPass", " items)", "core/build_env", "Frame", "Frame", "Auto Upgrade: แจก Pickaxe ให้ miner", "write reported success but ", "[Remotes] ", "core/i18n", "function", "Open menu", "string", "[Decompile] === end ===", ")", "core/debug_dump_remotes", "sold ", "UICorner", "core/teleport", "Auto Mine: ความเร็ว", "games/my_anime_mine/game_api", "%", "Dump Visible GUI", "ZoneRow", "games/my_anime_mine/auto_research", "core/run_job", "", "Modules", " possible holder(s) - not buying more", "[AutoSell] enabled", "core/debug_tab", "s - carrying on without it", "core/debug_dump_remotes", "RemoteEvent", "[AutoResearch] cheapest open node: ", "TextLabel", " '", " error: ", "Backpack", "TextLabel", "#", " (money ", "%.0f", " ", "", "games/my_anime_mine/game_api", "core/logger", "core/debug_tab", "TextButton", "%$", "auto", "=", "[AutoSell] MineSellItems ", "[AutoChest] MineTakeChest had no effect (", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", ": ", "UIPadding", "core/i18n", "RunService", "Players", "", "autoClaimInterval", "leaderstats", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "waiting", "LayerCollector", "Players", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "games/registry", "HumanoidRootPart", "[AutoPlace] nothing in the hotbar to place", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "both Zone and Core buying are switched off", "MekPro", "GuiObject", "[AutoPlace] unlocking ", ") ", "every open node is maxed", "core/debug_dump_gui", "s - jumping the queue", "needs ", "getgc", "may need us at the stand - trying that", ",", ")", "Dump", "Auto Research: ซื้อสูงสุดต่อรอบ", "Text", "Header", "Auto Upgrade: รอบเช็ค", "Players", "AutoPlace", "[AntiAFK] enabled - watching Player.Idled", "th", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "waiting", "autoClaim", "[Scheduler] ", "BasePart", "Badge", "x", "Auto Mine", "ReplicatedStorage", "[Config] couldn't write ", "Shop Stand", "autoSellMaxWait", "ScrollingFrame", "[AutoChest] disabled", "unlock", "[AutoUpgrade] bought pickaxe #", " (still ", ")", "GetCharacterUpgradeCost", "\n", "Frame", ",", "MineSellItems", ": ", "s", "s", "อังกฤษ", "write reported success but the line isn't in ", "autoChest", " (", "TextLabel", "auto", "getnilinstances", "autoResearchInterval", " has been busy for ", "games/my_anime_mine/auto_sell", "🏠", "games/my_anime_mine/auto_sell", " costs ", "ProximityPrompt", "disabled", "autoUpgradeEquipPickaxe", " (", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "Auto Place: เพดานราคาปลดช่อง", "enabled", "TextLabel", "AutoPlace", "ScreenGui", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "[Bb]uy", "Auto Upgrade Plot (Zone)", "[Decompile] decompile unavailable in this executor", " - upgrade the miners first", "HumanoidRootPart", " (Text=", " ", "debug.getupvalues", "[AutoChest] can't read the chest yet", "games/my_anime_mine/auto_claim", "core/logger", "", "games/my_anime_mine/game_api", "/s", "MekPro", "autoPlot", "[AutoUpgrade] ", "Frame", "[BestMiner] slot ", "^([%d,]+)$", "", "ProximityPrompt", "GuiButton", "⚙", "VirtualUser", "table", "enabled", "CharMineSpeedMaxLevel", "MekPro.log", " (x", "autoUpgradePickaxe", "autoPlotBuy", "table", ")", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Players", " (", "/", "All", "Content", " (ActionText=", " refused - ", "Badge", "yes", "AutoPlot", "TextLabel", " -> x", " node(s) looked buyable but the server refused", "games/my_anime_mine/get_plot_parts", "core/logger", "Frame", "games/my_anime_mine/auto_plot", "games/my_anime_mine/get_current_plot", "disabled", " still empty after placing ", "en-us", "autoUpgradePickaxeIndex", "[Decompile] === executor capabilities ===", "games/my_anime_mine/game_api", "AutoSell", "[AutoResearch] Core ", "languageIndex", "getscriptbytecode", ") didn't take: ", "core/i18n", " (", "Auto Research: กันเงินไว้", "Frame", "🐛", "games/my_anime_mine/game_api", "Auto Research: ซื้อสาย Core", "ItemFrame", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/logger", "[AutoResearch] "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(534)] = function()
return function(import)
local log = import(S(321))
local Players = game:GetService(S(439))
local VirtualUser = game:GetService(S(892))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(815))
        connection = player.Idled:Connect(function()
            log(S(334))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(652))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(725)] = function()
return { Env = S(218), IsDev = true, Commit = S(395), BuiltAt = S(214) }
end
modules[S(110)] = function()
local VirtualInputManager = game:GetService(S(658))
local GuiService = game:GetService(S(641))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(575)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(670)] = function()
return function(import)
local HttpService = game:GetService(S(85))
local getPlaceId = import(S(722))
local log = import(S(878))
local ROOT_FOLDER = S(796)
local placeFolder = ROOT_FOLDER .. S(246) .. tostring(getPlaceId())
local filePath = placeFolder .. S(339)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(107) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(582) .. filePath .. S(233) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(827) .. filePath .. S(59) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(410)] = function()
return function(import)
local Players = game:GetService(S(781))
local log = import(S(639))
local CAPABILITIES = {
    S(675), S(930), S(95), S(804), S(314),
    S(240), S(10), S(17), S(167), S(356),
    S(875), S(623), S(448), S(850),
}
local OUT_FOLDER = S(394)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(666)) do
        if type(target) ~= S(232) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(925))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(354) .. name .. S(407) .. ((ok and type(value) == S(732)) and S(911) or S(270)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(870))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(224))
    if not playerScripts then
        log(S(398))
        return 0
    end
    pcall(makefolder, S(461))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(414)) or target:IsA(S(134)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(305) and #source > 0 then
                local name = target:GetFullName():gsub(S(94), S(567))
                if pcall(writefile, OUT_FOLDER .. S(703) .. name .. S(70), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(346) .. written .. S(584) .. OUT_FOLDER
        .. S(624) .. failed .. S(577))
    return written
end
local function run()
    log(S(159))
    reportCapabilities()
    local count = dumpAll()
    log(S(735))
    return count
end
return run
end
end
modules[S(801)] = function()
return function(import)
local Players = game:GetService(S(713))
local log = import(S(694))
local function dumpVisible(root, indent)
    indent = indent or S(656)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(797)) and child.Visible then
            local line = indent .. child.ClassName .. S(758) .. child.Name .. S(132)
            if child:IsA(S(61)) or child:IsA(S(66)) then
                line = line .. S(494) .. tostring(child.Text) .. S(901)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(483) .. attrName .. S(118) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(327))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(585))
    if not playerGui then
        log(S(113))
        return 0
    end
    local screens = 0
    log(S(255))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(788)) and screen.Enabled and screen.Name ~= S(662) then
            screens = screens + 1
            log(S(631) .. screen.ClassName .. S(230) .. screen.Name .. S(207))
            dumpVisible(screen, S(138))
        end
    end
    log(S(487) .. screens .. S(274))
    return screens
end
return dumpGui
end
end
modules[S(754)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(49))
local log = import(S(941))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(755)) or descendant:IsA(S(127)) then
            count = count + 1
            log(S(730) .. descendant.ClassName .. S(646) .. descendant:GetFullName())
        end
    end
    log(S(600) .. count .. S(64))
    return count
end
return dumpRemotes
end
end
modules[S(673)] = function()
return function(import)
local Players = game:GetService(S(617))
local Workspace = game:GetService(S(574))
local UserInputService = game:GetService(S(150))
local log = import(S(705))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(311) .. describe(prompt)
            .. S(908) .. prompt.ActionText .. S(570))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(549) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(102) .. describe(button)
            .. S(873) .. tostring(button.Text) .. S(528))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(322)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(569)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(399)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(689))
        return
    end
    connections = {}
    log(S(225))
    local playerGui = Players.LocalPlayer:WaitForChild(S(186))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(857)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(644)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(890)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(665),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(752)] = function()
return function(import)
local interactionLogger = import(S(601))(import)
local dumpRemotes = import(S(737))(import)
local dumpGui = import(S(93))(import)
local decompileScripts = import(S(690))(import)
local humanoid = import(S(489))
local function build(window, persist)
    local tab = window:AddTab(S(409), S(936))
    persist.Toggle(
        tab:AddCard(
            S(359),
            S(197)
        ),
        S(696),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(333),
        S(237)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(808), function()
        remoteBtn.Text = dumpRemotes() .. S(693)
    end)
    local guiCard = tab:AddCard(
        S(744),
        S(499)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(235), function()
        guiBtn.Text = dumpGui() .. S(636)
    end)
    local decompileCard = tab:AddCard(
        S(416),
        S(12)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(248), function()
        decompileBtn.Text = decompileScripts() .. S(406)
    end)
    humanoid.Start()
    persist.Slider(tab, S(464), S(335),
        8, 200, S(698), 16, humanoid.SetWalkSpeed, S(357))
    persist.Slider(tab, S(41), S(28),
        30, 300, S(261), 50, humanoid.SetJumpPower, S(105))
    persist.Toggle(
        tab:AddCard(S(620), S(156)),
        S(404), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(456), S(32)),
        S(437), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(201), S(299)),
        S(561), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(21)] = function()
return function(import)
    local getPlaceId = import(S(560))
    local registry = import(S(177))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(275))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(688)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(784))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(48)] = function()
local Players = game:GetService(S(813))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(453))
    local backpack = player:WaitForChild(S(760))
    return player, humanoid, backpack
end
return getMe
end
modules[S(458)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(136)] = function()
local Players = game:GetService(S(789))
local RunService = game:GetService(S(367))
local UserInputService = game:GetService(S(318))
local Workspace = game:GetService(S(236))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(51))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(149))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(444))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(822)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(651)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(103))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(779)] = function()
local Players = game:GetService(S(27))
local STRINGS = {
    en = {
        search_placeholder = S(325),
        open_menu_hint = S(733),
    },
    th = {
        search_placeholder = S(145),
        open_menu_hint = S(269),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(923)
    if locale:sub(1, 2):lower() == S(362) then
        return S(682)
    end
    return S(355)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(63) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(422)] = function()
local LOG_FILE = S(896)
local function log(message)
    local line = os.date(S(215)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(169))
            end
            appendfile(LOG_FILE, line .. S(486))
        elseif writefile then
            local existing = S(679)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(837))
        else
            error(S(6))
        end
        if not (isfile and readfile) then
            error(S(196))
        end
        if not isfile(LOG_FILE) then
            error(S(729) .. LOG_FILE .. S(520))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(845) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(387) .. LOG_FILE .. S(777) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(78)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(635) then
        return nil
    end
    local number, suffix = text:match(S(139))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(839), S(888))))
    if not value then
        return nil
    end
    if suffix ~= S(477) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(323)] = function()
return function(import)
local log = import(S(57))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(595) .. name .. S(509) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(247)] = function()
return function(import)
local RunService = game:GetService(S(780))
local log = import(S(590))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(710) .. task_.name .. S(759) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(168) .. task_.name .. S(852)
                    .. string.format(S(424), now - task_.busySince)
                    .. S(753))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(821) .. starved.name .. S(645)
            .. string.format(S(764), now - starved.waitingSince)
            .. S(802))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(358) .. (currentTask and currentTask.name or S(309)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(657)] = function()
local Players = game:GetService(S(903))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(792))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(180)] = function()
return function(import)
local i18n = import(S(932))
local UserInputService = game:GetService(S(9))
local THEME = {
    BrandName = S(882),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(739))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(31))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(778))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(637))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(748)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(288))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(3))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(249)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(413))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(252)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(830))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(281))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(376))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(918))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(260))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(761))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(181))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(273))
            if not valueLbl then
                valueLbl = Instance.new(S(913))
                valueLbl.Name = S(147)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(910))
            if not badge then
                badge = Instance.new(S(757))
                badge.Name = S(823)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(496))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(726))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(838))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(685))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(245)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(743)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(727))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(935))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(770))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(612)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(45))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(101))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(268))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(630)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(866))
    screenGui.Name = S(148)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(669))
    local main = Instance.new(S(160))
    main.Name = S(184)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(506))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(254))
    tabBar.Name = S(684)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(69))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(576))
    header.Name = S(811)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(428))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(8))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(864))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(435)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(350))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(208)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(25))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(704)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(519))
    searchBox.Name = S(210)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(510))
    searchBox.Text = S(272)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(885))
    content.Name = S(603)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(405))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(120))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(96))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(421))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(810)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(879) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(329) .. (item.Subtitle or S(782))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(451))
        footerLabel.Text = i18n.T(S(293))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(338)] = function()
return function(import)
local gameApi = import(S(926))(import)
local log = import(S(482))
local runGuarded = import(S(366))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(80) .. minItemsToCollect .. S(724))
    else
        log(S(831))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(104) then
            log(S(876))
            lastReported = S(344)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(536) then
            log(S(516))
            lastReported = S(500)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(787) then
            local capacity = gameApi.ChestCapacity()
            log(S(283) .. count .. S(337) .. want .. S(709)
                .. (capacity and (S(109) .. capacity .. S(551)) or S(511)))
            lastReported = S(819)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(286), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(468) .. count .. S(455) .. after .. S(257))
        else
            log(S(775) .. count
                .. S(142) .. tostring(after) .. S(55))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(161)] = function()
return function(import)
local gameApi = import(S(313))(import)
local log = import(S(312))
local runGuarded = import(S(396))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(217) .. (enabled and S(863) or S(352)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(199) .. reward.category .. S(765)
                .. tostring(reward.tierKey) .. S(141) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(302) .. #pending .. S(277))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(287), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(241)] = function()
return function(import)
local getCurrentPlot = import(S(351))
local getPlotParts = import(S(602))
local log = import(S(315))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(250), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(492) .. (enabled and S(271) or S(858)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(417)] = function()
return function(import)
local Players = game:GetService(S(360))
local getCurrentPlot = import(S(326))
local getPlotParts = import(S(676))
local getMinerSlots = import(S(491))(import)
local teleport = import(S(238))
local parseMoney = import(S(443))
local gameApi = import(S(20))(import)
local log = import(S(253))
local runGuarded = import(S(721))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(74)
local LOCKED_SLOT_TEXT = S(135)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(495))
    return pad and pad:FindFirstChildWhichIsA(S(429), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(365)) then
            local digits = descendant.Text:match(S(887))
            if digits then
                return tonumber((digits:gsub(S(806), S(266))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(664)) and descendant.Text:match(S(162)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(77) .. holder.Name .. S(434)
            .. gameApi.MinerSlots() .. S(905) .. gameApi.MaxMinerSlots() .. S(687)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(377)
    end
    if not prompt.Enabled then
        return true, S(174)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(340) .. price
    end
    local action = prompt.ActionText or S(562)
    if action:match(S(151)) or action:match(S(868)) or action:match(S(771)) then
        return true, S(58) .. action .. S(695)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(872))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(677) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(219) .. why .. S(195))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(717))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(773) .. prompt.ObjectText
                    .. S(372) .. shown .. S(484) .. math.floor(base) .. S(736))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(621))
    if line ~= lastSurvey then
        log(S(221) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(91), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(420), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(124), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(505) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(76))
    else
        lastAttempt = 0
        log(S(392))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(786),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(459)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(34)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(412)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(793))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(79) .. holder.Name .. S(525))
            return false
        end
        log(S(279) .. candidate.slot .. S(30) .. candidate.name
            .. S(931) .. tostring(why))
    end
    if not miner then
        log(S(587))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(129) .. holder.Name .. S(922) .. miner.name)
        return false
    end
    log(S(378) .. miner.name .. S(632) .. miner.power
        .. S(87) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(433) .. entry.name .. S(331) .. entry.holder.Name
            .. S(33) .. tostring(entry.prompt.ActionText) .. S(137))
        return false
    end
    log(S(397) .. entry.name .. S(332) .. entry.power
        .. S(604) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(282) .. pad.holder.Name .. S(542)
                .. tostring(pad.priceText))
        else
            log(S(502) .. pad.holder.Name
                .. S(347) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(538))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(202) then
        runGuarded(S(814), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(832) then
        log(S(798) .. target.holder.Name
            .. S(276) .. tostring(target.priceText))
        runGuarded(S(865), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(209) .. target.holder.Name .. S(841) .. target.name
        .. S(718) .. target.power .. S(588) .. best.name .. S(860) .. best.power .. S(807))
    runGuarded(S(674), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(919)] = function()
return function(import)
local gameApi = import(S(547))(import)
local parseMoney = import(S(606))
local log = import(S(768))
local runGuarded = import(S(628))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(697) .. (enabled and S(615) or S(301)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(490)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(54) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(571) .. deepestKey .. S(81))
        return true
    end
    log(S(300) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(353) .. gameApi.ZoneDisplayName(zoneKey) .. S(598)
                .. math.floor(price) .. S(581) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(470) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(52) .. zoneKey .. S(557))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(212) then
            reportedLocked = S(385)
            log(S(43) .. zoneKey .. S(909)
                .. gameApi.ZoneDisplayName(currentKey) .. S(46)
                .. done .. S(701) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(912), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(746)] = function()
return function(import)
local gameApi = import(S(880))(import)
local log = import(S(188))
local runGuarded = import(S(438))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(411) .. (enabled and S(894) or S(921)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(18) .. node.zoneKey .. S(463) .. node.id
                    .. S(116) .. node.family .. S(19) .. node.level .. S(115) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(928) .. node.id
                    .. S(847) .. node.family .. S(446) .. node.level .. S(130) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(1) .. blocked
                .. S(915))
        elseif powerBlocked then
            log(S(942) .. powerBlocked.zoneKey .. S(874) .. powerBlocked.id
                .. S(441) .. powerBlocked.family .. S(531)
                .. math.floor(powerBlocked.powerReq) .. S(707)
                .. math.floor(powerBlocked.power)
                .. S(871))
        else
            log(S(559)
                .. S(763) .. math.floor(gameApi.GetMoney()) .. S(391))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(756) .. nearest.zoneKey
                    .. S(44) .. nearest.id .. S(933) .. nearest.family .. S(292)
                    .. nearest.level .. S(700) .. nearest.max
                    .. S(856) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(795)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(479),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(153))
        end
        if not buyCore then
            table.insert(parts, S(650))
        end
        if #parts == 0 then
            return S(800)
        end
        return S(667) .. table.concat(parts, S(284)) .. S(593)
    end
    local why
    if nearest.cost > budget then
        why = S(368) .. string.format(S(543), nearest.cost)
            .. S(157) .. string.format(S(545), money)
    elseif nearest.powerOk == false then
        why = S(803) .. string.format(S(213), nearest.powerReq) .. S(374)
    else
        why = S(654)
    end
    return S(566) .. nearest.zoneKey .. S(178) .. tostring(nearest.id)
        .. S(172) .. tostring(nearest.family) .. S(799) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(11) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(627), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(853)] = function()
return function(import)
local Workspace = game:GetService(S(56))
local gameApi = import(S(742))(import)
local teleport = import(S(558))
local log = import(S(523))
local runGuarded = import(S(114))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(594))
    else
        lastAttempt = 0
        log(S(751))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(226))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(889), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(774)
                .. (sold and S(589)
                         or S(256)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(259) .. (sold and (S(738) .. #keys .. S(211)) or S(613)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(291), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(65)] = function()
return function(import)
local Workspace = game:GetService(S(450))
local gameApi = import(S(767))(import)
local teleport = import(S(740))
local log = import(S(533))
local runGuarded = import(S(747))(import)
local STATS = { S(38), S(619), S(285) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(668)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(568) .. (enabled and S(262) or S(190)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(900) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(349) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(622) or S(152)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(388) .. holderKey .. S(164) .. choice.stat .. S(155)
                        .. choice.info.level .. S(328) .. after.level
                        .. S(583) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(565) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(532))
    local shop = stand and stand:FindFirstChild(S(828))
    if not shop then
        return nil
    end
    if shop:IsA(S(660)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(418), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(84)
                .. (bought and S(264)
                           or S(805)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(363))
        end
    end
    if bought then
        log(S(833) .. pickaxe.index .. S(466) .. pickaxe.name
            .. S(26) .. pickaxe.multiplier .. S(423))
        return true
    end
    log(S(15) .. pickaxe.index .. S(13) .. pickaxe.name
        .. S(634))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(497) .. holderKey .. S(478)
                    .. tostring(worn or S(481)) .. S(23) .. wanted.name)
            else
                log(S(415) .. holderKey .. S(39)
                    .. wanted.name .. S(834) .. tostring(nowWorn or S(348)) .. S(546))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(556) .. holderKey .. S(475) .. variant
                .. S(914) .. after .. S(716) .. cost .. S(112))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(884) .. gameApi.OwnedPickaxeCount()
                    .. S(36) .. gameApi.MaxMinerSlots()
                    .. S(750))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(605))
        elseif maxed > 0 then
            log(S(310))
        else
            log(S(265))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(381), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(937)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(826))
local Workspace = game:GetService(S(330))
local log = import(S(917))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(749), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(308) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(552))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(649) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(304) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(714) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(16) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(504) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(258))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(170) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(183))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(906), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(106) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(373) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(653) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(840), keys)
end
function api.EquipSlot(slot)
    return send(S(540), slot == S(473) and S(75) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(640), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(610), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(537), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(503), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(893)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(474)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(204))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(73) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(680) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(35), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(320))
    plots = plots and plots:FindFirstChild(S(37))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(97)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(123)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(648))
    local gui = board and board:FindFirstChild(S(507))
    local root = gui and gui:FindFirstChild(S(191))
    local list = root and root:FindFirstChild(S(554))
    local row = list and list:FindFirstChild(S(745) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(220)) and descendant.Text ~= S(185) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(638) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(216), characterKey, stat == S(513) and S(53) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(324), max = S(158), cost = S(836) },
    Speed = { level = S(47), max = S(608), cost = S(535) },
    MineSpeed = { level = S(462), max = S(895), cost = S(564) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(599) or charName == S(766) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(294) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(524) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(734) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(626) and type(holder.CharName) == S(671) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(480) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(647) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(342)] = function()
return function(import)
local Players = game:GetService(S(72))
local gameApi = import(S(522))(import)
local log = import(S(384))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(382))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(306), S(550)))) or 0
    if suffix ~= S(548) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(672))
    local mineGui = playerGui and playerGui:FindFirstChild(S(193))
    local hotbar = mineGui and mineGui:FindFirstChild(S(508))
    local content = hotbar and hotbar:FindFirstChild(S(189))
    return content and content:FindFirstChild(S(681))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(848)) then
            local count = descendant.Text:match(S(400))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(939))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(907))
    local nameLabel = content and content:FindFirstChild(S(98))
    local damageLabel = content and content:FindFirstChild(S(154))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(90) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(488) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(886) .. slot .. S(457) .. info.name
                        .. S(440) .. info.power .. S(194) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(920)] = function()
local Players = game:GetService(S(187))
local Workspace = game:GetService(S(515))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(242))
    local plots = mineWorld and mineWorld:FindFirstChild(S(14))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(227)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(916)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(165)),
        Nodes = plot:FindFirstChild(S(530)),
        Chest = plot:FindFirstChild(S(60)),
        Zones = plot:FindFirstChild(S(715)),
        Crew = plot:FindFirstChild(S(289)),
        Loot = plot:FindFirstChild(S(223)),
        PlotIndex = plot:GetAttribute(S(452)),
        ChestCapacity = plot:GetAttribute(S(267)),
    }
end
return getPlotParts
end
modules[S(371)] = function()
return function(import)
    local createWindow = import(S(485))(import)
    local i18n = import(S(731))
    local antiAfk = import(S(586))(import)
    local buildEnv = import(S(663))
    local scheduler = import(S(316))(import)
    local config = import(S(449))(import)
    local log = import(S(361))
    log(S(40) .. buildEnv.Commit .. S(904) .. buildEnv.Env
        .. S(243) .. buildEnv.BuiltAt .. S(835))
    local window = createWindow({
        Subtitle = S(234),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(163))(import)
    local autoPlace = import(S(580))(import)
    local autoChest = import(S(702))(import)
    local autoSell = import(S(855))(import)
    local autoUpgrade = import(S(386))(import)
    local autoResearch = import(S(616))(import)
    local autoClaim = import(S(877))(import)
    local autoPlot = import(S(518))(import)
    local gameApi = import(S(678))(import)
    scheduler.RegisterStandalone(S(699), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(370), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(99), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(431), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(82), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(62), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(128), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(829), 20)
    scheduler.RegisterExclusive(S(927), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(383), S(854))
    persistentToggle(farmTab:AddCard(
        S(825),
        S(471)
    ), S(454), autoMine.SetEnabled)
    persistentSlider(farmTab, S(741), S(544),
        1, 30, S(92), 5, autoMine.SetCyclesPerSecond, S(881))
    persistentSlider(farmTab, S(596), S(607),
        1, 10, S(86), 1, autoMine.SetClicksPerCycle, S(824))
    persistentToggle(farmTab:AddCard(
        S(203),
        S(527)
    ), S(521), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(467), S(597),
        1, 60, S(579), 3, autoPlace.SetCheckInterval, S(228))
    persistentToggle(
        farmTab:AddCard(S(67),
            S(629)),
        S(432), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(683),
            S(655)),
        S(176), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(862), S(498),
        0, 500, S(50), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(4))
    persistentToggle(farmTab:AddCard(
        S(442),
        S(493)
    ), S(846), autoChest.SetEnabled)
    persistentSlider(farmTab, S(539), S(182),
        50, 3000, S(529), 100, autoChest.SetMinItems, S(380))
    persistentSlider(farmTab, S(166), S(121),
        1, 60, S(642), 3, autoChest.SetCheckInterval, S(345))
    persistentToggle(farmTab:AddCard(
        S(198),
        S(100)
    ), S(633), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(393),
            S(476)),
        S(517), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(83), S(711),
        5, 300, S(280), 20, autoSell.SetCheckInterval, S(573))
    farmTab:AddSlider(S(239),
        S(298),
        5, 120, sellMaxWait, function(value)
            config.Set(S(205), value)
        end, S(843))
    persistentToggle(farmTab:AddCard(
        S(175),
        S(692)
    ), S(592), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(812), S(785),
        5, 300, S(131), 20, autoUpgrade.SetInterval, S(460))
    persistentSlider(farmTab, S(691), S(408),
        1, 20, S(723), 5, autoUpgrade.SetMaxPerPass, S(341))
    persistentSegmented(
        farmTab,
        { S(68), S(244), S(7), S(231) },
        { S(144), S(514), S(425), S(686) },
        S(24), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(720), S(122),
        0, 500, S(297), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(307))
    persistentToggle(
        farmTab:AddCard(S(659),
            S(555)),
        S(898), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(897) .. pickaxe.multiplier .. S(427)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(867)
            end
            return S(762) .. index .. S(389) .. (pickaxeLabels[index] or S(303))
        end
        local savedPick = config.Get(S(924), 0)
        pickaxeCard = farmTab:AddSlider(
            S(296),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(719), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(126)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(790),
            S(512)),
        S(89), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(728),
            S(578)),
        S(859), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(364),
            S(117)),
        S(29), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(125), S(336),
        1, 20, S(192), 2, autoUpgrade.SetMutationTarget, S(343))
    persistentSlider(farmTab, S(295), S(251),
        0, 500, S(88), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(430))
    persistentToggle(farmTab:AddCard(
        S(375),
        S(818)
    ), S(143), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(541), S(133),
        10, 600, S(851), 30, autoResearch.SetCheckInterval, S(140))
    persistentSlider(farmTab, S(809), S(402),
        1, 20, S(222), 5, autoResearch.SetMaxPerPass, S(171))
    persistentSlider(farmTab, S(934), S(445),
        0, 500, S(465), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(2))
    persistentToggle(
        farmTab:AddCard(S(614),
            S(940)),
        S(278), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(938),
            S(426)),
        S(553), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(111),
            S(794)),
        S(526), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(447), S(173) },
        { true, false },
        S(319), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(469),
        S(902)
    ), S(820), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(369), S(379),
        5, 300, S(783), 30, autoClaim.SetCheckInterval, S(501))
    persistentSlider(farmTab, S(401), S(229),
        1, 30, S(206), 10, autoClaim.SetMaxPerPass, S(611))
    persistentToggle(farmTab:AddCard(
        S(869),
        S(861)
    ), S(883), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(419), S(290),
        5, 300, S(706), 30, autoPlot.SetCheckInterval, S(842))
    persistentToggle(
        farmTab:AddCard(S(712),
            S(263)),
        S(899), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(572),
            S(776)),
        S(708), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(200), S(42),
        0, 500, S(390), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(5))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(769))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(591), S(891))
    persistentToggle(
        settingsTab:AddCard(S(661), S(817)),
        S(403),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(472), 100)
    settingsTab:AddSlider(S(563), S(436), 50, 150, savedScale, function(value)
        config.Set(S(146), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(929), 1)
    settingsTab:AddSegmented({ S(108), S(844), S(71) }, savedLanguage, function(index, value)
        local code = ({ S(849), S(317), S(816) })[index]
        config.Set(S(179), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(119), S(625), S(618) })[savedLanguage] or S(772))
    log(S(22) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(791)] = function()
return {
    [79389059854988] = S(609),
}
end
import(S(643))(import)
