-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "MineSellItems", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " screen(s)) ===", "enabled", "[AutoResearch] Core ", "s", "AutoPlot", "ย่อขยายเมนูทั้งชุด", "core/logger", "TextButton", "yes", "TabBar", "AutoUpgrade", "GuiButton", "MineEquipPickaxe", "games/my_anime_mine/get_plot_parts", "=", "GetHolderSpeedLevel", "[GuiDump] === end (", "games/my_anime_mine/game_api", "OwnerUserId", ",", "-", "[AutoUpgrade] ", "/", "ItemName", "autoPlotBuy", "ดีบัก", "games/my_anime_mine/get_best_miner", ") on ", "8e58cd6+", "TextLabel", "Workspace", "s", "UserInputService", "[AutoPlace] couldn't pick up ", "CharMaxLevel", "ความเร็ววิ่ง", "function", "games/my_anime_mine/get_current_plot", "%$", "ProximityPrompt", "[Interaction] Key pressed: %s (gameProcessed=%s)", "[AutoPlace] holders: ", "[^%.]+", "JumpPower (ปกติ 50)", "autoChestMinItems", "[AutoUpgrade] ", "ValueLabel", "en", "autoClaim", "", "Auto Plot: กันเงินไว้", " still locked after paying ", "Auto Claim (Index)", "[AntiAFK] idle detected, simulating input", "autoPlaceEquipBest", "ลำดับการใช้เงินรวมทุกระบบ", "Auto Plot: ใช้โซนล่าสุดเสมอ", "CharSpeedMaxLevel", "core/logger", "AutoPlace", "MineSelectZone", "ClaimableTiers failed", "Auto Upgrade: Pickaxe ที่จะซื้อ", " (", "MineUpgradeCharacter", "games/my_anime_mine/get_plot_parts", "core/logger", ")", "auto", "autoResearchCheapestFirst", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "core/debug_dump_gui", "table", " items", "ถูกสุดก่อน", "games/my_anime_mine/game_api", " waited ", "buying the ", "[AutoUpgrade] ", " = ", " -> ", "autoUpgradeEquipPickaxe", "LocalScript", "games/my_anime_mine/auto_place", "[AutoPlot] unlocked ", " - upgrade the miners first", "UserInputService", "th", "ค้นหาฟังก์ชัน...", "Anti-AFK", ",", ") needs ", "table", "UIListLayout", "ProximityPrompt", "PickaxesStand", ") - leaving it alone for ", "s", "%.3g", "debug.getconstants", "fill", "games/my_anime_mine/auto_upgrade", " ", "games/registry", "", "table", "Players", "", "\n", "games/my_anime_mine/game_api", "[AutoChest] collected ", "[AutoSell] disabled", " = ", "Auto Mine: ความเร็ว", "disabled", "Frame", "AutoPlot", "Players", "core/spend_policy", "", "Players", "affordable - waiting for a turn", "MekPro/decompiled", "Interaction Logger", "Auto Chest", " (prompt says '", "core/debug_tab", "Limited", " reward(s) looked claimable but none went through", "autoSellMaxWait", "OwnerUserId", "getsenv", "cheapest", "Mutation: เพดานราคาต่อครั้ง", " #", "MineTakeChest", ": ", "WalkSpeed (ปกติ 16)", "works from anywhere - no teleport needed", "Mining", "TextLabel", ")", "core/logger", "dearest", "/", "AutoPlace", "core/scheduler", "%.3g", "core/parse_money", "AutoChest", "autoUpgradeInterval", "core/logger", "Auto Upgrade: รอบเช็ค", "AutoSell", "AutoResearch", "MineWorld", "games/my_anime_mine/auto_plot", "may need us at the stand - trying that", "core/get_place_id", ")", "getscripts", "table", "autoSellProtectUnits", "[AntiAFK] disabled", "GetCharacterSpeedUpgradeCost", "Crew", "table", "Character", "TextLabel", "[AutoResearch] cheapest open node: ", "ไล่ซื้อทุกกี่วินาที", "search_placeholder", "UIScale", "[GameAPI] couldn't load the game's modules: ", "[AutoResearch] ", ", you have ", " (", "AutoPlace", "", "BasePart", "[AutoResearch] nothing open and affordable right now", "HumanoidRootPart", " didn't go through (out of stock or too expensive)", "All", "Modules", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "[AutoChest] disabled", "AutoPlot", "spendOrder", "TextButton", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "Frame", "Workspace", "table", "TextLabel", ", have ", "; ", "(", "enabled", "sale didn't go through", "games/my_anime_mine/game_api", "Players", ", base ", "Loot", "[AutoSell] enabled", "Workspace", " (still ", " but no unclaimed reward in its Track", "PlayerGui", "BodyVelocity", "AutoClaim", "core/logger", " waited ", "dearest", "autoPlotLatest", "ZoneRow", "Auto Research: กันเงินไว้", "Humanoid", "core/logger", "%.0f", "AutoUpgrade", "index rewards are disabled in this game build", ")", "autoUpgradePriority", "M", "string", "Speed", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", " costs ", "Shop Stand", "/config.json", " (Text=", " team power, you have ", "autoUpgradeMutationTarget", "เดินทะลุกำแพงและพื้น", "Frame", "[AutoUpgrade] ", "/", " from ", "Frame", "RemoteFunction", ") from ", "core/logger", " (", "[AutoPlace] ", "none", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "Players", ")", "core/debug_decompile", "en-us", "Pad", ": State(", "Auto Place: ปลดล็อกช่อง miner", "[AutoPlace] fireproximityprompt error: ", "games/my_anime_mine/auto_place", "", "เช็คโซนทุกกี่วินาที", "[AutoUpgrade] ", ")", "Auto Place: เพดานราคาปลดช่อง", "core/logger", "UIListLayout", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "%$%s*([%d,%.]+)%s*(%a*)", "games/my_anime_mine/auto_mine", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "autoUpgradePickaxeIndex", "autoResearchZones", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", ")", "[Interaction] logger disabled", ")", "[Uu]nlock", "/", "auto", "/", "works from anywhere - dropping the teleport", "Auto Upgrade: กันเงินไว้", "s", " (", " ", "%.0f", "neither appendfile nor writefile is available", "TextButton", "[AutoPlace] placed ", "upgrade", "core/logger", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "games/my_anime_mine/auto_chest", "Players", "GetCharacterMineSpeedUpgradeCost", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " error: ", "[AntiAFK] enabled - watching Player.Idled", " - ", "ReplicatedStorage", "table", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "[Decompile] === start ===", "Speed", "%.3g", "ItemFrame", "table", "autoSell", "Frame", "TextLabel", "", "", ")", "Auto Chest: รอบเช็ค", "getnilinstances", "getloadedmodules", "string", "GuiService", "s", "no isfile/readfile available to verify the write", "%.3g", "core/click", " ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", ")", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "leaderstats", "Auto Upgrade: แจก Pickaxe ให้ miner", "[AutoClaim] ", ") ", "core/ui", "th", "debug.getupvalues", " stack(s)", "MineSpeed", "ScreenGui", "core/run_job", "Decompile Client Scripts", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "", "มุดแมพ (Noclip)", "CharMineSpeedMaxLevel", " ", "X", "Empty miner slot", "games/my_anime_mine/auto_claim", " (power ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "no mine data", "ReplicatedStorage", "TextBox", "[AutoPlot] now mining ", "[GameAPI] SendEvent failed: ", "GetHolderLevel", "nothing left to buy (", "needs us at the stand - keeping the teleport", " (cost ", "core/logger", " still empty after placing ", "core/logger", "core/run_job", "interactionLogger", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "[Remotes] === total: ", "[AutoResearch] ", "ModuleScript", "autoPlot", "AutoResearch", "[Decompile] ", " script(s) to ", "autoMine", "games/my_anime_mine/game_api", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "Nodes", "'", "string", "games/my_anime_mine/get_plot_parts", "Main", "table", "table", "ไทย", "[%X] ", "MekPro", "[AutoPlace] upgrading ", "ตั้งค่า", "", "research", "number", "core/debug_dump_remotes", " -> ", "HttpService", "enabled", "core/logger", "/s", "core/run_job", "debug.getproto", "PlotIndex", "none", "AutoPlace", ": badge says ", "Auto Upgrade: สุ่ม Mutation", "Auto Place Miner", "🐛", "games/my_anime_mine/main", " (", "[AutoSell] MineSellItems ", "[AutoPlace] picked up ", "maxed", "TextButton", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "GuiObject", "[AutoPlace] unlocked ", "TextButton", "=locked(", "table", "[AutoPlace] ", "auto", " has been busy for ", " items)", "", ": ", "core/logger", " ", "MineSpeed", "autoUpgradeMutationBudget", " pickaxe(s) for ", "ProximityPrompt", "ZoneBoardGui", "fill", "/", "Players", "Root", "[Decompile] === end ===", "[MekPro] build ", "^%$", "table", "MineWorld", "games/my_anime_mine/auto_chest", "games/my_anime_mine/auto_mine", "Players", "[AutoPlace] drifted out of range of ", "/", "Auto Research: รอบเช็ค", "Hauler", ": ", "autoChest", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "AutoUpgrade", "AutoClaim", "BasePart", "string", "Content", "core/spend_policy", " mutation x", "[Scheduler] ", "games/my_anime_mine/auto_sell", " doesn't exist", "", " failed)", "[BestMiner] slot ", "string", "search_placeholder", "')", "autoPlaceUnlockBudget", "Dump", "TextLabel", "=empty", "Chest", " for ", "[AutoPlot] ", "", "Core", "My Anime Mine · Mining Hub", "[AutoResearch] ", "Auto Sell: รอคิวได้นานสุด", "  [attr] ", "no", "Plots", "x", "MineBuySkill", "AutoPlot", ")", "s - jumping the queue", "Auto Research (Plot)", "games/my_anime_mine/auto_plot", "core/debug_interaction_logger", "[AutoUpgrade] every stat on every miner is maxed", "[AutoPlace] nothing in the hotbar to place", "VirtualUser", "", "Zones", "autoUpgradeLimitPickaxes", "unreadable", "TextButton", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "ปุ่มเปิดเมนู", "Core buying is off", "Text", "games/my_anime_mine/auto_sell", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "Search functions...", " thing available first", "Workspace", "GuiButton", "Content", " ", "", "PlayerGui", "[AutoPlace] unlocking ", "[AutoResearch] idle: ", "[AutoPlace] ", "[AutoChest] enabled (collects once >= ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "อังกฤษ", "Damage", "TextLabel", "en", "ClickDetector", "แพงสุดก่อน", "⚙", " (", "Frame", "s", " is no better after the swap (", "x", "PlayerGui", "autoPlaceEquipBestInterval", "table", " (money ", "needs ", "'", "waiting", "[Decompile] decompile unavailable in this executor", "ขนาดเมนู", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "BasePart", "[AutoMine] ", "", " costs ", "", " pickaxe ", "autoResearchMaxPerPass", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "[Decompile] === executor capabilities ===", "badge says ", "core/get_place_id", "ValueLabel", "UIListLayout", " ชิ้น", "ScrollingFrame", "[^%w]+", "autoPlaceInterval", "[Decompile] PlayerScripts not found", "Open menu", "languageIndex", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "games/my_anime_mine/game_api", "Auto Research: ซื้อสูงสุดต่อรอบ", " for ", "decompile", "core/debug_dump_gui", " power ", " possible holder(s) - not buying more", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "core/teleport", "prompt disabled", "core/logger", " while selecting", "Auto Place: รอบดึงตัวเก่ง", "autoUpgradePickaxe", "action '", "server didn't equip it", " ===", "RunService", "dearest", "Content", "x", "_", " (", " claimable but none could be resolved", "", "s", "Auto Sell: รอบขาย", " owned)", "autoResearch", "autoResearchCore", "ยิง Equip Best ทุกกี่วินาที", " (", "TextButton", "Run", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Search", "Damage", ")", "VirtualInputManager", " -> ", "write reported success but the line isn't in ", "🏠", "", "games/my_anime_mine/game_api", "unreadable", "games/my_anime_mine/auto_upgrade", "core/logger", " -> x", "table", "Badge", "TextLabel", "  ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "[Logger] failed to write ", "Auto Sell", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "SellStand", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "core/teleport", "s", "TextLabel", "[AutoUpgrade] pickaxe #", " (", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "disabled", "[AutoPlot] ", "AutoResearch", "M", " research is ", "slot past the usable cap", "?", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "core/debug_dump_remotes", "UICorner", "Frame", "autoUpgradeRollMutation", "table", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "disabled", "Plots", "core/teleport", "languageIndex", "", "[Scheduler] ", "[AutoClaim] claimed ", "walkSpeed", "Players", "%.3g", "MineBuyPickaxe", "core/build_env", "games/my_anime_mine/get_current_plot", "[", "games/my_anime_mine/auto_research", "RunService", " node(s) looked buyable but the server refused", "GetCharacterUpgradeCost", "[AutoChest] chest is empty", "2026-09-21 13:05:54", "HumanoidRootPart", "", "ฟาร์ม", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " '", "#", "[AutoClaim] ", "autoUpgradeKeepMoney", "", "core/logger", "core/run_job", "", " ", "core/spend_policy", "Auto Place: รอบเช็ค", "[Interaction] ClickDetector clicked: ", "autoPlotKeepMoney", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "", "List", "/", "cost", "Auto Mine: คลิกต่อรอบ", " x", "Hauler", " ", ") didn't take: ", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "HotbarFrame", "CoreGui", " files", "MinePower", "BodyPosition", "", "games/my_anime_mine/game_api", ": ", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "This is not a supported place.", "[AutoPlot] couldn't switch to ", "disabled", "hookfunction", "noclip", "modules not loaded", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "", "Mining ก่อน", " items (now ", "core/teleport", "games/my_anime_mine/get_best_miner", "GetHolderMineSpeedLevel", "UICorner", " (", "ZoneBoard", "MekPro", "core/build_env", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "autoResearchInterval", "TextLabel", "[AutoChest] MineTakeChest had no effect (", "button has no size on screen", "MineEquipBest", " ", "core/ui", "unlock", " team power", "core/logger", "core/i18n", "off", "getscriptbytecode", "core/anti_afk", "[AutoPlace] disabled", "autoResearchCoreLast", "MekPro.log", "cheapest open node ", "th", "[AutoPlace] slot ", "core/run_job", "core/parse_money", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "TextLabel", "float", "MineEquipSlot", "RemoteEvent", "Frame", "locked slot", " -> ", ", ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "cost", " remotes", " ", "[AutoResearch] ", "getgc", "แพงสุดก่อน", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "[AutoChest] can't read the chest yet", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "Exclusive", ") gave nothing", "core/dispatch", " ", "  ", "games/my_anime_mine/get_current_plot", ": ", "MineGui", "AutoPlace", "write reported success but ", "Dump", "^Swap out ", "([%d,%.]+)%s*(%a*)", "core/run_job", "Auto Research: ซื้อสาย Zone", "%", "dearest", "AutoUpgrade", "Dump Visible GUI", "] job failed: ", "PlayerScripts", "autoResearchKeepMoney", "Frame", "[Bb]uy", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "core/get_leaderstat", "AutoPlace", "Auto Place: เปลี่ยนตัวอ่อนออก", "nothing to place or upgrade", "LayerCollector", "slot ", ")", "[GuiDump] PlayerGui not found", "autoClaimInterval", "empty", "core/logger", "BasePart", "autoUpgradeMaxPerPass", "x", "(nothing to do)", "TextLabel", "autoMineClicks", "[Decompile] wrote ", "Players", "AutoPlace", "TextLabel", "[AutoClaim] idle: ", "MineRollMutation", ")", " | ", "[Interaction] Button activated: ", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "getconnections", "[Remotes] ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "core/humanoid", "ลอย (Float)", "getrenv", "[AutoUpgrade] ", "costs ", ")", "Auto Plot: รอบเช็ค", " wouldn't take ", "core/i18n", " '", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "ถูกสุดก่อน", "core/debug_decompile", "core/i18n", "autoPlace", "off", "waiting", "core/get_place_id", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "AutoUpgrade", "games/my_anime_mine/auto_claim", "[Interaction] logger enabled - go click/interact with things now", ") ", "MekPro", "MekPro", " (ActionText=", "research", ") -> ", "AutoMine", "open_menu_hint", "[MekPro] settings file: ", "autoUpgrade", "lowest", "%.3g", "empty", "[Config] couldn't write ", "ClickDetector", "AutoSell", "games/registry", "MineSpeed", "[AutoPlace] ", "M", "off - every automation spends on its own", "0 = ไม่จำกัด", "string", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", " -> ", ")", "autoPlaceUnlock", "getcallbackvalue", "[Interaction] ProximityPrompt triggered: ", "UserInputService", " (", "", "Backpack", "table", "ClickDetector", " (Text=", "HumanoidRootPart", "core/get_me", "Workspace", " -> ", "%.3g", "AutoPlot", "autoPlotInterval", "ต่ำสุดก่อน", "games/my_anime_mine/game_api", "", "core/config", "dev", "Players", "core/logger", "Damage", "core/debug_tab", "open_menu_hint", "^x(%d+)$", "off", "x", "Header", "Auto Claim: รอบเช็ค", "needs ", "[SpendPolicy] ", "TextLabel", " refused - ", "nothing claimable", "[AutoChest] waiting: ", "UIStroke", "ไล่อัปเกรด miner ทุกกี่วินาที", "Text", "\n", "core/humanoid", "Auto Upgrade Plot (Zone)", "core/spend_policy", " (", "Speed ก่อน", "Auto Upgrade Unit", "Auto Mine", "Container", "M", "", "Auto Claim: เก็บสูงสุดต่อรอบ", "sold ", "%$", " not bought yet (", "every open node is maxed", " (power ", "core/config", "CoreUI", "function", "Mutation: หยุดเมื่อได้ x", "[AutoPlace] couldn't hold anything - not pressing E", "autoClaimMaxPerPass", "", "Locked miner slot", "ProximityPrompt", "unlock", "[AutoUpgrade] bought pickaxe #", " (chest holds ", "core/parse_money", "th", ": ", "function", "s", "0 = ไม่จำกัด", "core/anti_afk", "Frame", "Auto Plot: ซื้อโซนถัดไป", "core/debug_interaction_logger", "[Scheduler] ", "Humanoid", "อ่านป้ายบน chest ทุกกี่วินาที", ") ", "^([%d,%.]+)%s*(%a*)$", "menuScale", ",", "TextButton", "s behind a purchase that never happened - letting it spend", "Players", "core/logger", "[AutoSell] ", "core/scheduler", "off", "[SpendPolicy] ", "Auto Chest: เกณฑ์เก็บ", "TextLabel", "[GuiDump] === visible PlayerGui tree ===", "disabled", "menuScale", "TextLabel", "ตามเครื่อง", "autoChestInterval", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "[AutoPlot] ", "lowest", ") ", "^M(%d+)$", "TextButton", " -> ", "AutoResearch", ")", "ต่างคนต่างซื้อ", "[AutoPlace] Equip Best: hotbar's strongest ", "core/run_job", "games/my_anime_mine/game_api", "Damage ก่อน", "GuiService", "[Scheduler] turn: ", "M", "s", "enabled", "Auto Research: ซื้อสาย Core", "Workspace", "autoSellInterval", "UIPadding", "TextLabel", "core/dispatch", "jumpPower", "Zone buying is off", "antiAfk", "ChestCapacity", "slot not in the hotbar", "games/my_anime_mine/auto_research", "core/spend_policy", "AutoChest", "[GuiDump] ", "dearest", ".lua", "%.0f", "s - carrying on without it", "dearest", "ขายทุกกี่วินาที", " (x", "enabled", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[Config] couldn't read ", "แรงกระโดด", "Players", "table", "autoUpgradePickaxeIndex", "Frame", " screens", "cheapest", "games/my_anime_mine/main", "autoMineSpeed", ", built ", "both Zone and Core buying are switched off", "Holders", "en", "บิน (Fly)", "Dump All Remotes", ")", "fly", "'", " (x", "core/run_job", "Badge", "[AutoUpgrade] MineBuyPickaxe ", "auto", "autoPlaceUpgradeWeak", "core/spend_policy", "autoSellMaxWait"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(932)] = function()
return function(import)
local log = import(S(265))
local Players = game:GetService(S(110))
local VirtualUser = game:GetService(S(490))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(298))
        connection = player.Idled:Connect(function()
            log(S(57))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(167))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(647)] = function()
return { Env = S(877), IsDev = true, Commit = S(32), BuiltAt = S(655) }
end
modules[S(322)] = function()
local VirtualInputManager = game:GetService(S(596))
local GuiService = game:GetService(S(318))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(715)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(914)] = function()
return function(import)
local HttpService = game:GetService(S(391))
local getPlaceId = import(S(547))
local log = import(S(422))
local ROOT_FOLDER = S(709)
local placeFolder = ROOT_FOLDER .. S(241) .. tostring(getPlaceId())
local filePath = placeFolder .. S(234)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(436) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(1002) .. filePath .. S(421) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(843) .. filePath .. S(691) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(820)] = function()
return function(import)
local Players = game:GetService(S(440))
local log = import(S(10))
local CAPABILITIES = {
    S(561), S(724), S(164), S(748), S(316),
    S(805), S(857), S(135), S(810), S(103),
    S(333), S(396), S(696), S(315),
}
local OUT_FOLDER = S(126)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(46)) do
        if type(target) ~= S(76) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(545))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(369) .. name .. S(83) .. ((ok and type(value) == S(40)) and S(12) or S(478)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(534))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(773))
    if not playerScripts then
        log(S(554))
        return 0
    end
    pcall(makefolder, S(832))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(86)) or target:IsA(S(366)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(462) and #source > 0 then
                local name = target:GetFullName():gsub(S(552), S(579))
                if pcall(writefile, OUT_FOLDER .. S(26) .. name .. S(994), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(795) .. written .. S(370) .. OUT_FOLDER
        .. S(580) .. failed .. S(460))
    return written
end
local function run()
    log(S(303))
    reportCapabilities()
    local count = dumpAll()
    log(S(433))
    return count
end
return run
end
end
modules[S(562)] = function()
return function(import)
local Players = game:GetService(S(205))
local log = import(S(721))
local function dumpVisible(root, indent)
    indent = indent or S(472)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(411)) and child.Visible then
            local line = indent .. child.ClassName .. S(817) .. child.Name .. S(1020)
            if child:IsA(S(310)) or child:IsA(S(943)) then
                line = line .. S(235) .. tostring(child.Text) .. S(145)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(477) .. attrName .. S(116) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(609))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(212))
    if not playerGui then
        log(S(785))
        return 0
    end
    local screens = 0
    log(S(953))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(782)) and screen.Enabled and screen.Name ~= S(831) then
            screens = screens + 1
            log(S(992) .. screen.ClassName .. S(660) .. screen.Name .. S(532))
            dumpVisible(screen, S(757))
        end
    end
    log(S(20) .. screens .. S(4))
    return screens
end
return dumpGui
end
end
modules[S(630)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(350))
local log = import(S(604))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(738)) or descendant:IsA(S(244)) then
            count = count + 1
            log(S(806) .. descendant.ClassName .. S(140) .. descendant:GetFullName())
        end
    end
    log(S(364) .. count .. S(574))
    return count
end
return dumpRemotes
end
end
modules[S(487)] = function()
return function(import)
local Players = game:GetService(S(431))
local Workspace = game:GetService(S(34))
local UserInputService = game:GetService(S(859))
local log = import(S(879))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(858) .. describe(prompt)
            .. S(833) .. prompt.ActionText .. S(313))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(671) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(803) .. describe(button)
            .. S(865) .. tostring(button.Text) .. S(163))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(427)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(519)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(15)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(275))
        return
    end
    connections = {}
    log(S(829))
    local playerGui = Players.LocalPlayer:WaitForChild(S(527))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(98)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(864)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(505)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(44),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(881)] = function()
return function(import)
local interactionLogger = import(S(935))(import)
local dumpRemotes = import(S(389))(import)
local dumpGui = import(S(75))(import)
local decompileScripts = import(S(253))(import)
local humanoid = import(S(808))
local function build(window, persist)
    local tab = window:AddTab(S(29), S(403))
    persist.Toggle(
        tab:AddCard(
            S(127),
            S(804)
        ),
        S(362),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(1017),
        S(296)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(763), function()
        remoteBtn.Text = dumpRemotes() .. S(745)
    end)
    local guiCard = tab:AddCard(
        S(771),
        S(629)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(466), function()
        guiBtn.Text = dumpGui() .. S(1008)
    end)
    local decompileCard = tab:AddCard(
        S(338),
        S(807)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(591), function()
        decompileBtn.Text = decompileScripts() .. S(686)
    end)
    humanoid.Start()
    persist.Slider(tab, S(39), S(141),
        8, 200, S(643), 16, humanoid.SetWalkSpeed, S(459))
    persist.Slider(tab, S(1003), S(47),
        30, 300, S(984), 50, humanoid.SetJumpPower, S(260))
    persist.Toggle(
        tab:AddCard(S(1016), S(692)),
        S(1019), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(341), S(238)),
        S(697), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(809), S(683)),
        S(736), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(983)] = function()
return function(import)
    local getPlaceId = import(S(825))
    local registry = import(S(107))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(693))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(778)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(327))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(867)] = function()
local Players = game:GetService(S(124))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(937))
    local backpack = player:WaitForChild(S(862))
    return player, humanoid, backpack
end
return getMe
end
modules[S(162)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(898)] = function()
local Players = game:GetService(S(796))
local RunService = game:GetService(S(575))
local UserInputService = game:GetService(S(90))
local Workspace = game:GetService(S(979))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(221))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(656))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(213))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(183)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(537)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(688))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(821)] = function()
local Players = game:GetService(S(251))
local STRINGS = {
    en = {
        search_placeholder = S(502),
        open_menu_hint = S(555),
    },
    th = {
        search_placeholder = S(92),
        open_menu_hint = S(497),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(254)
    if locale:sub(1, 2):lower() == S(91) then
        return S(332)
    end
    return S(518)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(72) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(788)] = function()
local LOG_FILE = S(728)
local function log(message)
    local line = os.date(S(382)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(541))
            end
            appendfile(LOG_FILE, line .. S(897))
        elseif writefile then
            local existing = S(582)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(112))
        else
            error(S(287))
        end
        if not (isfile and readfile) then
            error(S(320))
        end
        if not isfile(LOG_FILE) then
            error(S(762) .. LOG_FILE .. S(458))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(598) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(611) .. LOG_FILE .. S(759) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(152)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(852) then
        return nil
    end
    local number, suffix = text:match(S(268))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(94), S(508))))
    if not value then
        return nil
    end
    if suffix ~= S(920) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(395)] = function()
return function(import)
local log = import(S(215))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(649) .. name .. S(772) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(150)] = function()
return function(import)
local RunService = game:GetService(S(651))
local log = import(S(146))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(456) .. task_.name .. S(297) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(641) .. task_.name .. S(418)
                    .. string.format(S(223), now - task_.busySince)
                    .. S(996))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(936) .. starved.name .. S(216)
            .. string.format(S(995), now - starved.waitingSince)
            .. S(484))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(974) .. (currentTask and currentTask.name or S(792)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(122)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(946))
local policy = {}
local probes = {}
local order = {}
local mode = S(723)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(889) .. (mode == S(949)
        and S(850)
        or (S(81) .. mode .. S(503))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(823) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(136) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(217) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(950) .. name .. S(80)
            .. string.format(S(286), now - blockedSince[name])
            .. S(944))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(703)] = function()
local Players = game:GetService(S(644))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(185))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(331)] = function()
return function(import)
local i18n = import(S(816))
local UserInputService = game:GetService(S(36))
local GuiService = game:GetService(S(973))
local THEME = {
    BrandName = S(383),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(631))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(706))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(981))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(11))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(311)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(894))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(172))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(628)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(198))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(657)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(551))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(549))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(517))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(632))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(956))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(890))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(608))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(548))
            if not valueLbl then
                valueLbl = Instance.new(S(618))
                valueLbl.Name = S(50)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(1023))
            if not badge then
                badge = Instance.new(S(713))
                badge.Name = S(607)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(288))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(239))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(119))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(590))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(861)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(768)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(243))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(523))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(193))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(689)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(933))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(266))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(413))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(896)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(336))
    screenGui.Name = S(915)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(685))
    local main = Instance.new(S(309))
    main.Name = S(378)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(176))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(739))
    tabBar.Name = S(13)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(97))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(1007))
    header.Name = S(886)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(495))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(793))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(982))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(667)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(964))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(344)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(409))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(24)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(351))
    searchBox.Name = S(593)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(463))
    searchBox.Text = S(875)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(195))
    content.Name = S(577)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(775))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(952))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(882))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(735))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(499)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(53) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(285) .. (item.Subtitle or S(600))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(175))
        footerLabel.Text = i18n.T(S(837))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(438)] = function()
return function(import)
local gameApi = import(S(79))(import)
local log = import(S(360))
local runGuarded = import(S(970))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(513) .. minItemsToCollect .. S(419))
    else
        log(S(190))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(494) then
            log(S(751))
            lastReported = S(602)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(842) then
            log(S(654))
            lastReported = S(787)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(824) then
            local capacity = gameApi.ChestCapacity()
            log(S(893) .. count .. S(676) .. want .. S(77)
                .. (capacity and (S(925) .. capacity .. S(276)) or S(907)))
            lastReported = S(533)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(991), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(114) .. count .. S(702) .. after .. S(801))
        else
            log(S(714) .. count
                .. S(741) .. tostring(after) .. S(226))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(828)] = function()
return function(import)
local gameApi = import(S(21))(import)
local log = import(S(358))
local runGuarded = import(S(732))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(329) .. (enabled and S(977) or S(636)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(799) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(642) .. reward.category .. S(423)
                .. tostring(reward.tierKey) .. S(138) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(662) .. #pending .. S(132))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(450), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(269)] = function()
return function(import)
local getCurrentPlot = import(S(41))
local getPlotParts = import(S(17))
local log = import(S(246))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(844), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(538) .. (enabled and S(5) or S(954)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(259)] = function()
return function(import)
local Players = game:GetService(S(121))
local getCurrentPlot = import(S(758))
local getPlotParts = import(S(377))
local getMinerSlots = import(S(704))(import)
local teleport = import(S(566))
local parseMoney = import(S(926))
local gameApi = import(S(690))(import)
local log = import(S(393))
local runGuarded = import(S(766))(import)
local spendPolicy = import(S(900))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(345)
local LOCKED_SLOT_TEXT = S(921)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(255))
    return pad and pad:FindFirstChildWhichIsA(S(922), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(33)) then
            local digits, suffix = descendant.Text:match(S(940))
            if digits then
                local value = tonumber((digits:gsub(S(23), S(340))))
                if value then
                    if suffix ~= S(640) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(674)
    return (text:gsub(S(764), S(700)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(798)) and descendant.Text:match(S(435)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(783) .. holder.Name .. S(911)
            .. gameApi.MinerSlots() .. S(442) .. gameApi.MaxMinerSlots() .. S(585)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(740)
    end
    if not prompt.Enabled then
        return true, S(567)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(812) .. price
    end
    local action = prompt.ActionText or S(111)
    if action:match(S(277)) or action:match(S(776)) or action:match(S(910)) then
        return true, S(572) .. action .. S(375)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(866))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(258) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(414) .. why .. S(252))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(468))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(promptCharName(prompt))
                    or shown
                table.insert(summary, holder.Name .. S(18) .. prompt.ObjectText
                    .. S(201) .. shown .. S(206) .. math.floor(base) .. S(325))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(802))
    if line ~= lastSurvey then
        log(S(45) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(429), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(290), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(149)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(719), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(512) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(726))
    else
        lastAttempt = 0
        log(S(273))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(74),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(988)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(627)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(573)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(489))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(441) .. holder.Name .. S(569))
            return false
        end
        log(S(731) .. candidate.slot .. S(67) .. candidate.name
            .. S(682) .. tostring(why))
    end
    if not miner then
        log(S(918))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(848) .. holder.Name .. S(359) .. miner.name)
        return false
    end
    log(S(289) .. miner.name .. S(913) .. miner.power
        .. S(31) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(37) .. entry.name .. S(242) .. entry.holder.Name
            .. S(129) .. tostring(entry.prompt.ActionText) .. S(464))
        return false
    end
    log(S(407) .. entry.name .. S(347) .. entry.power
        .. S(245) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(416) .. pad.holder.Name .. S(55)
                .. tostring(pad.priceText))
        else
            log(S(412) .. pad.holder.Name
                .. S(560) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local after = gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(248) .. holderName .. S(525)
            .. string.format(S(645), before) .. S(854) .. string.format(S(102), after)
            .. S(100) .. SWAP_COOLDOWN .. S(319))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(969)
            .. string.format(S(305), bestBefore) .. S(597)
            .. string.format(S(321), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(797), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(781))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(104) then
        runGuarded(S(181), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(923) then
        log(S(510) .. target.holder.Name
            .. S(470) .. tostring(target.priceText))
        runGuarded(S(761), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(384) .. target.holder.Name .. S(445) .. target.name
        .. S(247) .. target.power .. S(835) .. best.name .. S(589) .. best.power .. S(1018))
    runGuarded(S(63), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(486)] = function()
return function(import)
local gameApi = import(S(372))(import)
local parseMoney = import(S(733))
local log = import(S(62))
local runGuarded = import(S(337))(import)
local spendPolicy = import(S(454))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(471) .. (enabled and S(392) or S(118)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(42)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(352) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(405) .. deepestKey .. S(967))
        return true
    end
    log(S(694) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(8)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(623) .. gameApi.ZoneDisplayName(zoneKey) .. S(232)
                .. math.floor(price) .. S(179) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(88) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(620) .. zoneKey .. S(483))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(387) then
            reportedLocked = S(834)
            log(S(960) .. zoneKey .. S(891)
                .. gameApi.ZoneDisplayName(currentKey) .. S(626)
                .. done .. S(148) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(120)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(191), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(989)] = function()
return function(import)
local gameApi = import(S(204))(import)
local log = import(S(665))
local runGuarded = import(S(666))(import)
local spendPolicy = import(S(669))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(178) .. (enabled and S(202) or S(695)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(747) .. node.zoneKey .. S(668) .. node.id
                    .. S(180) .. node.family .. S(330) .. node.level .. S(965) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(6) .. node.id
                    .. S(860) .. node.family .. S(830) .. node.level .. S(869) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(365) .. blocked
                .. S(652))
        elseif powerBlocked then
            log(S(475) .. powerBlocked.zoneKey .. S(717) .. powerBlocked.id
                .. S(901) .. powerBlocked.family .. S(95)
                .. math.floor(powerBlocked.powerReq) .. S(236)
                .. math.floor(powerBlocked.power)
                .. S(89))
        else
            log(S(184)
                .. S(530) .. math.floor(gameApi.GetMoney()) .. S(71))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(173) .. nearest.zoneKey
                    .. S(343) .. nearest.id .. S(522) .. nearest.family .. S(962)
                    .. nearest.level .. S(280) .. nearest.max
                    .. S(540) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(1013)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(473),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(985))
        end
        if not buyCore then
            table.insert(parts, S(498))
        end
        if #parts == 0 then
            return S(912)
        end
        return S(355) .. table.concat(parts, S(742)) .. S(855)
    end
    local why
    if nearest.cost > budget then
        why = S(888) .. string.format(S(151), nearest.cost)
            .. S(199) .. string.format(S(870), money)
    elseif nearest.powerOk == false then
        why = S(531) .. string.format(S(841), nearest.powerReq) .. S(720)
    else
        why = S(125)
    end
    return S(729) .. nearest.zoneKey .. S(681) .. tostring(nearest.id)
        .. S(284) .. tostring(nearest.family) .. S(939) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(511) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(993) and cost > best)
            or (mode ~= S(576) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(368)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(966), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(457)] = function()
return function(import)
local Workspace = game:GetService(S(868))
local gameApi = import(S(971))(import)
local teleport = import(S(616))
local log = import(S(222))
local runGuarded = import(S(361))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(115))
    else
        lastAttempt = 0
        log(S(208))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(614))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(43), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(406)
                .. (sold and S(281)
                         or S(356)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(947) .. (sold and (S(909) .. #keys .. S(334)) or S(203)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(845), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(105)] = function()
return function(import)
local Workspace = game:GetService(S(209))
local gameApi = import(S(601))(import)
local teleport = import(S(638))
local log = import(S(70))
local runGuarded = import(S(1022))(import)
local spendPolicy = import(S(990))(import)
local STATS = { S(516), S(304), S(847) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(840)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(49) .. (enabled and S(1000) or S(622)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(380) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(744) or S(408)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(262) .. holderKey .. S(746) .. choice.stat .. S(323)
                        .. choice.info.level .. S(84) .. after.level
                        .. S(430) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(677) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(99))
    local shop = stand and stand:FindFirstChild(S(233))
    if not shop then
        return nil
    end
    if shop:IsA(S(789)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(451), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(1024)
                .. (bought and S(142)
                           or S(161)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(267))
        end
    end
    if bought then
        log(S(924) .. pickaxe.index .. S(507) .. pickaxe.name
            .. S(1021) .. pickaxe.multiplier .. S(813))
        return true
    end
    log(S(619) .. pickaxe.index .. S(106) .. pickaxe.name
        .. S(186))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(811) .. holderKey .. S(542)
                    .. tostring(worn or S(249)) .. S(390) .. wanted.name)
            else
                log(S(82) .. holderKey .. S(815)
                    .. wanted.name .. S(210) .. tostring(nowWorn or S(398)) .. S(274))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(240) .. holderKey .. S(455) .. variant
                .. S(605) .. after .. S(357) .. cost .. S(784))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(827))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(25) .. gameApi.OwnedPickaxeCount()
                    .. S(426) .. gameApi.MaxMinerSlots()
                    .. S(564))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(302))
        elseif maxed > 0 then
            log(S(488))
        else
            log(S(447))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(997) and price > best)
            or (mode ~= S(769) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(14))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(224), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(874)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(300))
local Workspace = game:GetService(S(504))
local log = import(S(155))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(188), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(177) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(109))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(301) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(680) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(96) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(170) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(444) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(963))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(353) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(139))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(187), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(529) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(863) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(171) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(2), keys)
end
function api.EquipSlot(slot)
    return send(S(737), slot == S(108) and S(420) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(646), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(16), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(800), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(716))
end
function api.BuySkill(zoneKey, family)
    return send(S(481), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(379)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(294)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(687))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(916) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(165) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(64), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(159))
    plots = plots and plots:FindFirstChild(S(479))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(1004)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(22)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(708))
    local gui = board and board:FindFirstChild(S(428))
    local root = gui and gui:FindFirstChild(S(432))
    local list = root and root:FindFirstChild(S(675))
    local row = list and list:FindFirstChild(S(219) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(144)) and descendant.Text ~= S(123) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(929) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(68), characterKey, stat == S(143) and S(424) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(354), max = S(38), cost = S(653) },
    Speed = { level = S(19), max = S(61), cost = S(168) },
    MineSpeed = { level = S(705), max = S(342), cost = S(295) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(229) or charName == S(386) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(1005) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(388) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(317) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(415) and type(holder.CharName) == S(452) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(131), S(753) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(606) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(698)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(349)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(225)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(634) then
        return {}, S(65)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(892)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(197) or type(state.Track) ~= S(307) then
                table.insert(notes, tabKey .. S(256) .. tostring(category) .. S(754))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(400) .. waiting
                        .. S(211))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(546) .. total .. S(581)
            .. (#notes > 0 and (S(299) .. table.concat(notes, S(200))) or S(539))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(376) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(30)] = function()
return function(import)
local Players = game:GetService(S(878))
local gameApi = import(S(558))(import)
local log = import(S(568))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(765))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(942), S(182))))
    if not value then
        return nil
    end
    if suffix ~= S(312) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(509))
    local mineGui = playerGui and playerGui:FindFirstChild(S(760))
    local hotbar = mineGui and mineGui:FindFirstChild(S(684))
    local content = hotbar and hotbar:FindFirstChild(S(506))
    return content and content:FindFirstChild(S(905))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(467)) then
            local count = descendant.Text:match(S(883))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(306))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(453))
    local nameLabel = content and content:FindFirstChild(S(27))
    local damageLabel = content and content:FindFirstChild(S(594))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(491) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(278) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(461) .. slot .. S(928) .. info.name
                        .. S(563) .. info.power .. S(679) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(648)] = function()
local Players = game:GetService(S(945))
local Workspace = game:GetService(S(196))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(437))
    local plots = mineWorld and mineWorld:FindFirstChild(S(637))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(134)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(69)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(1014)),
        Nodes = plot:FindFirstChild(S(374)),
        Chest = plot:FindFirstChild(S(469)),
        Zones = plot:FindFirstChild(S(492)),
        Crew = plot:FindFirstChild(S(169)),
        Loot = plot:FindFirstChild(S(207)),
        PlotIndex = plot:GetAttribute(S(397)),
        ChestCapacity = plot:GetAttribute(S(987)),
    }
end
return getPlotParts
end
modules[S(1010)] = function()
return function(import)
    local createWindow = import(S(718))(import)
    local i18n = import(S(722))
    local antiAfk = import(S(725))(import)
    local buildEnv = import(S(710))
    local scheduler = import(S(948))(import)
    local spendPolicy = import(S(1027))(import)
    local config = import(S(876))(import)
    local log = import(S(291))
    log(S(434) .. buildEnv.Commit .. S(707) .. buildEnv.Env
        .. S(1012) .. buildEnv.BuiltAt .. S(595))
    local window = createWindow({
        Subtitle = S(474),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(439))(import)
    local autoPlace = import(S(87))(import)
    local autoChest = import(S(293))(import)
    local autoSell = import(S(500))(import)
    local autoUpgrade = import(S(603))(import)
    local autoResearch = import(S(650))(import)
    local autoClaim = import(S(346))(import)
    local autoPlot = import(S(160))(import)
    local gameApi = import(S(113))(import)
    scheduler.RegisterStandalone(S(836), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(153), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(779), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(770), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(624), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(214), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(482), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(1028), 20)
    scheduler.RegisterExclusive(S(157), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(158), autoResearch.NextPrice)
    spendPolicy.Register(S(449), autoUpgrade.NextPrice)
    spendPolicy.Register(S(871), autoPlot.NextPrice)
    spendPolicy.Register(S(399), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(658), S(599))
    farmTab:AddSectionHeader(S(59))
    persistentSegmented(
        farmTab,
        { S(968), S(819), S(520) },
        { S(884), S(1009), S(147) },
        S(192), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(904),
        S(231)
    ), S(371), autoMine.SetEnabled)
    persistentSlider(farmTab, S(117), S(610),
        1, 30, S(1011), 5, autoMine.SetCyclesPerSecond, S(394))
    persistentSlider(farmTab, S(678), S(613),
        1, 10, S(794), 1, autoMine.SetClicksPerCycle, S(791))
    persistentToggle(farmTab:AddCard(
        S(402),
        S(699)
    ), S(822), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(670), S(818),
        1, 60, S(553), 3, autoPlace.SetCheckInterval, S(617))
    persistentToggle(
        farmTab:AddCard(S(780),
            S(750)),
        S(1026), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(621),
            S(292)),
        S(58), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(570), S(588),
        5, 300, S(528), 60, autoPlace.SetEquipBestInterval, S(35))
    persistentToggle(
        farmTab:AddCard(S(257),
            S(959)),
        S(856), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(264), S(851),
        0, 500, S(465), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(975))
    persistentToggle(farmTab:AddCard(
        S(128),
        S(826)
    ), S(446), autoChest.SetEnabled)
    persistentSlider(farmTab, S(951), S(743),
        50, 3000, S(48), 100, autoChest.SetMinItems, S(550))
    persistentSlider(farmTab, S(314), S(938),
        1, 60, S(958), 3, autoChest.SetCheckInterval, S(101))
    persistentToggle(farmTab:AddCard(
        S(612),
        S(410)
    ), S(308), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(1),
            S(373)),
        S(166), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(584), S(998),
        5, 300, S(980), 20, autoSell.SetCheckInterval, S(930))
    farmTab:AddSlider(S(476),
        S(326),
        5, 120, sellMaxWait, function(value)
            config.Set(S(133), value)
        end, S(524))
    persistentToggle(farmTab:AddCard(
        S(903),
        S(501)
    ), S(839), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(156), S(895),
        5, 300, S(154), 20, autoUpgrade.SetInterval, S(976))
    persistentSlider(farmTab, S(324), S(348),
        1, 20, S(790), 5, autoUpgrade.SetMaxPerPass, S(885))
    persistentSegmented(
        farmTab,
        { S(873), S(972), S(902), S(701) },
        { S(961), S(880), S(230), S(335) },
        S(227), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(282), S(557),
        0, 500, S(663), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(228))
    persistentToggle(
        farmTab:AddCard(S(565),
            S(448)),
        S(571), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(999) .. pickaxe.multiplier .. S(263)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(752)
            end
            return S(661) .. index .. S(756) .. (pickaxeLabels[index] or S(514))
        end
        local savedPick = config.Get(S(1006), 0)
        pickaxeCard = farmTab:AddSlider(
            S(66),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(271), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(664)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(711),
            S(363)),
        S(493), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(328),
            S(189)),
        S(85), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(401),
            S(592)),
        S(633), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(917), S(734),
        1, 20, S(237), 2, autoUpgrade.SetMutationTarget, S(480))
    persistentSlider(farmTab, S(137), S(931),
        0, 500, S(425), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(906))
    persistentToggle(farmTab:AddCard(
        S(485),
        S(496)
    ), S(586), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(443), S(174),
        10, 600, S(712), 30, autoResearch.SetCheckInterval, S(583))
    persistentSlider(farmTab, S(559), S(1001),
        1, 20, S(543), 5, autoResearch.SetMaxPerPass, S(578))
    persistentSlider(farmTab, S(220), S(853),
        0, 500, S(774), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(849))
    persistentToggle(
        farmTab:AddCard(S(767),
            S(673)),
        S(272), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(978),
            S(270)),
        S(587), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(194),
            S(536)),
        S(727), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(78), S(749) },
        { true, false },
        S(73), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(56),
        S(635)
    ), S(52), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(887), S(544),
        5, 300, S(786), 30, autoClaim.SetCheckInterval, S(283))
    persistentSlider(farmTab, S(908), S(3),
        1, 30, S(919), 10, autoClaim.SetMaxPerPass, S(526))
    persistentToggle(farmTab:AddCard(
        S(899),
        S(250)
    ), S(367), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(814), S(261),
        5, 300, S(872), 30, autoPlot.SetCheckInterval, S(7))
    persistentToggle(
        farmTab:AddCard(S(934),
            S(615)),
        S(28), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(60),
            S(339)),
        S(218), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(54), S(777),
        0, 500, S(672), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(625))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(130))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(385), S(521))
    persistentToggle(
        settingsTab:AddCard(S(93), S(659)),
        S(986),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(955), 100)
    settingsTab:AddSlider(S(535), S(9), 50, 150, savedScale, function(value)
        config.Set(S(941), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(556), 1)
    settingsTab:AddSegmented({ S(957), S(515), S(381) }, savedLanguage, function(index, value)
        local code = ({ S(417), S(51), S(927) })[index]
        config.Set(S(639), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(1025), S(1015), S(730) })[savedLanguage] or S(279))
    log(S(838) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(846)] = function()
return {
    [79389059854988] = S(404),
}
end
import(S(755))(import)
