-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"dearest", "action '", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", " (Text=", "decompile", "Auto Chest: รอบเช็ค", " ", "games/registry", "th", "AutoMine", "\n", " (", "Plots", "Damage ก่อน", "autoSellProtectUnits", "", "0 = ไม่จำกัด", "core/i18n", "Speed", "^M(%d+)$", " not bought yet (", "MineBuyPickaxe", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", " (", "core/logger", "RemoteEvent", " (x", ", built ", "autoClaimMaxPerPass", " node(s) looked buyable but the server refused", "CoreGui", "table", "", ") gave nothing", "เช็คโซนทุกกี่วินาที", "", "[AutoUpgrade] ", "TextLabel", "table", "TextLabel", "games/my_anime_mine/get_plot_parts", "core/debug_interaction_logger", "games/my_anime_mine/game_api", "cheapest", " ", ", have ", "ReplicatedStorage", "core/run_job", "M", "MineBuySkill", "[AutoPlot] ", ") on ", " ", " mutation x", "MineWorld", "[MekPro] settings file: ", "table", " costs ", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "getcallbackvalue", ")", ": ", "TextLabel", "ZoneBoard", "=empty", "Auto Plot: ซื้อโซนถัดไป", "PickaxesStand", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "AutoPlace", "games/my_anime_mine/auto_plot", "core/debug_interaction_logger", "core/i18n", "Open menu", "games/my_anime_mine/game_api", "nothing to place or upgrade", "core/dispatch", "AutoPlot", "^x(%d+)$", "search_placeholder", " '", "[AutoPlace] nothing in the hotbar to place", "BasePart", "Hauler", "/", "Frame", "HttpService", "buying the ", "Text", "แรงกระโดด", ",", "ค้นหาฟังก์ชัน...", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "games/my_anime_mine/auto_mine", "GetCharacterUpgradeCost", " - upgrade the miners first", "sale didn't go through", "[Config] couldn't write ", "Players", "[AutoChest] collected ", "games/my_anime_mine/get_current_plot", "core/logger", "research", ")", "[^%.]+", "works from anywhere - dropping the teleport", " screen(s)) ===", " (", "SellStand", "[AutoUpgrade] every stat on every miner is maxed", "[AutoUpgrade] MineBuyPickaxe ", "autoUpgradeMutationTarget", "[AutoUpgrade] pickaxe #", "Exclusive", "s", "auto", "x", "[SpendPolicy] ", "MinePower", "table", "autoResearch", " still locked after paying ", "HumanoidRootPart", "enabled", "[GuiDump] PlayerGui not found", "core/logger", "costs ", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "GuiObject", "number", "may need us at the stand - trying that", "games/my_anime_mine/main", "ต่างคนต่างซื้อ", "TextBox", "dearest", " stack(s)", "autoUpgrade", "[GuiDump] === visible PlayerGui tree ===", "Dump Visible GUI", "", "MineEquipBest", "core/get_leaderstat", "%$", " ", "Frame", "ต่ำสุดก่อน", "Humanoid", "core/get_place_id", "string", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " power ", "upgrade", "AutoPlace", "🐛", "ยิง Equip Best ทุกกี่วินาที", "UIScale", "both Zone and Core buying are switched off", "TextButton", "autoUpgradeRollMutation", ")", "autoUpgradeEquipPickaxe", "autoPlotInterval", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "Auto Plot: กันเงินไว้", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Auto Place Miner", "[AutoResearch] idle: ", "ClaimableTiers failed", "s - jumping the queue", "enabled", "autoUpgradeInterval", "; ", "[AutoUpgrade] ", "TextLabel", "core/run_job", "", "M", "core/logger", "table", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "getconnections", "%$%s*([%d,%.]+)%s*(%a*)", "core/parse_money", "games/my_anime_mine/auto_upgrade", "GetHolderLevel", "games/my_anime_mine/game_api", "UIListLayout", " (power ", "Players", "CharSpeedMaxLevel", "🏠", " team power, you have ", "Badge", "GuiService", "Damage", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "Workspace", "core/ui", ") ", "GetHolderSpeedLevel", "[GameAPI] couldn't load the game's modules: ", "string", "'", "core/run_job", "dearest", ": ", "core/debug_tab", "GetCharacterMineSpeedUpgradeCost", "[Scheduler] ", "table", "X", "autoUpgradePickaxeIndex", "ZoneBoardGui", " pickaxe(s) for ", "GetHolderMineSpeedLevel", "works from anywhere - no teleport needed", "TextLabel", "autoUpgradeKeepMoney", ", base ", "Workspace", " screens", "Auto Upgrade: Pickaxe ที่จะซื้อ", "core/ui", "MineEquipPickaxe", "ไล่อัปเกรด miner ทุกกี่วินาที", "", "games/my_anime_mine/auto_plot", "Main", ": State(", "", "prompt disabled", "Core", "Auto Upgrade: กันเงินไว้", "s", "TextButton", "autoPlaceEquipBest", ")", "AutoPlot", "Zone buying is off", " (money ", "=locked(", " wouldn't take ", " (chest holds ", "(", "[AutoPlace] ", ")", "Hauler", "Shop Stand", "autoUpgradePickaxe", "Empty miner slot", "autoMine", " but no unclaimed reward in its Track", "2026-09-21 13:01:50", "dearest", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "core/logger", "games/my_anime_mine/auto_research", " (power ", "function", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "Zones", "core/run_job", "autoChest", "แพงสุดก่อน", "OwnerUserId", "Workspace", ") ", "unlock", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "games/my_anime_mine/auto_chest", "[AutoPlace] holders: ", "%$", "table", "25d66fc", "Dump", "  ", " ชิ้น", "[AutoMine] ", " '", "[Bb]uy", "slot ", "Holders", "table", ")", ": badge says ", "Damage", "ตามเครื่อง", "[AutoResearch] ", "core/get_place_id", "autoMineSpeed", "games/my_anime_mine/game_api", "core/teleport", "Workspace", " - ", "every open node is maxed", ") ", "Mutation: เพดานราคาต่อครั้ง", "AutoUpgrade", ")", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "TabBar", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "Auto Research: ซื้อสูงสุดต่อรอบ", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "Players", "autoSell", "", "autoUpgradePriority", " team power", "[AutoPlot] unlocked ", "WalkSpeed (ปกติ 16)", "Players", "[AutoChest] waiting: ", " claimable but none could be resolved", "games/registry", "Auto Place: รอบดึงตัวเก่ง", "s behind a purchase that never happened - letting it spend", "", ", you have ", "[AutoResearch] Core ", " still empty after placing ", "/", "core/config", "autoPlaceEquipBestInterval", "Workspace", "needs ", "BasePart", "?", "[Remotes] === total: ", "autoResearchCoreLast", "Dump", "AutoSell", "ClickDetector", "no", "waiting", "", " research is ", ")", "[AutoChest] disabled", "getscriptbytecode", "Auto Research (Plot)", "Locked miner slot", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "cost", "[Logger] failed to write ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "  [attr] ", "core/logger", "core/build_env", " ", "%.0f", "slot past the usable cap", "debug.getproto", "ลอย (Float)", "Frame", "Limited", "[AutoSell] ", "core/spend_policy", "Auto Mine: ความเร็ว", "[AntiAFK] idle detected, simulating input", "en", "[AutoPlace] fireproximityprompt error: ", "ขายทุกกี่วินาที", "", "AutoPlot", "dev", "autoUpgradeLimitPickaxes", "[AutoResearch] nothing open and affordable right now", "core/debug_tab", " (still ", "[Interaction] logger disabled", "Mining ก่อน", " (prompt says '", "มุดแมพ (Noclip)", " costs ", "[Decompile] wrote ", "[AutoPlace] couldn't pick up ", "core/logger", ")", "core/logger", "Plots", "write reported success but the line isn't in ", "games/my_anime_mine/auto_chest", "autoSellMaxWait", "autoUpgradeMutationBudget", ",", "[Uu]nlock", "CharMaxLevel", "Auto Plot: ใช้โซนล่าสุดเสมอ", "core/scheduler", "x", "Frame", "PlayerGui", " ", "[AutoPlace] ", " doesn't exist", "All", " thing available first", "autoPlotLatest", "debug.getconstants", "debug.getupvalues", "games/my_anime_mine/auto_claim", "LayerCollector", "affordable - waiting for a turn", "games/my_anime_mine/get_plot_parts", " (", "[Decompile] decompile unavailable in this executor", "autoPlot", "s", "[AutoPlace] Equip Best: hotbar's strongest ", "TextLabel", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "off", "MineEquipSlot", "Auto Upgrade: รอบเช็ค", "Auto Sell", "Auto Claim: เก็บสูงสุดต่อรอบ", " ", "core/logger", " (", "core/debug_dump_remotes", "ขนาดเมนู", "cheapest", " (", "s", "core/logger", "no mine data", "fill", "none", "MineSpeed", "lowest", ")", "core/debug_dump_gui", "BodyPosition", ")", "Workspace", " items (now ", "[AutoChest] MineTakeChest had no effect (", " pickaxe ", "Core buying is off", "Content", "AutoClaim", "getscripts", "Players", ")", "autoPlotKeepMoney", "Auto Mine", "search_placeholder", "games/my_anime_mine/auto_mine", "index rewards are disabled in this game build", "/s", "s", "AutoPlace", "HotbarFrame", "%.0f", "Modules", "", "getloadedmodules", " (", "[AutoPlace] disabled", " (x", "ProximityPrompt", "_", "autoResearchCheapestFirst", "empty", "languageIndex", "antiAfk", "Frame", "Frame", "[", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/debug_dump_remotes", "UserInputService", "MekPro/decompiled", "Auto Sell: รอคิวได้นานสุด", "table", "core/humanoid", "[AutoPlace] drifted out of range of ", "core/spend_policy", "Character", "nothing left to buy (", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "core/logger", "ValueLabel", " failed)", "'", "[AutoResearch] cheapest open node: ", "research", "core/spend_policy", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", ")", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", " ", "/", "(nothing to do)", "table", "M", "MekPro", "GuiButton", "GuiButton", "", "", "write reported success but ", "[AutoPlot] now mining ", "x", "เดินทะลุกำแพงและพื้น", "Search functions...", "games/my_anime_mine/main", " has been busy for ", ") from ", "th", "core/config", "autoResearchMaxPerPass", "", "[Scheduler] turn: ", "disabled", "Players", "string", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "TextButton", "needs us at the stand - keeping the teleport", "Auto Upgrade Unit", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "ProximityPrompt", "Players", "Auto Mine: คลิกต่อรอบ", "enabled", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "AutoPlot", "\n", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Players", "MineRollMutation", "", "", "%.0f", "MineSellItems", "BasePart", "[AutoChest] chest is empty", " #", "modules not loaded", "[GuiDump] === end (", ") ", "ตั้งค่า", "enabled", " (", "ProximityPrompt", "/", "locked slot", "บิน (Fly)", "[Decompile] === executor capabilities ===", "TextLabel", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Auto Upgrade: สุ่ม Mutation", "open_menu_hint", "[AutoPlace] couldn't hold anything - not pressing E", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", " refused - ", " = ", "fly", "[GuiDump] ", "MekPro.log", "Interaction Logger", "[AutoPlace] unlocked ", "", "games/my_anime_mine/game_api", "string", "games/my_anime_mine/auto_research", "[AutoSell] MineSellItems ", ")", "[Config] couldn't read ", "core/teleport", ")", " remotes", "Pad", "core/anti_afk", "AutoPlace", "[AutoPlace] picked up ", "autoChestInterval", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "/", "Auto Chest", "[AutoSell] disabled", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[AutoPlot] ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "[Scheduler] ", "ScrollingFrame", "s", "RunService", "Auto Research: ซื้อสาย Zone", "autoSellInterval", "[Interaction] logger enabled - go click/interact with things now", "autoResearchCore", "Speed ก่อน", "MineTakeChest", "interactionLogger", "/", "Auto Research: ซื้อสาย Core", " (", "UIPadding", "float", "GetCharacterSpeedUpgradeCost", "MekPro", "core/logger", "s", "TextLabel", " ===", "[SpendPolicy] ", "core/logger", "[AutoPlace] placed ", "autoResearchKeepMoney", "ไทย", "[AutoChest] can't read the chest yet", "ปุ่มเปิดเมนู", ".lua", "core/debug_decompile", "AutoSell", "Auto Upgrade Plot (Zone)", "Root", " | ", "autoUpgradePickaxeIndex", "ClickDetector", "ZoneRow", "[AutoUpgrade] ", "M", "[AutoClaim] ", "core/parse_money", "Players", "Loot", "HumanoidRootPart", "AutoResearch", " items)", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "TextButton", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "[AutoPlace] ", "neither appendfile nor writefile is available", "cheapest open node ", "core/logger", "ลำดับการใช้เงินรวมทุกระบบ", "string", "autoClaimInterval", "[BestMiner] slot ", "AutoResearch", "sold ", "Auto Chest: เกณฑ์เก็บ", "yes", "[%X] ", "VirtualInputManager", " items", "=", "", "[Decompile] ", "ความเร็ววิ่ง", "spendOrder", "AutoPlot", " (Text=", "Crew", "autoResearchZones", "", " -> ", "AutoUpgrade", "AutoPlace", "games/my_anime_mine/auto_place", "Auto Place: เพดานราคาปลดช่อง", "TextLabel", "LocalScript", "core/i18n", ")", " (ActionText=", "Mining", "auto", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "languageIndex", "[Decompile] === end ===", "Anti-AFK", "function", "table", " for ", "Auto Upgrade: แจก Pickaxe ให้ miner", "-", "MineSpeed", "Auto Place: เปลี่ยนตัวอ่อนออก", "BodyVelocity", "dearest", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "core/logger", "off", "autoPlaceInterval", "x", "autoClaim", "PlotIndex", "Content", ") needs ", "table", "0 = ไม่จำกัด", " (", "walkSpeed", "core/logger", "dearest", "Auto Plot: รอบเช็ค", "en-us", "noclip", "Frame", "autoPlaceUnlockBudget", "cost", "Humanoid", "autoChestMinItems", "waiting", "%.3g", "core/teleport", "core/get_place_id", "Frame", "s", "#", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "none", "AutoResearch", "UserInputService", "disabled", "fill", "server didn't equip it", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "แพงสุดก่อน", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "⚙", " possible holder(s) - not buying more", "ItemName", "[AutoPlace] upgrading ", "table", "TextLabel", "UIStroke", "อ่านป้ายบน chest ทุกกี่วินาที", "hookfunction", "core/spend_policy", "core/run_job", "[AutoUpgrade] ", ")", ": ", "MineUpgradeCharacter", "AutoChest", "ModuleScript", "PlayerScripts", "Backpack", "Chest", "no isfile/readfile available to verify the write", "AutoUpgrade", "games/my_anime_mine/get_plot_parts", "RunService", "core/debug_decompile", "getrenv", "HumanoidRootPart", "Auto Place: ปลดล็อกช่อง miner", "button has no size on screen", "[Decompile] === start ===", "slot not in the hotbar", " (cost ", "AutoUpgrade", "string", "Dump All Remotes", "] job failed: ", "autoUpgradeMaxPerPass", "en", ")", "อังกฤษ", "OwnerUserId", "Content", "My Anime Mine · Mining Hub", ") didn't take: ", "ItemFrame", "Text", "off - every automation spends on its own", "UserInputService", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "unreadable", " owned)", "VirtualUser", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "[AutoUpgrade] bought pickaxe #", "games/my_anime_mine/get_best_miner", "core/scheduler", "UICorner", ": ", "PlayerGui", "This is not a supported place.", "[AutoUpgrade] ", "Auto Research: รอบเช็ค", "UICorner", "ScreenGui", "Damage", "en", "games/my_anime_mine/auto_claim", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "needs ", " (", "TextButton", " didn't go through (out of stock or too expensive)", ": ", "autoPlace", " -> ", " -> ", "core/logger", "MineWorld", " script(s) to ", "AutoPlace", " ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", " -> ", "off", "%.3g", "games/my_anime_mine/game_api", "  ", "jumpPower", "Auto Sell: รอบขาย", "[AutoResearch] ", "games/my_anime_mine/get_current_plot", "[Decompile] PlayerScripts not found", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "[Interaction] ClickDetector clicked: ", "[AntiAFK] disabled", ", ", "core/get_me", " waited ", "AutoResearch", "TextLabel", "AutoClaim", "TextLabel", "TextButton", "/", "core/run_job", "autoPlaceUpgradeWeak", ") -> ", "menuScale", "[AutoPlace] unlocking ", "th", "^([%d,]+)$", "%.3g", "TextLabel", "lowest", "%.3g", "ไล่ซื้อทุกกี่วินาที", "MineGui", " ", ",", "CoreUI", "[^%w]+", "Auto Claim: รอบเช็ค", "Nodes", "games/my_anime_mine/auto_place", " error: ", "empty", "autoResearchInterval", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "getgc", "[AutoSell] enabled", "auto", "RemoteFunction", "MekPro", "/", "ProximityPrompt", " (", "ฟาร์ม", "UIListLayout", "/config.json", "", "ReplicatedStorage", "Header", "Frame", "games/my_anime_mine/auto_sell", "[AutoUpgrade] ", "BasePart", " for ", "UIListLayout", "auto", "core/spend_policy", "TextLabel", "ValueLabel", "unlock", "games/my_anime_mine/get_current_plot", " -> ", "[AutoPlot] couldn't switch to ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "Decompile Client Scripts", "AutoPlace", "disabled", "JumpPower (ปกติ 50)", "badge says ", "leaderstats", "maxed", "Players", "core/dispatch", "table", "core/logger", "core/parse_money", "autoSellMaxWait", "[Interaction] Button activated: ", "[MekPro] build ", "[AutoResearch] ", "AutoChest", "off", " files", "core/build_env", "core/spend_policy", "TextLabel", "TextButton", "getnilinstances", "')", "%", "Frame", "table", "TextLabel", "core/click", "Auto Claim (Index)", ": ", "games/my_anime_mine/auto_upgrade", "Players", "open_menu_hint", " reward(s) looked claimable but none went through", "ClickDetector", "games/my_anime_mine/game_api", "menuScale", "", "TextButton", "", "ChestCapacity", "List", "core/run_job", " -> ", "TextLabel", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "[AutoClaim] claimed ", "PlayerGui", "unreadable", " = ", "disabled", "th", "", "[AutoClaim] idle: ", "M", " from ", "Frame", "AutoUpgrade", "ดีบัก", "s - carrying on without it", "[AutoPlot] ", "getsenv", "'", "[AutoResearch] ", "([%d,%.]+)%s*(%a*)", "GuiService", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "[Interaction] ProximityPrompt triggered: ", "games/my_anime_mine/game_api", "", "", "core/logger", "disabled", " waited ", "games/my_anime_mine/auto_sell", "Speed", "ถูกสุดก่อน", "core/logger", "^%$", "table", "games/my_anime_mine/game_api", "nothing claimable", "autoPlotBuy", "[Interaction] Key pressed: %s (gameProcessed=%s)", " x", "[AutoClaim] ", "enabled", "x", "[Remotes] ", "Auto Research: กันเงินไว้", "MineSelectZone", "Search", "ย่อขยายเมนูทั้งชุด", "MekPro", "Badge", "[Scheduler] ", "Auto Place: รอบเช็ค", "Container", "[GameAPI] SendEvent failed: ", "Players", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "core/anti_afk", " ", "function", "MineSpeed", "CharMineSpeedMaxLevel", "TextButton", "", "core/run_job", "Players", "autoPlaceUnlock", "core/teleport", "games/my_anime_mine/game_api", "s", " while selecting", "[AntiAFK] enabled - watching Player.Idled", "autoMineClicks", "games/my_anime_mine/get_best_miner", " -> x", "Run", "%.3g", "[AutoPlace] slot ", "core/humanoid", "core/debug_dump_gui", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Mutation: หยุดเมื่อได้ x", "ถูกสุดก่อน", "table", "[AutoChest] enabled (collects once >= ", "core/logger"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(989)] = function()
return function(import)
local log = import(S(125))
local Players = game:GetService(S(893))
local VirtualUser = game:GetService(S(780))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(1003))
        connection = player.Idled:Connect(function()
            log(S(360))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(823))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(349)] = function()
return { Env = S(366), IsDev = true, Commit = S(273), BuiltAt = S(252) }
end
modules[S(915)] = function()
local VirtualInputManager = game:GetService(S(651))
local GuiService = game:GetService(S(193))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(757)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(512)] = function()
return function(import)
local HttpService = game:GetService(S(86))
local getPlaceId = import(S(288))
local log = import(S(25))
local ROOT_FOLDER = S(861)
local placeFolder = ROOT_FOLDER .. S(582) .. tostring(getPlaceId())
local filePath = placeFolder .. S(867)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(895) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(571) .. filePath .. S(917) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(97) .. filePath .. S(801) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(753)] = function()
return function(import)
local Players = game:GetService(S(517))
local log = import(S(611))
local CAPABILITIES = {
    S(5), S(340), S(443), S(857), S(458),
    S(180), S(60), S(949), S(754), S(400),
    S(401), S(353), S(737), S(909),
}
local OUT_FOLDER = S(474)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(104)) do
        if type(target) ~= S(680) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(551))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(655) .. name .. S(937) .. ((ok and type(value) == S(991)) and S(649) or S(334)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(407))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(746))
    if not playerScripts then
        log(S(820))
        return 0
    end
    pcall(makefolder, S(605))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(669)) or target:IsA(S(745)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(567) and #source > 0 then
                local name = target:GetFullName():gsub(S(849), S(463))
                if pcall(writefile, OUT_FOLDER .. S(832) .. name .. S(617), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(376) .. written .. S(807) .. OUT_FOLDER
        .. S(699) .. failed .. S(485))
    return written
end
local function run()
    log(S(758))
    reportCapabilities()
    local count = dumpAll()
    log(S(677))
    return count
end
return run
end
end
modules[S(1011)] = function()
return function(import)
local Players = game:GetService(S(630))
local log = import(S(348))
local function dumpVisible(root, indent)
    indent = indent or S(175)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(128)) and child.Visible then
            local line = indent .. child.ClassName .. S(80) .. child.Name .. S(486)
            if child:IsA(S(216)) or child:IsA(S(994)) then
                line = line .. S(4) .. tostring(child.Text) .. S(236)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(347) .. attrName .. S(559) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(815))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(393))
    if not playerGui then
        log(S(124))
        return 0
    end
    local screens = 0
    log(S(137))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(403)) and screen.Enabled and screen.Name ~= S(498) then
            screens = screens + 1
            log(S(561) .. screen.ClassName .. S(278) .. screen.Name .. S(202))
            dumpVisible(screen, S(275))
        end
    end
    log(S(542) .. screens .. S(106))
    return screens
end
return dumpGui
end
end
modules[S(472)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(869))
local log = import(S(177))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(26)) or descendant:IsA(S(860)) then
            count = count + 1
            log(S(976) .. descendant.ClassName .. S(62) .. descendant:GetFullName())
        end
    end
    log(S(329) .. count .. S(609))
    return count
end
return dumpRemotes
end
end
modules[S(42)] = function()
return function(import)
local Players = game:GetService(S(305))
local Workspace = game:GetService(S(292))
local UserInputService = game:GetService(S(722))
local log = import(S(805))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(955) .. describe(prompt)
            .. S(672) .. prompt.ActionText .. S(379))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(822) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(899) .. describe(button)
            .. S(659) .. tostring(button.Text) .. S(445))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(524)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(624)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(499)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(371))
        return
    end
    connections = {}
    log(S(594))
    local playerGui = Players.LocalPlayer:WaitForChild(S(935))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(863)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(333)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(500)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(971),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(206)] = function()
return function(import)
local interactionLogger = import(S(71))(import)
local dumpRemotes = import(S(421))(import)
local dumpGui = import(S(433))(import)
local decompileScripts = import(S(618))(import)
local humanoid = import(S(477))
local function build(window, persist)
    local tab = window:AddTab(S(946), S(153))
    persist.Toggle(
        tab:AddCard(
            S(563),
            S(92)
        ),
        S(598),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(763),
        S(59)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(331), function()
        remoteBtn.Text = dumpRemotes() .. S(574)
    end)
    local guiCard = tab:AddCard(
        S(138),
        S(933)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(274), function()
        guiBtn.Text = dumpGui() .. S(220)
    end)
    local decompileCard = tab:AddCard(
        S(886),
        S(954)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(1007), function()
        decompileBtn.Text = decompileScripts() .. S(904)
    end)
    humanoid.Start()
    persist.Slider(tab, S(656), S(311),
        8, 200, S(700), 16, humanoid.SetWalkSpeed, S(457))
    persist.Slider(tab, S(89), S(889),
        30, 300, S(816), 50, humanoid.SetJumpPower, S(307))
    persist.Toggle(
        tab:AddCard(S(550), S(581)),
        S(560), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(374), S(506)),
        S(705), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(354), S(179)),
        S(603), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(894)] = function()
return function(import)
    local getPlaceId = import(S(714))
    local registry = import(S(8))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(788))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(141)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(891))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(825)] = function()
local Players = game:GetService(S(188))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(146))
    local backpack = player:WaitForChild(S(747))
    return player, humanoid, backpack
end
return getMe
end
modules[S(147)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(1010)] = function()
local Players = game:GetService(S(98))
local RunService = game:GetService(S(752))
local UserInputService = game:GetService(S(473))
local Workspace = game:GetService(S(325))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(709))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(755))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(686))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(874)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(82)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(434))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(72)] = function()
local Players = game:GetService(S(987))
local STRINGS = {
    en = {
        search_placeholder = S(507),
        open_menu_hint = S(73),
    },
    th = {
        search_placeholder = S(91),
        open_menu_hint = S(616),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(704)
    if locale:sub(1, 2):lower() == S(9) then
        return S(511)
    end
    return S(766)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(877) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(641)] = function()
local LOG_FILE = S(562)
local function log(message)
    local line = os.date(S(650)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(958))
            end
            appendfile(LOG_FILE, line .. S(530))
        elseif writefile then
            local existing = S(33)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(11))
        else
            error(S(639))
        end
        if not (isfile and readfile) then
            error(S(749))
        end
        if not isfile(LOG_FILE) then
            error(S(503) .. LOG_FILE .. S(396))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(382) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(345) .. LOG_FILE .. S(205) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(897)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(643) then
        return nil
    end
    local number, suffix = text:match(S(181))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(847), S(662))))
    if not value then
        return nil
    end
    if suffix ~= S(654) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(174)] = function()
return function(import)
local log = import(S(483))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(470) .. name .. S(764) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(784)] = function()
return function(import)
local RunService = game:GetService(S(591))
local log = import(S(101))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(208) .. task_.name .. S(853) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(983) .. task_.name .. S(509)
                    .. string.format(S(351), now - task_.busySince)
                    .. S(947))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(588) .. starved.name .. S(826)
            .. string.format(S(536), now - starved.waitingSince)
            .. S(168))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(515) .. (currentTask and currentTask.name or S(495)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(479)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(378))
local policy = {}
local probes = {}
local order = {}
local mode = S(903)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(610) .. (mode == S(413)
        and S(775)
        or (S(87) .. mode .. S(398))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(690) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(44) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(134) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(117) .. name .. S(961)
            .. string.format(S(455), now - blockedSince[name])
            .. S(317))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(713)] = function()
local Players = game:GetService(S(312))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(122))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(197)] = function()
return function(import)
local i18n = import(S(670))
local UserInputService = game:GetService(S(776))
local GuiService = game:GetService(S(953))
local THEME = {
    BrandName = S(981),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(791))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(785))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(602))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(520))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(318)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(735))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(608))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(328)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(830))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(225)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(589))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(186))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(907))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(392))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(879))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(411))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(841))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(484))
            if not valueLbl then
                valueLbl = Instance.new(S(932))
                valueLbl.Name = S(880)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(192))
            if not badge then
                badge = Instance.new(S(40))
                badge.Name = S(982)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(926))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(468))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(85))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(799))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(502)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(911)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(144))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(944))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(831))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(336)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(715))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(866))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(636))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(88)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(792))
    screenGui.Name = S(848)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(31))
    local main = Instance.new(S(912))
    main.Name = S(227)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(155))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(871))
    tabBar.Name = S(300)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(876))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(706))
    header.Name = S(870)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(234))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(63))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(173))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(535)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(908))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(210)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(157))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(683)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(133))
    searchBox.Name = S(979)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(448))
    searchBox.Text = S(229)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(355))
    content.Name = S(770)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(469))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(668))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(555))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(828))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(774)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(957) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(53) .. (item.Subtitle or S(36))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(79))
        footerLabel.Text = i18n.T(S(920))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(269)] = function()
return function(import)
local gameApi = import(S(1000))(import)
local log = import(S(606))
local runGuarded = import(S(996))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(1016) .. minItemsToCollect .. S(634))
    else
        log(S(339))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(778) then
            log(S(615))
            lastReported = S(936)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(854) then
            log(S(539))
            lastReported = S(465)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(335) then
            local capacity = gameApi.ChestCapacity()
            log(S(313) .. count .. S(494) .. want .. S(652)
                .. (capacity and (S(242) .. capacity .. S(245)) or S(364)))
            lastReported = S(711)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(744), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(99) .. count .. S(437) .. after .. S(435))
        else
            log(S(438) .. count
                .. S(663) .. tostring(after) .. S(671))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(795)] = function()
return function(import)
local gameApi = import(S(923))(import)
local log = import(S(959))
local runGuarded = import(S(930))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(973) .. (enabled and S(974) or S(888)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(941) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(934) .. reward.category .. S(7)
                .. tostring(reward.tierKey) .. S(540) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(628) .. #pending .. S(921))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(442), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(93)] = function()
return function(import)
local getCurrentPlot = import(S(100))
local getPlotParts = import(S(41))
local log = import(S(965))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(922), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(277) .. (enabled and S(169) or S(516)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(666)] = function()
return function(import)
local Players = game:GetService(S(444))
local getCurrentPlot = import(S(882))
local getPlotParts = import(S(751))
local getMinerSlots = import(S(783))(import)
local teleport = import(S(572))
local parseMoney = import(S(182))
local gameApi = import(S(956))(import)
local log = import(S(380))
local runGuarded = import(S(833))(import)
local spendPolicy = import(S(738))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(249)
local LOCKED_SLOT_TEXT = S(342)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(575))
    return pad and pad:FindFirstChildWhichIsA(S(547), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(38)) then
            local digits = descendant.Text:match(S(839))
            if digits then
                return tonumber((digits:gsub(S(386), S(868))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(552)) and descendant.Text:match(S(966)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(280) .. holder.Name .. S(21)
            .. gameApi.MinerSlots() .. S(862) .. gameApi.MaxMinerSlots() .. S(779)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(549)
    end
    if not prompt.Enabled then
        return true, S(230)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(126) .. price
    end
    local action = prompt.ActionText or S(139)
    if action:match(S(387)) or action:match(S(279)) or action:match(S(142)) then
        return true, S(2) .. action .. S(950)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(632))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(362) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(240) .. why .. S(741))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(65))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(653) .. prompt.ObjectText
                    .. S(243) .. shown .. S(218) .. math.floor(base) .. S(283))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(622))
    if line ~= lastSurvey then
        log(S(270) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(428), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(151), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(577)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(881), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(395) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(460))
    else
        lastAttempt = 0
        log(S(580))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(810),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(759)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(352)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(725)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(81))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(478) .. holder.Name .. S(1002))
            return false
        end
        log(S(1009) .. candidate.slot .. S(12) .. candidate.name
            .. S(772) .. tostring(why))
    end
    if not miner then
        log(S(556))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(244) .. holder.Name .. S(321) .. miner.name)
        return false
    end
    log(S(612) .. miner.name .. S(187) .. miner.power
        .. S(52) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(377) .. entry.name .. S(943) .. entry.holder.Name
            .. S(373) .. tostring(entry.prompt.ActionText) .. S(910))
        return false
    end
    log(S(578) .. entry.name .. S(257) .. entry.power
        .. S(510) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(638) .. pad.holder.Name .. S(121)
                .. tostring(pad.priceText))
        else
            log(S(564) .. pad.holder.Name
                .. S(875) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(410)
            .. string.format(S(712), bestBefore) .. S(883)
            .. string.format(S(840), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(665), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(75))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(724) then
        runGuarded(S(808), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(267) then
        log(S(837) .. target.holder.Name
            .. S(681) .. tostring(target.priceText))
        runGuarded(S(453), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(732) .. target.holder.Name .. S(786) .. target.name
        .. S(864) .. target.power .. S(835) .. best.name .. S(420) .. best.power .. S(573))
    runGuarded(S(69), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(226)] = function()
return function(import)
local gameApi = import(S(290))(import)
local parseMoney = import(S(629))
local log = import(S(426))
local runGuarded = import(S(739))(import)
local spendPolicy = import(S(906))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(51) .. (enabled and S(123) or S(723)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(271)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(504) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(601) .. deepestKey .. S(570))
        return true
    end
    log(S(884) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(237)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(948) .. gameApi.ZoneDisplayName(zoneKey) .. S(375)
                .. math.floor(price) .. S(319) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(310) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(107) .. zoneKey .. S(432))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(102) then
            reportedLocked = S(488)
            log(S(586) .. zoneKey .. S(558)
                .. gameApi.ZoneDisplayName(currentKey) .. S(337)
                .. done .. S(599) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(77)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(529), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(256)] = function()
return function(import)
local gameApi = import(S(185))(import)
local log = import(S(419))
local runGuarded = import(S(48))(import)
local spendPolicy = import(S(358))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(951) .. (enabled and S(527) or S(960)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(287) .. node.zoneKey .. S(45) .. node.id
                    .. S(24) .. node.family .. S(198) .. node.level .. S(803) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(320) .. node.id
                    .. S(546) .. node.family .. S(266) .. node.level .. S(804) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(901) .. blocked
                .. S(30))
        elseif powerBlocked then
            log(S(818) .. powerBlocked.zoneKey .. S(990) .. powerBlocked.id
                .. S(424) .. powerBlocked.family .. S(696)
                .. math.floor(powerBlocked.powerReq) .. S(191)
                .. math.floor(powerBlocked.power)
                .. S(95))
        else
            log(S(368)
                .. S(239) .. math.floor(gameApi.GetMoney()) .. S(298))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(487) .. nearest.zoneKey
                    .. S(418) .. nearest.id .. S(406) .. nearest.family .. S(543)
                    .. nearest.level .. S(548) .. nearest.max
                    .. S(58) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(156)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(231),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(238))
        end
        if not buyCore then
            table.insert(parts, S(440))
        end
        if #parts == 0 then
            return S(294)
        end
        return S(481) .. table.concat(parts, S(824)) .. S(61)
    end
    local why
    if nearest.cost > budget then
        why = S(326) .. string.format(S(813), nearest.cost)
            .. S(46) .. string.format(S(1008), money)
    elseif nearest.powerOk == false then
        why = S(797) .. string.format(S(843), nearest.powerReq) .. S(309)
    else
        why = S(404)
    end
    return S(640) .. nearest.zoneKey .. S(350) .. tostring(nearest.id)
        .. S(798) .. tostring(nearest.family) .. S(295) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(166) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(687) and cost > best)
            or (mode ~= S(253) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(646)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(633), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(872)] = function()
return function(import)
local Workspace = game:GetService(S(196))
local gameApi = import(S(968))(import)
local teleport = import(S(291))
local log = import(S(896))
local runGuarded = import(S(203))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(584))
    else
        lastAttempt = 0
        log(S(858))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(108))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(462), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(569)
                .. (sold and S(105)
                         or S(521)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(357) .. (sold and (S(647) .. #keys .. S(135)) or S(96)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(619), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(918)] = function()
return function(import)
local Workspace = game:GetService(S(436))
local gameApi = import(S(814))(import)
local teleport = import(S(999))
local log = import(S(1017))
local runGuarded = import(S(261))(import)
local spendPolicy = import(S(489))(import)
local STATS = { S(793), S(19), S(684) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(842)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(37) .. (enabled and S(545) or S(938)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(476) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(344) or S(892)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(626) .. holderKey .. S(493) .. choice.stat .. S(394)
                        .. choice.info.level .. S(931) .. after.level
                        .. S(84) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(708) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(67))
    local shop = stand and stand:FindFirstChild(S(247))
    if not shop then
        return nil
    end
    if shop:IsA(S(327)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(538), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(110)
                .. (bought and S(215)
                           or S(130)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(302))
        end
    end
    if bought then
        log(S(782) .. pickaxe.index .. S(846) .. pickaxe.name
            .. S(27) .. pickaxe.multiplier .. S(491))
        return true
    end
    log(S(112) .. pickaxe.index .. S(809) .. pickaxe.name
        .. S(800))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(740) .. holderKey .. S(439)
                    .. tostring(worn or S(429)) .. S(811) .. wanted.name)
            else
                log(S(873) .. holderKey .. S(241)
                    .. wanted.name .. S(370) .. tostring(nowWorn or S(720)) .. S(103))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(789) .. holderKey .. S(54) .. variant
                .. S(1006) .. after .. S(760) .. cost .. S(159))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(750))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(172) .. gameApi.OwnedPickaxeCount()
                    .. S(213) .. gameApi.MaxMinerSlots()
                    .. S(730))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(254))
        elseif maxed > 0 then
            log(S(109))
        else
            log(S(299))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(204) and price > best)
            or (mode ~= S(1) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(297))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(945), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(566)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(47))
local Workspace = game:GetService(S(265))
local log = import(S(689))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(456), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(200) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(913))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(272) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(83) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(733) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(209) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(246) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(20))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(986) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(597))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(397), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(496) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(697) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(480) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(537), keys)
end
function api.EquipSlot(slot)
    return send(S(414), slot == S(927) and S(501) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(22), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(223), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(533), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(140))
end
function api.BuySkill(zoneKey, family)
    return send(S(50), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(119)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(525)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(118))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(679) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(32) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(978), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(806))
    plots = plots and plots:FindFirstChild(S(381))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(919)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(264)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(64))
    local gui = board and board:FindFirstChild(S(212))
    local root = gui and gui:FindFirstChild(S(621))
    local list = root and root:FindFirstChild(S(929))
    local row = list and list:FindFirstChild(S(625) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(734)) and descendant.Text ~= S(940) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(258) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(743), characterKey, stat == S(673) and S(992) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(184), max = S(388), cost = S(94) },
    Speed = { level = S(199), max = S(189), cost = S(604) },
    MineSpeed = { level = S(214), max = S(993), cost = S(207) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(518) or charName == S(16) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(178) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(129) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(762) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(282) and type(holder.CharName) == S(201) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(356), S(113) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(967) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(541)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(427)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(450)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(39) then
        return {}, S(167)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(969)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(57) or type(state.Track) ~= S(1015) then
                table.insert(notes, tabKey .. S(228) .. tostring(category) .. S(34))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(284) .. waiting
                        .. S(251))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(890) .. total .. S(314)
            .. (#notes > 0 and (S(293) .. table.concat(notes, S(171))) or S(565))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(148) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(1005)] = function()
return function(import)
local Players = game:GetService(S(532))
local gameApi = import(S(43))(import)
local log = import(S(701))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(952))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(90), S(995))))
    if not value then
        return nil
    end
    if suffix ~= S(514) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(787))
    local mineGui = playerGui and playerGui:FindFirstChild(S(845))
    local hotbar = mineGui and mineGui:FindFirstChild(S(454))
    local content = hotbar and hotbar:FindFirstChild(S(695))
    return content and content:FindFirstChild(S(985))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(914)) then
            local count = descendant.Text:match(S(78))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(773))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(441))
    local nameLabel = content and content:FindFirstChild(S(731))
    local damageLabel = content and content:FindFirstChild(S(194))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(925) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(322) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(645) .. slot .. S(742) .. info.name
                        .. S(150) .. info.power .. S(972) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(819)] = function()
local Players = game:GetService(S(997))
local Workspace = game:GetService(S(219))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(55))
    local plots = mineWorld and mineWorld:FindFirstChild(S(13))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(769)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(405)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(281)),
        Nodes = plot:FindFirstChild(S(851)),
        Chest = plot:FindFirstChild(S(748)),
        Zones = plot:FindFirstChild(S(260)),
        Crew = plot:FindFirstChild(S(660)),
        Loot = plot:FindFirstChild(S(631)),
        PlotIndex = plot:GetAttribute(S(694)),
        ChestCapacity = plot:GetAttribute(S(928)),
    }
end
return getPlotParts
end
modules[S(131)] = function()
return function(import)
    local createWindow = import(S(222))(import)
    local i18n = import(S(18))
    local antiAfk = import(S(576))(import)
    local buildEnv = import(S(905))
    local scheduler = import(S(390))(import)
    local spendPolicy = import(S(878))(import)
    local config = import(S(323))(import)
    local log = import(S(255))
    log(S(900) .. buildEnv.Commit .. S(459) .. buildEnv.Env
        .. S(28) .. buildEnv.BuiltAt .. S(338))
    local window = createWindow({
        Subtitle = S(771),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(449))(import)
    local autoPlace = import(S(852))(import)
    local autoChest = import(S(383))(import)
    local autoSell = import(S(962))(import)
    local autoUpgrade = import(S(183))(import)
    local autoResearch = import(S(568))(import)
    local autoClaim = import(S(402))(import)
    local autoPlot = import(S(70))(import)
    local gameApi = import(S(74))(import)
    scheduler.RegisterStandalone(S(10), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(902), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(152), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(761), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(721), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(829), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(365), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(898), 20)
    scheduler.RegisterExclusive(S(332), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(827), autoResearch.NextPrice)
    spendPolicy.Register(S(664), autoUpgrade.NextPrice)
    spendPolicy.Register(S(658), autoPlot.NextPrice)
    spendPolicy.Register(S(887), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(865), S(190))
    farmTab:AddSectionHeader(S(642))
    persistentSegmented(
        farmTab,
        { S(132), S(1014), S(263) },
        { S(812), S(423), S(702) },
        S(657), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(447),
        S(531)
    ), S(250), autoMine.SetEnabled)
    persistentSlider(farmTab, S(359), S(195),
        1, 30, S(289), 5, autoMine.SetCyclesPerSecond, S(451))
    persistentSlider(farmTab, S(526), S(856),
        1, 10, S(1004), 1, autoMine.SetClicksPerCycle, S(116))
    persistentToggle(farmTab:AddCard(
        S(165),
        S(1012)
    ), S(802), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(984), S(343),
        1, 60, S(691), 3, autoPlace.SetCheckInterval, S(1001))
    persistentToggle(
        farmTab:AddCard(S(685),
            S(490)),
        S(834), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(675),
            S(301)),
        S(235), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(316), S(154),
        5, 300, S(324), 60, autoPlace.SetEquipBestInterval, S(716))
    persistentToggle(
        farmTab:AddCard(S(756),
            S(127)),
        S(998), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(667), S(698),
        0, 500, S(707), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(627))
    persistentToggle(farmTab:AddCard(
        S(583),
        S(688)
    ), S(262), autoChest.SetEnabled)
    persistentSlider(farmTab, S(648), S(587),
        50, 3000, S(710), 100, autoChest.SetMinItems, S(276))
    persistentSlider(farmTab, S(6), S(736),
        1, 60, S(579), 3, autoChest.SetCheckInterval, S(409))
    persistentToggle(farmTab:AddCard(
        S(416),
        S(553)
    ), S(306), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(3),
            S(23)),
        S(15), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(817), S(363),
        5, 300, S(593), 20, autoSell.SetCheckInterval, S(233))
    farmTab:AddSlider(S(475),
        S(728),
        5, 120, sellMaxWait, function(value)
            config.Set(S(384), value)
        end, S(607))
    persistentToggle(farmTab:AddCard(
        S(522),
        S(149)
    ), S(136), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(415), S(224),
        5, 300, S(170), 20, autoUpgrade.SetInterval, S(452))
    persistentSlider(farmTab, S(821), S(164),
        1, 20, S(765), 5, autoUpgrade.SetMaxPerPass, S(692))
    persistentSegmented(
        farmTab,
        { S(145), S(14), S(596), S(372) },
        { S(431), S(285), S(963), S(430) },
        S(308), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(232), S(519),
        0, 500, S(217), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(176))
    persistentToggle(
        farmTab:AddCard(S(528),
            S(346)),
        S(248), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(461) .. pickaxe.multiplier .. S(767)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(259)
            end
            return S(717) .. index .. S(143) .. (pickaxeLabels[index] or S(885))
        end
        local savedPick = config.Get(S(623), 0)
        pickaxeCard = farmTab:AddSlider(
            S(221),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(211), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(534)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(162),
            S(268)),
        S(367), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(682),
            S(718)),
        S(160), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(554),
            S(781)),
        S(158), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(1013), S(726),
        1, 20, S(111), 2, autoUpgrade.SetMutationTarget, S(391))
    persistentSlider(farmTab, S(296), S(17),
        0, 500, S(385), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(942))
    persistentToggle(farmTab:AddCard(
        S(341),
        S(635)
    ), S(120), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(790), S(844),
        10, 600, S(855), 30, autoResearch.SetCheckInterval, S(114))
    persistentSlider(farmTab, S(303), S(585),
        1, 20, S(513), 5, autoResearch.SetMaxPerPass, S(975))
    persistentSlider(farmTab, S(977), S(557),
        0, 500, S(613), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(497))
    persistentToggle(
        farmTab:AddCard(S(592),
            S(471)),
        S(661), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(600),
            S(492)),
        S(595), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(637),
            S(777)),
        S(330), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(964), S(727) },
        { true, false },
        S(464), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(916),
        S(523)
    ), S(693), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(850), S(412),
        5, 300, S(644), 30, autoClaim.SetCheckInterval, S(425))
    persistentSlider(farmTab, S(417), S(304),
        1, 30, S(29), 10, autoClaim.SetMaxPerPass, S(505))
    persistentToggle(farmTab:AddCard(
        S(620),
        S(796)
    ), S(408), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(703), S(35),
        5, 300, S(161), 30, autoPlot.SetCheckInterval, S(590))
    persistentToggle(
        farmTab:AddCard(S(66),
            S(988)),
        S(970), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(389),
            S(482)),
        S(399), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(163), S(719),
        0, 500, S(446), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(49))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(369))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(544), S(729))
    persistentToggle(
        settingsTab:AddCard(S(678), S(68)),
        S(467),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(836), 100)
    settingsTab:AddSlider(S(422), S(980), 50, 150, savedScale, function(value)
        config.Set(S(924), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(676), 1)
    settingsTab:AddSegmented({ S(286), S(768), S(614) }, savedLanguage, function(index, value)
        local code = ({ S(674), S(361), S(939) })[index]
        config.Set(S(466), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(859), S(794), S(838) })[savedLanguage] or S(115))
    log(S(56) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(315)] = function()
return {
    [79389059854988] = S(508),
}
end
import(S(76))(import)
