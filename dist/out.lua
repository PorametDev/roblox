-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"ItemFrame", "ตามเครื่อง", "OwnerUserId", " from ", "table", "en", "no plot", "[AutoChest] fireproximityprompt error: ", "menuScale", "'", "[ChestDump] === end ===", "[MyAnimeMine] === Holder: ", " cards left, label = '", "Content", " doesn't exist", "[AutoPlace] fireproximityprompt error: ", "Check", "games/my_anime_mine/get_plot_parts", "enabled", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/logger", "TextLabel", "[SellCards] Card #", "core/logger", "[AutoSell] click failed on ", "autoUpgrade", "Label", "TextButton", "Frame", "Content", ": ", "[AutoChest] Take All worked: ", "games/my_anime_mine/get_plot_parts", "Close", "[AutoSell] nothing selectable (", "ScrollingFrame", "Frame", "GuiButton", "TextLabel", "games/my_anime_mine/auto_place", "", "leaderstats", "interactionLogger", "UserInputService", "games/my_anime_mine/debug_dump_remotes", "GuiObject", "games/my_anime_mine/debug_dump_gui", "[ChestDump] === Chest subtree ===", ": ", "UIListLayout", "button has no size on screen", "[Scheduler] ", "en", "🏠", "Debug: Dump Sell Cards", " = ", "TextLabel", "[Remotes] === total: ", "Frame", ")", "Players", "[AutoUpgrade] ", "hotbar has no miner to place", "MineGui", "Debug: Dump Visible GUI", "", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "core/logger", "VirtualInputManager", "Players", "not standing in any plot", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "MineGui", "UpgradePart", "Frame", "UIScale", "Close", " '", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "autoSell", "core/logger", "TextLabel", "core/i18n", "Dump", "[Interaction] logger enabled - go click/interact with things now", "(none enabled)", "th", "UIListLayout", "en-us", "ClickDetector", "th", "Frame", "RunService", " '", "", "core/logger", "Players", " attributes ===", "HumanoidRootPart", "^(%d+)", "ค้นหาฟังก์ชัน...", " items, going to collect", "games/my_anime_mine/auto_upgrade", "Auto Sell", "Frame", "languageIndex", " (Text=", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", " (prompt says '", "games/my_anime_mine/auto_sell", "core/logger", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "core/anti_afk", "AutoMine", "GuiService", "core/logger", "core/click", "games/my_anime_mine/debug_dump", "  [attr] ", ") didn't take, trying the next one", "Debug: Dump Node/Holder", "games/my_anime_mine/debug_dump", "UIListLayout", "Place", " -> ", "?", "Players", "ChestCapacity", "auto", "\n", "core/ui", "Frame", " = ", " (Text=", "Card", " <-- remote, likely fired by the real interaction", "TextButton", "ย่อขยายเมนูทั้งชุด", "TabBar", "My Anime Mine · Mining Hub", "[AutoPlace] placed ", "core/logger", "Players", " error: ", "PlayerGui", "ClickDetector", "core/get_place_id", "Auto Mine: คลิกต่อรอบ", "  ", "core/logger", "Players", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "TextButton", " (power ", "Badge", ": ", "PlayerGui", "  ", "", "ClickDetector", "UserInputService", "core/click", "[AutoSell] fireproximityprompt error: ", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/auto_sell", "core/interaction_logger", "PlotIndex", "Done", "[Config] couldn't write ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "ReplicatedStorage", "auto", "menuScale", "TextLabel", "ChestFrame", " power ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", " ===", "Sell for", "MekPro.log", "core/logger", "core/logger", "", "Progress", " while selecting", "UICorner", "'", "core/click", "Players", "core/teleport", " ===", "RemoteEvent", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "games/my_anime_mine/get_plot_parts", "CoreUI", "/s", ": ", "core/get_place_id", "ProximityPrompt", "Done", "UIStroke", "core/logger", "games/my_anime_mine/get_current_plot", "TextLabel", "core/click", "SellStand", "core/scheduler", "Scroll", " items", "open_menu_hint", "[AutoSell] nothing to sell, closing", "Content", "  [attr] ", "Zones", "[%X] ", "TextLabel", "Tool", "ProximityPrompt", "[AutoChest] chest didn't open, aborting", "TextButton", "Label", "RemoteFunction", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "ValueBase", "Debug: Dump Chest Data", "core/scheduler", "'", "", "TextButton", "[MyAnimeMine] no nodes present right now", "[MekPro] settings file: ", "^x(%d+)$", "PlayerGui", "SellFrame", "[ChestDump] not standing in any plot", "write reported success but the line isn't in ", "Empty miner slot", "[AutoMine] ", "Open menu", "Workspace", "ScreenGui", "Content", "", "\n", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/get_current_plot", "ItemName", "", "Auto Upgrade", "TextLabel", "games/my_anime_mine/auto_upgrade", "[AutoSell] sell menu didn't open, aborting", "SelectAllButton", "autoMineSpeed", "AutoSell", "Main", "disabled", "disabled", "[AutoPlace] ", ",", "Debug: Dump All Remotes", "Auto Mine: ความเร็ว", "  ", "Sell for", "languageIndex", ": ", " ===", "[AutoPlace] disabled", "[^%d%.]", "games/my_anime_mine/debug_dump_chest", "", "[AntiAFK] disabled", "games/my_anime_mine/get_current_plot", ") from ", "games/my_anime_mine/main", "[AutoChest] ", "th", "[AntiAFK] idle detected, simulating input", "ไทย", "games/my_anime_mine/get_current_plot", "TextButton", "%", "/config.json", " '", "VirtualUser", "Holders", "TextBox", ": ", "s", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "HttpService", "[GuiDump] === end ===", "  ", "ClickDetector", " ===", "Backpack", "", "[MyAnimeMine] not standing in any plot", "core/config", " (Text=", "no tool", "[Config] couldn't read ", "games/my_anime_mine/auto_place", ")", "search_placeholder", "games/my_anime_mine/debug_dump_chest", "Text", "autoChest", "PlayerGui", " (power ", " ", "GuiObject", "Scroll", "MekPro", "  [attr] ", " ===", " = ", "[SellCards] === total cards: ", "core/dispatch", "HotbarFrame", "autoMineClicks", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " cards)", ": ", "[AutoSell] disabled", "games/my_anime_mine/auto_chest", "[Interaction] Button activated: ", "CoreGui", "TextLabel", "[MyAnimeMine] === Node: ", "[Remotes] ", "", "[MyAnimeMine] === Dump start ===", "Amount", "Plots", "autoMineSpeed", "Players", " (MaxActivationDistance=%s)", "ปุ่มเปิดเมนู", "TextLabel", "MineGui", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "[AutoPlace] ", "TextButton", "ขนาดเมนู", "'", "ProximityPrompt", "Card", "[ChestDump] === ReplicatedStorage.", " = ", "ตั้งค่า", " = ", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "HumanoidRootPart", "games/my_anime_mine/debug_dump_remotes", " cards", " '", "/", "write reported success but ", "[Interaction] ProximityPrompt triggered: ", "core/teleport", "TextLabel", "core/ui", "Auto Mine", "SellFrame", "every holder already has something at least as strong as the hotbar's best", "", "[AutoChest] can't read the chest billboard, is the plot loaded?", "th", "autoMineClicks", "[AutoSell] clicked SelectAllButton (", "TextLabel", "PlayerGui", "search_placeholder", "Chest", "[ChestDump] === plot ", "[Interaction] Key pressed: %s (gameProcessed=%s)", " items", "[AutoPlace] slot ", "^([%d,]+)$", ") -> ", " still empty after placing ", "TextButton", "games/my_anime_mine/debug_dump_sell_cards", " (", " items)", "Error: ", "MekPro", "core/get_place_id", "core/logger", "[GuiDump] MineGui not found", "[AutoPlace] nothing in the hotbar to place", " items)", "TextLabel", "Damage", "Dump", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "Header", "core/i18n", "  [attr] ", "", "Players", "", "games/my_anime_mine/get_plot_parts", "games/registry", "TextButton", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/auto_mine", "games/my_anime_mine/debug_dump_sell_cards", "core/interaction_logger", "Dump", "Nodes", "", "PlayerGui", "X", "[GuiDump] === visible MineGui tree ===", "games/my_anime_mine/get_plot_parts", "Search functions...", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "[AutoChest] enabled (collects once >= ", "MineWorld", "Auto Chest", "[Interaction] logger disabled", "Workspace", "no isfile/readfile available to verify the write", "ValueLabel", "log failed", "waiting", "ProximityPrompt", "waiting", " '", "This is not a supported place.", "[AntiAFK] enabled - watching Player.Idled", "core/get_me", " = ", "core/teleport", "", "ClickDetector", "UIPadding", "ValueBase", " (", "/", "[ChestDump] === Player subtree ===", "core/logger", "ProximityPrompt", "Players", "[AutoSell] clicked SellButton", "TextButton", "[AutoChest] Take All had NO effect (", "[MyAnimeMine] === Tool: ", "core/anti_afk", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[AutoPlace] couldn't pick up ", "auto", "[ChestDump] plot has no Chest", "core/config", "antiAfk", "Players", "Content", "[AutoSell] enabled (only sells when there's something in the scroll)", "Auto Place Miner", "Frame", "'), backing off ", "ValueLabel", "ReplicatedStorage", "Humanoid", ")", "[MyAnimeMine] no holders found", "autoMine", "Interaction Logger (debug)", " x", "Players", "ProximityPrompt", "[AutoChest] click failed on ", ": ", "Select All", "Content", "core/get_leaderstat", "neither appendfile nor writefile is available", "core/logger", "AutoChest", "games/my_anime_mine/get_current_plot", "games/registry", "core/i18n", "Frame", "  ", "Loot", " remotes", "Content", "", "Dump", "-", "open_menu_hint", "not found", "'", "Players", "RemoteEvent", "  ", "Dump", "core/logger", "", " (ActionText=", " = ", "Frame", ": ", "Pad", "Crew", "Content", "games/my_anime_mine/auto_mine", "autoPlace", "Players", "Text", "Backpack", "/", "[AutoChest] disabled", "Dump", "games/my_anime_mine/main", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[BestMiner] slot ", "games/my_anime_mine/get_current_plot", "TextLabel", "UICorner", " ===", "Actions", "ฟาร์ม", "MineGui", ",", "games/my_anime_mine/get_plot_parts", "[AutoPlace] picked up ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "games/my_anime_mine/auto_chest", ")", " -> ", "en", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "MineGui", " parts", "⚙", "unreadable", "", "TextLabel", "Search", "Done", "อังกฤษ", "[AutoChest] waiting: ", "AutoPlace", "games/my_anime_mine/debug_dump_gui", "')", "", "enabled", "Content", "Container", "[AutoPlace] drifted out of range of ", "Debug: Dump Backpack Tool", "Badge", "x", ")", "RemoteFunction", "SellButton", "[attr] ", "[Scheduler] turn: ", "games/my_anime_mine/get_plot_parts", "Debug: Current Plot", "Anti-AFK", "core/dispatch", "", "core/teleport", "[Logger] failed to write ", "TakeButton", "[AutoPlace] upgrading ", "AutoUpgrade", "Workspace", "Frame", ") on ", "unreadable", "auto", "TextLabel", "GuiButton", "[Interaction] ClickDetector clicked: ", " ("}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(113)] = function()
return function(import)
local log = import(S(111))
local Players = game:GetService(S(496))
local VirtualUser = game:GetService(S(285))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(433))
        connection = player.Idled:Connect(function()
            log(S(278))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(272))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(162)] = function()
local VirtualInputManager = game:GetService(S(69))
local GuiService = game:GetService(S(115))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(51)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(456)] = function()
return function(import)
local HttpService = game:GetService(S(291))
local getPlaceId = import(S(389))
local log = import(S(444))
local ROOT_FOLDER = S(388)
local placeFolder = ROOT_FOLDER .. S(514) .. tostring(getPlaceId())
local filePath = placeFolder .. S(283)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(5) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(302) .. filePath .. S(156) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(169) .. filePath .. S(49) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(319)] = function()
return function(import)
    local getPlaceId = import(S(147))
    local registry = import(S(405))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(432))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(478)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(42))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(434)] = function()
local Players = game:GetService(S(151))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(466))
    local backpack = player:WaitForChild(S(296))
    return player, humanoid, backpack
end
return getMe
end
modules[S(198)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(399)] = function()
local Players = game:GetService(S(143))
local STRINGS = {
    en = {
        search_placeholder = S(418),
        open_menu_hint = S(239),
    },
    th = {
        search_placeholder = S(101),
        open_menu_hint = S(339),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(89)
    if locale:sub(1, 2):lower() == S(87) then
        return S(369)
    end
    return S(6)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(576) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(166)] = function()
return function(import)
local Players = game:GetService(S(127))
local Workspace = game:GetService(S(572))
local UserInputService = game:GetService(S(161))
local log = import(S(142))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(360) .. describe(prompt)
            .. S(502) .. prompt.ActionText .. S(304))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(579) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(327) .. describe(button)
            .. S(300) .. tostring(button.Text) .. S(60))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(429)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(160)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(38)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(423))
        return
    end
    connections = {}
    log(S(85))
    local playerGui = Players.LocalPlayer:WaitForChild(S(309))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(473)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(438)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(578)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(377),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(21)] = function()
local LOG_FILE = S(180)
local function log(message)
    local line = os.date(S(215)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(41))
            end
            appendfile(LOG_FILE, line .. S(130))
        elseif writefile then
            local existing = S(297)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(244))
        else
            error(S(479))
        end
        if not (isfile and readfile) then
            error(S(425))
        end
        if not isfile(LOG_FILE) then
            error(S(359) .. LOG_FILE .. S(15))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(236) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(568) .. LOG_FILE .. S(288) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(226)] = function()
return function(import)
local RunService = game:GetService(S(93))
local log = import(S(24))
local SLICE_SECONDS = 3
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy)
    table.insert(exclusiveTasks, { name = name, step = step, isEnabled = isEnabled, isBusy = isBusy })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(52) .. task_.name .. S(144) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickNextEnabled()
    if #exclusiveTasks == 0 then
        return nil
    end
    for _ = 1, #exclusiveTasks do
        rrIndex = (rrIndex % #exclusiveTasks) + 1
        local candidate = exclusiveTasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(561) .. (currentTask and currentTask.name or S(86)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(567)] = function()
local Players = game:GetService(S(337))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(354))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(363)] = function()
return function(import)
local i18n = import(S(83))
local UserInputService = game:GetService(S(44))
local THEME = {
    BrandName = S(314),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(522))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(186))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(439))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(229))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(95)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(201))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(39))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(126)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(362))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(183)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(36))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(123))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(372))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(92))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(250))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(340))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(521))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(464))
            if not valueLbl then
                valueLbl = Instance.new(S(541))
                valueLbl.Name = S(426)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(155))
            if not badge then
                badge = Instance.new(S(577))
                badge.Name = S(555)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(344))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(59))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(504))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(383))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(66)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(282)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(29))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(75))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(406))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(566)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(485))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(50))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(137))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(307)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(241))
    screenGui.Name = S(195)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(328))
    local main = Instance.new(S(462))
    main.Name = S(256)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(76))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(105))
    tabBar.Name = S(139)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(88))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(573))
    header.Name = S(398)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(448))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(216))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(57))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(413)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(220))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(415)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(153))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(492)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(287))
    searchBox.Name = S(542)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(305))
    searchBox.Text = S(228)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(132))
    content.Name = S(14)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(37))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(394))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(210))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(174))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(512)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(271) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(311) .. (item.Subtitle or S(248))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(374))
        footerLabel.Text = i18n.T(S(493))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(531)] = function()
return function(import)
local Players = game:GetService(S(97))
local getCurrentPlot = import(S(246))
local getPlotParts = import(S(33))
local teleport = import(S(436))
local click = import(S(117))
local log = import(S(96))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(145))
    local mineGui = playerGui and playerGui:FindFirstChild(S(73))
    return mineGui and mineGui:FindFirstChild(S(175))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(489))
    local actions = content and content:FindFirstChild(S(524))
    return actions and actions:FindFirstChild(S(569))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(260), S(243)):match(S(100))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(334), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(30))
    local progress = content and content:FindFirstChild(S(184))
    local label = progress and progress:FindFirstChild(S(27))
    return parseCount(label and label.Text or S(437))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(474) .. button.Name .. S(505) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(218), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(515))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(420) .. MIN_ITEMS_TO_COLLECT .. S(386))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(8) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(219))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(32) .. count .. S(125) .. after .. S(378))
        else
            log(S(449) .. tostring(count)
                .. S(533) .. tostring(after) .. S(393))
        end
    end
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(575) then
            log(S(368))
            lastReportedCount = S(539)
        end
        return
    end
    if count < MIN_ITEMS_TO_COLLECT then
        if lastReportedCount ~= S(428) then
            log(S(545) .. count .. S(358) .. MIN_ITEMS_TO_COLLECT .. S(209))
            lastReportedCount = S(430)
        end
        return
    end
    lastReportedCount = nil
    log(S(276) .. count .. S(102))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(509)] = function()
return function(import)
local getCurrentPlot = import(S(407))
local getPlotParts = import(S(194))
local log = import(S(150))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(294), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(238) .. (enabled and S(550) or S(257)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(40)] = function()
return function(import)
local Players = game:GetService(S(61))
local getCurrentPlot = import(S(482))
local getPlotParts = import(S(528))
local getMinerSlots = import(S(164))(import)
local teleport = import(S(361))
local click = import(S(205))
local log = import(S(500))
local CHECK_INTERVAL = 3
local EMPTY_SLOT_TEXT = S(237)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(506))
    return pad and pad:FindFirstChildWhichIsA(S(199), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(204)) then
            local digits = descendant.Text:match(S(380))
            if digits then
                return tonumber((digits:gsub(S(527), S(367))))
            end
        end
    end
    return nil
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(99))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(16) .. tostring(err))
    end
    return ok
end
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt and prompt.Enabled then
            if isEmpty(prompt) then
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = readPlacedPower(holder) or 0,
                })
            end
        end
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return empty, occupied
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(259) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(268))
    else
        lastAttempt = 0
        log(S(193))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(518),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(124), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(392))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(553) .. holder.Name .. S(185))
            return false
        end
        log(S(379) .. candidate.slot .. S(441) .. candidate.name
            .. S(120))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(343) .. holder.Name .. S(382) .. miner.name)
        return false
    end
    log(S(141) .. miner.name .. S(310) .. miner.power
        .. S(574) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(453) .. entry.name .. S(4) .. entry.holder.Name
            .. S(109) .. tostring(entry.prompt.ActionText) .. S(548))
        return false
    end
    log(S(529) .. entry.name .. S(154) .. entry.power
        .. S(274) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local empty, occupied = surveyHolders()
    if not empty then
        reason(S(71))
        return
    end
    local candidates = getMinerSlots()
    if #candidates == 0 then
        reason(S(63))
        return
    end
    if #empty > 0 then
        lastReason = nil
        task.spawn(function()
            runFillJob(empty[1])
        end)
        return
    end
    local weakest = occupied[1]
    local best = candidates[1]
    if weakest and best.power > weakest.power then
        lastReason = nil
        log(S(570) .. weakest.holder.Name .. S(197) .. weakest.name
            .. S(580) .. weakest.power .. S(381) .. best.name .. S(385) .. best.power .. S(557))
        task.spawn(function()
            runUpgradeJob(weakest)
        end)
        return
    end
    reason(S(366))
end
return autoPlace
end
end
modules[S(110)] = function()
return function(import)
local Players = game:GetService(S(446))
local Workspace = game:GetService(S(424))
local teleport = import(S(190))
local click = import(S(188))
local log = import(S(182))
local CHECK_INTERVAL = 45
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(233))
    local mineGui = playerGui and playerGui:FindFirstChild(S(536))
    return mineGui and mineGui:FindFirstChild(S(234))
end
local function labelText(button)
    local content = button:FindFirstChild(S(477))
    local label = content and content:FindFirstChild(S(221))
    return label and label.Text or S(501)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(459))
    local scroll = content and content:FindFirstChild(S(313))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(135) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(25) .. button.Name .. S(31) .. tostring(err))
    end
    return ok
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(206))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(445), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(325))
    else
        lastAttempt = 0
        log(S(460))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(163) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(252))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(211))
        local closeButton = sellFrame:FindFirstChild(S(77))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(242))
    local selectAllButton = content and content:FindFirstChild(S(253))
    local sellButton = content and content:FindFirstChild(S(559))
    if selectAllButton and labelText(selectAllButton) == S(476) then
        clickButton(selectAllButton)
        log(S(371) .. cardCount .. S(323))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(264), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(179), 1, true) then
        clickButton(sellButton)
        log(S(447))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(34))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(35) .. cardCount .. S(13)
            .. tostring(sawLabel) .. S(463) .. NOTHING_SELLABLE_COOLDOWN .. S(289))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(103)] = function()
return function(import)
local getCurrentPlot = import(S(520))
local getPlotParts = import(S(562))
local log = import(S(202))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(74))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(90))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(62) .. (enabled and S(19) or S(258)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(118)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(540)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(94) .. child.Name .. S(346)
        if child:IsA(S(347)) then
            line = line .. string.format(
                S(419),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(146)) then
            line = line .. string.format(S(338), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(192)) or child:IsA(S(558)) then
            line = line .. S(136)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(213) .. attrName .. S(503) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(486))
    end
end
return dumpInstance
end
modules[S(270)] = function()
return function(import)
local Players = game:GetService(S(402))
local ReplicatedStorage = game:GetService(S(465))
local getCurrentPlot = import(S(203))
local getPlotParts = import(S(18))
local log = import(S(480))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(560) .. name .. S(435) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(158))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(78) .. child.Name .. S(187)
            if child:IsA(S(440)) then
                line = line .. S(133) .. tostring(child.Value)
            elseif child:IsA(S(329)) or child:IsA(S(28)) then
                line = line .. S(107) .. tostring(child.Text) .. S(532)
            end
            log(line)
            dumpSubtree(child, indent .. S(498))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(235))
        return 0
    end
    log(S(376) .. plot.Name .. S(98))
    dumpAttributes(plot, S(263))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(455))
        return 0
    end
    log(S(48))
    dumpSubtree(parts.Chest, S(401))
    local player = Players.LocalPlayer
    log(S(443))
    dumpSubtree(player, S(549))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(349) .. player.Name .. S(178))
        dumpSubtree(mine, S(490))
    end
    log(S(11))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(47)] = function()
return function(import)
local Players = game:GetService(S(511))
local log = import(S(81))
local function dumpVisible(root, indent)
    indent = indent or S(159)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(312)) and child.Visible then
            local line = indent .. child.ClassName .. S(431) .. child.Name .. S(227)
            if child:IsA(S(22)) or child:IsA(S(281)) then
                line = line .. S(134) .. tostring(child.Text) .. S(467)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(400) .. attrName .. S(352) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(293))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(157))
    local mineGui = playerGui and playerGui:FindFirstChild(S(526))
    if not mineGui then
        log(S(391))
        return
    end
    log(S(416))
    dumpVisible(mineGui)
    log(S(292))
end
return dumpMineGui
end
end
modules[S(45)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(171))
local log = import(S(390))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(497)) or descendant:IsA(S(222)) then
            count = count + 1
            log(S(331) .. descendant.ClassName .. S(324) .. descendant:GetFullName())
        end
    end
    log(S(58) .. count .. S(295))
    return count
end
return dumpRemotes
end
end
modules[S(409)] = function()
return function(import)
local Players = game:GetService(S(70))
local log = import(S(116))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(414))
    local mineGui = playerGui and playerGui:FindFirstChild(S(341))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(365))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(508)) and sellFrame.Content:FindFirstChild(S(208))
    if not scroll then
        log(S(108))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(348) or card:IsA(S(46)) then
            count = count + 1
            log(S(23) .. count .. S(475) .. card.ClassName .. S(284) .. card.Name .. S(10))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(119) .. attrName .. S(317) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(149) .. child.ClassName .. S(357) .. child.Name .. S(495)
                if child:IsA(S(224)) then
                    line = line .. S(56) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(318) .. count .. S(191))
    return count
end
return dumpSellCards
end
end
modules[S(245)] = function()
return function(import)
local Players = game:GetService(S(458))
local log = import(S(181))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(373))
    local mineGui = playerGui and playerGui:FindFirstChild(S(64))
    local hotbar = mineGui and mineGui:FindFirstChild(S(320))
    local content = hotbar and hotbar:FindFirstChild(S(551))
    return content and content:FindFirstChild(S(552))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(82)) then
            local count = descendant.Text:match(S(232))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(1))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(212))
    local nameLabel = content and content:FindFirstChild(S(247))
    local damageLabel = content and content:FindFirstChild(S(395))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(403) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(269), S(332)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(442) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(519) .. slot .. S(266) .. info.name
                        .. S(176) .. info.power .. S(471) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(273)] = function()
local Players = game:GetService(S(189))
local Workspace = game:GetService(S(240))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(421))
    local plots = mineWorld and mineWorld:FindFirstChild(S(335))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(3)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(404)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(286)),
        Nodes = plot:FindFirstChild(S(412)),
        Chest = plot:FindFirstChild(S(375)),
        Zones = plot:FindFirstChild(S(214)),
        Crew = plot:FindFirstChild(S(507)),
        Loot = plot:FindFirstChild(S(487)),
        PlotIndex = plot:GetAttribute(S(167)),
        ChestCapacity = plot:GetAttribute(S(128)),
    }
end
return getPlotParts
end
modules[S(275)] = function()
return function(import)
    local Players = game:GetService(S(472))
    local createWindow = import(S(131))(import)
    local i18n = import(S(484))
    local antiAfk = import(S(451))(import)
    local interactionLogger = import(S(410))(import)
    local scheduler = import(S(207))(import)
    local config = import(S(299))(import)
    local log = import(S(68))
    local window = createWindow({
        Subtitle = S(140),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local getCurrentPlot = import(S(280))
    local getPlotParts = import(S(417))
    local dumpInstance = import(S(122))
    local dumpRemotes = import(S(355))(import)
    local dumpSellCards = import(S(384))(import)
    local dumpGui = import(S(547))(import)
    local dumpChest = import(S(306))(import)
    local autoMine = import(S(408))(import)
    local autoPlace = import(S(303))(import)
    local autoChest = import(S(326))(import)
    local autoSell = import(S(165))(import)
    local autoUpgrade = import(S(251))(import)
    scheduler.RegisterStandalone(S(114), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterStandalone(S(571), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.RegisterExclusive(S(546), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy)
    scheduler.RegisterExclusive(S(481), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy)
    scheduler.RegisterExclusive(S(255), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(525), S(54))
    persistentToggle(farmTab:AddCard(
        S(364),
        S(452)
    ), S(469), autoMine.SetEnabled)
    local savedMineSpeed = config.Get(S(336), 5)
    farmTab:AddSlider(
        S(262),
        S(177),
        1, 30, savedMineSpeed,
        function(value)
            config.Set(S(254), value)
            autoMine.SetCyclesPerSecond(value)
        end,
        S(196)
    )
    autoMine.SetCyclesPerSecond(savedMineSpeed)
    local savedMineClicks = config.Get(S(321), 1)
    farmTab:AddSlider(
        S(148),
        S(152),
        1, 10, savedMineClicks,
        function(value)
            config.Set(S(370), value)
            autoMine.SetClicksPerCycle(value)
        end,
        S(556)
    )
    autoMine.SetClicksPerCycle(savedMineClicks)
    persistentToggle(farmTab:AddCard(
        S(461),
        S(20)
    ), S(510), autoPlace.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(422),
        S(342)
    ), S(308), autoChest.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(104),
        S(170)
    ), S(80), autoSell.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(249),
        S(223)
    ), S(26), autoUpgrade.SetEnabled)
    local debugCard = farmTab:AddCard(S(563), S(79))
    local checkBtn
    checkBtn = debugCard:AddButton(S(17), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(494)
            log(S(298))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(112),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(121),
        S(290)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(516), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(7)
            return
        end
        local ok, err = log(S(333))
        if not ok then
            dumpBtn.Text = S(427)
            dumpCard:SetSubtitle(S(387) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(330) .. firstNode.Name .. S(267))
            dumpInstance(firstNode, log)
        else
            log(S(230))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(12) .. firstHolder.Name .. S(316))
            dumpInstance(firstHolder, log)
        else
            log(S(468))
        end
        dumpBtn.Text = S(200)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(554),
        S(535)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(396), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(513))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(217))
        if not tool then
            toolDumpBtn.Text = S(301)
            return
        end
        log(S(450) .. tool.Name .. S(523))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(315) .. attrName .. S(350) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(168)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(261),
        S(322)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(491), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(488)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(55),
        S(397)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(411), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(356)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(65),
        S(353)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(499), function()
        dumpGui()
        guiDumpBtn.Text = S(543)
    end)
    local chestDumpCard = farmTab:AddCard(
        S(225),
        S(72)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(84), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(537)
    end)
    local settingsTab = window:AddTab(S(351), S(538))
    persistentToggle(
        settingsTab:AddCard(S(564), S(530)),
        S(457),
        antiAfk.SetEnabled
    )
    persistentToggle(
        settingsTab:AddCard(
            S(470),
            S(67)
        ),
        S(43),
        interactionLogger.SetEnabled
    )
    local savedScale = config.Get(S(173), 100)
    settingsTab:AddSlider(S(345), S(138), 50, 150, savedScale, function(value)
        config.Set(S(9), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(265), 1)
    settingsTab:AddSegmented({ S(2), S(544), S(279) }, savedLanguage, function(index, value)
        local code = ({ S(129), S(53), S(91) })[index]
        config.Set(S(106), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(172), S(534), S(277) })[savedLanguage] or S(454))
    log(S(231) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(483)] = function()
return {
    [79389059854988] = S(517),
}
end
import(S(565))(import)
