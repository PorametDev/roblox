-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/run_job", "string", "Auto Chest: เกณฑ์เก็บ", "games/my_anime_mine/get_best_miner", "table", "enabled", "Speed", "GetHolderSpeedLevel", "string", "BasePart", "UIListLayout", "PlayerGui", "", "s", "Players", ")", "games/my_anime_mine/auto_plot", "lowest", "jumpPower", "Frame", "Players", "ValueLabel", "autoUpgradePickaxe", "Dump Visible GUI", "[AutoPlace] fireproximityprompt error: ", "PlayerGui", "auto", "autoSell", "[AutoPlace] ", "ตามเครื่อง", " ", "[AutoResearch] cheapest open node: ", "languageIndex", "Frame", "enabled", ")", ")", "แพงสุดก่อน", "[Interaction] ClickDetector clicked: ", "ReplicatedStorage", "TextLabel", "[AutoChest] chest is empty", " -> x", ": ", " screens", "Workspace", "s", "s", "[AutoChest] MineTakeChest had no effect (", ") didn't take, trying the next one", "action '", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "เช็คโซนทุกกี่วินาที", "ItemName", " buyZones=", "", "Auto Research: ซื้อสูงสุดต่อรอบ", "autoResearchCore", "debug.getupvalues", "may need us at the stand - trying that", "core/scheduler", "HumanoidRootPart", "autoSellProtectUnits", "core/run_job", "Character", "Frame", "Auto Place: รอบเช็ค", "UserInputService", " (cost ", "autoUpgradeKeepMoney", "autoPlotInterval", " ===", ")", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "Hauler", "ValueLabel", "ย่อขยายเมนูทั้งชุด", "ไล่อัปเกรด miner ทุกกี่วินาที", "getscripts", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "core/get_place_id", "core/debug_tab", "table", "core/debug_decompile", "en", "ขนาดเมนู", "", " while selecting", "%$", "core/get_place_id", " items)", "[Decompile] === executor capabilities ===", "Workspace", "Players", "[AutoPlot] now mining ", "games/my_anime_mine/main", "core/logger", "games/my_anime_mine/get_plot_parts", "autoUpgradeLimitPickaxes", "games/my_anime_mine/game_api", ") ", "autoUpgradeEquipPickaxe", "[AutoUpgrade] ", " doesn't exist", "[Interaction] ProximityPrompt triggered: ", "", "M", "en", "lowest", "[Interaction] logger enabled - go click/interact with things now", "core/run_job", " ชิ้น", " node(s) looked buyable but the server refused", "autoUpgradeMutationBudget", "autoResearchZones", "empty", "AutoResearch", "UIListLayout", "GetHolderMineSpeedLevel", "Run", " ", "autoResearchCheapestFirst", "AutoChest", "ZoneBoard", " powerOk=", "/config.json", "autoPlotBuy", "MineRollMutation", "autoSellInterval", "TextLabel", "?", "MekPro", "ProximityPrompt", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "AutoPlace", "", " (chest holds ", "games/my_anime_mine/game_api", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "Mining", "Content", "games/my_anime_mine/auto_mine", " screen(s)) ===", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "UICorner", "[Config] couldn't read ", "TextLabel", "Frame", "  ", " -> ", "debug.getproto", "Container", "no isfile/readfile available to verify the write", "(", "[%X] ", "string", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "core/run_job", "AutoPlot", " items (now ", "ไล่ซื้อทุกกี่วินาที", " buyCore=", "", "GuiObject", " #", "th", "getcallbackvalue", "ต่ำสุดก่อน", "HotbarFrame", "table", "Humanoid", "MekPro", "TextButton", " -> ", "ClickDetector", " still empty after placing ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "Auto Claim (Index)", "Empty miner slot", "/", "Players", " (power ", "autoClaim", "GetCharacterUpgradeCost", "Mutation: หยุดเมื่อได้ x", "Search", "waiting", "core/teleport", "no", "Badge", "write reported success but ", "core/build_env", "=locked(", "[AutoResearch] ", " ", "[AutoPlace] couldn't pick up ", "core/logger", "menuScale", "=empty", "[AutoPlace] holders: ", "(nothing to do)", "⚙", "[Uu]nlock", "[AutoResearch] ", "table", "Text", "🏠", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "CharSpeedMaxLevel", "Damage", "0 = ไม่จำกัด", "  [attr] ", ") ", "open_menu_hint", "autoClaimMaxPerPass", "ItemFrame", "autoUpgradePickaxeIndex", ", you have ", "disabled", "games/my_anime_mine/auto_research", "core/teleport", ")", " ", "TextLabel", "", "", "PickaxesStand", "Locked miner slot", ": ", "string", "core/i18n", "/", "Dump All Remotes", "PlotIndex", "/", "autoChest", ") needs ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", ")", " = ", "TextLabel", "[GuiDump] === end (", "s", "Chest", "Players", "", " budget=", "autoUpgradePickaxeIndex", "Auto Upgrade Plot (Zone)", "search_placeholder", "Nodes", "เดินทะลุกำแพงและพื้น", "", "[Remotes] === total: ", ": ", "core/logger", "/", "core/logger", "[GuiDump] PlayerGui not found", "autoUpgradeRollMutation", "AutoClaim", "[Decompile] ", " mutation x", "[Bb]uy", "MekPro/decompiled", "getconnections", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "core/humanoid", "/", "MinePower", " (", "Text", "LayerCollector", "MineGui", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "table", "Auto Mine: คลิกต่อรอบ", " (", "CoreGui", "games/my_anime_mine/game_api", "", "Shop Stand", "[AutoUpgrade] ", "", "TextButton", "[AutoChest] disabled", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "core/ui", "[AutoPlace] picked up ", "ClickDetector", " remotes", "core/teleport", "ZoneBoardGui", "x", "MineEquipPickaxe", " ", "[AutoResearch] idle: money=", "TabBar", "antiAfk", " has been busy for ", "Crew", "", "cost", "TextButton", "RunService", " open=", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "Modules", "core/logger", "write reported success but the line isn't in ", "^M(%d+)$", "", ") from ", "Players", "search_placeholder", "[AutoPlace] upgrading ", "autoResearch", "core/logger", "core/teleport", "Auto Research: กันเงินไว้", "ProximityPrompt", ") ", "CoreUI", "This is not a supported place.", " (x", "%.0f", "TextButton", "Auto Upgrade: สุ่ม Mutation", "games/my_anime_mine/auto_upgrade", "'", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "Main", "Auto Research: ซื้อสาย Zone", "AutoMine", "MineEquipSlot", "autoResearchKeepMoney", "function", "[AutoPlace] drifted out of range of ", "[AutoPlot] ", " '", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "M", "TextLabel", "core/scheduler", " (", "maxed", "%", "core/dispatch", "Auto Research: รอบเช็ค", "neither appendfile nor writefile is available", "[AutoUpgrade] pickaxe #", "walkSpeed", "core/logger", "M", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "autoSellMaxWait", "UIPadding", "autoResearchCoreLast", "unreadable", "LocalScript", "Interaction Logger", "autoMineSpeed", "TextBox", "ขายทุกกี่วินาที", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "autoUpgrade", "autoUpgradeInterval", "core/logger", "unreadable", " not bought yet (", "Mining", "AutoPlot", "[AutoResearch]   ", "ZoneRow", "Pad", "[AutoUpgrade] every stat on every miner is maxed", "%$", "M", "Header", "[BestMiner] slot ", "", ")", "ModuleScript", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "^([%d,]+)$", "Frame", ",", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "autoChestMinItems", "games/my_anime_mine/auto_research", "RemoteEvent", "en", "games/my_anime_mine/auto_sell", "TextButton", " waited ", "core/run_job", " x", "autoPlaceInterval", "core/debug_interaction_logger", "function", "autoPlaceUpgradeWeak", "core/logger", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " (", " (x", "VirtualUser", "[AutoSell] enabled", "Frame", "CharMaxLevel", "TextLabel", "fill", "Search functions...", "languageIndex", "ความเร็ววิ่ง", "core/logger", "MineWorld", "core/config", "table", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", ")", "[^%w]+", "auto", "Workspace", "UserInputService", "interactionLogger", "sold ", "Frame", "Auto Plot: กันเงินไว้", "th", "([%d,%.]+)%s*(%a*)", "Speed", "^%$", "TextLabel", "table", "autoUpgradePriority", "Auto Plot: รอบเช็ค", "core/debug_tab", "JumpPower (ปกติ 50)", "List", "MineSelectZone", "auto", "Auto Mine", "games/my_anime_mine/auto_sell", "[AutoPlace] disabled", "disabled", ")", "getgc", "[AutoResearch]   ", "x", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "[AutoResearch] Core ", "core/logger", "TextButton", "table", " refused - ", "🐛", "games/my_anime_mine/get_current_plot", "getloadedmodules", "games/registry", "[AutoUpgrade] ", ",", "core/click", " '", "[AutoResearch]   Core: ", "ScreenGui", "Mutation: เพดานราคาต่อครั้ง", "[Interaction] Key pressed: %s (gameProcessed=%s)", "open_menu_hint", "Content", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " (", " (", " costs ", ") -> ", "s", "TextButton", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Auto Research (Plot)", "waiting", " req=", "[GuiDump] === visible PlayerGui tree ===", "Hauler", "string", "core/logger", " ", "core/get_leaderstat", " power ", "[Decompile] decompile unavailable in this executor", "[Logger] failed to write ", "TextLabel", "number", " (", "prompt disabled", "games/my_anime_mine/get_plot_parts", "s - carrying on without it", "games/my_anime_mine/game_api", "table", "core/dispatch", "[AutoPlot] couldn't switch to ", "/", ")", "Auto Sell: รอคิวได้นานสุด", "games/my_anime_mine/game_api", "[AutoMine] ", "-", "[GuiDump] ", "Dump", " ", " costs ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "TextLabel", " power=", "autoPlotKeepMoney", "core/anti_afk", "locked slot", "[AntiAFK] idle detected, simulating input", "[Decompile] === end ===", "", "games/my_anime_mine/game_api", "CharMineSpeedMaxLevel", "[AutoClaim] ", " script(s) to ", "nothing to place or upgrade", "getnilinstances", "noclip", ")", "BodyPosition", "TextLabel", "ปุ่มเปิดเมนู", "UIListLayout", "Auto Claim: รอบเช็ค", "research", "autoUpgradeMaxPerPass", ",", "", ")", "ClickDetector", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", " reward(s) looked claimable but none went through", "x", "Anti-AFK", "[AutoClaim] claimed ", "[Decompile] === start ===", "empty", "MineTakeChest", "HumanoidRootPart", "needs us at the stand - keeping the teleport", "Auto Place Miner", "HttpService", "VirtualInputManager", "RemoteFunction", "autoResearchMaxPerPass", "Decompile Client Scripts", "", " (", " ", "table", "cost", "core/logger", "TextLabel", ": ", "แรงกระโดด", "autoMine", "core/parse_money", "UIScale", "[AutoChest] can't read the chest yet", "games/my_anime_mine/game_api", "games/my_anime_mine/auto_chest", " cost=", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "[Decompile] PlayerScripts not found", "Auto Claim: เก็บสูงสุดต่อรอบ", "games/registry", "ProximityPrompt", "TextLabel", " stack(s)", "[Config] couldn't write ", "Auto Plot: ซื้อโซนถัดไป", "leaderstats", ",", "GuiButton", "core/debug_dump_gui", "[AutoResearch] ", "AutoUpgrade", " - upgrade the miners first", " (", "debug.getconstants", ")", "x", "GetHolderLevel", "ChestCapacity", "core/config", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "games/my_anime_mine/auto_plot", "[Scheduler] ", "GetCharacterMineSpeedUpgradeCost", "/s", "อ่านป้ายบน chest ทุกกี่วินาที", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "getrenv", "th", "", "core/logger", "table", "function", " -> ", "AutoPlace", "core/debug_dump_remotes", "games/my_anime_mine/auto_upgrade", "  ", "yes", "Frame", "UICorner", " didn't go through (out of stock or too expensive)", "[AutoPlot] unlocked ", " -> ", "table", " owned)", "core/ui", "core/humanoid", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "costs ", "[AutoPlace] nothing in the hotbar to place", "enabled", "", "Dump", "MineBuyPickaxe", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "autoClaimInterval", "core/build_env", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "#", "/", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "MineSpeed", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "", "games/my_anime_mine/auto_place", " (Text=", "none", "/", "core/logger", " pickaxe ", "Players", ")", " team power, you have ", "MekPro.log", " ", "'", "Players", "Workspace", "[", "Players", "AutoSell", "[Remotes] ", "ตั้งค่า", "autoChestInterval", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "getsenv", "Auto Upgrade: รอบเช็ค", "table", "games/my_anime_mine/auto_claim", "Frame", "HumanoidRootPart", "[AutoPlace] ", "Auto Research: ซื้อสาย Core", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[AutoUpgrade] bought pickaxe #", "[Scheduler] turn: ", "TextLabel", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "ProximityPrompt", " (Text=", ": ", "มุดแมพ (Noclip)", "Backpack", "", " ", "[AutoResearch] nothing open and affordable right now", "core/debug_interaction_logger", "[AntiAFK] enabled - watching Player.Idled", "^x(%d+)$", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "dev", "AutoChest", "[AutoPlace] placed ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "disabled", "Root", "MekPro", "[AutoChest] collected ", "autoMineClicks", "บิน (Fly)", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Damage", "", "ถูกสุดก่อน", "AutoClaim", " node(s), ", "GuiButton", " zones=", "games/my_anime_mine/get_current_plot", "table", "Workspace", "AutoPlace", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "\n", "Auto Sell", "MineSpeed", "AutoResearch", "games/my_anime_mine/auto_chest", "ดีบัก", "ไทย", " open and unmaxed", "[AutoPlot] ", "/", "อังกฤษ", "[AutoChest] enabled (collects once >= ", "Content", ": ", "core/debug_dump_remotes", "core/debug_dump_gui", "ฟาร์ม", "core/logger", "')", "[AutoUpgrade] ", "BasePart", " = ", "Frame", " holder(s) - not buying more", "MekPro", "autoSellMaxWait", "core/run_job", "core/i18n", "Auto Sell: รอบขาย", "2026-09-21 05:12:17", "Players", "games/my_anime_mine/auto_claim", "[Scheduler] ", "PlayerGui", "th", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Frame", "Auto Chest: รอบเช็ค", "Plots", " -> ", "s", "] job failed: ", "autoResearchInterval", "TextButton", "Speed", " pickaxe(s) for ", "_", "Auto Place: เปลี่ยนตัวอ่อนออก", "games/my_anime_mine/game_api", ", base ", "'", "fill", "d820662", "[GameAPI] SendEvent failed: ", "Open menu", "All", "SellStand", ") ", "WalkSpeed (ปกติ 16)", " research is ", "autoPlotLatest", "sale didn't go through", "OwnerUserId", "autoPlot", "AutoSell", "My Anime Mine · Mining Hub", "OwnerUserId", ") on ", "AutoUpgrade", "slot ", "MineSellItems", "[AutoResearch] ", "Auto Plot: ใช้โซนล่าสุดเสมอ", " (ActionText=", "[Interaction] logger disabled", " from ", " | ", "[Scheduler] ", ", built ", "Players", " files", ": ", "Auto Upgrade: แจก Pickaxe ให้ miner", "TextLabel", "games/my_anime_mine/auto_mine", ")", "[AutoSell] ", "UIStroke", "enabled", "upgrade", "core/get_place_id", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " (prompt says '", "MineBuySkill", "games/my_anime_mine/game_api", "X", "", ".lua", "en-us", "disabled", "[AutoSell] MineSellItems ", "core/run_job", "TextLabel", "table", "BodyVelocity", "TextButton", "games/my_anime_mine/get_plot_parts", "autoPlace", "string", "Place", " items", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "s - jumping the queue", "Holders", "\n", "MineUpgradeCharacter", "enabled", "research", "Badge", "hookfunction", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/i18n", "autoUpgradeMutationTarget", "[MekPro] settings file: ", "core/logger", "ลอย (Float)", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "[Decompile] wrote ", "works from anywhere - dropping the teleport", "", "[AutoUpgrade] ", "lowest", "core/logger", "[MekPro] build ", "[AutoPlace] slot ", "%$%s*([%d,%.]+)%s*(%a*)", "works from anywhere - no teleport needed", "games/my_anime_mine/get_current_plot", "(none)", "games/my_anime_mine/get_best_miner", "Players", " (power ", "MineSpeed", "BasePart", "Players", "[^%.]+", "TextLabel", "Zones", "[AutoUpgrade] MineBuyPickaxe ", "float", "core/logger", "RunService", "Auto Upgrade: Pickaxe ที่จะซื้อ", "core/run_job", " error: ", "Auto Mine: ความเร็ว", "UserInputService", "core/debug_decompile", " (", "Auto Upgrade Unit", "=", "x", "Plots", "[AutoSell] disabled", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "GetCharacterSpeedUpgradeCost", "getscriptbytecode", "Workspace", "button has no size on screen", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "ค้นหาฟังก์ชัน...", "Auto Chest", "Humanoid", "[AutoChest] waiting: ", "Damage", " ", "fly", "Damage", "Loot", "ScrollingFrame", "Auto Upgrade: กันเงินไว้", "s", "games/my_anime_mine/game_api", "[AntiAFK] disabled", " not maxed", "PlayerScripts", "decompile", "[Interaction] Button activated: ", "menuScale", "core/parse_money", "games/my_anime_mine/auto_place", "MineWorld", "s", "core/get_me", "GuiService", "core/logger", "%.0f", " (", "disabled", "auto", "[AutoClaim] ", "core/anti_afk", " node(s), ", " (money ", " failed)", "BasePart", "[AutoPlot] ", "[GameAPI] couldn't load the game's modules: ", ")", "ReplicatedStorage", "games/my_anime_mine/main", " ("}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(516)] = function()
return function(import)
local log = import(S(369))
local Players = game:GetService(S(246))
local VirtualUser = game:GetService(S(407))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(683))
        connection = player.Idled:Connect(function()
            log(S(518))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(892))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(632)] = function()
return { Env = S(686), IsDev = true, Commit = S(761), BuiltAt = S(738) }
end
modules[S(463)] = function()
local VirtualInputManager = game:GetService(S(552))
local GuiService = game:GetService(S(903))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(877)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(418)] = function()
return function(import)
local HttpService = game:GetService(S(551))
local getPlaceId = import(S(90))
local log = import(S(319))
local ROOT_FOLDER = S(733)
local placeFolder = ROOT_FOLDER .. S(635) .. tostring(getPlaceId())
local filePath = placeFolder .. S(126)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(663) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(147) .. filePath .. S(230) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(579) .. filePath .. S(563) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(866)] = function()
return function(import)
local Players = game:GetService(S(315))
local log = import(S(257))
local CAPABILITIES = {
    S(895), S(875), S(79), S(448), S(459),
    S(267), S(168), S(661), S(602), S(589),
    S(59), S(152), S(828), S(526),
}
local OUT_FOLDER = S(266)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(854)) do
        if type(target) ~= S(705) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(92))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(263) .. name .. S(730) .. ((ok and type(value) == S(401)) and S(613) or S(190)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(490))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(894))
    if not playerScripts then
        log(S(573))
        return 0
    end
    pcall(makefolder, S(692))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(361)) or target:IsA(S(384)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(817) and #source > 0 then
                local name = target:GetFullName():gsub(S(422), S(755))
                if pcall(writefile, OUT_FOLDER .. S(643) .. name .. S(806), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(836) .. written .. S(524) .. OUT_FOLDER
        .. S(472) .. failed .. S(913))
    return written
end
local function run()
    log(S(545))
    reportCapabilities()
    local count = dumpAll()
    log(S(519))
    return count
end
return run
end
end
modules[S(724)] = function()
return function(import)
local Players = game:GetService(S(652))
local log = import(S(453))
local function dumpVisible(root, indent)
    indent = indent or S(164)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(165)) and child.Visible then
            local line = indent .. child.ClassName .. S(341) .. child.Name .. S(331)
            if child:IsA(S(148)) or child:IsA(S(328)) then
                line = line .. S(641) .. tostring(child.Text) .. S(794)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(213) .. attrName .. S(241) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(150))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(26))
    if not playerGui then
        log(S(260))
        return 0
    end
    local screens = 0
    log(S(483))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(274)) and screen.Enabled and screen.Name ~= S(132) then
            screens = screens + 1
            log(S(508) .. screen.ClassName .. S(464) .. screen.Name .. S(759))
            dumpVisible(screen, S(612))
        end
    end
    log(S(243) .. screens .. S(144))
    return screens
end
return dumpGui
end
end
modules[S(723)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(40))
local log = import(S(841))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(392)) or descendant:IsA(S(553)) then
            count = count + 1
            log(S(657) .. descendant.ClassName .. S(44) .. descendant:GetFullName())
        end
    end
    log(S(255) .. count .. S(72))
    return count
end
return dumpRemotes
end
end
modules[S(400)] = function()
return function(import)
local Players = game:GetService(S(646))
local Workspace = game:GetService(S(653))
local UserInputService = game:GetService(S(865))
local log = import(S(403))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(105) .. describe(prompt)
            .. S(782) .. prompt.ActionText .. S(647))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(39) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(896) .. describe(button)
            .. S(675) .. tostring(button.Text) .. S(528))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(674)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(539)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(583)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(783))
        return
    end
    connections = {}
    log(S(110))
    local playerGui = Players.LocalPlayer:WaitForChild(S(742))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(133)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(176)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(702)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(468),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(438)] = function()
return function(import)
local interactionLogger = import(S(682))(import)
local dumpRemotes = import(S(610))(import)
local dumpGui = import(S(584))(import)
local decompileScripts = import(S(84))(import)
local humanoid = import(S(269))
local function build(window, persist)
    local tab = window:AddTab(S(714), S(457))
    persist.Toggle(
        tab:AddCard(
            S(362),
            S(696)
        ),
        S(426),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(234),
        S(404)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(628), function()
        remoteBtn.Text = dumpRemotes() .. S(292)
    end)
    local guiCard = tab:AddCard(
        S(24),
        S(332)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(509), function()
        guiBtn.Text = dumpGui() .. S(45)
    end)
    local decompileCard = tab:AddCard(
        S(555),
        S(623)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(120), function()
        decompileBtn.Text = decompileScripts() .. S(789)
    end)
    humanoid.Start()
    persist.Slider(tab, S(415), S(767),
        8, 200, S(353), 16, humanoid.SetWalkSpeed, S(247))
    persist.Slider(tab, S(564), S(439),
        30, 300, S(19), 50, humanoid.SetJumpPower, S(282))
    persist.Toggle(
        tab:AddCard(S(695), S(820)),
        S(885), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(677), S(253)),
        S(527), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(834), S(276)),
        S(858), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(349)] = function()
return function(import)
    local getPlaceId = import(S(799))
    local registry = import(S(575))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(325))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(488)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(581))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(902)] = function()
local Players = game:GetService(S(739))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(172))
    local backpack = player:WaitForChild(S(678))
    return player, humanoid, backpack
end
return getMe
end
modules[S(81)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(622)] = function()
local Players = game:GetService(S(21))
local RunService = game:GetService(S(860))
local UserInputService = game:GetService(S(68))
local Workspace = game:GetService(S(876))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(881))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(666))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(813))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(852)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(10)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(529))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(830)] = function()
local Players = game:GetService(S(655))
local STRINGS = {
    en = {
        search_placeholder = S(413),
        open_menu_hint = S(763),
    },
    th = {
        search_placeholder = S(879),
        open_menu_hint = S(531),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(807)
    if locale:sub(1, 2):lower() == S(743) then
        return S(167)
    end
    return S(85)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(27) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(561)] = function()
local LOG_FILE = S(649)
local function log(message)
    local line = os.date(S(156)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(227))
            end
            appendfile(LOG_FILE, line .. S(709))
        elseif writefile then
            local existing = S(313)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(823))
        else
            error(S(351))
        end
        if not (isfile and readfile) then
            error(S(154))
        end
        if not isfile(LOG_FILE) then
            error(S(192) .. LOG_FILE .. S(104))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(311) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(491) .. LOG_FILE .. S(676) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(566)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(9) then
        return nil
    end
    local number, suffix = text:match(S(844))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(582), S(303))))
    if not value then
        return nil
    end
    if suffix ~= S(679) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(397)] = function()
return function(import)
local log = import(S(310))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(654) .. name .. S(750) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(61)] = function()
return function(import)
local RunService = game:GetService(S(306))
local log = import(S(904))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(786) .. task_.name .. S(863) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(741) .. task_.name .. S(301)
                    .. string.format(S(905), now - task_.busySince)
                    .. S(497))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(597) .. starved.name .. S(396)
            .. string.format(S(327), now - starved.waitingSince)
            .. S(821))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(671) .. (currentTask and currentTask.name or S(202)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(293)] = function()
local Players = game:GetService(S(15))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(548))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(621)] = function()
return function(import)
local i18n = import(S(232))
local UserInputService = game:GetService(S(425))
local THEME = {
    BrandName = S(173),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(146))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(615))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(358))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(477))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(805)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(796))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(672))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(131)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(562))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(698)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(888))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(118))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(792))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(614))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(344))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(225))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(130))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(76))
            if not valueLbl then
                valueLbl = Instance.new(S(41))
                valueLbl.Name = S(22)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(191))
            if not badge then
                badge = Instance.new(S(411))
                badge.Name = S(827)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(174))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(428))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(731))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(286))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(13)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(348)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(665))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(409))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(454))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(226)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(149))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(532))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(752))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(273)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(466))
    screenGui.Name = S(324)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(280))
    local main = Instance.new(S(34))
    main.Name = S(333)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(567))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(66))
    tabBar.Name = S(299)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(11))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(745))
    header.Name = S(380)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(395))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(811))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(242))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(627)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(814))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(804)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(305))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(507)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(364))
    searchBox.Name = S(187)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(316))
    searchBox.Text = S(382)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(20))
    content.Name = S(470)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(387))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(434))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(469))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(855))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(207)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(87) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(650) .. (item.Subtitle or S(137))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(251))
        footerLabel.Text = i18n.T(S(215))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(713)] = function()
return function(import)
local gameApi = import(S(569))(import)
local log = import(S(416))
local runGuarded = import(S(64))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(720) .. minItemsToCollect .. S(91))
    else
        log(S(287))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(360) then
            log(S(568))
            lastReported = S(370)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(116) then
            log(S(42))
            lastReported = S(546)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(481) then
            local capacity = gameApi.ChestCapacity()
            log(S(882) .. count .. S(181) .. want .. S(819)
                .. (capacity and (S(138) .. capacity .. S(917)) or S(106)))
            lastReported = S(188)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(687), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(693) .. count .. S(161) .. after .. S(36))
        else
            log(S(49) .. count
                .. S(618) .. tostring(after) .. S(447))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(740)] = function()
return function(import)
local gameApi = import(S(100))(import)
local log = import(S(354))
local runGuarded = import(S(810))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(909) .. (enabled and S(35) or S(907)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(544) .. reward.category .. S(297)
                .. tostring(reward.tierKey) .. S(166) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(523) .. #pending .. S(541))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(262), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(143)] = function()
return function(import)
local getCurrentPlot = import(S(458))
local getPlotParts = import(S(815))
local log = import(S(486))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(291), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(506) .. (enabled and S(6) or S(446)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(640)] = function()
return function(import)
local Players = game:GetService(S(788))
local getCurrentPlot = import(S(704))
local getPlotParts = import(S(98))
local getMinerSlots = import(S(4))(import)
local teleport = import(S(189))
local gameApi = import(S(498))(import)
local log = import(S(259))
local runGuarded = import(S(159))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(180)
local LOCKED_SLOT_TEXT = S(229)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(376))
    return pad and pad:FindFirstChildWhichIsA(S(322), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(492)) then
            local digits = descendant.Text:match(S(386))
            if digits then
                return tonumber((digits:gsub(S(388), S(537))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(530)) and descendant.Text:match(S(433)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(778) .. holder.Name .. S(371)
            .. gameApi.MinerSlots() .. S(718) .. gameApi.MaxMinerSlots() .. S(620)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(517)
    end
    if not prompt.Enabled then
        return true, S(495)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(624) .. price
    end
    local action = prompt.ActionText or S(285)
    if action:match(S(204)) or action:match(S(265)) or action:match(S(378)) then
        return true, S(51) .. action .. S(651)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(62))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(25) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(194) .. why .. S(538))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(200))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(869) .. prompt.ObjectText
                    .. S(155) .. shown .. S(758) .. math.floor(base) .. S(421))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(785))
    if line ~= lastSurvey then
        log(S(201) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(760), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(798), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(29) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(445))
    else
        lastAttempt = 0
        log(S(633))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(601),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(818), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(625))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(339) .. holder.Name .. S(88))
            return false
        end
        log(S(843) .. candidate.slot .. S(588) .. candidate.name
            .. S(50))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(667) .. holder.Name .. S(177) .. miner.name)
        return false
    end
    log(S(688) .. miner.name .. S(850) .. miner.power
        .. S(776) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(197) .. entry.name .. S(784) .. entry.holder.Name
            .. S(801) .. tostring(entry.prompt.ActionText) .. S(727))
        return false
    end
    log(S(290) .. entry.name .. S(183) .. entry.power
        .. S(314) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(525))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(412) then
        runGuarded(S(136), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(317) .. target.holder.Name .. S(722) .. target.name
        .. S(867) .. target.power .. S(475) .. best.name .. S(405) .. best.power .. S(16))
    runGuarded(S(609), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(596)] = function()
return function(import)
local gameApi = import(S(521))(import)
local parseMoney = import(S(898))
local log = import(S(644))
local runGuarded = import(S(1))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(717) .. (enabled and S(797) or S(808)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(89)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(95) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(279) .. deepestKey .. S(37))
        return true
    end
    log(S(501) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(915) .. gameApi.ZoneDisplayName(zoneKey) .. S(511)
                .. math.floor(price) .. S(219) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(617) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(906) .. zoneKey .. S(503))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(534) then
            reportedLocked = S(826)
            log(S(340) .. zoneKey .. S(456)
                .. gameApi.ZoneDisplayName(currentKey) .. S(768)
                .. done .. S(236) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(373), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(391)] = function()
return function(import)
local gameApi = import(S(803))(import)
local log = import(S(198))
local runGuarded = import(S(735))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(205) .. (enabled and S(825) or S(690)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(195) .. node.zoneKey .. S(558) .. node.id
                    .. S(920) .. node.family .. S(214) .. node.level .. S(175) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(452) .. node.id
                    .. S(557) .. node.family .. S(101) .. node.level .. S(608) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(780) .. blocked
                .. S(113))
        elseif powerBlocked then
            log(S(585) .. powerBlocked.zoneKey .. S(884) .. powerBlocked.id
                .. S(473) .. powerBlocked.family .. S(238)
                .. math.floor(powerBlocked.powerReq) .. S(648)
                .. math.floor(powerBlocked.power)
                .. S(587))
        else
            log(S(681)
                .. S(912) .. math.floor(gameApi.GetMoney()) .. S(240))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(32) .. nearest.zoneKey
                    .. S(510) .. nearest.id .. S(494) .. nearest.family .. S(323)
                    .. nearest.level .. S(258) .. nearest.max
                    .. S(474) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(298) .. math.floor(money)
        .. S(248) .. math.floor(budget)
        .. S(703) .. (#zones > 0 and table.concat(zones, S(536)) or S(847))
        .. S(55) .. tostring(buyZones) .. S(163) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(374) .. zoneKey .. S(196) .. tostring(node.id)
                        .. S(272) .. tostring(node.family) .. S(766)
                        .. node.level .. S(502) .. node.max
                        .. S(307) .. tostring(node.open)
                        .. S(571) .. tostring(node.cost)
                        .. S(125) .. tostring(node.powerOk)
                        .. S(482) .. tostring(node.powerReq)
                        .. S(514) .. tostring(node.power))
                end
            end
        end
        log(S(449) .. zoneKey .. S(790) .. #nodes
            .. S(911) .. pending .. S(893))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(465) .. #coreNodes .. S(701)
        .. coreOpen .. S(716))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(712), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(394)] = function()
return function(import)
local Workspace = game:GetService(S(706))
local gameApi = import(S(281))(import)
local teleport = import(S(320))
local log = import(S(97))
local runGuarded = import(S(111))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(872))
    else
        lastAttempt = 0
        log(S(408))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(765))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(576), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(809)
                .. (sold and S(837)
                         or S(549)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(795) .. (sold and (S(427) .. #keys .. S(578)) or S(770)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(773), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(330)] = function()
return function(import)
local Workspace = game:GetService(S(424))
local gameApi = import(S(139))(import)
local teleport = import(S(222))
local log = import(S(605))
local runGuarded = import(S(862))(import)
local STATS = { S(211), S(753), S(711) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(109)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(839) .. (enabled and S(626) or S(220)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(435) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(840) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(560) or S(347)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(461) .. holderKey .. S(487) .. choice.stat .. S(31)
                        .. choice.info.level .. S(748) .. after.level
                        .. S(270) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(304) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(228))
    local shop = stand and stand:FindFirstChild(S(283))
    if not shop then
        return nil
    end
    if shop:IsA(S(729)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(914), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(857)
                .. (bought and S(845)
                           or S(60)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(268))
        end
    end
    if bought then
        log(S(670) .. pickaxe.index .. S(680) .. pickaxe.name
            .. S(326) .. pickaxe.multiplier .. S(590))
        return true
    end
    log(S(352) .. pickaxe.index .. S(224) .. pickaxe.name
        .. S(616))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(728) .. holderKey .. S(645)
                .. tostring(entry and entry.Pickaxe or S(642)) .. S(151) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(284) .. holderKey .. S(264) .. variant
                .. S(43) .. after .. S(69) .. cost .. S(223))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(103) .. gameApi.OwnedPickaxeCount()
                    .. S(754) .. gameApi.MinerSlots()
                    .. S(732))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(74))
        elseif maxed > 0 then
            log(S(377))
        else
            log(S(835))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(812) and entry.Pickaxe and entry.Pickaxe ~= S(639) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(777), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(891)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(918))
local Workspace = game:GetService(S(93))
local log = import(S(726))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(309), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(916) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(83))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(499) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(484) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(619) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(5) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(75) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(312))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(762) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(547))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(764), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(455) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(419) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(65) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(779), keys)
end
function api.EquipSlot(key)
    return send(S(336), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(629), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(296), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(128), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(802), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(277)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(94)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(271))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(338) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(559) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(441), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(417))
    plots = plots and plots:FindFirstChild(S(747))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(853)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(775)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(124))
    local gui = board and board:FindFirstChild(S(294))
    local root = gui and gui:FindFirstChild(S(691))
    local list = root and root:FindFirstChild(S(440))
    local row = list and list:FindFirstChild(S(375) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(513)) and descendant.Text ~= S(254) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(607) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(824), characterKey, stat == S(141) and S(637) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(592), max = S(410), cost = S(185) },
    Speed = { level = S(8), max = S(210), cost = S(874) },
    MineSpeed = { level = S(119), max = S(522), cost = S(598) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(485) or charName == S(838) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(606) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(493) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(231) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(171) and type(holder.CharName) == S(2) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(206) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(157) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(848)] = function()
return function(import)
local Players = game:GetService(S(182))
local gameApi = import(S(757))(import)
local log = import(S(833))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(431))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(462), S(556)))) or 0
    if suffix ~= S(604) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(12))
    local mineGui = playerGui and playerGui:FindFirstChild(S(275))
    local hotbar = mineGui and mineGui:FindFirstChild(S(170))
    local content = hotbar and hotbar:FindFirstChild(S(142))
    return content and content:FindFirstChild(S(153))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(577)) then
            local count = descendant.Text:match(S(684))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(217))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(721))
    local nameLabel = content and content:FindFirstChild(S(54))
    local damageLabel = content and content:FindFirstChild(S(886))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(56) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(233) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(381) .. slot .. S(256) .. info.name
                        .. S(489) .. info.power .. S(398) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(846)] = function()
local Players = game:GetService(S(849))
local Workspace = game:GetService(S(46))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(900))
    local plots = mineWorld and mineWorld:FindFirstChild(S(871))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(771)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(496)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(822)),
        Nodes = plot:FindFirstChild(S(252)),
        Chest = plot:FindFirstChild(S(245)),
        Zones = plot:FindFirstChild(S(856)),
        Crew = plot:FindFirstChild(S(302)),
        Loot = plot:FindFirstChild(S(887)),
        PlotIndex = plot:GetAttribute(S(235)),
        ChestCapacity = plot:GetAttribute(S(593)),
    }
end
return getPlotParts
end
modules[S(919)] = function()
return function(import)
    local createWindow = import(S(289))(import)
    local i18n = import(S(736))
    local antiAfk = import(S(910))(import)
    local buildEnv = import(S(193))
    local scheduler = import(S(345))(import)
    local config = import(S(594))(import)
    local log = import(S(859))
    log(S(842) .. buildEnv.Commit .. S(346) .. buildEnv.Env
        .. S(787) .. buildEnv.BuiltAt .. S(383))
    local window = createWindow({
        Subtitle = S(774),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(793))(import)
    local autoPlace = import(S(899))(import)
    local autoChest = import(S(570))(import)
    local autoSell = import(S(444))(import)
    local autoUpgrade = import(S(611))(import)
    local autoResearch = import(S(221))(import)
    local autoClaim = import(S(664))(import)
    local autoPlot = import(S(17))(import)
    local gameApi = import(S(505))(import)
    scheduler.RegisterStandalone(S(335), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(123), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(707), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(586), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(117), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(700), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(160), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(734), 20)
    scheduler.RegisterExclusive(S(656), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(725), S(208))
    persistentToggle(farmTab:AddCard(
        S(443),
        S(158)
    ), S(565), autoMine.SetEnabled)
    persistentSlider(farmTab, S(864), S(366),
        1, 30, S(363), 5, autoMine.SetCyclesPerSecond, S(599))
    persistentSlider(farmTab, S(278), S(689),
        1, 10, S(694), 1, autoMine.SetClicksPerCycle, S(450))
    persistentToggle(farmTab:AddCard(
        S(550),
        S(744)
    ), S(816), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(67), S(178),
        1, 60, S(399), 3, autoPlace.SetCheckInterval, S(890))
    persistentToggle(
        farmTab:AddCard(S(756),
            S(673)),
        S(402), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(880),
        S(478)
    ), S(237), autoChest.SetEnabled)
    persistentSlider(farmTab, S(3), S(479),
        50, 3000, S(390), 100, autoChest.SetMinItems, S(112))
    persistentSlider(farmTab, S(746), S(600),
        1, 60, S(659), 3, autoChest.SetCheckInterval, S(749))
    persistentToggle(farmTab:AddCard(
        S(710),
        S(512)
    ), S(28), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(636),
            S(708)),
        S(63), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(737), S(365),
        5, 300, S(129), 20, autoSell.SetCheckInterval, S(48))
    farmTab:AddSlider(S(504),
        S(140),
        5, 120, sellMaxWait, function(value)
            config.Set(S(357), value)
        end, S(14))
    persistentToggle(farmTab:AddCard(
        S(868),
        S(420)
    ), S(367), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(662), S(78),
        5, 300, S(368), 20, autoUpgrade.SetInterval, S(476))
    persistentSlider(farmTab, S(342), S(385),
        1, 20, S(535), 5, autoUpgrade.SetMaxPerPass, S(870))
    persistentSegmented(
        farmTab,
        { S(169), S(883), S(432), S(372) },
        { S(18), S(697), S(7), S(851) },
        S(436), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(889), S(540),
        0, 500, S(70), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(107))
    persistentToggle(
        farmTab:AddCard(S(669),
            S(829)),
        S(23), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(406) .. pickaxe.multiplier .. S(73)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(873)
            end
            return S(634) .. index .. S(121) .. (pickaxeLabels[index] or S(308))
        end
        local savedPick = config.Get(S(249), 0)
        pickaxeCard = farmTab:AddSlider(
            S(861),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(218), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(520)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(595),
            S(52)),
        S(99), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(791),
            S(288)),
        S(102), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(329),
            S(80)),
        S(261), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(186), S(135),
        1, 20, S(831), 2, autoUpgrade.SetMutationTarget, S(542))
    persistentSlider(farmTab, S(467), S(212),
        0, 500, S(114), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(355))
    persistentToggle(farmTab:AddCard(
        S(480),
        S(451)
    ), S(318), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(350), S(162),
        10, 600, S(751), 30, autoResearch.SetCheckInterval, S(901))
    persistentSlider(farmTab, S(57), S(471),
        1, 20, S(554), 5, autoResearch.SetMaxPerPass, S(295))
    persistentSlider(farmTab, S(321), S(572),
        0, 500, S(337), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(343))
    persistentToggle(
        farmTab:AddCard(S(334),
            S(145)),
        S(115), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(668),
            S(134)),
        S(58), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(660),
            S(638)),
        S(359), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(699), S(38) },
        { true, false },
        S(122), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(179),
        S(800)
    ), S(184), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(533), S(356),
        5, 300, S(631), 30, autoClaim.SetCheckInterval, S(244))
    persistentSlider(farmTab, S(574), S(878),
        1, 30, S(216), 10, autoClaim.SetMaxPerPass, S(591))
    persistentToggle(farmTab:AddCard(
        S(250),
        S(630)
    ), S(772), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(437), S(53),
        5, 300, S(71), 30, autoPlot.SetCheckInterval, S(47))
    persistentToggle(
        farmTab:AddCard(S(580),
            S(389)),
        S(127), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(781),
            S(685)),
        S(769), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(429), S(209),
        0, 500, S(515), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(379))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(82))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(658), S(203))
    persistentToggle(
        settingsTab:AddCard(S(543), S(239)),
        S(300),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(199), 100)
    settingsTab:AddSlider(S(86), S(77), 50, 150, savedScale, function(value)
        config.Set(S(897), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(414), 1)
    settingsTab:AddSegmented({ S(30), S(719), S(715) }, savedLanguage, function(index, value)
        local code = ({ S(908), S(108), S(430) })[index]
        config.Set(S(33), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(442), S(393), S(603) })[savedLanguage] or S(423))
    log(S(832) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(460)] = function()
return {
    [79389059854988] = S(96),
}
end
import(S(500))(import)
