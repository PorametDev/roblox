-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"\n", " items", " cards left, label = '", "Frame", "[^%d%.]", "$", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "GuiService", "Place", "ScreenGui", "Canvas", "Nodes", " ", "core/logger", " items)", "Inspect", "Badge", "?", "ValueBase", "ProximityPrompt", "button has no size on screen", ": ", ",", " parts", "Frame", " while selecting", "", "games/my_anime_mine/auto_research", "en", "", "Content", "Players", "TextButton", ")", "", "search_placeholder", "costs ", "MekPro", "PlayerGui", "autoChestInterval", "Container", "en-us", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[Interaction] logger enabled - go click/interact with things now", "Loot", "[AutoChest] ", " (", "core/logger", "[ChestDump] plot has no Chest", "[Interaction] Key pressed: %s (gameProcessed=%s)", " unit card(s) Select All skipped", "Backpack", "[AutoPlace] picked up ", "BottomPanels", "[MyAnimeMine] === Dump start ===", "Workspace", "PlayerGui", "disabled", "autoChestMinItems", "HotbarFrame", "[AutoPlace] placed ", "Players", "MineGui", "⚙", "Mining", "/", " '", "Debug: Dump Sell Cards", "Done", "AutoResearch", "[ChestDump] === plot ", "games/my_anime_mine/debug_dump_gui", "[AutoResearch] bought ", " cards)", "Auto Research: รอบเช็ค", "[AutoSell] clicked SellButton", "[AutoPlace] couldn't pick up ", "TextButton", "games/my_anime_mine/auto_upgrade", "Players", "  ", "')", "games/my_anime_mine/get_plot_parts", "Players", "core/dispatch", "  (debug.getconstants unavailable)", "TextLabel", "Unit Damage UpgradeButton", "[ChestDump] === ReplicatedStorage.", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Actions", "[AutoUpgrade] upgrade screen didn't open for ", "[Handlers] ", "", "TextLabel", "/config.json", "  ", "SellFrame", "Error: ", "core/dispatch", "%$", "ReplicatedStorage", " remotes", "^%$", "UIListLayout", "[AutoSell] nothing selectable (", "core/click", "  [attr] ", "Lock", "Tree", "[GuiDump] === end ===", "PlayerScripts", " power ", "RemoteFunction", "Crew", "ReplicatedStorage", "", "open_menu_hint", "StatsPanel", "Label", "core/click", "RemoteFunction", "  [upvalue] ", ": ", ",", "MineGui", "neither appendfile nor writefile is available", "[Interaction] ClickDetector clicked: ", "[AutoMine] ", "Auto Mine: คลิกต่อรอบ", "Players", " (", " (power ", " ===", "", "number", ") on ", "SelectAllButton", "core/get_me", "games/my_anime_mine/get_plot_parts", " chars) -> ", "getconnections", "[Bb]uy", "[MyAnimeMine] no nodes present right now", "OwnerUserId", "BuyButton", "Max", " error: ", "write reported success but the line isn't in ", "Damage", " ", " = ", "[Handlers] === end ===", "Frame", "[AutoSell] also selected ", "TextButton", "", "getcallbackvalue", "Chest TakeButton", "ExitButton", "TextBox", "core/logger", "-", "🏠", "core/logger", "core/logger", "core/scheduler", ": ", "waiting", "TabBar", "autoMine", "Label", "core/interaction_logger", "VirtualUser", "games/my_anime_mine/debug_dump", "open_menu_hint", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "games/my_anime_mine/debug_dump_chest", "games/my_anime_mine/get_current_plot", "autoResearch", ") didn't take, trying the next one", "boolean", "Players", "(nothing to do)", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoPlace] disabled", "table", "locked slot", "Content", "games/my_anime_mine/auto_chest", "[MyAnimeMine] === Tool: ", "x", "%", "[AutoChest] fireproximityprompt error: ", "BuyButton", "RunService", "TextButton", "[Handlers]   ", "core/config", "MineHUDClient", ": ", "[Handlers]   #", "Content", "games/my_anime_mine/auto_upgrade", "Main", "[AutoPlace] fireproximityprompt error: ", "Card", "no isfile/readfile available to verify the write", "core/get_place_id", "Check", "not found", "  (debug.getupvalues unavailable)", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "games/my_anime_mine/get_current_plot", ")", "core/anti_afk", "prompt disabled", ")", "ValueBase", "'", "[AutoChest] Take All had NO effect (", "Scroll", "Sell SelectAllButton", "[AutoPlace] ", "Done", "UICorner", " on ", "Tool", "core/logger", "PlayerGui", "TextButton", "[AutoResearch] upgrades menu didn't open", "disabled", "Content", "GuiButton", "เปิดหน้าอัปเกรดของ miner ที่วางอยู่ แล้วซื้อ Damage/Speed/Mining ที่ราคาไม่เกินงบ", "VirtualInputManager", "Frame", "CoreUI", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "'), backing off ", "debug.getproto", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "(", " from ", "Dump", "[AutoResearch] ", "'", "MekPro", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "core/anti_afk", "[Uu]nlock", "autoSell", "hookfunction", "core/logger", "TakeButton", "s", "ProximityPrompt", "ExitButton", "games/my_anime_mine/debug_dump_sell_cards", "ProximityPrompt", "table", "[Handlers] === ", "core/i18n", "TextLabel", "$", "Content", "This is not a supported place.", "StatsPanel", "fill", "core/ui", "autoMineSpeed", "\n", "MineGame", "^Node_", "autoResearchInterval", "core/scheduler", "GuiObject", "  [attr] ", "[Interaction] ProximityPrompt triggered: ", "Players", "Backpack", "games/my_anime_mine/debug_inspect_handlers", "", "Frame", "Auto Sell", "[GuiDump] MineGui not found", "Detail", "UI", "Auto Upgrade: รอบเช็ค", "Players", "[AntiAFK] disabled", "autoUpgradeInterval", "games/my_anime_mine/get_plot_parts", "[Probe] === end ===", "s", "interactionLogger", "MineWorld", "", "core/logger", "", "[AutoResearch] no BuyButton after selecting ", "core/logger", "core/parse_money", " ===", "Card", "games/my_anime_mine/get_plot_parts", "[AutoUpgrade] ", "SellButton", "PlayerGui", "[Logger] failed to write ", "TextLabel", "Auto Mine: ความเร็ว", ")", " (", ")", "Frame", "^([%d,]+)$", "core/logger", ") -> ", "Content", "MouseButton1Click", "core/parse_money", "[AutoSell] sell menu didn't open, aborting", "", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "PlayerGui", "[Probe] PlayerScripts.UI.MineGame not found", "", "Auto Place: รอบเช็ค", "[Interaction] Button activated: ", "Content", "ClickDetector", "getscriptbytecode", " <-- remote, likely fired by the real interaction", "unreadable", "games/registry", "TextLabel", "[Scheduler] ", "games/my_anime_mine/get_best_miner", "[AutoUpgrade] bought ", "ValueLabel", "[AutoSell] click failed on ", "Auto Mine", "core/get_place_id", "TextButton", "autoUpgrade", "Done", "Progress", ",", "SellStand", " doesn't exist", "leaderstats", "", " still empty after placing ", "languageIndex", "UpgradeButton", "th", "[AutoSell] nothing to sell, closing", " items", "%$%s*([%d,%.]+)%s*(%a*)", "auto", "menuScale", "'", "TextLabel", "disabled", "[AutoSell] disabled", "AutoPlace", "Open menu", "RemoteEvent", "ขนาดเมนู", "core/teleport", "enabled", "[MekPro] settings file: ", "[AutoPlace] ", "games/my_anime_mine/debug_dump", "Humanoid", "no tool", "/s", "Instance", "core/logger", "[MyAnimeMine] no holders found", "[ChestDump] === Player subtree ===", " = ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " not found", "[Probe] ", "Players", "TextButton", "[AntiAFK] enabled - watching Player.Idled", "SkillTreeFullFrame", ")", "core/click", "UpgradeButton", "games/my_anime_mine/get_current_plot", "Debug: Dump Backpack Tool", "games/my_anime_mine/main", "Sell for", "Frame", "Content", "Probe", "Auto Upgrade Unit", "Scroll", "core/click", "TextLabel", "[AutoSell] fireproximityprompt error: ", "SelectAllButton", "Debug: Probe Executor + Scripts", "Close", "getsenv", "table", "defined at ", ") from ", " (Text=", "[AutoChest] disabled", "Done", "Holders", ":", "s", "PlotIndex", "Select All", "[AutoPlace] drifted out of range of ", "/", " '", "", "games/my_anime_mine/get_current_plot", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "[MyAnimeMine] not standing in any plot", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", "เปิดหน้า Upgrades แล้วซื้อ node ที่ปลดได้ในทุกโซน เรียงจากถูกไปแพง", "[ChestDump] not standing in any plot", "TextLabel", "Zones", "games/my_anime_mine/debug_inspect_handlers", "[AutoChest] enabled (collects once >= ", "Done", "waiting", "[Probe] ", " (prompt says '", "games/my_anime_mine/get_plot_parts", "enabled", "games/my_anime_mine/auto_mine", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Debug: Current Plot", "อ่านป้ายบน chest ทุกกี่วินาที", "Close", "'", "Players", "MineGui", "", "TextLabel", "MineHotbarClient", "core/teleport", "games/registry", "[Probe] === executor capabilities ===", "getnilinstances", "Plots", "Damage", "RemoteFunction", "autoMineClicks", "=empty", " (Text=", "auto", "core/click", "Frame", " = ", "core/logger", "Content", "Locked miner slot", "Sidebar", "Debug: Dump Node/Holder", "search_placeholder", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "Players", "core/config", " (", "getgc", "function", "X", "MineGui", "upgrade", "games/my_anime_mine/get_current_plot", " (MaxActivationDistance=%s)", "Research BuyButton", "Anti-AFK", "antiAfk", "table", "[AutoPlace] slot ", "[^%.]+", "games/my_anime_mine/debug_dump_chest", "Label", "MineGui", "[SellCards] Card #", "s", "TextButton", "[AutoChest] Take All worked: ", "Auto Research: งบต่อครั้ง", "[Remotes] ", " = ", "core/get_leaderstat", "GuiButton", "core/i18n", "Auto Chest: รอบเช็ค", " ", "LeftBar", "autoChest", "^(%d+)", "My Anime Mine · Mining Hub", "", "ItemFrame", "games/my_anime_mine/get_current_plot", "Cost", "'", "menuScale", "List", "Content", "  [const] ", "SkillTreeFullFrame", "games/my_anime_mine/debug_dump_gui", "PlayerGui", "[Probe] decompiled ", "TextLabel", "ChestFrame", "=", "table", "TextLabel", ") ===", "Players", "Frame", "UIStroke", "RemoteEvent", "[ChestDump] === end ===", "Card", "UIListLayout", "ValueLabel", "Frame", "SellFrame", "en", "AutoChest", "[MyAnimeMine] === Node: ", "ไม่ซื้อ node ที่แพงกว่านี้", "  ", "[ChestDump] === Chest subtree ===", "getrenv", " x", "Dump", "HumanoidRootPart", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "[AutoChest] click failed on ", "Content", "games/my_anime_mine/auto_place", "TextButton", "TextButton", "'", "core/click", "MineChestClient", ".lua", "debug.getupvalues", "Auto Sell: รอบขาย", " = ", "games/my_anime_mine/get_plot_parts", "core/logger", " cards", "core/parse_money", " = ", "autoUpgradeMaxSpend", "games/my_anime_mine/auto_mine", "Scroll", "Scroll", "write reported success but ", "core/i18n", "HumanoidRootPart", "[AutoSell] enabled (only sells when there's something in the scroll)", "[Probe] decompile unavailable - can't read the game's source this way", "Auto Place Miner", "ย่อขยายเมนูทั้งชุด", "ScrollingFrame", "BottomPanels", "Dump", "languageIndex", "MineSkillTreeClient", "th", ": ", "ProximityPrompt", "Players", "Auto Upgrade: งบต่อครั้ง", "[SellCards] === total cards: ", "auto", "[AutoSell] clicked SelectAllButton (", "MineGui", "games/my_anime_mine/auto_place", "Header", "Damage", ": ", " (power ", "Amount", "function", "ChestCapacity", "Pips", "Content", "[Config] couldn't read ", "Pad", ": ", "decompile", "ตามเครื่อง", "Activated", "[Probe] === start ===", "core/logger", " = ", "UpgradesButton", "Players", "sl", "UIScale", "[Remotes] === total: ", " | ", "SellButton", "ไทย", ": ", "", "[Handlers] ", "AutoSell", "games/my_anime_mine/debug_dump_sell_cards", "[AntiAFK] idle detected, simulating input", " '", "ค้นหาฟังก์ชัน...", "core/logger", "TextLabel", " ===", "[AutoResearch] nothing affordable under the spend cap", "", "[MyAnimeMine] === Holder: ", "th", "Content", "MekPro/decompiled_", "core/logger", "ProximityPrompt", "auto", " (ActionText=", "SelectOverlay", "UIPadding", "Sell for", "[AutoChest] waiting: ", "", "s", "games/my_anime_mine/auto_sell", "", "ClickDetector", "[AutoChest] chest didn't open, aborting", "unreadable", "[Scheduler] turn: ", "string", "games/my_anime_mine/auto_chest", " -> ", "UIListLayout", "no", "[AutoPlace] upgrading ", "", "[AutoPlace] nothing in the hotbar to place", "Search", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "SlotFocusFrame", "UICorner", "Scroll", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "s", "yes", "อังกฤษ", "Workspace", "games/my_anime_mine/debug_probe_env", "[GuiDump] === visible MineGui tree ===", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "core/ui", "  [attr] ", "log failed", "CoreGui", "Interaction Logger", "Content", "  ", ": not found", ": ", "Text", "[AutoChest] can't read the chest billboard, is the plot loaded?", " items)", "autoPlace", "fill", "", "Content", "[Handlers] getconnections isn't available in this executor", "games/my_anime_mine/get_current_plot", "MineSlotFocusClient", "TakeButton", "[Interaction] logger disabled", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "MineGui", "games/my_anime_mine/auto_research", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "  [attr] ", "=locked(", "Content", "core/teleport", ") for ", " ชิ้น", "autoSellInterval", "ChestFrame", " ===", "ดีบัก", "[BestMiner] slot ", "Chest", "RemoteEvent", "enabled", "Label", " (", "SellFrame", "table", "[Config] couldn't write ", "Dump", "Debug: Dump Chest Data", "^x(%d+)$", "TextLabel", " = ", "ตั้งค่า", "write failed", "th", "core/get_place_id", "Auto Chest", ")", "Folder", "en", "Debug: Dump Visible GUI", "core/teleport", "action '", " (", "", "core/logger", "ฟาร์ม", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Frame", "AutoUpgrade", "Container", ": ", "Players", "Debug: Inspect Button Handlers", "ItemName", "ClickDetector", "HttpService", "getloadedmodules", "autoPlaceInterval", "PlayerGui", "SellFrame", "TextButton", "core/logger", "autoResearchMaxSpend", "games/my_anime_mine/debug_probe_env", "Sell SellButton", "Content", " ===", " (Text=", "no plot", "[%X] ", "UpgradePart", "UserInputService", "ClickDetector", " items, going to collect", "nothing to place or upgrade", "games/my_anime_mine/debug_dump_remotes", "Text", "Actions", "Workspace", "  ", "TextLabel", "", "games/my_anime_mine/get_best_miner", "[AutoPlace] holders: ", "Badge", "MineSellClient", "ProximityPrompt", "games/my_anime_mine/auto_sell", "Dump", "🐛", " -> ", "Content", "[Probe] couldn't decompile ", "string", "UserInputService", "Debug: Dump All Remotes", "TextLabel", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "?", "MineGui", "GuiObject", " attributes ===", "Players", "core/logger", " '", "games/my_anime_mine/main", "function", "Close", "[Handlers] === start (open the relevant menus first) ===", "string", "Players", "เปิดหน้า research ทุกกี่วินาที", " ===", "TextButton", "debug.getconstants", "Empty miner slot", "Auto Chest: เกณฑ์เก็บ", "  ", "ไม่ซื้ออะไรที่แพงกว่านี้", "ปุ่มเปิดเมนู", "Players", "", " for ", "Instance", "Speed", "/", "PlayerGui", "SlotFocusFrame", "games/my_anime_mine/debug_dump_remotes", "games/my_anime_mine/get_plot_parts", "ClickDetector", "core/interaction_logger", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", " '", "TextLabel", "[attr] ", "MekPro.log", "Label", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "PlayerGui", "Auto Research (Plot)", "Search functions...", "getscripts", ": ", "Dump", "TextLabel", "Content", " connection(s)", "AutoMine", "Detail", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(216)] = function()
return function(import)
local log = import(S(793))
local Players = game:GetService(S(448))
local VirtualUser = game:GetService(S(174))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(390))
        connection = player.Idled:Connect(function()
            log(S(623))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(292))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(121)] = function()
local VirtualInputManager = game:GetService(S(237))
local GuiService = game:GetService(S(8))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(21)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(199)] = function()
return function(import)
local HttpService = game:GetService(S(745))
local getPlaceId = import(S(209))
local log = import(S(229))
local ROOT_FOLDER = S(38)
local placeFolder = ROOT_FOLDER .. S(66) .. tostring(getPlaceId())
local filePath = placeFolder .. S(96)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(487) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(601) .. filePath .. S(740) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(715) .. filePath .. S(201) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(85)] = function()
return function(import)
    local getPlaceId = import(S(724))
    local registry = import(S(337))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(268))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(500)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(353))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(139)] = function()
local Players = game:GetService(S(585))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(377))
    local backpack = player:WaitForChild(S(52))
    return player, humanoid, backpack
end
return getMe
end
modules[S(345)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(264)] = function()
local Players = game:GetService(S(611))
local STRINGS = {
    en = {
        search_placeholder = S(831),
        open_menu_hint = S(369),
    },
    th = {
        search_placeholder = S(625),
        open_menu_hint = S(809),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(42)
    if locale:sub(1, 2):lower() == S(723) then
        return S(358)
    end
    return S(538)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(637) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(821)] = function()
return function(import)
local Players = game:GetService(S(84))
local Workspace = game:GetService(S(668))
local UserInputService = game:GetService(S(761))
local log = import(S(14))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(280) .. describe(prompt)
            .. S(638) .. prompt.ActionText .. S(34))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(128) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(331) .. describe(button)
            .. S(757) .. tostring(button.Text) .. S(726))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(776)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(762)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(501)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(692))
        return
    end
    connections = {}
    log(S(44))
    local playerGui = Players.LocalPlayer:WaitForChild(S(520))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(20)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(744)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(235)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(50),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(162)] = function()
local LOG_FILE = S(826)
local function log(message)
    local line = os.date(S(759)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(425))
            end
            appendfile(LOG_FILE, line .. S(1))
        elseif writefile then
            local existing = S(325)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(273))
        else
            error(S(127))
        end
        if not (isfile and readfile) then
            error(S(208))
        end
        if not isfile(LOG_FILE) then
            error(S(570) .. LOG_FILE .. S(352))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(149) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(311) .. LOG_FILE .. S(22) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(564)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(783) then
        return nil
    end
    local number, suffix = text:match(S(361))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(125), S(686))))
    if not value then
        return nil
    end
    if suffix ~= S(643) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(167)] = function()
return function(import)
local RunService = game:GetService(S(196))
local log = import(S(48))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(339) .. task_.name .. S(148) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(650) .. (currentTask and currentTask.name or S(184)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(730)] = function()
local Players = game:GetService(S(62))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(572))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(271)] = function()
return function(import)
local i18n = import(S(571))
local UserInputService = game:GetService(S(784))
local THEME = {
    BrandName = S(249),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(662))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(226))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(640))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(389))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(94)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(530))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(365))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(788)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(312))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(509)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(577))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(534))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(338))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(399))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(87))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(719))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(522))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(342))
            if not valueLbl then
                valueLbl = Instance.new(S(265))
                valueLbl.Name = S(535)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(774))
            if not badge then
                badge = Instance.new(S(95))
                badge.Name = S(17)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(495))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(529))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(536))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(552))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(284)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(193)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(465))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(737))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(750))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(733)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(285))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(105))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(156))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(681)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(10))
    screenGui.Name = S(239)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(675))
    local main = Instance.new(S(154))
    main.Name = S(205)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(613))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(317))
    tabBar.Name = S(170)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(654))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(4))
    header.Name = S(592)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(346))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(526))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(786))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(157)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(803))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(479)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(78))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(163)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(161))
    searchBox.Name = S(659)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(36))
    searchBox.Text = S(35)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(25))
    content.Name = S(687)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(238))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(451))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(176))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(405))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(766)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(329) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(504) .. (item.Subtitle or S(811))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(472))
        footerLabel.Text = i18n.T(S(118))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(190)] = function()
return function(import)
local Players = game:GetService(S(810))
local getCurrentPlot = import(S(426))
local getPlotParts = import(S(83))
local teleport = import(S(453))
local click = import(S(393))
local log = import(S(635))
local checkInterval = 3
local minItemsToCollect = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(829))
    local mineGui = playerGui and playerGui:FindFirstChild(S(694))
    return mineGui and mineGui:FindFirstChild(S(704))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(677))
    local actions = content and content:FindFirstChild(S(91))
    return actions and actions:FindFirstChild(S(691))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(350), S(354)):match(S(507))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(596), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(550))
    local progress = content and content:FindFirstChild(S(349))
    local label = progress and progress:FindFirstChild(S(120))
    return parseCount(label and label.Text or S(301))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(549) .. button.Name .. S(833) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(258), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(415))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(435) .. minItemsToCollect .. S(683))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local cachedCount, cachedAt = nil, 0
function autoChest.HasWork()
    local now = tick()
    if now - cachedAt > 1 then
        cachedCount = getChestCount()
        cachedAt = now
    end
    return cachedCount ~= nil and cachedCount >= minItemsToCollect
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(194) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(648))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(496) .. count .. S(780) .. after .. S(360))
        else
            log(S(221) .. tostring(count)
                .. S(653) .. tostring(after) .. S(15))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(336) then
            log(S(682))
            lastReportedCount = S(649)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReportedCount ~= S(169) then
            log(S(642) .. count .. S(423) .. minItemsToCollect .. S(2))
            lastReportedCount = S(437)
        end
        return
    end
    lastReportedCount = nil
    log(S(46) .. count .. S(763))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(442)] = function()
return function(import)
local getCurrentPlot = import(S(689))
local getPlotParts = import(S(561))
local log = import(S(255))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(333), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(129) .. (enabled and S(441) or S(233)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(591)] = function()
return function(import)
local Players = game:GetService(S(800))
local getCurrentPlot = import(S(179))
local getPlotParts = import(S(819))
local getMinerSlots = import(S(772))(import)
local teleport = import(S(700))
local click = import(S(555))
local log = import(S(165))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(805)
local LOCKED_SLOT_TEXT = S(469)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(602))
    return pad and pad:FindFirstChildWhichIsA(S(584), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(432)) then
            local digits = descendant.Text:match(S(318))
            if digits then
                return tonumber((digits:gsub(S(23), S(646))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(824)) and descendant.Text:match(S(104)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(188)
    end
    if not prompt.Enabled then
        return true, S(217)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(37) .. price
    end
    local action = prompt.ActionText or S(630)
    if action:match(S(252)) or action:match(S(143)) or action:match(S(101)) then
        return true, S(731) .. action .. S(447)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(547))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(206) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(698) .. why .. S(392))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(461))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(524) .. prompt.ObjectText .. S(244) .. power .. S(215))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(615))
    if line ~= lastSurvey then
        log(S(773) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(685), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(481), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(224) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(186))
    else
        lastAttempt = 0
        log(S(243))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(671),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(9), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(658))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(422) .. holder.Name .. S(26))
            return false
        end
        log(S(488) .. candidate.slot .. S(315) .. candidate.name
            .. S(181))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(375) .. holder.Name .. S(355) .. miner.name)
        return false
    end
    log(S(61) .. miner.name .. S(133) .. miner.power
        .. S(137) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(77) .. entry.name .. S(245) .. entry.holder.Name
            .. S(439) .. tostring(entry.prompt.ActionText) .. S(82))
        return false
    end
    log(S(53) .. entry.name .. S(595) .. entry.power
        .. S(413) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(764))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(270) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(656) .. target.holder.Name .. S(124) .. target.name
        .. S(732) .. target.power .. S(320) .. best.name .. S(476) .. best.power .. S(218))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(695)] = function()
return function(import)
local Players = game:GetService(S(474))
local parseMoney = import(S(323))
local click = import(S(464))
local log = import(S(562))
local checkInterval = 30
local maxSpend = 100000
local function getMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(230))
    return playerGui and playerGui:FindFirstChild(S(590))
end
local function getTreeFrame()
    local mineGui = getMineGui()
    return mineGui and mineGui:FindFirstChild(S(518))
end
local function getUpgradesButton()
    local mineGui = getMineGui()
    local leftBar = mineGui and mineGui:FindFirstChild(S(505))
    local content = leftBar and leftBar:FindFirstChild(S(699))
    local container = content and content:FindFirstChild(S(41))
    return container and container:FindFirstChild(S(610))
end
local function zoneTabs(frame)
    local sidebar = frame:FindFirstChild(S(470))
    local list = sidebar and sidebar:FindFirstChild(S(515))
    if not list then
        return {}
    end
    local tabs = {}
    for _, tab in ipairs(list:GetChildren()) do
        if tab:IsA(S(197)) then
            local content = tab:FindFirstChild(S(755))
            local locked = content and content:FindFirstChild(S(109))
            if not locked then
                local label = content and content:FindFirstChild(S(711))
                table.insert(tabs, {
                    button = tab,
                    name = (label and label.Text ~= S(30) and label.Text) or tab.Name,
                })
            end
        end
    end
    return tabs
end
local function affordableNodes(frame)
    local tree = frame:FindFirstChild(S(110))
    local canvas = tree and tree:FindFirstChild(S(11))
    if not canvas then
        return {}
    end
    local nodes = {}
    for _, node in ipairs(canvas:GetChildren()) do
        if node:IsA(S(553)) and node.Name:match(S(275)) then
            local content = node:FindFirstChild(S(321))
            local cost = content and content:FindFirstChild(S(512))
            local price = cost and parseMoney(cost.Text)
            if price and price <= maxSpend then
                local pips = content:FindFirstChild(S(599))
                table.insert(nodes, {
                    button = node,
                    name = node.Name:sub(6),
                    price = price,
                    priceText = cost.Text,
                    pips = pips and pips.Text or S(18),
                })
            end
        end
    end
    table.sort(nodes, function(a, b)
        return a.price < b.price
    end)
    return nodes
end
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(247) .. (enabled and S(373) or S(58)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyNode(frame, node)
    click.Real(node.button)
    task.wait(0.4)
    local detail = frame:FindFirstChild(S(839))
    local buyButton = detail and detail:FindFirstChild(S(195))
    if not buyButton then
        log(S(302) .. node.name)
        return false
    end
    local content = buyButton:FindFirstChild(S(332))
    local label = content and content:FindFirstChild(S(172))
    if label and label.Text == S(147) then
        return false
    end
    click.Real(buyButton)
    task.wait(0.5)
    log(S(73) .. node.name .. S(132) .. node.pips .. S(701) .. node.priceText)
    return true
end
local function runJob()
    busy = true
    local button = getUpgradesButton()
    if not button then
        busy = false
        return
    end
    click.Real(button)
    task.wait(0.8)
    local frame = getTreeFrame()
    if not (frame and frame.Visible) then
        log(S(232))
        busy = false
        return
    end
    local bought = 0
    for _, tab in ipairs(zoneTabs(frame)) do
        if not enabled then
            break
        end
        click.Real(tab.button)
        task.wait(0.5)
        for _, node in ipairs(affordableNodes(frame)) do
            if not enabled then
                break
            end
            if buyNode(frame, node) then
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(629))
    end
    local close = frame:FindFirstChild(S(797)) or frame:FindFirstChild(S(160))
    if close then
        click.Real(close)
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(777)] = function()
return function(import)
local Players = game:GetService(S(528))
local Workspace = game:GetService(S(768))
local teleport = import(S(372))
local click = import(S(107))
local log = import(S(319))
local checkInterval = 45
local nothingSellableCooldown = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(748))
    local mineGui = playerGui and playerGui:FindFirstChild(S(449))
    return mineGui and mineGui:FindFirstChild(S(749))
end
local function labelText(button)
    local content = button:FindFirstChild(S(31))
    local label = content and content:FindFirstChild(S(491))
    return label and label.Text or S(117)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(234))
    local scroll = content and content:FindFirstChild(S(403))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(306) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(343) .. button.Name .. S(594) .. tostring(err))
    end
    return ok
end
local function isCardSelected(card)
    local content = card:FindFirstChild(S(189))
    local overlay = content and content:FindFirstChild(S(639))
    return overlay ~= nil and overlay.Visible
end
local function isOnScreen(card, scroll)
    local top = scroll.AbsolutePosition.Y
    local bottom = top + scroll.AbsoluteSize.Y
    local cardTop = card.AbsolutePosition.Y
    local cardBottom = cardTop + card.AbsoluteSize.Y
    return cardTop >= top and cardBottom <= bottom
end
local function selectUnselectedCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(836))
    local scroll = content and content:FindFirstChild(S(222))
    if not scroll then
        return 0
    end
    local selected = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(207) and not isCardSelected(card) and isOnScreen(card, scroll) then
            clickButton(card)
            selected = selected + 1
            task.wait(0.1)
        end
    end
    return selected
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(351))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(636), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(367))
    else
        lastAttempt = 0
        log(S(573))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(406) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(324))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(359))
        local closeButton = sellFrame:FindFirstChild(S(446))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(468))
    local selectAllButton = content and content:FindFirstChild(S(138))
    local sellButton = content and content:FindFirstChild(S(309))
    if selectAllButton and labelText(selectAllButton) == S(421) then
        clickButton(selectAllButton)
        log(S(589) .. cardCount .. S(74))
        task.wait(0.2)
    end
    local selected = selectUnselectedCards(sellFrame)
    if selected > 0 then
        log(S(155) .. selected .. S(51))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(398), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(641), 1, true) then
        clickButton(sellButton)
        log(S(76))
    else
        lastAttempt = tick() + nothingSellableCooldown - checkInterval
        local closeButton = sellFrame:FindFirstChild(S(409))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(106) .. cardCount .. S(3)
            .. tostring(sawLabel) .. S(241) .. nothingSellableCooldown .. S(494))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(79)] = function()
return function(import)
local Players = game:GetService(S(281))
local getCurrentPlot = import(S(395))
local getPlotParts = import(S(294))
local parseMoney = import(S(304))
local click = import(S(404))
local log = import(S(300))
local STAT_NAMES = { S(458), S(814), S(65) }
local checkInterval = 20
local maxSpend = 100000
local function getFocusFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(816))
    local mineGui = playerGui and playerGui:FindFirstChild(S(126))
    return mineGui and mineGui:FindFirstChild(S(817))
end
local function findUpgradeClick(holder)
    local upgradePart = holder:FindFirstChild(S(760))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(820))
end
local function occupiedHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return {}
    end
    local found = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClick(holder)
        if detector then
            table.insert(found, { holder = holder, detector = detector })
        end
    end
    return found
end
local function readStat(scroll, statName)
    local row = scroll:FindFirstChild(statName)
    local button = row and row:FindFirstChild(S(394))
    local content = button and button:FindFirstChild(S(516))
    local label = content and content:FindFirstChild(S(827))
    if not label then
        return nil
    end
    return {
        name = statName,
        button = button,
        price = parseMoney(label.Text),
        text = label.Text,
    }
end
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(308) .. (enabled and S(710) or S(366)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyAffordableStats(entry)
    local frame = getFocusFrame()
    local panels = frame and frame:FindFirstChild(S(54))
    local statsPanel = panels and panels:FindFirstChild(S(119))
    local scroll = statsPanel and statsPanel:FindFirstChild(S(569))
    if not scroll then
        log(S(92) .. entry.holder.Name)
        return 0
    end
    local bought = 0
    for _ = 1, #STAT_NAMES do
        local cheapest = nil
        for _, statName in ipairs(STAT_NAMES) do
            local stat = readStat(scroll, statName)
            if stat and stat.price and stat.price <= maxSpend then
                if not cheapest or stat.price < cheapest.price then
                    cheapest = stat
                end
            end
        end
        if not cheapest then
            break
        end
        click.Real(cheapest.button)
        log(S(341) .. cheapest.name .. S(812) .. cheapest.text
            .. S(227) .. entry.holder.Name)
        bought = bought + 1
        task.wait(0.4)
    end
    return bought
end
local function closeFrame()
    local frame = getFocusFrame()
    local exit = frame and frame:FindFirstChild(S(259))
    if exit then
        click.Real(exit)
        task.wait(0.3)
    end
end
local function runJob()
    busy = true
    for _, entry in ipairs(occupiedHolders()) do
        if not enabled then
            break
        end
        fireclickdetector(entry.detector)
        task.wait(0.8)
        local frame = getFocusFrame()
        if frame and frame.Visible then
            buyAffordableStats(entry)
            closeFrame()
        end
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(376)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(450)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(67) .. child.Name .. S(554)
        if child:IsA(S(261)) then
            line = line .. string.format(
                S(548),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(647)) then
            line = line .. string.format(S(483), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(709)) or child:IsA(S(122)) then
            line = line .. S(335)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(697) .. attrName .. S(384) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(769))
    end
end
return dumpInstance
end
modules[S(178)] = function()
return function(import)
local Players = game:GetService(S(131))
local ReplicatedStorage = game:GetService(S(116))
local getCurrentPlot = import(S(511))
local getPlotParts = import(S(140))
local log = import(S(626))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(825) .. name .. S(560) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(807))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(794) .. child.Name .. S(513)
            if child:IsA(S(19)) then
                line = line .. S(609) .. tostring(child.Value)
            elseif child:IsA(S(835)) or child:IsA(S(231)) then
                line = line .. S(462) .. tostring(child.Text) .. S(316)
            end
            log(line)
            dumpSubtree(child, indent .. S(97))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(431))
        return 0
    end
    log(S(71) .. plot.Name .. S(791))
    dumpAttributes(plot, S(81))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(49))
        return 0
    end
    log(S(543))
    dumpSubtree(parts.Chest, S(135))
    local player = Players.LocalPlayer
    log(S(383))
    dumpSubtree(player, S(27))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(89) .. player.Name .. S(802))
        dumpSubtree(mine, S(619))
    end
    log(S(532))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(519)] = function()
return function(import)
local Players = game:GetService(S(388))
local log = import(S(166))
local function dumpVisible(root, indent)
    indent = indent or S(657)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(278)) and child.Visible then
            local line = indent .. child.ClassName .. S(424) .. child.Name .. S(364)
            if child:IsA(S(627)) or child:IsA(S(33)) then
                line = line .. S(414) .. tostring(child.Text) .. S(314)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(279) .. attrName .. S(152) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(542))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(39))
    local mineGui = playerGui and playerGui:FindFirstChild(S(63))
    if not mineGui then
        log(S(287))
        return
    end
    log(S(670))
    dumpVisible(mineGui)
    log(S(111))
end
return dumpMineGui
end
end
modules[S(765)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(102))
local log = import(S(734))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(531)) or descendant:IsA(S(459)) then
            count = count + 1
            log(S(498) .. descendant.ClassName .. S(168) .. descendant:GetFullName())
        end
    end
    log(S(614) .. count .. S(628))
    return count
end
return dumpRemotes
end
end
modules[S(260)] = function()
return function(import)
local Players = game:GetService(S(32))
local log = import(S(467))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(327))
    local mineGui = playerGui and playerGui:FindFirstChild(S(492))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(98))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(203)) and sellFrame.Content:FindFirstChild(S(568))
    if not scroll then
        log(S(7))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(533) or card:IsA(S(790)) then
            count = count + 1
            log(S(493) .. count .. S(618) .. card.ClassName .. S(624) .. card.Name .. S(248))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(673) .. attrName .. S(565) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(678) .. child.ClassName .. S(823) .. child.Name .. S(220)
                if child:IsA(S(219)) then
                    line = line .. S(466) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(587) .. count .. S(756))
    return count
end
return dumpSellCards
end
end
modules[S(283)] = function()
return function(import)
local Players = game:GetService(S(291))
local log = import(S(608))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(380) then
        return value.ClassName .. S(13) .. value:GetFullName()
    end
    if valueType == S(525) then
        return S(411)
    end
    if valueType == S(796) then
        return S(597)
    end
    return valueType .. S(151) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(813) then
        return value:IsA(S(370)) or value:IsA(S(114)) or value:IsA(S(727))
    end
    return valueType == S(799) or valueType == S(136) or valueType == S(182)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(612))
    end)
    if ok and source then
        log(indent .. S(412) .. tostring(source) .. S(418) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(714) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(517) .. describe(constant))
            end
        end
    else
        log(indent .. S(86))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(262) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(123) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(212))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(93) .. label .. S(679))
        return
    end
    log(S(263) .. label .. S(712) .. button:GetFullName() .. S(527))
    if not getconnections then
        log(S(688))
        return
    end
    for _, signalName in ipairs({ S(606), S(322) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(620) .. signalName .. S(680) .. #connections .. S(837))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(202) .. index)
                    inspectFunction(fn, S(198))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(57))
    local current = playerGui and playerGui:FindFirstChild(S(789))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(798))
    inspectButton(S(159), find({ S(523), S(267), S(767), S(256) }))
    inspectButton(S(223), find({ S(537), S(400), S(407) }))
    inspectButton(S(754), find({ S(713), S(781), S(616) }))
    inspectButton(S(484), find({ S(391), S(288), S(146) }))
    local statsScroll = find({ S(661), S(578), S(269), S(663) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(593))
    inspectButton(S(88), damageRow and damageRow:FindFirstChild(S(357)))
    log(S(153))
end
return inspectHandlers
end
end
modules[S(669)] = function()
return function(import)
local Players = game:GetService(S(183))
local log = import(S(381))
local CAPABILITIES = {
    S(604), S(334), S(832), S(477), S(746),
    S(142), S(158), S(410), S(544), S(804),
    S(558), S(242), S(254), S(456),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(489)) do
        if type(target) ~= S(187) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(455))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(387) .. name .. S(720) .. ((ok and type(value) == S(478)) and S(666) or S(655)))
    end
end
local WANTED_SCRIPTS = {
    S(556),
    S(775),
    S(581),
    S(690),
    S(452),
    S(200),
}
local function tryDecompile()
    if not decompile then
        log(S(574))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(112))
    local ui = playerScripts and playerScripts:FindFirstChild(S(289))
    local mineGame = ui and ui:FindFirstChild(S(274))
    if not mineGame then
        log(S(328))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(438) .. name .. S(386))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(651) and #source > 0 then
                local path = S(634) .. name .. S(557)
                local written = pcall(writefile, path, source)
                log(S(521) .. name .. S(47) .. #source .. S(141)
                    .. (written and path or S(722)))
            else
                log(S(782) .. name .. S(603) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(607))
    reportCapabilities()
    tryDecompile()
    log(S(295))
end
return probe
end
end
modules[S(340)] = function()
return function(import)
local Players = game:GetService(S(792))
local log = import(S(303))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(310))
    local mineGui = playerGui and playerGui:FindFirstChild(S(480))
    local hotbar = mineGui and mineGui:FindFirstChild(S(60))
    local content = hotbar and hotbar:FindFirstChild(S(600))
    return content and content:FindFirstChild(S(739))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(770)) then
            local count = descendant.Text:match(S(718))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(510))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(633))
    local nameLabel = content and content:FindFirstChild(S(743))
    local damageLabel = content and content:FindFirstChild(S(150))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(299) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(5), S(771)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(815) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(707) .. slot .. S(583) .. info.name
                        .. S(113) .. info.power .. S(545) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(214)] = function()
local Players = game:GetService(S(741))
local Workspace = game:GetService(S(56))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(298))
    local plots = mineWorld and mineWorld:FindFirstChild(S(457))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(145)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(307)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(417)),
        Nodes = plot:FindFirstChild(S(12)),
        Chest = plot:FindFirstChild(S(708)),
        Zones = plot:FindFirstChild(S(433)),
        Crew = plot:FindFirstChild(S(115)),
        Loot = plot:FindFirstChild(S(45)),
        PlotIndex = plot:GetAttribute(S(420)),
        ChestCapacity = plot:GetAttribute(S(598)),
    }
end
return getPlotParts
end
modules[S(795)] = function()
return function(import)
    local Players = game:GetService(S(80))
    local createWindow = import(S(672))(import)
    local i18n = import(S(502))
    local antiAfk = import(S(251))(import)
    local interactionLogger = import(S(173))(import)
    local scheduler = import(S(277))(import)
    local config = import(S(475))(import)
    local log = import(S(751))
    local window = createWindow({
        Subtitle = S(508),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(482))
    local getPlotParts = import(S(440))
    local dumpInstance = import(S(175))
    local dumpRemotes = import(S(818))(import)
    local dumpSellCards = import(S(622))(import)
    local dumpGui = import(S(72))(import)
    local dumpChest = import(S(490))(import)
    local inspectHandlers = import(S(434))(import)
    local probeEnv = import(S(753))(import)
    local autoMine = import(S(567))(import)
    local autoPlace = import(S(551))(import)
    local autoChest = import(S(652))(import)
    local autoSell = import(S(645))(import)
    local autoUpgrade = import(S(204))(import)
    local autoResearch = import(S(28))(import)
    scheduler.RegisterStandalone(S(838), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(539), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(368), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(738), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(70), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(621), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(735), S(164))
    persistentToggle(farmTab:AddCard(
        S(344),
        S(177)
    ), S(171), autoMine.SetEnabled)
    persistentSlider(farmTab, S(313), S(326),
        1, 30, S(272), 5, autoMine.SetCyclesPerSecond, S(379))
    persistentSlider(farmTab, S(130), S(43),
        1, 10, S(460), 1, autoMine.SetClicksPerCycle, S(192))
    persistentToggle(farmTab:AddCard(
        S(575),
        S(385)
    ), S(684), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(330), S(828),
        1, 60, S(747), 3, autoPlace.SetCheckInterval, S(257))
    persistentToggle(farmTab:AddCard(
        S(725),
        S(427)
    ), S(506), autoChest.SetEnabled)
    persistentSlider(farmTab, S(806), S(90),
        50, 3000, S(59), 100, autoChest.SetMinItems, S(702))
    persistentSlider(farmTab, S(503), S(445),
        1, 60, S(40), 3, autoChest.SetCheckInterval, S(296))
    persistentToggle(farmTab:AddCard(
        S(286),
        S(822)
    ), S(253), autoSell.SetEnabled)
    persistentSlider(farmTab, S(559), S(660),
        10, 300, S(703), 45, autoSell.SetCheckInterval, S(644))
    persistentToggle(farmTab:AddCard(
        S(402),
        S(236)
    ), S(347), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(290), S(185),
        5, 300, S(293), 20, autoUpgrade.SetInterval, S(665))
    persistentSlider(farmTab, S(586), S(808),
        1000, 1000000, S(566), 100000, autoUpgrade.SetMaxSpend, S(266))
    persistentToggle(farmTab:AddCard(
        S(830),
        S(430)
    ), S(180), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(75), S(801),
        10, 600, S(276), 30, autoResearch.SetCheckInterval, S(419))
    persistentSlider(farmTab, S(497), S(541),
        1000, 1000000, S(752), 100000, autoResearch.SetMaxSpend, S(6))
    local debugTab = window:AddTab(S(706), S(779))
    persistentToggle(
        debugTab:AddCard(
            S(676),
            S(443)
        ),
        S(297),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(444), S(693))
    local checkBtn
    checkBtn = debugCard:AddButton(S(210), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(211)
            log(S(428))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(736),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(471),
        S(787)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(246), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(758)
            return
        end
        local ok, err = log(S(55))
        if not ok then
            dumpBtn.Text = S(674)
            dumpCard:SetSubtitle(S(99) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(540) .. firstNode.Name .. S(305))
            dumpInstance(firstNode, log)
        else
            log(S(144))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(631) .. firstHolder.Name .. S(705))
            dumpInstance(firstHolder, log)
        else
            log(S(382))
        end
        dumpBtn.Text = S(436)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(396),
        S(473)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(834), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(282))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(228))
        if not tool then
            toolDumpBtn.Text = S(378)
            return
        end
        log(S(191) .. tool.Name .. S(134))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(108) .. attrName .. S(499) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(225)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(785),
        S(664)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(546), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(103)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(68),
        S(696)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(778), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(563)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(729),
        S(213)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(579), function()
        dumpGui()
        guiDumpBtn.Text = S(416)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(717),
        S(250)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(716), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(24)
    end)
    local handlersCard = debugTab:AddCard(
        S(742),
        S(840)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(16), function()
        inspectHandlers()
        handlersBtn.Text = S(348)
    end)
    local probeCard = debugTab:AddCard(
        S(408),
        S(429)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(401), function()
        probeEnv()
        probeBtn.Text = S(69)
    end)
    local settingsTab = window:AddTab(S(721), S(64))
    persistentToggle(
        settingsTab:AddCard(S(485), S(240)),
        S(486),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(363), 100)
    settingsTab:AddSlider(S(371), S(576), 50, 150, savedScale, function(value)
        config.Set(S(514), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(356), 1)
    settingsTab:AddSegmented({ S(605), S(667), S(617) }, savedLanguage, function(index, value)
        local code = ({ S(463), S(29), S(632) })[index]
        config.Set(S(580), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(588), S(728), S(582) })[savedLanguage] or S(362))
    log(S(374) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
    end)
end
end
modules[S(454)] = function()
return {
    [79389059854988] = S(397),
}
end
import(S(100))(import)
