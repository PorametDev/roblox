-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoUpgrade] ", "[AntiAFK] enabled - watching Player.Idled", "none", "[AutoPlace] picked up ", "MekPro.log", "[AutoSell] enabled", "TextButton", "core/logger", " (prompt says '", " (money ", "/", "walkSpeed", "games/my_anime_mine/auto_research", ")", " = ", " ", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "getgc", "autoPlotLatest", ": ", "table", "", "TextLabel", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "[Decompile] === end ===", ": ", "games/my_anime_mine/game_api", "may need us at the stand - trying that", "[AutoPlace] placed ", "core/logger", " costs ", "[AutoPlot] ", "string", "autoResearchCore", "table", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "[GuiDump] === visible PlayerGui tree ===", "table", "a6b8d09+", " didn't go through (out of stock or too expensive)", "AutoChest", "Mining", "TextLabel", "ZoneBoardGui", "%$%s*([%d,%.]+)%s*(%a*)", "autoResearchCoreLast", " '", "string", ")", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", ") ", "core/debug_decompile", "Header", "[AutoResearch] ", "Players", "core/logger", "[AutoResearch] nothing open and affordable right now", "Frame", "autoResearchKeepMoney", "Auto Plot: ใช้โซนล่าสุดเสมอ", "Search functions...", "Dump Visible GUI", "[%X] ", ")", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "", "core/logger", "/", "[AutoResearch] Core ", "Frame", "games/my_anime_mine/auto_chest", "HumanoidRootPart", "", "/config.json", "ไล่ซื้อทุกกี่วินาที", "Anti-AFK", "ReplicatedStorage", "Humanoid", " screens", "autoChestMinItems", "core/teleport", " ", " pickaxe(s) for ", "MineTakeChest", "Auto Chest", "Workspace", " holder(s) - not buying more", "%", "ไทย", "games/my_anime_mine/game_api", "[AutoSell] disabled", "MineWorld", "AutoUpgrade", "[AutoClaim] ", "debug.getupvalues", "autoResearchMaxPerPass", "s", "languageIndex", "Players", "'", "TextButton", "s", "^%$", "auto", "HumanoidRootPart", "MineSpeed", "Content", "core/dispatch", "core/parse_money", " owned)", "th", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", " mutation x", "MineSellItems", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "ValueLabel", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Players", "[AutoPlot] ", "autoMine", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "disabled", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "Pad", "GetHolderMineSpeedLevel", "", "enabled", "Auto Sell: รอบขาย", "[Decompile] === start ===", "MinePower", " research is ", " screen(s)) ===", "number", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " - upgrade the miners first", "[Decompile] wrote ", "Crew", "disabled", "debug.getconstants", "games/my_anime_mine/main", ": ", "autoUpgradeMutationTarget", "[AutoPlace] upgrading ", "TextButton", "string", "open_menu_hint", " files", "core/humanoid", " open=", "core/humanoid", "RemoteEvent", "core/build_env", "TextButton", "x", "en", "[AutoResearch] cheapest open node: ", "autoSellMaxWait", "[AntiAFK] idle detected, simulating input", ", you have ", "games/my_anime_mine/auto_sell", "core/scheduler", "games/my_anime_mine/auto_claim", "Nodes", "core/logger", "TabBar", ".lua", "[Decompile] === executor capabilities ===", "ต่ำสุดก่อน", "core/teleport", " -> ", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Players", " node(s) looked buyable but the server refused", " failed)", "core/get_place_id", "ClickDetector", "[Remotes] ", " (x", "games/registry", "lowest", "(nothing to do)", "MineSpeed", "ถูกสุดก่อน", "BasePart", "", "Run", " still empty after placing ", " (", "TextLabel", "TextButton", "autoChestInterval", "autoPlotInterval", "[AutoPlace] disabled", "core/ui", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "jumpPower", "games/my_anime_mine/game_api", "Auto Claim (Index)", "[Interaction] logger enabled - go click/interact with things now", "Auto Research: ซื้อสูงสุดต่อรอบ", "ZoneRow", "core/config", "core/i18n", "/", "core/teleport", "getnilinstances", "ฟาร์ม", "getscripts", "CharSpeedMaxLevel", " reward(s) looked claimable but none went through", "s", "no", "Auto Chest: รอบเช็ค", "BasePart", "prompt disabled", "Frame", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", ")", "core/logger", "action '", "", "ขนาดเมนู", " (", "core/scheduler", "RemoteFunction", "Damage", "disabled", " -> x", "", " (x", "-", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "antiAfk", "This is not a supported place.", "TextLabel", "TextLabel", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "My Anime Mine · Mining Hub", "GuiService", "interactionLogger", "autoPlotBuy", "MineBuySkill", "autoUpgradeRollMutation", "Damage", ")", "[Interaction] ClickDetector clicked: ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "/s", " while selecting", "BodyPosition", "", "core/debug_dump_gui", "Text", "%$", "[AutoPlace] ", "TextLabel", "UserInputService", "[AutoResearch]   ", "leaderstats", "Players", "Auto Upgrade Plot (Zone)", ") on ", "'", "/", "autoSellMaxWait", "[AutoSell] MineSellItems ", " -> ", "%$", "BodyVelocity", "TextLabel", "[AutoPlot] couldn't switch to ", "CoreGui", "MekPro", "nothing to place or upgrade", "MineEquipSlot", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", " (", " (ActionText=", "en-us", "lowest", "⚙", "research", "แรงกระโดด", "Damage", "core/debug_interaction_logger", "autoUpgradePickaxeIndex", "UICorner", "empty", "core/logger", "TextLabel", "[AutoResearch] idle: money=", " (", "s", "UIScale", "Dump", "GuiButton", "Text", "Auto Research (Plot)", "%.0f", "Dump All Remotes", "autoMineSpeed", " req=", "[AutoPlace] nothing in the hotbar to place", "MekPro/decompiled", "อ่านป้ายบน chest ทุกกี่วินาที", "🐛", "PlayerGui", "[AutoChest] chest is empty", "^([%d,]+)$", "autoPlotKeepMoney", "=locked(", "getsenv", "core/click", " (", "", "VirtualInputManager", "PickaxesStand", "auto", "Badge", "table", " waited ", "Container", "yes", "core/logger", "string", "Auto Plot: กันเงินไว้", "TextLabel", "')", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "[AutoMine] ", "Frame", "", " ===", "VirtualUser", "[Decompile] decompile unavailable in this executor", "Frame", "TextLabel", "_", "", "ตามเครื่อง", "sale didn't go through", "LocalScript", "ค้นหาฟังก์ชัน...", "autoUpgradeEquipPickaxe", "MekPro", "autoResearchInterval", " remotes", "[AutoUpgrade] every stat on every miner is maxed", "open_menu_hint", "Search", "[MekPro] build ", "autoUpgradePriority", "UserInputService", "decompile", "Hauler", "write reported success but ", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "มุดแมพ (Noclip)", "auto", "ProximityPrompt", "=", "autoChest", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "autoUpgradeMaxPerPass", "games/my_anime_mine/get_current_plot", "s", "UIPadding", " doesn't exist", "GuiButton", ") didn't take, trying the next one", "autoUpgradeLimitPickaxes", ") -> ", "Content", "core/parse_money", "games/my_anime_mine/auto_place", "Badge", " node(s), ", "research", " (", "autoClaim", "BasePart", "Auto Place: เปลี่ยนตัวอ่อนออก", "Workspace", "ProximityPrompt", "SellStand", "th", "Auto Research: ซื้อสาย Core", "UserInputService", "[AutoPlace] couldn't pick up ", "LayerCollector", "Locked miner slot", " (", " -> ", " ", "ValueLabel", "x", "[Interaction] ProximityPrompt triggered: ", ") ", "[GuiDump] === end (", "[AutoUpgrade] ", "[Scheduler] turn: ", "[GameAPI] SendEvent failed: ", "dev", "[AutoChest] collected ", "X", "hookfunction", "Auto Plot: รอบเช็ค", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", ") from ", "games/my_anime_mine/auto_upgrade", "core/get_place_id", "MineWorld", "", ", built ", "core/logger", "autoUpgradeMutationBudget", "[AutoPlot] unlocked ", " not bought yet (", "[AutoUpgrade] pickaxe #", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "disabled", " ", ")", "ScrollingFrame", " costs ", ": ", "GetHolderSpeedLevel", "Players", "en", "", "ขายทุกกี่วินาที", "getrenv", "Mutation: หยุดเมื่อได้ x", " items)", "Auto Research: รอบเช็ค", "/", " = ", "Auto Research: ซื้อสาย Zone", "Auto Claim: รอบเช็ค", " (", "Auto Upgrade: สุ่ม Mutation", "no isfile/readfile available to verify the write", " (power ", "core/debug_interaction_logger", " power=", "UIListLayout", "autoUpgradeKeepMoney", "disabled", "", "autoClaimInterval", "Chest", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "write reported success but the line isn't in ", " ", "GetCharacterUpgradeCost", "neither appendfile nor writefile is available", "slot ", ") ", " ", "core/i18n", "Workspace", "autoClaimMaxPerPass", "games/my_anime_mine/game_api", "TextLabel", "core/ui", "core/i18n", "'", "core/debug_decompile", "table", " open and unmaxed", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "(none)", "ItemName", "ProximityPrompt", " stack(s)", "[GuiDump] ", "Place", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "games/my_anime_mine/game_api", "games/my_anime_mine/auto_mine", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "TextButton", "core/logger", "table", "WalkSpeed (ปกติ 16)", ",", "cost", "[Remotes] === total: ", "Auto Mine", "  ", "games/my_anime_mine/game_api", "fly", "Loot", "ChestCapacity", "Frame", "AutoClaim", "MineSelectZone", "[AutoPlace] ", " power ", ": ", "games/my_anime_mine/auto_plot", "[^%w]+", "[AutoResearch] ", "[AutoResearch] ", "function", " buyZones=", "Root", "PlayerGui", ")", "autoResearchZones", "/", " (", "costs ", "", " (cost ", "Speed", "TextButton", "[Logger] failed to write ", "ScreenGui", " budget=", "?", "ClickDetector", "unreadable", "", "MineGui", "", "Backpack", "Auto Chest: เกณฑ์เก็บ", "enabled", "[AutoPlace] fireproximityprompt error: ", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", " refused - ", "[Interaction] Button activated: ", "Damage", "search_placeholder", "/", " (Text=", "menuScale", "Auto Place Miner", " (", "[AutoUpgrade] ", "เดินทะลุกำแพงและพื้น", "[Config] couldn't write ", "Hauler", " '", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " ", "games/my_anime_mine/game_api", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "core/get_leaderstat", "Auto Sell: รอคิวได้นานสุด", "core/logger", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", " x", "GetHolderLevel", "debug.getproto", "sold ", "ClickDetector", "Shop Stand", "games/my_anime_mine/get_current_plot", "[GuiDump] PlayerGui not found", " | ", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "Mining", "GetCharacterSpeedUpgradeCost", "Modules", " (", "games/my_anime_mine/game_api", "^x(%d+)$", "", "games/my_anime_mine/get_best_miner", "MineEquipPickaxe", "autoSell", "UIListLayout", " script(s) to ", " -> ", "Empty miner slot", "[AutoPlace] drifted out of range of ", "Humanoid", "Auto Sell", " not maxed", "Players", "empty", "games/my_anime_mine/auto_plot", ",", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "ReplicatedStorage", "x", "core/dispatch", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "core/get_me", "Players", "[Bb]uy", "Players", "core/logger", ": ", "core/logger", "TextLabel", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "games/my_anime_mine/get_plot_parts", "autoUpgrade", "Frame", "table", " buyCore=", "autoPlace", "button has no size on screen", "ตั้งค่า", "getscriptbytecode", "Workspace", "All", "[^%.]+", "AutoPlace", " powerOk=", "th", "string", "games/my_anime_mine/auto_chest", "TextLabel", "th", "table", "AutoPlot", "table", "[AutoResearch]   Core: ", "maxed", "Workspace", "Dump", "", "List", "getloadedmodules", "[Uu]nlock", "RunService", "", " team power, you have ", "Auto Research: กันเงินไว้", "Plots", "getcallbackvalue", "autoPlaceUpgradeWeak", " cost=", "0 = ไม่จำกัด", "s", "core/logger", "", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "autoUpgradePickaxe", "core/anti_afk", "2026-09-21 04:56:41", "MineBuyPickaxe", " ", "Speed", "games/my_anime_mine/game_api", "Plots", "core/logger", "MineSpeed", "games/my_anime_mine/auto_research", "languageIndex", "Content", "Frame", " items", "x", "[BestMiner] slot ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", ")", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "fill", "Auto Mine: ความเร็ว", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "ItemFrame", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "MekPro", "HttpService", "s - jumping the queue", "autoMineClicks", "HotbarFrame", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "locked slot", "core/debug_dump_remotes", "ModuleScript", "เช็คโซนทุกกี่วินาที", "cost", " ", "core/config", "core/build_env", "\n", "[AutoResearch]   ", "[AutoChest] enabled (collects once >= ", "works from anywhere - dropping the teleport", "[Interaction] Key pressed: %s (gameProcessed=%s)", "ProximityPrompt", "TextLabel", "[AutoResearch] ", ")", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Zones", "Open menu", ")", "แพงสุดก่อน", "autoPlot", "Auto Claim: เก็บสูงสุดต่อรอบ", "games/my_anime_mine/auto_upgrade", ")", "[Decompile] PlayerScripts not found", " #", " node(s), ", "autoUpgradePickaxeIndex", "(", "[AutoUpgrade] MineBuyPickaxe ", ")", "Auto Place: รอบเช็ค", "core/anti_afk", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "enabled", "[AutoChest] disabled", "", "search_placeholder", ")", "[AutoUpgrade] ", "PlayerGui", "Auto Upgrade: แจก Pickaxe ให้ miner", " items (now ", "lowest", "games/my_anime_mine/main", "ย่อขยายเมนูทั้งชุด", "[Decompile] ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "core/debug_tab", "[Config] couldn't read ", "UICorner", "[AutoChest] can't read the chest yet", "Decompile Client Scripts", "enabled", "อังกฤษ", "OwnerUserId", "RunService", "table", "ZoneBoard", "autoResearchCheapestFirst", " (chest holds ", " ", ") needs ", "", "Auto Upgrade: กันเงินไว้", "GuiObject", "[AutoUpgrade] bought pickaxe #", "[AutoClaim] claimed ", "games/my_anime_mine/auto_sell", "core/logger", "Players", "[GameAPI] couldn't load the game's modules: ", ")", "  [attr] ", "=empty", "ลอย (Float)", "waiting", " -> ", "games/my_anime_mine/get_current_plot", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "HumanoidRootPart", "[Scheduler] ", "[AntiAFK] disabled", "🏠", " from ", "[Interaction] logger disabled", " ", "[AutoChest] waiting: ", "noclip", "getconnections", "core/logger", "[AutoUpgrade] ", "[AutoPlace] holders: ", "Interaction Logger", " (", "\n", "PlayerScripts", "table", "Speed", ") ", "en", "unreadable", "Holders", "autoSellInterval", "OwnerUserId", "upgrade", " zones=", "x", "MineRollMutation", "Auto Upgrade: รอบเช็ค", "M", "core/logger", "enabled", "Frame", "Players", "games/registry", "M", "TextButton", ")", "UIStroke", "[AutoChest] MineTakeChest had no effect (", "M", "Frame", "core/get_place_id", "TextButton", ": ", "s", "Main", "^M(%d+)$", "CharMineSpeedMaxLevel", ")", "needs us at the stand - keeping the teleport", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Auto Plot: ซื้อโซนถัดไป", "table", "Frame", "CharMaxLevel", "games/my_anime_mine/auto_mine", "autoPlaceInterval", "MekPro", "/", "ดีบัก", "TextLabel", "M", "CoreUI", ",", "core/teleport", "GetCharacterMineSpeedUpgradeCost", "function", "#", "s", "ปุ่มเปิดเมนู", "[Scheduler] ", "Auto Upgrade: Pickaxe ที่จะซื้อ", "AutoMine", "table", " pickaxe ", "autoUpgradeInterval", "games/my_anime_mine/get_plot_parts", "[MekPro] settings file: ", "Players", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "float", "menuScale", "waiting", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoSell] ", " (power ", "BasePart", "[^%d%.]", "/", "MineUpgradeCharacter", "[AutoPlot] ", "auto", "works from anywhere - no teleport needed", "games/my_anime_mine/auto_claim", "Auto Upgrade Unit", "ความเร็ววิ่ง", "Workspace", "AutoSell", "[AutoPlace] slot ", "table", " (Text=", "Auto Mine: คลิกต่อรอบ", "JumpPower (ปกติ 50)", "core/debug_dump_remotes", "TextBox", "core/debug_dump_gui", "[AutoPlot] now mining ", "TextLabel", "UIListLayout", " ชิ้น", "บิน (Fly)", "games/my_anime_mine/get_best_miner", "AutoResearch", " error: ", "function", "games/my_anime_mine/get_plot_parts", "Mutation: เพดานราคาต่อครั้ง", "core/debug_tab", "TextLabel", "games/my_anime_mine/auto_place", "[AutoClaim] ", "  ", "fill", "core/logger", "PlotIndex", "autoResearch", "Players"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(645)] = function()
return function(import)
local log = import(S(747))
local Players = game:GetService(S(55))
local VirtualUser = game:GetService(S(335))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(2))
        connection = player.Idled:Connect(function()
            log(S(159))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(760))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(683)] = function()
return { Env = S(404), IsDev = true, Commit = S(39), BuiltAt = S(646) }
end
modules[S(314)] = function()
local VirtualInputManager = game:GetService(S(317))
local GuiService = game:GetService(S(240))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(607)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(682)] = function()
return function(import)
local HttpService = game:GetService(S(671))
local getPlaceId = import(S(176))
local log = import(S(290))
local ROOT_FOLDER = S(346)
local placeFolder = ROOT_FOLDER .. S(437) .. tostring(getPlaceId())
local filePath = placeFolder .. S(74)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(775) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(727) .. filePath .. S(20) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(544) .. filePath .. S(803) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(52)] = function()
return function(import)
local Players = game:GetService(S(429))
local log = import(S(789))
local CAPABILITIES = {
    S(355), S(609), S(209), S(18), S(629),
    S(767), S(636), S(313), S(433), S(140),
    S(95), S(557), S(407), S(207),
}
local OUT_FOLDER = S(305)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(612)) do
        if type(target) ~= S(833) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(168))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(724) .. name .. S(15) .. ((ok and type(value) == S(826)) and S(324) or S(213)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(336))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(774))
    if not playerScripts then
        log(S(702))
        return 0
    end
    pcall(makefolder, S(274))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(343)) or target:IsA(S(678)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(146) and #source > 0 then
                local name = target:GetFullName():gsub(S(503), S(339))
                if pcall(writefile, OUT_FOLDER .. S(848) .. name .. S(167), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(137) .. written .. S(576) .. OUT_FOLDER
        .. S(541) .. failed .. S(175))
    return written
end
local function run()
    log(S(130))
    reportCapabilities()
    local count = dumpAll()
    log(S(25))
    return count
end
return run
end
end
modules[S(865)] = function()
return function(import)
local Players = game:GetService(S(119))
local log = import(S(652))
local function dumpVisible(root, indent)
    indent = indent or S(714)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(743)) and child.Visible then
            local line = indent .. child.ClassName .. S(546) .. child.Name .. S(468)
            if child:IsA(S(291)) or child:IsA(S(795)) then
                line = line .. S(860) .. tostring(child.Text) .. S(692)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(751) .. attrName .. S(438) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(881))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(308))
    if not playerGui then
        log(S(562))
        return 0
    end
    local screens = 0
    log(S(37))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(391)) and screen.Enabled and screen.Name ~= S(817) then
            screens = screens + 1
            log(S(477) .. screen.ClassName .. S(47) .. screen.Name .. S(264))
            dumpVisible(screen, S(491))
        end
    end
    log(S(400) .. screens .. S(133))
    return screens
end
return dumpGui
end
end
modules[S(863)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(77))
local log = import(S(67))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(152)) or descendant:IsA(S(226)) then
            count = count + 1
            log(S(178) .. descendant.ClassName .. S(597) .. descendant:GetFullName())
        end
    end
    log(S(489) .. count .. S(334))
    return count
end
return dumpRemotes
end
end
modules[S(445)] = function()
return function(import)
local Players = game:GetService(S(792))
local Workspace = game:GetService(S(610))
local UserInputService = game:GetService(S(354))
local log = import(S(220))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(398) .. describe(prompt)
            .. S(279) .. prompt.ActionText .. S(14))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(247) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(534) .. describe(button)
            .. S(538) .. tostring(button.Text) .. S(750))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(689)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(559)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(297)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(763))
        return
    end
    connections = {}
    log(S(200))
    local playerGui = Players.LocalPlayer:WaitForChild(S(718))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(361)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(177)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(370)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(688),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(877)] = function()
return function(import)
local interactionLogger = import(S(286))(import)
local dumpRemotes = import(S(677))(import)
local dumpGui = import(S(253))(import)
local decompileScripts = import(S(469))(import)
local humanoid = import(S(149))
local function build(window, persist)
    local tab = window:AddTab(S(819), S(307))
    persist.Toggle(
        tab:AddCard(
            S(771),
            S(118)
        ),
        S(241),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(301),
        S(24)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(296), function()
        remoteBtn.Text = dumpRemotes() .. S(348)
    end)
    local guiCard = tab:AddCard(
        S(62),
        S(600)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(626), function()
        guiBtn.Text = dumpGui() .. S(79)
    end)
    local decompileCard = tab:AddCard(
        S(730),
        S(587)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(187), function()
        decompileBtn.Text = decompileScripts() .. S(148)
    end)
    humanoid.Start()
    persist.Slider(tab, S(855), S(486),
        8, 200, S(12), 16, humanoid.SetWalkSpeed, S(515))
    persist.Slider(tab, S(284), S(862),
        30, 300, S(197), 50, humanoid.SetJumpPower, S(222))
    persist.Toggle(
        tab:AddCard(S(870), S(50)),
        S(493), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(359), S(543)),
        S(766), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(753), S(358)),
        S(840), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(108)] = function()
return function(import)
    local getPlaceId = import(S(412))
    local registry = import(S(793))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(235))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(551)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(260))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(592)] = function()
local Players = game:GetService(S(173))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(580))
    local backpack = player:WaitForChild(S(528))
    return player, humanoid, backpack
end
return getMe
end
modules[S(801)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(151)] = function()
local Players = game:GetService(S(261))
local RunService = game:GetService(S(631))
local UserInputService = game:GetService(S(389))
local Workspace = game:GetService(S(384))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(78))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(758))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(270))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(846)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(382)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(251))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(467)] = function()
local Players = game:GetService(S(838))
local STRINGS = {
    en = {
        search_placeholder = S(61),
        open_menu_hint = S(695),
    },
    th = {
        search_placeholder = S(344),
        open_menu_hint = S(829),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(280)
    if locale:sub(1, 2):lower() == S(387) then
        return S(111)
    end
    return S(156)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(851) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(165)] = function()
local LOG_FILE = S(5)
local function log(message)
    local line = os.date(S(63)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(316))
            end
            appendfile(LOG_FILE, line .. S(684))
        elseif writefile then
            local existing = S(186)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(773))
        else
            error(S(457))
        end
        if not (isfile and readfile) then
            error(S(443))
        end
        if not isfile(LOG_FILE) then
            error(S(357) .. LOG_FILE .. S(369))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(454) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(519) .. LOG_FILE .. S(501) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(375)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(48) then
        return nil
    end
    local number, suffix = text:match(S(45))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(487), S(527))))
    if not value then
        return nil
    end
    if suffix ~= S(414) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(162)] = function()
return function(import)
local RunService = game:GetService(S(734))
local log = import(S(56))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(759) .. task_.name .. S(873) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(830) .. starved.name .. S(322)
            .. string.format(S(300), now - starved.waitingSince)
            .. S(672))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(402) .. (currentTask and currentTask.name or S(182)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(206)] = function()
local Players = game:GetService(S(595))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(72))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(466)] = function()
return function(import)
local i18n = import(S(204))
local UserInputService = game:GetService(S(258))
local THEME = {
    BrandName = S(670),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(728))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(288))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(368))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(101))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(230)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(797))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(465))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(522)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(599))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(66)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(425))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(447))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(190))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(813))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(236))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(338))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(878))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(117))
            if not valueLbl then
                valueLbl = Instance.new(S(618))
                valueLbl.Name = S(396)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(377))
            if not badge then
                badge = Instance.new(S(23))
                badge.Name = S(320)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(518))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(496))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(58))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(154))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(571)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(88)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(791))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(337))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(7))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(127)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(657))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(575))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(145))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(254)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(520))
    screenGui.Name = S(822)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(273))
    local main = Instance.new(S(800))
    main.Name = S(805)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(295))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(217))
    tabBar.Name = S(166)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(868))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(70))
    header.Name = S(53)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(191))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(328))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(271))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(22)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(483))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(406)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(802))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(232)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(864))
    searchBox.Name = S(351)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(536))
    searchBox.Text = S(340)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(603))
    content.Name = S(107)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(332))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(820))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(147))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(690))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(298)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(525) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(648) .. (item.Subtitle or S(632))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(715))
        footerLabel.Text = i18n.T(S(350))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(71)] = function()
return function(import)
local gameApi = import(S(549))(import)
local log = import(S(883))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(686) .. minItemsToCollect .. S(435))
    else
        log(S(713))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(779) then
            log(S(729))
            lastReported = S(524)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(584) then
            log(S(309))
            lastReported = S(289)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(754) then
            local capacity = gameApi.ChestCapacity()
            log(S(765) .. count .. S(68) .. want .. S(658)
                .. (capacity and (S(738) .. capacity .. S(696)) or S(252)))
            lastReported = S(842)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(405) .. count .. S(720) .. after .. S(424))
        else
            log(S(798) .. count
                .. S(171) .. tostring(after) .. S(510))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(163)] = function()
return function(import)
local gameApi = import(S(492))(import)
local log = import(S(768))
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(94) .. (enabled and S(530) or S(422)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(745) .. reward.category .. S(764)
                .. tostring(reward.tierKey) .. S(703) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(880) .. #pending .. S(211))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    task.spawn(runJob)
end
return autoClaim
end
end
modules[S(815)] = function()
return function(import)
local getCurrentPlot = import(S(366))
local getPlotParts = import(S(875))
local log = import(S(598))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(523), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(331) .. (enabled and S(128) or S(449)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(879)] = function()
return function(import)
local Players = game:GetService(S(99))
local getCurrentPlot = import(S(561))
local getPlotParts = import(S(601))
local getMinerSlots = import(S(871))(import)
local teleport = import(S(170))
local gameApi = import(S(90))(import)
local log = import(S(30))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(578)
local LOCKED_SLOT_TEXT = S(392)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(125))
    return pad and pad:FindFirstChildWhichIsA(S(475), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(43)) then
            local digits = descendant.Text:match(S(310))
            if digits then
                return tonumber((digits:gsub(S(823), S(73))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(257)) and descendant.Text:match(S(103)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(458) .. holder.Name .. S(419)
            .. gameApi.MinerSlots() .. S(818) .. gameApi.MaxMinerSlots() .. S(110)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(676)
    end
    if not prompt.Enabled then
        return true, S(216)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(514) .. price
    end
    local action = prompt.ActionText or S(627)
    if action:match(S(630)) or action:match(S(594)) or action:match(S(255)) then
        return true, S(221) .. action .. S(100)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(105))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(531) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(312) .. why .. S(808))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(752))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(362) .. prompt.ObjectText .. S(706) .. power .. S(701))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(563))
    if line ~= lastSurvey then
        log(S(770) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(882), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(783), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(499) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(194))
    else
        lastAttempt = 0
        log(S(330))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(453),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(478), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(304))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(579) .. holder.Name .. S(250))
            return false
        end
        log(S(858) .. candidate.slot .. S(189) .. candidate.name
            .. S(371))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(256) .. holder.Name .. S(188) .. miner.name)
        return false
    end
    log(S(29) .. miner.name .. S(845) .. miner.power
        .. S(263) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(390) .. entry.name .. S(762) .. entry.holder.Name
            .. S(9) .. tostring(entry.prompt.ActionText) .. S(329))
        return false
    end
    log(S(4) .. entry.name .. S(444) .. entry.power
        .. S(410) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(275))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(664) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(144) .. target.holder.Name .. S(26) .. target.name
        .. S(513) .. target.power .. S(373) .. best.name .. S(772) .. best.power .. S(716))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(502)] = function()
return function(import)
local gameApi = import(S(27))(import)
local parseMoney = import(S(109))
local log = import(S(553))
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(32) .. (enabled and S(712) or S(123)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(269)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(866) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(393) .. deepestKey .. S(662))
        return true
    end
    log(S(272) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(850) .. gameApi.ZoneDisplayName(zoneKey) .. S(426)
                .. math.floor(price) .. S(160) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(418) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(568) .. zoneKey .. S(246))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(283) then
            reportedLocked = S(379)
            log(S(120) .. zoneKey .. S(533)
                .. gameApi.ZoneDisplayName(currentKey) .. S(132)
                .. done .. S(205) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoPlot
end
end
modules[S(654)] = function()
return function(import)
local gameApi = import(S(480))(import)
local log = import(S(325))
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(505) .. (enabled and S(790) or S(228)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(54) .. node.zoneKey .. S(16) .. node.id
                    .. S(293) .. node.family .. S(777) .. node.level .. S(755) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(69) .. node.id
                    .. S(224) .. node.family .. S(399) .. node.level .. S(394) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(691) .. blocked
                .. S(174))
        elseif powerBlocked then
            log(S(504) .. powerBlocked.zoneKey .. S(423) .. powerBlocked.id
                .. S(278) .. powerBlocked.family .. S(740)
                .. math.floor(powerBlocked.powerReq) .. S(633)
                .. math.floor(powerBlocked.power)
                .. S(136))
        else
            log(S(57)
                .. S(10) .. math.floor(gameApi.GetMoney()) .. S(219))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(157) .. nearest.zoneKey
                    .. S(739) .. nearest.id .. S(315) .. nearest.family .. S(459)
                    .. nearest.level .. S(265) .. nearest.max
                    .. S(31) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(292) .. math.floor(money)
        .. S(521) .. math.floor(budget)
        .. S(784) .. (#zones > 0 and table.concat(zones, S(586)) or S(473))
        .. S(507) .. tostring(buyZones) .. S(605) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(685) .. zoneKey .. S(460) .. tostring(node.id)
                        .. S(441) .. tostring(node.family) .. S(51)
                        .. node.level .. S(11) .. node.max
                        .. S(150) .. tostring(node.open)
                        .. S(638) .. tostring(node.cost)
                        .. S(614) .. tostring(node.powerOk)
                        .. S(303) .. tostring(node.powerReq)
                        .. S(446) .. tostring(node.power))
                end
            end
        end
        log(S(259) .. zoneKey .. S(142) .. #nodes
            .. S(704) .. pending .. S(582))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(623) .. #coreNodes .. S(378)
        .. coreOpen .. S(471))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(161)] = function()
return function(import)
local Workspace = game:GetService(S(625))
local gameApi = import(S(198))(import)
local teleport = import(S(81))
local log = import(S(641))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(91))
    else
        lastAttempt = 0
        log(S(6))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(386))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(385), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(267)
                .. (sold and S(687)
                         or S(809)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(844) .. (sold and (S(558) .. #keys .. S(476)) or S(342)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(411)] = function()
return function(import)
local Workspace = game:GetService(S(462))
local gameApi = import(S(569))(import)
local teleport = import(S(824))
local log = import(S(596))
local STATS = { S(535), S(517), S(183) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(281)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(1) .. (enabled and S(731) or S(139)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(470) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(721) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(488) or S(624)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(717) .. holderKey .. S(395) .. choice.stat .. S(82)
                        .. choice.info.level .. S(268) .. after.level
                        .. S(537) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(680) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(318))
    local shop = stand and stand:FindFirstChild(S(560))
    if not shop then
        return nil
    end
    if shop:IsA(S(185)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(215), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(707)
                .. (bought and S(852)
                           or S(28)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(421))
        end
    end
    if bought then
        log(S(744) .. pickaxe.index .. S(681) .. pickaxe.name
            .. S(179) .. pickaxe.multiplier .. S(49))
        return true
    end
    log(S(420) .. pickaxe.index .. S(548) .. pickaxe.name
        .. S(40))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(769) .. holderKey .. S(834)
                .. tostring(entry and entry.Pickaxe or S(3)) .. S(577) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(542) .. holderKey .. S(113) .. variant
                .. S(229) .. after .. S(516) .. cost .. S(64))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(401) .. gameApi.OwnedPickaxeCount()
                    .. S(83) .. gameApi.MinerSlots()
                    .. S(87))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(564))
        elseif maxed > 0 then
            log(S(349))
        else
            log(S(233))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(622) and entry.Pickaxe and entry.Pickaxe ~= S(333) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(650)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(588))
local Workspace = game:GetService(S(856))
local log = import(S(416))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(567), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(749) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(321))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(604) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(545) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(35) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(735) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(356) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(806))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(403) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(84))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(611), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(812) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(114), keys)
end
function api.EquipSlot(key)
    return send(S(276), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(647), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(573), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(786), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(243), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(859)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(748)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(131))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(874) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(38) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(498), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(413))
    plots = plots and plots:FindFirstChild(S(635))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(583)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(782)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(736))
    local gui = board and board:FindFirstChild(S(44))
    local root = gui and gui:FindFirstChild(S(508))
    local list = root and root:FindFirstChild(S(628))
    local row = list and list:FindFirstChild(S(202) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(237)) and descendant.Text ~= S(450) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(506) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(849), characterKey, stat == S(42) and S(106) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(556), max = S(814), cost = S(456) },
    Speed = { level = S(428), max = S(210), cost = S(566) },
    MineSpeed = { level = S(126), max = S(807), cost = S(825) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(485) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(134) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(33) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(21) and type(holder.CharName) == S(326) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(620) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(616) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(572)] = function()
return function(import)
local Players = game:GetService(S(886))
local log = import(S(8))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(509))
    local mineGui = playerGui and playerGui:FindFirstChild(S(526))
    local hotbar = mineGui and mineGui:FindFirstChild(S(674))
    local content = hotbar and hotbar:FindFirstChild(S(656))
    return content and content:FindFirstChild(S(323))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(867)) then
            local count = descendant.Text:match(S(570))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(667))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(374))
    local nameLabel = content and content:FindFirstChild(S(474))
    local damageLabel = content and content:FindFirstChild(S(285))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(642) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(847), S(741)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(512) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(660) .. slot .. S(427) .. info.name
                        .. S(500) .. info.power .. S(555) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(756)] = function()
local Players = game:GetService(S(593))
local Workspace = game:GetService(S(86))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(92))
    local plots = mineWorld and mineWorld:FindFirstChild(S(651))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(733)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(836)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(780)),
        Nodes = plot:FindFirstChild(S(164)),
        Chest = plot:FindFirstChild(S(452)),
        Zones = plot:FindFirstChild(S(694)),
        Crew = plot:FindFirstChild(S(138)),
        Loot = plot:FindFirstChild(S(494)),
        PlotIndex = plot:GetAttribute(S(884)),
        ChestCapacity = plot:GetAttribute(S(495)),
    }
end
return getPlotParts
end
modules[S(722)] = function()
return function(import)
    local createWindow = import(S(195))(import)
    local i18n = import(S(461))
    local antiAfk = import(S(710))(import)
    local buildEnv = import(S(153))
    local scheduler = import(S(225))(import)
    local config = import(S(203))(import)
    local log = import(S(484))
    log(S(352) .. buildEnv.Commit .. S(380) .. buildEnv.Env
        .. S(415) .. buildEnv.BuiltAt .. S(708))
    local window = createWindow({
        Subtitle = S(239),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(481))(import)
    local autoPlace = import(S(376))(import)
    local autoChest = import(S(617))(import)
    local autoSell = import(S(746))(import)
    local autoUpgrade = import(S(700))(import)
    local autoResearch = import(S(13))(import)
    local autoClaim = import(S(853))(import)
    local autoPlot = import(S(585))(import)
    local gameApi = import(S(464))(import)
    scheduler.RegisterStandalone(S(832), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(41), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(613), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(93), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(872), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(497), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(621), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(266), 20)
    scheduler.RegisterExclusive(S(857), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(208), S(761))
    persistentToggle(farmTab:AddCard(
        S(490),
        S(693)
    ), S(121), autoMine.SetEnabled)
    persistentSlider(farmTab, S(665), S(364),
        1, 30, S(302), 5, autoMine.SetCyclesPerSecond, S(249))
    persistentSlider(farmTab, S(861), S(550),
        1, 10, S(673), 1, autoMine.SetClicksPerCycle, S(397))
    persistentToggle(farmTab:AddCard(
        S(540),
        S(65)
    ), S(606), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(709), S(757),
        1, 60, S(816), 3, autoPlace.SetCheckInterval, S(367))
    persistentToggle(
        farmTab:AddCard(S(383),
            S(172)),
        S(637), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(85),
        S(482)
    ), S(363), autoChest.SetEnabled)
    persistentSlider(farmTab, S(529), S(409),
        50, 3000, S(80), 100, autoChest.SetMinItems, S(869))
    persistentSlider(farmTab, S(214), S(306),
        1, 60, S(192), 3, autoChest.SetCheckInterval, S(102))
    persistentToggle(farmTab:AddCard(
        S(581),
        S(218)
    ), S(574), autoSell.SetEnabled)
    persistentSlider(farmTab, S(129), S(432),
        5, 300, S(781), 20, autoSell.SetCheckInterval, S(212))
    farmTab:AddSlider(S(552),
        S(661),
        5, 120, sellMaxWait, function(value)
            config.Set(S(158), value)
        end, S(828))
    persistentToggle(farmTab:AddCard(
        S(854),
        S(547)
    ), S(602), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(787), S(843),
        5, 300, S(835), 20, autoUpgrade.SetInterval, S(804))
    persistentSlider(farmTab, S(554), S(248),
        1, 20, S(365), 5, autoUpgrade.SetMaxPerPass, S(785))
    persistentSegmented(
        farmTab,
        { S(169), S(245), S(776), S(565) },
        { S(181), S(227), S(649), S(653) },
        S(353), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(742), S(675),
        0, 500, S(448), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(788))
    persistentToggle(
        farmTab:AddCard(S(810),
            S(116)),
        S(644), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(231) .. pickaxe.multiplier .. S(796)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(196)
            end
            return S(827) .. index .. S(455) .. (pickaxeLabels[index] or S(669))
        end
        local savedPick = config.Get(S(287), 0)
        pickaxeCard = farmTab:AddSlider(
            S(831),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(705), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(431)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(17),
            S(668)),
        S(372), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(719),
            S(277)),
        S(345), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(442),
            S(36)),
        S(244), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(434), S(711),
        1, 20, S(143), 2, autoUpgrade.SetMutationTarget, S(589))
    persistentSlider(farmTab, S(876), S(639),
        0, 500, S(417), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(799))
    persistentToggle(farmTab:AddCard(
        S(299),
        S(591)
    ), S(885), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(436), S(75),
        10, 600, S(347), 30, autoResearch.SetCheckInterval, S(97))
    persistentSlider(farmTab, S(201), S(839),
        1, 20, S(96), 5, autoResearch.SetMaxPerPass, S(659))
    persistentSlider(farmTab, S(634), S(238),
        0, 500, S(59), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(794))
    persistentToggle(
        farmTab:AddCard(S(439),
            S(666)),
        S(511), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(388),
            S(725)),
        S(34), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(472),
            S(112)),
        S(46), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(184), S(697) },
        { true, false },
        S(737), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(199),
        S(135)
    ), S(381), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(440), S(643),
        5, 300, S(451), 30, autoClaim.SetCheckInterval, S(640))
    persistentSlider(farmTab, S(699), S(663),
        1, 30, S(463), 10, autoClaim.SetMaxPerPass, S(155))
    persistentToggle(farmTab:AddCard(
        S(262),
        S(124)
    ), S(698), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(408), S(679),
        5, 300, S(193), 30, autoPlot.SetCheckInterval, S(294))
    persistentToggle(
        farmTab:AddCard(S(811),
            S(122)),
        S(242), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(60),
            S(532)),
        S(19), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(327), S(479),
        0, 500, S(311), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(821))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(726))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(608), S(282))
    persistentToggle(
        settingsTab:AddCard(S(76), S(115)),
        S(234),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(539), 100)
    settingsTab:AddSlider(S(223), S(723), 50, 150, savedScale, function(value)
        config.Set(S(841), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(655), 1)
    settingsTab:AddSegmented({ S(341), S(732), S(89) }, savedLanguage, function(index, value)
        local code = ({ S(104), S(778), S(619) })[index]
        config.Set(S(98), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(319), S(430), S(615) })[savedLanguage] or S(360))
    log(S(837) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(180)] = function()
return {
    [79389059854988] = S(141),
}
end
import(S(590))(import)
