-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"auto", "AutoUpgrade", ",", "[ChestDump] === plot ", " (", "core/logger", "costs ", " cards)", "[AutoPlace] ", "nothing to place or upgrade", "open_menu_hint", "HotbarFrame", ")", "เปิดหน้าอัปเกรดของ miner ที่วางอยู่ แล้วซื้อ Damage/Speed/Mining ที่ราคาไม่เกินงบ", "ProximityPrompt", "TextLabel", "Search functions...", "write reported success but the line isn't in ", "Debug: Dump Node/Holder", "%$%s*([%d,%.]+)%s*(%a*)", "/", "Chest", "[AutoChest] chest didn't open, aborting", "[AutoSell] nothing selectable (", "core/teleport", "[AutoPlace] ", "GuiObject", "Auto Research: งบต่อครั้ง", "ปุ่มเปิดเมนู", "games/my_anime_mine/debug_dump", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "core/logger", "Detail", " '", "TextLabel", "UIPadding", "core/click", " items)", "core/click", "games/my_anime_mine/debug_dump_sell_cards", "[AutoSell] clicked SelectAllButton (", "LeftBar", "disabled", "Dump", "autoResearchInterval", "Players", "MekPro", "[Interaction] ClickDetector clicked: ", "autoUpgrade", "[AutoSell] click failed on ", "RemoteFunction", ": ", "TextButton", "[Config] couldn't write ", "core/logger", ": ", "Plots", "SellStand", "$", "Backpack", "อ่านป้ายบน chest ทุกกี่วินาที", "^Node_", "autoMine", "GuiObject", " power ", "[MyAnimeMine] === Dump start ===", "autoMineSpeed", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Auto Upgrade: รอบเช็ค", "Debug: Current Plot", "core/logger", "en", "GuiButton", "StatsPanel", "UICorner", "core/logger", "UpgradeButton", "(", "core/logger", "Frame", "Content", " (", "Badge", "[AutoPlace] placed ", "antiAfk", "games/my_anime_mine/get_best_miner", "Auto Chest: รอบเช็ค", "search_placeholder", "games/my_anime_mine/get_current_plot", "upgrade", "ValueLabel", "s", "PlayerGui", " ===", "games/my_anime_mine/get_plot_parts", "ValueLabel", "[MyAnimeMine] no nodes present right now", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "ClickDetector", "auto", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "ตั้งค่า", "UpgradePart", "Frame", " ===", "%", "Players", "-", "เปิดหน้า Upgrades แล้วซื้อ node ที่ปลดได้ในทุกโซน เรียงจากถูกไปแพง", "games/my_anime_mine/get_current_plot", "autoResearch", "Label", "core/logger", "PlayerGui", "ItemFrame", "search_placeholder", "leaderstats", "Interaction Logger", "Auto Upgrade Unit", "languageIndex", "OwnerUserId", "[ChestDump] === end ===", "core/interaction_logger", "core/ui", "[AutoSell] disabled", "[SellCards] === total cards: ", "MekPro", "Players", "Auto Research: รอบเช็ค", "[AutoSell] clicked SellButton", "autoSell", "ไม่ซื้ออะไรที่แพงกว่านี้", "autoUpgradeInterval", " '", ") -> ", "core/config", "ค้นหาฟังก์ชัน...", " items)", "UserInputService", "Auto Research (Plot)", "Tree", "ValueBase", " = ", "enabled", "ChestFrame", "[AutoResearch] ", "Done", "AutoPlace", "", "th", "Players", "", "Debug: Dump Visible GUI", "", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Auto Mine", ")", "TextButton", "core/click", "Dump", "[AutoResearch] upgrades menu didn't open", "games/my_anime_mine/get_plot_parts", "BuyButton", "ScrollingFrame", "[Scheduler] turn: ", "PlayerGui", "games/my_anime_mine/get_plot_parts", "RunService", "", "[AutoSell] nothing to sell, closing", "Dump", "games/my_anime_mine/main", "core/click", "core/click", "Text", "Content", "Label", "games/my_anime_mine/auto_chest", " = ", "core/logger", "Debug: Dump Backpack Tool", "UserInputService", "core/teleport", "", "[AutoResearch] no BuyButton after selecting ", "Text", "[Interaction] ProximityPrompt triggered: ", ") didn't take, trying the next one", " ===", "s", "menuScale", "TextLabel", "TextLabel", "[AutoChest] Take All had NO effect (", "games/my_anime_mine/get_best_miner", "autoSellInterval", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "unreadable", "=empty", " -> ", " parts", "[ChestDump] === Player subtree ===", "games/my_anime_mine/auto_place", "core/i18n", " still empty after placing ", "UpgradesButton", "ProximityPrompt", "ProximityPrompt", "PlayerGui", "[AutoSell] sell menu didn't open, aborting", "[AutoChest] click failed on ", "ExitButton", "[Scheduler] ", "Done", "games/my_anime_mine/get_current_plot", "Players", " (MaxActivationDistance=%s)", "Dump", "[AutoUpgrade] upgrade screen didn't open for ", "languageIndex", "core/dispatch", "Sidebar", "en", " items", " ", "th", "ProximityPrompt", "waiting", ": ", "", "Frame", "PlotIndex", "TextButton", "SellFrame", "ไทย", "Players", "Auto Upgrade: งบต่อครั้ง", "[AutoUpgrade] ", "Check", " ===", "SkillTreeFullFrame", "Select All", "MineGui", "  [attr] ", "SelectAllButton", "SellButton", "[ChestDump] plot has no Chest", "[AutoPlace] nothing in the hotbar to place", "Canvas", "autoResearchMaxSpend", "games/my_anime_mine/auto_place", "GuiService", "Close", "[Config] couldn't read ", "Content", "enabled", "TakeButton", "Auto Place Miner", "TextButton", "Label", "Frame", "Speed", "ClickDetector", "log failed", "[AutoChest] can't read the chest billboard, is the plot loaded?", "", "X", "HumanoidRootPart", "Content", " doesn't exist", "games/my_anime_mine/get_current_plot", "Badge", "TextButton", "MineGui", "autoPlace", "Error: ", "core/parse_money", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "core/dispatch", "enabled", "games/my_anime_mine/auto_upgrade", "\n", "string", "Nodes", "')", "core/ui", "UIScale", "/s", ": ", "core/anti_afk", "SelectOverlay", "Damage", ") on ", "SellFrame", "open_menu_hint", "My Anime Mine · Mining Hub", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "", "Pad", "  [attr] ", "[AutoPlace] upgrading ", " from ", "$", "ClickDetector", "Card", "MineGui", " cards", "[AutoPlace] drifted out of range of ", "TextLabel", "⚙", " items", "  ", " while selecting", "s", "UIListLayout", "ChestCapacity", "Pips", " (Text=", "core/logger", " (power ", " (Text=", "TextButton", "HttpService", "Progress", "TextButton", ")", "Header", " (prompt says '", "ValueBase", "Players", "Players", "Dump", "VirtualInputManager", "games/my_anime_mine/auto_mine", "s", " (Text=", " unit card(s) Select All skipped", "HumanoidRootPart", "action '", " cards left, label = '", "Content", "Workspace", "CoreUI", "games/my_anime_mine/auto_mine", "Crew", "RemoteFunction", "  ", "disabled", "Content", "[^%d%.]", "core/logger", "Scroll", "", "Frame", "[attr] ", "Actions", "core/parse_money", "ExitButton", "CoreGui", "MekPro.log", "games/my_anime_mine/auto_research", "Auto Sell", "\n", "UIListLayout", "games/registry", "not found", "TextLabel", "MineWorld", "Players", " on ", "TextLabel", "Damage", "Holders", "[SellCards] Card #", "[AutoChest] Take All worked: ", ") for ", "autoChestInterval", "auto", "TextButton", "TextBox", "", "core/i18n", "", "/", "neither appendfile nor writefile is available", "Sell for", "Scroll", "Auto Chest", "Frame", "ReplicatedStorage", "ClickDetector", "ดีบัก", "[ChestDump] not standing in any plot", "[AutoPlace] fireproximityprompt error: ", "prompt disabled", "ProximityPrompt", "[AutoSell] enabled (only sells when there's something in the scroll)", " x", " items, going to collect", "Cost", "TextLabel", " '", "[MyAnimeMine] === Tool: ", "[GuiDump] === visible MineGui tree ===", "games/my_anime_mine/auto_upgrade", "TextLabel", ",", ")", "Debug: Dump Sell Cards", "Search", "waiting", "TextButton", "fill", "Container", "Content", "SlotFocusFrame", "Auto Mine: คลิกต่อรอบ", "games/my_anime_mine/auto_research", " <-- remote, likely fired by the real interaction", " '", "[AutoChest] disabled", "Scroll", "Players", "[Bb]uy", "[Interaction] logger enabled - go click/interact with things now", "[MyAnimeMine] not standing in any plot", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "BottomPanels", "[AutoPlace] disabled", "[AutoChest] ", "  ", "Frame", "^(%d+)", "games/my_anime_mine/auto_sell", "Zones", "games/my_anime_mine/debug_dump_chest", "[AutoPlace] couldn't pick up ", "Players", "'", "games/my_anime_mine/debug_dump_chest", "games/my_anime_mine/get_current_plot", "[Remotes] === total: ", "core/click", "games/my_anime_mine/debug_dump_remotes", "", "TextLabel", "", "อังกฤษ", "", "en", "table", "?", "Sell for", "Max", "Content", " ===", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "write reported success but ", "UIStroke", "Players", "[ChestDump] === Chest subtree ===", "", "Debug: Dump All Remotes", "Label", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/get_place_id", "ProximityPrompt", "Auto Chest: เกณฑ์เก็บ", "Locked miner slot", "TextLabel", "", "[MyAnimeMine] === Holder: ", "games/my_anime_mine/debug_dump_sell_cards", " attributes ===", "MineGui", "Amount", "Players", ",", "", "no isfile/readfile available to verify the write", "'", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "games/my_anime_mine/debug_dump", "", "core/get_place_id", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "games/my_anime_mine/debug_dump_gui", "This is not a supported place.", "", " '", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "Frame", "core/interaction_logger", "Players", "Dump", "Close", "TextButton", "core/scheduler", "Auto Place: รอบเช็ค", "core/logger", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Auto Mine: ความเร็ว", "core/logger", "^([%d,]+)$", " ชิ้น", "", "th", "no plot", "", " = ", "[%X] ", " (", "[MyAnimeMine] === Node: ", "th", "TextLabel", "Lock", ": ", "ItemName", "games/my_anime_mine/get_plot_parts", "'", "  [attr] ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Frame", "games/my_anime_mine/get_plot_parts", " error: ", ")", "Content", "[AutoResearch] bought ", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/get_current_plot", "Content", " | ", "UIListLayout", "=locked(", "/config.json", "Players", "Open menu", "[Interaction] Key pressed: %s (gameProcessed=%s)", "core/logger", " for ", "ฟาร์ม", "ตามเครื่อง", "🏠", ": ", "core/anti_afk", "TextLabel", "Container", ") from ", "GuiButton", "TextButton", " (ActionText=", "'", "Place", "[AntiAFK] idle detected, simulating input", "TextLabel", "[Logger] failed to write ", "List", "'), backing off ", "core/i18n", "[AntiAFK] disabled", "TextButton", "MineGui", "(nothing to do)", "ย่อขยายเมนูทั้งชุด", "games/my_anime_mine/auto_chest", "'", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "Main", "Label", ": ", "s", "games/my_anime_mine/auto_sell", ": ", "no tool", "core/teleport", "Content", "Workspace", "เปิดหน้า research ทุกกี่วินาที", "games/my_anime_mine/get_plot_parts", "", "Content", "core/logger", " (power ", "  ", " = ", "core/logger", " remotes", "Empty miner slot", "[AutoSell] fireproximityprompt error: ", "  ", " = ", "autoPlaceInterval", "[AutoUpgrade] bought ", "Content", "core/logger", ": ", "  [attr] ", ")", "🐛", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Card", "MineGui", "", "MineGui", " = ", "[AutoMine] ", "button has no size on screen", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", " = ", "Backpack", "[BestMiner] slot ", "=", "Frame", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "RemoteEvent", "disabled", "PlayerGui", "auto", "autoMineClicks", "[AutoPlace] holders: ", "games/my_anime_mine/get_current_plot", "core/logger", "games/my_anime_mine/main", "core/get_leaderstat", "Card", "VirtualUser", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "menuScale", "en-us", "Content", "[AutoPlace] slot ", "unreadable", "AutoResearch", "Loot", "[ChestDump] === ReplicatedStorage.", "[AutoPlace] picked up ", "[AntiAFK] enabled - watching Player.Idled", "AutoMine", "games/my_anime_mine/debug_dump_gui", "ReplicatedStorage", "Frame", "Scroll", "interactionLogger", "^x(%d+)$", "ScreenGui", "Mining", "ขนาดเมนู", "TextLabel", "Debug: Dump Chest Data", "x", "Close", "autoChestMinItems", "Content", "Humanoid", "Anti-AFK", "Players", "[MyAnimeMine] no holders found", "[Interaction] logger disabled", "[AutoResearch] nothing affordable under the spend cap", "PlayerGui", "[Uu]nlock", "[AutoChest] fireproximityprompt error: ", "?", "  ", "[GuiDump] === end ===", "Auto Sell: รอบขาย", "games/registry", " ===", "core/teleport", "ไม่ซื้อ node ที่แพงกว่านี้", "[AutoChest] waiting: ", "TabBar", "Tool", "TextLabel", "s", "core/config", "[AutoChest] enabled (collects once >= ", "locked slot", "core/scheduler", "TextLabel", "[MekPro] settings file: ", "", "'", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "/", "[AutoSell] also selected ", "Workspace", "PlayerGui", "ClickDetector", ")", "ไล่อัปเกรด miner ทุกกี่วินาที", "Done", "[GuiDump] MineGui not found", "UICorner", "^%$", "core/get_me", "fill", "[Remotes] ", "AutoSell", "PlayerGui", "Content", "%$", "autoUpgradeMaxSpend", " -> ", " (", "core/parse_money", "[Interaction] Button activated: ", "AutoChest", "core/get_place_id", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "autoChest", "", "games/my_anime_mine/debug_dump_remotes", "RemoteEvent"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(290)] = function()
return function(import)
local log = import(S(319))
local Players = game:GetService(S(369))
local VirtualUser = game:GetService(S(626))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(637))
        connection = player.Idled:Connect(function()
            log(S(554))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(560))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(37)] = function()
local VirtualInputManager = game:GetService(S(333))
local GuiService = game:GetService(S(252))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(607)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(136)] = function()
return function(import)
local HttpService = game:GetService(S(323))
local getPlaceId = import(S(466))
local log = import(S(55))
local ROOT_FOLDER = S(127)
local placeFolder = ROOT_FOLDER .. S(685) .. tostring(getPlaceId())
local filePath = placeFolder .. S(535)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(451) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(254) .. filePath .. S(56) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(54) .. filePath .. S(544) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(279)] = function()
return function(import)
    local getPlaceId = import(S(485))
    local registry = import(S(667))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(488))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(624)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(117))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(696)] = function()
local Players = game:GetService(S(656))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(654))
    local backpack = player:WaitForChild(S(60))
    return player, humanoid, backpack
end
return getMe
end
modules[S(709)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(382)] = function()
local Players = game:GetService(S(128))
local STRINGS = {
    en = {
        search_placeholder = S(17),
        open_menu_hint = S(537),
    },
    th = {
        search_placeholder = S(137),
        open_menu_hint = S(29),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(629)
    if locale:sub(1, 2):lower() == S(150) then
        return S(514)
    end
    return S(223)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(100) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(493)] = function()
return function(import)
local Players = game:GetService(S(477))
local Workspace = game:GetService(S(687))
local UserInputService = game:GetService(S(182))
local log = import(S(32))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(187) .. describe(prompt)
            .. S(551) .. prompt.ActionText .. S(408))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(48) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(707) .. describe(button)
            .. S(318) .. tostring(button.Text) .. S(157))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(227)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(304)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(549)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(658))
        return
    end
    connections = {}
    log(S(425))
    local playerGui = Players.LocalPlayer:WaitForChild(S(114))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(207)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(99)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(73)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(538),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(71)] = function()
local LOG_FILE = S(360)
local function log(message)
    local line = os.date(S(511)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(152))
            end
            appendfile(LOG_FILE, line .. S(282))
        elseif writefile then
            local existing = S(479)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(363))
        else
            error(S(385))
        end
        if not (isfile and readfile) then
            error(S(480))
        end
        if not isfile(LOG_FILE) then
            error(S(458) .. LOG_FILE .. S(270))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(18) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(556) .. LOG_FILE .. S(517) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(277)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(283) then
        return nil
    end
    local number, suffix = text:match(S(20))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(3), S(506))))
    if not value then
        return nil
    end
    if suffix ~= S(169) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(679)] = function()
return function(import)
local RunService = game:GetService(S(168))
local log = import(S(622))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(213) .. task_.name .. S(525) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(165) .. (currentTask and currentTask.name or S(563)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(669)] = function()
local Players = game:GetService(S(438))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(268))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(124)] = function()
return function(import)
local i18n = import(S(559))
local UserInputService = game:GetService(S(139))
local THEME = {
    BrandName = S(47),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(75))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(694))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(36))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(550))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(462)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(459))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(367))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(452)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(680))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(580)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(164))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(315))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(555))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(231))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(546))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(674))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(192))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(96))
            if not valueLbl then
                valueLbl = Instance.new(S(648))
                valueLbl.Name = S(91)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(83))
            if not badge then
                badge = Instance.new(S(446))
                badge.Name = S(272)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(325))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(80))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(613))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(259))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(383)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(106)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(492))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(523))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(233))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(603)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(432))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(364))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(322))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(175)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(645))
    screenGui.Name = S(343)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(359))
    local main = Instance.new(S(389))
    main.Name = S(568)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(287))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(641))
    tabBar.Name = S(672)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(533))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(354))
    header.Name = S(327)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(412))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(470))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(193))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(449)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(158))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(267)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(497))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(108)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(380))
    searchBox.Name = S(410)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(116))
    searchBox.Text = S(184)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(261))
    content.Name = S(630)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(104))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(35))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(295))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(309))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(186)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(353) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(225) .. (item.Subtitle or S(489))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(88))
        footerLabel.Text = i18n.T(S(11))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(178)] = function()
return function(import)
local Players = game:GetService(S(107))
local getCurrentPlot = import(S(271))
local getPlotParts = import(S(167))
local teleport = import(S(575))
local click = import(S(39))
local log = import(S(586))
local checkInterval = 3
local minItemsToCollect = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(617))
    local mineGui = playerGui and playerGui:FindFirstChild(S(475))
    return mineGui and mineGui:FindFirstChild(S(145))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(81))
    local actions = content and content:FindFirstChild(S(356))
    return actions and actions:FindFirstChild(S(257))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(407), S(298)):match(S(433))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(476), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(594))
    local progress = content and content:FindFirstChild(S(324))
    local label = progress and progress:FindFirstChild(S(569))
    return parseCount(label and label.Text or S(471))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(211) .. button.Name .. S(229) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(396), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(421))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(677) .. minItemsToCollect .. S(38))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local cachedCount, cachedAt = nil, 0
function autoChest.HasWork()
    local now = tick()
    if now - cachedAt > 1 then
        cachedCount = getChestCount()
        cachedAt = now
    end
    return cachedCount ~= nil and cachedCount >= minItemsToCollect
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(662) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(23))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(375) .. count .. S(704) .. after .. S(311))
        else
            log(S(194) .. tostring(count)
                .. S(200) .. tostring(after) .. S(138))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(198) then
            log(S(265))
            lastReportedCount = S(632)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReportedCount ~= S(228) then
            log(S(671) .. count .. S(384) .. minItemsToCollect .. S(224))
            lastReportedCount = S(411)
        end
        return
    end
    lastReportedCount = nil
    log(S(430) .. count .. S(399))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(344)] = function()
return function(import)
local getCurrentPlot = import(S(621))
local getPlotParts = import(S(529))
local log = import(S(582))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(689), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(606) .. (enabled and S(144) or S(616)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(251)] = function()
return function(import)
local Players = game:GetService(S(494))
local getCurrentPlot = import(S(530))
local getPlotParts = import(S(162))
local getMinerSlots = import(S(195))(import)
local teleport = import(S(25))
local click = import(S(443))
local log = import(S(76))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(588)
local LOCKED_SLOT_TEXT = S(469)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(299))
    return pad and pad:FindFirstChildWhichIsA(S(467), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(16)) then
            local digits = descendant.Text:match(S(504))
            if digits then
                return tonumber((digits:gsub(S(478), S(445))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(515)) and descendant.Text:match(S(695)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(678)
    end
    if not prompt.Enabled then
        return true, S(395)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(7) .. price
    end
    local action = prompt.ActionText or S(154)
    if action:match(S(661)) or action:match(S(424)) or action:match(S(702)) then
        return true, S(339) .. action .. S(481)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(338))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(394) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(534) .. why .. S(13))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(199))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(612) .. prompt.ObjectText .. S(78) .. power .. S(598))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(532))
    if line ~= lastSurvey then
        log(S(620) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(413), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(90), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(26) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(429))
    else
        lastAttempt = 0
        log(S(98))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(491),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(553), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(248))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(308) .. holder.Name .. S(313))
            return false
        end
        log(S(631) .. candidate.slot .. S(5) .. candidate.name
            .. S(188))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(9) .. holder.Name .. S(205) .. miner.name)
        return false
    end
    log(S(84) .. miner.name .. S(320) .. miner.power
        .. S(293) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(437) .. entry.name .. S(302) .. entry.holder.Name
            .. S(328) .. tostring(entry.prompt.ActionText) .. S(285))
        return false
    end
    log(S(636) .. entry.name .. S(583) .. entry.power
        .. S(548) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(10))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(697) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(301) .. target.holder.Name .. S(573) .. target.name
        .. S(705) .. target.power .. S(135) .. best.name .. S(512) .. best.power .. S(326))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(361)] = function()
return function(import)
local Players = game:GetService(S(331))
local parseMoney = import(S(706))
local click = import(S(174))
local log = import(S(500))
local checkInterval = 30
local maxSpend = 100000
local function getMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(166))
    return playerGui and playerGui:FindFirstChild(S(602))
end
local function getTreeFrame()
    local mineGui = getMineGui()
    return mineGui and mineGui:FindFirstChild(S(241))
end
local function getUpgradesButton()
    local mineGui = getMineGui()
    local leftBar = mineGui and mineGui:FindFirstChild(S(42))
    local content = leftBar and leftBar:FindFirstChild(S(581))
    local container = content and content:FindFirstChild(S(547))
    return container and container:FindFirstChild(S(206))
end
local function zoneTabs(frame)
    local sidebar = frame:FindFirstChild(S(222))
    local list = sidebar and sidebar:FindFirstChild(S(557))
    if not list then
        return {}
    end
    local tabs = {}
    for _, tab in ipairs(list:GetChildren()) do
        if tab:IsA(S(379)) then
            local content = tab:FindFirstChild(S(255))
            local locked = content and content:FindFirstChild(S(516))
            if not locked then
                local label = content and content:FindFirstChild(S(112))
                table.insert(tabs, {
                    button = tab,
                    name = (label and label.Text ~= S(230) and label.Text) or tab.Name,
                })
            end
        end
    end
    return tabs
end
local function affordableNodes(frame)
    local tree = frame:FindFirstChild(S(141))
    local canvas = tree and tree:FindFirstChild(S(249))
    if not canvas then
        return {}
    end
    local nodes = {}
    for _, node in ipairs(canvas:GetChildren()) do
        if node:IsA(S(561)) and node.Name:match(S(62)) then
            local content = node:FindFirstChild(S(531))
            local cost = content and content:FindFirstChild(S(400))
            local price = cost and parseMoney(cost.Text)
            if price and price <= maxSpend then
                local pips = content:FindFirstChild(S(317))
                table.insert(nodes, {
                    button = node,
                    name = node.Name:sub(6),
                    price = price,
                    priceText = cost.Text,
                    pips = pips and pips.Text or S(663),
                })
            end
        end
    end
    table.sort(nodes, function(a, b)
        return a.price < b.price
    end)
    return nodes
end
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(146) .. (enabled and S(256) or S(43)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyNode(frame, node)
    click.Real(node.button)
    task.wait(0.4)
    local detail = frame:FindFirstChild(S(33))
    local buyButton = detail and detail:FindFirstChild(S(163))
    if not buyButton then
        log(S(185) .. node.name)
        return false
    end
    local content = buyButton:FindFirstChild(S(576))
    local label = content and content:FindFirstChild(S(260))
    if label and label.Text == S(454) then
        return false
    end
    click.Real(buyButton)
    task.wait(0.5)
    log(S(528) .. node.name .. S(82) .. node.pips .. S(376) .. node.priceText)
    return true
end
local function runJob()
    busy = true
    local button = getUpgradesButton()
    if not button then
        busy = false
        return
    end
    click.Real(button)
    task.wait(0.8)
    local frame = getTreeFrame()
    if not (frame and frame.Visible) then
        log(S(161))
        busy = false
        return
    end
    local bought = 0
    for _, tab in ipairs(zoneTabs(frame)) do
        if not enabled then
            break
        end
        click.Real(tab.button)
        task.wait(0.5)
        for _, node in ipairs(affordableNodes(frame)) do
            if not enabled then
                break
            end
            if buyNode(frame, node) then
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(659))
    end
    local close = frame:FindFirstChild(S(651)) or frame:FindFirstChild(S(358))
    if close then
        click.Real(close)
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(434)] = function()
return function(import)
local Players = game:GetService(S(536))
local Workspace = game:GetService(S(342))
local teleport = import(S(183))
local click = import(S(173))
local log = import(S(351))
local checkInterval = 45
local nothingSellableCooldown = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(209))
    local mineGui = playerGui and playerGui:FindFirstChild(S(604))
    return mineGui and mineGui:FindFirstChild(S(234))
end
local function labelText(button)
    local content = button:FindFirstChild(S(455))
    local label = content and content:FindFirstChild(S(464))
    return label and label.Text or S(447)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(653))
    local scroll = content and content:FindFirstChild(S(422))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(305) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(50) .. button.Name .. S(596) .. tostring(err))
    end
    return ok
end
local function isCardSelected(card)
    local content = card:FindFirstChild(S(527))
    local overlay = content and content:FindFirstChild(S(291))
    return overlay ~= nil and overlay.Visible
end
local function isOnScreen(card, scroll)
    local top = scroll.AbsolutePosition.Y
    local bottom = top + scroll.AbsoluteSize.Y
    local cardTop = card.AbsolutePosition.Y
    local cardBottom = cardTop + card.AbsoluteSize.Y
    return cardTop >= top and cardBottom <= bottom
end
local function selectUnselectedCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(415))
    local scroll = content and content:FindFirstChild(S(352))
    if not scroll then
        return 0
    end
    local selected = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(625) and not isCardSelected(card) and isOnScreen(card, scroll) then
            clickButton(card)
            selected = selected + 1
            task.wait(0.1)
        end
    end
    return selected
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(58))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(208), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(125))
    else
        lastAttempt = 0
        log(S(397))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(589) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(210))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(170))
        local closeButton = sellFrame:FindFirstChild(S(253))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(269))
    local selectAllButton = content and content:FindFirstChild(S(245))
    local sellButton = content and content:FindFirstChild(S(246))
    if selectAllButton and labelText(selectAllButton) == S(242) then
        clickButton(selectAllButton)
        log(S(41) .. cardCount .. S(8))
        task.wait(0.2)
    end
    local selected = selectUnselectedCards(sellFrame)
    if selected > 0 then
        log(S(686) .. selected .. S(337))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(453), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(386), 1, true) then
        clickButton(sellButton)
        log(S(130))
    else
        lastAttempt = tick() + nothingSellableCooldown - checkInterval
        local closeButton = sellFrame:FindFirstChild(S(496))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(24) .. cardCount .. S(340)
            .. tostring(sawLabel) .. S(558) .. nothingSellableCooldown .. S(571))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(281)] = function()
return function(import)
local Players = game:GetService(S(423))
local getCurrentPlot = import(S(110))
local getPlotParts = import(S(579))
local parseMoney = import(S(357))
local click = import(S(159))
local log = import(S(79))
local STAT_NAMES = { S(292), S(262), S(646) }
local checkInterval = 20
local maxSpend = 100000
local function getFocusFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(660))
    local mineGui = playerGui and playerGui:FindFirstChild(S(306))
    return mineGui and mineGui:FindFirstChild(S(416))
end
local function findUpgradeClick(holder)
    local upgradePart = holder:FindFirstChild(S(103))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(263))
end
local function occupiedHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return {}
    end
    local found = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClick(holder)
        if detector then
            table.insert(found, { holder = holder, detector = detector })
        end
    end
    return found
end
local function readStat(scroll, statName)
    local row = scroll:FindFirstChild(statName)
    local button = row and row:FindFirstChild(S(77))
    local content = button and button:FindFirstChild(S(701))
    local label = content and content:FindFirstChild(S(177))
    if not label then
        return nil
    end
    return {
        name = statName,
        button = button,
        price = parseMoney(label.Text),
        text = label.Text,
    }
end
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(238) .. (enabled and S(280) or S(348)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyAffordableStats(entry)
    local frame = getFocusFrame()
    local panels = frame and frame:FindFirstChild(S(428))
    local statsPanel = panels and panels:FindFirstChild(S(74))
    local scroll = statsPanel and statsPanel:FindFirstChild(S(642))
    if not scroll then
        log(S(219) .. entry.holder.Name)
        return 0
    end
    local bought = 0
    for _ = 1, #STAT_NAMES do
        local cheapest = nil
        for _, statName in ipairs(STAT_NAMES) do
            local stat = readStat(scroll, statName)
            if stat and stat.price and stat.price <= maxSpend then
                if not cheapest or stat.price < cheapest.price then
                    cheapest = stat
                end
            end
        end
        if not cheapest then
            break
        end
        click.Real(cheapest.button)
        log(S(593) .. cheapest.name .. S(540) .. cheapest.text
            .. S(370) .. entry.holder.Name)
        bought = bought + 1
        task.wait(0.4)
    end
    return bought
end
local function closeFrame()
    local frame = getFocusFrame()
    local exit = frame and frame:FindFirstChild(S(212))
    if exit then
        click.Real(exit)
        task.wait(0.3)
    end
end
local function runJob()
    busy = true
    for _, entry in ipairs(occupiedHolders()) do
        if not enabled then
            break
        end
        fireclickdetector(entry.detector)
        task.wait(0.8)
        local frame = getFocusFrame()
        if frame and frame.Visible then
            buyAffordableStats(entry)
            closeFrame()
        end
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(483)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(712)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(402) .. child.Name .. S(552)
        if child:IsA(S(15)) then
            line = line .. string.format(
                S(68),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(391)) then
            line = line .. string.format(S(217), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(615)) or child:IsA(S(51)) then
            line = line .. S(419)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(521) .. attrName .. S(591) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(312))
    end
end
return dumpInstance
end
modules[S(436)] = function()
return function(import)
local Players = game:GetService(S(236))
local ReplicatedStorage = game:GetService(S(640))
local getCurrentPlot = import(S(441))
local getPlotParts = import(S(519))
local log = import(S(180))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(355) .. name .. S(609) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(347))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(134) .. child.Name .. S(683)
            if child:IsA(S(329)) then
                line = line .. S(585) .. tostring(child.Value)
            elseif child:IsA(S(401)) or child:IsA(S(273)) then
                line = line .. S(321) .. tostring(child.Text) .. S(690)
            end
            log(line)
            dumpSubtree(child, indent .. S(431))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(393))
        return 0
    end
    log(S(4) .. plot.Name .. S(474))
    dumpAttributes(plot, S(664))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(247))
        return 0
    end
    log(S(461))
    dumpSubtree(parts.Chest, S(266))
    local player = Players.LocalPlayer
    log(S(202))
    dumpSubtree(player, S(149))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(635) .. player.Name .. S(456))
        dumpSubtree(mine, S(381))
    end
    log(S(122))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(639)] = function()
return function(import)
local Players = game:GetService(S(46))
local log = import(S(595))
local function dumpVisible(root, indent)
    indent = indent or S(682)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(27)) and child.Visible then
            local line = indent .. child.ClassName .. S(490) .. child.Name .. S(566)
            if child:IsA(S(406)) or child:IsA(S(53)) then
                line = line .. S(336) .. tostring(child.Text) .. S(526)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(244) .. attrName .. S(510) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(590))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(688))
    local mineGui = playerGui and playerGui:FindFirstChild(S(243))
    if not mineGui then
        log(S(693))
        return
    end
    log(S(404))
    dumpVisible(mineGui)
    log(S(665))
end
return dumpMineGui
end
end
modules[S(444)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(390))
local log = import(S(6))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(714)) or descendant:IsA(S(346)) then
            count = count + 1
            log(S(698) .. descendant.ClassName .. S(289) .. descendant:GetFullName())
        end
    end
    log(S(442) .. count .. S(94))
    return count
end
return dumpRemotes
end
end
modules[S(473)] = function()
return function(import)
local Players = game:GetService(S(151))
local log = import(S(539))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(700))
    local mineGui = playerGui and playerGui:FindFirstChild(S(562))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(294))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(349)) and sellFrame.Content:FindFirstChild(S(387))
    if not scroll then
        log(S(501))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(601) or card:IsA(S(64)) then
            count = count + 1
            log(S(374) .. count .. S(52) .. card.ClassName .. S(34) .. card.Name .. S(520))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(597) .. attrName .. S(143) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(584) .. child.ClassName .. S(420) .. child.Name .. S(439)
                if child:IsA(S(142)) then
                    line = line .. S(605) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(126) .. count .. S(668))
    return count
end
return dumpSellCards
end
end
modules[S(86)] = function()
return function(import)
local Players = game:GetService(S(460))
local log = import(S(503))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(93))
    local mineGui = playerGui and playerGui:FindFirstChild(S(274))
    local hotbar = mineGui and mineGui:FindFirstChild(S(12))
    local content = hotbar and hotbar:FindFirstChild(S(341))
    return content and content:FindFirstChild(S(414))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(371)) then
            local count = descendant.Text:match(S(644))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(115))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(176))
    local nameLabel = content and content:FindFirstChild(S(518))
    local damageLabel = content and content:FindFirstChild(S(372))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(484) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(350), S(509)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(21) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(611) .. slot .. S(570) .. info.name
                        .. S(65) .. info.power .. S(398) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(215)] = function()
local Players = game:GetService(S(216))
local Workspace = game:GetService(S(577))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(368))
    local plots = mineWorld and mineWorld:FindFirstChild(S(57))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(121)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(95)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(373)),
        Nodes = plot:FindFirstChild(S(284)),
        Chest = plot:FindFirstChild(S(22)),
        Zones = plot:FindFirstChild(S(435)),
        Crew = plot:FindFirstChild(S(345)),
        Loot = plot:FindFirstChild(S(634)),
        PlotIndex = plot:GetAttribute(S(232)),
        ChestCapacity = plot:GetAttribute(S(316)),
    }
end
return getPlotParts
end
modules[S(172)] = function()
return function(import)
    local Players = game:GetService(S(330))
    local createWindow = import(S(286))(import)
    local i18n = import(S(204))
    local antiAfk = import(S(545))(import)
    local interactionLogger = import(S(123))(import)
    local scheduler = import(S(498))(import)
    local config = import(S(676))(import)
    local log = import(S(113))
    local window = createWindow({
        Subtitle = S(296),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(89))
    local getPlotParts = import(S(524))
    local dumpInstance = import(S(30))
    local dumpRemotes = import(S(713))(import)
    local dumpSellCards = import(S(40))(import)
    local dumpGui = import(S(487))(import)
    local dumpChest = import(S(440))(import)
    local autoMine = import(S(334))(import)
    local autoPlace = import(S(203))(import)
    local autoChest = import(S(565))(import)
    local autoSell = import(S(572))(import)
    local autoUpgrade = import(S(405))(import)
    local autoResearch = import(S(418))(import)
    scheduler.RegisterStandalone(S(638), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(708), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(148), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(2), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(633), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(699), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(541), S(543))
    persistentToggle(farmTab:AddCard(
        S(156),
        S(522)
    ), S(63), autoMine.SetEnabled)
    persistentSlider(farmTab, S(502), S(608),
        1, 30, S(67), 5, autoMine.SetCyclesPerSecond, S(288))
    persistentSlider(farmTab, S(417), S(627),
        1, 10, S(619), 1, autoMine.SetClicksPerCycle, S(650))
    persistentToggle(farmTab:AddCard(
        S(258),
        S(465)
    ), S(275), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(499), S(684),
        1, 60, S(592), 3, autoPlace.SetCheckInterval, S(314))
    persistentToggle(farmTab:AddCard(
        S(388),
        S(278)
    ), S(711), autoChest.SetEnabled)
    persistentSlider(farmTab, S(468), S(197),
        50, 3000, S(652), 100, autoChest.SetMinItems, S(505))
    persistentSlider(farmTab, S(87), S(61),
        1, 60, S(377), 3, autoChest.SetCheckInterval, S(335))
    persistentToggle(farmTab:AddCard(
        S(362),
        S(101)
    ), S(131), autoSell.SetEnabled)
    persistentSlider(farmTab, S(666), S(614),
        10, 300, S(196), 45, autoSell.SetCheckInterval, S(190))
    persistentToggle(farmTab:AddCard(
        S(119),
        S(14)
    ), S(49), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(69), S(691),
        5, 300, S(133), 20, autoUpgrade.SetInterval, S(92))
    persistentSlider(farmTab, S(237), S(132),
        1000, 1000000, S(703), 100000, autoUpgrade.SetMaxSpend, S(59))
    persistentToggle(farmTab:AddCard(
        S(140),
        S(109)
    ), S(111), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(129), S(578),
        10, 600, S(45), 30, autoResearch.SetCheckInterval, S(675))
    persistentSlider(farmTab, S(28), S(670),
        1000, 1000000, S(250), 100000, autoResearch.SetMaxSpend, S(303))
    local debugTab = window:AddTab(S(392), S(599))
    persistentToggle(
        debugTab:AddCard(
            S(118),
            S(486)
        ),
        S(643),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(70), S(155))
    local checkBtn
    checkBtn = debugCard:AddButton(S(239), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(366)
            log(S(426))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(427),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(19),
        S(482)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(171), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(508)
            return
        end
        local ok, err = log(S(66))
        if not ok then
            dumpBtn.Text = S(264)
            dumpCard:SetSubtitle(S(276) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(513) .. firstNode.Name .. S(240))
            dumpInstance(firstNode, log)
        else
            log(S(97))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(472) .. firstHolder.Name .. S(189))
            dumpInstance(firstHolder, log)
        else
            log(S(657))
        end
        dumpBtn.Text = S(214)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(181),
        S(31)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(332), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(610))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(673))
        if not tool then
            toolDumpBtn.Text = S(574)
            return
        end
        log(S(403) .. tool.Name .. S(105))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(300) .. attrName .. S(179) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(147)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(463),
        S(567)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(218), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(587)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(409),
        S(457)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(44), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(307)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(153),
        S(710)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(495), function()
        dumpGui()
        guiDumpBtn.Text = S(692)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(649),
        S(297)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(160), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(201)
    end)
    local settingsTab = window:AddTab(S(102), S(310))
    persistentToggle(
        settingsTab:AddCard(S(655), S(600)),
        S(85),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(191), 100)
    settingsTab:AddSlider(S(647), S(564), 50, 150, savedScale, function(value)
        config.Set(S(628), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(120), 1)
    settingsTab:AddSegmented({ S(542), S(448), S(235) }, savedLanguage, function(index, value)
        local code = ({ S(1), S(72), S(226) })[index]
        config.Set(S(220), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(618), S(450), S(507) })[savedLanguage] or S(378))
    log(S(681) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
    end)
end
end
modules[S(365)] = function()
return {
    [79389059854988] = S(623),
}
end
import(S(221))(import)
