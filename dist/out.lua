-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"RemoteFunction", "  ", "Close", "HumanoidRootPart", "Dump", "TextLabel", " error: ", "%$%s*([%d,%.]+)%s*(%a*)", "[GameAPI] SendEvent failed: ", "Pips", "Dump", "games/my_anime_mine/get_current_plot", "enabled", "ValueBase", "HttpService", "[AutoResearch] no BuyButton after selecting ", " | ", "autoPlaceInterval", "/s", "Players", "autoPlace", ": ", "Container", " power ", "Content", ")", "  [upvalue] ", "%$", "SellStand", ",", "games/my_anime_mine/get_current_plot", "core/i18n", "Auto Research: รอบเช็ค", "[Probe] ", "", "Frame", "SellFrame", "Content", "Content", "Players", "Auto Chest", "TextLabel", "Pad", "ขนาดเมนู", "", "[SellCards] Card #", "MineSkillTreeClient", "[AutoChest] enabled (collects once >= ", "s", ":", "", "UIListLayout", "[AutoSell] nothing selectable (", "TextButton", ")", "[AutoSell] clicked SelectAllButton (", "Players", "[Probe] ", "[Probe] PlayerScripts.UI.MineGame not found", "[Logger] failed to write ", "core/teleport", "UICorner", "games/my_anime_mine/auto_mine", "[Config] couldn't read ", "write reported success but ", "getloadedmodules", " <-- remote, likely fired by the real interaction", "Players", "function", "Auto Sell", "  [attr] ", "search_placeholder", "Done", "Label", "[MyAnimeMine] === Holder: ", "[Probe] === end ===", "MouseButton1Click", "[AutoPlace] upgrading ", "autoUpgradeInterval", "Content", " (ActionText=", "Text", "[Remotes] ", "Anti-AFK", "core/click", "GuiButton", "[Scheduler] ", "LeftBar", "UICorner", "Content", "games/my_anime_mine/auto_place", "games/my_anime_mine/auto_mine", "=", "", "')", "เปิดหน้า Upgrades แล้วซื้อ node ที่ปลดได้ในทุกโซน เรียงจากถูกไปแพง", "PlayerGui", "ดีบัก", "  ", "core/logger", "games/my_anime_mine/game_api", "TextLabel", "nothing to place or upgrade", "core/get_place_id", "Actions", "Canvas", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "PlayerGui", "Label", "Sell for", "games/my_anime_mine/get_current_plot", "core/logger", "[^%d%.]", "autoSell", "[AutoChest] disabled", "[AutoPlace] ", " for ", "MineSellItems", "ProximityPrompt", "table", "", "AutoMine", "core/click", " = ", "List", ")", " chars) -> ", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "games/my_anime_mine/main", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/get_plot_parts", "[AutoResearch] upgrades menu didn't open", "Folder", "AutoPlace", "Content", "(", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[AutoSell] click failed on ", "ValueLabel", "table", "[Handlers] === start (open the relevant menus first) ===", "core/logger", "string", "Empty miner slot", "Auto Upgrade: รอบเช็ค", "TextButton", "[Interaction] logger enabled - go click/interact with things now", " = ", "Open menu", "Probe", "games/my_anime_mine/debug_dump_chest", "Auto Upgrade Unit", "en", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "games/my_anime_mine/debug_dump_gui", "[SellCards] === total cards: ", "BottomPanels", "Damage", "[AutoChest] waiting: ", "Auto Place: รอบเช็ค", "Dump", "My Anime Mine · Mining Hub", " = ", ")", "MineGui", "MineTakeChest", "Auto Mine", "ReplicatedStorage", " (power ", "auto", "^x(%d+)$", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "TextLabel", "Detail", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "BuyButton", "MineChestClient", "TextLabel", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Players", "string", "debug.getconstants", "UIStroke", "TextButton", "[Handlers] === ", ": ", "s", "[Handlers]   #", "\n", "$", "MineGui", "[Probe] === start ===", "[MekPro] settings file: ", "Nodes", "getgc", "Mining", " (Text=", "เปิดหน้าอัปเกรดของ miner ที่วางอยู่ แล้วซื้อ Damage/Speed/Mining ที่ราคาไม่เกินงบ", " ===", "games/my_anime_mine/game_api", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", ") for ", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "[MyAnimeMine] === Tool: ", "⚙", "languageIndex", " items", " (", "[MyAnimeMine] no nodes present right now", "MineGui", "[AutoPlace] slot ", "Instance", "TextLabel", " ===", ")", "Inspect", "Workspace", "[ChestDump] === end ===", "Header", "^%$", "games/my_anime_mine/debug_dump_remotes", ": ", "Auto Place Miner", "Frame", "'), backing off ", "[AutoSell] disabled", "games/my_anime_mine/get_plot_parts", "[AntiAFK] enabled - watching Player.Idled", "[Bb]uy", "Card", "UIScale", "", "  [attr] ", "th", "core/logger", "open_menu_hint", "decompile", "games/my_anime_mine/get_best_miner", "disabled", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "[AutoPlace] nothing in the hotbar to place", "ค้นหาฟังก์ชัน...", " '", "games/my_anime_mine/debug_dump_sell_cards", "AutoChest", "core/logger", "[MyAnimeMine] not standing in any plot", "ClickDetector", "'", "", "CoreUI", "ScreenGui", "MekPro/decompiled_", "[AutoPlace] ", "Content", "core/anti_afk", "th", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", "number", "games/my_anime_mine/debug_dump_sell_cards", "TextButton", "Content", "games/my_anime_mine/main", " connection(s)", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " = ", "Auto Upgrade: งบต่อครั้ง", "Done", "ProximityPrompt", "auto", "string", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/auto_place", "TextButton", "TextLabel", "Content", "[BestMiner] slot ", " attributes ===", " (", "UpgradeButton", "[Interaction] logger disabled", "GuiObject", "RemoteFunction", ": ", "[AutoSell] enabled (only sells when there's something in the scroll)", " ===", "Lock", "waiting", "[AutoSell] nothing to sell, closing", "interactionLogger", "?", "UpgradePart", "^Node_", "autoChestMinItems", "autoUpgradeMaxSpend", "table", "no tool", "Close", "Frame", " = ", "[Probe] === executor capabilities ===", "MineSlotFocusClient", "TextLabel", "TextLabel", "RemoteEvent", "core/logger", "MineGui", "costs ", "ValueLabel", "getnilinstances", "TakeButton", "yes", "Done", "TabBar", ": ", "core/parse_money", "Scroll", "games/my_anime_mine/get_plot_parts", " (Text=", "", " ", "  [attr] ", "Scroll", "", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "ValueBase", "[Probe] couldn't decompile ", "Loot", " items (now ", "Backpack", "ItemName", "", "[Remotes] === total: ", "UIPadding", "[attr] ", "ChestCapacity", "[GameAPI] couldn't load the game's modules: ", "ProximityPrompt", " = ", "getcallbackvalue", "UserInputService", "SelectAllButton", "Crew", "neither appendfile nor writefile is available", "[Interaction] ProximityPrompt triggered: ", "/config.json", " (power ", ") from ", "core/logger", "[Probe] decompile unavailable - can't read the game's source this way", "[AutoResearch] bought ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[AutoUpgrade] ", "Main", "autoChest", "languageIndex", "auto", "[ChestDump] plot has no Chest", "debug.getupvalues", "no isfile/readfile available to verify the write", "Locked miner slot", "core/i18n", "", "autoSellInterval", "waiting", "Sell SelectAllButton", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "ตามเครื่อง", "table", "TextButton", "core/dispatch", "TextButton", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", ": ", " = ", "en-us", "MineSpeed", "Damage", "อังกฤษ", "core/ui", "ExitButton", "", "Players", "autoUpgrade", "SelectAllButton", " (prompt says '", "fill", "StatsPanel", "Activated", "games/my_anime_mine/auto_research", "Cost", "🏠", "s", "games/my_anime_mine/get_current_plot", "", " (", "UIListLayout", "core/click", "Scroll", "[AutoChest] collected ", "[ChestDump] not standing in any plot", "[AutoPlace] holders: ", "games/registry", "PlayerGui", "HumanoidRootPart", "-", "getsenv", "core/logger", "TextLabel", "ClickDetector", ") -> ", ")", "[AutoMine] ", "games/my_anime_mine/get_current_plot", "UpgradeButton", "'", "SelectOverlay", "core/parse_money", "Close", "[ChestDump] === ReplicatedStorage.", " '", "Research BuyButton", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", " cards", "ExitButton", ")", ") didn't take, trying the next one", "MineGame", "ฟาร์ม", "autoChestInterval", "TextButton", " ===", "  ", "ย่อขยายเมนูทั้งชุด", ": ", "TextLabel", "core/logger", "[AutoChest] can't read the chest yet", ")", " from ", "games/my_anime_mine/get_current_plot", "Debug: Dump All Remotes", "TextBox", "autoMineSpeed", "[Uu]nlock", "CoreGui", "core/dispatch", "Players", "core/logger", "[Handlers] === end ===", "core/anti_afk", "core/logger", "Players", "=empty", "GuiObject", "Damage", "SlotFocusFrame", "core/logger", "Auto Mine: ความเร็ว", "Place", "", "defined at ", "Done", "table", " on ", "Sidebar", "x", " unit card(s) Select All skipped", "getscripts", "Workspace", "core/get_me", "Mining", " (", ": ", "core/interaction_logger", "core/get_place_id", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Sell for", "TextButton", "games/my_anime_mine/debug_dump", "X", " while selecting", "UI", "Frame", "", "All", "core/logger", "games/my_anime_mine/debug_probe_env", "MineGui", "Frame", "[AutoUpgrade] bought ", "[Handlers] getconnections isn't available in this executor", "core/get_leaderstat", "GuiButton", "AutoUpgrade", "Tree", "[AutoPlace] drifted out of range of ", "function", "Error: ", "RemoteFunction", "Select All", "ปุ่มเปิดเมนู", "", "games/my_anime_mine/auto_chest", "table", "disabled", "MekPro", "Auto Research: งบต่อครั้ง", "Frame", "'", "TextLabel", "Debug: Dump Backpack Tool", "$", "games/registry", "games/my_anime_mine/auto_upgrade", "[AntiAFK] disabled", "  (debug.getupvalues unavailable)", ") on ", ": ", "autoMineClicks", "Frame", "%", "=locked(", "Players", "en", "  (debug.getconstants unavailable)", "table", "UIListLayout", "leaderstats", "ChestFrame", "[AutoResearch] ", " x", "games/my_anime_mine/debug_dump", "MineEquipSlot", "(nothing to do)", " (", "[Interaction] Key pressed: %s (gameProcessed=%s)", "  [const] ", "action '", "SellButton", "Frame", "🐛", "Card", "Players", "no", "[AutoPlace] placed ", "Players", "disabled", "Badge", "UserInputService", "autoResearchMaxSpend", "th", "autoResearch", "core/click", "UpgradesButton", ": not found", "", "locked slot", "menuScale", "Label", "ClickDetector", "^([%d,]+)$", "TextLabel", "th", "MineHUDClient", "not found", "MineUpgradeCharacter", " remotes", " '", "Card", "", "RemoteEvent", "Scroll", "core/interaction_logger", "MineGui", "", "SellFrame", "core/logger", "games/my_anime_mine/auto_sell", "ตั้งค่า", "Label", "Debug: Probe Executor + Scripts", " (MaxActivationDistance=%s)", "s", "open_menu_hint", "Dump", "s", "HotbarFrame", "antiAfk", "  [attr] ", "ไม่ซื้ออะไรที่แพงกว่านี้", "Content", "", "Debug: Dump Visible GUI", "core/scheduler", "Zones", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/debug_dump_chest", "Dump", "[AutoPlace] couldn't pick up ", "fill", "Content", "Frame", "Holders", "Debug: Dump Node/Holder", "en", "core/scheduler", "Plots", "MineHotbarClient", "log failed", "ItemFrame", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "ไทย", "Content", "[AntiAFK] idle detected, simulating input", "AutoSell", "/", "PlayerGui", "[AutoSell] fireproximityprompt error: ", "TextButton", "Unit Damage UpgradeButton", "[Interaction] Button activated: ", "Scroll", "[Probe] decompiled ", "RemoteEvent", "PlotIndex", "BottomPanels", ")", "TextLabel", "[MyAnimeMine] === Node: ", "no plot", " ", "Content", "  ", "Debug: Inspect Button Handlers", "core/logger", "MekPro", "debug.getproto", "menuScale", "getrenv", "sl", "autoResearchInterval", "[AutoUpgrade] upgrade screen didn't open for ", " ชิ้น", "games/my_anime_mine/auto_sell", "TextButton", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "MekPro.log", " ===", "games/my_anime_mine/auto_chest", "boolean", "GuiService", " cards left, label = '", "games/my_anime_mine/debug_inspect_handlers", "Frame", "[AutoChest] MineTakeChest had no effect (", "[AutoSell] sell menu didn't open, aborting", "Players", "Check", "core/logger", "core/teleport", "core/logger", "SellFrame", "getconnections", " still empty after placing ", "?", "core/config", "Humanoid", "Auto Sell: รอบขาย", " (", "PlayerGui", "unreadable", "[GuiDump] === visible MineGui tree ===", "SellButton", "games/my_anime_mine/auto_research", "SkillTreeFullFrame", "Content", "enabled", "Instance", " '", "[AutoPlace] disabled", "PlayerGui", "Search", "[AutoSell] also selected ", "unreadable", "Modules", "ClickDetector", "Players", "core/parse_money", "MineBuySkill", "PlayerScripts", "Content", "[ChestDump] === Player subtree ===", "StatsPanel", "getscriptbytecode", "write reported success but the line isn't in ", "ReplicatedStorage", "VirtualInputManager", "AutoResearch", "Auto Research (Plot)", " = ", "games/my_anime_mine/debug_dump_gui", ") ===", "Tool", "Auto Mine: คลิกต่อรอบ", " -> ", "Dump", "[Config] couldn't write ", "search_placeholder", " not found", "write failed", "Debug: Dump Chest Data", "Chest TakeButton", "[Interaction] ClickDetector clicked: ", "games/my_anime_mine/get_plot_parts", "'", "games/my_anime_mine/debug_probe_env", "[^%.]+", "Players", "PlayerGui", " cards)", "Players", "ClickDetector", "ProximityPrompt", "[Handlers]   ", "VirtualUser", " items)", "Workspace", " '", "/", " doesn't exist", "Search functions...", "OwnerUserId", "SellFrame", "TextLabel", "SkillTreeFullFrame", "Detail", "", "  ", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "[Handlers] ", "'", "core/i18n", "games/my_anime_mine/debug_dump_remotes", ",", "BuyButton", "function", "hookfunction", "core/teleport", "[AutoSell] clicked SellButton", " ===", "[%X] ", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", "Auto Chest: รอบเช็ค", "ไม่ซื้อ node ที่แพงกว่านี้", "Text", "MineSellClient", "ProximityPrompt", "[GuiDump] === end ===", "This is not a supported place.", "Speed", "prompt disabled", "MineWorld", " parts", ": ", "core/get_place_id", "อ่านป้ายบน chest ทุกกี่วินาที", "Auto Chest: เกณฑ์เก็บ", "s", "Backpack", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", " (Text=", "Content", "[Scheduler] turn: ", "games/my_anime_mine/debug_inspect_handlers", "enabled", "[ChestDump] === plot ", "[MyAnimeMine] no holders found", "Players", "เปิดหน้า research ทุกกี่วินาที", "core/config", "[AutoPlace] fireproximityprompt error: ", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "Debug: Current Plot", " ", "Sell SellButton", "button has no size on screen", "auto", "Frame", "", "games/my_anime_mine/get_plot_parts", "Done", "core/logger", "[MyAnimeMine] === Dump start ===", "Interaction Logger", "TextLabel", "core/logger", "RunService", "[ChestDump] === Chest subtree ===", "[GuiDump] MineGui not found", "SlotFocusFrame", "[Handlers] ", "Badge", "Players", "Chest", "PlayerGui", "\n", "ReplicatedStorage", "  ", "core/logger", "core/ui", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "ScrollingFrame", "Debug: Dump Sell Cards", "Container", "'", "[AutoPlace] picked up ", "autoMine", "core/click", "Max", "[AutoResearch] nothing affordable under the spend cap", "Content", "TextButton", "/", "upgrade", ".lua", "", "TextLabel", "ไล่อัปเกรด miner ทุกกี่วินาที", "MineGui"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(256)] = function()
return function(import)
local log = import(S(487))
local Players = game:GetService(S(524))
local VirtualUser = game:GetService(S(726))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(228))
        connection = player.Idled:Connect(function()
            log(S(615))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(516))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(554)] = function()
local VirtualInputManager = game:GetService(S(698))
local GuiService = game:GetService(S(652))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(787)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(781)] = function()
return function(import)
local HttpService = game:GetService(S(15))
local getPlaceId = import(S(766))
local log = import(S(660))
local ROOT_FOLDER = S(637)
local placeFolder = ROOT_FOLDER .. S(824) .. tostring(getPlaceId())
local filePath = placeFolder .. S(346)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(464) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(64) .. filePath .. S(222) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(708) .. filePath .. S(284) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(447)] = function()
return function(import)
    local getPlaceId = import(S(104))
    local registry = import(S(403))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(760))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(493)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(529))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(471)] = function()
local Players = game:GetService(S(658))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(668))
    local backpack = player:WaitForChild(S(770))
    return player, humanoid, backpack
end
return getMe
end
modules[S(476)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(362)] = function()
local Players = game:GetService(S(544))
local STRINGS = {
    en = {
        search_placeholder = S(732),
        open_menu_hint = S(149),
    },
    th = {
        search_placeholder = S(242),
        open_menu_hint = S(502),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(376)
    if locale:sub(1, 2):lower() == S(552) then
        return S(234)
    end
    return S(606)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(788) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(574)] = function()
return function(import)
local Players = game:GetService(S(688))
local Workspace = game:GetService(S(728))
local UserInputService = game:GetService(S(341))
local log = import(S(449))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(345) .. describe(prompt)
            .. S(81) .. prompt.ActionText .. S(26))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(714) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(622) .. describe(button)
            .. S(197) .. tostring(button.Text) .. S(126))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(724)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(687)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(494)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(281))
        return
    end
    connections = {}
    log(S(147))
    local playerGui = Players.LocalPlayer:WaitForChild(S(108))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(269)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(410)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(86)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(537),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(810)] = function()
local LOG_FILE = S(648)
local function log(message)
    local line = os.date(S(752)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(395))
            end
            appendfile(LOG_FILE, line .. S(189))
        elseif writefile then
            local existing = S(576)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(807))
        else
            error(S(344))
        end
        if not (isfile and readfile) then
            error(S(360))
        end
        if not isfile(LOG_FILE) then
            error(S(65) .. LOG_FILE .. S(731))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(696) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(60) .. LOG_FILE .. S(519) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(418)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(143) then
        return nil
    end
    local number, suffix = text:match(S(8))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(30), S(593))))
    if not value then
        return nil
    end
    if suffix ~= S(503) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(607)] = function()
return function(import)
local RunService = game:GetService(S(798))
local log = import(S(797))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(87) .. task_.name .. S(7) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(774) .. (currentTask and currentTask.name or S(535)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(61)] = function()
local Players = game:GetService(S(804))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(4))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(811)] = function()
return function(import)
local i18n = import(S(32))
local UserInputService = game:GetService(S(550))
local THEME = {
    BrandName = S(507),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(62))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(89))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(334))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(372))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(461)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(183))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(563))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(291)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(42))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(121)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(813))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(528))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(735))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(521))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(629))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(436))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(213))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(139))
            if not valueLbl then
                valueLbl = Instance.new(S(173))
                valueLbl.Name = S(309)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(803))
            if not badge then
                badge = Instance.new(S(6))
                badge.Name = S(549)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(431))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(655))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(484))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(620))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(324)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(522)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(224))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(541))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(823))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(332)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(299))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(397))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(184))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(756)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(252))
    screenGui.Name = S(251)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(446))
    local main = Instance.new(S(509))
    main.Name = S(354)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(231))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(603))
    tabBar.Name = S(314)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(52))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(789))
    header.Name = S(219)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(261))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(275))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(304))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(738)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(146))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(481)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(54))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(406)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(443))
    searchBox.Name = S(683)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(709))
    searchBox.Text = S(35)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(490))
    content.Name = S(255)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(36))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(511))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(585))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(102))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(82)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(51) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(785) .. (item.Subtitle or S(485))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(72))
        footerLabel.Text = i18n.T(S(236))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(650)] = function()
return function(import)
local gameApi = import(S(101))(import)
local log = import(S(246))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(48) .. minItemsToCollect .. S(727))
    else
        log(S(115))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(685) then
            log(S(438))
            lastReported = S(672)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(365) then
            log(S(159) .. count .. S(617) .. minItemsToCollect .. S(207))
            lastReported = S(288)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(400) .. count .. S(329) .. after .. S(215))
        else
            log(S(656) .. count
                .. S(706) .. tostring(after) .. S(628))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(63)] = function()
return function(import)
local getCurrentPlot = import(S(31))
local getPlotParts = import(S(715))
local log = import(S(452))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(248), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(413) .. (enabled and S(678) or S(239)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(273)] = function()
return function(import)
local Players = game:GetService(S(722))
local getCurrentPlot = import(S(441))
local getPlotParts = import(S(791))
local getMinerSlots = import(S(238))(import)
local teleport = import(S(661))
local click = import(S(819))
local log = import(S(349))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(144)
local LOCKED_SLOT_TEXT = S(361)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(43))
    return pad and pad:FindFirstChildWhichIsA(S(758), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(409)) then
            local digits = descendant.Text:match(S(562))
            if digits then
                return tonumber((digits:gsub(S(745), S(320))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(796)) and descendant.Text:match(S(220)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(558)
    end
    if not prompt.Enabled then
        return true, S(762)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(308) .. price
    end
    local action = prompt.ActionText or S(557)
    if action:match(S(445)) or action:match(S(229)) or action:match(S(28)) then
        return true, S(539) .. action .. S(742)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(405))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(782) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(523) .. why .. S(439))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(454))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(93) .. prompt.ObjectText .. S(136) .. power .. S(164))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(17))
    if line ~= lastSurvey then
        log(S(402) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(601), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(825), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(254) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(681))
    else
        lastAttempt = 0
        log(S(477))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(137),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(460), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(241))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(497) .. holder.Name .. S(482))
            return false
        end
        log(S(211) .. candidate.slot .. S(208) .. candidate.name
            .. S(427))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(116) .. holder.Name .. S(665) .. miner.name)
        return false
    end
    log(S(546) .. miner.name .. S(347) .. miner.power
        .. S(518) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(600) .. entry.name .. S(440) .. entry.holder.Name
            .. S(386) .. tostring(entry.prompt.ActionText) .. S(95))
        return false
    end
    log(S(817) .. entry.name .. S(169) .. entry.power
        .. S(348) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(103))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(387) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(78) .. target.holder.Name .. S(22) .. target.name
        .. S(670) .. target.power .. S(411) .. best.name .. S(473) .. best.power .. S(426))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(675)] = function()
return function(import)
local Players = game:GetService(S(40))
local parseMoney = import(S(689))
local click = import(S(398))
local log = import(S(437))
local checkInterval = 30
local maxSpend = 100000
local function getMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(404))
    return playerGui and playerGui:FindFirstChild(S(191))
end
local function getTreeFrame()
    local mineGui = getMineGui()
    return mineGui and mineGui:FindFirstChild(S(676))
end
local function getUpgradesButton()
    local mineGui = getMineGui()
    local leftBar = mineGui and mineGui:FindFirstChild(S(88))
    local content = leftBar and leftBar:FindFirstChild(S(90))
    local container = content and content:FindFirstChild(S(815))
    return container and container:FindFirstChild(S(555))
end
local function zoneTabs(frame)
    local sidebar = frame:FindFirstChild(S(466))
    local list = sidebar and sidebar:FindFirstChild(S(125))
    if not list then
        return {}
    end
    local tabs = {}
    for _, tab in ipairs(list:GetChildren()) do
        if tab:IsA(S(370)) then
            local content = tab:FindFirstChild(S(135))
            local locked = content and content:FindFirstChild(S(287))
            if not locked then
                local label = content and content:FindFirstChild(S(74))
                table.insert(tabs, {
                    button = tab,
                    name = (label and label.Text ~= S(250) and label.Text) or tab.Name,
                })
            end
        end
    end
    return tabs
end
local function affordableNodes(frame)
    local tree = frame:FindFirstChild(S(496))
    local canvas = tree and tree:FindFirstChild(S(106))
    if not canvas then
        return {}
    end
    local nodes = {}
    for _, node in ipairs(canvas:GetChildren()) do
        if node:IsA(S(646)) and node.Name:match(S(293)) then
            local content = node:FindFirstChild(S(822))
            local cost = content and content:FindFirstChild(S(391))
            local price = cost and parseMoney(cost.Text)
            if price and price <= maxSpend then
                local pips = content:FindFirstChild(S(10))
                table.insert(nodes, {
                    button = node,
                    name = node.Name:sub(6),
                    price = price,
                    priceText = cost.Text,
                    pips = pips and pips.Text or S(666),
                })
            end
        end
    end
    table.sort(nodes, function(a, b)
        return a.price < b.price
    end)
    return nodes
end
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(531) .. (enabled and S(776) or S(506)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyNode(frame, node)
    click.Real(node.button)
    task.wait(0.4)
    local detail = frame:FindFirstChild(S(174))
    local buyButton = detail and detail:FindFirstChild(S(746))
    if not buyButton then
        log(S(16) .. node.name)
        return false
    end
    local content = buyButton:FindFirstChild(S(262))
    local label = content and content:FindFirstChild(S(581))
    if label and label.Text == S(820) then
        return false
    end
    click.Real(buyButton)
    task.wait(0.5)
    log(S(351) .. node.name .. S(396) .. node.pips .. S(202) .. node.priceText)
    return true
end
local function runJob()
    busy = true
    local button = getUpgradesButton()
    if not button then
        busy = false
        return
    end
    click.Real(button)
    task.wait(0.8)
    local frame = getTreeFrame()
    if not (frame and frame.Visible) then
        log(S(132))
        busy = false
        return
    end
    local bought = 0
    for _, tab in ipairs(zoneTabs(frame)) do
        if not enabled then
            break
        end
        click.Real(tab.button)
        task.wait(0.5)
        for _, node in ipairs(affordableNodes(frame)) do
            if not enabled then
                break
            end
            if buyNode(frame, node) then
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(821))
    end
    local close = frame:FindFirstChild(S(298)) or frame:FindFirstChild(S(381))
    if close then
        click.Real(close)
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(579)] = function()
return function(import)
local Players = game:GetService(S(20))
local Workspace = game:GetService(S(217))
local teleport = import(S(749))
local click = import(S(85))
local log = import(S(662))
local checkInterval = 45
local nothingSellableCooldown = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(682))
    local mineGui = playerGui and playerGui:FindFirstChild(S(165))
    return mineGui and mineGui:FindFirstChild(S(37))
end
local function labelText(button)
    local content = button:FindFirstChild(S(25))
    local label = content and content:FindFirstChild(S(560))
    return label and label.Text or S(45)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(38))
    local scroll = content and content:FindFirstChild(S(399))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(570) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(138) .. button.Name .. S(474) .. tostring(err))
    end
    return ok
end
local function isCardSelected(card)
    local content = card:FindFirstChild(S(633))
    local overlay = content and content:FindFirstChild(S(417))
    return overlay ~= nil and overlay.Visible
end
local function isOnScreen(card, scroll)
    local top = scroll.AbsolutePosition.Y
    local bottom = top + scroll.AbsoluteSize.Y
    local cardTop = card.AbsolutePosition.Y
    local cardBottom = cardTop + card.AbsoluteSize.Y
    return cardTop >= top and cardBottom <= bottom
end
local function selectUnselectedCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(773))
    local scroll = content and content:FindFirstChild(S(573))
    if not scroll then
        return 0
    end
    local selected = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(230) and not isCardSelected(card) and isOnScreen(card, scroll) then
            clickButton(card)
            selected = selected + 1
            task.wait(0.1)
        end
    end
    return selected
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(29))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(119), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(226))
    else
        lastAttempt = 0
        log(S(285))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(619) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(657))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(289))
        local closeButton = sellFrame:FindFirstChild(S(3))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(276))
    local selectAllButton = content and content:FindFirstChild(S(385))
    local sellButton = content and content:FindFirstChild(S(674))
    if selectAllButton and labelText(selectAllButton) == S(501) then
        clickButton(selectAllButton)
        log(S(56) .. cardCount .. S(721))
        task.wait(0.2)
    end
    local selected = selectUnselectedCards(sellFrame)
    if selected > 0 then
        log(S(684) .. selected .. S(468))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(110), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(478), 1, true) then
        clickButton(sellButton)
        log(S(750))
    else
        lastAttempt = tick() + nothingSellableCooldown - checkInterval
        local closeButton = sellFrame:FindFirstChild(S(419))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(53) .. cardCount .. S(653)
            .. tostring(sawLabel) .. S(225) .. nothingSellableCooldown .. S(187))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(272)] = function()
return function(import)
local Players = game:GetService(S(180))
local getCurrentPlot = import(S(12))
local getPlotParts = import(S(318))
local parseMoney = import(S(316))
local click = import(S(123))
local log = import(S(100))
local STAT_NAMES = { S(158), S(761), S(472) }
local checkInterval = 20
local maxSpend = 100000
local function getFocusFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(806))
    local mineGui = playerGui and playerGui:FindFirstChild(S(830))
    return mineGui and mineGui:FindFirstChild(S(801))
end
local function findUpgradeClick(holder)
    local upgradePart = holder:FindFirstChild(S(292))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(561))
end
local function occupiedHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return {}
    end
    local found = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClick(holder)
        if detector then
            table.insert(found, { holder = holder, detector = detector })
        end
    end
    return found
end
local function readStat(scroll, statName)
    local row = scroll:FindFirstChild(statName)
    local button = row and row:FindFirstChild(S(280))
    local content = button and button:FindFirstChild(S(677))
    local label = content and content:FindFirstChild(S(109))
    if not label then
        return nil
    end
    return {
        name = statName,
        button = button,
        price = parseMoney(label.Text),
        text = label.Text,
    }
end
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(353) .. (enabled and S(13) or S(548)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyAffordableStats(entry)
    local frame = getFocusFrame()
    local panels = frame and frame:FindFirstChild(S(627))
    local statsPanel = panels and panels:FindFirstChild(S(388))
    local scroll = statsPanel and statsPanel:FindFirstChild(S(623))
    if not scroll then
        log(S(643) .. entry.holder.Name)
        return 0
    end
    local bought = 0
    for _ = 1, #STAT_NAMES do
        local cheapest = nil
        for _, statName in ipairs(STAT_NAMES) do
            local stat = readStat(scroll, statName)
            if stat and stat.price and stat.price <= maxSpend then
                if not cheapest or stat.price < cheapest.price then
                    cheapest = stat
                end
            end
        end
        if not cheapest then
            break
        end
        click.Real(cheapest.button)
        log(S(491) .. cheapest.name .. S(117) .. cheapest.text
            .. S(465) .. entry.holder.Name)
        bought = bought + 1
        task.wait(0.4)
    end
    return bought
end
local function closeFrame()
    local frame = getFocusFrame()
    local exit = frame and frame:FindFirstChild(S(425))
    if exit then
        click.Real(exit)
        task.wait(0.3)
    end
end
local function runJob()
    busy = true
    for _, entry in ipairs(occupiedHolders()) do
        if not enabled then
            break
        end
        fireclickdetector(entry.detector)
        task.wait(0.8)
        local frame = getFocusFrame()
        if frame and frame.Visible then
            buyAffordableStats(entry)
            closeFrame()
        end
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(533)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(94)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(421) .. child.Name .. S(716)
        if child:IsA(S(338)) then
            line = line .. string.format(
                S(647),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(723)) then
            line = line .. string.format(S(583), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(572)) or child:IsA(S(500)) then
            line = line .. S(67)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(590) .. attrName .. S(300) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(809))
    end
end
return dumpInstance
end
modules[S(598)] = function()
return function(import)
local Players = game:GetService(S(453))
local ReplicatedStorage = game:GetService(S(168))
local getCurrentPlot = import(S(414))
local getPlotParts = import(S(131))
local log = import(S(142))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(335) .. name .. S(339) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(99))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(569) .. child.Name .. S(249)
            if child:IsA(S(326)) then
                line = line .. S(124) .. tostring(child.Value)
            elseif child:IsA(S(178)) or child:IsA(S(274)) then
                line = line .. S(319) .. tostring(child.Text) .. S(412)
            end
            log(line)
            dumpSubtree(child, indent .. S(739))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(401))
        return 0
    end
    log(S(777) .. plot.Name .. S(278))
    dumpAttributes(plot, S(634))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(358))
        return 0
    end
    log(S(799))
    dumpSubtree(parts.Chest, S(827))
    local player = Players.LocalPlayer
    log(S(693))
    dumpSubtree(player, S(232))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(420) .. player.Name .. S(199))
        dumpSubtree(mine, S(790))
    end
    log(S(218))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(702)] = function()
return function(import)
local Players = game:GetService(S(383))
local log = import(S(408))
local function dumpVisible(root, indent)
    indent = indent or S(382)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(282)) and child.Visible then
            local line = indent .. child.ClassName .. S(729) .. child.Name .. S(510)
            if child:IsA(S(828)) or child:IsA(S(479)) then
                line = line .. S(772) .. tostring(child.Text) .. S(55)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(233) .. attrName .. S(163) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(433))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(720))
    local mineGui = playerGui and playerGui:FindFirstChild(S(210))
    if not mineGui then
        log(S(800))
        return
    end
    log(S(673))
    dumpVisible(mineGui)
    log(S(759))
end
return dumpMineGui
end
end
modules[S(221)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(697))
local log = import(S(636))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(625)) or descendant:IsA(S(283)) then
            count = count + 1
            log(S(83) .. descendant.ClassName .. S(315) .. descendant:GetFullName())
        end
    end
    log(S(333) .. count .. S(649))
    return count
end
return dumpRemotes
end
end
modules[S(260)] = function()
return function(import)
local Players = game:GetService(S(547))
local log = import(S(458))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(97))
    local mineGui = playerGui and playerGui:FindFirstChild(S(575))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(734))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(592)) and sellFrame.Content:FindFirstChild(S(317))
    if not scroll then
        log(S(154))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(543) or card:IsA(S(455)) then
            count = count + 1
            log(S(46) .. count .. S(435) .. card.ClassName .. S(680) .. card.Name .. S(816))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(322) .. attrName .. S(148) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(2) .. child.ClassName .. S(243) .. child.Name .. S(416)
                if child:IsA(S(14)) then
                    line = line .. S(375) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(156) .. count .. S(751))
    return count
end
return dumpSellCards
end
end
modules[S(654)] = function()
return function(import)
local Players = game:GetService(S(68))
local log = import(S(578))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(679) then
        return value.ClassName .. S(632) .. value:GetFullName()
    end
    if valueType == S(527) then
        return S(369)
    end
    if valueType == S(747) then
        return S(69)
    end
    return valueType .. S(321) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(212) then
        return value:IsA(S(305)) or value:IsA(S(1)) or value:IsA(S(133))
    end
    return valueType == S(181) or valueType == S(259) or valueType == S(651)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(641))
    end)
    if ok and source then
        log(indent .. S(462) .. tostring(source) .. S(50) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(140) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(538) .. describe(constant))
            end
        end
    else
        log(indent .. S(526))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(296) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(27) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(517))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(802) .. label .. S(556))
        return
    end
    log(S(185) .. label .. S(536) .. button:GetFullName() .. S(703))
    if not getconnections then
        log(S(492))
        return
    end
    for _, signalName in ipairs({ S(389), S(77) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(741) .. signalName .. S(186) .. #connections .. S(264))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(188) .. index)
                    inspectFunction(fn, S(725))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(671))
    local current = playerGui and playerGui:FindFirstChild(S(307))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(141))
    inspectButton(S(713), find({ S(530), S(80), S(105), S(311) }))
    inspectButton(S(366), find({ S(663), S(614), S(342) }))
    inspectButton(S(786), find({ S(577), S(39), S(540) }))
    inspectButton(S(422), find({ S(736), S(737), S(176) }))
    local statsScroll = find({ S(457), S(157), S(694), S(323) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(456))
    inspectButton(S(621), damageRow and damageRow:FindFirstChild(S(415)))
    log(S(450))
end
return inspectHandlers
end
end
modules[S(488)] = function()
return function(import)
local Players = game:GetService(S(57))
local log = import(S(793))
local CAPABILITIES = {
    S(237), S(695), S(469), S(195), S(66),
    S(664), S(340), S(407), S(640), S(182),
    S(359), S(638), S(748), S(310),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(718)) do
        if type(target) ~= S(505) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(301))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(58) .. name .. S(266) .. ((ok and type(value) == S(498)) and S(312) or S(545)))
    end
end
local WANTED_SCRIPTS = {
    S(177),
    S(757),
    S(47),
    S(302),
    S(609),
    S(565),
}
local function tryDecompile()
    if not decompile then
        log(S(350))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(691))
    local ui = playerScripts and playerScripts:FindFirstChild(S(483))
    local mineGame = ui and ui:FindFirstChild(S(428))
    if not mineGame then
        log(S(59))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(34) .. name .. S(710))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(271) and #source > 0 then
                local path = S(253) .. name .. S(826)
                local written = pcall(writefile, path, source)
                log(S(624) .. name .. S(279) .. #source .. S(127)
                    .. (written and path or S(711)))
            else
                log(S(327) .. name .. S(765) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(192))
    reportCapabilities()
    tryDecompile()
    log(S(76))
end
return probe
end
end
modules[S(200)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(808))
local log = import(S(235))
local api = {}
local gameState, mineInventory
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(686), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
    end)
    if not ok then
        loadFailed = true
        log(S(337) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetMineData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return nil
    end
    return data.MineGame
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(9) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(166))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(486), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(120) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(118), keys)
end
function api.EquipSlot(key)
    return send(S(534), key)
end
function api.BuySkill(nodeId, family)
    return send(S(690), nodeId, family)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(567), characterKey, stat == S(196) and S(377) or stat)
end
return api
end
end
modules[S(597)] = function()
return function(import)
local Players = game:GetService(S(719))
local log = import(S(306))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(618))
    local mineGui = playerGui and playerGui:FindFirstChild(S(489))
    local hotbar = mineGui and mineGui:FindFirstChild(S(588))
    local content = hotbar and hotbar:FindFirstChild(S(602))
    return content and content:FindFirstChild(S(23))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(303)) then
            local count = descendant.Text:match(S(171))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(611))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(692))
    local nameLabel = content and content:FindFirstChild(S(331))
    local damageLabel = content and content:FindFirstChild(S(378))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(571) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(113), S(363)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(730) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(277) .. slot .. S(374) .. info.name
                        .. S(24) .. info.power .. S(532) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(111)] = function()
local Players = game:GetService(S(779))
local Workspace = game:GetService(S(470))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(763))
    local plots = mineWorld and mineWorld:FindFirstChild(S(608))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(733)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(130)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(604)),
        Nodes = plot:FindFirstChild(S(194)),
        Chest = plot:FindFirstChild(S(805)),
        Zones = plot:FindFirstChild(S(596)),
        Crew = plot:FindFirstChild(S(343)),
        Loot = plot:FindFirstChild(S(328)),
        PlotIndex = plot:GetAttribute(S(626)),
        ChestCapacity = plot:GetAttribute(S(336)),
    }
end
return getPlotParts
end
modules[S(263)] = function()
return function(import)
    local Players = game:GetService(S(448))
    local createWindow = import(S(380))(import)
    local i18n = import(S(743))
    local antiAfk = import(S(451))(import)
    local interactionLogger = import(S(475))(import)
    local scheduler = import(S(595))(import)
    local config = import(S(667))(import)
    local log = import(S(112))
    local window = createWindow({
        Subtitle = S(162),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(394))
    local getPlotParts = import(S(227))
    local dumpInstance = import(S(480))
    local dumpRemotes = import(S(744))(import)
    local dumpSellCards = import(S(244))(import)
    local dumpGui = import(S(155))(import)
    local dumpChest = import(S(151))(import)
    local inspectHandlers = import(S(775))(import)
    local probeEnv = import(S(717))(import)
    local autoMine = import(S(92))(import)
    local autoPlace = import(S(91))(import)
    local autoChest = import(S(504))(import)
    local autoSell = import(S(645))(import)
    local autoUpgrade = import(S(515))(import)
    local autoResearch = import(S(390))(import)
    scheduler.RegisterStandalone(S(122), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(245), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(134), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(495), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(699), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(616), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(429), S(392))
    persistentToggle(farmTab:AddCard(
        S(167),
        S(172)
    ), S(818), autoMine.SetEnabled)
    persistentSlider(farmTab, S(459), S(203),
        1, 30, S(444), 5, autoMine.SetCyclesPerSecond, S(19))
    persistentSlider(farmTab, S(705), S(107),
        1, 10, S(520), 1, autoMine.SetClicksPerCycle, S(467))
    persistentToggle(farmTab:AddCard(
        S(223),
        S(179)
    ), S(21), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(160), S(265),
        1, 60, S(18), 3, autoPlace.SetCheckInterval, S(393))
    persistentToggle(farmTab:AddCard(
        S(41),
        S(783)
    ), S(355), autoChest.SetEnabled)
    persistentSlider(farmTab, S(768), S(612),
        50, 3000, S(294), 100, autoChest.SetMinItems, S(644))
    persistentSlider(farmTab, S(754), S(767),
        1, 60, S(430), 3, autoChest.SetCheckInterval, S(49))
    persistentToggle(farmTab:AddCard(
        S(70),
        S(352)
    ), S(114), autoSell.SetEnabled)
    persistentSlider(farmTab, S(669), S(812),
        10, 300, S(364), 45, autoSell.SetCheckInterval, S(769))
    persistentToggle(farmTab:AddCard(
        S(152),
        S(198)
    ), S(384), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(145), S(829),
        5, 300, S(79), 20, autoUpgrade.SetInterval, S(584))
    persistentSlider(farmTab, S(267), S(591),
        1000, 1000000, S(295), 100000, autoUpgrade.SetMaxSpend, S(513))
    persistentToggle(farmTab:AddCard(
        S(700),
        S(96)
    ), S(553), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(33), S(780),
        10, 600, S(642), 30, autoResearch.SetCheckInterval, S(587))
    persistentSlider(farmTab, S(508), S(755),
        1000, 1000000, S(551), 100000, autoResearch.SetMaxSpend, S(190))
    local debugTab = window:AddTab(S(98), S(542))
    persistentToggle(
        debugTab:AddCard(
            S(795),
            S(423)
        ),
        S(290),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(784), S(740))
    local checkBtn
    checkBtn = debugCard:AddButton(S(659), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(566)
            log(S(247))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(240),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(605),
        S(367)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(599), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(631)
            return
        end
        local ok, err = log(S(794))
        if not ok then
            dumpBtn.Text = S(610)
            dumpCard:SetSubtitle(S(499) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(630) .. firstNode.Name .. S(432))
            dumpInstance(firstNode, log)
        else
            log(S(209))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(75) .. firstHolder.Name .. S(286))
            dumpInstance(firstHolder, log)
        else
            log(S(778))
        end
        dumpBtn.Text = S(463)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(512),
        S(201)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(11), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(330))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(704))
        if not tool then
            toolDumpBtn.Text = S(297)
            return
        end
        log(S(204) .. tool.Name .. S(214))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(71) .. attrName .. S(701) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(73)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(442),
        S(128)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(5), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(568)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(814),
        S(771)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(586), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(424)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(594),
        S(325)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(161), function()
        dumpGui()
        guiDumpBtn.Text = S(792)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(712),
        S(373)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(707), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(764)
    end)
    local handlersCard = debugTab:AddCard(
        S(635),
        S(753)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(216), function()
        inspectHandlers()
        handlersBtn.Text = S(313)
    end)
    local probeCard = debugTab:AddCard(
        S(582),
        S(258)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(150), function()
        probeEnv()
        probeBtn.Text = S(268)
    end)
    local settingsTab = window:AddTab(S(580), S(205))
    persistentToggle(
        settingsTab:AddCard(S(84), S(175)),
        S(589),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(559), 100)
    settingsTab:AddSlider(S(44), S(434), 50, 150, savedScale, function(value)
        config.Set(S(639), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(356), 1)
    settingsTab:AddSegmented({ S(368), S(379), S(613) }, savedLanguage, function(index, value)
        local code = ({ S(357), S(153), S(257) })[index]
        config.Set(S(206), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(270), S(525), S(564) })[savedLanguage] or S(170))
    log(S(193) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
    end)
end
end
modules[S(514)] = function()
return {
    [79389059854988] = S(129),
}
end
import(S(371))(import)
