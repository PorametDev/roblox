-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoUpgrade] MineBuyPickaxe ", "SellStand", "ClickDetector", "BasePart", ") ", " node(s) looked buyable but the server refused", "Damage", "\n", "[AutoClaim] ", "autoUpgradeRollMutation", " screens", ")", "upgrade", "'", "RemoteEvent", "interactionLogger", "TextLabel", " pickaxe ", "Speed", "[Decompile] decompile unavailable in this executor", "core/i18n", ")", "Place", "Search", "[Interaction] logger enabled - go click/interact with things now", "Backpack", "games/my_anime_mine/auto_place", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", " = ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Auto Research: ซื้อสาย Core", "TextButton", "autoMine", "Auto Upgrade: สุ่ม Mutation", " reward(s) looked claimable but none went through", "Mutation: หยุดเมื่อได้ x", "MineSpeed", "enabled", "", "autoResearchCoreLast", "[AutoChest] collected ", "[GameAPI] SendEvent failed: ", "x", "GuiObject", "MineRollMutation", "decompile", "core/get_place_id", "unreadable", "s", "RemoteFunction", "core/get_leaderstat", "Workspace", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", ")", "[MekPro] settings file: ", "[AutoPlace] ", "Frame", "/", "MineEquipSlot", "core/debug_dump_remotes", "fill", "core/debug_tab", "Loot", "autoUpgradeInterval", "Modules", "Main", "autoMineClicks", "core/logger", "[AutoPlace] ", "autoResearchCore", "core/teleport", "core/ui", "ModuleScript", "MekPro", "Players", "Frame", "leaderstats", "AutoClaim", "core/debug_dump_gui", "neither appendfile nor writefile is available", "core/logger", ",", "search_placeholder", " owned)", "Players", "core/debug_dump_gui", " while selecting", "write reported success but ", ")", "enabled", "MineUpgradeCharacter", "Content", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "ต่ำสุดก่อน", "Decompile Client Scripts", "nothing to place or upgrade", " from ", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "maxed", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "core/anti_afk", "_", ")", "CharMaxLevel", " not maxed", "CharMineSpeedMaxLevel", "debug.getconstants", "Auto Claim: เก็บสูงสุดต่อรอบ", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "Text", "waiting", "games/my_anime_mine/get_best_miner", "ReplicatedStorage", "open_menu_hint", "games/registry", "[AutoChest] can't read the chest yet", "Speed", "Run", "table", "[AutoSell] disabled", ") didn't take, trying the next one", "CoreUI", "PlayerGui", "[AutoPlace] holders: ", "function", "ReplicatedStorage", "Content", "[AntiAFK] idle detected, simulating input", "enabled", "ไล่ซื้อทุกกี่วินาที", " -> ", "core/scheduler", "[GuiDump] === end (", "[AutoUpgrade] bought pickaxe #", "UIScale", "Auto Upgrade: Pickaxe ที่จะซื้อ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Auto Chest: รอบเช็ค", "UIStroke", "antiAfk", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "[Bb]uy", "M", "VirtualInputManager", "[Decompile] === start ===", "PickaxesStand", "core/get_place_id", "[AutoResearch] ", "TabBar", "')", " didn't go through (out of stock or too expensive)", "เดินทะลุกำแพงและพื้น", "core/build_env", "games/my_anime_mine/auto_claim", "Mining", "TextLabel", "Auto Research: ซื้อสาย Zone", "costs ", "locked slot", "VirtualUser", "Auto Mine: ความเร็ว", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "getrenv", "Content", "Humanoid", " -> ", "[GuiDump] ", "MineSpeed", "Locked miner slot", "ขนาดเมนู", "disabled", " (", "[^%.]+", "GuiButton", "Dump", " '", "Dump All Remotes", "", "", ": ", "JumpPower (ปกติ 50)", " buyCore=", "[AutoClaim] ", "[Interaction] Key pressed: %s (gameProcessed=%s)", " items", "[Decompile] ", "en", "Badge", " items)", "?", " ", "/s", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Auto Mine", "lowest", "none", "core/logger", " power ", "UIPadding", "autoMineSpeed", "core/config", "ไล่อัปเกรด miner ทุกกี่วินาที", " still empty after placing ", "BasePart", "TextLabel", " = ", "Frame", "[Scheduler] ", " ", "[AutoPlace] couldn't pick up ", "autoResearchMaxPerPass", "'", "M", "table", "[AutoUpgrade] ", "", "table", "", "autoUpgradeMaxPerPass", "string", "MineWorld", " (", "getnilinstances", "core/logger", " | ", "games/my_anime_mine/game_api", "core/click", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "getsenv", "TextBox", "GetHolderSpeedLevel", "[AutoPlace] nothing in the hotbar to place", "PlayerGui", "GetHolderLevel", " (", "Container", "[AutoPlace] picked up ", "", "autoUpgradeMutationBudget", "TextLabel", "ดีบัก", "Frame", "Frame", "/", "⚙", ")", "=locked(", "[Interaction] Button activated: ", "autoUpgradePickaxeIndex", "number", "autoSellInterval", "core/parse_money", "%", "Humanoid", "", "แรงกระโดด", "ChestCapacity", "games/my_anime_mine/get_current_plot", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " ", "Crew", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "jumpPower", "games/my_anime_mine/main", "getcallbackvalue", "games/registry", "en-us", "Mining", "Damage", "ตามเครื่อง", "/", "no", "table", "games/my_anime_mine/game_api", "TextLabel", "OwnerUserId", " (power ", "Dump", "", "GuiButton", "Frame", "Anti-AFK", "MineGui", "[AutoChest] MineTakeChest had no effect (", "core/i18n", "Frame", " ", "TextButton", "[AutoPlace] fireproximityprompt error: ", " ", "games/my_anime_mine/auto_claim", "autoUpgradeMutationTarget", "UIListLayout", "TextLabel", "[BestMiner] slot ", ".lua", "ScreenGui", "LayerCollector", "Auto Research: กันเงินไว้", "[Remotes] ", "CoreGui", "core/get_place_id", "fly", " team power, you have ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "core/logger", "PlayerGui", "ValueLabel", "core/debug_interaction_logger", "works from anywhere - no teleport needed", ")", " node(s), ", "ค้นหาฟังก์ชัน...", "AutoMine", "[AutoSell] ", ": ", "prompt disabled", ") -> ", " zones=", " failed)", "autoUpgradePriority", "x", "core/teleport", "s - jumping the queue", "unreadable", ")", "ความเร็ววิ่ง", " budget=", " powerOk=", "auto", " script(s) to ", "[AutoPlace] disabled", "ItemFrame", "/", "BasePart", "ตั้งค่า", "no isfile/readfile available to verify the write", "MineBuySkill", "[AutoSell] enabled", "/", "empty", "AutoUpgrade", "debug.getupvalues", "MineBuyPickaxe", "Auto Research (Plot)", "Auto Upgrade: แจก Pickaxe ให้ miner", "sold ", "WalkSpeed (ปกติ 16)", "Players", "search_placeholder", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", " pickaxe(s) for ", "Workspace", "string", " (prompt says '", "disabled", "[AutoResearch] ", "Auto Chest", "^([%d,]+)$", "Text", "ปุ่มเปิดเมนู", "games/my_anime_mine/main", "float", "Players", "MineTakeChest", "Nodes", "MekPro", "ScrollingFrame", "yes", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", ": ", " ชิ้น", "/", " -> ", " ", "games/my_anime_mine/game_api", "auto", " holder(s) - not buying more", "core/dispatch", "core/logger", "=empty", "autoChestInterval", "string", "ลอย (Float)", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "[AutoChest] chest is empty", ") needs ", " remotes", "may need us at the stand - trying that", " mutation x", "autoUpgradePickaxeIndex", "  ", "[AutoUpgrade] ", "games/my_anime_mine/auto_research", "Players", "[GuiDump] PlayerGui not found", "TextLabel", "[AutoResearch] ", " (", "enabled", "[AutoUpgrade] ", "disabled", "(nothing to do)", "Frame", "-", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "\n", "RunService", " not bought yet (", "games/my_anime_mine/auto_research", "games/my_anime_mine/get_plot_parts", " -> x", "autoSell", "BodyVelocity", "🏠", " costs ", "Frame", "LocalScript", "core/anti_afk", "[AutoResearch] idle: money=", "hookfunction", "en", "MinePower", "(none)", " (", "core/logger", "open_menu_hint", "auto", "auto", "M", "fill", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", " ", "TextLabel", "Auto Place: เปลี่ยนตัวอ่อนออก", "games/my_anime_mine/get_current_plot", "autoResearchInterval", "getscripts", "[Decompile] === end ===", "core/dispatch", "th", "TextButton", "HumanoidRootPart", "core/logger", "Auto Research: ซื้อสูงสุดต่อรอบ", "", "s", " req=", "This is not a supported place.", "[AutoUpgrade] pickaxe #", "GetCharacterMineSpeedUpgradeCost", "TextButton", "HumanoidRootPart", "games/my_anime_mine/auto_place", "Auto Mine: คลิกต่อรอบ", "needs us at the stand - keeping the teleport", "games/my_anime_mine/game_api", "%.0f", ") on ", "core/debug_interaction_logger", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "ProximityPrompt", "[AutoUpgrade] ", "บิน (Fly)", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "Players", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "X", "[Decompile] wrote ", "", "core/scheduler", "^M(%d+)$", "games/my_anime_mine/auto_sell", "Speed", "AutoResearch", "[AutoResearch] nothing open and affordable right now", "function", "[AutoResearch] Core ", "TextLabel", " (x", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", " stack(s)", "table", "อ่านป้ายบน chest ทุกกี่วินาที", "TextButton", "slot ", " waited ", "", "อังกฤษ", "BasePart", "[GuiDump] === visible PlayerGui tree ===", "Chest", "Damage", "autoSellMaxWait", "Auto Sell: รอบขาย", "Auto Chest: เกณฑ์เก็บ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", " (ActionText=", "table", "", "HttpService", "[AutoMine] ", "TextButton", "[AutoChest] waiting: ", "Interaction Logger", "table", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[AutoResearch] ", "games/my_anime_mine/get_plot_parts", "AutoPlace", ": ", "debug.getproto", "games/my_anime_mine/game_api", "UserInputService", "Mutation: เพดานราคาต่อครั้ง", "autoResearchKeepMoney", "works from anywhere - dropping the teleport", " #", " open=", "button has no size on screen", "TextLabel", "TextLabel", "มุดแมพ (Noclip)", "Players", ")", "core/logger", "s", "RunService", "cost", "s", "^x(%d+)$", "getconnections", "Players", "core/debug_decompile", "table", "Auto Research: รอบเช็ค", "[^%d%.]", "Auto Upgrade Unit", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "[Scheduler] ", "games/my_anime_mine/auto_mine", "UICorner", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " - upgrade the miners first", "autoChestMinItems", " (money ", " items (now ", "games/my_anime_mine/game_api", "table", "lowest", ")", "My Anime Mine · Mining Hub", "[AutoResearch]   ", "", "[AntiAFK] disabled", "HotbarFrame", " files", "[GameAPI] couldn't load the game's modules: ", "UIListLayout", "[Remotes] === total: ", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "", "core/debug_dump_remotes", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", " screen(s)) ===", "UICorner", "sale didn't go through", "empty", "[%X] ", " (chest holds ", "Frame", "^%$", "", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "PlayerScripts", "[Interaction] ProximityPrompt triggered: ", "games/my_anime_mine/auto_chest", "autoClaimMaxPerPass", "s", "ProximityPrompt", "table", "core/logger", "autoResearch", "Auto Upgrade: กันเงินไว้", "core/logger", "Open menu", "core/teleport", "Workspace", "noclip", "[^%w]+", "games/my_anime_mine/auto_sell", "[AutoSell] MineSellItems ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "table", "x", "[Interaction] ClickDetector clicked: ", "Dump Visible GUI", " (", "autoUpgradePickaxe", "[AutoPlace] drifted out of range of ", "core/logger", " ===", "th", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "x", "ClickDetector", "MineEquipPickaxe", "en", "MekPro", "ItemName", "Auto Sell", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "ย่อขยายเมนูทั้งชุด", "GetCharacterSpeedUpgradeCost", "TextLabel", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", " error: ", "UserInputService", "MineSellItems", "core/teleport", "table", "/config.json", "core/i18n", "'", "GuiService", ")", "[AutoResearch] cheapest open node: ", "  ", "[AutoPlace] placed ", " '", "ขายทุกกี่วินาที", "core/ui", "autoClaimInterval", " ", ")", "autoChest", "[Config] couldn't write ", "แพงสุดก่อน", "core/humanoid", ": ", "[Uu]nlock", ")", "string", "core/humanoid", "games/my_anime_mine/game_api", "menuScale", " buyZones=", "autoPlaceUpgradeWeak", "[AutoResearch]   ", "core/logger", "autoResearchCheapestFirst", "Players", "core/build_env", " (Text=", "ProximityPrompt", "0 = ไม่จำกัด", "TextButton", "", "lowest", "", " cost=", "core/debug_tab", "AutoChest", "games/my_anime_mine/game_api", "ถูกสุดก่อน", "dev", "[Config] couldn't read ", "=", "#", " (Text=", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Hauler", "core/debug_decompile", "core/logger", "getscriptbytecode", "th", "Plots", "(", " -> ", " power=", " doesn't exist", "games/my_anime_mine/auto_upgrade", "Shop Stand", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "TextButton", "UserInputService", "  [attr] ", "AutoSell", "[AutoChest] enabled (collects once >= ", "Frame", "BodyPosition", "TextLabel", "Auto Claim (Index)", "", "Workspace", "Header", "%$", ") ", "languageIndex", "MekPro/decompiled", " node(s), ", "cost", "[AutoResearch]   Core: ", "autoUpgrade", "UIListLayout", "TextButton", "GetCharacterUpgradeCost", ",", "autoResearchZones", "[AutoChest] disabled", " x", "core/logger", "Players", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", " ", "table", "string", "[AutoPlace] slot ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "games/my_anime_mine/auto_mine", ": ", "autoUpgradeLimitPickaxes", "disabled", "Auto Upgrade: รอบเช็ค", "Search functions...", "[Interaction] logger disabled", " open and unmaxed", "TextLabel", "TextLabel", "games/my_anime_mine/auto_chest", "Badge", "GetHolderMineSpeedLevel", "core/config", "ValueLabel", "[Logger] failed to write ", "walkSpeed", "autoPlaceInterval", "core/logger", "[Decompile] PlayerScripts not found", " (", ": ", "Hauler", "write reported success but the line isn't in ", "[AutoUpgrade] every stat on every miner is maxed", "Players", "", " (x", "table", "PlotIndex", "s", "/", "games/my_anime_mine/get_current_plot", "🐛", " ", "[Decompile] === executor capabilities ===", "MekPro.log", "%$%s*([%d,%.]+)%s*(%a*)", "ไทย", "core/get_me", "function", "", "games/my_anime_mine/auto_upgrade", "Auto Place: รอบเช็ค", "Zones", "", "MineSpeed", "[AntiAFK] enabled - watching Player.Idled", "ClickDetector", "core/logger", "", "getgc", " (", "games/my_anime_mine/get_best_miner", ",", "Holders", ") ", " (cost ", "[Scheduler] turn: ", "languageIndex", "Pad", " -> ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "TextLabel", "/", "x", "ProximityPrompt", "All", "MekPro", "HumanoidRootPart", "waiting", "action '", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Workspace", " (power ", "Auto Place Miner", "CharSpeedMaxLevel", "Auto Claim: รอบเช็ค", "th", ") ", "autoSellMaxWait", "autoPlace", "[AutoClaim] claimed ", ") from ", " ", "Damage", "[AutoUpgrade] ", "games/my_anime_mine/get_plot_parts", "Empty miner slot", "autoUpgradeEquipPickaxe", "ฟาร์ม", "[AutoPlace] upgrading ", "s", "Auto Sell: รอคิวได้นานสุด", "core/logger", "autoClaim", " (", "autoUpgradeKeepMoney", "menuScale", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "getloadedmodules", "Players"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(102)] = function()
return function(import)
local log = import(S(714))
local Players = game:GetService(S(654))
local VirtualUser = game:GetService(S(161))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(769))
        connection = player.Idled:Connect(function()
            log(S(129))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(556))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(154)] = function()
return { Env = S(668), IsDev = true }
end
modules[S(228)] = function()
local VirtualInputManager = game:GetService(S(145))
local GuiService = game:GetService(S(627))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(521)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(202)] = function()
return function(import)
local HttpService = game:GetService(S(502))
local getPlaceId = import(S(303))
local log = import(S(740))
local ROOT_FOLDER = S(368)
local placeFolder = ROOT_FOLDER .. S(272) .. tostring(getPlaceId())
local filePath = placeFolder .. S(624)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(507) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(669) .. filePath .. S(512) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(639) .. filePath .. S(723) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(535)] = function()
return function(import)
local Players = game:GetService(S(350))
local log = import(S(381))
local CAPABILITIES = {
    S(46), S(677), S(439), S(773), S(823),
    S(533), S(266), S(230), S(164), S(108),
    S(344), S(513), S(422), S(224),
}
local OUT_FOLDER = S(702)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(174)) do
        if type(target) ~= S(215) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(757))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(187) .. name .. S(29) .. ((ok and type(value) == S(126)) and S(370) or S(273)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(20))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(576))
    if not playerScripts then
        log(S(741))
        return 0
    end
    pcall(makefolder, S(75))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(419)) or target:IsA(S(74)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(719) and #source > 0 then
                local name = target:GetFullName():gsub(S(591), S(103))
                if pcall(writefile, OUT_FOLDER .. S(335) .. name .. S(297), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(470) .. written .. S(332) .. OUT_FOLDER
        .. S(223) .. failed .. S(321))
    return written
end
local function run()
    log(S(146))
    reportCapabilities()
    local count = dumpAll()
    log(S(440))
    return count
end
return run
end
end
modules[S(87)] = function()
return function(import)
local Players = game:GetService(S(76))
local log = import(S(652))
local function dumpVisible(root, indent)
    indent = indent or S(217)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(44)) and child.Visible then
            local line = indent .. child.ClassName .. S(177) .. child.Name .. S(14)
            if child:IsA(S(480)) or child:IsA(S(443)) then
                line = line .. S(656) .. tostring(child.Text) .. S(90)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(689) .. attrName .. S(207) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(393))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(234))
    if not playerGui then
        log(S(397))
        return 0
    end
    local screens = 0
    log(S(492))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(299)) and screen.Enabled and screen.Name ~= S(611) then
            screens = screens + 1
            log(S(168) .. screen.ClassName .. S(632) .. screen.Name .. S(626))
            dumpVisible(screen, S(630))
        end
    end
    log(S(134) .. screens .. S(566))
    return screens
end
return dumpGui
end
end
modules[S(564)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(114))
local log = import(S(583))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(15)) or descendant:IsA(S(50)) then
            count = count + 1
            log(S(301) .. descendant.ClassName .. S(181) .. descendant:GetFullName())
        end
    end
    log(S(561) .. count .. S(603))
    return count
end
return dumpRemotes
end
end
modules[S(461)] = function()
return function(import)
local Players = game:GetService(S(715))
local Workspace = game:GetService(S(796))
local UserInputService = game:GetService(S(688))
local log = import(S(427))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(577) .. describe(prompt)
            .. S(499) .. prompt.ActionText .. S(55))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(597) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(249) .. describe(button)
            .. S(672) .. tostring(button.Text) .. S(12))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(788)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(770)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(281)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(728))
        return
    end
    connections = {}
    log(S(25))
    local playerGui = Players.LocalPlayer:WaitForChild(S(124))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(463)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(608)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(175)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(185),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(63)] = function()
return function(import)
local interactionLogger = import(S(310))(import)
local dumpRemotes = import(S(61))(import)
local dumpGui = import(S(80))(import)
local decompileScripts = import(S(675))(import)
local humanoid = import(S(641))
local function build(window, persist)
    local tab = window:AddTab(S(242), S(755))
    persist.Toggle(
        tab:AddCard(
            S(506),
            S(673)
        ),
        S(16),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(178),
        S(260)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(279), function()
        remoteBtn.Text = dumpRemotes() .. S(389)
    end)
    local guiCard = tab:AddCard(
        S(598),
        S(686)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(176), function()
        guiBtn.Text = dumpGui() .. S(11)
    end)
    local decompileCard = tab:AddCard(
        S(96),
        S(138)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(119), function()
        decompileBtn.Text = decompileScripts() .. S(558)
    end)
    humanoid.Start()
    persist.Slider(tab, S(328), S(349),
        8, 200, S(738), 16, humanoid.SetWalkSpeed, S(280))
    persist.Slider(tab, S(257), S(182),
        30, 300, S(264), 50, humanoid.SetJumpPower, S(489))
    persist.Toggle(
        tab:AddCard(S(465), S(468)),
        S(304), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(524), S(153)),
        S(590), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(385), S(565)),
        S(364), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(441)] = function()
return function(import)
    local getPlaceId = import(S(47))
    local registry = import(S(267))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(450))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(51)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(78))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(761)] = function()
local Players = game:GetService(S(534))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(166))
    local backpack = player:WaitForChild(S(26))
    return player, humanoid, backpack
end
return getMe
end
modules[S(148)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(646)] = function()
local Players = game:GetService(S(396))
local RunService = game:GetService(S(529))
local UserInputService = game:GetService(S(515))
local Workspace = game:GetService(S(52))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(255))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(454))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(415))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(491)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(4)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(693))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(625)] = function()
local Players = game:GetService(S(86))
local STRINGS = {
    en = {
        search_placeholder = S(727),
        open_menu_hint = S(587),
    },
    th = {
        search_placeholder = S(314),
        open_menu_hint = S(362),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(268)
    if locale:sub(1, 2):lower() == S(801) then
        return S(442)
    end
    return S(188)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(429) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(198)] = function()
local LOG_FILE = S(758)
local function log(message)
    local line = os.date(S(570)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(179))
            end
            appendfile(LOG_FILE, line .. S(408))
        elseif writefile then
            local existing = S(574)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(8))
        else
            error(S(81))
        end
        if not (isfile and readfile) then
            error(S(338))
        end
        if not isfile(LOG_FILE) then
            error(S(89) .. LOG_FILE .. S(683))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(745) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(737) .. LOG_FILE .. S(317) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(253)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(645) then
        return nil
    end
    local number, suffix = text:match(S(759))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(710), S(219))))
    if not value then
        return nil
    end
    if suffix ~= S(447) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(472)] = function()
return function(import)
local RunService = game:GetService(S(409))
local log = import(S(69))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(209) .. task_.name .. S(619) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(541) .. starved.name .. S(488)
            .. string.format(S(459), now - starved.waitingSince)
            .. S(325))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(780) .. (currentTask and currentTask.name or S(404)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(72)] = function()
local Players = game:GetService(S(365))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(791))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(634)] = function()
return function(import)
local i18n = import(S(286))
local UserInputService = game:GetService(S(620))
local THEME = {
    BrandName = S(790),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(543))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(567))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(200))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(289))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(563)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(140))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(276))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(191)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(398))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(555)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(369))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(560))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(785))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(418))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(206))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(523))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(694))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(736))
            if not valueLbl then
                valueLbl = Instance.new(S(522))
                valueLbl.Name = S(309)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(189))
            if not badge then
                badge = Instance.new(S(617))
                badge.Name = S(733)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(659))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(282))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(77))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(504))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(256)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(254)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(405))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(243))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(486))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(748)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(692))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(707))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(687))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(111)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(298))
    screenGui.Name = S(123)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(302))
    local main = Instance.new(S(572))
    main.Name = S(67)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(136))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(244))
    tabBar.Name = S(150)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(294))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(58))
    header.Name = S(698)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(32))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(731))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(295))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(180)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(708))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(469)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(453))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(406)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(231))
    searchBox.Name = S(24)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(351))
    searchBox.Text = S(471)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(208))
    content.Name = S(93)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(287))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(730))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(428))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(241))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(361)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(660) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(717) .. (item.Subtitle or S(662))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(84))
        footerLabel.Text = i18n.T(S(115))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(732)] = function()
return function(import)
local gameApi = import(S(647))(import)
local log = import(S(602))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(691) .. minItemsToCollect .. S(190))
    else
        log(S(712))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(48) then
            log(S(117))
            lastReported = S(326)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(569) then
            log(S(387))
            lastReported = S(342)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(792) then
            local capacity = gameApi.ChestCapacity()
            log(S(505) .. count .. S(374) .. want .. S(186)
                .. (capacity and (S(571) .. capacity .. S(104)) or S(696)))
            lastReported = S(112)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(41) .. count .. S(548) .. after .. S(552))
        else
            log(S(285) .. count
                .. S(375) .. tostring(after) .. S(327))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(292)] = function()
return function(import)
local gameApi = import(S(549))(import)
local log = import(S(771))
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(184) .. (enabled and S(401) or S(172)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(805) .. reward.category .. S(291)
                .. tostring(reward.tierKey) .. S(519) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(9) .. #pending .. S(35))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    task.spawn(runJob)
end
return autoClaim
end
end
modules[S(542)] = function()
return function(import)
local getCurrentPlot = import(S(754))
local getPlotParts = import(S(510))
local log = import(S(586))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(3), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(503) .. (enabled and S(91) or S(725)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(27)] = function()
return function(import)
local Players = game:GetService(S(525))
local getCurrentPlot = import(S(259))
local getPlotParts = import(S(412))
local getMinerSlots = import(S(775))(import)
local teleport = import(S(588))
local gameApi = import(S(227))(import)
local log = import(S(82))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(811)
local LOCKED_SLOT_TEXT = S(170)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(782))
    return pad and pad:FindFirstChildWhichIsA(S(581), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(435)) then
            local digits = descendant.Text:match(S(360))
            if digits then
                return tonumber((digits:gsub(S(776), S(39))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(17)) and descendant.Text:match(S(573)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(487) .. holder.Name .. S(410)
            .. gameApi.MinerSlots() .. S(59) .. gameApi.MaxMinerSlots() .. S(85)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(160)
    end
    if not prompt.Enabled then
        return true, S(318)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(159) .. price
    end
    local action = prompt.ActionText or S(239)
    if action:match(S(643)) or action:match(S(143)) or action:match(S(699)) then
        return true, S(793) .. action .. S(213)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(444))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(290) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(248) .. why .. S(644))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(382))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(670) .. prompt.ObjectText .. S(680) .. power .. S(628))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(226))
    if line ~= lastSurvey then
        log(S(125) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(62), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(13), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(70) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(333))
    else
        lastAttempt = 0
        log(S(618))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(462),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(23), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(233))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(601) .. holder.Name .. S(88))
            return false
        end
        log(S(720) .. candidate.slot .. S(426) .. candidate.name
            .. S(122))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(57) .. holder.Name .. S(204) .. miner.name)
        return false
    end
    log(S(631) .. miner.name .. S(278) .. miner.power
        .. S(460) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(211) .. entry.name .. S(98) .. entry.holder.Name
            .. S(356) .. tostring(entry.prompt.ActionText) .. S(151))
        return false
    end
    log(S(238) .. entry.name .. S(797) .. entry.power
        .. S(806) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(97))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(432) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(814) .. target.holder.Name .. S(743) .. target.name
        .. S(400) .. target.power .. S(319) .. best.name .. S(173) .. best.power .. S(22))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(411)] = function()
return function(import)
local gameApi = import(S(377))(import)
local log = import(S(527))
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(358) .. (enabled and S(130) or S(357)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(149) .. node.zoneKey .. S(288) .. node.id
                    .. S(236) .. node.family .. S(700) .. node.level .. S(783) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(479) .. node.id
                    .. S(599) .. node.family .. S(5) .. node.level .. S(681) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(509) .. blocked
                .. S(6))
        elseif powerBlocked then
            log(S(399) .. powerBlocked.zoneKey .. S(192) .. powerBlocked.id
                .. S(819) .. powerBlocked.family .. S(388)
                .. math.floor(powerBlocked.powerReq) .. S(305)
                .. math.floor(powerBlocked.power)
                .. S(545))
        else
            log(S(477)
                .. S(547) .. math.floor(gameApi.GetMoney()) .. S(526))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(629) .. nearest.zoneKey
                    .. S(756) .. nearest.id .. S(774) .. nearest.family .. S(802)
                    .. nearest.level .. S(341) .. nearest.max
                    .. S(417) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(421) .. math.floor(money)
        .. S(329) .. math.floor(budget)
        .. S(320) .. (#zones > 0 and table.concat(zones, S(83)) or S(425))
        .. S(649) .. tostring(buyZones) .. S(183) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(554) .. zoneKey .. S(636) .. tostring(node.id)
                        .. S(742) .. tostring(node.family) .. S(778)
                        .. node.level .. S(245) .. node.max
                        .. S(520) .. tostring(node.open)
                        .. S(663) .. tostring(node.cost)
                        .. S(330) .. tostring(node.powerOk)
                        .. S(449) .. tostring(node.powerReq)
                        .. S(682) .. tostring(node.power))
                end
            end
        end
        log(S(651) .. zoneKey .. S(642) .. #nodes
            .. S(313) .. pending .. S(106))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(705) .. #coreNodes .. S(703)
        .. coreOpen .. S(729))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(592)] = function()
return function(import)
local Workspace = game:GetService(S(589))
local gameApi = import(S(666))(import)
local teleport = import(S(622))
local log = import(S(676))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(121))
    else
        lastAttempt = 0
        log(S(340))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(2))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(657), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(593)
                .. (sold and S(518)
                         or S(457)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(316) .. (sold and (S(348) .. #keys .. S(483)) or S(568)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(684)] = function()
return function(import)
local Workspace = game:GetService(S(354))
local gameApi = import(S(458))(import)
local teleport = import(S(324))
local log = import(S(225))
local STATS = { S(808), S(19), S(169) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(661)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(394) .. (enabled and S(38) or S(403)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(750) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(196) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(530) or S(100)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(402) .. holderKey .. S(261) .. choice.stat .. S(434)
                        .. choice.info.level .. S(132) .. after.level
                        .. S(786) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(704) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(147))
    local shop = stand and stand:FindFirstChild(S(685))
    if not shop then
        return nil
    end
    if shop:IsA(S(205)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(336), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(1)
                .. (bought and S(311)
                           or S(390)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(466))
        end
    end
    if bought then
        log(S(135) .. pickaxe.index .. S(376) .. pickaxe.name
            .. S(481) .. pickaxe.multiplier .. S(312))
        return true
    end
    log(S(451) .. pickaxe.index .. S(807) .. pickaxe.name
        .. S(152))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(809) .. holderKey .. S(18)
                .. tostring(entry and entry.Pickaxe or S(197)) .. S(167) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(216) .. holderKey .. S(391) .. variant
                .. S(413) .. after .. S(779) .. cost .. S(247))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(464) .. gameApi.OwnedPickaxeCount()
                    .. S(353) .. gameApi.MinerSlots()
                    .. S(379))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(53))
        elseif maxed > 0 then
            log(S(746))
        else
            log(S(614))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(718) and entry.Pickaxe and entry.Pickaxe ~= S(763) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(514)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(127))
local log = import(S(307))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(66), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(559) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(582))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(536) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(744) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(623) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(550) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(674) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(473))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(42) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(366))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(789), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(595) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(621), keys)
end
function api.EquipSlot(key)
    return send(S(60), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(345), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(609), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(45), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(339), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(500)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(824)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(424))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(478) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(120) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(762) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(92), characterKey, stat == S(269) and S(768) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(235), max = S(105), cost = S(709) },
    Speed = { level = S(232), max = S(799), cost = S(616) },
    MineSpeed = { level = S(734), max = S(107), cost = S(452) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(484) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(251) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(221) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(274) and type(holder.CharName) == S(384) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(218) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(355) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(113)] = function()
return function(import)
local Players = game:GetService(S(467))
local log = import(S(445))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(308))
    local mineGui = playerGui and playerGui:FindFirstChild(S(284))
    local hotbar = mineGui and mineGui:FindFirstChild(S(557))
    local content = hotbar and hotbar:FindFirstChild(S(165))
    return content and content:FindFirstChild(S(237))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(157)) then
            local count = descendant.Text:match(S(532))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(334))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(128))
    local nameLabel = content and content:FindFirstChild(S(612))
    local damageLabel = content and content:FindFirstChild(S(494))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(767) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(538), S(501)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(753) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(296) .. slot .. S(372) .. info.name
                        .. S(199) .. info.power .. S(713) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(437)] = function()
local Players = game:GetService(S(747))
local Workspace = game:GetService(S(697))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(222))
    local plots = mineWorld and mineWorld:FindFirstChild(S(679))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(277)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(810)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(777)),
        Nodes = plot:FindFirstChild(S(367)),
        Chest = plot:FindFirstChild(S(493)),
        Zones = plot:FindFirstChild(S(766)),
        Crew = plot:FindFirstChild(S(262)),
        Loot = plot:FindFirstChild(S(64)),
        PlotIndex = plot:GetAttribute(S(751)),
        ChestCapacity = plot:GetAttribute(S(258)),
    }
end
return getPlotParts
end
modules[S(265)] = function()
return function(import)
    local createWindow = import(S(73))(import)
    local i18n = import(S(21))
    local antiAfk = import(S(420))(import)
    local buildEnv = import(S(655))
    local scheduler = import(S(133))(import)
    local config = import(S(735))(import)
    local log = import(S(817))
    local window = createWindow({
        Subtitle = S(553),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(722))(import)
    local autoPlace = import(S(455))(import)
    local autoChest = import(S(578))(import)
    local autoSell = import(S(474))(import)
    local autoUpgrade = import(S(764))(import)
    local autoResearch = import(S(395))(import)
    local autoClaim = import(S(155))(import)
    local gameApi = import(S(275))(import)
    scheduler.RegisterStandalone(S(315), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(665), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(511), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(343), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(476), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(79), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(495), 20)
    scheduler.RegisterExclusive(S(690), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(813), S(416))
    persistentToggle(farmTab:AddCard(
        S(195),
        S(28)
    ), S(33), autoMine.SetEnabled)
    persistentSlider(farmTab, S(162), S(352),
        1, 30, S(201), 5, autoMine.SetCyclesPerSecond, S(193))
    persistentSlider(farmTab, S(456), S(508),
        1, 10, S(68), 1, autoMine.SetClicksPerCycle, S(323))
    persistentToggle(farmTab:AddCard(
        S(798),
        S(544)
    ), S(804), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(765), S(606),
        1, 60, S(739), 3, autoPlace.SetCheckInterval, S(580))
    persistentToggle(
        farmTab:AddCard(S(436),
            S(194)),
        S(650), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(359),
        S(575)
    ), S(638), autoChest.SetEnabled)
    persistentSlider(farmTab, S(497), S(498),
        50, 3000, S(546), 100, autoChest.SetMinItems, S(373))
    persistentSlider(farmTab, S(139), S(485),
        1, 60, S(383), 3, autoChest.SetCheckInterval, S(448))
    persistentToggle(farmTab:AddCard(
        S(613),
        S(306)
    ), S(414), autoSell.SetEnabled)
    persistentSlider(farmTab, S(496), S(633),
        5, 300, S(252), 20, autoSell.SetCheckInterval, S(528))
    farmTab:AddSlider(S(816),
        S(142),
        5, 120, sellMaxWait, function(value)
            config.Set(S(803), value)
        end, S(531))
    persistentToggle(farmTab:AddCard(
        S(539),
        S(386)
    ), S(706), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(726), S(203),
        5, 300, S(65), 20, autoUpgrade.SetInterval, S(815))
    persistentSlider(farmTab, S(433), S(721),
        1, 20, S(220), 5, autoUpgrade.SetMaxPerPass, S(787))
    persistentSegmented(
        farmTab,
        { S(95), S(7), S(118), S(156) },
        { S(551), S(270), S(475), S(37) },
        S(322), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(585), S(540),
        0, 500, S(820), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(214))
    persistentToggle(
        farmTab:AddCard(S(94),
            S(794)),
        S(600), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(749) .. pickaxe.multiplier .. S(637)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(407)
            end
            return S(671) .. index .. S(210) .. (pickaxeLabels[index] or S(101))
        end
        local savedPick = config.Get(S(392), 0)
        pickaxeCard = farmTab:AddSlider(
            S(137),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(250), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(772)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(110),
            S(716)),
        S(724), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(347),
            S(822)),
        S(812), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(34),
            S(795)),
        S(10), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(36), S(482),
        1, 20, S(293), 2, autoUpgrade.SetMutationTarget, S(607))
    persistentSlider(farmTab, S(516), S(658),
        0, 500, S(240), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(431))
    persistentToggle(farmTab:AddCard(
        S(346),
        S(263)
    ), S(584), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(537), S(131),
        10, 600, S(438), 30, autoResearch.SetCheckInterval, S(49))
    persistentSlider(farmTab, S(446), S(30),
        1, 20, S(212), 5, autoResearch.SetMaxPerPass, S(596))
    persistentSlider(farmTab, S(300), S(605),
        0, 500, S(517), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(144))
    persistentToggle(
        farmTab:AddCard(S(158),
            S(99)),
        S(711), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(31),
            S(784)),
        S(71), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(229),
            S(562)),
        S(40), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(667), S(640) },
        { true, false },
        S(653), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(695),
        S(594)
    ), S(818), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(800), S(54),
        5, 300, S(635), 30, autoClaim.SetCheckInterval, S(752))
    persistentSlider(farmTab, S(109), S(371),
        1, 30, S(579), 10, autoClaim.SetMaxPerPass, S(43))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(664))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(337), S(246))
    persistentToggle(
        settingsTab:AddCard(S(283), S(163)),
        S(141),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(648), 100)
    settingsTab:AddSlider(S(171), S(615), 50, 150, savedScale, function(value)
        config.Set(S(821), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(781), 1)
    settingsTab:AddSegmented({ S(271), S(490), S(760) }, savedLanguage, function(index, value)
        local code = ({ S(331), S(423), S(678) })[index]
        config.Set(S(701), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(378), S(610), S(604) })[savedLanguage] or S(430))
    log(S(56) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(116)] = function()
return {
    [79389059854988] = S(363),
}
end
import(S(380))(import)
