-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Open menu", "Frame", "Players", "VirtualUser", "[AutoSell] SellButton never showed a price, label = '", "core/logger", " items)", "[AutoChest] fireproximityprompt error: ", "enabled", "[AutoSell] clicked SellButton", "core/logger", "Debug: Current Plot", "ProximityPrompt", "ScreenGui", "no plot", "Card", "core/logger", "Debug: Dump Node/Holder", "core/logger", "Content", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "", "[AutoChest] only ", "no character/backpack", "search_placeholder", "Players", "games/my_anime_mine/auto_upgrade", "UIScale", "ClickDetector", "[AutoChest] enabled (collects once >= ", "Tool", "games/registry", "games/my_anime_mine/auto_place", "ตั้งค่า", "search_placeholder", "", " '", "Frame", "Frame", "core/get_leaderstat", "games/my_anime_mine/get_plot_parts", "", "[AntiAFK] disabled", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "UIPadding", " ===", "Pad", "core/anti_afk", "Sell for", "[Interaction] Button activated: ", "ตามเครื่อง", "[%X] ", "Backpack", "AutoUpgrade", "[AutoSell] fireproximityprompt error: ", "-", "leaderstats", "Done", "[Logger] failed to write ", "Card", "Content", "core/teleport", "Tool", "Badge", " (Text=", "TextLabel", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "ค้นหาฟังก์ชัน...", " (ActionText=", ")", "(none enabled)", "[AutoSell] disabled", "Frame", "TextLabel", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "games/my_anime_mine/auto_sell", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "no empty holder found (", "Nodes", "Content", "games/my_anime_mine/debug_dump_remotes", "games/my_anime_mine/get_current_plot", "Loot", "Text", "core/dispatch", "'", "[SellCards] Card #", "[Scheduler] ", "Auto Mine", " = ", "TextButton", "enabled", "ChestFrame", "write reported success but the line isn't in ", "AutoMine", "ProximityPrompt", ": ", " (Text=", "TextLabel", "Dump", "th", "en", "core/logger", "Chest", "games/my_anime_mine/get_plot_parts", "UserInputService", "ChestCapacity", "TabBar", ",", "[Scheduler] turn: ", "Crew", "", "[Interaction] logger disabled", " on ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "GuiButton", ": ", "Players", "AutoChest", "[MyAnimeMine] not standing in any plot", "core/logger", "[MyAnimeMine] === Holder: ", "Debug: Dump Visible GUI", "TextLabel", "[AutoUpgrade] ", "core/ui", "Select All", "ProximityPrompt", "PlotIndex", "Frame", "no humanoid", "TextLabel", "ฟาร์ม", "  [attr] ", "not found", "no tool", "Auto Chest", "Label", "  ", "'", "UIListLayout", "en", "core/i18n", "[Interaction] Key pressed: %s (gameProcessed=%s)", "ปุ่มเปิดเมนู", "UIStroke", "MineWorld", "open_menu_hint", "X", "games/my_anime_mine/get_plot_parts", "[Interaction] logger enabled - go click/interact with things now", "th", "auto", ")", "PlayerGui", "SellFrame", "Content", "Frame", "Content", "\n", "RemoteFunction", "GuiObject", "', SelectAllButton label = '", "Done", "[AutoPlace] ", "games/my_anime_mine/get_current_plot", "[SellCards] === total cards: ", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", " holders checked)", "Debug: Dump Backpack Tool", "core/i18n", "TextLabel", "core/logger", "ClickDetector", " <-- remote, likely fired by the real interaction", "MineGui", "UIListLayout", "games/my_anime_mine/debug_dump_sell_cards", "SelectAllButton", "Frame", "[AntiAFK] idle detected, simulating input", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "🏠", "Frame", " (MaxActivationDistance=%s)", "UpgradePart", "TextLabel", "Zones", "อังกฤษ", "Actions", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "ValueBase", "TextLabel", "core/logger", "Close", "Anti-AFK", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Plots", "SellButton", "en-us", "games/my_anime_mine/main", "MineGui", " ===", "Text", "games/registry", "no isfile/readfile available to verify the write", "TextButton", "core/get_place_id", "UserInputService", "TextLabel", "Progress", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "neither appendfile nor writefile is available", "nil", "CoreUI", "  ", "disabled", "Backpack", "OwnerUserId", "", "", "th", " '", "games/my_anime_mine/auto_sell", "ClickDetector", "AutoSell", "MineGui", "ScrollingFrame", "[Remotes] ", "TextButton", "TextLabel", "core/logger", "Backpack", "Frame", "core/get_me", "TextButton", "TextBox", "ValueLabel", " items, closing", "[AutoSell] nothing to sell, closing", "[AutoChest] disabled", "  [attr] ", " error: ", "", "Scroll", "games/my_anime_mine/get_current_plot", "Debug: Dump All Remotes", "RemoteEvent", "TextButton", "ValueLabel", "TextButton", " cards", "Workspace", " = ", "", "core/logger", "games/my_anime_mine/debug_dump_remotes", "core/teleport", "Humanoid", "games/my_anime_mine/get_plot_parts", "PlayerGui", "CoreGui", "Header", "\n", "", "Holders", "  ", "'", "games/my_anime_mine/get_current_plot", " doesn't exist", "[GuiDump] === visible MineGui tree ===", "ProximityPrompt", "[AutoPlace] fireproximityprompt error: ", "Empty miner slot", "ProximityPrompt", "games/my_anime_mine/get_plot_parts", "core/teleport", "open_menu_hint", "TakeButton", "Workspace", "games/my_anime_mine/main", "ไทย", "?", "Dump", "Players", "GuiObject", "Dump", "[Interaction] ProximityPrompt triggered: ", "core/interaction_logger", "RemoteEvent", "[MyAnimeMine] no nodes present right now", "PlayerGui", "Workspace", " remotes", "UICorner", "UICorner", "Frame", "", "[AutoMine] ", "Players", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "TextButton", "", "Players", "not standing in any plot", ")", "Main", "TextButton", " items)", "[Remotes] === total: ", "[AutoPlace] placed ", "write reported success but ", " ===", "Done", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/auto_chest", "core/dispatch", "Players", "RunService", "MekPro.log", "games/my_anime_mine/auto_chest", "Search", "disabled", "core/logger", "TextLabel", "ClickDetector", "games/my_anime_mine/auto_place", "PlayerGui", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "Players", "Check", "games/my_anime_mine/auto_mine", "[MyAnimeMine] no holders found", "ย่อขยายเมนูทั้งชุด", "MineGui", "Sell for", "[AutoPlace] ", "Players", "[AutoSell] sell menu didn't open, aborting", "[AutoSell] enabled (only sells when there's something in the scroll)", " = ", "core/logger", "GuiButton", "ProximityPrompt", "Players", "games/my_anime_mine/get_current_plot", "", "games/my_anime_mine/debug_dump_gui", "games/my_anime_mine/debug_dump_sell_cards", "TextLabel", "  [attr] ", "Players", "[Interaction] ClickDetector clicked: ", "AutoPlace", "ขนาดเมนู", "Content", "Content", "Badge", ": ", "[MyAnimeMine] === Dump start ===", "'", "games/my_anime_mine/get_plot_parts", "[MyAnimeMine] === Node: ", "[AntiAFK] enabled - watching Player.Idled", "Auto Sell", "'", "", "This is not a supported place.", "[GuiDump] === end ===", "games/my_anime_mine/debug_dump", "HumanoidRootPart", "Interaction Logger (debug)", "", "Close", "auto", "ReplicatedStorage", "My Anime Mine · Mining Hub", "RemoteFunction", "Humanoid", " '", " '", " = ", "core/i18n", "[MyAnimeMine] === Tool: ", "UIListLayout", "games/my_anime_mine/debug_dump_gui", "disabled", "%", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "SellFrame", "[AutoChest] chest didn't open, aborting", "", "games/my_anime_mine/auto_upgrade", "  [attr] ", "plot has no Holders folder", "MekPro", "[AutoChest] clicked TakeButton (", "Error: ", "core/get_place_id", "core/interaction_logger", "log failed", "PlayerGui", "[AutoSell] clicked SelectAllButton (", "enabled", "Search functions...", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", " ", "Auto Place Miner", "Debug: Dump Sell Cards", "TextButton", "^(%d+)", " ===", "⚙", "%", "Dump", "core/anti_afk", "core/turn_scheduler", "Scroll", "core/logger", "Label", "core/turn_scheduler", " = ", "Frame", " ===", "Players", "Dump", "[GuiDump] MineGui not found", "core/ui", "ClickDetector", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "no tool in Backpack (all miners already placed?)", "SellStand", " cards)", "TextLabel", "games/my_anime_mine/auto_mine", "Auto Upgrade", "games/my_anime_mine/debug_dump"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(48)] = function()
return function(import)
local log = import(S(194))
local Players = game:GetService(S(345))
local VirtualUser = game:GetService(S(4))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(364))
        connection = player.Idled:Connect(function()
            log(S(181))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(43))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(317)] = function()
return function(import)
    local getPlaceId = import(S(399))
    local registry = import(S(32))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(368))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(40)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(57))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(235)] = function()
local Players = game:GetService(S(300))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(259))
    local backpack = player:WaitForChild(S(53))
    return player, humanoid, backpack
end
return getMe
end
modules[S(208)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(171)] = function()
local Players = game:GetService(S(352))
local STRINGS = {
    en = {
        search_placeholder = S(405),
        open_menu_hint = S(1),
    },
    th = {
        search_placeholder = S(68),
        open_menu_hint = S(145),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(200)
    if locale:sub(1, 2):lower() == S(101) then
        return S(152)
    end
    return S(142)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(375) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(289)] = function()
return function(import)
local Players = game:GetService(S(285))
local Workspace = game:GetService(S(253))
local UserInputService = game:GetService(S(209))
local log = import(S(6))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(288) .. describe(prompt)
            .. S(69) .. prompt.ActionText .. S(306))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(353) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(50) .. describe(button)
            .. S(65) .. tostring(button.Text) .. S(154))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(128)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(429)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(343)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(113))
        return
    end
    connections = {}
    log(S(151))
    local playerGui = Players.LocalPlayer:WaitForChild(S(261))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(13)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(326)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(116)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(144),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(342)] = function()
local LOG_FILE = S(320)
local function log(message)
    local line = os.date(S(52)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(244))
            end
            appendfile(LOG_FILE, line .. S(264))
        elseif writefile then
            local existing = S(112)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(160))
        else
            error(S(213))
        end
        if not (isfile and readfile) then
            error(S(206))
        end
        if not isfile(LOG_FILE) then
            error(S(312) .. LOG_FILE .. S(270))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(94) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(59) .. LOG_FILE .. S(97) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(277)] = function()
return function(import)
local Players = game:GetService(S(3))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(371))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
end
modules[S(421)] = function()
return function(import)
local RunService = game:GetService(S(319))
local log = import(S(121))
local SLICE_SECONDS = 3
local scheduler = {}
local tasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.Register(name, step, isEnabled)
    table.insert(tasks, { name = name, step = step, isEnabled = isEnabled })
end
local function pickNextEnabled()
    if #tasks == 0 then
        return nil
    end
    for _ = 1, #tasks do
        rrIndex = (rrIndex % #tasks) + 1
        local candidate = tasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(110) .. (currentTask and currentTask.name or S(71)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            local ok, err = pcall(currentTask.step)
            if not ok then
                log(S(88) .. currentTask.name .. S(243) .. tostring(err))
            end
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(428)] = function()
return function(import)
local i18n = import(S(383))
local UserInputService = game:GetService(S(106))
local THEME = {
    BrandName = S(396),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(296))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(295))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(45))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(230))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(36)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(146))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(325))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(283)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(132))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(220)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(228))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(385))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(434))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(158))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(99))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(193))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(350))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(250))
            if not valueLbl then
                valueLbl = Instance.new(S(172))
                valueLbl.Name = S(238)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(64))
            if not badge then
                badge = Instance.new(S(231))
                badge.Name = S(358)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(251))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(184))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(180))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(236))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(265)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(388))
        local track = Instance.new(S(38))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(423))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(207))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(22)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(414)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(2))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(141))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(308))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(204)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(14))
    screenGui.Name = S(215)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(262))
    local main = Instance.new(S(130))
    main.Name = S(307)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(28))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(234))
    tabBar.Name = S(108)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(177))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(297))
    header.Name = S(263)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(410))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(74))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(66))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(298)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(249))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(149)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(91))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(56)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(237))
    searchBox.Name = S(322)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(35))
    searchBox.Text = S(367)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(39))
    content.Name = S(61)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(73))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(210))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(148))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(187))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(84)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(255) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(407) .. (item.Subtitle or S(303))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(25))
        footerLabel.Text = i18n.T(S(278))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(316)] = function()
return function(import)
local Players = game:GetService(S(304))
local getCurrentPlot = import(S(82))
local getPlotParts = import(S(41))
local teleport = import(S(62))(import)
local log = import(S(11))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(155))
    local mineGui = playerGui and playerGui:FindFirstChild(S(227))
    return mineGui and mineGui:FindFirstChild(S(93))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(356))
    local actions = content and content:FindFirstChild(S(190))
    return actions and actions:FindFirstChild(S(279))
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(157))
    local progress = content and content:FindFirstChild(S(211))
    local label = progress and progress:FindFirstChild(S(138))
    local text = label and label.Text or S(373)
    local digits = text:gsub(S(109), S(347)):match(S(411))
    return tonumber(digits)
end
local function clickButton(button)
    pcall(function()
        firesignal(button.Activated)
    end)
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(344), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(241))
    else
        lastAttempt = 0
        log(S(30) .. MIN_ITEMS_TO_COLLECT .. S(309))
    end
end
function autoChest.IsEnabled()
    return enabled
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(8) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.3)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(391))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    if count and count < MIN_ITEMS_TO_COLLECT then
        log(S(23) .. count .. S(239))
        local closeButton = chestFrame:FindFirstChild(S(195))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        log(S(397) .. tostring(count) .. S(7))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(435)] = function()
return function(import)
local getCurrentPlot = import(S(269))
local getPlotParts = import(S(276))
local log = import(S(173))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(29), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(299) .. (enabled and S(92) or S(217)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(33)] = function()
return function(import)
local Players = game:GetService(S(425))
local getCurrentPlot = import(S(315))
local getPlotParts = import(S(105))
local log = import(S(17))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(47))
    return pad and pad:FindFirstChildWhichIsA(S(96), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(274)
end
local autoPlace = {}
local enabled = false
local lastFire = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(337) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    log(S(165) .. (enabled and S(404) or S(323)))
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local player = Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild(S(233))
    if not character or not backpack then
        reason(S(24))
        return
    end
    local humanoid = character:FindFirstChildOfClass(S(379))
    local tool = backpack:FindFirstChildWhichIsA(S(31))
    if not humanoid then
        reason(S(131))
        return
    end
    if not tool then
        reason(S(431))
        return
    end
    local plot = getCurrentPlot()
    if not plot then
        reason(S(305))
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        reason(S(395))
        return
    end
    local placed = false
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderEmpty(prompt) then
            humanoid:EquipTool(tool)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if ok then
                log(S(311) .. tool.Name .. S(114) .. holder.Name)
            else
                log(S(273) .. tostring(err))
            end
            placed = true
            break
        end
    end
    if not placed then
        reason(S(78) .. #parts.Holders:GetChildren() .. S(169))
    else
        lastReason = nil
    end
end
return autoPlace
end
end
modules[S(224)] = function()
return function(import)
local Players = game:GetService(S(338))
local Workspace = game:GetService(S(280))
local teleport = import(S(258))(import)
local log = import(S(103))
local CHECK_INTERVAL = 3
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(328))
    local mineGui = playerGui and playerGui:FindFirstChild(S(202))
    return mineGui and mineGui:FindFirstChild(S(390))
end
local function labelText(button)
    local content = button:FindFirstChild(S(20))
    local label = content and content:FindFirstChild(S(420))
    return label and label.Text or S(42)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(80))
    local scroll = content and content:FindFirstChild(S(245))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(60) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    pcall(function()
        firesignal(button.Activated)
    end)
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(432))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(275), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(72))
    else
        lastAttempt = 0
        log(S(340))
    end
end
function autoSell.IsEnabled()
    return enabled
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(55) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.3)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(339))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(240))
        local closeButton = sellFrame:FindFirstChild(S(374))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(159))
    local selectAllButton = content and content:FindFirstChild(S(179))
    local sellButton = content and content:FindFirstChild(S(199))
    if selectAllButton and labelText(selectAllButton) == S(127) then
        clickButton(selectAllButton)
        log(S(403) .. cardCount .. S(433))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(49), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(336), 1, true) then
        clickButton(sellButton)
        log(S(10))
    else
        log(S(5) .. tostring(sawLabel)
            .. S(163) .. (selectAllButton and labelText(selectAllButton) or S(214)) .. S(86))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(27)] = function()
return function(import)
local getCurrentPlot = import(S(246))
local getPlotParts = import(S(150))
local log = import(S(19))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(186))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(174))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(125) .. (enabled and S(9) or S(387)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(437)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(392)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(381) .. child.Name .. S(268)
        if child:IsA(S(272)) then
            line = line .. string.format(
                S(212),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(225)) then
            line = line .. string.format(S(185), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(248)) or child:IsA(S(378)) then
            line = line .. S(175)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(242) .. attrName .. S(422) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(139))
    end
end
return dumpInstance
end
modules[S(386)] = function()
return function(import)
local Players = game:GetService(S(26))
local log = import(S(324))
local function dumpVisible(root, indent)
    indent = indent or S(221)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(162)) and child.Visible then
            local line = indent .. child.ClassName .. S(223) .. child.Name .. S(366)
            if child:IsA(S(124)) or child:IsA(S(302)) then
                line = line .. S(98) .. tostring(child.Text) .. S(70)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(351) .. attrName .. S(254) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(216))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(402))
    local mineGui = playerGui and playerGui:FindFirstChild(S(176))
    if not mineGui then
        log(S(427))
        return
    end
    log(S(271))
    dumpVisible(mineGui)
    log(S(369))
end
return dumpMineGui
end
end
modules[S(257)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(376))
local log = import(S(419))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(290)) or descendant:IsA(S(161)) then
            count = count + 1
            log(S(229) .. descendant.ClassName .. S(117) .. descendant:GetFullName())
        end
    end
    log(S(310) .. count .. S(203))
    return count
end
return dumpRemotes
end
end
modules[S(178)] = function()
return function(import)
local Players = game:GetService(S(118))
local log = import(S(232))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(292))
    local mineGui = playerGui and playerGui:FindFirstChild(S(335))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(156))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(357)) and sellFrame.Content:FindFirstChild(S(418))
    if not scroll then
        log(S(67))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(16) or card:IsA(S(286)) then
            count = count + 1
            log(S(87) .. count .. S(359) .. card.ClassName .. S(37) .. card.Name .. S(140))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(394) .. attrName .. S(341) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(267) .. child.ClassName .. S(380) .. child.Name .. S(361)
                if child:IsA(S(192)) then
                    line = line .. S(382) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(167) .. count .. S(424))
    return count
end
return dumpSellCards
end
end
modules[S(346)] = function()
local Players = game:GetService(S(318))
local Workspace = game:GetService(S(293))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(147))
    local plots = mineWorld and mineWorld:FindFirstChild(S(198))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(219)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(260)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(266)),
        Nodes = plot:FindFirstChild(S(79)),
        Chest = plot:FindFirstChild(S(104)),
        Zones = plot:FindFirstChild(S(188)),
        Crew = plot:FindFirstChild(S(111)),
        Loot = plot:FindFirstChild(S(83)),
        PlotIndex = plot:GetAttribute(S(129)),
        ChestCapacity = plot:GetAttribute(S(107)),
    }
end
return getPlotParts
end
modules[S(281)] = function()
return function(import)
    local Players = game:GetService(S(330))
    local createWindow = import(S(126))(import)
    local i18n = import(S(143))
    local antiAfk = import(S(416))(import)
    local interactionLogger = import(S(400))(import)
    local scheduler = import(S(417))(import)
    local log = import(S(256))
    local window = createWindow({
        Subtitle = S(377),
    })
    local getCurrentPlot = import(S(166))
    local getPlotParts = import(S(362))
    local dumpInstance = import(S(370))
    local dumpRemotes = import(S(81))(import)
    local dumpSellCards = import(S(349))(import)
    local dumpGui = import(S(348))(import)
    local autoMine = import(S(332))(import)
    local autoPlace = import(S(327))(import)
    local autoChest = import(S(321))(import)
    local autoSell = import(S(76))(import)
    local autoUpgrade = import(S(393))(import)
    scheduler.Register(S(95), autoMine.Step, autoMine.IsEnabled)
    scheduler.Register(S(354), autoPlace.Step, autoPlace.IsEnabled)
    scheduler.Register(S(119), autoChest.Step, autoChest.IsEnabled)
    scheduler.Register(S(226), autoSell.Step, autoSell.IsEnabled)
    scheduler.Register(S(54), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.Start()
    local farmTab = window:AddTab(S(133), S(183))
    farmTab:AddCard(
        S(89),
        S(182)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(408),
        S(191)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(137),
        S(430)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(365),
        S(168)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(436),
        S(75)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(12), S(21))
    local checkBtn
    checkBtn = debugCard:AddButton(S(331), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(135)
            log(S(120))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(197),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(18),
        S(406)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(100), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(15)
            return
        end
        local ok, err = log(S(360))
        if not ok then
            dumpBtn.Text = S(401)
            dumpCard:SetSubtitle(S(398) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(363) .. firstNode.Name .. S(412))
            dumpInstance(firstNode, log)
        else
            log(S(291))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(122) .. firstHolder.Name .. S(313))
            dumpInstance(firstHolder, log)
        else
            log(S(333))
        end
        dumpBtn.Text = S(164)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(170),
        S(77)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(284), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(218))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(63))
        if not tool then
            toolDumpBtn.Text = S(136)
            return
        end
        log(S(384) .. tool.Name .. S(46))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(134) .. attrName .. S(90) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(314)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(247),
        S(329)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(415), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(294)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(409),
        S(44)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(426), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(252)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(123),
        S(389)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(287), function()
        dumpGui()
        guiDumpBtn.Text = S(58)
    end)
    local settingsTab = window:AddTab(S(34), S(413))
    settingsTab:AddCard(S(196), S(115)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(372),
        S(301)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(355), S(334), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(51), S(189), S(282) }, 1, function(index, value)
        local code = ({ S(153), S(102), S(222) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(205)] = function()
return {
    [79389059854988] = S(201),
}
end
import(S(85))(import)
