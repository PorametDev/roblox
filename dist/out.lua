-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/click", "[Handlers] ", "sl", "Content", "core/ui", "Content", "Players", "autoResearchMaxPerPass", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "decompile", "", "autoResearch", "ไล่อัปเกรด miner ทุกกี่วินาที", "Frame", "พิมพ์โครงสร้าง GameState.GetData2().MineGame เพื่อหา key ที่ remote ต้องใช้", "ไทย", "costs ", "float", "Debug: Dump Chest Data", " ===", ")", "waiting", "HumanoidRootPart", "Frame", " stack(s)", "^([%d,]+)$", "games/my_anime_mine/auto_sell", ": ", "Content", "ตามเครื่อง", "log failed", "core/anti_afk", "function", "games/my_anime_mine/get_current_plot", ") on ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "HotbarFrame", "games/my_anime_mine/get_plot_parts", "core/logger", "Luck", "Holders", "🐛", "games/my_anime_mine/auto_place", "[AutoPlace] drifted out of range of ", "Content", "Open menu", "", "Scroll", "AutoMine", "autoUpgrade", "MineHotbarClient", "Players", " attributes ===", "Content", "TextLabel", "Tool", " ===", "SellButton", "games/my_anime_mine/debug_dump_state", "MineGui", "ความเร็ววิ่ง", "%$%s*([%d,%.]+)%s*(%a*)", "", " = ", "core/get_place_id", "TextLabel", "MekPro", "function", "Place", "", "locked slot", " while selecting", "TextLabel", "games/my_anime_mine/debug_dump_sell_cards", "no", "[AutoPlace] upgrading ", "Humanoid", "[MyAnimeMine] not standing in any plot", "⚙", ") ===", "ValueBase", "core/logger", "ขนาดเมนู", "TextLabel", "^%$", "[AutoMine] ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "BodyPosition", "core/logger", "[Handlers]   ", "StatsPanel", "UserInputService", "core/logger", "  [attr] ", "=empty", "core/interaction_logger", " ชิ้น", "มุดแมพ (Noclip)", "core/logger", "table", "games/my_anime_mine/auto_upgrade", "[AutoPlace] nothing in the hotbar to place", "(", "Nodes", " ", "UI", "'", "MineUpgradeCharacter", "TextButton", "Search functions...", "[StateDump] === MineGame ===", "'", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "search_placeholder", "core/get_place_id", "", "disabled", "CharLevel", "BasePart", "[MyAnimeMine] === Dump start ===", "games/my_anime_mine/debug_dump_sell_cards", "ClickDetector", "core/config", "'", "games/my_anime_mine/get_best_miner", "[Uu]nlock", "Detail", "table", " items (now ", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "TextButton", " (MaxActivationDistance=%s)", "  ", "[SellCards] === total cards: ", "PlayerScripts", "ClickDetector", "Players", "yes", "x", "[Interaction] Key pressed: %s (gameProcessed=%s)", " (", "PlayerGui", ",", "Interaction Logger", "  ", "Search", "MineSpeed", "games/my_anime_mine/auto_upgrade", "HumanoidRootPart", "Auto Place: รอบเช็ค", "", "boolean", "\n", " ", ") from ", "Backpack", ")", " '", "Sell SelectAllButton", "table(", "core/humanoid", "table", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "fly", "TextButton", "button has no size on screen", "Debug: Dump Visible GUI", "[Remotes] === total: ", "games/my_anime_mine/debug_probe_env", "autoSell", "UIListLayout", "", "Workspace", "Done", "MineSlotFocusClient", "MineHUDClient", ")", "[AutoSell] enabled", "[AutoChest] collected ", "ซื้อ research ทุก family ในโซนที่ปลดแล้วผ่าน remote ตรง (ไม่เปิด UI)", "core/logger", "core/i18n", "Empty miner slot", "core/teleport", "Dump", "Auto Chest", "en", "th", "prompt disabled", "[StateDump] no mine data (is the game API loaded?)", "Players", "autoChestInterval", "core/config", "[AutoResearch] ", "write failed", "Players", "Frame", "[AutoPlace] slot ", " = ", "Main", "NodeCount", "[AutoChest] disabled", "[MyAnimeMine] no holders found", "games/registry", "s", "UIPadding", ")", "interactionLogger", "Badge", " = ", "Auto Chest: รอบเช็ค", ")", "x", " | ", "hookfunction", "en", "Frame", "อ่านป้ายบน chest ทุกกี่วินาที", " chars) -> ", "Dump", " (Text=", "core/anti_afk", "games/my_anime_mine/debug_inspect_handlers", "core/scheduler", "AutoChest", "SkillTreeFullFrame", "SellFrame", " ===", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", "[ChestDump] plot has no Chest", "", "[ChestDump] === ReplicatedStorage.", "RemoteEvent", "[Probe] ", "[AutoUpgrade] ", "games/my_anime_mine/auto_place", " remotes", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "core/logger", "UIStroke", "autoUpgradeInterval", "Workspace", "ScrollingFrame", " -> ", ")", "games/my_anime_mine/debug_dump_remotes", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "core/parse_money", "[AntiAFK] disabled", "Done", "", "[MekPro] settings file: ", " = ", "[BestMiner] slot ", "Frame", "Modules", "[AutoPlace] ", "Done", "th", ")", "games/my_anime_mine/debug_dump_gui", "getloadedmodules", "[MyAnimeMine] === Node: ", "MineTakeChest", "Chest TakeButton", "", "MineGui", "UpgradeButton", "games/my_anime_mine/get_plot_parts", "บิน (Fly)", "ปุ่มเปิดเมนู", "s", "Players", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "Auto Mine", "games/my_anime_mine/game_api", "games/my_anime_mine/debug_dump_chest", ")", "TextLabel", "debug.getproto", "TextButton", "core/logger", "games/my_anime_mine/game_api", "[Config] couldn't write ", "/s", "MineGui", "  ", ": ", "upgrade", "games/my_anime_mine/auto_mine", " error: ", "Players", "games/my_anime_mine/get_plot_parts", "getsenv", "neither appendfile nor writefile is available", " cards", "WalkSpeed (ปกติ 16)", "", "Auto Sell: รอบขาย", "RemoteFunction", "TextLabel", "games/my_anime_mine/get_current_plot", "autoMineSpeed", "nothing to place or upgrade", "BasePart", " doesn't exist", "UserInputService", "Debug: Inspect Button Handlers", "/", "TextButton", "Debug: Dump Sell Cards", "CoreGui", "  [attr] ", "Actions", "UICorner", "[Interaction] ClickDetector clicked: ", "core/logger", "[AutoSell] disabled", "  [upvalue] ", " (ActionText=", "getcallbackvalue", "disabled", "Workspace", "Auto Mine: คลิกต่อรอบ", "BodyVelocity", "ValueBase", "write reported success but ", " x", "MineEquipSlot", "PlayerGui", "games/my_anime_mine/debug_dump_chest", "MekPro.log", " = ", "Players", "Frame", "  (debug.getconstants unavailable)", " parts", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "[AutoUpgrade] ", "Dump", "MineBuySkill", " (", "All", "open_menu_hint", "อังกฤษ", "[AutoPlace] fireproximityprompt error: ", "[ChestDump] === end ===", "games/my_anime_mine/debug_dump_gui", " ", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "Check", " from ", "Auto Research (Plot)", "Content", ": ", "", " (", "Backpack", "[Bb]uy", "?", "UICorner", "debug.getconstants", "MineSkillTreeClient", "leaderstats", "debug.getupvalues", " ", "core/logger", "core/interaction_logger", "Content", " (prompt says '", "/", "open_menu_hint", "Debug: Dump All Remotes", " -> ", " not found", "MineWorld", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "แรงกระโดด", "Players", "[MyAnimeMine] === Holder: ", "MineGame", "This is not a supported place.", "Players", "SellStand", ".lua", "languageIndex", "games/my_anime_mine/auto_chest", " ===", "[Interaction] Button activated: ", "OwnerUserId", "ตั้งค่า", "SellFrame", "games/my_anime_mine/get_best_miner", "/", "^x(%d+)$", "My Anime Mine · Mining Hub", "ProximityPrompt", "Sell SellButton", "table", "Probe", "[GuiDump] MineGui not found", "Text", "core/logger", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", ") didn't take, trying the next one", "  [attr] ", "[AutoSell] MineSellItems ", "[Handlers] getconnections isn't available in this executor", "[Probe] === end ===", "autoSellInterval", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "SlotFocusFrame", "CharSpeedLevel", "[Remotes] ", "  ", "Zones", "", ") -> ", "Loot", "Frame", "ProximityPrompt", " = ", "", "TextLabel", "Inspect", "auto", "Auto Upgrade: รอบเช็ค", "TextLabel", "core/logger", "GuiObject", "[Handlers] === ", "Instance", "ลอย (Float)", "Dump", "TextLabel", "[Interaction] logger disabled", "autoPlaceInterval", "[AutoUpgrade] nothing upgraded (maxed out or not enough cash)", "MineSellItems", " still empty after placing ", "th", "", "PlayerGui", ",", "VirtualUser", " = ", "[GameAPI] SendEvent failed: ", "TextLabel", ": ", "AutoResearch", "core/humanoid", "[AutoPlace] disabled", "TextLabel", "core/dispatch", "string", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "Damage", "[Handlers] === end ===", "[^%d%.]", "languageIndex", "[Probe] decompile unavailable - can't read the game's source this way", "write reported success but the line isn't in ", "", "'", "[AutoChest] waiting: ", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "BuyButton", "Debug: Dump Node/Holder", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Auto Research: ซื้อสูงสุดต่อรอบ", "[%X] ", "[AntiAFK] idle detected, simulating input", "ValueLabel", "Frame", "games/my_anime_mine/get_current_plot", "core/logger", "games/my_anime_mine/debug_dump", "Locked miner slot", "ดีบัก", "[Probe] ", "games/my_anime_mine/auto_sell", "MineSellClient", "menuScale", "getgc", "HttpService", "TextButton", "[AutoResearch] ", "sale didn't go through", "Dump", "[MyAnimeMine] === Tool: ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "number", ": ", "[StateDump] === end ===", "UserInputService", "MineGui", "games/my_anime_mine/game_api", "[Handlers] ", " ", " -> ", "TabBar", " = ", "/config.json", "Header", "[AutoPlace] placed ", "Workspace", "PlayerGui", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "GuiButton", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "[^%.]+", "enabled", "autoChestMinItems", "🏠", "core/logger", "games/my_anime_mine/game_api", "PlotIndex", "", "games/my_anime_mine/auto_mine", "search_placeholder", " (Text=", " = ", "[ChestDump] === plot ", "TextBox", "ReplicatedStorage", "Players", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "UIListLayout", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "[AntiAFK] enabled - watching Player.Idled", "[GameAPI] couldn't load the game's modules: ", "[SellCards] Card #", " power ", "  [attr] ", "VirtualInputManager", "'", "action '", "[AutoSell] ", " (power ", "(nothing to do)", "s", "games/my_anime_mine/game_api", " items", "core/teleport", "[attr] ", " (Text=", "core/logger", "Damage", "games/my_anime_mine/auto_research", "UIScale", "เดินทะลุกำแพงและพื้น", "  [const] ", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "unreadable", "ClickDetector", "Humanoid", "Text", "Mining", "ProximityPrompt", " items)", "needs us at the stand - keeping the teleport", "Auto Upgrade Unit", "defined at ", "games/my_anime_mine/get_current_plot", "[ChestDump] === Player subtree ===", "TextButton", "games/my_anime_mine/get_plot_parts", "core/dispatch", "games/my_anime_mine/debug_dump_state", "string", "[Scheduler] turn: ", "GuiObject", "TakeButton", "Players", "[GuiDump] === visible MineGui tree ===", "Auto Research: รอบเช็ค", "not found", "Players", "waiting", "menuScale", "ReplicatedStorage", "')", "autoResearchInterval", "function", "games/my_anime_mine/main", ")", "Speed", " '", "ChestFrame", "x", " '", "TextLabel", "Debug: Current Plot", "en-us", " ", "games/my_anime_mine/get_current_plot", "  (debug.getupvalues unavailable)", "core/scheduler", "autoUpgradeMaxPerPass", " connection(s)", "enabled", ": ", "RemoteFunction", "getconnections", "autoMine", "[Interaction] logger enabled - go click/interact with things now", "GuiService", "MekPro/decompiled_", "X", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", " ===", "no isfile/readfile available to verify the write", "Instance", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "th", "s", "RunService", "[Handlers]   #", "Done", "ReplicatedStorage", "Chest", "core/logger", "[AutoChest] can't read the chest yet", "[Logger] failed to write ", "ClickDetector", "games/my_anime_mine/debug_inspect_handlers", "Damage", "TextLabel", "\n", "MineChestClient", "SellFrame", ": ", "noclip", "ProximityPrompt", "ฟาร์ม", "disabled", "Research BuyButton", "getnilinstances", "core/teleport", "unreadable", "[AutoPlace] picked up ", "getscripts", "RemoteEvent", "games/my_anime_mine/debug_dump", "Debug: Probe Executor + Scripts", "%", "autoMineClicks", "core/logger", "games/registry", "", "[AutoChest] MineTakeChest had no effect (", "Auto Mine: ความเร็ว", "[Scheduler] ", "", "core/i18n", "[MyAnimeMine] no nodes present right now", "Container", "no tool", "MouseButton1Click", "CharMineSpeedLevel", "ItemName", " (power ", "Plots", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "TextButton", "string", "core/get_leaderstat", "autoPlace", "table", "ScreenGui", "", " '", "fill", "[ChestDump] not standing in any plot", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", " (", "table", "[AutoResearch] nothing bought (maxed out or not enough cash)", " ===", "MineSpeed", "ChestCapacity", "auto", "games/my_anime_mine/main", "Unit Damage UpgradeButton", "UIListLayout", "Players", "'", "AutoSell", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "TextButton", "core/click", "[Probe] PlayerScripts.UI.MineGame not found", "core/get_me", "Done", "JumpPower (ปกติ 50)", "table", "[AutoPlace] ", " '", "games/my_anime_mine/debug_dump_remotes", "autoChest", "AutoUpgrade", "enabled", "[ChestDump] === Chest subtree ===", "games/my_anime_mine/get_plot_parts", "เปิดหน้า research ทุกกี่วินาที", "sold ", "CoreUI", "Folder", "SelectAllButton", "Auto Sell", "ValueLabel", "TextLabel", "PlayerGui", "antiAfk", "Frame", "[GuiDump] === end ===", "Dump", "no plot", "core/logger", "games/my_anime_mine/debug_probe_env", "%$", "s", "[Probe] decompiled ", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "RunService", "ค้นหาฟังก์ชัน...", "jumpPower", "core/logger", "[Probe] === executor capabilities ===", "table", "Pad", "=locked(", "Anti-AFK", "RemoteFunction", "NodeHealth", " <-- remote, likely fired by the real interaction", "core/logger", "", "fill", "works from anywhere - dropping the teleport", "[Interaction] ProximityPrompt triggered: ", "Error: ", "=", ": not found", "games/my_anime_mine/auto_research", "Done", "Activated", "Scroll", "", "ProximityPrompt", " ", "TextLabel", "  ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[AutoPlace] couldn't pick up ", ": ", "[Handlers] === start (open the relevant menus first) ===", "TextLabel", "en", "RemoteEvent", "Frame", "AutoPlace", "Card", "Badge", "[AutoPlace] holders: ", "Auto Chest: เกณฑ์เก็บ", "-", "getrenv", "[Config] couldn't read ", "BottomPanels", "ItemFrame", "core/i18n", "Debug: Dump Backpack Tool", "HumanoidRootPart", "ย่อขยายเมนูทั้งชุด", "Auto Place Miner", "Players", "  ", "TextLabel", " (", "Crew", "core/get_place_id", "core/logger", "GuiButton", "Debug: Dump Game State", "walkSpeed", ")", "TextButton", "auto", "[Probe] === start ===", "auto", "games/my_anime_mine/game_api", "  ", "core/ui", "table", "", ": ", "games/my_anime_mine/auto_chest", "Frame", " ", "Dump", "getscriptbytecode", "  ", "[AutoChest] enabled (collects once >= ", ":", "[Probe] couldn't decompile ", "MekPro"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(32)] = function()
return function(import)
local log = import(S(476))
local Players = game:GetService(S(686))
local VirtualUser = game:GetService(S(445))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(530))
        connection = player.Idled:Connect(function()
            log(S(472))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(249))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(1)] = function()
local VirtualInputManager = game:GetService(S(535))
local GuiService = game:GetService(S(607))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(166)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(193)] = function()
return function(import)
local HttpService = game:GetService(S(485))
local getPlaceId = import(S(115))
local log = import(S(93))
local ROOT_FOLDER = S(807)
local placeFolder = ROOT_FOLDER .. S(309) .. tostring(getPlaceId())
local filePath = placeFolder .. S(503)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(100) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(769) .. filePath .. S(493) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(284) .. filePath .. S(28) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(568)] = function()
return function(import)
    local getPlaceId = import(S(782))
    local registry = import(S(204))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(382))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(667)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(364))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(693)] = function()
local Players = game:GetService(S(526))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(77))
    local backpack = player:WaitForChild(S(156))
    return player, humanoid, backpack
end
return getMe
end
modules[S(65)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(451)] = function()
local Players = game:GetService(S(383))
local RunService = game:GetService(S(617))
local UserInputService = game:GetService(S(307))
local Workspace = game:GetService(S(173))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(556))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(149))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(325))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(119)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(305)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(88))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(772)] = function()
local Players = game:GetService(S(578))
local STRINGS = {
    en = {
        search_placeholder = S(110),
        open_menu_hint = S(46),
    },
    th = {
        search_placeholder = S(726),
        open_menu_hint = S(271),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(594)
    if locale:sub(1, 2):lower() == S(259) then
        return S(615)
    end
    return S(759)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(426) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(96)] = function()
return function(import)
local Players = game:GetService(S(52))
local Workspace = game:GetService(S(242))
local UserInputService = game:GetService(S(495))
local log = import(S(515))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(741) .. describe(prompt)
            .. S(320) .. prompt.ActionText .. S(245))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(316) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(389) .. describe(button)
            .. S(521) .. tostring(button.Text) .. S(207))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(397)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(122)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(784)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(436))
        return
    end
    connections = {}
    log(S(606))
    local playerGui = Players.LocalPlayer:WaitForChild(S(507))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(559)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(136)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(509)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(140),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(39)] = function()
local LOG_FILE = S(332)
local function log(message)
    local line = os.date(S(471)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(650))
            end
            appendfile(LOG_FILE, line .. S(153))
        elseif writefile then
            local existing = S(423)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(629))
        else
            error(S(295))
        end
        if not (isfile and readfile) then
            error(S(612))
        end
        if not isfile(LOG_FILE) then
            error(S(327) .. LOG_FILE .. S(306))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(462) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(624) .. LOG_FILE .. S(756) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(248)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(455) then
        return nil
    end
    local number, suffix = text:match(S(62))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(143), S(116))))
    if not value then
        return nil
    end
    if suffix ~= S(749) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(224)] = function()
return function(import)
local RunService = game:GetService(S(725))
local log = import(S(181))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(653) .. task_.name .. S(291) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(571) .. (currentTask and currentTask.name or S(540)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(544)] = function()
local Players = game:GetService(S(777))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(774))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(5)] = function()
return function(import)
local i18n = import(S(182))
local UserInputService = game:GetService(S(92))
local THEME = {
    BrandName = S(67),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(361))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(315))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(206))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(486))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(417)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(240))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(779))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(360)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(752))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(463)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(243))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(685))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(279))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(761))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(73))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(55))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(448))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(473))
            if not valueLbl then
                valueLbl = Instance.new(S(66))
                valueLbl.Name = S(711)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(764))
            if not badge then
                badge = Instance.new(S(592))
                badge.Name = S(209)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(109))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(474))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(217))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(165))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(47)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(646)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(335))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(14))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(131))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(151)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(799))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(171))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(281))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(402)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(670))
    screenGui.Name = S(707)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(312))
    local main = Instance.new(S(255))
    main.Name = S(200)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(550))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(420))
    tabBar.Name = S(501)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(528))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(197))
    header.Name = S(504)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(665))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(453))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(758))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(231)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(566))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(609)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(690))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(767)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(524))
    searchBox.Name = S(146)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(114))
    searchBox.Text = S(63)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(715))
    content.Name = S(54)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(24))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(424))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(372))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(84))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(557)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(172) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(154) .. (item.Subtitle or S(251))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(520))
        footerLabel.Text = i18n.T(S(344))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(798)] = function()
return function(import)
local gameApi = import(S(283))(import)
local log = import(S(737))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(804) .. minItemsToCollect .. S(560))
    else
        log(S(202))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(554) then
            log(S(623))
            lastReported = S(640)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(579) then
            log(S(465) .. count .. S(371) .. minItemsToCollect .. S(543))
            lastReported = S(22)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(179) .. count .. S(129) .. after .. S(278))
        else
            log(S(651) .. count
                .. S(374) .. tostring(after) .. S(212))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(519)] = function()
return function(import)
local getCurrentPlot = import(S(302))
local getPlotParts = import(S(269))
local log = import(S(783))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(555), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(86) .. (enabled and S(512) or S(636)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(236)] = function()
return function(import)
local Players = game:GetService(S(191))
local getCurrentPlot = import(S(475))
local getPlotParts = import(S(38))
local getMinerSlots = import(S(125))(import)
local teleport = import(S(639))
local click = import(S(691))
local log = import(S(82))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(183)
local LOCKED_SLOT_TEXT = S(478)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(731))
    return pad and pad:FindFirstChildWhichIsA(S(634), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(301)) then
            local digits = descendant.Text:match(S(26))
            if digits then
                return tonumber((digits:gsub(S(444), S(11))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(435)) and descendant.Text:match(S(85)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(71)
    end
    if not prompt.Enabled then
        return true, S(189)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(17) .. price
    end
    local action = prompt.ActionText or S(356)
    if action:match(S(126)) or action:match(S(359)) or action:match(S(721)) then
        return true, S(537) .. action .. S(536)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(23))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(346) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(732) .. why .. S(157))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(95))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(743) .. prompt.ObjectText .. S(103) .. power .. S(260))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(214))
    if line ~= lastSurvey then
        log(S(765) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(739), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(289), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(697) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(452))
    else
        lastAttempt = 0
        log(S(377))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(163),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(69), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(102))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(44) .. holder.Name .. S(72))
            return false
        end
        log(S(198) .. candidate.slot .. S(342) .. candidate.name
            .. S(405))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(257) .. holder.Name .. S(440) .. miner.name)
        return false
    end
    log(S(505) .. miner.name .. S(539) .. miner.power
        .. S(35) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(755) .. entry.name .. S(352) .. entry.holder.Name
            .. S(370) .. tostring(entry.prompt.ActionText) .. S(582))
        return false
    end
    log(S(641) .. entry.name .. S(662) .. entry.power
        .. S(155) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(304))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(673) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(76) .. target.holder.Name .. S(797) .. target.name
        .. S(676) .. target.power .. S(418) .. best.name .. S(780) .. best.power .. S(21))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(549)] = function()
return function(import)
local gameApi = import(S(516))(import)
local log = import(S(728))
local KNOWN_FAMILIES = { S(201), S(40), S(735) }
local checkInterval = 30
local maxPerPass = 5
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(487) .. (enabled and S(601) or S(322)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
local function unlockedZones()
    local mine = gameApi.GetMineData()
    local zones = mine and mine.UnlockedZones or {}
    local list = {}
    for zoneKey, unlocked in pairs(zones) do
        if unlocked then
            table.insert(list, zoneKey)
        end
    end
    table.sort(list)
    return list
end
local function skillLevel(zoneKey, family)
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    return skills and skills[family] or 0
end
local function familiesFor(zoneKey)
    local seen = {}
    local list = {}
    for _, family in ipairs(KNOWN_FAMILIES) do
        seen[family] = true
        table.insert(list, family)
    end
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    if skills then
        for family in pairs(skills) do
            if not seen[family] then
                table.insert(list, family)
            end
        end
    end
    return list
end
local function runJob()
    busy = true
    local bought = 0
    for _, zoneKey in ipairs(unlockedZones()) do
        for _, family in ipairs(familiesFor(zoneKey)) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local before = skillLevel(zoneKey, family)
            gameApi.BuySkill(zoneKey, family)
            task.wait(0.4)
            local after = skillLevel(zoneKey, family)
            if after > before then
                log(S(194) .. zoneKey .. S(800) .. family .. S(366)
                    .. before .. S(500) .. after)
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(678))
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(481)] = function()
return function(import)
local Workspace = game:GetService(S(323))
local gameApi = import(S(497))(import)
local teleport = import(S(184))
local log = import(S(99))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(318))
    else
        lastAttempt = 0
        log(S(178))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(384))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(750), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(407)
                .. (sold and S(740)
                         or S(561)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(538) .. (sold and (S(706) .. #keys .. S(25)) or S(488)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(148)] = function()
return function(import)
local gameApi = import(S(792))(import)
local log = import(S(367))
local STATS = {
    { event = S(548), field = S(118) },
    { event = S(587), field = S(413) },
    { event = S(680), field = S(660) },
}
local checkInterval = 20
local maxPerPass = 5
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(339) .. (enabled and S(702) or S(117)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function levelOf(holderKey, field)
    local entry = holders()[holderKey]
    return entry and entry[field] or nil
end
local function weakestStat(entry)
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local level = entry[stat.field]
        if level and (not lowest or level < lowest.level) then
            lowest = { event = stat.event, field = stat.field, level = level }
        end
    end
    return lowest
end
local function runJob()
    busy = true
    local bought = 0
    for holderKey, entry in pairs(holders()) do
        if not enabled or bought >= maxPerPass then
            break
        end
        local stat = weakestStat(entry)
        if stat then
            gameApi.UpgradeCharacter(holderKey, stat.event)
            task.wait(0.4)
            local after = levelOf(holderKey, stat.field)
            if after and after > stat.level then
                log(S(235) .. holderKey .. S(595) .. stat.event .. S(105)
                    .. stat.level .. S(244) .. after)
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(438))
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(644)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(796)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(588) .. child.Name .. S(107)
        if child:IsA(S(421)) then
            line = line .. string.format(
                S(466),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(625)) then
            line = line .. string.format(S(132), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(760)) or child:IsA(S(734)) then
            line = line .. S(736)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(94) .. attrName .. S(446) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(133))
    end
end
return dumpInstance
end
modules[S(277)] = function()
return function(import)
local Players = game:GetService(S(196))
local ReplicatedStorage = game:GetService(S(581))
local getCurrentPlot = import(S(596))
local getPlotParts = import(S(704))
local log = import(S(403))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(545) .. name .. S(199) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(803))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(591) .. child.Name .. S(687)
            if child:IsA(S(81)) then
                line = line .. S(422) .. tostring(child.Value)
            elseif child:IsA(S(428)) or child:IsA(S(788)) then
                line = line .. S(546) .. tostring(child.Text) .. S(177)
            end
            log(line)
            dumpSubtree(child, indent .. S(287))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(674))
        return 0
    end
    log(S(523) .. plot.Name .. S(53))
    dumpAttributes(plot, S(793))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(230))
        return 0
    end
    log(S(703))
    dumpSubtree(parts.Chest, S(738))
    local player = Players.LocalPlayer
    log(S(565))
    dumpSubtree(player, S(70))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(232) .. player.Name .. S(57))
        dumpSubtree(mine, S(298))
    end
    log(S(347))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(261)] = function()
return function(import)
local Players = game:GetService(S(7))
local log = import(S(429))
local function dumpVisible(root, indent)
    indent = indent or S(671)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(430)) and child.Visible then
            local line = indent .. child.ClassName .. S(158) .. child.Name .. S(112)
            if child:IsA(S(712)) or child:IsA(S(310)) then
                line = line .. S(221) .. tostring(child.Text) .. S(787)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(534) .. attrName .. S(333) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(778))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(330))
    local mineGui = playerGui and playerGui:FindFirstChild(S(286))
    if not mineGui then
        log(S(401))
        return
    end
    log(S(575))
    dumpVisible(mineGui)
    log(S(716))
end
return dumpMineGui
end
end
modules[S(699)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(620))
local log = import(S(89))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(233)) or descendant:IsA(S(603)) then
            count = count + 1
            log(S(414) .. descendant.ClassName .. S(355) .. descendant:GetFullName())
        end
    end
    log(S(168) .. count .. S(679))
    return count
end
return dumpRemotes
end
end
modules[S(74)] = function()
return function(import)
local Players = game:GetService(S(292))
local log = import(S(317))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(443))
    local mineGui = playerGui and playerGui:FindFirstChild(S(496))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(227))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(29)) and sellFrame.Content:FindFirstChild(S(48))
    if not scroll then
        log(S(527))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(763) or card:IsA(S(572)) then
            count = count + 1
            log(S(532) .. count .. S(288) .. card.ClassName .. S(672) .. card.Name .. S(124))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(313) .. attrName .. S(64) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(415) .. child.ClassName .. S(698) .. child.Name .. S(464)
                if child:IsA(S(326)) then
                    line = line .. S(522) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(134) .. count .. S(228))
    return count
end
return dumpSellCards
end
end
modules[S(569)] = function()
return function(import)
local gameApi = import(S(276))(import)
local log = import(S(282))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(669) then
        local count = 0
        for _ in pairs(value) do
            count = count + 1
        end
        return S(160) .. count .. S(586)
    end
    return valueType .. S(349) .. tostring(value)
end
local function dumpTable(value, indent, depth)
    for key, entry in pairs(value) do
        log(indent .. tostring(key) .. S(253) .. describe(entry))
        if type(entry) == S(162) and depth > 0 then
            dumpTable(entry, indent .. S(753), depth - 1)
        end
    end
end
local function dumpState()
    local mine = gameApi.GetMineData()
    if not mine then
        log(S(190))
        return 0
    end
    log(S(111))
    dumpTable(mine, S(145), 2)
    log(S(494))
    return 1
end
return dumpState
end
end
modules[S(626)] = function()
return function(import)
local Players = game:GetService(S(574))
local log = import(S(547))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(432) then
        return value.ClassName .. S(499) .. value:GetFullName()
    end
    if valueType == S(795) then
        return S(399)
    end
    if valueType == S(33) then
        return S(68)
    end
    return valueType .. S(751) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(613) then
        return value:IsA(S(643)) or value:IsA(S(300)) or value:IsA(S(708))
    end
    return valueType == S(570) or valueType == S(492) or valueType == S(152)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(3))
    end)
    if ok and source then
        log(indent .. S(563) .. tostring(source) .. S(805) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(730) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(552) .. describe(constant))
            end
        end
    else
        log(indent .. S(336))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(677) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(319) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(597))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(2) .. label .. S(744))
        return
    end
    log(S(431) .. label .. S(141) .. button:GetFullName() .. S(80))
    if not getconnections then
        log(S(408))
        return
    end
    for _, signalName in ipairs({ S(747), S(659) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(498) .. signalName .. S(632) .. #connections .. S(600))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(618) .. index)
                    inspectFunction(fn, S(90))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(713))
    local current = playerGui and playerGui:FindFirstChild(S(60))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(757))
    inspectButton(S(265), find({ S(589), S(6), S(314), S(573) }))
    inspectButton(S(159), find({ S(392), S(369), S(709) }))
    inspectButton(S(398), find({ S(631), S(45), S(58) }))
    inspectButton(S(637), find({ S(226), S(127), S(467) }))
    local statsScroll = find({ S(412), S(770), S(91), S(748) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(627))
    inspectButton(S(684), damageRow and damageRow:FindFirstChild(S(268)))
    log(S(458))
end
return inspectHandlers
end
end
modules[S(169)] = function()
return function(import)
local Players = game:GetService(S(379))
local log = import(S(239))
local CAPABILITIES = {
    S(10), S(802), S(642), S(484), S(262),
    S(604), S(321), S(294), S(768), S(362),
    S(365), S(280), S(215), S(638),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(511)) do
        if type(target) ~= S(128) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(729))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(480) .. name .. S(502) .. ((ok and type(value) == S(584)) and S(138) or S(75)))
    end
end
local WANTED_SCRIPTS = {
    S(630),
    S(482),
    S(363),
    S(175),
    S(51),
    S(176),
}
local function tryDecompile()
    if not decompile then
        log(S(461))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(135))
    local ui = playerScripts and playerScripts:FindFirstChild(S(106))
    local mineGame = ui and ui:FindFirstChild(S(381))
    if not mineGame then
        log(S(692))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(234) .. name .. S(375))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(666) and #source > 0 then
                local path = S(608) .. name .. S(385)
                local written = pcall(writefile, path, source)
                log(S(723) .. name .. S(357) .. #source .. S(219)
                    .. (written and path or S(195)))
            else
                log(S(806) .. name .. S(602) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(790))
    reportCapabilities()
    tryDecompile()
    log(S(409))
end
return probe
end
end
modules[S(542)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(525))
local log = import(S(719))
local api = {}
local gameState, mineInventory
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(256), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
    end)
    if not ok then
        loadFailed = true
        log(S(531) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetMineData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return nil
    end
    return data.MineGame
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(447) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(264))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(343), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(696) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(439), keys)
end
function api.EquipSlot(key)
    return send(S(329), key)
end
function api.BuySkill(nodeId, family)
    return send(S(341), nodeId, family)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(108), characterKey, stat == S(558) and S(147) or stat)
end
return api
end
end
modules[S(393)] = function()
return function(import)
local Players = game:GetService(S(137))
local log = import(S(622))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(142))
    local mineGui = playerGui and playerGui:FindFirstChild(S(267))
    local hotbar = mineGui and mineGui:FindFirstChild(S(37))
    local content = hotbar and hotbar:FindFirstChild(S(4))
    return content and content:FindFirstChild(S(657))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(628)) then
            local count = descendant.Text:match(S(395))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(771))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(354))
    local nameLabel = content and content:FindFirstChild(S(661))
    local damageLabel = content and content:FindFirstChild(S(457))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(442) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(459), S(518)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(394) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(254) .. slot .. S(449) .. info.name
                        .. S(533) .. info.power .. S(328) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(564)] = function()
local Players = game:GetService(S(273))
local Workspace = game:GetService(S(506))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(376))
    local plots = mineWorld and mineWorld:FindFirstChild(S(663))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(390)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(293)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(41)),
        Nodes = plot:FindFirstChild(S(104)),
        Chest = plot:FindFirstChild(S(621)),
        Zones = plot:FindFirstChild(S(416)),
        Crew = plot:FindFirstChild(S(781)),
        Loot = plot:FindFirstChild(S(419)),
        PlotIndex = plot:GetAttribute(S(517)),
        ChestCapacity = plot:GetAttribute(S(681)),
    }
end
return getPlotParts
end
modules[S(585)] = function()
return function(import)
    local Players = game:GetService(S(334))
    local createWindow = import(S(794))(import)
    local i18n = import(S(655))
    local antiAfk = import(S(222))(import)
    local interactionLogger = import(S(368))(import)
    local scheduler = import(S(598))(import)
    local config = import(S(123))(import)
    local log = import(S(648))
    local window = createWindow({
        Subtitle = S(396),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(34))
    local getPlotParts = import(S(567))
    local dumpInstance = import(S(477))
    local dumpRemotes = import(S(246))(import)
    local dumpSellCards = import(S(121))(import)
    local dumpGui = import(S(348))(import)
    local dumpChest = import(S(331))(import)
    local inspectHandlers = import(S(223))(import)
    local probeEnv = import(S(720))(import)
    local dumpState = import(S(59))(import)
    local humanoid = import(S(161))
    local autoMine = import(S(290))(import)
    local autoPlace = import(S(43))(import)
    local autoChest = import(S(387))(import)
    local autoSell = import(S(27))(import)
    local autoUpgrade = import(S(101))(import)
    local autoResearch = import(S(745))(import)
    scheduler.RegisterStandalone(S(49), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(225), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(762), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(701), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(450), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(688), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(635), S(514))
    persistentToggle(farmTab:AddCard(
        S(275),
        S(610)
    ), S(605), autoMine.SetEnabled)
    persistentSlider(farmTab, S(652), S(689),
        1, 30, S(303), 5, autoMine.SetCyclesPerSecond, S(285))
    persistentSlider(farmTab, S(324), S(754),
        1, 10, S(647), 1, autoMine.SetClicksPerCycle, S(590))
    persistentToggle(farmTab:AddCard(
        S(776),
        S(491)
    ), S(668), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(150), S(36),
        1, 60, S(437), 3, autoPlace.SetCheckInterval, S(541))
    persistentToggle(farmTab:AddCard(
        S(186),
        S(9)
    ), S(700), autoChest.SetEnabled)
    persistentSlider(farmTab, S(766), S(87),
        50, 3000, S(513), 100, autoChest.SetMinItems, S(97))
    persistentSlider(farmTab, S(211), S(218),
        1, 60, S(192), 3, autoChest.SetCheckInterval, S(205))
    persistentToggle(farmTab:AddCard(
        S(710),
        S(238)
    ), S(170), autoSell.SetEnabled)
    persistentSlider(farmTab, S(299), S(553),
        10, 300, S(410), 45, autoSell.SetCheckInterval, S(616))
    persistentToggle(farmTab:AddCard(
        S(562),
        S(529)
    ), S(50), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(427), S(13),
        5, 300, S(241), 20, autoUpgrade.SetInterval, S(722))
    persistentSlider(farmTab, S(113), S(247),
        1, 20, S(599), 5, autoUpgrade.SetMaxPerPass, S(213))
    persistentToggle(farmTab:AddCard(
        S(353),
        S(180)
    ), S(12), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(576), S(705),
        10, 600, S(583), 30, autoResearch.SetCheckInterval, S(272))
    persistentSlider(farmTab, S(470), S(411),
        1, 20, S(8), 5, autoResearch.SetMaxPerPass, S(139))
    local debugTab = window:AddTab(S(479), S(42))
    persistentToggle(
        debugTab:AddCard(
            S(144),
            S(338)
        ),
        S(208),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(593), S(469))
    local checkBtn
    checkBtn = debugCard:AddButton(S(351), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(577)
            log(S(78))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(614),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(468),
        S(456)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(801), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(718)
            return
        end
        local ok, err = log(S(120))
        if not ok then
            dumpBtn.Text = S(31)
            dumpCard:SetSubtitle(S(742) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(263) .. firstNode.Name .. S(611))
            dumpInstance(firstNode, log)
        else
            log(S(656))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(380) .. firstHolder.Name .. S(388))
            dumpInstance(firstHolder, log)
        else
            log(S(203))
        end
        dumpBtn.Text = S(174)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(773),
        S(274)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(220), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(358))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(56))
        if not tool then
            toolDumpBtn.Text = S(658)
            return
        end
        log(S(490) .. tool.Name .. S(20))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(406) .. attrName .. S(210) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(250)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(373),
        S(724)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(185), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(237)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(311),
        S(130)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(489), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(296)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(167),
        S(510)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(434), function()
        dumpGui()
        guiDumpBtn.Text = S(746)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(19),
        S(675)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(340), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(337)
    end)
    local handlersCard = debugTab:AddCard(
        S(308),
        S(229)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(425), function()
        inspectHandlers()
        handlersBtn.Text = S(258)
    end)
    local probeCard = debugTab:AddCard(
        S(645),
        S(404)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(400), function()
        probeEnv()
        probeBtn.Text = S(694)
    end)
    local stateCard = debugTab:AddCard(
        S(785),
        S(15)
    )
    local stateBtn
    stateBtn = stateCard:AddButton(S(717), function()
        dumpState()
        stateBtn.Text = S(619)
    end)
    humanoid.Start()
    persistentSlider(debugTab, S(61), S(297),
        8, 200, S(786), 16, humanoid.SetWalkSpeed, S(266))
    persistentSlider(debugTab, S(378), S(695),
        30, 300, S(727), 50, humanoid.SetJumpPower, S(654))
    persistentToggle(
        debugTab:AddCard(S(270), S(350)),
        S(164), humanoid.SetFlying
    )
    persistentToggle(
        debugTab:AddCard(S(98), S(551)),
        S(633), humanoid.SetNoclip
    )
    persistentToggle(
        debugTab:AddCard(S(433), S(508)),
        S(18), humanoid.SetFloating
    )
    local settingsTab = window:AddTab(S(391), S(79))
    persistentToggle(
        settingsTab:AddCard(S(733), S(664)),
        S(714),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(483), 100)
    settingsTab:AddSlider(S(83), S(775), 50, 150, savedScale, function(value)
        config.Set(S(580), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(386), 1)
    settingsTab:AddSegmented({ S(30), S(345), S(16) }, savedLanguage, function(index, value)
        local code = ({ S(682), S(216), S(188) })[index]
        config.Set(S(460), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(791), S(187), S(441) })[savedLanguage] or S(789))
    log(S(252) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        humanoid.Stop()
    end)
end
end
modules[S(649)] = function()
return {
    [79389059854988] = S(683),
}
end
import(S(454))(import)
