-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/teleport", " from ", "Label", "Dump", "Frame", "TextLabel", "TextLabel", "Anti-AFK", "[ChestDump] === ReplicatedStorage.", "Content", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "core/logger", "s", " = ", "[Interaction] Key pressed: %s (gameProcessed=%s)", " '", "", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "ProximityPrompt", "[Uu]nlock", "games/my_anime_mine/debug_dump_gui", "[Interaction] logger disabled", "[AutoPlace] placed ", "enabled", "write reported success but the line isn't in ", "auto", "[AntiAFK] disabled", "games/my_anime_mine/auto_place", "autoPlace", "Frame", "TextLabel", "[attr] ", "ClickDetector", "TextButton", "'", "autoMineSpeed", "Auto Mine: คลิกต่อรอบ", "Tool", "ค้นหาฟังก์ชัน...", "Debug: Dump Visible GUI", "games/my_anime_mine/auto_chest", " = ", "[Interaction] ClickDetector clicked: ", "", "[AutoChest] disabled", "[AutoSell] disabled", "VirtualUser", "Frame", "TextButton", "Frame", "PlayerGui", "Debug: Dump All Remotes", "[MekPro] settings file: ", "auto", "  ", "TextLabel", "core/get_place_id", "Players", "ขนาดเมนู", "UICorner", "[AutoPlace] drifted out of range of ", "autoMineSpeed", "ProximityPrompt", "'", "games/my_anime_mine/get_current_plot", "ChestCapacity", "antiAfk", "games/my_anime_mine/get_best_miner", " ===", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Sell for", " | ", "  [attr] ", "?", "ChestFrame", "games/my_anime_mine/get_plot_parts", "  ", "Auto Mine: ความเร็ว", "MineGui", "", "[AutoChest] fireproximityprompt error: ", "PlayerGui", "/", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "core/click", "fill", "fill", "Label", "core/logger", "[Config] couldn't write ", "core/teleport", "[Logger] failed to write ", "MineWorld", "Debug: Dump Node/Holder", "[AutoPlace] couldn't pick up ", "Done", "HumanoidRootPart", "^%$", ")", "core/i18n", " parts", "Scroll", "games/my_anime_mine/get_current_plot", "MekPro.log", "[AutoChest] ", "'", "[AutoPlace] disabled", "[Scheduler] turn: ", "PlayerGui", "[AutoUpgrade] ", "", "open_menu_hint", "en", "[AutoChest] enabled (collects once >= ", "core/logger", "RunService", "[ChestDump] === Player subtree ===", "Dump", "TextButton", "core/logger", "prompt disabled", "ฟาร์ม", " '", "core/i18n", "games/my_anime_mine/auto_chest", "Content", "games/my_anime_mine/auto_mine", "", "[AutoSell] enabled (only sells when there's something in the scroll)", "SelectAllButton", "games/my_anime_mine/auto_sell", "\n", "nothing to place or upgrade", "autoMine", "ProximityPrompt", "TextLabel", "UIScale", "X", " power ", "ValueBase", "PlayerGui", "Players", ")", "", "interactionLogger", "th", "core/get_place_id", "", "TextButton", "Backpack", "UIPadding", "th", "SellFrame", "costs ", " (MaxActivationDistance=%s)", "core/dispatch", "Humanoid", "no plot", "games/my_anime_mine/debug_dump", " cards left, label = '", ": ", "🏠", "[AutoSell] sell menu didn't open, aborting", "games/my_anime_mine/get_plot_parts", " = ", ": ", "[AutoChest] chest didn't open, aborting", "TextLabel", ") -> ", "Check", "TextLabel", "", "  ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "games/my_anime_mine/get_current_plot", "th", "Done", "MekPro", "autoUpgrade", "  [attr] ", " '", "Search", "enabled", "", "Auto Chest", "Card", "[MyAnimeMine] === Tool: ", "Badge", "games/my_anime_mine/get_plot_parts", " cards", "TextButton", "UpgradePart", "SelectOverlay", "core/logger", "RemoteEvent", "core/logger", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/debug_dump_remotes", "ItemFrame", "UserInputService", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "core/get_place_id", ",", "UICorner", "SellButton", "core/click", "[AutoPlace] nothing in the hotbar to place", " (Text=", "GuiObject", "[AntiAFK] idle detected, simulating input", "[AutoPlace] slot ", "no isfile/readfile available to verify the write", "RemoteEvent", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "", "core/anti_afk", " ", "games/my_anime_mine/auto_upgrade", "Players", "leaderstats", "", "Content", "games/my_anime_mine/main", "TakeButton", ") didn't take, trying the next one", " items)", "core/teleport", "", "Search functions...", "Auto Sell", "", "[Config] couldn't read ", " <-- remote, likely fired by the real interaction", "ตามเครื่อง", "SellFrame", "[AutoSell] clicked SelectAllButton (", "VirtualInputManager", "TextButton", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "core/config", "languageIndex", "ไทย", "Auto Upgrade", "ProximityPrompt", "Holders", "core/logger", "AutoPlace", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "Scroll", "CoreGui", ": ", "GuiService", " (power ", "Amount", "games/my_anime_mine/get_plot_parts", "[AutoPlace] ", "OwnerUserId", "TextButton", "[Interaction] logger enabled - go click/interact with things now", " -> ", "HttpService", ": ", " ===", "TextButton", "[AutoSell] nothing to sell, closing", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "'", "ClickDetector", "Close", " (ActionText=", "Backpack", "[ChestDump] === end ===", "MineGui", "Debug: Dump Chest Data", "core/logger", "AutoSell", "games/my_anime_mine/debug_dump_sell_cards", "Players", "Frame", "ValueLabel", "UserInputService", "Progress", "search_placeholder", "games/my_anime_mine/get_plot_parts", "autoChest", "Scroll", "games/my_anime_mine/get_current_plot", "ProximityPrompt", "Header", "^(%d+)", "[AutoSell] click failed on ", "locked slot", "games/my_anime_mine/debug_dump", "Frame", "[ChestDump] not standing in any plot", "TextBox", "en", "games/registry", "[MyAnimeMine] no nodes present right now", "[AutoPlace] picked up ", " ===", "Workspace", "core/click", "", "auto", "[%X] ", "Card", "[AutoPlace] upgrading ", "%$", "waiting", "UIStroke", "autoMineClicks", "This is not a supported place.", "ScreenGui", "Card", "CoreUI", "[SellCards] Card #", "AutoMine", "Locked miner slot", " = ", " items)", "neither appendfile nor writefile is available", "[MyAnimeMine] not standing in any plot", "games/my_anime_mine/auto_mine", " (Text=", "GuiButton", "/", "[AutoPlace] ", "'", "Damage", "[ChestDump] plot has no Chest", "core/anti_afk", " '", "games/my_anime_mine/get_current_plot", "Container", "[AutoChest] Take All had NO effect (", "languageIndex", " while selecting", "Players", "TextLabel", "/config.json", "TextLabel", "", "SellStand", " ===", ": ", "RemoteFunction", " = ", "Open menu", "ItemName", "games/my_anime_mine/get_best_miner", "Content", "TextLabel", "Dump", "Debug: Dump Sell Cards", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", ") on ", "อังกฤษ", "PlotIndex", "Content", "[GuiDump] MineGui not found", "ScrollingFrame", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "MineGui", "Frame", "TextButton", " (", "", "PlayerGui", "Content", "en", "action '", "[ChestDump] === Chest subtree ===", "games/my_anime_mine/auto_sell", "core/logger", "core/click", "  ", "core/logger", "UIListLayout", "games/my_anime_mine/get_current_plot", " items, going to collect", ": ", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "ClickDetector", "MekPro", "disabled", "=locked(", ")", "[GuiDump] === end ===", "Content", "no tool", "menuScale", "UIListLayout", "  [attr] ", "games/my_anime_mine/get_plot_parts", "core/interaction_logger", "Frame", ": ", "/s", "auto", "GuiObject", "Actions", "Frame", "(", "[GuiDump] === visible MineGui tree ===", "en-us", "')", "Badge", "log failed", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "core/teleport", "", "Loot", "Interaction Logger (debug)", "table", "Select All", "Debug: Current Plot", "unreadable", "Players", "autoSell", " items", "TextLabel", "%", "Players", "PlayerGui", "AutoUpgrade", " -> ", "[AutoChest] can't read the chest billboard, is the plot loaded?", "Dump", "core/get_me", "Error: ", "Sell for", "RemoteFunction", "ReplicatedStorage", "core/ui", "Players", "games/my_anime_mine/auto_place", "[Remotes] ", "-", "games/my_anime_mine/debug_dump_sell_cards", ",", "[AutoChest] Take All worked: ", " = ", "core/logger", "Place", "^([%d,]+)$", "games/my_anime_mine/debug_dump_chest", "Frame", " ===", " items", "TextLabel", "[AutoSell] fireproximityprompt error: ", "Players", "games/registry", "games/my_anime_mine/get_plot_parts", "UIListLayout", " (", "[AutoMine] ", "[AutoChest] waiting: ", " = ", "Chest", ")", "Content", "", "Content", ") from ", "upgrade", "Content", ": ", "HotbarFrame", "TextLabel", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "[Interaction] Button activated: ", "[AutoSell] clicked SellButton", "write reported success but ", "ย่อขยายเมนูทั้งชุด", "/", "Dump", "core/get_leaderstat", "^x(%d+)$", ")", "Workspace", " (", " '", "Debug: Dump Backpack Tool", "Players", "[AutoPlace] fireproximityprompt error: ", "Done", "Empty miner slot", "core/logger", "My Anime Mine · Mining Hub", "  ", "Auto Mine", "[AntiAFK] enabled - watching Player.Idled", "Crew", " error: ", "core/config", "games/my_anime_mine/get_current_plot", "Players", "", "button has no size on screen", "", "Zones", "Text", " (power ", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "'", "⚙", "Content", "GuiButton", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Players", "ValueBase", "games/my_anime_mine/debug_dump_remotes", "[SellCards] === total cards: ", "Players", "Plots", "open_menu_hint", "[AutoSell] also selected ", ")", "core/ui", "[MyAnimeMine] === Dump start ===", "TabBar", ")", "Auto Place Miner", "ตั้งค่า", "Players", "AutoChest", "=empty", "core/interaction_logger", "th", "", "MineGui", "unreadable", "core/dispatch", "TextLabel", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "[Scheduler] ", "  [attr] ", "[MyAnimeMine] === Node: ", "TextLabel", "core/logger", "HumanoidRootPart", "games/my_anime_mine/debug_dump_chest", "[AutoSell] nothing selectable (", "Main", "[MyAnimeMine] no holders found", "'), backing off ", " (prompt says '", "waiting", "", " (Text=", " cards)", "[^%d%.]", "core/scheduler", "Dump", "Frame", "[AutoPlace] holders: ", " still empty after placing ", "Pad", " attributes ===", "Close", "ปุ่มเปิดเมนู", "TextLabel", "[Interaction] ProximityPrompt triggered: ", "Text", "core/logger", "ClickDetector", "[Bb]uy", ": ", "Nodes", "\n", "core/i18n", "ReplicatedStorage", " doesn't exist", "[ChestDump] === plot ", "disabled", "games/my_anime_mine/main", "core/logger", "core/logger", "ValueLabel", "[AutoChest] click failed on ", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "ProximityPrompt", "[MyAnimeMine] === Holder: ", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "[Remotes] === total: ", " x", "x", "not found", "games/my_anime_mine/debug_dump_gui", "Workspace", "menuScale", "[BestMiner] slot ", "TextButton", " unit card(s) Select All skipped", "ClickDetector", "TextLabel", "=", "core/scheduler", "  ", "search_placeholder", " remotes", "MineGui", "autoMineClicks", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "(nothing to do)", " ==="}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(332)] = function()
return function(import)
local log = import(S(567))
local Players = game:GetService(S(436))
local VirtualUser = game:GetService(S(47))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(494))
        connection = player.Idled:Connect(function()
            log(S(210))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(27))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(85)] = function()
local VirtualInputManager = game:GetService(S(237))
local GuiService = game:GetService(S(252))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(501)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(240)] = function()
return function(import)
local HttpService = game:GetService(S(261))
local getPlaceId = import(S(147))
local log = import(S(378))
local ROOT_FOLDER = S(178)
local placeFolder = ROOT_FOLDER .. S(327) .. tostring(getPlaceId())
local filePath = placeFolder .. S(341)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(415) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(232) .. filePath .. S(469) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(90) .. filePath .. S(398) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(535)] = function()
return function(import)
    local getPlaceId = import(S(202))
    local registry = import(S(298))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(313))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(479)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(220))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(430)] = function()
local Players = game:GetService(S(516))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(157))
    local backpack = player:WaitForChild(S(150))
    return player, humanoid, backpack
end
return getMe
end
modules[S(57)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(573)] = function()
local Players = game:GetService(S(453))
local STRINGS = {
    en = {
        search_placeholder = S(229),
        open_menu_hint = S(349),
    },
    th = {
        search_placeholder = S(39),
        open_menu_hint = S(563),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(406)
    if locale:sub(1, 2):lower() == S(531) then
        return S(152)
    end
    return S(113)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(26) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(530)] = function()
return function(import)
local Players = game:GetService(S(419))
local Workspace = game:GetService(S(302))
local UserInputService = game:GetService(S(281))
local log = import(S(275))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(565) .. describe(prompt)
            .. S(270) .. prompt.ActionText .. S(524))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(43) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(473) .. describe(button)
            .. S(208) .. tostring(button.Text) .. S(388))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(63)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(597)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(326)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(22))
        return
    end
    connections = {}
    log(S(259))
    local playerGui = Players.LocalPlayer:WaitForChild(S(425))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(19)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(33)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(510)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(15),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(89)] = function()
local LOG_FILE = S(104)
local function log(message)
    local line = os.date(S(306)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(412))
            end
            appendfile(LOG_FILE, line .. S(572))
        elseif writefile then
            local existing = S(464)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(132))
        else
            error(S(322))
        end
        if not (isfile and readfile) then
            error(S(212))
        end
        if not isfile(LOG_FILE) then
            error(S(475) .. LOG_FILE .. S(575))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(25) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(92) .. LOG_FILE .. S(570) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(555)] = function()
return function(import)
local RunService = game:GetService(S(116))
local log = import(S(194))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(538) .. task_.name .. S(496) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(108) .. (currentTask and currentTask.name or S(607)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(1)] = function()
local Players = game:GetService(S(339))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(97))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(521)] = function()
return function(import)
local i18n = import(S(100))
local UserInputService = game:GetService(S(200))
local THEME = {
    BrandName = S(385),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(204))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(60))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(151))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(258))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(231)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(311))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(340))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(74)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(136))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(17)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(362))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(379))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(353))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(397))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(7))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(451))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(56))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(581))
            if not valueLbl then
                valueLbl = Instance.new(S(471))
                valueLbl.Name = S(280)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(408))
            if not badge then
                badge = Instance.new(S(564))
                badge.Name = S(188)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(264))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(448))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(5))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(366))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(44)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(423)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(48))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(365))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(119))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(228)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(294))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(393))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(34))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(566)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(314))
    screenGui.Name = S(316)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(250))
    local main = Instance.new(S(403))
    main.Name = S(546)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(137))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(50))
    tabBar.Name = S(523)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(456))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(557))
    header.Name = S(289)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(238))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(541))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(342))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(304)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(49))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(138)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(191))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(439)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(296))
    searchBox.Name = S(182)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(283))
    searchBox.Text = S(500)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(30))
    content.Name = S(509)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(279))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(171))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(112))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(536))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(504)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(221) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(217) .. (item.Subtitle or S(172))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(602))
        footerLabel.Text = i18n.T(S(518))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(125)] = function()
return function(import)
local Players = game:GetService(S(219))
local getCurrentPlot = import(S(380))
local getPlotParts = import(S(395))
local teleport = import(S(411))
local click = import(S(206))
local log = import(S(579))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(369))
    local mineGui = playerGui and playerGui:FindFirstChild(S(604))
    return mineGui and mineGui:FindFirstChild(S(75))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(10))
    local actions = content and content:FindFirstChild(S(402))
    return actions and actions:FindFirstChild(S(224))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(441), S(532)):match(S(290))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(254), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(370))
    local progress = content and content:FindFirstChild(S(282))
    local label = progress and progress:FindFirstChild(S(3))
    return parseCount(label and label.Text or S(551))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(582) .. button.Name .. S(346) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(584), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(45))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(114) .. MIN_ITEMS_TO_COLLECT .. S(321))
    end
end
function autoChest.IsEnabled()
    return enabled
end
local cachedCount, cachedAt = nil, 0
function autoChest.HasWork()
    local now = tick()
    if now - cachedAt > 1 then
        cachedCount = getChestCount()
        cachedAt = now
    end
    return cachedCount ~= nil and cachedCount >= MIN_ITEMS_TO_COLLECT
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(81) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(167))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(442) .. count .. S(260) .. after .. S(450))
        else
            log(S(336) .. tostring(count)
                .. S(427) .. tostring(after) .. S(226))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(534) then
            log(S(428))
            lastReportedCount = S(418)
        end
        return
    end
    if count < MIN_ITEMS_TO_COLLECT then
        if lastReportedCount ~= S(310) then
            log(S(459) .. count .. S(477) .. MIN_ITEMS_TO_COLLECT .. S(421))
            lastReportedCount = S(550)
        end
        return
    end
    lastReportedCount = nil
    log(S(105) .. count .. S(381))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(127)] = function()
return function(import)
local getCurrentPlot = import(S(103))
local getPlotParts = import(S(455))
local log = import(S(580))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(268), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(458) .. (enabled and S(183) or S(577)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(28)] = function()
return function(import)
local Players = game:GetService(S(486))
local getCurrentPlot = import(S(287))
local getPlotParts = import(S(164))
local getMinerSlots = import(S(68))(import)
local teleport = import(S(227))
local click = import(S(303))
local log = import(S(120))
local CHECK_INTERVAL = 3
local EMPTY_SLOT_TEXT = S(489)
local LOCKED_SLOT_TEXT = S(319)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(560))
    return pad and pad:FindFirstChildWhichIsA(S(244), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(31)) then
            local digits = descendant.Text:match(S(446))
            if digits then
                return tonumber((digits:gsub(S(203), S(80))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(6)) and descendant.Text:match(S(98)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(292)
    end
    if not prompt.Enabled then
        return true, S(121)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(154) .. price
    end
    local action = prompt.ActionText or S(215)
    if action:match(S(20)) or action:match(S(569)) or action:match(S(309)) then
        return true, S(372) .. action .. S(329)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(543))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(487) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(387) .. why .. S(99))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(529))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(599) .. prompt.ObjectText .. S(404) .. power .. S(481))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(72))
    if line ~= lastSurvey then
        log(S(558) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(87), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(467), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(256) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(107))
    else
        lastAttempt = 0
        log(S(266))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(174),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(445), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(207))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(61) .. holder.Name .. S(338))
            return false
        end
        log(S(211) .. candidate.slot .. S(483) .. candidate.name
            .. S(225))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(328) .. holder.Name .. S(559) .. miner.name)
        return false
    end
    log(S(23) .. miner.name .. S(253) .. miner.power
        .. S(357) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(95) .. entry.name .. S(2) .. entry.holder.Name
            .. S(549) .. tostring(entry.prompt.ActionText) .. S(407))
        return false
    end
    log(S(300) .. entry.name .. S(505) .. entry.power
        .. S(466) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(133))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(86) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(308) .. target.holder.Name .. S(262) .. target.name
        .. S(367) .. target.power .. S(169) .. best.name .. S(457) .. best.power .. S(520))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(131)] = function()
return function(import)
local Players = game:GetService(S(512))
local Workspace = game:GetService(S(592))
local teleport = import(S(91))
local click = import(S(376))
local log = import(S(196))
local CHECK_INTERVAL = 45
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(82))
    local mineGui = playerGui and playerGui:FindFirstChild(S(533))
    return mineGui and mineGui:FindFirstChild(S(153))
end
local function labelText(button)
    local content = button:FindFirstChild(S(222))
    local label = content and content:FindFirstChild(S(88))
    return label and label.Text or S(144)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(126))
    local scroll = content and content:FindFirstChild(S(102))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(186) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(291) .. button.Name .. S(251) .. tostring(err))
    end
    return ok
end
local function isCardSelected(card)
    local content = card:FindFirstChild(S(463))
    local overlay = content and content:FindFirstChild(S(193))
    return overlay ~= nil and overlay.Visible
end
local function isOnScreen(card, scroll)
    local top = scroll.AbsolutePosition.Y
    local bottom = top + scroll.AbsoluteSize.Y
    local cardTop = card.AbsolutePosition.Y
    local cardBottom = cardTop + card.AbsoluteSize.Y
    return cardTop >= top and cardBottom <= bottom
end
local function selectUnselectedCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(465))
    local scroll = content and content:FindFirstChild(S(286))
    if not scroll then
        return 0
    end
    local selected = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(307) and not isCardSelected(card) and isOnScreen(card, scroll) then
            clickButton(card)
            selected = selected + 1
            task.wait(0.1)
        end
    end
    return selected
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(344))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(288), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(46))
    else
        lastAttempt = 0
        log(S(129))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(452) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(163))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(265))
        local closeButton = sellFrame:FindFirstChild(S(562))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(468))
    local selectAllButton = content and content:FindFirstChild(S(130))
    local sellButton = content and content:FindFirstChild(S(205))
    if selectAllButton and labelText(selectAllButton) == S(416) then
        clickButton(selectAllButton)
        log(S(236) .. cardCount .. S(553))
        task.wait(0.2)
    end
    local selected = selectUnselectedCards(sellFrame)
    if selected > 0 then
        log(S(519) .. selected .. S(596))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(71), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(432), 1, true) then
        clickButton(sellButton)
        log(S(474))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(269))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(545) .. cardCount .. S(160)
            .. tostring(sawLabel) .. S(548) .. NOTHING_SELLABLE_COOLDOWN .. S(13))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(197)] = function()
return function(import)
local getCurrentPlot = import(S(498))
local getPlotParts = import(S(284))
local log = import(S(115))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(192))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(568))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(110) .. (enabled and S(24) or S(386)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(293)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(111)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(181) .. child.Name .. S(64)
        if child:IsA(S(135)) then
            line = line .. string.format(
                S(583),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(384)) then
            line = line .. string.format(S(155), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(195)) or child:IsA(S(347)) then
            line = line .. S(233)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(539) .. attrName .. S(443) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(377))
    end
end
return dumpInstance
end
modules[S(544)] = function()
return function(import)
local Players = game:GetService(S(58))
local ReplicatedStorage = game:GetService(S(434))
local getCurrentPlot = import(S(334))
local getPlotParts = import(S(76))
local log = import(S(375))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(32) .. name .. S(42) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(77))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(123) .. child.Name .. S(35)
            if child:IsA(S(513)) then
                line = line .. S(348) .. tostring(child.Value)
            elseif child:IsA(S(422)) or child:IsA(S(149)) then
                line = line .. S(552) .. tostring(child.Text) .. S(143)
            end
            log(line)
            dumpSubtree(child, indent .. S(55))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(295))
        return 0
    end
    log(S(576) .. plot.Name .. S(561))
    dumpAttributes(plot, S(601))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(331))
        return 0
    end
    log(S(373))
    dumpSubtree(parts.Chest, S(148))
    local player = Players.LocalPlayer
    log(S(117))
    dumpSubtree(player, S(343))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(9) .. player.Name .. S(301))
        dumpSubtree(mine, S(128))
    end
    log(S(272))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(591)] = function()
return function(import)
local Players = game:GetService(S(142))
local log = import(S(542))
local function dumpVisible(root, indent)
    indent = indent or S(502)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(401)) and child.Visible then
            local line = indent .. child.ClassName .. S(484) .. child.Name .. S(507)
            if child:IsA(S(598)) or child:IsA(S(595)) then
                line = line .. S(325) .. tostring(child.Text) .. S(462)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(73) .. attrName .. S(320) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(492))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(141))
    local mineGui = playerGui and playerGui:FindFirstChild(S(273))
    if not mineGui then
        log(S(361))
        return
    end
    log(S(405))
    dumpVisible(mineGui)
    log(S(389))
end
return dumpMineGui
end
end
modules[S(514)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(574))
local log = import(S(490))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(213)) or descendant:IsA(S(433)) then
            count = count + 1
            log(S(438) .. descendant.ClassName .. S(166) .. descendant:GetFullName())
        end
    end
    log(S(587) .. count .. S(345))
    return count
end
return dumpRemotes
end
end
modules[S(440)] = function()
return function(import)
local Players = game:GetService(S(278))
local log = import(S(444))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(109))
    local mineGui = playerGui and playerGui:FindFirstChild(S(79))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(235))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(352)) and sellFrame.Content:FindFirstChild(S(249))
    if not scroll then
        log(S(70))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(315) or card:IsA(S(209)) then
            count = count + 1
            log(S(317) .. count .. S(382) .. card.ClassName .. S(333) .. card.Name .. S(106))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(180) .. attrName .. S(460) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(173) .. child.ClassName .. S(16) .. child.Name .. S(267)
                if child:IsA(S(140)) then
                    line = line .. S(14) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(515) .. count .. S(608))
    return count
end
return dumpSellCards
end
end
modules[S(351)] = function()
return function(import)
local Players = game:GetService(S(424))
local log = import(S(246))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(51))
    local mineGui = playerGui and playerGui:FindFirstChild(S(364))
    local hotbar = mineGui and mineGui:FindFirstChild(S(470))
    local content = hotbar and hotbar:FindFirstChild(S(360))
    return content and content:FindFirstChild(S(335))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(168)) then
            local count = descendant.Text:match(S(480))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(199))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(390))
    local nameLabel = content and content:FindFirstChild(S(350))
    local damageLabel = content and content:FindFirstChild(S(330))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(184) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(554), S(368)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(83) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(594) .. slot .. S(161) .. info.name
                        .. S(139) .. info.power .. S(588) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(175)] = function()
local Players = game:GetService(S(499))
local Workspace = game:GetService(S(482))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(93))
    local plots = mineWorld and mineWorld:FindFirstChild(S(517))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(257)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(189)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(245)),
        Nodes = plot:FindFirstChild(S(571)),
        Chest = plot:FindFirstChild(S(461)),
        Zones = plot:FindFirstChild(S(503)),
        Crew = plot:FindFirstChild(S(495)),
        Loot = plot:FindFirstChild(S(413)),
        PlotIndex = plot:GetAttribute(S(359)),
        ChestCapacity = plot:GetAttribute(S(66)),
    }
end
return getPlotParts
end
modules[S(578)] = function()
return function(import)
    local Players = game:GetService(S(527))
    local createWindow = import(S(435))(import)
    local i18n = import(S(124))
    local antiAfk = import(S(216))(import)
    local interactionLogger = import(S(396))(import)
    local scheduler = import(S(600))(import)
    local config = import(S(497))(import)
    local log = import(S(12))
    local window = createWindow({
        Subtitle = S(491),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local getCurrentPlot = import(S(65))
    local getPlotParts = import(S(255))
    local dumpInstance = import(S(159))
    local dumpRemotes = import(S(198))(import)
    local dumpSellCards = import(S(277))(import)
    local dumpGui = import(S(21))(import)
    local dumpChest = import(S(447))(import)
    local autoMine = import(S(324))(import)
    local autoPlace = import(S(437))(import)
    local autoChest = import(S(41))(import)
    local autoSell = import(S(374))(import)
    local autoUpgrade = import(S(218))(import)
    scheduler.RegisterStandalone(S(318), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterStandalone(S(426), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.RegisterExclusive(S(528), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(247), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(276), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(122), S(162))
    persistentToggle(farmTab:AddCard(
        S(493),
        S(214)
    ), S(134), autoMine.SetEnabled)
    local savedMineSpeed = config.Get(S(62), 5)
    farmTab:AddSlider(
        S(78),
        S(201),
        1, 30, savedMineSpeed,
        function(value)
            config.Set(S(36), value)
            autoMine.SetCyclesPerSecond(value)
        end,
        S(399)
    )
    autoMine.SetCyclesPerSecond(savedMineSpeed)
    local savedMineClicks = config.Get(S(312), 1)
    farmTab:AddSlider(
        S(37),
        S(363),
        1, 10, savedMineClicks,
        function(value)
            config.Set(S(605), value)
            autoMine.SetClicksPerCycle(value)
        end,
        S(589)
    )
    autoMine.SetClicksPerCycle(savedMineClicks)
    persistentToggle(farmTab:AddCard(
        S(525),
        S(18)
    ), S(29), autoPlace.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(185),
        S(248)
    ), S(285), autoChest.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(230),
        S(356)
    ), S(420), autoSell.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(243),
        S(606)
    ), S(179), autoUpgrade.SetEnabled)
    local debugCard = farmTab:AddCard(S(417), S(383))
    local checkBtn
    checkBtn = debugCard:AddButton(S(170), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(590)
            log(S(323))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(11),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(94),
        S(410)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(478), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(158)
            return
        end
        local ok, err = log(S(522))
        if not ok then
            dumpBtn.Text = S(409)
            dumpCard:SetSubtitle(S(431) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(540) .. firstNode.Name .. S(69))
            dumpInstance(firstNode, log)
        else
            log(S(299))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(585) .. firstHolder.Name .. S(449))
            dumpInstance(firstHolder, log)
        else
            log(S(547))
        end
        dumpBtn.Text = S(177)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(485),
        S(506)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(4), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(271))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(38))
        if not tool then
            toolDumpBtn.Text = S(391)
            return
        end
        log(S(187) .. tool.Name .. S(263))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(394) .. attrName .. S(165) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(96)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(52),
        S(472)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(118), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(603)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(355),
        S(84)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(429), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(190)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(40),
        S(511)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(556), function()
        dumpGui()
        guiDumpBtn.Text = S(488)
    end)
    local chestDumpCard = farmTab:AddCard(
        S(274),
        S(586)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(354), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(101)
    end)
    local settingsTab = window:AddTab(S(526), S(508))
    persistentToggle(
        settingsTab:AddCard(S(8), S(537)),
        S(67),
        antiAfk.SetEnabled
    )
    persistentToggle(
        settingsTab:AddCard(
            S(414),
            S(239)
        ),
        S(145),
        interactionLogger.SetEnabled
    )
    local savedScale = config.Get(S(593), 100)
    settingsTab:AddSlider(S(59), S(476), 50, 150, savedScale, function(value)
        config.Set(S(392), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(337), 1)
    settingsTab:AddSegmented({ S(234), S(358), S(242) }, savedLanguage, function(index, value)
        local code = ({ S(54), S(297), S(146) })[index]
        config.Set(S(241), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(305), S(371), S(176) })[savedLanguage] or S(400))
    log(S(53) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(454)] = function()
return {
    [79389059854988] = S(223),
}
end
import(S(156))(import)
