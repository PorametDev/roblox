-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Content", "TextLabel", "s - carrying on without it", "[AutoResearch] nothing open and affordable right now", "Humanoid", " waited ", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "MineTakeChest", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "core/ui", ", built ", "[AutoUpgrade] ", "[AutoUpgrade] MineBuyPickaxe ", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "Badge", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "function", "", "Frame", "Auto Research: กันเงินไว้", "[AutoChest] enabled (collects once >= ", "games/my_anime_mine/get_current_plot", "Content", " (", " ", "core/debug_decompile", "core/humanoid", "TextBox", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "autoResearchCoreLast", " items", " (", "Auto Place: รอบดึงตัวเก่ง", "Frame", "Frame", "enabled", "Players", " files", "Auto Claim (Index)", "table", "", "works from anywhere - dropping the teleport", ", ", "search_placeholder", "action '", "TextButton", "ตั้งค่า", "_", "games/my_anime_mine/auto_chest", "Auto Place: ปลดล็อกช่อง miner", "[AutoPlot] ", "HumanoidRootPart", "getscriptbytecode", " claimable but none could be resolved", "MineSpeed", "", "none", "autoChest", "autoChestMinItems", "] job failed: ", "MineGui", "Frame", ")", " ", "ถูกสุดก่อน", "Auto Research (Plot)", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "UserInputService", "PlotIndex", "string", "games/my_anime_mine/auto_chest", "UICorner", "affordable - waiting for a turn", "autoPlaceEquipBestInterval", "  [attr] ", "🐛", "All", "fill", "มุดแมพ (Noclip)", "core/config", "debug.getconstants", "fly", "string", "TextLabel", "[AutoUpgrade] ", "[Decompile] === start ===", "getconnections", "Players", " #", "CoreUI", " remotes", "en", "/", "UICorner", "Players", "TextLabel", "?", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "string", "MineEquipPickaxe", "float", "PlayerGui", "", "autoUpgradeMutationTarget", " screens", "core/humanoid", "core/parse_money", "[Uu]nlock", "prompt disabled", "/", "Auto Mine: คลิกต่อรอบ", "badge says ", "Container", ")", "TextButton", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "Damage", " screen(s)) ===", " ", ") ", "Players", " ", "Auto Plot: รอบเช็ค", "%$%s*([%d,%.]+)%s*(%a*)", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "", ", base ", "autoMine", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "autoResearchMaxPerPass", "core/get_place_id", "[AutoClaim] ", "ClickDetector", "⚙", "Damage", "Core", "ReplicatedStorage", " (cost ", "core/dispatch", "BasePart", ")", "autoPlotLatest", " (money ", "games/my_anime_mine/game_api", "/", "GuiObject", "Auto Research: รอบเช็ค", "core/logger", "function", "Players", ")", "RemoteEvent", "BasePart", "th", "table", " - upgrade the miners first", "AutoChest", "Speed", ")", " power ", "autoSellProtectUnits", "autoChestInterval", "[AutoPlace] holders: ", "noclip", "every open node is maxed", "disabled", " -> ", " (", "[AutoPlace] fireproximityprompt error: ", "Workspace", "[GuiDump] PlayerGui not found", "ย่อขยายเมนูทั้งชุด", "enabled", "🏠", "write reported success but the line isn't in ", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "AutoClaim", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "core/logger", "s", "/s", "core/teleport", "", ": ", "[AutoPlace] Equip Best: hotbar's strongest ", "[AutoResearch] ", "core/config", "autoClaim", "ค้นหาฟังก์ชัน...", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "Plots", "SellStand", "[AutoChest] MineTakeChest had no effect (", "games/my_anime_mine/get_current_plot", "=empty", "VirtualInputManager", "disabled", "games/my_anime_mine/auto_research", "nothing left to buy (", "MekPro", ") on ", "%", "Search", " still empty after placing ", "UIListLayout", "[AutoPlace] ", "Frame", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "[Remotes] ", "autoPlaceEquipBest", "TextButton", "core/build_env", "", "table", "no", ") gave nothing", "enabled", "core/run_job", "ProximityPrompt", "autoPlace", "core/logger", "", "Auto Claim: เก็บสูงสุดต่อรอบ", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "s", "lowest", "jumpPower", "", "[AutoPlace] disabled", ": ", "th", "table", "My Anime Mine · Mining Hub", "OwnerUserId", "games/my_anime_mine/main", "ChestCapacity", "modules not loaded", "Damage ก่อน", "Search functions...", "en", ") -> ", " reward(s) looked claimable but none went through", "core/logger", "no mine data", "Auto Mine: ความเร็ว", "autoUpgradeMutationBudget", "games/registry", "  ", "core/teleport", "TextLabel", " '", "^([%d,]+)$", "Auto Research: ซื้อสูงสุดต่อรอบ", ")", "TextButton", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " = ", " (", ": ", "disabled", "core/logger", "Auto Sell", " owned)", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " (power ", "[AutoPlot] unlocked ", "games/my_anime_mine/game_api", "[Interaction] logger enabled - go click/interact with things now", "MineRollMutation", "core/run_job", "games/my_anime_mine/game_api", "เช็คโซนทุกกี่วินาที", "table", "Frame", "table", "ClaimableTiers failed", "HotbarFrame", "MineSpeed", "LocalScript", "TextButton", "Auto Claim: รอบเช็ค", "autoClaimInterval", "search_placeholder", " error: ", "", "s", "open_menu_hint", "MekPro", "auto", "core/run_job", "MineEquipBest", "[AutoPlace] upgrading ", "PickaxesStand", " -> ", "games/my_anime_mine/auto_research", "ความเร็ววิ่ง", ": badge says ", "[AutoPlot] now mining ", "unreadable", "-", "=", "Decompile Client Scripts", "Frame", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "autoMineClicks", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "core/i18n", "", "(nothing to do)", "LayerCollector", "List", "", "research", "TextLabel", "games/my_anime_mine/auto_plot", "[Decompile] PlayerScripts not found", "Dump Visible GUI", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "", "Core buying is off", "[AutoUpgrade] bought pickaxe #", "s", "This is not a supported place.", "Players", "core/debug_tab", "autoUpgradePickaxeIndex", "core/logger", "core/logger", "function", "GuiButton", "TextButton", "AutoPlace", "M", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "games/my_anime_mine/auto_mine", "write reported success but ", " (", "core/dispatch", "%.3g", " team power, you have ", "M", "games/my_anime_mine/game_api", ": ", " stack(s)", "[Decompile] ", ")", "core/anti_afk", " ", ")", " node(s) looked buyable but the server refused", "games/my_anime_mine/game_api", "บิน (Fly)", "[Remotes] === total: ", "ProximityPrompt", "TextLabel", " ", "core/parse_money", "[", ")", "[AutoClaim] claimed ", "core/debug_dump_remotes", "core/i18n", "games/my_anime_mine/auto_upgrade", "autoSellInterval", "may need us at the stand - trying that", "AutoChest", "MineBuyPickaxe", "ProximityPrompt", "AutoSell", " has been busy for ", " (", "AutoPlace", "core/logger", "table", " refused - ", "autoClaimMaxPerPass", "0 = ไม่จำกัด", "getloadedmodules", "core/debug_dump_gui", "[Decompile] === executor capabilities ===", "table", " items (now ", "Nodes", "Workspace", "[Interaction] Key pressed: %s (gameProcessed=%s)", "autoResearchCheapestFirst", "core/logger", "x", "Locked miner slot", "M", "MekPro", " - ", "ZoneRow", "%.3g", "/", ") needs ", "auto", "TextLabel", ")", "[Interaction] logger disabled", "core/debug_decompile", " -> ", "\n", " (prompt says '", "core/run_job", "[GameAPI] SendEvent failed: ", "TextLabel", "[AutoSell] MineSellItems ", "AutoClaim", "CharMaxLevel", "[AutoMine] ", "[Scheduler] ", "sale didn't go through", "Speed", "hookfunction", "Holders", "TextLabel", "AutoPlace", "VirtualUser", "", "BodyVelocity", "อังกฤษ", "Limited", "no isfile/readfile available to verify the write", "autoUpgradeLimitPickaxes", "ValueLabel", "Mutation: หยุดเมื่อได้ x", "[AutoChest] can't read the chest yet", "HumanoidRootPart", "slot ", "button has no size on screen", "%.3g", "core/logger", " (x", "[Decompile] wrote ", ": ", "[AutoClaim] ", "[MekPro] build ", "OwnerUserId", "Shop Stand", " doesn't exist", "TextButton", " (", "GuiService", "[AutoPlace] couldn't hold anything - not pressing E", "Players", "games/my_anime_mine/auto_place", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "core/run_job", "RunService", "[Decompile] decompile unavailable in this executor", ") didn't take: ", " '", "Anti-AFK", "Text", "Auto Upgrade: Pickaxe ที่จะซื้อ", "core/logger", "[AntiAFK] disabled", " items)", "ScreenGui", "string", "unlock", "[AutoPlace] ", "walkSpeed", "Workspace", "cheapest open node ", "table", "Pad", "cost", "Frame", "JumpPower (ปกติ 50)", "TextLabel", "[AutoResearch] ", "autoPlotKeepMoney", " mutation x", "[AutoResearch] idle: ", "x", "MineSellItems", "MekPro.log", "#", "[MekPro] settings file: ", "Players", "core/anti_afk", ",", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Exclusive", "index rewards are disabled in this game build", "core/get_place_id", "%$", "x", ") ", "UIListLayout", "ไล่อัปเกรด miner ทุกกี่วินาที", "th", "core/run_job", "", "[Scheduler] ", "waiting", "CoreGui", "TextLabel", "works from anywhere - no teleport needed", "ขนาดเมนู", "autoUpgradePickaxeIndex", "Auto Plot: กันเงินไว้", "^x(%d+)$", "TextLabel", "ItemName", "[AutoPlot] ", "UIScale", "Auto Plot: ใช้โซนล่าสุดเสมอ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "'", "Hauler", " while selecting", "core/logger", "Loot", "", "M", "core/logger", "s - jumping the queue", "core/logger", "MineWorld", "แพงสุดก่อน", "table", "[AutoPlot] ", "s", " -> x", "games/my_anime_mine/auto_place", "lowest", "AutoPlace", " x", "getgc", "845d238+", ",", "TabBar", ")", "table", "leaderstats", "unlock", "menuScale", "core/logger", "needs ", "autoUpgradeRollMutation", " ", "autoUpgradePriority", "none", "TextLabel", ": ", "autoUpgradeMaxPerPass", "([%d,%.]+)%s*(%a*)", ") ", "research", "[AutoChest] disabled", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", " research is ", " costs ", "[%X] ", " -> ", " (still ", ")", "fill", "", "Speed ก่อน", "=locked(", "languageIndex", "/", "[GuiDump] === visible PlayerGui tree ===", "s", "[AutoClaim] idle: ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[AutoPlace] unlocked ", "[AutoUpgrade] pickaxe #", " ", "[GuiDump] ", "Auto Sell: รอคิวได้นานสุด", ", have ", "[Scheduler] ", "", "Auto Upgrade: รอบเช็ค", "[AutoPlace] slot ", "AutoPlace", "MinePower", "autoPlaceUpgradeWeak", "getscripts", "costs ", "M", ")", "[Bb]uy", "needs ", "Auto Upgrade: แจก Pickaxe ให้ miner", "Workspace", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "enabled", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "core/debug_interaction_logger", "table", "0 = ไม่จำกัด", "[AutoPlace] picked up ", "ScrollingFrame", "core/teleport", "ยิง Equip Best ทุกกี่วินาที", "Auto Research: ซื้อสาย Core", "", "getcallbackvalue", "Badge", "UserInputService", " (chest holds ", " but no unclaimed reward in its Track", ".lua", ")", "เดินทะลุกำแพงและพื้น", "core/parse_money", "[AutoChest] collected ", "[Interaction] Button activated: ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Mutation: เพดานราคาต่อครั้ง", "WalkSpeed (ปกติ 16)", "nothing to place or upgrade", "autoMineSpeed", "core/logger", "core/get_me", "Text", "sold ", "[AntiAFK] enabled - watching Player.Idled", "Main", "games/my_anime_mine/game_api", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "PlayerGui", "disabled", "ฟาร์ม", "", "MekPro", " ชิ้น", "core/logger", "autoSellMaxWait", ") from ", "[AutoResearch] ", ")", " ", " team power", "[BestMiner] slot ", "ReplicatedStorage", " | ", "games/my_anime_mine/get_best_miner", "Humanoid", "ModuleScript", "server didn't equip it", "enabled", "[AutoResearch] Core ", " for ", "2026-09-21 12:56:41", " (x", "ValueLabel", "[AutoPlace] unlocking ", ": State(", "Header", "core/run_job", "AutoPlot", "Mining", "MekPro/decompiled", "BodyPosition", "Auto Upgrade: กันเงินไว้", " still locked after paying ", "games/my_anime_mine/get_current_plot", "autoResearchZones", "  ", "s", "%.3g", "Frame", "\n", "Dump", "core/debug_interaction_logger", "antiAfk", "core/debug_tab", "autoPlaceUnlock", "autoResearchInterval", "maxed", "empty", " possible holder(s) - not buying more", "^%$", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "neither appendfile nor writefile is available", "GuiButton", "yes", "AutoSell", "Auto Chest: เกณฑ์เก็บ", " (power ", "decompile", "ClickDetector", "en", "[Config] couldn't write ", "GetCharacterUpgradeCost", "AutoResearch", "ตามเครื่อง", "cost", "core/logger", "GetCharacterSpeedUpgradeCost", "", " failed)", "games/my_anime_mine/auto_mine", " script(s) to ", "games/my_anime_mine/auto_upgrade", "autoResearch", "games/my_anime_mine/auto_sell", "CharSpeedMaxLevel", "empty", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "[AutoPlace] nothing in the hotbar to place", "", ",", " (", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "getrenv", "')", "Frame", "AutoResearch", "/config.json", "games/my_anime_mine/get_best_miner", "core/get_leaderstat", "", " costs ", "Auto Chest", "[AutoChest] waiting: ", "nothing claimable", "games/my_anime_mine/game_api", "HumanoidRootPart", "TextLabel", "[AutoPlace] drifted out of range of ", "core/logger", "autoUpgradeInterval", "อ่านป้ายบน chest ทุกกี่วินาที", "UIListLayout", "debug.getproto", " (Text=", "games/my_anime_mine/game_api", " -> ", "BasePart", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "[AutoChest] chest is empty", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "Workspace", "[AutoSell] disabled", "waiting", "GetHolderMineSpeedLevel", "[AutoUpgrade] ", "autoSell", "Run", "%.0f", "BasePart", ")", "Interaction Logger", "x", "HttpService", " -> ", "AutoPlot", "GetHolderLevel", "core/run_job", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "(", " not bought yet (", "debug.getupvalues", "autoUpgradeKeepMoney", "Workspace", "Players", "[AutoPlace] placed ", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "/", "table", "en-us", "TextLabel", "th", "AutoUpgrade", "auto", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "table", "/", "Auto Upgrade Unit", "TextButton", "Open menu", ")", "MineBuySkill", "both Zone and Core buying are switched off", "dev", "[AutoUpgrade] ", "X", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", ", you have ", "Zones", "GetHolderSpeedLevel", "Content", "TextButton", "ปุ่มเปิดเมนู", "แรงกระโดด", "ProximityPrompt", "/", "AutoUpgrade", "ขายทุกกี่วินาที", "table", "RemoteFunction", "Hauler", "TextLabel", " ===", "[^%.]+", "autoUpgradePickaxe", "core/click", "[Scheduler] turn: ", "Auto Research: ซื้อสาย Zone", "core/debug_dump_remotes", "games/my_anime_mine/game_api", " didn't go through (out of stock or too expensive)", "games/my_anime_mine/auto_claim", "menuScale", " = ", "s", "core/i18n", "auto", "", "[AutoUpgrade] ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/logger", "MineSpeed", "Auto Chest: รอบเช็ค", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", ")", "autoUpgrade", "[Interaction] ClickDetector clicked: ", "table", "games/my_anime_mine/main", "%.3g", " pickaxe(s) for ", "Auto Place: รอบเช็ค", " (", "RunService", "[AutoSell] enabled", "UIStroke", "games/my_anime_mine/auto_sell", "Frame", "open_menu_hint", "Character", "autoPlotBuy", "[AutoSell] ", "interactionLogger", "MineSelectZone", "string", "Zone buying is off", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "locked slot", "Auto Sell: รอบขาย", "[AutoPlace] couldn't pick up ", " for ", "ลอย (Float)", "'", "Mining ก่อน", "s", "ZoneBoardGui", " (", "", "[Config] couldn't read ", "UIPadding", "ItemFrame", "core/scheduler", "Modules", "autoSellMaxWait", "Auto Place: เพดานราคาปลดช่อง", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "Empty miner slot", "upgrade", "[^%w]+", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "%.0f", "core/get_place_id", "GetCharacterMineSpeedUpgradeCost", " (Text=", "getnilinstances", "Players", "number", "slot not in the hotbar", "games/my_anime_mine/get_plot_parts", "ดีบัก", "autoPlotInterval", "AutoMine", "[AutoPlace] ", "autoUpgradeEquipPickaxe", "autoResearchCore", "MineEquipSlot", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " (", "x", "[AntiAFK] idle detected, simulating input", "autoPlot", "disabled", " ", "Players", "Chest", ") ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "slot past the usable cap", "Players", "; ", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/teleport", "Crew", " ", "[Interaction] ProximityPrompt triggered: ", "'", "", "Damage", "games/my_anime_mine/get_plot_parts", "core/ui", "ClickDetector", "Root", "TextLabel", "table", "core/debug_dump_gui", "%$", " (ActionText=", "games/registry", "autoResearchKeepMoney", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "games/my_anime_mine/auto_claim", "ไล่ซื้อทุกกี่วินาที", "GuiService", "[GameAPI] couldn't load the game's modules: ", "[AutoResearch] ", "string", "", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "languageIndex", "core/build_env", "ไทย", "PlayerScripts", "Players", "[GuiDump] === end (", "[AutoPlot] couldn't switch to ", "Auto Place Miner", "CharMineSpeedMaxLevel", "autoPlaceInterval", "MineWorld", "autoPlaceUnlockBudget", " from ", "Dump All Remotes", "Auto Upgrade Plot (Zone)", "getsenv", "Auto Upgrade: สุ่ม Mutation", "Auto Mine", "Auto Plot: ซื้อโซนถัดไป", "games/my_anime_mine/auto_plot", "PlayerGui", "games/my_anime_mine/get_plot_parts", "[Decompile] === end ===", "Dump", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "needs us at the stand - keeping the teleport", "unreadable", " (", "ZoneBoard", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", " wouldn't take ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Backpack", "MineUpgradeCharacter", " pickaxe ", "UserInputService", "[AutoResearch] cheapest open node: ", "ต่ำสุดก่อน", "[Logger] failed to write ", "core/scheduler", "Auto Place: เปลี่ยนตัวอ่อนออก", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "Plots", "[AutoUpgrade] every stat on every miner is maxed", "^M(%d+)$"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(484)] = function()
return function(import)
local log = import(S(221))
local Players = game:GetService(S(88))
local VirtualUser = game:GetService(S(420))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(626))
        connection = player.Idled:Connect(function()
            log(S(891))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(459))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(212)] = function()
return { Env = S(785), IsDev = true, Commit = S(535), BuiltAt = S(653) }
end
modules[S(807)] = function()
local VirtualInputManager = game:GetService(S(196))
local GuiService = game:GetService(S(445))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(432)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(80)] = function()
return function(import)
local HttpService = game:GetService(S(755))
local getPlaceId = import(S(131))
local log = import(S(698))
local ROOT_FOLDER = S(200)
local placeFolder = ROOT_FOLDER .. S(797) .. tostring(getPlaceId())
local filePath = placeFolder .. S(719)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(915) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(860) .. filePath .. S(437) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(693) .. filePath .. S(184) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(402)] = function()
return function(import)
local Players = game:GetService(S(150))
local log = import(S(434))
local CAPABILITIES = {
    S(690), S(53), S(586), S(534), S(379),
    S(87), S(606), S(946), S(715), S(81),
    S(763), S(735), S(416), S(876),
}
local OUT_FOLDER = S(662)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(805)) do
        if type(target) ~= S(214) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(381))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(346) .. name .. S(258) .. ((ok and type(value) == S(330)) and S(686) or S(215)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(452))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(934))
    if not playerScripts then
        log(S(317))
        return 0
    end
    pcall(makefolder, S(634))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(280)) or target:IsA(S(648)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(99) and #source > 0 then
                local name = target:GetFullName():gsub(S(870), S(48))
                if pcall(writefile, OUT_FOLDER .. S(568) .. name .. S(611), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(436) .. written .. S(703) .. OUT_FOLDER
        .. S(24) .. failed .. S(701))
    return written
end
local function run()
    log(S(86))
    reportCapabilities()
    local count = dumpAll()
    log(S(953))
    return count
end
return run
end
end
modules[S(916)] = function()
return function(import)
local Players = game:GetService(S(447))
local log = import(S(521))
local function dumpVisible(root, indent)
    indent = indent or S(103)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(146)) and child.Visible then
            local line = indent .. child.ClassName .. S(252) .. child.Name .. S(907)
            if child:IsA(S(356)) or child:IsA(S(115)) then
                line = line .. S(736) .. tostring(child.Text) .. S(360)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(75) .. attrName .. S(815) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(668))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(951))
    if not playerGui then
        log(S(171))
        return 0
    end
    local screens = 0
    log(S(569))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(311)) and screen.Enabled and screen.Name ~= S(392) then
            screens = screens + 1
            log(S(576) .. screen.ClassName .. S(454) .. screen.Name .. S(854))
            dumpVisible(screen, S(249))
        end
    end
    log(S(936) .. screens .. S(118))
    return screens
end
return dumpGui
end
end
modules[S(362)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(137))
local log = import(S(523))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(152)) or descendant:IsA(S(801)) then
            count = count + 1
            log(S(209) .. descendant.ClassName .. S(550) .. descendant:GetFullName())
        end
    end
    log(S(354) .. count .. S(804))
    return count
end
return dumpRemotes
end
end
modules[S(597)] = function()
return function(import)
local Players = game:GetService(S(325))
local Workspace = game:GetService(S(743))
local UserInputService = game:GetService(S(68))
local log = import(S(543))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(906) .. describe(prompt)
            .. S(918) .. prompt.ActionText .. S(159))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(828) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(616) .. describe(button)
            .. S(875) .. tostring(button.Text) .. S(538))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(355)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(691)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(685)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(401))
        return
    end
    connections = {}
    log(S(269))
    local playerGui = Players.LocalPlayer:WaitForChild(S(630))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(369)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(133)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(331)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(386),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(326)] = function()
return function(import)
local interactionLogger = import(S(674))(import)
local dumpRemotes = import(S(810))(import)
local dumpGui = import(S(380))(import)
local decompileScripts = import(S(26))(import)
local humanoid = import(S(27))
local function build(window, persist)
    local tab = window:AddTab(S(881), S(76))
    persist.Toggle(
        tab:AddCard(
            S(753),
            S(335)
        ),
        S(844),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(944),
        S(788)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(954), function()
        remoteBtn.Text = dumpRemotes() .. S(91)
    end)
    local guiCard = tab:AddCard(
        S(318),
        S(129)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(673), function()
        guiBtn.Text = dumpGui() .. S(105)
    end)
    local decompileCard = tab:AddCard(
        S(303),
        S(617)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(749), function()
        decompileBtn.Text = decompileScripts() .. S(38)
    end)
    humanoid.Start()
    persist.Slider(tab, S(297), S(619),
        8, 200, S(465), 16, humanoid.SetWalkSpeed, S(313))
    persist.Slider(tab, S(795), S(472),
        30, 300, S(228), 50, humanoid.SetJumpPower, S(222))
    persist.Toggle(
        tab:AddCard(S(353), S(225)),
        S(82), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(79), S(613)),
        S(164), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(853), S(848)),
        S(101), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(339)] = function()
return function(import)
    local getPlaceId = import(S(489))
    local registry = import(S(919))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(324))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(721)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(540))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(623)] = function()
local Players = game:GetService(S(37))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(647))
    local backpack = player:WaitForChild(S(963))
    return player, humanoid, backpack
end
return getMe
end
modules[S(873)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(106)] = function()
local Players = game:GetService(S(483))
local RunService = game:GetService(S(835))
local UserInputService = game:GetService(S(966))
local Workspace = game:GetService(S(170))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(5))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(430))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(422))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(739)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(751)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(663))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(308)] = function()
local Players = game:GetService(S(900))
local STRINGS = {
    en = {
        search_placeholder = S(240),
        open_menu_hint = S(781),
    },
    th = {
        search_placeholder = S(189),
        open_menu_hint = S(794),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(771)
    if locale:sub(1, 2):lower() == S(232) then
        return S(495)
    end
    return S(692)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(290) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(517)] = function()
local LOG_FILE = S(480)
local function log(message)
    local line = os.date(S(559)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(320))
            end
            appendfile(LOG_FILE, line .. S(672))
        elseif writefile then
            local existing = S(183)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(404))
        else
            error(S(684))
        end
        if not (isfile and readfile) then
            error(S(425))
        end
        if not isfile(LOG_FILE) then
            error(S(337) .. LOG_FILE .. S(442))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(175) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(969) .. LOG_FILE .. S(344) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(614)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(83) then
        return nil
    end
    local number, suffix = text:match(S(124))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(536), S(497))))
    if not value then
        return nil
    end
    if suffix ~= S(41) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(450)] = function()
return function(import)
local log = import(S(179))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(359) .. name .. S(60) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(863)] = function()
return function(import)
local RunService = game:GetService(S(451))
local log = import(S(328))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(498) .. task_.name .. S(285) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(413) .. task_.name .. S(371)
                    .. string.format(S(872), now - task_.busySince)
                    .. S(3))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(579) .. starved.name .. S(6)
            .. string.format(S(750), now - starved.waitingSince)
            .. S(522))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(808) .. (currentTask and currentTask.name or S(310)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(182)] = function()
local Players = game:GetService(S(895))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(728))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(911)] = function()
return function(import)
local i18n = import(S(817))
local UserInputService = game:GetService(S(608))
local GuiService = game:GetService(S(924))
local THEME = {
    BrandName = S(289),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(72))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(94))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(861))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(256))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(286)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(837))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(96))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(97)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(473))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(580)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(601))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(493))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(772))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(304))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(2))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(507))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(501))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(655))
            if not valueLbl then
                valueLbl = Instance.new(S(315))
                valueLbl.Name = S(427)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(607))
            if not badge then
                badge = Instance.new(S(399))
                badge.Name = S(15)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(281))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(34))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(839))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(211))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(421)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(202)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(35))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(471))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(443))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(519)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(671))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(734))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(46))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(456)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(461))
    screenGui.Name = S(90)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(500))
    local main = Instance.new(S(717))
    main.Name = S(627)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(510))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(207))
    tabBar.Name = S(537)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(205))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(275))
    header.Name = S(658)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(780))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(84))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(729))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(564)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(332))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(787)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(793))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(301)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(28))
    searchBox.Name = S(203)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(284))
    searchBox.Text = S(700)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(62))
    content.Name = S(23)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(19))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(408))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(840))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(914))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(624)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(18) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(349) .. (item.Subtitle or S(126))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(44))
        footerLabel.Text = i18n.T(S(288))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(49)] = function()
return function(import)
local gameApi = import(S(811))(import)
local log = import(S(148))
local runGuarded = import(S(406))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(21) .. minItemsToCollect .. S(460))
    else
        log(S(555))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(300) then
            log(S(429))
            lastReported = S(957)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(680) then
            log(S(741))
            lastReported = S(708)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(499) then
            local capacity = gameApi.ChestCapacity()
            log(S(725) .. count .. S(769) .. want .. S(31)
                .. (capacity and (S(609) .. capacity .. S(151)) or S(229)))
            lastReported = S(745)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(157), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(615) .. count .. S(383) .. after .. S(589))
        else
            log(S(193) .. count
                .. S(295) .. tostring(after) .. S(782))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(922)] = function()
return function(import)
local gameApi = import(S(144))(import)
local log = import(S(388))
local runGuarded = import(S(291))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(438) .. (enabled and S(217) or S(166)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(571) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(361) .. reward.category .. S(25)
                .. tostring(reward.tierKey) .. S(89) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(132) .. #pending .. S(243))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(410), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(336)] = function()
return function(import)
local getCurrentPlot = import(S(666))
local getPlotParts = import(S(880))
local log = import(S(374))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(912), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(412) .. (enabled and S(36) or S(893)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(448)] = function()
return function(import)
local Players = game:GetService(S(95))
local getCurrentPlot = import(S(194))
local getPlotParts = import(S(952))
local getMinerSlots = import(S(720))(import)
local teleport = import(S(903))
local parseMoney = import(S(358))
local gameApi = import(S(628))(import)
local log = import(S(329))
local runGuarded = import(S(659))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(868)
local LOCKED_SLOT_TEXT = S(390)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(469))
    return pad and pad:FindFirstChildWhichIsA(S(796), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(549)) then
            local digits = descendant.Text:match(S(253))
            if digits then
                return tonumber((digits:gsub(S(712), S(859))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(803)) and descendant.Text:match(S(682)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(431) .. holder.Name .. S(762)
            .. gameApi.MinerSlots() .. S(396) .. gameApi.MaxMinerSlots() .. S(264)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(849)
    end
    if not prompt.Enabled then
        return true, S(109)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(587) .. price
    end
    local action = prompt.ActionText or S(711)
    if action:match(S(108)) or action:match(S(590)) or action:match(S(490)) then
        return true, S(45) .. action .. S(514)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(52))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(169) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(566) .. why .. S(255))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(195))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(302) .. prompt.ObjectText
                    .. S(761) .. shown .. S(127) .. math.floor(base) .. S(347))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(645))
    if line ~= lastSurvey then
        log(S(163) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(78), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(869), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(463), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(464) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(230))
    else
        lastAttempt = 0
        log(S(955))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(305),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(879)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(899)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(649)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(710))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(730) .. holder.Name .. S(516))
            return false
        end
        log(S(582) .. candidate.slot .. S(338) .. candidate.name
            .. S(453) .. tostring(why))
    end
    if not miner then
        log(S(446))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(884) .. holder.Name .. S(204) .. miner.name)
        return false
    end
    log(S(767) .. miner.name .. S(266) .. miner.power
        .. S(201) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(851) .. entry.name .. S(943) .. entry.holder.Name
            .. S(405) .. tostring(entry.prompt.ActionText) .. S(716))
        return false
    end
    log(S(600) .. entry.name .. S(689) .. entry.power
        .. S(638) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(206) .. pad.holder.Name .. S(665)
                .. tostring(pad.priceText))
        else
            log(S(573) .. pad.holder.Name
                .. S(652) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(185)
            .. string.format(S(395), bestBefore) .. S(167)
            .. string.format(S(433), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(532), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(620))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(563) then
        runGuarded(S(419), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(541) then
        log(S(656) .. target.holder.Name
            .. S(852) .. tostring(target.priceText))
        runGuarded(S(583), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(293) .. target.holder.Name .. S(260) .. target.name
        .. S(889) .. target.power .. S(242) .. best.name .. S(858) .. best.power .. S(141))
    runGuarded(S(373), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(316)] = function()
return function(import)
local gameApi = import(S(268))(import)
local parseMoney = import(S(107))
local log = import(S(244))
local runGuarded = import(S(496))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(527) .. (enabled and S(173) or S(261)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(917)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(299) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(32) .. deepestKey .. S(114))
        return true
    end
    log(S(937) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(51) .. gameApi.ZoneDisplayName(zoneKey) .. S(558)
                .. math.floor(price) .. S(789) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(267) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(372) .. zoneKey .. S(612))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(554) then
            reportedLocked = S(314)
            log(S(509) .. zoneKey .. S(376)
                .. gameApi.ZoneDisplayName(currentKey) .. S(557)
                .. done .. S(93) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(660), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(296)] = function()
return function(import)
local gameApi = import(S(930))(import)
local log = import(S(636))
local runGuarded = import(S(271))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(926) .. (enabled and S(650) or S(631)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(639) .. node.zoneKey .. S(575) .. node.id
                    .. S(713) .. node.family .. S(492) .. node.level .. S(403) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(651) .. node.id
                    .. S(259) .. node.family .. S(553) .. node.level .. S(560) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(474) .. blocked
                .. S(351))
        elseif powerBlocked then
            log(S(186) .. powerBlocked.zoneKey .. S(357) .. powerBlocked.id
                .. S(834) .. powerBlocked.family .. S(397)
                .. math.floor(powerBlocked.powerReq) .. S(341)
                .. math.floor(powerBlocked.power)
                .. S(156))
        else
            log(S(4)
                .. S(143) .. math.floor(gameApi.GetMoney()) .. S(63))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(967) .. nearest.zoneKey
                    .. S(894) .. nearest.id .. S(958) .. nearest.family .. S(897)
                    .. nearest.level .. S(778) .. nearest.max
                    .. S(723) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(784)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(136),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(847))
        end
        if not buyCore then
            table.insert(parts, S(321))
        end
        if #parts == 0 then
            return S(165)
        end
        return S(199) .. table.concat(parts, S(43)) .. S(562)
    end
    local why
    if nearest.cost > budget then
        why = S(544) .. string.format(S(670), nearest.cost)
            .. S(578) .. string.format(S(831), money)
    elseif nearest.powerOk == false then
        why = S(591) .. string.format(S(340), nearest.powerReq) .. S(642)
    else
        why = S(73)
    end
    return S(467) .. nearest.zoneKey .. S(641) .. tostring(nearest.id)
        .. S(444) .. tostring(nearest.family) .. S(120) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(477) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(718), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(706)] = function()
return function(import)
local Workspace = game:GetService(S(385))
local gameApi = import(S(352))(import)
local teleport = import(S(602))
local log = import(S(822))
local runGuarded = import(S(218))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(744))
    else
        lastAttempt = 0
        log(S(836))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(192))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(219), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(409)
                .. (sold and S(42)
                         or S(956)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(843) .. (sold and (S(625) .. #keys .. S(345)) or S(414)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(370), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(704)] = function()
return function(import)
local Workspace = game:GetService(S(765))
local gameApi = import(S(343))(import)
local teleport = import(S(250))
local log = import(S(262))
local runGuarded = import(S(759))(import)
local STATS = { S(117), S(415), S(55) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(531)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(820) .. (enabled and S(595) or S(197)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(382) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(470) or S(679)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(85) .. holderKey .. S(64) .. choice.stat .. S(905)
                        .. choice.info.level .. S(756) .. after.level
                        .. S(110) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(697) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(294))
    local shop = stand and stand:FindFirstChild(S(441))
    if not shop then
        return nil
    end
    if shop:IsA(S(140)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(153), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(13)
                .. (bought and S(502)
                           or S(366)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(768))
        end
    end
    if bought then
        log(S(322) .. pickaxe.index .. S(119) .. pickaxe.name
            .. S(654) .. pickaxe.multiplier .. S(350))
        return true
    end
    log(S(574) .. pickaxe.index .. S(546) .. pickaxe.name
        .. S(812))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(929) .. holderKey .. S(965)
                    .. tostring(worn or S(57)) .. S(738) .. wanted.name)
            else
                log(S(12) .. holderKey .. S(961)
                    .. wanted.name .. S(561) .. tostring(nowWorn or S(548)) .. S(400))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(747) .. holderKey .. S(476) .. variant
                .. S(529) .. after .. S(138) .. cost .. S(640))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(786) .. gameApi.OwnedPickaxeCount()
                    .. S(832) .. gameApi.MaxMinerSlots()
                    .. S(681))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(960))
        elseif maxed > 0 then
            log(S(974))
        else
            log(S(740))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(798), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(727)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(644))
local Workspace = game:GetService(S(466))
local log = import(S(458))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(864), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(925) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(375))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(829) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(802) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(276) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(777) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(515) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(975))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(407) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(8))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(77), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(155) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(598) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(841) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(479), keys)
end
function api.EquipSlot(slot)
    return send(S(887), slot == S(633) and S(722) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(368), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(100), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(270), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(292))
end
function api.BuySkill(zoneKey, family)
    return send(S(783), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(40)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(121)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(584))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(17) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(800) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(845), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(941))
    plots = plots and plots:FindFirstChild(S(973))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(935)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(235)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(959))
    local gui = board and board:FindFirstChild(S(857))
    local root = gui and gui:FindFirstChild(S(913))
    local list = root and root:FindFirstChild(S(312))
    local row = list and list:FindFirstChild(S(394) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(418)) and descendant.Text ~= S(819) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(149) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(964), characterKey, stat == S(661) and S(279) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(758), max = S(411), cost = S(694) },
    Speed = { level = S(791), max = S(707), cost = S(699) },
    MineSpeed = { level = S(746), max = S(939), cost = S(874) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(462) or charName == S(928) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(770) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(878) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(927) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(526) and type(holder.CharName) == S(846) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(424), S(487) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(233) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(238)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(245)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(488)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(468) then
        return {}, S(277)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(726)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(274) or type(state.Track) ~= S(539) then
                table.insert(notes, tabKey .. S(657) .. tostring(category) .. S(216))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(298) .. waiting
                        .. S(610))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(112) .. total .. S(54)
            .. (#notes > 0 and (S(393) .. table.concat(notes, S(901))) or S(213))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(70) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(646)] = function()
return function(import)
local Players = game:GetService(S(877))
local gameApi = import(S(272))(import)
local log = import(S(622))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(552))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(485), S(309))))
    if not value then
        return nil
    end
    if suffix ~= S(908) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(102))
    local mineGui = playerGui and playerGui:FindFirstChild(S(61))
    local hotbar = mineGui and mineGui:FindFirstChild(S(278))
    local content = hotbar and hotbar:FindFirstChild(S(1))
    return content and content:FindFirstChild(S(113))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(251)) then
            local count = descendant.Text:match(S(506))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(862))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(792))
    local nameLabel = content and content:FindFirstChild(S(508))
    local damageLabel = content and content:FindFirstChild(S(135))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(605) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(145) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(643) .. slot .. S(231) .. info.name
                        .. S(160) .. info.power .. S(533) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(22)] = function()
local Players = game:GetService(S(766))
local Workspace = game:GetService(S(593))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(524))
    local plots = mineWorld and mineWorld:FindFirstChild(S(191))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(440)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(910)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(417)),
        Nodes = plot:FindFirstChild(S(384)),
        Chest = plot:FindFirstChild(S(896)),
        Zones = plot:FindFirstChild(S(790)),
        Crew = plot:FindFirstChild(S(904)),
        Loot = plot:FindFirstChild(S(518)),
        PlotIndex = plot:GetAttribute(S(69)),
        ChestCapacity = plot:GetAttribute(S(237)),
    }
end
return getPlotParts
end
modules[S(830)] = function()
return function(import)
    local createWindow = import(S(10))(import)
    local i18n = import(S(363))
    local antiAfk = import(S(348))(import)
    local buildEnv = import(S(932))
    local scheduler = import(S(970))(import)
    local config = import(S(187))(import)
    local log = import(S(731))
    log(S(439) .. buildEnv.Commit .. S(168) .. buildEnv.Env
        .. S(11) .. buildEnv.BuiltAt .. S(826))
    local window = createWindow({
        Subtitle = S(234),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(702))(import)
    local autoPlace = import(S(530))(import)
    local autoChest = import(S(71))(import)
    local autoSell = import(S(838))(import)
    local autoUpgrade = import(S(364))(import)
    local autoResearch = import(S(198))(import)
    local autoClaim = import(S(813))(import)
    local autoPlot = import(S(950))(import)
    local gameApi = import(S(737))(import)
    scheduler.RegisterStandalone(S(883), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(367), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(333), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(774), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(695), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(177), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(757), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(637), 20)
    scheduler.RegisterExclusive(S(687), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(632), S(174))
    persistentToggle(farmTab:AddCard(
        S(948),
        S(572)
    ), S(128), autoMine.SetEnabled)
    persistentSlider(farmTab, S(246), S(972),
        1, 30, S(621), 5, autoMine.SetCyclesPerSecond, S(181))
    persistentSlider(farmTab, S(111), S(319),
        1, 10, S(306), 1, autoMine.SetClicksPerCycle, S(389))
    persistentToggle(farmTab:AddCard(
        S(938),
        S(962)
    ), S(220), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(833), S(888),
        1, 60, S(940), 3, autoPlace.SetCheckInterval, S(669))
    persistentToggle(
        farmTab:AddCard(S(971),
            S(176)),
        S(585), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(307),
            S(125)),
        S(210), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(33), S(603),
        5, 300, S(74), 60, autoPlace.SetEquipBestInterval, S(226))
    persistentToggle(
        farmTab:AddCard(S(50),
            S(449)),
        S(677), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(866), S(599),
        0, 500, S(942), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(520))
    persistentToggle(farmTab:AddCard(
        S(724),
        S(116)
    ), S(58), autoChest.SetEnabled)
    persistentSlider(farmTab, S(688), S(898),
        50, 3000, S(59), 100, autoChest.SetMinItems, S(635))
    persistentSlider(farmTab, S(824), S(733),
        1, 60, S(162), 3, autoChest.SetCheckInterval, S(180))
    persistentToggle(farmTab:AddCard(
        S(263),
        S(98)
    ), S(748), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(825),
            S(871)),
        S(161), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(850), S(799),
        5, 300, S(365), 20, autoSell.SetCheckInterval, S(287))
    farmTab:AddSlider(S(577),
        S(596),
        5, 120, sellMaxWait, function(value)
            config.Set(S(865), value)
        end, S(323))
    persistentToggle(farmTab:AddCard(
        S(779),
        S(629)
    ), S(827), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(581), S(494),
        5, 300, S(732), 20, autoUpgrade.SetInterval, S(528))
    persistentSlider(farmTab, S(921), S(594),
        1, 20, S(551), 5, autoUpgrade.SetMaxPerPass, S(754))
    persistentSegmented(
        farmTab,
        { S(968), S(239), S(565), S(855) },
        { S(227), S(909), S(158), S(823) },
        S(547), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(664), S(29),
        0, 500, S(764), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(391))
    persistentToggle(
        farmTab:AddCard(S(760),
            S(821)),
        S(806), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(435) .. pickaxe.multiplier .. S(752)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(208)
            end
            return S(481) .. index .. S(122) .. (pickaxeLabels[index] or S(513))
        end
        local savedPick = config.Get(S(327), 0)
        pickaxeCard = farmTab:AddSlider(
            S(457),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(504), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(56)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(867),
            S(224)),
        S(426), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(592),
            S(178)),
        S(885), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(947),
            S(486)),
        S(545), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(428), S(556),
        1, 20, S(104), 2, autoUpgrade.SetMutationTarget, S(478))
    persistentSlider(farmTab, S(618), S(378),
        0, 500, S(247), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(334))
    persistentToggle(farmTab:AddCard(
        S(66),
        S(512)
    ), S(705), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(147), S(923),
        10, 600, S(678), 30, autoResearch.SetCheckInterval, S(816))
    persistentSlider(farmTab, S(254), S(776),
        1, 20, S(130), 5, autoResearch.SetMaxPerPass, S(491))
    persistentSlider(farmTab, S(20), S(67),
        0, 500, S(920), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(342))
    persistentToggle(
        farmTab:AddCard(S(809),
            S(902)),
        S(667), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(604),
            S(742)),
        S(886), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(714),
            S(14)),
        S(30), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(65), S(525) },
        { true, false },
        S(387), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(39),
        S(265)
    ), S(188), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(282), S(7),
        5, 300, S(283), 30, autoClaim.SetCheckInterval, S(570))
    persistentSlider(farmTab, S(223), S(16),
        1, 30, S(377), 10, autoClaim.SetMaxPerPass, S(890))
    persistentToggle(farmTab:AddCard(
        S(945),
        S(9)
    ), S(892), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(123), S(273),
        5, 300, S(882), 30, autoPlot.SetCheckInterval, S(856))
    persistentToggle(
        farmTab:AddCard(S(949),
            S(709)),
        S(842), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(511),
            S(683)),
        S(142), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(505), S(190),
        0, 500, S(475), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(588))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(676))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(47), S(134))
    persistentToggle(
        settingsTab:AddCard(S(455), S(257)),
        S(675),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(814), 100)
    settingsTab:AddSlider(S(503), S(172), 50, 150, savedScale, function(value)
        config.Set(S(542), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(567), 1)
    settingsTab:AddSegmented({ S(696), S(423), S(933) }, savedLanguage, function(index, value)
        local code = ({ S(818), S(241), S(154) })[index]
        config.Set(S(931), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(398), S(92), S(773) })[savedLanguage] or S(775))
    log(S(482) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(248)] = function()
return {
    [79389059854988] = S(236),
}
end
import(S(139))(import)
