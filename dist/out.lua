-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"", "games/my_anime_mine/debug_dump_remotes", "autoMineClicks", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "TakeButton", "ReplicatedStorage", "games/my_anime_mine/debug_dump_sell_cards", "[Config] couldn't write ", "[AutoChest] enabled (collects once >= ", "(", "Content", "ค้นหาฟังก์ชัน...", "^([%d,]+)$", "Main", "[AntiAFK] disabled", "Debug: Dump All Remotes", "[AutoPlace] placed ", " '", "VirtualUser", "games/my_anime_mine/get_plot_parts", "", "'", "core/get_me", " ===", "Frame", "games/my_anime_mine/main", "MineGui", "Plots", "games/my_anime_mine/debug_dump_chest", "games/my_anime_mine/get_best_miner", "autoMineSpeed", "TextButton", "RunService", ": ", "[Config] couldn't read ", ": ", "ClickDetector", "PlayerGui", "[MyAnimeMine] === Dump start ===", "ปุ่มเปิดเมนู", "MineGui", "ProximityPrompt", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[SellCards] === total cards: ", "Content", "?", "th", "core/logger", "Anti-AFK", ": ", "[AntiAFK] idle detected, simulating input", "autoMineClicks", "MekPro", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "en", "Holders", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "[MyAnimeMine] not standing in any plot", "  ", "ฟาร์ม", "=locked(", "Search functions...", "[Interaction] logger disabled", "Auto Upgrade", "core/scheduler", "games/my_anime_mine/get_plot_parts", "  ", "", "  [attr] ", "[GuiDump] MineGui not found", "UIScale", "^(%d+)", "Players", "[AntiAFK] enabled - watching Player.Idled", "Content", "Players", "Frame", "games/my_anime_mine/get_current_plot", "core/logger", "[attr] ", "th", "-", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "GuiObject", "core/interaction_logger", "Players", "Damage", " attributes ===", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "Error: ", "%$", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "Done", "games/my_anime_mine/auto_mine", "Badge", "core/logger", "  [attr] ", "OwnerUserId", "games/registry", "games/my_anime_mine/get_current_plot", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[AutoChest] disabled", "core/dispatch", "games/my_anime_mine/get_plot_parts", "Frame", "core/ui", "Badge", "enabled", "Check", "auto", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Frame", "/", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Debug: Dump Chest Data", "core/teleport", "'", "", "core/logger", "", " '", "[AutoPlace] couldn't pick up ", "[AutoChest] Take All worked: ", "not standing in any plot", "core/logger", ")", "Players", "UIListLayout", "", "SellFrame", "autoMineSpeed", " -> ", "core/logger", "", "core/scheduler", "Debug: Current Plot", "", "games/my_anime_mine/debug_dump", "[AutoMine] ", "TextLabel", "", " = ", "Sell for", "games/my_anime_mine/debug_dump_chest", "RemoteEvent", "no tool", "TextLabel", " = ", " remotes", "no plot", "Auto Sell", "[ChestDump] plot has no Chest", "core/interaction_logger", "Content", "games/my_anime_mine/auto_upgrade", ") didn't take, trying the next one", " (", "[AutoChest] waiting: ", "/", "/config.json", "Close", "Open menu", " doesn't exist", "TextLabel", "core/logger", "[AutoSell] fireproximityprompt error: ", "en", "Players", "[Interaction] Button activated: ", " (Text=", "[AutoPlace] fireproximityprompt error: ", "not found", "TextLabel", "Content", "=empty", "core/click", "", "core/logger", "SellButton", "[Scheduler] turn: ", "disabled", "'", "[MyAnimeMine] === Node: ", ")", "core/teleport", "[ChestDump] not standing in any plot", ": ", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "[AutoSell] nothing to sell, closing", "Scroll", "HumanoidRootPart", " = ", "core/i18n", "[Remotes] === total: ", "TextLabel", "core/i18n", "MineGui", "games/my_anime_mine/get_plot_parts", "autoPlace", ",", "unreadable", "[AutoPlace] holders: ", " '", "  [attr] ", "[Scheduler] ", "[AutoPlace] upgrading ", "games/my_anime_mine/get_plot_parts", "Card", "TextLabel", "[ChestDump] === plot ", "=", "TextButton", "", "games/my_anime_mine/auto_place", "[GuiDump] === visible MineGui tree ===", "Actions", ")", "VirtualInputManager", "auto", "PlayerGui", "[Interaction] logger enabled - go click/interact with things now", "ItemName", " still empty after placing ", "MineGui", "/", "games/my_anime_mine/get_current_plot", "'), backing off ", "autoUpgrade", "Dump", "Dump", "", "UpgradePart", "Frame", "TextButton", "ClickDetector", "Auto Mine", "ขนาดเมนู", "TextLabel", "TextLabel", "^%$", "Auto Mine: ความเร็ว", "[MyAnimeMine] === Tool: ", "search_placeholder", "core/logger", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", " cards)", "games/my_anime_mine/debug_dump_remotes", "core/dispatch", "[AutoUpgrade] ", "autoMine", ") -> ", "UserInputService", "TextButton", "", " while selecting", "RemoteEvent", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", " error: ", "Amount", ": ", "[AutoPlace] nothing in the hotbar to place", "neither appendfile nor writefile is available", " '", "  ", "TextButton", " ===", "Card", "core/click", "enabled", "", "[AutoChest] chest didn't open, aborting", "[AutoChest] click failed on ", "games/my_anime_mine/get_plot_parts", "[AutoSell] disabled", "games/my_anime_mine/debug_dump_gui", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " items)", "[Uu]nlock", "core/get_place_id", " from ", " = ", " (power ", "Chest", " '", "SellFrame", "Humanoid", "Players", "auto", "leaderstats", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", " | ", "Debug: Dump Backpack Tool", "UIListLayout", "core/config", "TextLabel", "ReplicatedStorage", " parts", "antiAfk", "PlayerGui", "^x(%d+)$", "core/logger", "Dump", "open_menu_hint", "menuScale", "games/my_anime_mine/debug_dump_sell_cards", "disabled", " = ", "Tool", "Select All", "", "languageIndex", "Debug: Dump Sell Cards", "Frame", "Container", "games/registry", "autoSell", "TextLabel", "ProximityPrompt", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "AutoMine", "SelectAllButton", "[AutoPlace] ", "TabBar", "Header", "TextButton", "[ChestDump] === Player subtree ===", " items", "", "[Logger] failed to write ", "prompt disabled", "ItemFrame", "core/anti_afk", "Workspace", " (", "Frame", "Progress", "Interaction Logger (debug)", "core/logger", "autoChest", "th", "TextButton", "core/config", "[ChestDump] === ReplicatedStorage.", "games/my_anime_mine/debug_dump", " (Text=", "[MyAnimeMine] no nodes present right now", "games/my_anime_mine/get_current_plot", "Players", "TextLabel", "UserInputService", "Content", "  ", "core/get_leaderstat", "[AutoChest] can't read the chest billboard, is the plot loaded?", "en", "HumanoidRootPart", ",", "  [attr] ", "search_placeholder", "x", "[%X] ", "", "waiting", "[AutoChest] ", "Players", "Auto Place Miner", ")", "Players", "[ChestDump] === Chest subtree ===", "[MekPro] settings file: ", ") on ", "PlayerGui", " ", "ProximityPrompt", " ===", "[AutoSell] enabled (only sells when there's something in the scroll)", " (ActionText=", "UICorner", "every holder already has something at least as strong as the hotbar's best", "[AutoSell] click failed on ", "Sell for", ")", "[AutoChest] Take All had NO effect (", "[ChestDump] === end ===", "Content", "TextLabel", "MineWorld", "[AutoSell] sell menu didn't open, aborting", " cards", "games/my_anime_mine/auto_upgrade", "Text", "core/get_place_id", "  ", "core/logger", "TextButton", "Workspace", "ScreenGui", "GuiButton", "[GuiDump] === end ===", "Auto Chest", "auto", "Nodes", "[BestMiner] slot ", "Text", "Dump", "🏠", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "games/my_anime_mine/auto_mine", "')", "waiting", "Loot", "[AutoPlace] disabled", "  ", "HotbarFrame", "My Anime Mine · Mining Hub", "AutoSell", " ===", "games/my_anime_mine/get_plot_parts", "ProximityPrompt", "core/logger", "Frame", "RemoteFunction", "core/logger", "en-us", "UIPadding", "'", "GuiService", "ตามเครื่อง", "Backpack", "s", " (prompt says '", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "Done", "SellStand", "Search", " = ", "ProximityPrompt", "RemoteFunction", "Crew", "TextLabel", " (Text=", "[AutoSell] nothing selectable (", "languageIndex", "TextLabel", "[Interaction] ProximityPrompt triggered: ", "Frame", "X", "Frame", "CoreGui", "games/my_anime_mine/debug_dump_gui", "TextButton", "TextButton", "[AutoPlace] slot ", "[MyAnimeMine] === Holder: ", "games/my_anime_mine/auto_chest", "", "core/logger", "write reported success but the line isn't in ", "'", ": ", "", "UICorner", "[Interaction] ClickDetector clicked: ", "log failed", "MekPro.log", "PlayerGui", "PlayerGui", "games/my_anime_mine/auto_place", "Zones", ")", "core/get_place_id", "[Bb]uy", "AutoPlace", "TextLabel", "games/my_anime_mine/get_current_plot", "core/ui", "[Remotes] ", "TextLabel", "[AutoSell] clicked SelectAllButton (", "", "GuiObject", "%", "UIListLayout", "ScrollingFrame", " ===", "Debug: Dump Node/Holder", "core/click", "core/teleport", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "games/my_anime_mine/get_best_miner", "[AutoPlace] ", "Backpack", " power ", "HttpService", "ProximityPrompt", "CoreUI", "Done", "Content", "ตั้งค่า", "ไทย", "'", " -> ", "อังกฤษ", "ValueLabel", "core/logger", "Players", " <-- remote, likely fired by the real interaction", "games/my_anime_mine/main", " items", "hotbar has no miner to place", " (power ", "\n", " x", "table", "⚙", "Label", "core/click", ")", " items, going to collect", "games/my_anime_mine/auto_chest", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "ย่อขยายเมนูทั้งชุด", "open_menu_hint", ": ", " = ", "/s", "GuiButton", ") from ", "games/my_anime_mine/auto_sell", " items)", "PlotIndex", ": ", "[AutoPlace] picked up ", "core/teleport", "Players", "Debug: Dump Visible GUI", "Dump", "This is not a supported place.", "TextLabel", "interactionLogger", "[AutoPlace] drifted out of range of ", "action '", "write reported success but ", "ValueBase", "[^%d%.]", " (", "ChestFrame", "core/anti_afk", "", "Label", "ChestCapacity", " ===", "", " (MaxActivationDistance=%s)", "\n", "ClickDetector", "MekPro", "Dump", "[MyAnimeMine] no holders found", "[AutoSell] clicked SellButton", "games/my_anime_mine/get_current_plot", "costs ", "Workspace", "UIStroke", "games/my_anime_mine/get_current_plot", "unreadable", "Pad", "[Interaction] Key pressed: %s (gameProcessed=%s)", "menuScale", ": ", "th", "games/my_anime_mine/auto_sell", "Auto Mine: คลิกต่อรอบ", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "Players", "ValueLabel", "ClickDetector", "Empty miner slot", "Players", "Close", "Content", "MineGui", "AutoChest", "Players", "no isfile/readfile available to verify the write", "TextBox", "core/i18n", "[SellCards] Card #", "Scroll", " cards left, label = '", "AutoUpgrade", "ClickDetector", "button has no size on screen", "[AutoChest] fireproximityprompt error: ", "ValueBase", "Frame", "(none enabled)", "Place"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(548)] = function()
return function(import)
local log = import(S(125))
local Players = game:GetService(S(127))
local VirtualUser = game:GetService(S(19))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(74))
        connection = player.Idled:Connect(function()
            log(S(51))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(15))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(517)] = function()
local VirtualInputManager = game:GetService(S(218))
local GuiService = game:GetService(S(427))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(593)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(294)] = function()
return function(import)
local HttpService = game:GetService(S(494))
local getPlaceId = import(S(471))
local log = import(S(244))
local ROOT_FOLDER = S(53)
local placeFolder = ROOT_FOLDER .. S(225) .. tostring(getPlaceId())
local filePath = placeFolder .. S(160)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(514) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(35) .. filePath .. S(524) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(8) .. filePath .. S(460) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(103)] = function()
return function(import)
    local getPlaceId = import(S(392))
    local registry = import(S(315))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(538))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(353)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(289))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(23)] = function()
local Players = game:GetService(S(348))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(286))
    local backpack = player:WaitForChild(S(492))
    return player, humanoid, backpack
end
return getMe
end
modules[S(279)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(196)] = function()
local Players = game:GetService(S(365))
local STRINGS = {
    en = {
        search_placeholder = S(62),
        open_menu_hint = S(162),
    },
    th = {
        search_placeholder = S(12),
        open_menu_hint = S(40),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(424)
    if locale:sub(1, 2):lower() == S(81) then
        return S(571)
    end
    return S(55)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(110) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(85)] = function()
return function(import)
local Players = game:GetService(S(73))
local Workspace = game:GetService(S(396))
local UserInputService = game:GetService(S(252))
local log = import(S(48))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(445) .. describe(prompt)
            .. S(377) .. prompt.ActionText .. S(518))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(463) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(169) .. describe(button)
            .. S(345) .. tostring(button.Text) .. S(367))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(437)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(235)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(398)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(63))
        return
    end
    connections = {}
    log(S(221))
    local playerGui = Players.LocalPlayer:WaitForChild(S(467))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(374)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(577)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(527)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(568),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(338)] = function()
local LOG_FILE = S(465)
local function log(message)
    local line = os.date(S(361)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(1))
            end
            appendfile(LOG_FILE, line .. S(555))
        elseif writefile then
            local existing = S(480)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(512))
        else
            error(S(262))
        end
        if not (isfile and readfile) then
            error(S(585))
        end
        if not isfile(LOG_FILE) then
            error(S(543) .. LOG_FILE .. S(163))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(458) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(329) .. LOG_FILE .. S(570) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(135)] = function()
return function(import)
local RunService = game:GetService(S(33))
local log = import(S(96))
local SLICE_SECONDS = 3
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy)
    table.insert(exclusiveTasks, { name = name, step = step, isEnabled = isEnabled, isBusy = isBusy })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(205) .. task_.name .. S(258) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickNextEnabled()
    if #exclusiveTasks == 0 then
        return nil
    end
    for _ = 1, #exclusiveTasks do
        rrIndex = (rrIndex % #exclusiveTasks) + 1
        local candidate = exclusiveTasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(180) .. (currentTask and currentTask.name or S(597)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(488)] = function()
local Players = game:GetService(S(506))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(191))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(476)] = function()
return function(import)
local i18n = import(S(193))
local UserInputService = game:GetService(S(350))
local THEME = {
    BrandName = S(557),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(462))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(378))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(425))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(212))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(213)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(564))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(147))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(46)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(349))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(231)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(484))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(128))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(474))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(596))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(317))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(440))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(173))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(504))
            if not valueLbl then
                valueLbl = Instance.new(S(386))
                valueLbl.Name = S(576)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(107))
            if not badge then
                badge = Instance.new(S(239))
                badge.Name = S(95)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(452))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(112))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(421))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(395))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(141)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(482)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(25))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(448))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(341))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(137)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(335))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(293))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(32))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(404)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(397))
    screenGui.Name = S(496)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(449))
    local main = Instance.new(S(77))
    main.Name = S(14)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(71))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(105))
    tabBar.Name = S(323)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(483))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(233))
    header.Name = S(324)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(253))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(295))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(209))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(310)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(451))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(447)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(234))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(82)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(586))
    searchBox.Name = S(435)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(243))
    searchBox.Text = S(456)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(313))
    content.Name = S(45)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(446))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(164))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(303))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(140))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(391)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(549) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(373) .. (item.Subtitle or S(177))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(359))
        footerLabel.Text = i18n.T(S(523))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(455)] = function()
return function(import)
local Players = game:GetService(S(76))
local getCurrentPlot = import(S(100))
local getPlotParts = import(S(207))
local teleport = import(S(185))
local click = import(S(268))
local log = import(S(505))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(38))
    local mineGui = playerGui and playerGui:FindFirstChild(S(582))
    return mineGui and mineGui:FindFirstChild(S(547))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(154))
    local actions = content and content:FindFirstChild(S(216))
    return actions and actions:FindFirstChild(S(5))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(200), S(68)):match(S(72))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(259), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(75))
    local progress = content and content:FindFirstChild(S(336))
    local label = progress and progress:FindFirstChild(S(516))
    return parseCount(label and label.Text or S(21))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(272) .. button.Name .. S(260) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(42), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(102))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(9) .. MIN_ITEMS_TO_COLLECT .. S(530))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(594) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(271))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(123) .. count .. S(502) .. after .. S(327))
        else
            log(S(383) .. tostring(count)
                .. S(132) .. tostring(after) .. S(277))
        end
    end
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(201) then
            log(S(354))
            lastReportedCount = S(566)
        end
        return
    end
    if count < MIN_ITEMS_TO_COLLECT then
        if lastReportedCount ~= S(363) then
            log(S(158) .. count .. S(159) .. MIN_ITEMS_TO_COLLECT .. S(509))
            lastReportedCount = S(410)
        end
        return
    end
    lastReportedCount = nil
    log(S(364) .. count .. S(519))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(408)] = function()
return function(import)
local getCurrentPlot = import(S(347))
local getPlotParts = import(S(104))
local log = import(S(423))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(37), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(139) .. (enabled and S(108) or S(306)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(214)] = function()
return function(import)
local Players = game:GetService(S(535))
local getCurrentPlot = import(S(226))
local getPlotParts = import(S(418))
local getMinerSlots = import(S(30))(import)
local teleport = import(S(116))
local click = import(S(487))
local log = import(S(457))
local CHECK_INTERVAL = 3
local EMPTY_SLOT_TEXT = S(578)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(567))
    return pad and pad:FindFirstChildWhichIsA(S(495), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(444)) then
            local digits = descendant.Text:match(S(13))
            if digits then
                return tonumber((digits:gsub(S(357), S(362))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(539)) and descendant.Text:match(S(240)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if not prompt.Enabled then
        return true, S(330)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(562) .. price
    end
    local action = prompt.ActionText or S(254)
    if action:match(S(278)) or action:match(S(472)) or action:match(S(91)) then
        return true, S(542) .. action .. S(459)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(356))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(171) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(61) .. why .. S(217))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(175))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(211) .. prompt.ObjectText .. S(10) .. power .. S(126))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(291))
    if line ~= lastSurvey then
        log(S(202) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return empty, occupied
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(322) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(412))
    else
        lastAttempt = 0
        log(S(407))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(92),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(598), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(261))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(541) .. holder.Name .. S(255))
            return false
        end
        log(S(453) .. candidate.slot .. S(334) .. candidate.name
            .. S(156))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(491) .. holder.Name .. S(223) .. miner.name)
        return false
    end
    log(S(17) .. miner.name .. S(511) .. miner.power
        .. S(371) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(122) .. entry.name .. S(280) .. entry.holder.Name
            .. S(431) .. tostring(entry.prompt.ActionText) .. S(409))
        return false
    end
    log(S(533) .. entry.name .. S(282) .. entry.power
        .. S(528) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local empty, occupied = surveyHolders()
    if not empty then
        reason(S(124))
        return
    end
    local candidates = getMinerSlots()
    if #candidates == 0 then
        reason(S(510))
        return
    end
    if #empty > 0 then
        lastReason = nil
        task.spawn(function()
            runFillJob(empty[1])
        end)
        return
    end
    local weakest = occupied[1]
    local best = candidates[1]
    if weakest and best.power > weakest.power then
        lastReason = nil
        log(S(206) .. weakest.holder.Name .. S(34) .. weakest.name
            .. S(157) .. weakest.power .. S(251) .. best.name .. S(546) .. best.power .. S(184))
        task.spawn(function()
            runUpgradeJob(weakest)
        end)
        return
    end
    reason(S(379))
end
return autoPlace
end
end
modules[S(572)] = function()
return function(import)
local Players = game:GetService(S(168))
local Workspace = game:GetService(S(333))
local teleport = import(S(534))
local click = import(S(176))
local log = import(S(119))
local CHECK_INTERVAL = 45
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(466))
    local mineGui = playerGui and playerGui:FindFirstChild(S(197))
    return mineGui and mineGui:FindFirstChild(S(285))
end
local function labelText(button)
    local content = button:FindFirstChild(S(498))
    local label = content and content:FindFirstChild(S(550))
    return label and label.Text or S(120)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(581))
    local scroll = content and content:FindFirstChild(S(190))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(267) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(380) .. button.Name .. S(532) .. tostring(err))
    end
    return ok
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(434))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(318), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(274))
    else
        lastAttempt = 0
        log(S(376))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(166) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(388))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(189))
        local closeButton = sellFrame:FindFirstChild(S(580))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(174))
    local selectAllButton = content and content:FindFirstChild(S(321))
    local sellButton = content and content:FindFirstChild(S(179))
    if selectAllButton and labelText(selectAllButton) == S(309) then
        clickButton(selectAllButton)
        log(S(479) .. cardCount .. S(246))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(143), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(381), 1, true) then
        clickButton(sellButton)
        log(S(560))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(161))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(442) .. cardCount .. S(590)
            .. tostring(sawLabel) .. S(227) .. NOTHING_SELLABLE_COOLDOWN .. S(430))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(390)] = function()
return function(import)
local getCurrentPlot = import(S(561))
local getPlotParts = import(S(273))
local log = import(S(301))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(232))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(592))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(249) .. (enabled and S(269) or S(181)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(344)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(270)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(263) .. child.Name .. S(22)
        if child:IsA(S(419)) then
            line = line .. string.format(
                S(114),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(556)) then
            line = line .. string.format(S(554), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(256)) or child:IsA(S(422)) then
            line = line .. S(507)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(97) .. attrName .. S(307) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(393))
    end
end
return dumpInstance
end
modules[S(144)] = function()
return function(import)
local Players = game:GetService(S(579))
local ReplicatedStorage = game:GetService(S(296))
local getCurrentPlot = import(S(475))
local getPlotParts = import(S(198))
local log = import(S(165))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(80) .. name .. S(281) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(59))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(203) .. child.Name .. S(501)
            if child:IsA(S(595)) then
                line = line .. S(525) .. tostring(child.Value)
            elseif child:IsA(S(238)) or child:IsA(S(265)) then
                line = line .. S(170) .. tostring(child.Text) .. S(382)
            end
            log(line)
            dumpSubtree(child, indent .. S(352))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(186))
        return 0
    end
    log(S(210) .. plot.Name .. S(88))
    dumpAttributes(plot, S(264))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(152))
        return 0
    end
    log(S(369))
    dumpSubtree(parts.Chest, S(134))
    local player = Players.LocalPlayer
    log(S(326))
    dumpSubtree(player, S(328))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(343) .. player.Name .. S(266))
        dumpSubtree(mine, S(129))
    end
    log(S(384))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(275)] = function()
return function(import)
local Players = game:GetService(S(368))
local log = import(S(420))
local function dumpVisible(root, indent)
    indent = indent or S(553)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(481)) and child.Visible then
            local line = indent .. child.ClassName .. S(121) .. child.Name .. S(182)
            if child:IsA(S(478)) or child:IsA(S(325)) then
                line = line .. S(441) .. tostring(child.Text) .. S(470)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(69) .. attrName .. S(436) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(67))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(372))
    local mineGui = playerGui and playerGui:FindFirstChild(S(27))
    if not mineGui then
        log(S(70))
        return
    end
    log(S(215))
    dumpVisible(mineGui)
    log(S(399))
end
return dumpMineGui
end
end
modules[S(247)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(6))
local log = import(S(394))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(145)) or descendant:IsA(S(438)) then
            count = count + 1
            log(S(477) .. descendant.ClassName .. S(50) .. descendant:GetFullName())
        end
    end
    log(S(194) .. count .. S(552))
    return count
end
return dumpRemotes
end
end
modules[S(7)] = function()
return function(import)
local Players = game:GetService(S(584))
local log = import(S(79))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(220))
    local mineGui = playerGui and playerGui:FindFirstChild(S(224))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(130))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(385)) and sellFrame.Content:FindFirstChild(S(589))
    if not scroll then
        log(S(188))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(208) or card:IsA(S(84)) then
            count = count + 1
            log(S(588) .. count .. S(187) .. card.ClassName .. S(18) .. card.Name .. S(117))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(204) .. attrName .. S(142) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(413) .. child.ClassName .. S(284) .. child.Name .. S(426)
                if child:IsA(S(544)) then
                    line = line .. S(192) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(44) .. count .. S(375))
    return count
end
return dumpSellCards
end
end
modules[S(490)] = function()
return function(import)
local Players = game:GetService(S(86))
local log = import(S(178))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(299))
    local mineGui = playerGui and playerGui:FindFirstChild(S(41))
    local hotbar = mineGui and mineGui:FindFirstChild(S(414))
    local content = hotbar and hotbar:FindFirstChild(S(11))
    return content and content:FindFirstChild(S(314))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(195)) then
            local count = descendant.Text:match(S(300))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(331))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(351))
    local nameLabel = content and content:FindFirstChild(S(222))
    local damageLabel = content and content:FindFirstChild(S(87))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(118) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(545), S(461)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(113) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(403) .. slot .. S(36) .. info.name
                        .. S(493) .. info.power .. S(513) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(565)] = function()
local Players = game:GetService(S(575))
local Workspace = game:GetService(S(563))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(387))
    local plots = mineWorld and mineWorld:FindFirstChild(S(28))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(98)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(20)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(56)),
        Nodes = plot:FindFirstChild(S(402)),
        Chest = plot:FindFirstChild(S(283)),
        Zones = plot:FindFirstChild(S(469)),
        Crew = plot:FindFirstChild(S(439)),
        Loot = plot:FindFirstChild(S(411)),
        PlotIndex = plot:GetAttribute(S(531)),
        ChestCapacity = plot:GetAttribute(S(551)),
    }
end
return getPlotParts
end
modules[S(26)] = function()
return function(import)
    local Players = game:GetService(S(287))
    local createWindow = import(S(106))(import)
    local i18n = import(S(587))
    local antiAfk = import(S(332))(import)
    local interactionLogger = import(S(153))(import)
    local scheduler = import(S(65))(import)
    local config = import(S(342))(import)
    local log = import(S(133))
    local window = createWindow({
        Subtitle = S(415),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local getCurrentPlot = import(S(78))
    local getPlotParts = import(S(66))
    local dumpInstance = import(S(138))
    local dumpRemotes = import(S(2))(import)
    local dumpSellCards = import(S(305))(import)
    local dumpGui = import(S(450))(import)
    local dumpChest = import(S(29))(import)
    local autoMine = import(S(94))(import)
    local autoPlace = import(S(468))(import)
    local autoChest = import(S(520))(import)
    local autoSell = import(S(529))(import)
    local autoUpgrade = import(S(155))(import)
    scheduler.RegisterStandalone(S(320), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterStandalone(S(591), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.RegisterExclusive(S(473), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy)
    scheduler.RegisterExclusive(S(583), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy)
    scheduler.RegisterExclusive(S(416), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(60), S(406))
    persistentToggle(farmTab:AddCard(
        S(236),
        S(43)
    ), S(250), autoMine.SetEnabled)
    local savedMineSpeed = config.Get(S(31), 5)
    farmTab:AddSlider(
        S(241),
        S(245),
        1, 30, savedMineSpeed,
        function(value)
            config.Set(S(131), value)
            autoMine.SetCyclesPerSecond(value)
        end,
        S(526)
    )
    autoMine.SetCyclesPerSecond(savedMineSpeed)
    local savedMineClicks = config.Get(S(3), 1)
    farmTab:AddSlider(
        S(573),
        S(319),
        1, 10, savedMineClicks,
        function(value)
            config.Set(S(52), value)
            autoMine.SetClicksPerCycle(value)
        end,
        S(360)
    )
    autoMine.SetClicksPerCycle(savedMineClicks)
    persistentToggle(farmTab:AddCard(
        S(366),
        S(111)
    ), S(199), autoPlace.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(400),
        S(89)
    ), S(339), autoChest.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(151),
        S(101)
    ), S(316), autoSell.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(64),
        S(521)
    ), S(228), autoUpgrade.SetEnabled)
    local debugCard = farmTab:AddCard(S(136), S(54))
    local checkBtn
    checkBtn = debugCard:AddButton(S(109), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(172)
            log(S(58))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(83),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(486),
        S(290)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(229), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(150)
            return
        end
        local ok, err = log(S(39))
        if not ok then
            dumpBtn.Text = S(464)
            dumpCard:SetSubtitle(S(90) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(183) .. firstNode.Name .. S(417))
            dumpInstance(firstNode, log)
        else
            log(S(346))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(454) .. firstHolder.Name .. S(485))
            dumpInstance(firstHolder, log)
        else
            log(S(559))
        end
        dumpBtn.Text = S(93)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(292),
        S(432)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(537), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(429))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(308))
        if not tool then
            toolDumpBtn.Text = S(146)
            return
        end
        log(S(242) .. tool.Name .. S(24))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(358) .. attrName .. S(148) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(433)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(16),
        S(276)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(302), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(149)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(312),
        S(574)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(230), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(389)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(536),
        S(489)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(405), function()
        dumpGui()
        guiDumpBtn.Text = S(497)
    end)
    local chestDumpCard = farmTab:AddCard(
        S(115),
        S(257)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(558), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(297)
    end)
    local settingsTab = window:AddTab(S(499), S(515))
    persistentToggle(
        settingsTab:AddCard(S(49), S(57)),
        S(298),
        antiAfk.SetEnabled
    )
    persistentToggle(
        settingsTab:AddCard(
            S(337),
            S(4)
        ),
        S(540),
        interactionLogger.SetEnabled
    )
    local savedScale = config.Get(S(304), 100)
    settingsTab:AddSlider(S(237), S(522), 50, 150, savedScale, function(value)
        config.Set(S(569), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(443), 1)
    settingsTab:AddSegmented({ S(428), S(503), S(500) }, savedLanguage, function(index, value)
        local code = ({ S(219), S(355), S(47) })[index]
        config.Set(S(311), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(401), S(167), S(340) })[savedLanguage] or S(288))
    log(S(370) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(99)] = function()
return {
    [79389059854988] = S(508),
}
end
import(S(248))(import)
