-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Dump All Remotes", "AutoPlace", "[AutoSell] ", "  [attr] ", "core/debug_decompile", "Damage ก่อน", ",", "", " = ", "TextLabel", "Workspace", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "เช็คโซนทุกกี่วินาที", "walkSpeed", "GetHolderSpeedLevel", "interactionLogger", "string", " screen(s)) ===", "ขนาดเมนู", "games/my_anime_mine/auto_research", "[Config] couldn't read ", "autoResearchInterval", "⚙", "Plots", ": ", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "core/dispatch", "Auto Mine: คลิกต่อรอบ", "none", "=locked(", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "function", "Frame", "'", "AutoPlot", "[Bb]uy", " -> x", "games/my_anime_mine/auto_mine", "table", "core/get_me", "Auto Claim: รอบเช็ค", "games/my_anime_mine/auto_chest", "GetCharacterUpgradeCost", "RemoteFunction", "search_placeholder", "[AutoPlot] unlocked ", "core/get_leaderstat", "All", " ", "Anti-AFK", "[Interaction] ClickDetector clicked: ", "AutoUpgrade", "[Decompile] === end ===", "autoPlaceInterval", "server didn't equip it", "write reported success but ", "TextLabel", "games/my_anime_mine/auto_plot", "core/logger", "getscriptbytecode", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", " refused - ", "autoUpgradePickaxeIndex", "MineEquipSlot", "Dump Visible GUI", "Auto Place: รอบดึงตัวเก่ง", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "prompt disabled", "[GameAPI] shapes: Hotbar[", "TextButton", "autoResearchCore", "games/my_anime_mine/auto_research", "games/my_anime_mine/get_current_plot", ")", "empty", " GetHolderDamage=", "UIListLayout", "button has no size on screen", "ย่อขยายเมนูทั้งชุด", "open_menu_hint", "Header", "[AutoPlace] ", "ยิง Equip Best ทุกกี่วินาที", ")", "Auto Plot: รอบเช็ค", "AutoClaim", "Auto Research (Plot)", "debug.getproto", "languageIndex", "MineRollMutation", "enabled", "[AutoResearch] Core ", "[GuiDump] === end (", "core/logger", ")", "", "Players", "core/get_place_id", " fields: ", "table", "; ", "Auto Mine: ความเร็ว", "core/logger", "MineBuyPickaxe", "yes", "", "%.3g", "[GameAPI] shapes: inventory entry ", ") ", "MekPro", "ScrollingFrame", "Main", "menuScale", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "core/click", "core/parse_money", "getgc", "Players", "Players", "", "autoResearchCoreLast", "[AutoPlace] Equip Best: hotbar's strongest ", "ไล่อัปเกรด miner ทุกกี่วินาที", "/", "[GameAPI] shapes: Holders.", "AutoResearch", "MekPro.log", "ClaimableTiers failed", " error: ", "ModuleScript", "\n", "", " ชิ้น", "core/dispatch", "[AutoPlace] unlocking ", "table", "enabled", "Zone buying is off", " pickaxe ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "AutoPlace", "[GameAPI] holder ", ") ", ", base ", "games/my_anime_mine/auto_upgrade", ") didn't take: ", ": ", "s", "Frame", "Limited", ") from ", "cost", "core/logger", "s", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Badge", "no isfile/readfile available to verify the write", "s", "'", "", "[AutoClaim] idle: ", " ", "CharMaxLevel", "[Decompile] === executor capabilities ===", " costs ", "[GuiDump] ", "[AutoPlace] couldn't pick up ", "core/build_env", "core/get_place_id", "Auto Upgrade: รอบเช็ค", " failed)", "core/debug_dump_gui", ")", "Auto Upgrade: กันเงินไว้", "core/humanoid", "AutoUpgrade", "", "autoResearchCheapestFirst", ": ", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "core/ui", "[AutoPlace] fireproximityprompt error: ", "autoChestInterval", "ZoneRow", "autoMineClicks", "off", "[Remotes] === total: ", "UIScale", "[AutoPlace] placed ", ")", " waited ", "[AutoClaim] ", "%.3g", "🏠", ", built ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "=", "core/spend_policy", "MineSpeed", " possible holder(s) - not buying more", "autoPlaceEquipBestInterval", "", "research", "noclip", ") on ", "MineSpeed", " from ", "s", "[AutoSell] MineSellItems ", "[Interaction] Key pressed: %s (gameProcessed=%s)", "lowest", "PlayerGui", "MekPro", " items (now ", "games/my_anime_mine/get_plot_parts", "Text", "Core", "Hauler", "table", "table", " for ", "dev", "MekPro", "(nothing to do)", "MekPro/decompiled", "games/my_anime_mine/auto_chest", "en", " (", "getloadedmodules", "neither appendfile nor writefile is available", "MinePower", "table", "games/my_anime_mine/game_api", " (", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "PlayerGui", "Auto Upgrade: Pickaxe ที่จะซื้อ", "autoUpgradeRollMutation", "BasePart", "table", "games/my_anime_mine/auto_sell", "] = ", "games/my_anime_mine/auto_claim", "enabled", "[AutoUpgrade] ", "core/run_job", "core/run_job", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "games/my_anime_mine/auto_place", " - ", "ReplicatedStorage", " costs ", "Speed", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "TextButton", "LocalScript", " power ", "AutoClaim", "AutoResearch", ")", " (power ", "MineEquipBest", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Workspace", " research is ", "AutoPlot", "Workspace", "Auto Place: เพดานราคาปลดช่อง", "[Decompile] === start ===", "table", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "disabled", "Shop Stand", "ตามเครื่อง", ") needs ", "MineSpeed", "sold ", "Run", "AutoPlot", "sale didn't go through", "=empty", "[AutoChest] collected ", "table", "แพงสุดก่อน", "disabled", "games/my_anime_mine/auto_upgrade", "core/logger", " (power ", "decompile", "Auto Sell: รอบขาย", "", " still locked after paying ", "[AutoPlot] ", "dc8a1d7+", "AutoUpgrade", "maxed", ")", "ProximityPrompt", "[AutoPlace] picked up ", " GetHolderLevel=", "core/config", "", " not bought yet (", "autoSellProtectUnits", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "games/registry", "Speed ก่อน", "cheapest open node ", "core/spend_policy", " (money ", "string", "affordable - waiting for a turn", "core/debug_dump_remotes", "Auto Mine", "games/my_anime_mine/auto_plot", "มุดแมพ (Noclip)", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "games/my_anime_mine/get_best_miner", "", "auto", ", you have ", " node(s) looked buyable but the server refused", "autoUpgradePriority", "autoUpgrade", "Players", "TextLabel", "nothing to place or upgrade", " #", "upgrade", ") -> ", " but no unclaimed reward in its Track", "Damage", " is no better after the swap (", "AutoPlot", "s", "/", "🐛", ") ", "[AutoPlot] now mining ", "games/my_anime_mine/game_api", "RunService", "core/run_job", "getconnections", "Auto Upgrade Plot (Zone)", "Workspace", "SellStand", "table", "autoPlaceUpgradeWeak", " ", "^x(%d+)$", "TabBar", "en", "works from anywhere - no teleport needed", "ดีบัก", " files", "AutoChest", "ZoneBoardGui", "ขายทุกกี่วินาที", "UserInputService", "en", ": State(", "s", " items", "TextButton", "Auto Upgrade: สุ่ม Mutation", "waiting", "Humanoid", "[AutoResearch] cheapest open node: ", "Exclusive", "Damage", "table", "autoUpgradeMaxPerPass", "getsenv", "s behind a purchase that never happened - letting it spend", "[AutoResearch] nothing open and affordable right now", "All", "core/logger", "Mutation: หยุดเมื่อได้ x", "Auto Chest", "Badge", "th", "Auto Upgrade: แจก Pickaxe ให้ miner", "core/logger", "AutoPlace", "core/ui", "BasePart", " (ActionText=", "M", "autoClaimMaxPerPass", "x", "Empty miner slot", "en-us", "games/my_anime_mine/game_api", "core/logger", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "Text", "%.3g", "TextLabel", " pickaxe(s) for ", " (x", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "ChestCapacity", " (x", "Container", "[Decompile] ", " '", "", "ItemName", "  ", "/", "AutoPlace", "getscripts", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "string", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "x", ": ", "costs ", "[AutoPlace] disabled", "buying the ", "VirtualUser", "autoUpgradeMutationBudget", "autoSell", "'", " - upgrade the miners first", "%.0f", "[AutoSell] enabled", "autoUpgradePickaxe", " didn't go through (out of stock or too expensive)", " -> ", "[Logger] failed to write ", "LayerCollector", "Players", "core/teleport", "[MekPro] settings file: ", "[AutoChest] MineTakeChest had no effect (", "Auto Research: ซื้อสาย Zone", " mutation x", "string", "th", "/", "[AutoUpgrade] pickaxe #", "TextButton", "กันไม่ให้ใช้เงินหมดในรอบเดียว", ", have ", "dearest", "[Interaction] logger enabled - go click/interact with things now", "ClickDetector", "core/teleport", "Players", "autoUpgradeEquipPickaxe", " thing available first", "AutoChest", "table", "/", " (still ", "[%X] ", "", "Interaction Logger", "ค้นหาฟังก์ชัน...", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "", " (cost ", "TextLabel", "auto", " = ", "dearest", "TextButton", "CharMineSpeedMaxLevel", " doesn't exist", "autoSellInterval", " x", "อ่านป้ายบน chest ทุกกี่วินาที", " items)", "unreadable", "Speed", "x", "getnilinstances", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "off", "games/my_anime_mine/auto_claim", "Frame", "Search", "/s", "slot past the usable cap", "ไทย", "MineUpgradeCharacter", "[Interaction] ProximityPrompt triggered: ", "HumanoidRootPart", " | ", "no mine data", "core/debug_tab", "lowest", "[", " (", "", "[Scheduler] turn: ", "UICorner", "RemoteEvent", "autoResearch", "%$", "ProximityPrompt", "BodyPosition", "[GameAPI] shapes: Hotbar is empty", "WalkSpeed (ปกติ 16)", "Players", "[AutoPlot] couldn't switch to ", "TextLabel", "float", ".lua", "[Remotes] ", "AutoMine", "TextLabel", "0 = ไม่จำกัด", "core/build_env", "getrenv", "ฟาร์ม", "disabled", "[AutoMine] ", "Auto Plot: ใช้โซนล่าสุดเสมอ", "Auto Place Miner", "This is not a supported place.", ")", "fly", "  ", "[Uu]nlock", "[Decompile] wrote ", "autoPlotInterval", ")", "autoUpgradeMutationTarget", "core/logger", "core/debug_tab", "Auto Sell", " ", "ปุ่มเปิดเมนู", "core/debug_dump_remotes", "core/scheduler", "[GuiDump] === visible PlayerGui tree ===", "core/run_job", "autoPlotBuy", "Frame", "}", "core/run_job", "[AutoUpgrade] ", "[Scheduler] ", "", "Auto Place: ปลดล็อกช่อง miner", "GetHolderLevel", "/", "ลำดับการใช้เงินรวมทุกระบบ", " '", "HumanoidRootPart", "TextLabel", "function", "/", "\n", "[AutoResearch] ", " (Text=", "[AutoPlace] holders: ", "both Zone and Core buying are switched off", "ItemFrame", "Dump", "[MekPro] build ", "none", "autoClaimInterval", "[Decompile] decompile unavailable in this executor", "action '", "ScreenGui", ") gave nothing", "TextLabel", "AutoResearch", "number", " has been busy for ", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "[Interaction] logger disabled", "AutoPlot", " owned)", " ParseCharStack=", "GuiObject", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "[AutoUpgrade] bought pickaxe #", "autoChest", ")", " ", " (", "[AutoUpgrade] every stat on every miner is maxed", "M", "games/my_anime_mine/game_api", "autoUpgradeLimitPickaxes", "", "Workspace", "core/run_job", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "slot not in the hotbar", ")", "core/logger", "[Scheduler] ", "autoPlace", "%.3g", "core/logger", "GuiButton", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "UIStroke", "[AutoUpgrade] ", "autoPlotKeepMoney", "[AutoPlace] ", "string", "CoreUI", "off", "[AutoPlace] couldn't hold anything - not pressing E", "MineEquipPickaxe", "แรงกระโดด", "Auto Research: รอบเช็ค", "slot ", "[^%.]+", "Pad", "ProximityPrompt", "Frame", "table", "=", "games/my_anime_mine/main", " (prompt says '", "[AutoSell] disabled", " ", "Content", "TextLabel", "[Scheduler] ", "UIListLayout", "", "core/i18n", "[GameAPI] couldn't load the game's modules: ", "?", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "th", "M", "Auto Place: เปลี่ยนตัวอ่อนออก", "core/logger", "core/anti_afk", "[AutoPlace] ", ": ", "research", "auto", "search_placeholder", "UserInputService", "fill", "core/teleport", "=", "-", "OwnerUserId", "[AutoUpgrade] ", "Dump", "TextLabel", "s", "games/my_anime_mine/game_api", "BasePart", "menuScale", "[AutoClaim] claimed ", ": badge says ", "[AutoResearch] ", "[AutoChest] waiting: ", " ", "Frame", "off - every automation spends on its own", "HotbarFrame", "My Anime Mine · Mining Hub", "Mining ก่อน", "core/parse_money", "[AutoPlace] unlocked ", "[GuiDump] PlayerGui not found", "open_menu_hint", "unlock", " (", "Character", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "Players", "%.3g", "string", "table", " (", " (chest holds ", "Damage", "dearest", " team power, you have ", "needs ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "ValueLabel", "core/logger", "Auto Upgrade Unit", "Nodes", "List", "2026-09-21 13:09:09", "Frame", "ลอย (Float)", "[AutoPlot] ", "{", "JumpPower (ปกติ 50)", "Players", "autoClaim", "core/debug_decompile", "dearest", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "UICorner", "Players", "M", " -> ", "MineBuySkill", "อังกฤษ", "[AutoChest] chest is empty", "RunService", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "games/my_anime_mine/get_current_plot", "[AutoPlot] ", "[AutoPlace] nothing in the hotbar to place", "[AutoPlace] drifted out of range of ", " stack(s)", " team power", ")", "TextButton", " ", "core/spend_policy", " wouldn't take ", " -> ", "^M(%d+)$", "TextButton", "dearest", "games/my_anime_mine/auto_sell", ")", "antiAfk", "AutoSell", "unreadable", "games/registry", "autoChestMinItems", "th", "Locked miner slot", "#", "UserInputService", "core/i18n", ",", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "Workspace", "Mutation: เพดานราคาต่อครั้ง", " (", "Players", ")", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "TextButton", "works from anywhere - dropping the teleport", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Humanoid", "TextLabel", "TextLabel", "Auto Chest: รอบเช็ค", "%$%s*([%d,%.]+)%s*(%a*)", "Character", "", " (", "games/my_anime_mine/get_plot_parts", "[Interaction] Button activated: ", "%.0f", "debug.getconstants", "function", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "Loot", "Frame", "ProximityPrompt", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "", "Players", "Auto Plot: กันเงินไว้", "s", "core/scheduler", "[^%w]+", ")", "^%$", "", "", "core/logger", "HttpService", "s", "table", "core/debug_interaction_logger", "locked slot", "[AutoClaim] ", "PickaxesStand", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "core/logger", "Players", " -> ", "games/my_anime_mine/get_current_plot", " -> ", "", "เดินทะลุกำแพงและพื้น", " while selecting", "[AntiAFK] disabled", "([%d,%.]+)%s*(%a*)", "Decompile Client Scripts", "[AutoResearch] ", "MekPro", "ReplicatedStorage", "Auto Chest: เกณฑ์เก็บ", "s - jumping the queue", "autoResearchKeepMoney", "core/teleport", "Auto Place: รอบเช็ค", " ===", "[AutoChest] disabled", "Content", "Frame", "nothing claimable", "[BestMiner] slot ", " script(s) to ", "core/logger", " (", " for ", "[AutoChest] can't read the chest yet", "PlayerScripts", "Search functions...", ")", "0 = ไม่จำกัด", "TextLabel", "%.0f", "[GameAPI] shapes:   GetHolderVariant=", "/config.json", "Auto Research: ซื้อสาย Core", "PlotIndex", "MineWorld", "autoMineSpeed", "languageIndex", " ", "[AutoPlace] slot ", "every open node is maxed", "write reported success but the line isn't in ", "[AutoUpgrade] MineBuyPickaxe ", "[AutoResearch] idle: ", "core/logger", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", " GetVariantMult=", "needs ", "GuiService", " screens", "needs us at the stand - keeping the teleport", "[AutoChest] enabled (collects once >= ", "autoSellMaxWait", "core/logger", "auto", "autoResearchZones", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "BodyVelocity", "empty", "[Decompile] PlayerScripts not found", "Auto Claim (Index)", "Auto Research: กันเงินไว้", "core/parse_money", "hookfunction", "Auto Sell: รอคิวได้นานสุด", "[GameAPI] shapes: no Character entries in the inventory", " (Text=", "Frame", "[GameAPI] SendEvent failed: ", "unlock", "disabled", "may need us at the stand - trying that", " reward(s) looked claimable but none went through", "Chest", "core/i18n", " (", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "core/config", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "VirtualInputManager", "fill", "Backpack", "TextBox", "%.3g", "Crew", "autoUpgradePickaxeIndex", "core/run_job", "MineSelectZone", "ValueLabel", "CharSpeedMaxLevel", "Auto Claim: เก็บสูงสุดต่อรอบ", "ตั้งค่า", "Modules", " -> ", "ไล่ซื้อทุกกี่วินาที", "autoPlaceUnlockBudget", "core/logger", "แพงสุดก่อน", "core/debug_interaction_logger", "OwnerUserId", "[AutoUpgrade] ", "BasePart", "x", "ถูกสุดก่อน", "(", " -> ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "enabled", "บิน (Fly)", "%.3g", "Hauler", "autoPlot", "ต่างคนต่างซื้อ", "AutoSell", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "TextLabel", "AutoPlace", "autoPlotLatest", "dearest", "games/my_anime_mine/get_plot_parts", "enabled", "AutoPlace", "[AutoPlace] ", "core/logger", ") ", ") - leaving it alone for ", "MineWorld", "leaderstats", "", "cheapest", "ClickDetector", "nothing left to buy (", "[AutoResearch] ", " ", "%", "x", "games/my_anime_mine/game_api", "Content", "games/my_anime_mine/get_best_miner", "autoPlaceUnlock", "ถูกสุดก่อน", "Open menu", "Core buying is off", "ClickDetector", "autoSellMaxWait", " ", "ต่ำสุดก่อน", "core/debug_dump_gui", "TextLabel", "[SpendPolicy] ", "no", "GetCharacterSpeedUpgradeCost", "Mining", "core/anti_afk", "M", "core/get_place_id", " claimable but none could be resolved", "core/spend_policy", "[AntiAFK] enabled - watching Player.Idled", "[SpendPolicy] ", " ", "", "[AntiAFK] idle detected, simulating input", "] job failed: ", "off", " ", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Root", "core/spend_policy", "core/run_job", "", "jumpPower", "AutoUpgrade", "UIListLayout", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "autoPlaceEquipBest", "Auto Plot: ซื้อโซนถัดไป", "GuiButton", "modules not loaded", " (", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", " still empty after placing ", "table", "Holders", "autoUpgradeInterval", "Frame", "PlayerGui", "spendOrder", "GetCharacterMineSpeedUpgradeCost", "index rewards are disabled in this game build", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", ")", "table", "HumanoidRootPart", "cheapest", "autoUpgradeKeepMoney", "MineTakeChest", "badge says ", "debug.getupvalues", " waited ", "^([%d,%.]+)%s*(%a*)$", "disabled", "", "[GameAPI] shapes: no mine data yet", "ความเร็ววิ่ง", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "cost", "AutoPlace", "AutoUpgrade", "getcallbackvalue", "", "s", "')", "_", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Zones", "GuiService", "[Config] couldn't write ", "/", "Auto Research: ซื้อสูงสุดต่อรอบ", "table", ")", "", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "AutoResearch", "s - carrying on without it", ", ", "Plots", "GetHolderMineSpeedLevel", "UIPadding", "MineGui", "TextLabel", "autoResearchMaxPerPass", "", " (", "waiting", "games/my_anime_mine/main", " remotes", ": ", "core/humanoid", "[AutoPlace] upgrading ", "table", "MineSellItems", " ", "TextButton", "core/logger", "games/my_anime_mine/game_api", "games/my_anime_mine/game_api", "^Swap out ", "games/my_anime_mine/auto_place", "games/my_anime_mine/auto_mine", "X", "autoMine", ",", "core/spend_policy", "games/my_anime_mine/game_api", "CoreGui", "%$", "ZoneBoard"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(948)] = function()
return function(import)
local log = import(S(94))
local Players = game:GetService(S(707))
local VirtualUser = game:GetService(S(423))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(953))
        connection = player.Idled:Connect(function()
            log(S(957))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(798))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(169)] = function()
return { Env = S(223), IsDev = true, Commit = S(296), BuiltAt = S(695) }
end
modules[S(116)] = function()
local VirtualInputManager = game:GetService(S(874))
local GuiService = game:GetService(S(843))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(78)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(303)] = function()
return function(import)
local HttpService = game:GetService(S(782))
local getPlaceId = import(S(98))
local log = import(S(891))
local ROOT_FOLDER = S(224)
local placeFolder = ROOT_FOLDER .. S(457) .. tostring(getPlaceId())
local filePath = placeFolder .. S(827)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(784) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(21) .. filePath .. S(1033) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(1012) .. filePath .. S(643) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(5)] = function()
return function(import)
local Players = game:GetService(S(772))
local log = import(S(790))
local CAPABILITIES = {
    S(291), S(60), S(414), S(118), S(230),
    S(345), S(1004), S(375), S(518), S(764),
    S(993), S(88), S(858), S(480),
}
local OUT_FOLDER = S(226)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(618)) do
        if type(target) ~= S(241) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(165))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(407) .. name .. S(9) .. ((ok and type(value) == S(32)) and S(105) or S(945)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(568))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(820))
    if not playerScripts then
        log(S(854))
        return 0
    end
    pcall(makefolder, S(802))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(257)) or target:IsA(S(131)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(441) and #source > 0 then
                local name = target:GetFullName():gsub(S(776), S(1008))
                if pcall(writefile, OUT_FOLDER .. S(443) .. name .. S(512), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(529) .. written .. S(815) .. OUT_FOLDER
        .. S(870) .. failed .. S(172))
    return written
end
local function run()
    log(S(270))
    reportCapabilities()
    local count = dumpAll()
    log(S(53))
    return count
end
return run
end
end
modules[S(173)] = function()
return function(import)
local Players = game:GetService(S(97))
local log = import(S(103))
local function dumpVisible(root, indent)
    indent = indent or S(759)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(582)) and child.Visible then
            local line = indent .. child.ClassName .. S(408) .. child.Name .. S(426)
            if child:IsA(S(754)) or child:IsA(S(70)) then
                line = line .. S(560) .. tostring(child.Text) .. S(721)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(4) .. attrName .. S(468) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(527))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(237))
    if not playerGui then
        log(S(672))
        return 0
    end
    local screens = 0
    log(S(540))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(434)) and screen.Enabled and screen.Name ~= S(110) then
            screens = screens + 1
            log(S(167) .. screen.ClassName .. S(553) .. screen.Name .. S(160))
            dumpVisible(screen, S(411))
        end
    end
    log(S(93) .. screens .. S(18))
    return screens
end
return dumpGui
end
end
modules[S(538)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(252))
local log = import(S(816))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(501)) or descendant:IsA(S(44)) then
            count = count + 1
            log(S(513) .. descendant.ClassName .. S(419) .. descendant:GetFullName())
        end
    end
    log(S(188) .. count .. S(809))
    return count
end
return dumpRemotes
end
end
modules[S(893)] = function()
return function(import)
local Players = game:GetService(S(679))
local Workspace = game:GetService(S(347))
local UserInputService = game:GetService(S(361))
local log = import(S(289))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(490) .. describe(prompt)
            .. S(389) .. prompt.ActionText .. S(95))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(51) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(762) .. describe(button)
            .. S(861) .. tostring(button.Text) .. S(598))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(620)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(450)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(972)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(578))
        return
    end
    connections = {}
    log(S(449))
    local playerGui = Players.LocalPlayer:WaitForChild(S(981))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(769)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(938)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(604)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(211),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(534)] = function()
return function(import)
local interactionLogger = import(S(785))(import)
local dumpRemotes = import(S(315))(import)
local dumpGui = import(S(942))(import)
local decompileScripts = import(S(703))(import)
local humanoid = import(S(1034))
local function build(window, persist)
    local tab = window:AddTab(S(356), S(339))
    persist.Toggle(
        tab:AddCard(
            S(461),
            S(273)
        ),
        S(16),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(1),
        S(181)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(654), function()
        remoteBtn.Text = dumpRemotes() .. S(1032)
    end)
    local guiCard = tab:AddCard(
        S(65),
        S(31)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(564), function()
        guiBtn.Text = dumpGui() .. S(844)
    end)
    local decompileCard = tab:AddCard(
        S(800),
        S(752)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(280), function()
        decompileBtn.Text = decompileScripts() .. S(357)
    end)
    humanoid.Start()
    persist.Slider(tab, S(999), S(507),
        8, 200, S(14), 16, humanoid.SetWalkSpeed, S(997))
    persist.Slider(tab, S(615), S(700),
        30, 300, S(966), 50, humanoid.SetJumpPower, S(632))
    persist.Toggle(
        tab:AddCard(S(903), S(714)),
        S(526), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(318), S(796)),
        S(205), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(697), S(840)),
        S(511), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(27)] = function()
return function(import)
    local getPlaceId = import(S(170))
    local registry = import(S(308))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(524))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(47)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(922))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(40)] = function()
local Players = game:GetService(S(701))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(753))
    local backpack = player:WaitForChild(S(876))
    return player, humanoid, backpack
end
return getMe
end
modules[S(950)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(176)] = function()
local Players = game:GetService(S(119))
local RunService = game:GetService(S(343))
local UserInputService = game:GetService(S(647))
local Workspace = game:GetService(S(594))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(369))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(554))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(852))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(240)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(658)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(505))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(741)] = function()
local Players = game:GetService(S(120))
local STRINGS = {
    en = {
        search_placeholder = S(821),
        open_menu_hint = S(936),
    },
    th = {
        search_placeholder = S(462),
        open_menu_hint = S(537),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(394)
    if locale:sub(1, 2):lower() == S(383) then
        return S(637)
    end
    return S(362)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(322) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(781)] = function()
local LOG_FILE = S(128)
local function log(message)
    local line = os.date(S(459)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(121))
            end
            appendfile(LOG_FILE, line .. S(132))
        elseif writefile then
            local existing = S(321)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(558))
        else
            error(S(231))
        end
        if not (isfile and readfile) then
            error(S(158))
        end
        if not isfile(LOG_FILE) then
            error(S(56) .. LOG_FILE .. S(472))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(836) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(433) .. LOG_FILE .. S(148) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(670)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(313) then
        return nil
    end
    local number, suffix = text:match(S(757))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(742), S(1028))))
    if not value then
        return nil
    end
    if suffix ~= S(304) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(545)] = function()
return function(import)
local log = import(S(154))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(496) .. name .. S(958) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(539)] = function()
return function(import)
local RunService = game:GetService(S(713))
local log = import(S(599))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(547) .. task_.name .. S(130) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(630) .. task_.name .. S(575)
                    .. string.format(S(825), now - task_.busySince)
                    .. S(1020))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(600) .. starved.name .. S(192)
            .. string.format(S(763), now - starved.waitingSince)
            .. S(805))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(499) .. (currentTask and currentTask.name or S(225)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(963)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(603))
local policy = {}
local probes = {}
local order = {}
local mode = S(187)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(954) .. (mode == S(482)
        and S(666)
        or (S(422) .. mode .. S(454))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(959) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(924) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(448) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(944) .. name .. S(994)
            .. string.format(S(428), now - blockedSince[name])
            .. S(376))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(436)] = function()
local Players = game:GetService(S(747))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(988))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(182)] = function()
return function(import)
local i18n = import(S(633))
local UserInputService = game:GetService(S(740))
local GuiService = game:GetService(S(1011))
local THEME = {
    BrandName = S(214),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(706))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(500))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(1024))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(728))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(8)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(606))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(515))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(635)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(510))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(956)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(111))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(77))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(555))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(862))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(824))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(755))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(10))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(883))
            if not valueLbl then
                valueLbl = Instance.new(S(910))
                valueLbl.Name = S(690)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(157))
            if not badge then
                badge = Instance.new(S(655))
                badge.Name = S(382)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(1039))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(665))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(543))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(256))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(779)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(929)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(980))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(812))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(722))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(498)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(150))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(631))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(445))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(217)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(570))
    screenGui.Name = S(611)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(1051))
    local main = Instance.new(S(621))
    main.Name = S(112)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(189))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(484))
    tabBar.Name = S(353)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(968))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(33))
    header.Name = S(81)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(366))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(943))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(57))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(780)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(470))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(1046)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(750))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(651)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(877))
    searchBox.Name = S(485)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(646))
    searchBox.Text = S(593)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(768))
    content.Name = S(811)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(696))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(629))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(673))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(466))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(398)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(178) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(351) .. (item.Subtitle or S(464))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(45))
        footerLabel.Text = i18n.T(S(80))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(42)] = function()
return function(import)
local gameApi = import(S(1050))(import)
local log = import(S(839))
local runGuarded = import(S(248))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(846) .. minItemsToCollect .. S(476))
    else
        log(S(810))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(477) then
            log(S(819))
            lastReported = S(734)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(75) then
            log(S(712))
            lastReported = S(853)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(1030) then
            local capacity = gameApi.ChestCapacity()
            log(S(663) .. count .. S(551) .. want .. S(365)
                .. (capacity and (S(684) .. capacity .. S(525)) or S(548)))
            lastReported = S(368)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(455), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(284) .. count .. S(215) .. after .. S(531))
        else
            log(S(438) .. count
                .. S(792) .. tostring(after) .. S(74))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(244)] = function()
return function(import)
local gameApi = import(S(342))(import)
local log = import(S(640))
local runGuarded = import(S(964))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(787) .. (enabled and S(902) or S(520)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(162) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(660) .. reward.category .. S(1038)
                .. tostring(reward.tierKey) .. S(330) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(193) .. #pending .. S(867))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(259), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(38)] = function()
return function(import)
local getCurrentPlot = import(S(73))
local getPlotParts = import(S(761))
local log = import(S(396))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(925), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(521) .. (enabled and S(138) or S(865)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(1044)] = function()
return function(import)
local Players = game:GetService(S(435))
local getCurrentPlot = import(S(793))
local getPlotParts = import(S(914))
local getMinerSlots = import(S(933))(import)
local teleport = import(S(649))
local parseMoney = import(S(117))
local gameApi = import(S(577))(import)
local log = import(S(848))
local runGuarded = import(S(247))(import)
local spendPolicy = import(S(724))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(393)
local LOCKED_SLOT_TEXT = S(738)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(619))
    return pad and pad:FindFirstChildWhichIsA(S(504), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(1026)) then
            local digits, suffix = descendant.Text:match(S(995))
            if digits then
                local value = tonumber((digits:gsub(S(7), S(133))))
                if value then
                    if suffix ~= S(293) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(460)
    return (text:gsub(S(1043), S(203)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(572)) and descendant.Text:match(S(778)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(617) .. holder.Name .. S(305)
            .. gameApi.MinerSlots() .. S(338) .. gameApi.MaxMinerSlots() .. S(580)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(786)
    end
    if not prompt.Enabled then
        return true, S(68)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(420) .. price
    end
    local action = prompt.ActionText or S(771)
    if action:match(S(528)) or action:match(S(36)) or action:match(S(503)) then
        return true, S(569) .. action .. S(34)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(491))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(183) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(30) .. why .. S(299))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(283))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(623) .. prompt.ObjectText
                    .. S(899) .. shown .. S(145) .. math.floor(base) .. S(586))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(492))
    if line ~= lastSurvey then
        log(S(561) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(648), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(331), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(2)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(674), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(642) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(421))
    else
        lastAttempt = 0
        log(S(249))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(789),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(597)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(487)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(55)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(717))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(718) .. holder.Name .. S(797))
            return false
        end
        log(S(834) .. candidate.slot .. S(235) .. candidate.name
            .. S(147) .. tostring(why))
    end
    if not miner then
        log(S(613))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(609) .. holder.Name .. S(976) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(190) .. miner.name .. S(262) .. miner.power
        .. S(206) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(168) .. entry.name .. S(208) .. entry.holder.Name
            .. S(625) .. tostring(entry.prompt.ActionText) .. S(1007))
        return false
    end
    log(S(301) .. entry.name .. S(290) .. entry.power
        .. S(152) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(917) .. pad.holder.Name .. S(294)
                .. tostring(pad.priceText))
        else
            log(S(671) .. pad.holder.Name
                .. S(222) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(82) .. holderName .. S(335)
            .. string.format(S(194), before) .. S(709) .. string.format(S(399), after)
            .. S(920) .. SWAP_COOLDOWN .. S(155))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(123)
            .. string.format(S(602), bestBefore) .. S(900)
            .. string.format(S(878), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(1002), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(329))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(875) then
        runGuarded(S(386), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(864) then
        log(S(136) .. target.holder.Name
            .. S(818) .. tostring(target.priceText))
        runGuarded(S(413), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(1035) .. target.holder.Name .. S(180) .. target.name
        .. S(675) .. target.power .. S(332) .. best.name .. S(746) .. best.power .. S(731))
    runGuarded(S(911), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(58)] = function()
return function(import)
local gameApi = import(S(657))(import)
local parseMoney = import(S(857))
local log = import(S(533))
local runGuarded = import(S(881))(import)
local spendPolicy = import(S(311))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(295) .. (enabled and S(245) or S(287)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(1052)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(341) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(817) .. deepestKey .. S(174))
        return true
    end
    log(S(509) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(35)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(698) .. gameApi.ZoneDisplayName(zoneKey) .. S(166)
                .. math.floor(price) .. S(323) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(46) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(229) .. zoneKey .. S(1016))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(644) then
            reportedLocked = S(204)
            log(S(716) .. zoneKey .. S(62)
                .. gameApi.ZoneDisplayName(currentKey) .. S(266)
                .. done .. S(557) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(336)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(579), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(20)] = function()
return function(import)
local gameApi = import(S(395))(import)
local log = import(S(691))
local runGuarded = import(S(595))(import)
local spendPolicy = import(S(1049))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(559) .. (enabled and S(91) or S(274)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(927) .. node.zoneKey .. S(536) .. node.id
                    .. S(683) .. node.family .. S(144) .. node.level .. S(432) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(92) .. node.id
                    .. S(588) .. node.family .. S(919) .. node.level .. S(888) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(662) .. blocked
                .. S(324))
        elseif powerBlocked then
            log(S(801) .. powerBlocked.zoneKey .. S(833) .. powerBlocked.id
                .. S(497) .. powerBlocked.family .. S(277)
                .. math.floor(powerBlocked.powerReq) .. S(687)
                .. math.floor(powerBlocked.power)
                .. S(427))
        else
            log(S(377)
                .. S(312) .. math.floor(gameApi.GetMoney()) .. S(986))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(370) .. nearest.zoneKey
                    .. S(955) .. nearest.id .. S(760) .. nearest.family .. S(340)
                    .. nearest.level .. S(125) .. nearest.max
                    .. S(253) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(562)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(218),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(139))
        end
        if not buyCore then
            table.insert(parts, S(937))
        end
        if #parts == 0 then
            return S(835)
        end
        return S(926) .. table.concat(parts, S(1021)) .. S(261)
    end
    local why
    if nearest.cost > budget then
        why = S(688) .. string.format(S(680), nearest.cost)
            .. S(447) .. string.format(S(904), money)
    elseif nearest.powerOk == false then
        why = S(842) .. string.format(S(107), nearest.powerReq) .. S(720)
    else
        why = S(314)
    end
    return S(310) .. nearest.zoneKey .. S(960) .. tostring(nearest.id)
        .. S(974) .. tostring(nearest.family) .. S(109) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(838) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(729) and cost > best)
            or (mode ~= S(469) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(127)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(573), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(730)] = function()
return function(import)
local Workspace = game:GetService(S(744))
local gameApi = import(S(234))(import)
local teleport = import(S(807))
local log = import(S(59))
local runGuarded = import(S(344))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(626))
    else
        lastAttempt = 0
        log(S(429))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(348))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(300), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(210)
                .. (sold and S(751)
                         or S(845)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(3) .. (sold and (S(279) .. #keys .. S(719)) or S(282)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(908), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(146)] = function()
return function(import)
local Workspace = game:GetService(S(268))
local gameApi = import(S(931))(import)
local teleport = import(S(451))
local log = import(S(918))
local runGuarded = import(S(541))(import)
local spendPolicy = import(S(199))(import)
local STATS = { S(334), S(254), S(200) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(495)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(653) .. (enabled and S(915) or S(996)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(1015) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(1001) or S(298)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(546) .. holderKey .. S(163) .. choice.stat .. S(664)
                        .. choice.info.level .. S(726) .. after.level
                        .. S(1013) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(153) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(788))
    local shop = stand and stand:FindFirstChild(S(275))
    if not shop then
        return nil
    end
    if shop:IsA(S(388)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(896), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(837)
                .. (bought and S(355)
                           or S(866)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(909))
        end
    end
    if bought then
        log(S(584) .. pickaxe.index .. S(928) .. pickaxe.name
            .. S(402) .. pickaxe.multiplier .. S(84))
        return true
    end
    log(S(444) .. pickaxe.index .. S(627) .. pickaxe.name
        .. S(431))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(246) .. holderKey .. S(140)
                    .. tostring(worn or S(566)) .. S(794) .. wanted.name)
            else
                log(S(895) .. holderKey .. S(725)
                    .. wanted.name .. S(458) .. tostring(nowWorn or S(29)) .. S(822))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(607) .. holderKey .. S(440) .. variant
                .. S(37) .. after .. S(465) .. cost .. S(777))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(297))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(576) .. gameApi.OwnedPickaxeCount()
                    .. S(401) .. gameApi.MaxMinerSlots()
                    .. S(201))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(67))
        elseif maxed > 0 then
            log(S(589))
        else
            log(S(901))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(686) and price > best)
            or (mode ~= S(704) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(1003))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(967), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(1041)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(803))
local Workspace = game:GetService(S(11))
local log = import(S(379))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(887), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(634) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(456))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(137) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(905) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(1036) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(220) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(219) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(727))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(863) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(991))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(378), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(373) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(100) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(676) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(1037), keys)
end
function api.EquipSlot(slot)
    return send(S(64), slot == S(409) and S(795) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(104), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(614), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(90), holderKey)
end
local describedShapes = false
function api.DescribeShapes()
    if describedShapes or not modules() then
        return
    end
    describedShapes = true
    local mine = api.GetMineData()
    if not mine then
        log(S(998))
        return
    end
    local function describe(value)
        if type(value) ~= S(977) then
            return tostring(value)
        end
        local parts = {}
        for key, item in pairs(value) do
            table.insert(parts, tostring(key) .. S(198) .. tostring(item))
        end
        table.sort(parts)
        return S(699) .. table.concat(parts, S(940)) .. S(544)
    end
    pcall(function()
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(39) and holder.CharName then
                log(S(126) .. holderKey .. S(49) .. describe(holder))
                log(S(826) ..
                    tostring(mineConfig.GetHolderVariant(holder))
                    .. S(841) ..
                    tostring(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
                    .. S(76) .. tostring(mineConfig.GetHolderDamage(holder))
                    .. S(302) .. tostring(mineConfig.GetHolderLevel(holder)))
                break
            end
        end
    end)
    pcall(function()
        local shown = 0
        for slot, key in pairs(mine.Hotbar or {}) do
            log(S(69) .. tostring(slot) .. S(243) .. tostring(key)
                .. S(581) .. tostring(mineConfig.ParseCharStack(key)))
            shown = shown + 1
            if shown >= 3 then
                break
            end
        end
        if shown == 0 then
            log(S(506))
        end
    end)
    pcall(function()
        local entries = mineInventory.Collect(mine, S(48), mineInventory.KindOrder.OreFirst)
        local shown = 0
        for _, entry in pairs(entries or {}) do
            if entry.Kind == S(758) then
                log(S(108) .. describe(entry))
                shown = shown + 1
                if shown >= 2 then
                    break
                end
            end
        end
        if shown == 0 then
            log(S(860))
        end
    end)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(263))
end
function api.BuySkill(zoneKey, family)
    return send(S(710), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(285)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(452)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(232))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(556) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(271) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(882), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(921))
    plots = plots and plots:FindFirstChild(S(24))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(791)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(652)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(1053))
    local gui = board and board:FindFirstChild(S(359))
    local root = gui and gui:FindFirstChild(S(962))
    local list = root and root:FindFirstChild(S(694))
    local row = list and list:FindFirstChild(S(185) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(400)) and descendant.Text ~= S(106) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(765) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(489), characterKey, stat == S(947) and S(278) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(550), max = S(164), cost = S(43) },
    Speed = { level = S(15), max = S(884), cost = S(946) },
    MineSpeed = { level = S(1023), max = S(471), cost = S(983) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(610) or charName == S(1005) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(650) .. tostring(value))
        end
        table.sort(fields)
        log(S(143) .. holderKey .. S(99) .. table.concat(fields, S(723)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(622) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(574) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(416) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(682) and type(holder.CharName) == S(17) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(151), S(371) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(349) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(973)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(493)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(984)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(221) then
        return {}, S(129)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(813)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(987) or type(state.Track) ~= S(233) then
                table.insert(notes, tabKey .. S(363) .. tostring(category) .. S(571))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(661) .. waiting
                        .. S(333))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(992) .. total .. S(951)
            .. (#notes > 0 and (S(251) .. table.concat(notes, S(101))) or S(923))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(681) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(320)] = function()
return function(import)
local Players = game:GetService(S(327))
local gameApi = import(S(591))(import)
local log = import(S(385))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(799))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(1048), S(96))))
    if not value then
        return nil
    end
    if suffix ~= S(161) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(213))
    local mineGui = playerGui and playerGui:FindFirstChild(S(1025))
    local hotbar = mineGui and mineGui:FindFirstChild(S(667))
    local content = hotbar and hotbar:FindFirstChild(S(932))
    return content and content:FindFirstChild(S(406))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(328)) then
            local count = descendant.Text:match(S(352))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(563))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(628))
    local nameLabel = content and content:FindFirstChild(S(410))
    local damageLabel = content and content:FindFirstChild(S(372))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(1017) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(412) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(814) .. slot .. S(25) .. info.name
                        .. S(258) .. info.power .. S(474) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(715)] = function()
local Players = game:GetService(S(508))
local Workspace = game:GetService(S(265))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(830))
    local plots = mineWorld and mineWorld:FindFirstChild(S(1022))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(894)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(216)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(978)),
        Nodes = plot:FindFirstChild(S(693)),
        Chest = plot:FindFirstChild(S(868)),
        Zones = plot:FindFirstChild(S(1010)),
        Crew = plot:FindFirstChild(S(879)),
        Loot = plot:FindFirstChild(S(767)),
        PlotIndex = plot:GetAttribute(S(829)),
        ChestCapacity = plot:GetAttribute(S(404)),
    }
end
return getPlotParts
end
modules[S(1031)] = function()
return function(import)
    local createWindow = import(S(387))(import)
    local i18n = import(S(869))
    local antiAfk = import(S(641))(import)
    local buildEnv = import(S(517))
    local scheduler = import(S(775))(import)
    local spendPolicy = import(S(952))(import)
    local config = import(S(872))(import)
    local log = import(S(1040))
    log(S(565) .. buildEnv.Commit .. S(1029) .. buildEnv.Env
        .. S(196) .. buildEnv.BuiltAt .. S(191))
    local window = createWindow({
        Subtitle = S(668),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(1045))(import)
    local autoPlace = import(S(250))(import)
    local autoChest = import(S(227))(import)
    local autoSell = import(S(242))(import)
    local autoUpgrade = import(S(288))(import)
    local autoResearch = import(S(72))(import)
    local autoClaim = import(S(483))(import)
    local autoPlot = import(S(317))(import)
    local gameApi = import(S(1042))(import)
    scheduler.RegisterStandalone(S(514), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(358), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(142), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(52), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(260), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(86), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(281), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(939), 20)
    scheduler.RegisterExclusive(S(733), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(1019), autoResearch.NextPrice)
    spendPolicy.Register(S(177), autoUpgrade.NextPrice)
    spendPolicy.Register(S(267), autoPlot.NextPrice)
    spendPolicy.Register(S(916), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(519), S(195))
    farmTab:AddSectionHeader(S(552))
    persistentSegmented(
        farmTab,
        { S(907), S(898), S(286) },
        { S(612), S(989), S(913) },
        S(982), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(316),
        S(985)
    ), S(1047), autoMine.SetEnabled)
    persistentSlider(farmTab, S(102), S(770),
        1, 30, S(831), 5, autoMine.SetCyclesPerSecond, S(486))
    persistentSlider(farmTab, S(28), S(1018),
        1, 10, S(186), 1, autoMine.SetClicksPerCycle, S(930))
    persistentToggle(farmTab:AddCard(
        S(523),
        S(403)
    ), S(601), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(808), S(319),
        1, 60, S(54), 3, autoPlace.SetCheckInterval, S(209))
    persistentToggle(
        farmTab:AddCard(S(639),
            S(272)),
        S(350), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(873),
            S(115)),
        S(970), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(66), S(83),
        5, 300, S(202), 60, autoPlace.SetEquipBestInterval, S(364))
    persistentToggle(
        farmTab:AddCard(S(549),
            S(26)),
        S(934), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(269), S(516),
        0, 500, S(890), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(590))
    persistentToggle(farmTab:AddCard(
        S(381),
        S(481)
    ), S(585), autoChest.SetEnabled)
    persistentSlider(farmTab, S(804), S(264),
        50, 3000, S(736), 100, autoChest.SetMinItems, S(134))
    persistentSlider(farmTab, S(756), S(475),
        1, 60, S(184), 3, autoChest.SetCheckInterval, S(149))
    persistentToggle(farmTab:AddCard(
        S(535),
        S(636)
    ), S(425), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(871),
            S(255)),
        S(306), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(292), S(360),
        5, 300, S(473), 20, autoSell.SetCheckInterval, S(159))
    farmTab:AddSlider(S(859),
        S(197),
        5, 120, sellMaxWait, function(value)
            config.Set(S(847), value)
        end, S(337))
    persistentToggle(farmTab:AddCard(
        S(692),
        S(689)
    ), S(326), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(171), S(124),
        5, 300, S(979), 20, autoUpgrade.SetInterval, S(1006))
    persistentSlider(farmTab, S(596), S(446),
        1, 20, S(374), 5, autoUpgrade.SetMaxPerPass, S(418))
    persistentSegmented(
        farmTab,
        { S(941), S(6), S(309), S(669) },
        { S(212), S(685), S(478), S(207) },
        S(325), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(175), S(463),
        0, 500, S(990), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(390))
    persistentToggle(
        farmTab:AddCard(S(961),
            S(141)),
        S(430), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(405) .. pickaxe.multiplier .. S(748)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(851)
            end
            return S(739) .. index .. S(587) .. (pickaxeLabels[index] or S(307))
        end
        local savedPick = config.Get(S(63), 0)
        pickaxeCard = farmTab:AddSlider(
            S(238),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(880), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(965)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(605),
            S(583)),
        S(592), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(384),
            S(397)),
        S(453), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(367),
            S(677)),
        S(239), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(380), S(415),
        1, 20, S(532), 2, autoUpgrade.SetMutationTarget, S(479))
    persistentSlider(farmTab, S(745), S(823),
        0, 500, S(424), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(949))
    persistentToggle(farmTab:AddCard(
        S(87),
        S(12)
    ), S(502), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(616), S(889),
        10, 600, S(22), 30, autoResearch.SetCheckInterval, S(774))
    persistentSlider(farmTab, S(1014), S(749),
        1, 20, S(1027), 5, autoResearch.SetMaxPerPass, S(392))
    persistentSlider(farmTab, S(856), S(1000),
        0, 500, S(806), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(708))
    persistentToggle(
        farmTab:AddCard(S(439),
            S(975)),
        S(850), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(828),
            S(705)),
        S(71), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(743),
            S(969)),
        S(122), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(935), S(892) },
        { true, false },
        S(179), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(855),
        S(156)
    ), S(702), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(41), S(678),
        5, 300, S(567), 30, autoClaim.SetCheckInterval, S(783))
    persistentSlider(farmTab, S(885), S(236),
        1, 30, S(391), 10, autoClaim.SetMaxPerPass, S(897))
    persistentToggle(farmTab:AddCard(
        S(346),
        S(766)
    ), S(906), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(85), S(13),
        5, 300, S(530), 30, autoPlot.SetCheckInterval, S(656))
    persistentToggle(
        farmTab:AddCard(S(971),
            S(61)),
        S(542), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(522),
            S(114)),
        S(912), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(773), S(417),
        0, 500, S(608), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(638))
    if buildEnv.IsDev then
        task.delay(8, gameApi.DescribeShapes)
    end
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(494))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(886), S(23))
    persistentToggle(
        settingsTab:AddCard(S(50), S(1009)),
        S(732),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(659), 100)
    settingsTab:AddSlider(S(19), S(79), 50, 150, savedScale, function(value)
        config.Set(S(113), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(832), 1)
    settingsTab:AddSegmented({ S(276), S(711), S(488) }, savedLanguage, function(index, value)
        local code = ({ S(849), S(228), S(442) })[index]
        config.Set(S(89), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(645), S(354), S(737) })[savedLanguage] or S(467))
    log(S(437) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(735)] = function()
return {
    [79389059854988] = S(624),
}
end
import(S(135))(import)
