-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Search", " (MaxActivationDistance=%s)", "[AutoChest] disabled", "ProximityPrompt", "'", "AutoChest", "games/my_anime_mine/auto_mine", "games/my_anime_mine/auto_sell", "TextLabel", "'", "[SellCards] === total cards: ", "TextLabel", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Tool", "no tool", " '", "[Remotes] ", "open_menu_hint", "Backpack", "UIListLayout", "", "search_placeholder", "ProximityPrompt", "Frame", "ValueLabel", "core/interaction_logger", "Dump", "Text", "core/logger", "[AutoPlace] fireproximityprompt error: ", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "[AutoChest] chest didn't open, aborting", "no tool in Backpack (all miners already placed?)", "Players", "ClickDetector", "", "games/my_anime_mine/get_plot_parts", "UserInputService", "[AntiAFK] enabled - watching Player.Idled", "Auto Sell", "Close", "games/my_anime_mine/main", " <-- remote, likely fired by the real interaction", "PlayerGui", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Card", "MineWorld", "SellFrame", "Content", "Auto Place Miner", "Backpack", "Empty miner slot", "'", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "%", " items, closing", "[AutoChest] only ", "Backpack", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/debug_dump_remotes", "RemoteFunction", "TextButton", "games/my_anime_mine/get_current_plot", "[MyAnimeMine] === Node: ", "UIPadding", "UIStroke", " ", "Humanoid", "games/my_anime_mine/debug_dump_sell_cards", "Frame", "games/my_anime_mine/auto_upgrade", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Anti-AFK", "Players", ": ", "UserInputService", "Content", "TextLabel", "core/i18n", "Pad", " items)", " '", "core/logger", "enabled", "Players", "[Interaction] ProximityPrompt triggered: ", "", "games/my_anime_mine/debug_dump", "Search functions...", " = ", "[%X] ", "Frame", "Frame", " (ActionText=", "core/i18n", "not standing in any plot", "ClickDetector", "AutoPlace", "UIListLayout", "Sell for", "SelectAllButton", "🏠", "not found", "Frame", "Auto Upgrade", "RemoteFunction", "Players", "[AutoSell] sell menu didn't open, aborting", " items)", "core/logger", "Debug: Current Plot", ",", "GuiButton", "GuiButton", "ClickDetector", " = ", "[AutoPlace] ", "core/logger", "ChestCapacity", "log failed", "[AutoSell] disabled", "enabled", "TextLabel", "UICorner", "TextLabel", "", "no character/backpack", "[AntiAFK] idle detected, simulating input", "[Scheduler] ", "ProximityPrompt", "[GuiDump] === visible MineGui tree ===", "Zones", "games/my_anime_mine/auto_place", "Frame", "[Interaction] logger disabled", " on ", "[MyAnimeMine] === Dump start ===", "Done", "PlayerGui", "PlayerGui", "ScrollingFrame", " = ", "core/get_place_id", "enabled", "games/my_anime_mine/auto_sell", "TextButton", "Frame", "ChestFrame", "TextLabel", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "UICorner", " ===", "games/my_anime_mine/debug_dump", "games/my_anime_mine/auto_chest", "  ", "TextButton", "core/logger", "My Anime Mine · Mining Hub", "write reported success but ", "[MyAnimeMine] === Tool: ", "Auto Chest", "RemoteEvent", "Workspace", "Card", "TextLabel", "Content", " '", "Scroll", "core/logger", "[AutoUpgrade] ", "", "This is not a supported place.", "Dump", "no isfile/readfile available to verify the write", "Crew", "core/dispatch", "ตามเครื่อง", " (Text=", "games/my_anime_mine/get_current_plot", "⚙", "Badge", "[Logger] failed to write ", "CoreUI", "Debug: Dump Node/Holder", "Label", "core/logger", "TextLabel", "Players", "disabled", "[Remotes] === total: ", "Open menu", "TextButton", "", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "core/ui", "Content", "Nodes", "TextButton", "core/get_place_id", "ค้นหาฟังก์ชัน...", "  [attr] ", "\n", "Players", "Actions", "ขนาดเมนู", "[GuiDump] === end ===", "ย่อขยายเมนูทั้งชุด", "[Interaction] ClickDetector clicked: ", "UIListLayout", "games/my_anime_mine/auto_chest", "TextButton", "ProximityPrompt", "ValueLabel", "ClickDetector", "[MyAnimeMine] no holders found", "th", "ProximityPrompt", "en-us", "%", "Dump", "core/logger", "ProximityPrompt", "^(%d+)", "Done", "Frame", ")", "[AutoSell] clicked SelectAllButton (", "search_placeholder", "", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " '", "games/my_anime_mine/main", "TextBox", "Content", "Text", "auto", "Holders", "", "games/my_anime_mine/get_current_plot", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Frame", "Dump", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", " cards)", " holders checked)", "PlotIndex", "games/my_anime_mine/get_plot_parts", "  ", "CoreGui", "core/ui", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "MekPro.log", "Players", "SellStand", "AutoUpgrade", "disabled", "[MyAnimeMine] not standing in any plot", "Debug: Dump Backpack Tool", "games/my_anime_mine/debug_dump_sell_cards", " ===", "TextButton", "games/registry", "Humanoid", "-", "[AutoChest] enabled (collects once >= ", "ไทย", "", "core/anti_afk", "core/i18n", "core/teleport", "games/my_anime_mine/get_plot_parts", "core/dispatch", "ReplicatedStorage", "[Interaction] Button activated: ", "th", "no humanoid", "SellFrame", "Players", "leaderstats", "[AntiAFK] disabled", "RunService", "Workspace", "core/turn_scheduler", "UIScale", "TabBar", "Plots", " ===", "core/get_leaderstat", "plot has no Holders folder", "Content", "", "th", "games/my_anime_mine/auto_place", "OwnerUserId", "games/my_anime_mine/debug_dump_gui", "[Interaction] logger enabled - go click/interact with things now", "Dump", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "(none enabled)", "ClickDetector", "Loot", " (Text=", "Players", "Interaction Logger (debug)", "Workspace", "TextLabel", "neither appendfile nor writefile is available", " ===", " cards", "core/logger", "Check", "[GuiDump] MineGui not found", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", ")", "Auto Mine", "MineGui", "Frame", "[AutoChest] clicked TakeButton (", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/debug_dump_remotes", "TextLabel", "TextButton", "\n", "core/logger", "Content", "open_menu_hint", "GuiObject", "Badge", "Main", "VirtualUser", "core/teleport", "TextLabel", "games/registry", "AutoMine", "MekPro", "MineGui", "MineGui", "Debug: Dump Sell Cards", "Close", "core/interaction_logger", "[MyAnimeMine] === Holder: ", "MineGui", "Progress", "?", "Players", "  ", "Frame", "Label", "no empty holder found (", "games/my_anime_mine/get_plot_parts", "write reported success but the line isn't in ", "[Scheduler] turn: ", "ปุ่มเปิดเมนู", "  [attr] ", "[Interaction] Key pressed: %s (gameProcessed=%s)", "[AutoSell] nothing to sell, closing", "Players", "core/logger", "X", " = ", "Done", "", "RemoteEvent", "'", "[AutoSell] clicked SellButton", "core/logger", "core/logger", "[AutoSell] enabled (only sells when there's something in the scroll)", ")", "HumanoidRootPart", "ScreenGui", "[AutoMine] ", "ฟาร์ม", "Select All", "en", "Debug: Dump Visible GUI", "Debug: Dump All Remotes", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "en", "", "Scroll", "AutoSell", "games/my_anime_mine/get_current_plot", " doesn't exist", "games/my_anime_mine/get_plot_parts", "TakeButton", "", "[AutoSell] fireproximityprompt error: ", "TextButton", "อังกฤษ", "PlayerGui", "UpgradePart", "", "Error: ", "Chest", " = ", "Tool", "TextLabel", ": ", "games/my_anime_mine/debug_dump_gui", "TextLabel", "games/my_anime_mine/auto_mine", "Players", "core/anti_afk", "Header", "PlayerGui", "[MyAnimeMine] no nodes present right now", "[AutoPlace] ", "disabled", "auto", "SellButton", "[SellCards] Card #", "  [attr] ", "[AutoChest] fireproximityprompt error: ", "  [attr] ", "[AutoPlace] placed ", "", "no plot", "GuiObject", "core/get_me", "ตั้งค่า", " remotes", " ===", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", ": ", " error: ", "core/teleport", "games/my_anime_mine/auto_upgrade", "core/turn_scheduler", "ValueBase"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(269)] = function()
return function(import)
local log = import(S(369))
local Players = game:GetService(S(34))
local VirtualUser = game:GetService(S(332))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(39))
        connection = player.Idled:Connect(function()
            log(S(128))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(281))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(273)] = function()
return function(import)
    local getPlaceId = import(S(199))
    local registry = import(S(263))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(172))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(289)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(280))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(422)] = function()
local Players = game:GetService(S(203))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(68))
    local backpack = player:WaitForChild(S(51))
    return player, humanoid, backpack
end
return getMe
end
modules[S(143)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(79)] = function()
local Players = game:GetService(S(254))
local STRINGS = {
    en = {
        search_placeholder = S(89),
        open_menu_hint = S(191),
    },
    th = {
        search_placeholder = S(200),
        open_menu_hint = S(355),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(218)
    if locale:sub(1, 2):lower() == S(216) then
        return S(276)
    end
    return S(381)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(412) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(26)] = function()
return function(import)
local Players = game:GetService(S(188))
local Workspace = game:GetService(S(283))
local UserInputService = game:GetService(S(38))
local log = import(S(221))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(86) .. describe(prompt)
            .. S(94) .. prompt.ActionText .. S(371))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(208) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(275) .. describe(button)
            .. S(303) .. tostring(button.Text) .. S(226))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(212)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(214)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(113)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(135))
        return
    end
    connections = {}
    log(S(297))
    local playerGui = Players.LocalPlayer:WaitForChild(S(139))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(4)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(35)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(114)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(357),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(110)] = function()
local LOG_FILE = S(253)
local function log(message)
    local line = os.date(S(91)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(126))
            end
            appendfile(LOG_FILE, line .. S(325))
        elseif writefile then
            local existing = S(238)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(202))
        else
            error(S(308))
        end
        if not (isfile and readfile) then
            error(S(174))
        end
        if not isfile(LOG_FILE) then
            error(S(159) .. LOG_FILE .. S(386))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(353) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(182) .. LOG_FILE .. S(401) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(333)] = function()
return function(import)
local Players = game:GetService(S(74))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(372))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
end
modules[S(431)] = function()
return function(import)
local RunService = game:GetService(S(282))
local log = import(S(186))
local SLICE_SECONDS = 3
local scheduler = {}
local tasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.Register(name, step, isEnabled)
    table.insert(tasks, { name = name, step = step, isEnabled = isEnabled })
end
local function pickNextEnabled()
    if #tasks == 0 then
        return nil
    end
    for _ = 1, #tasks do
        rrIndex = (rrIndex % #tasks) + 1
        local candidate = tasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(354) .. (currentTask and currentTask.name or S(300)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            local ok, err = pcall(currentTask.step)
            if not ok then
                log(S(129) .. currentTask.name .. S(428) .. tostring(err))
            end
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(195)] = function()
return function(import)
local i18n = import(S(270))
local UserInputService = game:GetService(S(76))
local THEME = {
    BrandName = S(337),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(151))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(124))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(65))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(192))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(389)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(66))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(12))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(346)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(165))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(171)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(141))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(20))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(400))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(225))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(9))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(403))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(78))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(213))
            if not valueLbl then
                valueLbl = Instance.new(S(307))
                valueLbl.Name = S(25)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(181))
            if not badge then
                badge = Instance.new(S(187))
                badge.Name = S(330)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(62))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(70))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(92))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(156))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(382)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(55))
        local track = Instance.new(S(104))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(147))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(146))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(36)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(219)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(24))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(99))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(324))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(28)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(373))
    screenGui.Name = S(183)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(249))
    local main = Instance.new(S(134))
    main.Name = S(331)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(285))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(349))
    tabBar.Name = S(286)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(209))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(241))
    header.Name = S(407)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(211))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(123))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(125))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(268)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(262))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(361)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(391))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(265)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(233))
    searchBox.Name = S(1)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(22))
    searchBox.Text = S(229)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(93))
    content.Name = S(49)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(318))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(323))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(328))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(334))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(235)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(419) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(67) .. (item.Subtitle or S(193))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(228))
        footerLabel.Text = i18n.T(S(18))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(154)] = function()
return function(import)
local Players = game:GetService(S(359))
local getCurrentPlot = import(S(320))
local getPlotParts = import(S(37))
local teleport = import(S(271))(import)
local log = import(S(118))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(393))
    local mineGui = playerGui and playerGui:FindFirstChild(S(344))
    return mineGui and mineGui:FindFirstChild(S(148))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(327))
    local actions = content and content:FindFirstChild(S(204))
    return actions and actions:FindFirstChild(S(388))
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(196))
    local progress = content and content:FindFirstChild(S(345))
    local label = progress and progress:FindFirstChild(S(350))
    local text = label and label.Text or S(87)
    local digits = text:gsub(S(112), S(364)):match(S(223))
    return tonumber(digits)
end
local function clickButton(button)
    pcall(function()
        firesignal(button.Activated)
    end)
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(23), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(3))
    else
        lastAttempt = 0
        log(S(266) .. MIN_ITEMS_TO_COLLECT .. S(109))
    end
end
function autoChest.IsEnabled()
    return enabled
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(416) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.3)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(32))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    if count and count < MIN_ITEMS_TO_COLLECT then
        log(S(57) .. count .. S(56))
        local closeButton = chestFrame:FindFirstChild(S(341))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        log(S(319) .. tostring(count) .. S(81))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(7)] = function()
return function(import)
local getCurrentPlot = import(S(63))
local getPlotParts = import(S(352))
local log = import(S(368))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(97), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(374) .. (enabled and S(144) or S(189)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(294)] = function()
return function(import)
local Players = game:GetService(S(85))
local getCurrentPlot = import(S(239))
local getPlotParts = import(S(247))
local log = import(S(157))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(80))
    return pad and pad:FindFirstChildWhichIsA(S(217), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(52)
end
local autoPlace = {}
local enabled = false
local lastFire = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(410) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    log(S(117) .. (enabled and S(122) or S(257)))
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local player = Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild(S(58))
    if not character or not backpack then
        reason(S(127))
        return
    end
    local humanoid = character:FindFirstChildOfClass(S(264))
    local tool = backpack:FindFirstChildWhichIsA(S(399))
    if not humanoid then
        reason(S(277))
        return
    end
    if not tool then
        reason(S(33))
        return
    end
    local plot = getCurrentPlot()
    if not plot then
        reason(S(96))
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        reason(S(290))
        return
    end
    local placed = false
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderEmpty(prompt) then
            humanoid:EquipTool(tool)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if ok then
                log(S(418) .. tool.Name .. S(136) .. holder.Name)
            else
                log(S(30) .. tostring(err))
            end
            placed = true
            break
        end
    end
    if not placed then
        reason(S(351) .. #parts.Holders:GetChildren() .. S(245))
    else
        lastReason = nil
    end
end
return autoPlace
end
end
modules[S(8)] = function()
return function(import)
local Players = game:GetService(S(304))
local Workspace = game:GetService(S(163))
local teleport = import(S(429))(import)
local log = import(S(326))
local CHECK_INTERVAL = 3
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(408))
    local mineGui = playerGui and playerGui:FindFirstChild(S(339))
    return mineGui and mineGui:FindFirstChild(S(48))
end
local function labelText(button)
    local content = button:FindFirstChild(S(234))
    local label = content and content:FindFirstChild(S(185))
    return label and label.Text or S(292)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(77))
    local scroll = content and content:FindFirstChild(S(383))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(46) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    pcall(function()
        firesignal(button.Activated)
    end)
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(255))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(130), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(121))
    else
        lastAttempt = 0
        log(S(370))
    end
end
function autoSell.IsEnabled()
    return enabled
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(390) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.3)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(108))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(358))
        local closeButton = sellFrame:FindFirstChild(S(41))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(166))
    local selectAllButton = content and content:FindFirstChild(S(101))
    local sellButton = content and content:FindFirstChild(S(413))
    if selectAllButton and labelText(selectAllButton) == S(376) then
        clickButton(selectAllButton)
        log(S(227) .. cardCount .. S(244))
        task.wait(0.3)
    end
    if sellButton and labelText(sellButton):find(S(100), 1, true) then
        clickButton(sellButton)
        log(S(367))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(430)] = function()
return function(import)
local getCurrentPlot = import(S(385))
local getPlotParts = import(S(387))
local log = import(S(29))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(394))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(115))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(170) .. (enabled and S(84) or S(411)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(88)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(395)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(82) .. child.Name .. S(366)
        if child:IsA(S(222)) then
            line = line .. string.format(
                S(31),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(301)) then
            line = line .. string.format(S(2), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(365)) or child:IsA(S(106)) then
            line = line .. S(43)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(201) .. attrName .. S(142) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(348))
    end
end
return dumpInstance
end
modules[S(402)] = function()
return function(import)
local Players = game:GetService(S(107))
local log = import(S(83))
local function dumpVisible(root, indent)
    indent = indent or S(21)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(329)) and child.Visible then
            local line = indent .. child.ClassName .. S(167) .. child.Name .. S(10)
            if child:IsA(S(149)) or child:IsA(S(198)) then
                line = line .. S(178) .. tostring(child.Text) .. S(315)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(356) .. attrName .. S(398) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(155))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(140))
    local mineGui = playerGui and playerGui:FindFirstChild(S(338))
    if not mineGui then
        log(S(313))
        return
    end
    log(S(131))
    dumpVisible(mineGui)
    log(S(206))
end
return dumpMineGui
end
end
modules[S(322)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(274))
local log = import(S(169))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(162)) or descendant:IsA(S(61)) then
            count = count + 1
            log(S(17) .. descendant.ClassName .. S(427) .. descendant:GetFullName())
        end
    end
    log(S(190) .. count .. S(261))
    return count
end
return dumpRemotes
end
end
modules[S(260)] = function()
return function(import)
local Players = game:GetService(S(405))
local log = import(S(311))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(44))
    local mineGui = playerGui and playerGui:FindFirstChild(S(317))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(278))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(291)) and sellFrame.Content:FindFirstChild(S(168))
    if not scroll then
        log(S(194))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(164) or card:IsA(S(421)) then
            count = count + 1
            log(S(414) .. count .. S(75) .. card.ClassName .. S(16) .. card.Name .. S(53))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(415) .. attrName .. S(362) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(248) .. child.ClassName .. S(231) .. child.Name .. S(5)
                if child:IsA(S(432)) then
                    line = line .. S(116) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(11) .. count .. S(309))
    return count
end
return dumpSellCards
end
end
modules[S(179)] = function()
local Players = game:GetService(S(279))
local Workspace = game:GetService(S(306))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(47))
    local plots = mineWorld and mineWorld:FindFirstChild(S(287))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(295)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(59)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(237)),
        Nodes = plot:FindFirstChild(S(197)),
        Chest = plot:FindFirstChild(S(397)),
        Zones = plot:FindFirstChild(S(132)),
        Crew = plot:FindFirstChild(S(175)),
        Loot = plot:FindFirstChild(S(302)),
        PlotIndex = plot:GetAttribute(S(246)),
        ChestCapacity = plot:GetAttribute(S(119)),
    }
end
return getPlotParts
end
modules[S(232)] = function()
return function(import)
    local Players = game:GetService(S(347))
    local createWindow = import(S(250))(import)
    local i18n = import(S(95))
    local antiAfk = import(S(406))(import)
    local interactionLogger = import(S(342))(import)
    local scheduler = import(S(284))(import)
    local log = import(S(360))
    local window = createWindow({
        Subtitle = S(158),
    })
    local getCurrentPlot = import(S(321))
    local getPlotParts = import(S(272))
    local dumpInstance = import(S(153))
    local dumpRemotes = import(S(60))(import)
    local dumpSellCards = import(S(69))(import)
    local dumpGui = import(S(296))(import)
    local autoMine = import(S(404))(import)
    local autoPlace = import(S(133))(import)
    local autoChest = import(S(210))(import)
    local autoSell = import(S(145))(import)
    local autoUpgrade = import(S(71))(import)
    scheduler.Register(S(336), autoMine.Step, autoMine.IsEnabled)
    scheduler.Register(S(98), autoPlace.Step, autoPlace.IsEnabled)
    scheduler.Register(S(6), autoChest.Step, autoChest.IsEnabled)
    scheduler.Register(S(384), autoSell.Step, autoSell.IsEnabled)
    scheduler.Register(S(256), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.Start()
    local farmTab = window:AddTab(S(375), S(102))
    farmTab:AddCard(
        S(316),
        S(45)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(50),
        S(426)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(161),
        S(314)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(40),
        S(299)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(105),
        S(54)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(111), S(72))
    local checkBtn
    checkBtn = debugCard:AddButton(S(312), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(103)
            log(S(258))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(13),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(184),
        S(380)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(173), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(420)
            return
        end
        local ok, err = log(S(137))
        if not ok then
            dumpBtn.Text = S(120)
            dumpCard:SetSubtitle(S(396) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(64) .. firstNode.Name .. S(288))
            dumpInstance(firstNode, log)
        else
            log(S(409))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(343) .. firstHolder.Name .. S(152))
            dumpInstance(firstHolder, log)
        else
            log(S(215))
        end
        dumpBtn.Text = S(224)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(259),
        S(251)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(220), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(19))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(14))
        if not tool then
            toolDumpBtn.Text = S(15)
            return
        end
        log(S(160) .. tool.Name .. S(425))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(417) .. attrName .. S(90) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(363)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(379),
        S(252)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(298), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(424)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(340),
        S(243)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(27), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(310)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(378),
        S(240)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(242), function()
        dumpGui()
        guiDumpBtn.Text = S(138)
    end)
    local settingsTab = window:AddTab(S(423), S(180))
    settingsTab:AddCard(S(73), S(230)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(305),
        S(150)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(205), S(207), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(177), S(392), S(267) }, 1, function(index, value)
        local code = ({ S(236), S(377), S(293) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(335)] = function()
return {
    [79389059854988] = S(42),
}
end
import(S(176))(import)
