-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Debug: Dump Node/Holder", "", "sold ", "core/config", "number", ") didn't take, trying the next one", "write reported success but ", " ", ")", "[AutoChest] can't read the chest yet", "core/teleport", "", "Players", "core/config", "CoreGui", "Damage", ")", "[AutoChest] collected ", "Dump", "Text", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[Probe] === executor capabilities ===", "HotbarFrame", "Content", "core/logger", "[Probe] decompile unavailable - can't read the game's source this way", "JumpPower (ปกติ 50)", "[GameAPI] SendEvent failed: ", "getscripts", "Frame", "[Remotes] === total: ", "PlayerGui", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "Nodes", "core/logger", "nothing to place or upgrade", "games/my_anime_mine/debug_dump_remotes", "UpgradeButton", "sl", "core/humanoid", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "[Scheduler] turn: ", "All", "ItemFrame", "games/my_anime_mine/debug_dump_state", "Auto Upgrade Unit", "[AutoPlace] holders: ", " still empty after placing ", "PlayerGui", "[%X] ", "  [upvalue] ", "Auto Place: รอบเช็ค", "getgc", "core/logger", "RemoteEvent", "Chest TakeButton", "games/my_anime_mine/debug_dump_chest", "MineWorld", "x", " ", ")", "MineSpeed", "ค้นหาฟังก์ชัน...", "auto", "[Handlers]   #", "Auto Research (Plot)", "[Probe] PlayerScripts.UI.MineGame not found", "[^%d%.]", " ===", "TextLabel", "[Handlers] ", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "ProximityPrompt", "core/logger", "TextLabel", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Holders", "Instance", "yes", " -> ", "'", "[Config] couldn't write ", "Auto Research: รอบเช็ค", " (", " doesn't exist", "games/my_anime_mine/debug_dump", "core/anti_afk", "MineUpgradeCharacter", "Debug: Dump Chest Data", "UserInputService", "AutoMine", "", "ต่ำสุดก่อน", " parts", "[ChestDump] === end ===", "core/logger", "Auto Mine: คลิกต่อรอบ", "games/my_anime_mine/debug_dump_gui", "lowest", "AutoUpgrade", "Dump", "autoUpgradePickaxe", "getnilinstances", "Debug: Dump All Remotes", "SellStand", "  ", "[AutoSell] MineSellItems ", "ValueBase", ":", "games/my_anime_mine/auto_place", " items (now ", "(", "TextButton", "\n", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", "core/logger", "[Scheduler] ", "Search", "", "Modules", "ซื้อ research ทุก family ในโซนที่ปลดแล้วผ่าน remote ตรง (ไม่เปิด UI)", "  ", ": ", "ProximityPrompt", "leaderstats", "Players", "[Interaction] Button activated: ", "[Handlers] === ", "neither appendfile nor writefile is available", "MineGui", "Damage", "autoUpgradePriority", "core/logger", "[Interaction] Key pressed: %s (gameProcessed=%s)", "games/my_anime_mine/get_current_plot", "th", "-", "table", "Dump", "[AutoChest] enabled (collects once >= ", "Done", "PlayerScripts", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Dump", "Auto Chest: รอบเช็ค", "core/logger", "waiting", "languageIndex", "core/logger", "Speed", "fill", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Debug: Inspect Button Handlers", "  ", "table", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "games/my_anime_mine/debug_dump", "TextLabel", "SlotFocusFrame", "games/my_anime_mine/debug_dump_remotes", "')", "Speed", "core/i18n", "[AutoUpgrade] ", "ClickDetector", "waiting", "GuiButton", "MineHUDClient", "hookfunction", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "UIListLayout", "jumpPower", "games/my_anime_mine/get_current_plot", " '", "core/get_place_id", " (power ", "ปุ่มเปิดเมนู", ")", " '", "HttpService", "[BestMiner] slot ", "ไทย", "Container", "AutoSell", "ProximityPrompt", " remotes", "GuiObject", "noclip", "Frame", "[StateDump] === end ===", "getscriptbytecode", "s", "[Interaction] ProximityPrompt triggered: ", "Done", "Players", "/", "en", "MineGui", "MineBuyPickaxe", "log failed", "CoreUI", "TextLabel", "defined at ", "s", "Workspace", "[GameAPI] couldn't load the game's modules: ", "[Handlers]   ", "", "TakeButton", "Actions", "[AutoResearch] ", "Auto Mine", "Players", "Pad", "PlayerGui", " = ", "ตามเครื่อง", "HumanoidRootPart", " attributes ===", "core/interaction_logger", "core/logger", "locked slot", "core/teleport", "[AutoPlace] disabled", "getconnections", "[MyAnimeMine] no nodes present right now", "TextButton", "NodeCount", "?", "[Remotes] ", "games/my_anime_mine/auto_chest", "CharLevel", "Open menu", ": ", "Mining", "=locked(", "", "Done", "games/my_anime_mine/get_plot_parts", " (ActionText=", "Content", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "[AutoSell] disabled", "Dump", "[Probe] ", "fill", "core/teleport", "games/my_anime_mine/auto_research", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Zones", "no plot", " ===", "MineChestClient", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "core/parse_money", "[Handlers] getconnections isn't available in this executor", "games/my_anime_mine/auto_upgrade", "Auto Research: ซื้อสูงสุดต่อรอบ", "enabled", " ===", "autoMineClicks", "'", "s", " (Text=", "", "Loot", "/s", "", "", "[Probe] ", "[Probe] decompiled ", "TextButton", "core/i18n", "[MyAnimeMine] === Holder: ", "Workspace", "games/my_anime_mine/get_current_plot", "Debug: Dump Visible GUI", "Header", " from ", "autoSell", "แรงกระโดด", "TextLabel", "PlayerGui", "  ", "[AutoPlace] couldn't pick up ", "  [attr] ", "core/scheduler", "ChestCapacity", "Plots", "ReplicatedStorage", "games/registry", "", "ReplicatedStorage", "Debug: Dump Backpack Tool", "games/my_anime_mine/main", "Done", "  [attr] ", "SellFrame", "[AutoResearch] nothing bought (maxed out or not enough cash)", "core/logger", "string", "  ", "function", "core/dispatch", "อัปเกรด pickaxe ไปเทียร์ถัดไปเมื่อเงินพอ", "core/logger", "[Interaction] logger disabled", "games/my_anime_mine/game_api", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " items", "fly", "table", "Auto Mine: ความเร็ว", "ValueBase", "  ", "CharMineSpeedLevel", "UICorner", "[MyAnimeMine] === Node: ", "=", "GuiService", "autoMineSpeed", "", "มุดแมพ (Noclip)", "debug.getconstants", "PlayerGui", "games/my_anime_mine/game_api", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "works from anywhere - dropping the teleport", " -> ", "Locked miner slot", " (MaxActivationDistance=%s)", "TextLabel", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", "Players", "", "[AutoResearch] ", "open_menu_hint", " '", "ClickDetector", ".lua", "Content", "UI", "[Handlers] === end ===", "no isfile/readfile available to verify the write", "boolean", ")", "disabled", "core/get_leaderstat", "games/my_anime_mine/get_current_plot", "%$", "core/humanoid", "TextButton", "auto", "games/my_anime_mine/get_plot_parts", "Badge", "Content", "Activated", "table", "%", "auto", "  ", "Players", "[StateDump] === MineGame ===", "[AntiAFK] disabled", "autoResearch", "Luck", "Players", "debug.getproto", "TextButton", "games/my_anime_mine/auto_place", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "prompt disabled", "GuiObject", "Auto Chest: เกณฑ์เก็บ", "RemoteFunction", ": not found", "string", "ย่อขยายเมนูทั้งชุด", "Auto Sell: รอบขาย", "Backpack", "Detail", "ดีบัก", "TextLabel", "[MekPro] settings file: ", " ", "en", "Anti-AFK", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "[AutoPlace] placed ", "th", "  [const] ", "RunService", "Frame", "[StateDump] no mine data (is the game API loaded?)", "TextLabel", "Auto Sell", "ขนาดเมนู", " chars) -> ", "[ChestDump] === Chest subtree ===", "X", "^%$", " ", "s", "[MyAnimeMine] === Dump start ===", "RemoteEvent", ": ", "My Anime Mine · Mining Hub", "เปิดหน้า research ทุกกี่วินาที", "core/ui", "SellFrame", "", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", " error: ", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "core/get_place_id", "Workspace", "core/logger", "getrenv", "Scroll", "OwnerUserId", "search_placeholder", "RunService", ",", "TextLabel", "MineSpeed", "ไล่อัปเกรด miner ทุกกี่วินาที", ")", "HumanoidRootPart", "lowest", "costs ", "UIListLayout", "MineSpeed", ")", "autoPlaceUpgradeWeak", " = ", "Players", "[AutoMine] ", " stack(s)", "Frame", "  [attr] ", "autoChest", " (", "", " ", "SellButton", "[Config] couldn't read ", "Unit Damage UpgradeButton", "RemoteFunction", "core/logger", " (", "RemoteFunction", "[Probe] couldn't decompile ", "[AutoPlace] slot ", "button has no size on screen", "UIScale", "MekPro", "Card", "Frame", "Done", "no tool", "🏠", "games/my_anime_mine/auto_chest", "games/my_anime_mine/get_plot_parts", "MineGame", "[GuiDump] === visible MineGui tree ===", "HumanoidRootPart", "write reported success but the line isn't in ", "Content", "MineSkillTreeClient", "core/logger", "x", "en", "MekPro/decompiled_", "languageIndex", ": ", "[MyAnimeMine] not standing in any plot", " (prompt says '", "Damage", " ", "Damage", "games/my_anime_mine/debug_dump_sell_cards", "Players", "Humanoid", "games/my_anime_mine/debug_inspect_handlers", "Search functions...", "Interaction Logger", "ฟาร์ม", "s", "เดินทะลุกำแพงและพื้น", "Empty miner slot", "[AutoPlace] upgrading ", "=empty", "TextButton", "  [attr] ", "'", ": ", "games/my_anime_mine/get_best_miner", "Dump", "core/logger", ") ===", "TextLabel", "games/registry", "disabled", "autoResearchInterval", "  (debug.getupvalues unavailable)", "MineTakeChest", "UIListLayout", "'", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "function", "games/my_anime_mine/get_best_miner", "NodeHealth", "", "core/scheduler", "🐛", "^([%d,]+)$", "PlotIndex", "Auto Upgrade: รอบเช็ค", "MineSlotFocusClient", " -> ", "GuiButton", "action '", "%$%s*([%d,%.]+)%s*(%a*)", ") from ", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/debug_dump_gui", "Humanoid", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "TextLabel", "MineSellItems", "MineBuySkill", "[Interaction] ClickDetector clicked: ", "AutoChest", "Research BuyButton", "core/i18n", "[Probe] === end ===", "Folder", "MekPro", "Damage", "Done", "TextButton", "Players", "core/anti_afk", " connection(s)", "Instance", "Speed", " (", "Tool", "games/my_anime_mine/game_api", "games/my_anime_mine/debug_probe_env", "ตั้งค่า", "[AutoChest] waiting: ", "Sell SellButton", "ItemName", "SelectAllButton", "Place", "games/my_anime_mine/debug_probe_env", "Workspace", "ClickDetector", "enabled", "Frame", " = ", "autoPlaceInterval", "ClickDetector", "'", ": ", "", "BasePart", "TextButton", "autoResearchMaxPerPass", "UIPadding", "Players", "games/my_anime_mine/auto_sell", "getsenv", "menuScale", "BodyPosition", "float", ") -> ", "th", "Main", "Chest", "table(", "ValueLabel", "getcallbackvalue", " = ", " = ", "getloadedmodules", "Text", " items)", "[Logger] failed to write ", "Auto Place: เปลี่ยนตัวอ่อนออก", "[GuiDump] MineGui not found", "autoUpgradeInterval", "games/my_anime_mine/game_api", "", "[SellCards] === total cards: ", "", "This is not a supported place.", " '", "Probe", "Backpack", "[AutoChest] MineTakeChest had no effect (", " <-- remote, likely fired by the real interaction", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "games/my_anime_mine/get_current_plot", "[AntiAFK] idle detected, simulating input", "autoMine", "TabBar", "[Handlers] ", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", " = ", "games/my_anime_mine/auto_mine", "games/my_anime_mine/main", "\n", "[Interaction] logger enabled - go click/interact with things now", "ProximityPrompt", " '", "table", " cards", "[SellCards] Card #", "games/my_anime_mine/debug_dump_state", "Players", "enabled", "games/my_anime_mine/game_api", "open_menu_hint", "พิมพ์โครงสร้าง GameState.GetData2().MineGame เพื่อหา key ที่ remote ต้องใช้", "autoPlace", "[Uu]nlock", "Frame", "ChestFrame", "[Probe] === start ===", "core/logger", "StatsPanel", "", "MekPro.log", "BodyVelocity", "autoUpgrade", " ===", "sale didn't go through", "[MyAnimeMine] === Tool: ", "unreadable", "(nothing to do)", "needs us at the stand - keeping the teleport", "autoChestMinItems", "'", "UIStroke", "AutoPlace", "Players", "core/logger", "Debug: Probe Executor + Scripts", "games/my_anime_mine/auto_sell", "", "autoSellInterval", "[AutoUpgrade] pickaxe tier ", "BuyButton", "MineHotbarClient", "autoChestInterval", "Crew", "/", "Auto Place Miner", "[ChestDump] not standing in any plot", "ReplicatedStorage", "", "MineEquipSlot", "Mining", "Error: ", "table", "table", "function", ": ", "Inspect", "unreadable", "[AutoPlace] nothing in the hotbar to place", "บิน (Fly)", "[Handlers] === start (open the relevant menus first) ===", "Content", "auto", "ScreenGui", "MineGui", " (Text=", "Debug: Current Plot", "antiAfk", "Content", "ScrollingFrame", "UserInputService", "อังกฤษ", "AutoResearch", "core/logger", " x", "Debug: Dump Game State", "games/my_anime_mine/get_plot_parts", "SellFrame", "[AntiAFK] enabled - watching Player.Idled", "RemoteEvent", "  (debug.getconstants unavailable)", "core/dispatch", "/", "th", "TextLabel", "core/ui", " = ", "/config.json", "core/get_place_id", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "MouseButton1Click", "[ChestDump] plot has no Chest", " ", "games/my_anime_mine/game_api", ")", "games/my_anime_mine/auto_mine", "UICorner", "interactionLogger", " (", "Dump", " ", "Frame", "TextBox", "MineSellClient", "games/my_anime_mine/debug_inspect_handlers", "SkillTreeFullFrame", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[attr] ", "string", ": ", "[ChestDump] === Player subtree ===", "[AutoPlace] ", " while selecting", " ชิ้น", "Frame", " (Text=", "TextButton", "ProximityPrompt", "core/get_me", "UserInputService", " not found", "core/click", "[GuiDump] === end ===", "x", "[AutoChest] disabled", "Auto Chest", "[ChestDump] === plot ", "", ": ", ") on ", " power ", "decompile", "WalkSpeed (ปกติ 16)", "ความเร็ววิ่ง", "ลอย (Float)", "  ", " ===", "[AutoPlace] ", "Sell SelectAllButton", "TextLabel", "search_placeholder", " (power ", "autoUpgradeMaxPerPass", "[AutoPlace] drifted out of range of ", "Players", " ===", "", "MineGui", " = ", "Scroll", ")", " | ", "no", " = ", "BasePart", "CharSpeedLevel", "Frame", "[AutoPlace] picked up ", "core/logger", "TextButton", "[AutoSell] ", "[^%.]+", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[ChestDump] === ReplicatedStorage.", "[AutoUpgrade] ", "games/my_anime_mine/debug_dump_sell_cards", "en-us", "อ่านป้ายบน chest ทุกกี่วินาที", "TextLabel", "VirtualUser", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "table", "core/interaction_logger", "not found", "write failed", "[Bb]uy", " -> ", "debug.getupvalues", "upgrade", "^x(%d+)$", "games/my_anime_mine/debug_dump_chest", "games/my_anime_mine/auto_research", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " (ActionText=%s ObjectText=%s HoldDuration=%s)", ",", "games/my_anime_mine/get_plot_parts", "VirtualInputManager", "[AutoPlace] fireproximityprompt error: ", "Frame", "walkSpeed", "disabled", "⚙", ")", "[AutoUpgrade] nothing upgraded (maxed out or not enough cash)", "TextLabel", "menuScale", "[AutoSell] enabled", "TextLabel", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "BottomPanels", "Check", "ValueLabel", "Players", "Debug: Dump Sell Cards", "lowest", "[MyAnimeMine] no holders found", "TextLabel", "games/my_anime_mine/game_api", "Badge", "table", "core/logger"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(544)] = function()
return function(import)
local log = import(S(690))
local Players = game:GetService(S(126))
local VirtualUser = game:GetService(S(786))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(695))
        connection = player.Idled:Connect(function()
            log(S(607))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(365))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(738)] = function()
local VirtualInputManager = game:GetService(S(803))
local GuiService = game:GetService(S(321))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(455)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(4)] = function()
return function(import)
local HttpService = game:GetService(S(180))
local getPlaceId = import(S(705))
local log = import(S(651))
local ROOT_FOLDER = S(457)
local placeFolder = ROOT_FOLDER .. S(699) .. tostring(getPlaceId())
local filePath = placeFolder .. S(704)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(313) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(447) .. filePath .. S(567) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(82) .. filePath .. S(234) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(305)] = function()
return function(import)
    local getPlaceId = import(S(175))
    local registry = import(S(292))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(599))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(349)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(125))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(735)] = function()
local Players = game:GetService(S(761))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(528))
    local backpack = player:WaitForChild(S(381))
    return player, humanoid, backpack
end
return getMe
end
modules[S(416)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(40)] = function()
local Players = game:GetService(S(13))
local RunService = game:GetService(S(393))
local UserInputService = game:GetService(S(736))
local Workspace = game:GetService(S(559))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(484))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(218))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(638))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(771)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(569)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(577))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(163)] = function()
local Players = game:GetService(S(573))
local STRINGS = {
    en = {
        search_placeholder = S(486),
        open_menu_hint = S(233),
    },
    th = {
        search_placeholder = S(63),
        open_menu_hint = S(177),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(783)
    if locale:sub(1, 2):lower() == S(391) then
        return S(136)
    end
    return S(473)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(354) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(789)] = function()
return function(import)
local Players = game:GetService(S(624))
local Workspace = game:GetService(S(276))
local UserInputService = game:GetService(S(90))
local log = import(S(471))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(193) .. describe(prompt)
            .. S(240) .. prompt.ActionText .. S(767))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(533) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(127) .. describe(button)
            .. S(682) .. tostring(button.Text) .. S(61))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(734)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(560)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(167)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(308))
        return
    end
    connections = {}
    log(S(617))
    local playerGui = Players.LocalPlayer:WaitForChild(S(215))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(73)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(340)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(522)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(134),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(146)] = function()
local LOG_FILE = S(637)
local function log(message)
    local line = os.date(S(50)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(654))
            end
            appendfile(LOG_FILE, line .. S(616))
        elseif writefile then
            local existing = S(744)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(114))
        else
            error(S(129))
        end
        if not (isfile and readfile) then
            error(S(345))
        end
        if not isfile(LOG_FILE) then
            error(S(7) .. LOG_FILE .. S(85))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(468) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(591) .. LOG_FILE .. S(123) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(256)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(725) then
        return nil
    end
    local number, suffix = text:match(S(524))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(424), S(336))))
    if not value then
        return nil
    end
    if suffix ~= S(665) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(515)] = function()
return function(import)
local RunService = game:GetService(S(423))
local log = import(S(775))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(117) .. task_.name .. S(414) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(42) .. (currentTask and currentTask.name or S(644)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(223)] = function()
local Players = game:GetService(S(335))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(429))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(410)] = function()
return function(import)
local i18n = import(S(536))
local UserInputService = game:GetService(S(687))
local THEME = {
    BrandName = S(539),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(713))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(318))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(572))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(494))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(237)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(648))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(158))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(229)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(756))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(412)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(686))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(508))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(333))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(718))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(425))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(202))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(811))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(584))
            if not valueLbl then
                valueLbl = Instance.new(S(75))
                valueLbl.Name = S(818)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(356))
            if not badge then
                badge = Instance.new(S(701))
                badge.Name = S(825)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(370))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(805))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(631))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(542))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(323)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(360)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(562))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(30))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(776))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(92)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(189))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(432))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(353))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(20)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(680))
    screenGui.Name = S(201)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(15))
    local main = Instance.new(S(773))
    main.Name = S(581)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(456))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(394))
    tabBar.Name = S(609)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(171))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(440))
    header.Name = S(279)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(570))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(283))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(502))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(12)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(733))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(401)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(227))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(137)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(719))
    searchBox.Name = S(118)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(757))
    searchBox.Text = S(208)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(459))
    content.Name = S(357)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(731))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(384))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(338))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(70))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(589)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(568) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(709) .. (item.Subtitle or S(266))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(422))
        footerLabel.Text = i18n.T(S(627))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(463)] = function()
return function(import)
local gameApi = import(S(309))(import)
local log = import(S(74))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(140) .. minItemsToCollect .. S(590))
    else
        log(S(741))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(674) then
            log(S(10))
            lastReported = S(643)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(147) then
            log(S(553) .. count .. S(196) .. minItemsToCollect .. S(311))
            lastReported = S(166)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(18) .. count .. S(111) .. after .. S(809))
        else
            log(S(603) .. count
                .. S(521) .. tostring(after) .. S(178))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(712)] = function()
return function(import)
local getCurrentPlot = import(S(606))
local getPlotParts = import(S(693))
local log = import(S(418))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(565), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(438) .. (enabled and S(260) or S(348)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(371)] = function()
return function(import)
local Players = game:GetService(S(437))
local getCurrentPlot = import(S(350))
local getPlotParts = import(S(464))
local getMinerSlots = import(S(498))(import)
local teleport = import(S(11))
local gameApi = import(S(824))(import)
local log = import(S(450))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(491)
local LOCKED_SLOT_TEXT = S(331)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(214))
    return pad and pad:FindFirstChildWhichIsA(S(124), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(823)) then
            local digits = descendant.Text:match(S(517))
            if digits then
                return tonumber((digits:gsub(S(801), S(596))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(396)) and descendant.Text:match(S(402)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(222)
    end
    if not prompt.Enabled then
        return true, S(373)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(431) .. price
    end
    local action = prompt.ActionText or S(119)
    if action:match(S(630)) or action:match(S(792)) or action:match(S(351)) then
        return true, S(523) .. action .. S(566)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(467))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(804) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(236) .. why .. S(711))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(493))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(320) .. prompt.ObjectText .. S(112) .. power .. S(434))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(768))
    if line ~= lastSurvey then
        log(S(47) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(151), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(795), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(728) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(224))
    else
        lastAttempt = 0
        log(S(41))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(779),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(557), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(675))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(760) .. holder.Name .. S(729))
            return false
        end
        log(S(454) .. candidate.slot .. S(84) .. candidate.name
            .. S(6))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(754) .. holder.Name .. S(48) .. miner.name)
        return false
    end
    log(S(390) .. miner.name .. S(758) .. miner.power
        .. S(746) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(286) .. entry.name .. S(280) .. entry.holder.Name
            .. S(478) .. tostring(entry.prompt.ActionText) .. S(161))
        return false
    end
    log(S(774) .. entry.name .. S(176) .. entry.power
        .. S(525) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(36))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(246) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(492) .. target.holder.Name .. S(745) .. target.name
        .. S(715) .. target.power .. S(579) .. best.name .. S(443) .. best.power .. S(428))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(798)] = function()
return function(import)
local gameApi = import(S(710))(import)
local log = import(S(307))
local KNOWN_FAMILIES = { S(228), S(367), S(513) }
local checkInterval = 30
local maxPerPass = 5
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(337) .. (enabled and S(561) or S(504)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
local function unlockedZones()
    local mine = gameApi.GetMineData()
    local zones = mine and mine.UnlockedZones or {}
    local list = {}
    for zoneKey, unlocked in pairs(zones) do
        if unlocked then
            table.insert(list, zoneKey)
        end
    end
    table.sort(list)
    return list
end
local function skillLevel(zoneKey, family)
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    return skills and skills[family] or 0
end
local function familiesFor(zoneKey)
    local seen = {}
    local list = {}
    for _, family in ipairs(KNOWN_FAMILIES) do
        seen[family] = true
        table.insert(list, family)
    end
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    if skills then
        for family in pairs(skills) do
            if not seen[family] then
                table.insert(list, family)
            end
        end
    end
    return list
end
local function runJob()
    busy = true
    local bought = 0
    for _, zoneKey in ipairs(unlockedZones()) do
        for _, family in ipairs(familiesFor(zoneKey)) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local before = skillLevel(zoneKey, family)
            gameApi.BuySkill(zoneKey, family)
            task.wait(0.4)
            local after = skillLevel(zoneKey, family)
            if after > before then
                log(S(211) .. zoneKey .. S(386) .. family .. S(480)
                    .. before .. S(793) .. after)
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(300))
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(574)] = function()
return function(import)
local Workspace = game:GetService(S(417))
local gameApi = import(S(550))(import)
local teleport = import(S(247))
local log = import(S(35))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(243))
    else
        lastAttempt = 0
        log(S(813))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(105))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(618), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(107)
                .. (sold and S(329)
                         or S(645)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(777) .. (sold and (S(3) .. #keys .. S(439)) or S(641)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(258)] = function()
return function(import)
local gameApi = import(S(626))(import)
local log = import(S(827))
local STATS = {
    { event = S(481), field = S(232) },
    { event = S(162), field = S(772) },
    { event = S(426), field = S(317) },
}
local checkInterval = 20
local maxPerPass = 5
local priority = S(430)
local buyPickaxe = true
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(781) .. (enabled and S(625) or S(807)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function levelOf(holderKey, field)
    local entry = holders()[holderKey]
    return entry and entry[field] or nil
end
local function chooseStat(entry)
    if priority ~= S(99) then
        for _, stat in ipairs(STATS) do
            if stat.event == priority and entry[stat.field] then
                return { event = stat.event, field = stat.field, level = entry[stat.field] }
            end
        end
    end
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local level = entry[stat.field]
        if level and (not lowest or level < lowest.level) then
            lowest = { event = stat.event, field = stat.field, level = level }
        end
    end
    return lowest
end
local function tryPickaxe()
    local mine = gameApi.GetMineData()
    local tier = mine and mine.PickaxeTier
    if not tier then
        return false
    end
    gameApi.BuyPickaxe(tier + 1)
    task.wait(0.4)
    local after = gameApi.GetMineData()
    if after and (after.PickaxeTier or tier) > tier then
        log(S(656) .. tier .. S(80) .. after.PickaxeTier)
        return true
    end
    return false
end
local function runJob()
    busy = true
    local bought = 0
    for holderKey, entry in pairs(holders()) do
        if not enabled or bought >= maxPerPass then
            break
        end
        local stat = chooseStat(entry)
        if stat then
            gameApi.UpgradeCharacter(holderKey, stat.event)
            task.wait(0.4)
            local after = levelOf(holderKey, stat.field)
            if after and after > stat.level then
                log(S(164) .. holderKey .. S(717) .. stat.event .. S(403)
                    .. stat.level .. S(330) .. after)
                bought = bought + 1
            end
        end
    end
    if buyPickaxe and enabled and bought < maxPerPass and tryPickaxe() then
        bought = bought + 1
    end
    if bought == 0 then
        log(S(810))
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(86)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(444)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(174) .. child.Name .. S(496)
        if child:IsA(S(185)) then
            line = line .. string.format(
                S(800),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(165)) then
            line = line .. string.format(S(332), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(55)) or child:IsA(S(449)) then
            line = line .. S(604)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(287) .. attrName .. S(586) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(303))
    end
end
return dumpInstance
end
modules[S(57)] = function()
return function(import)
local Players = game:GetService(S(650))
local ReplicatedStorage = game:GetService(S(294))
local getCurrentPlot = import(S(277))
local getPlotParts = import(S(355))
local log = import(S(301))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(724) .. name .. S(216) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(285))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(339) .. child.Name .. S(509)
            if child:IsA(S(108)) then
                line = line .. S(770) .. tostring(child.Value)
            elseif child:IsA(S(785)) or child:IsA(S(113)) then
                line = line .. S(265) .. tostring(child.Text) .. S(347)
            end
            log(line)
            dumpSubtree(child, indent .. S(154))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(663))
        return 0
    end
    log(S(743) .. plot.Name .. S(219))
    dumpAttributes(plot, S(362))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(708))
        return 0
    end
    log(S(400))
    dumpSubtree(parts.Chest, S(598))
    local player = Players.LocalPlayer
    log(S(727))
    dumpSubtree(player, S(270))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(780) .. player.Name .. S(69))
        dumpSubtree(mine, S(763))
    end
    log(S(95))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(98)] = function()
return function(import)
local Players = game:GetService(S(195))
local log = import(S(54))
local function dumpVisible(root, indent)
    indent = indent or S(269)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(374)) and child.Visible then
            local line = indent .. child.ClassName .. S(179) .. child.Name .. S(263)
            if child:IsA(S(530)) or child:IsA(S(273)) then
                line = line .. S(732) .. tostring(child.Text) .. S(9)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(495) .. attrName .. S(703) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(752))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(32))
    local mineGui = playerGui and playerGui:FindFirstChild(S(130))
    if not mineGui then
        log(S(593))
        return
    end
    log(S(466))
    dumpVisible(mineGui)
    log(S(739))
end
return dumpMineGui
end
end
modules[S(160)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(664))
local log = import(S(133))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(406)) or descendant:IsA(S(376)) then
            count = count + 1
            log(S(230) .. descendant.ClassName .. S(407) .. descendant:GetFullName())
        end
    end
    log(S(31) .. count .. S(762))
    return count
end
return dumpRemotes
end
end
modules[S(482)] = function()
return function(import)
local Players = game:GetService(S(368))
local log = import(S(96))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(49))
    local mineGui = playerGui and playerGui:FindFirstChild(S(764))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(299))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(685)) and sellFrame.Content:FindFirstChild(S(766))
    if not scroll then
        log(S(170))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(458) or card:IsA(S(187)) then
            count = count + 1
            log(S(622) .. count .. S(672) .. card.ClassName .. S(600) .. card.Name .. S(647))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(441) .. attrName .. S(436) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(106) .. child.ClassName .. S(619) .. child.Name .. S(81)
                if child:IsA(S(315)) then
                    line = line .. S(613) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(597) .. count .. S(261))
    return count
end
return dumpSellCards
end
end
modules[S(45)] = function()
return function(import)
local gameApi = import(S(327))(import)
local log = import(S(116))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(359) then
        local count = 0
        for _ in pairs(value) do
            count = count + 1
        end
        return S(583) .. count .. S(17)
    end
    return valueType .. S(8) .. tostring(value)
end
local function dumpTable(value, indent, depth)
    for key, entry in pairs(value) do
        log(indent .. tostring(key) .. S(765) .. describe(entry))
        if type(entry) == S(788) and depth > 0 then
            dumpTable(entry, indent .. S(316), depth - 1)
        end
    end
end
local function dumpState()
    local mine = gameApi.GetMineData()
    if not mine then
        log(S(395))
        return 0
    end
    log(S(364))
    dumpTable(mine, S(122), 2)
    log(S(190))
    return 1
end
return dumpState
end
end
modules[S(485)] = function()
return function(import)
local Players = game:GetService(S(543))
local log = import(S(149))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(546) then
        return value.ClassName .. S(445) .. value:GetFullName()
    end
    if valueType == S(155) then
        return S(620)
    end
    if valueType == S(671) then
        return S(511)
    end
    return valueType .. S(60) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(78) then
        return value:IsA(S(696)) or value:IsA(S(452)) or value:IsA(S(538))
    end
    return valueType == S(378) or valueType == S(5) or valueType == S(346)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(39))
    end)
    if ok and source then
        log(indent .. S(203) .. tostring(source) .. S(109) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(669) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(392) .. describe(constant))
            end
        end
    else
        log(indent .. S(697))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(670) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(51) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(506))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(610) .. label .. S(377))
        return
    end
    log(S(128) .. label .. S(451) .. button:GetFullName() .. S(501))
    if not getconnections then
        log(S(257))
        return
    end
    for _, signalName in ipairs({ S(358), S(707) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(71) .. signalName .. S(726) .. #connections .. S(545))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(65) .. index)
                    inspectFunction(fn, S(207))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(284))
    local current = playerGui and playerGui:FindFirstChild(S(198))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(677))
    inspectButton(S(56), find({ S(632), S(241), S(210), S(209) }))
    inspectButton(S(755), find({ S(694), S(469), S(556) }))
    inspectButton(S(554), find({ S(411), S(342), S(446) }))
    inspectButton(S(535), find({ S(722), S(382), S(657) }))
    local statsScroll = find({ S(159), S(816), S(635), S(420) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(16))
    inspectButton(S(448), damageRow and damageRow:FindFirstChild(S(38)))
    log(S(344))
end
return inspectHandlers
end
end
modules[S(551)] = function()
return function(import)
local Players = game:GetService(S(213))
local log = import(S(25))
local CAPABILITIES = {
    S(748), S(191), S(29), S(53), S(588),
    S(225), S(585), S(575), S(419), S(325),
    S(794), S(369), S(169), S(103),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(778)) do
        if type(target) ~= S(138) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(22))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(271) .. name .. S(587) .. ((ok and type(value) == S(304)) and S(79) or S(769)))
    end
end
local WANTED_SCRIPTS = {
    S(254),
    S(720),
    S(470),
    S(520),
    S(658),
    S(168),
}
local function tryDecompile()
    if not decompile then
        log(S(26))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(142))
    local ui = playerScripts and playerScripts:FindFirstChild(S(343))
    local mineGame = ui and ui:FindFirstChild(S(465))
    if not mineGame then
        log(S(67))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(245) .. name .. S(737))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(302) and #source > 0 then
                local path = S(474) .. name .. S(341)
                local written = pcall(writefile, path, source)
                log(S(272) .. name .. S(548) .. #source .. S(399)
                    .. (written and path or S(791)))
            else
                log(S(453) .. name .. S(497) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(633))
    reportCapabilities()
    tryDecompile()
    log(S(537))
end
return probe
end
end
modules[S(595)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(291))
local log = import(S(500))
local api = {}
local gameState, mineInventory
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(120), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
    end)
    if not ok then
        loadFailed = true
        log(S(206) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetMineData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return nil
    end
    return data.MineGame
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(28) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(507))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(43), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(826) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(531), keys)
end
function api.EquipSlot(key)
    return send(S(666), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(199), tierIndex)
end
function api.BuySkill(nodeId, family)
    return send(S(532), nodeId, family)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(88), characterKey, stat == S(235) and S(433) or stat)
end
return api
end
end
modules[S(512)] = function()
return function(import)
local Players = game:GetService(S(363))
local log = import(S(221))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(326))
    local mineGui = playerGui and playerGui:FindFirstChild(S(681))
    local hotbar = mineGui and mineGui:FindFirstChild(S(23))
    local content = hotbar and hotbar:FindFirstChild(S(678))
    return content and content:FindFirstChild(S(183))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(814)) then
            local count = descendant.Text:match(S(796))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(44))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(24))
    local nameLabel = content and content:FindFirstChild(S(555))
    local damageLabel = content and content:FindFirstChild(S(540))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(636) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(68), S(293)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(661) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(181) .. slot .. S(476) .. info.name
                        .. S(747) .. info.power .. S(691) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(135)] = function()
local Players = game:GetService(S(819))
local Workspace = game:GetService(S(205))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(58))
    local plots = mineWorld and mineWorld:FindFirstChild(S(290))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(421)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(239)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(77)),
        Nodes = plot:FindFirstChild(S(34)),
        Chest = plot:FindFirstChild(S(582)),
        Zones = plot:FindFirstChild(S(251)),
        Crew = plot:FindFirstChild(S(660)),
        Loot = plot:FindFirstChild(S(267)),
        PlotIndex = plot:GetAttribute(S(518)),
        ChestCapacity = plot:GetAttribute(S(289)),
    }
end
return getPlotParts
end
modules[S(615)] = function()
return function(import)
    local Players = game:GetService(S(483))
    local createWindow = import(S(702))(import)
    local i18n = import(S(274))
    local antiAfk = import(S(87))(import)
    local interactionLogger = import(S(220))(import)
    local scheduler = import(S(288))(import)
    local config = import(S(14))(import)
    local log = import(S(634))
    local window = createWindow({
        Subtitle = S(408),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local getCurrentPlot = import(S(173))
    local getPlotParts = import(S(802))
    local dumpInstance = import(S(157))
    local dumpRemotes = import(S(37))(import)
    local dumpSellCards = import(S(782))(import)
    local dumpGui = import(S(527))(import)
    local dumpChest = import(S(797))(import)
    local inspectHandlers = import(S(721))(import)
    local probeEnv = import(S(558))(import)
    local dumpState = import(S(623))(import)
    local humanoid = import(S(352))
    local autoMine = import(S(614))(import)
    local autoPlace = import(S(110))(import)
    local autoChest = import(S(231))(import)
    local autoSell = import(S(653))(import)
    local autoUpgrade = import(S(526))(import)
    local autoResearch = import(S(248))(import)
    scheduler.RegisterStandalone(S(91), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(534), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(649), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(100), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(689), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(184), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(488), S(462))
    persistentToggle(farmTab:AddCard(
        S(212),
        S(328)
    ), S(608), autoMine.SetEnabled)
    persistentSlider(farmTab, S(314), S(787),
        1, 30, S(322), 5, autoMine.SetCyclesPerSecond, S(268))
    persistentSlider(farmTab, S(97), S(510),
        1, 10, S(262), 1, autoMine.SetClicksPerCycle, S(472))
    persistentToggle(farmTab:AddCard(
        S(662),
        S(143)
    ), S(629), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(52), S(605),
        1, 60, S(564), 3, autoPlace.SetCheckInterval, S(489))
    persistentToggle(
        farmTab:AddCard(S(592),
            S(76)),
        S(435), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(742),
        S(310)
    ), S(442), autoChest.SetEnabled)
    persistentSlider(farmTab, S(375), S(815),
        50, 3000, S(646), 100, autoChest.SetMinItems, S(730))
    persistentSlider(farmTab, S(145), S(784),
        1, 60, S(659), 3, autoChest.SetCheckInterval, S(264))
    persistentToggle(farmTab:AddCard(
        S(397),
        S(21)
    ), S(281), autoSell.SetEnabled)
    persistentSlider(farmTab, S(380), S(706),
        10, 300, S(655), 45, autoSell.SetCheckInterval, S(192))
    persistentToggle(farmTab:AddCard(
        S(46),
        S(799)
    ), S(639), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(519), S(427),
        5, 300, S(594), 20, autoUpgrade.SetInterval, S(204))
    persistentSlider(farmTab, S(413), S(156),
        1, 20, S(759), 5, autoUpgrade.SetMaxPerPass, S(740))
    persistentSegmented(
        farmTab,
        { S(93), S(131), S(547), S(667) },
        { S(821), S(479), S(150), S(62) },
        S(132), 1, autoUpgrade.SetPriority
    )
    persistentToggle(
        farmTab:AddCard(S(723),
            S(306)),
        S(102), autoUpgrade.SetBuyPickaxe
    )
    persistentToggle(farmTab:AddCard(
        S(66),
        S(121)
    ), S(366), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(83), S(409),
        10, 600, S(505), 30, autoResearch.SetCheckInterval, S(404))
    persistentSlider(farmTab, S(259), S(249),
        1, 20, S(571), 5, autoResearch.SetMaxPerPass, S(59))
    local debugTab = window:AddTab(S(383), S(516))
    persistentToggle(
        debugTab:AddCard(
            S(487),
            S(372)
        ),
        S(714),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(683), S(152))
    local checkBtn
    checkBtn = debugCard:AddButton(S(817), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(790)
            log(S(477))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(242),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(1),
        S(612)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(244), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(252)
            return
        end
        local ok, err = log(S(405))
        if not ok then
            dumpBtn.Text = S(200)
            dumpCard:SetSubtitle(S(668) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(319) .. firstNode.Name .. S(253))
            dumpInstance(firstNode, log)
        else
            log(S(226))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(275) .. firstHolder.Name .. S(640))
            dumpInstance(firstHolder, log)
        else
            log(S(822))
        end
        dumpBtn.Text = S(460)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(295),
        S(389)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(101), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(602))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(549))
        if not tool then
            toolDumpBtn.Text = S(461)
            return
        end
        log(S(642) .. tool.Name .. S(753))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(298) .. attrName .. S(563) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(141)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(104),
        S(415)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(499), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(186)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(820),
        S(611)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(139), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(621)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(278),
        S(255)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(716), function()
        dumpGui()
        guiDumpBtn.Text = S(194)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(89),
        S(72)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(144), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(94)
    end)
    local handlersCard = debugTab:AddCard(
        S(153),
        S(115)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(673), function()
        inspectHandlers()
        handlersBtn.Text = S(541)
    end)
    local probeCard = debugTab:AddCard(
        S(652),
        S(334)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(601), function()
        probeEnv()
        probeBtn.Text = S(238)
    end)
    local stateCard = debugTab:AddCard(
        S(692),
        S(628)
    )
    local stateBtn
    stateBtn = stateCard:AddButton(S(19), function()
        dumpState()
        stateBtn.Text = S(297)
    end)
    humanoid.Start()
    persistentSlider(debugTab, S(750), S(749),
        8, 200, S(806), 16, humanoid.SetWalkSpeed, S(514))
    persistentSlider(debugTab, S(282), S(27),
        30, 300, S(172), 50, humanoid.SetJumpPower, S(2))
    persistentToggle(
        debugTab:AddCard(S(676), S(33)),
        S(312), humanoid.SetFlying
    )
    persistentToggle(
        debugTab:AddCard(S(324), S(490)),
        S(188), humanoid.SetNoclip
    )
    persistentToggle(
        debugTab:AddCard(S(751), S(529)),
        S(578), humanoid.SetFloating
    )
    local settingsTab = window:AddTab(S(552), S(808))
    persistentToggle(
        settingsTab:AddCard(S(388), S(250)),
        S(684),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(812), 100)
    settingsTab:AddSlider(S(398), S(379), 50, 150, savedScale, function(value)
        config.Set(S(576), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(148), 1)
    settingsTab:AddSegmented({ S(217), S(688), S(182) }, savedLanguage, function(index, value)
        local code = ({ S(679), S(387), S(700) })[index]
        config.Set(S(475), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(361), S(197), S(580) })[savedLanguage] or S(64))
    log(S(385) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        humanoid.Stop()
    end)
end
end
modules[S(503)] = function()
return {
    [79389059854988] = S(296),
}
end
import(S(698))(import)
