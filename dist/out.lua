-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Auto Research: รอบเช็ค", "%$%s*([%d,%.]+)%s*(%a*)", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "server didn't equip it", "ดีบัก", "(", "games/my_anime_mine/auto_research", "dev", " = ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/logger", "th", "Auto Sell: รอบขาย", "disabled", ")", ": ", "games/my_anime_mine/auto_mine", "[AutoChest] enabled (collects once >= ", "[AutoPlace] unlocking ", "core/i18n", "Players", "games/my_anime_mine/auto_claim", "[AutoPlace] ", "Workspace", "Chest", "noclip", "core/ui", "Auto Place Miner", " for ", "core/config", "RunService", "MineEquipPickaxe", "[AutoChest] disabled", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "%.0f", "core/logger", "", "core/anti_afk", "TextLabel", "fill", "core/parse_money", "AutoChest", "ลอย (Float)", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "core/debug_interaction_logger", "Auto Upgrade: กันเงินไว้", "=", "MekPro", "MineSpeed", "[Interaction] ClickDetector clicked: ", " (", "ตั้งค่า", "", "[Decompile] decompile unavailable in this executor", "autoMineSpeed", "ขนาดเมนู", "M", "core/ui", "", "fill", "[AutoUpgrade] pickaxe #", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "auto", "core/run_job", "Plots", "getconnections", "[Scheduler] turn: ", "TextBox", " waited ", "PlotIndex", "ปุ่มเปิดเมนู", "Auto Chest: รอบเช็ค", "autoPlaceUpgradeWeak", "autoPlotLatest", "CharMineSpeedMaxLevel", "MekPro", "เช็คโซนทุกกี่วินาที", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "getsenv", ")", "CoreGui", " (", "autoPlotKeepMoney", ",", "", "AutoClaim", "Auto Place: เปลี่ยนตัวอ่อนออก", " failed)", "[AutoResearch] nothing open and affordable right now", "[Decompile] ", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "[Scheduler] ", "[AutoResearch] cheapest open node: ", "UIStroke", "All", "JumpPower (ปกติ 50)", "This is not a supported place.", "[Remotes] === total: ", " (Text=", "/", "[AutoPlace] picked up ", "Mutation: เพดานราคาต่อครั้ง", "getloadedmodules", "GetCharacterUpgradeCost", "มุดแมพ (Noclip)", "Auto Mine: ความเร็ว", " ", "Workspace", "/", "/", "ee4d27c", " (still ", "AutoResearch", "TextLabel", "TextLabel", "TextLabel", "([%d,%.]+)%s*(%a*)", "MineWorld", "[AutoUpgrade] ", "Auto Claim (Index)", "ZoneBoardGui", "TabBar", "[GameAPI] couldn't load the game's modules: ", "core/logger", "[GuiDump] === end (", "empty", "", "[AutoPlace] unlocked ", " script(s) to ", "MineUpgradeCharacter", ")", "Frame", "autoUpgradeInterval", "[AntiAFK] enabled - watching Player.Idled", "M", " (power ", " (", " still empty after placing ", "Auto Claim: เก็บสูงสุดต่อรอบ", "button has no size on screen", " (", "core/debug_tab", "-", ") needs ", "ReplicatedStorage", "Auto Research: กันเงินไว้", "Auto Chest: เกณฑ์เก็บ", "Frame", "s", "needs us at the stand - keeping the teleport", "[Logger] failed to write ", "Mining", "games/my_anime_mine/get_current_plot", "leaderstats", "[AutoChest] waiting: ", " has been busy for ", "%.0f", "autoClaim", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "%$", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "ProximityPrompt", "ClickDetector", " #", "walkSpeed", ".lua", "[AutoUpgrade] bought pickaxe #", " ชิ้น", "debug.getconstants", " stack(s)", "WalkSpeed (ปกติ 16)", ", have ", "unlock", "ZoneBoard", "ย่อขยายเมนูทั้งชุด", "float", "waiting", "s", "core/dispatch", "hookfunction", "string", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "TextLabel", "%.3g", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/game_api", "core/debug_dump_remotes", "enabled", "ZoneRow", "[Interaction] Key pressed: %s (gameProcessed=%s)", "enabled", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " didn't go through (out of stock or too expensive)", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "upgrade", "[MekPro] build ", "Zones", "[^%w]+", "games/my_anime_mine/auto_chest", "autoUpgradeLimitPickaxes", "Players", "PlayerGui", "[GameAPI] SendEvent failed: ", "/", "", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "MekPro", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "UIScale", "[AutoPlace] nothing in the hotbar to place", "MineTakeChest", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "[Decompile] === end ===", " x", "autoResearchCore", ")", "(nothing to do)", "", "core/dispatch", "decompile", "[BestMiner] slot ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " = ", " files", " research is ", " -> ", "autoChestInterval", "[AutoPlace] fireproximityprompt error: ", "works from anywhere - no teleport needed", "GetHolderSpeedLevel", "[Decompile] PlayerScripts not found", "[AutoUpgrade] MineBuyPickaxe ", "function", "[AutoMine] ", "Damage", "antiAfk", "debug.getproto", "open_menu_hint", " ", "slot ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "\n", "core/teleport", "ClickDetector", "AutoPlot", "nothing left to buy (", "lowest", "[AutoClaim] ", " costs ", "Frame", "table", "string", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "function", "Auto Upgrade: สุ่ม Mutation", "Hauler", " from ", "[AutoPlace] drifted out of range of ", "Holders", "core/run_job", "PlayerScripts", "Frame", "Speed", "", ")", "[AutoResearch] ", " '", "RemoteFunction", "🐛", "string", "AutoSell", "getgc", " not bought yet (", "autoMineClicks", "GetHolderLevel", " node(s) looked buyable but the server refused", "auto", "s", "s - jumping the queue", "_", "CoreUI", "x", "", ")", "HumanoidRootPart", ")", "MineSpeed", "autoUpgradePickaxe", "no isfile/readfile available to verify the write", "^%$", "search_placeholder", "AutoUpgrade", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "TextLabel", " items (now ", "autoSellProtectUnits", "BasePart", "[AutoPlace] upgrading ", "core/i18n", "menuScale", "games/my_anime_mine/auto_research", " (x", "[Scheduler] ", "", "core/teleport", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "Auto Mine: คลิกต่อรอบ", " refused - ", "table", "MekPro/decompiled", "disabled", "table", "games/registry", "games/my_anime_mine/get_best_miner", "Workspace", ") ", "x", "Speed", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "ไล่อัปเกรด miner ทุกกี่วินาที", "sold ", "อังกฤษ", "Interaction Logger", "games/my_anime_mine/main", "s", "interactionLogger", "BasePart", "/", "neither appendfile nor writefile is available", ": ", "^M(%d+)$", "auto", "[Config] couldn't write ", "core/logger", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " (", "'", "2026-09-21 12:38:05", "[AutoChest] chest is empty", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/get_plot_parts", "VirtualUser", "Players", " pickaxe ", "slot not in the hotbar", "[AutoClaim] ", "Auto Plot: รอบเช็ค", "autoUpgradeMutationBudget", "UIPadding", "games/my_anime_mine/auto_plot", "Empty miner slot", "string", "table", "autoPlotInterval", "%$", "\n", "แรงกระโดด", "languageIndex", "MekPro.log", " -> x", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "games/my_anime_mine/game_api", "action '", "[GuiDump] === visible PlayerGui tree ===", " costs ", "  ", ") -> ", "TextButton", "core/get_place_id", "core/config", "autoUpgradePriority", "[AutoChest] collected ", "autoSellMaxWait", "%.3g", "HttpService", "both Zone and Core buying are switched off", "Auto Upgrade: Pickaxe ที่จะซื้อ", "th", ")", ") from ", " -> ", "ProximityPrompt", "TextLabel", "[AutoResearch] ", " (", " ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "core/run_job", "TextLabel", "Frame", "^([%d,]+)$", "getnilinstances", "menuScale", "?", ": ", "'", " (", " (x", "UIListLayout", "Character", "MineBuySkill", "[AutoSell] enabled", "cost", "", "')", "may need us at the stand - trying that", "core/get_leaderstat", "RunService", "Auto Research: ซื้อสาย Zone", "Modules", "games/my_anime_mine/game_api", "Players", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "Frame", " ", "core/logger", "Open menu", " ", "en", "AutoPlace", "", " power ", " pickaxe(s) for ", " -> ", "ProximityPrompt", "Players", "games/registry", "core/logger", "enabled", "[AutoSell] disabled", "MineBuyPickaxe", "SellStand", "PlayerGui", "Dump", "[AutoPlace] holders: ", "AutoPlace", "Anti-AFK", "s", "autoPlace", "[AutoPlot] unlocked ", "ChestCapacity", "AutoPlot", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "core/teleport", "[AutoResearch] Core ", "Auto Upgrade Unit", "autoSellMaxWait", "s", "TextLabel", "Badge", "core/humanoid", "My Anime Mine · Mining Hub", "autoResearch", "autoUpgradeMaxPerPass", "GuiButton", "Humanoid", "TextButton", " ", " ", "core/scheduler", "autoResearchCoreLast", "TextButton", ")", "th", ")", "M", " reward(s) looked claimable but none went through", " still locked after paying ", "TextLabel", "core/build_env", " (power ", "Auto Mine", "Text", "core/humanoid", "Search functions...", "ProximityPrompt", "MineRollMutation", " -> ", "Zone buying is off", "ไทย", "เดินทะลุกำแพงและพื้น", "Auto Upgrade: แจก Pickaxe ให้ miner", "Auto Upgrade Plot (Zone)", "needs ", "  [attr] ", "s - carrying on without it", "works from anywhere - dropping the teleport", "sale didn't go through", "TextLabel", "Players", "function", "[AutoUpgrade] ", "Hauler", "core/run_job", "table", "yes", "Frame", "", "Content", "core/debug_dump_remotes", ")", "games/my_anime_mine/auto_chest", "enabled", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/game_api", "Auto Plot: ใช้โซนล่าสุดเสมอ", " items", "core/logger", "TextButton", "GuiButton", " items)", "MineEquipSlot", "", "core/logger", " (cost ", "number", "autoUpgradeRollMutation", "", " ", "[AutoPlace] ", "ValueLabel", " (", "cheapest open node ", "HumanoidRootPart", "disabled", "TextButton", "TextButton", "Players", "[AutoPlace] placed ", "core/debug_tab", "M", "[AutoUpgrade] ", "M", "games/my_anime_mine/get_current_plot", "autoPlotBuy", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "#", "ReplicatedStorage", "research", "Workspace", "[AutoChest] MineTakeChest had no effect (", "[AutoPlot] couldn't switch to ", "Run", "/config.json", " (Text=", "ต่ำสุดก่อน", "locked slot", "[", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "", "games/my_anime_mine/auto_place", "", "MineSpeed", "core/debug_interaction_logger", "none", "autoUpgradeEquipPickaxe", "autoChest", "autoUpgradeKeepMoney", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "[AntiAFK] idle detected, simulating input", "Auto Place: ปลดล็อกช่อง miner", "MineSellItems", "research", "Workspace", "[MekPro] settings file: ", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "ModuleScript", " | ", "affordable - waiting for a turn", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", " ===", "Crew", "^x(%d+)$", "games/my_anime_mine/auto_sell", "LayerCollector", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "no", "games/my_anime_mine/auto_place", "[Decompile] === executor capabilities ===", "jumpPower", ")", "table", "Decompile Client Scripts", "core/get_place_id", "Dump All Remotes", "", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "HumanoidRootPart", "[AutoPlace] couldn't hold anything - not pressing E", "games/my_anime_mine/auto_upgrade", "0 = ไม่จำกัด", "Players", "th", "[AntiAFK] disabled", "core/logger", "Pad", "core/parse_money", "core/debug_decompile", "Content", "TextLabel", "games/my_anime_mine/auto_sell", "LocalScript", "  ", "autoResearchMaxPerPass", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "ScrollingFrame", "", "MineSelectZone", "games/my_anime_mine/game_api", "autoPlaceUnlock", "table", "VirtualInputManager", "core/logger", "Auto Sell", "core/run_job", "core/get_place_id", "Auto Sell: รอคิวได้นานสุด", "AutoSell", "disabled", "core/debug_dump_gui", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "core/run_job", "ขายทุกกี่วินาที", "table", "OwnerUserId", "Auto Plot: กันเงินไว้", "AutoResearch", "getcallbackvalue", "Backpack", "s", ")", ") didn't take: ", "core/scheduler", "write reported success but ", "cost", "Main", "ฟาร์ม", "table", "TextLabel", "Players", "prompt disabled", "AutoChest", " possible holder(s) - not buying more", "[AutoPlace] slot ", "core/logger", "UICorner", "TextLabel", "/", "UICorner", "autoUpgrade", "Plots", "อ่านป้ายบน chest ทุกกี่วินาที", "write reported success but the line isn't in ", "autoUpgradePickaxeIndex", "costs ", "AutoMine", "[Config] couldn't read ", "", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "Damage", " while selecting", " (", "autoClaimInterval", "autoSellInterval", "[Scheduler] ", "ValueLabel", ", ", "[AutoClaim] claimed ", "GuiObject", "[AutoUpgrade] ", "ไล่ซื้อทุกกี่วินาที", "[Decompile] wrote ", "UserInputService", " for ", "Players", "Auto Plot: ซื้อโซนถัดไป", "List", " -> ", "Core buying is off", "games/my_anime_mine/get_best_miner", "[Interaction] ProximityPrompt triggered: ", "core/anti_afk", "core/parse_money", "", "autoMine", "[AutoUpgrade] ", "Frame", "GetHolderMineSpeedLevel", "core/logger", "en", "core/get_me", "autoPlot", "[AutoPlot] ", "core/build_env", "MinePower", "autoPlaceInterval", ",", " - upgrade the miners first", "core/i18n", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Header", "table", "games/my_anime_mine/auto_mine", "lowest", "Damage", "[%X] ", " ", "core/debug_dump_gui", "HotbarFrame", ": ", "Search", "games/my_anime_mine/game_api", "core/logger", "GuiService", "=empty", "autoSell", "core/logger", "TextLabel", "Text", "ItemFrame", "Humanoid", " wouldn't take ", ") ", "Frame", "🏠", "UserInputService", " owned)", "games/my_anime_mine/game_api", "MineGui", "s", "Damage", "] job failed: ", "บิน (Fly)", "Dump", "Auto Place: เพดานราคาปลดช่อง", ",", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", " doesn't exist", "MekPro", "UIListLayout", ")", ": ", "table", "[^%.]+", "maxed", "", "autoUpgradePickaxeIndex", " screen(s)) ===", "[GuiDump] ", "", "'", "games/my_anime_mine/auto_claim", "Frame", "Container", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "core/logger", ": ", "X", " (", "table", "", "Root", "[AutoSell] ", "Nodes", "[AutoPlot] now mining ", "Core", "core/run_job", "[AutoResearch] ", "string", "Players", "enabled", "AutoUpgrade", "ค้นหาฟังก์ชัน...", "en-us", ")", "MineWorld", "Players", "[AutoPlot] ", "ScreenGui", "", "Content", "Auto Research (Plot)", "languageIndex", " error: ", "BasePart", "[Interaction] Button activated: ", "none", "auto", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", " (", "RemoteEvent", " team power, you have ", "core/logger", "autoResearchCheapestFirst", "ClickDetector", "UserInputService", "[GuiDump] PlayerGui not found", "⚙", "unlock", "autoResearchInterval", "search_placeholder", "[Bb]uy", "Loot", "", " mutation x", "[Remotes] ", "games/my_anime_mine/auto_plot", " (prompt says '", " (money ", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Auto Chest", "table", "games/my_anime_mine/game_api", "open_menu_hint", "ถูกสุดก่อน", "autoChestMinItems", "getscriptbytecode", "[AutoUpgrade] ", "string", "BasePart", "every open node is maxed", "[Decompile] === start ===", "Shop Stand", " ", "Mining", " (", "ItemName", "TextLabel", "AutoPlace", "getrenv", "core/teleport", "slot past the usable cap", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/main", "autoUpgradeMutationTarget", "UIListLayout", " (ActionText=", "[Uu]nlock", "waiting", "[AutoChest] can't read the chest yet", "debug.getupvalues", "games/my_anime_mine/game_api", "[AutoPlot] ", "TextButton", "TextButton", " team power", "autoPlaceUnlockBudget", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "Dump Visible GUI", ") ", "Workspace", "[AutoResearch] idle: ", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "TextLabel", "[Interaction] logger disabled", "%", "Players", "core/click", "x", "getscripts", "=locked(", "Frame", ", base ", "Auto Claim: รอบเช็ค", "autoResearchKeepMoney", "Locked miner slot", "[AutoUpgrade] every stat on every miner is maxed", " remotes", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "autoClaimMaxPerPass", "unreadable", "Auto Research: ซื้อสาย Core", " '", "ความเร็ววิ่ง", "core/logger", " (chest holds ", "ตามเครื่อง", "0 = ไม่จำกัด", "PickaxesStand", "fly", "%.3g", "AutoClaim", "/s", "/", "[AutoPlace] disabled", " screens", "[AutoResearch] ", "games/my_anime_mine/game_api", "CharMaxLevel", "AutoPlace", "x", ", built ", "nothing to place or upgrade", "empty", "Mutation: หยุดเมื่อได้ x", "BodyVelocity", ")", "lowest", "core/logger", "[Interaction] logger enabled - go click/interact with things now", "needs ", "autoResearchZones", "Auto Place: รอบเช็ค", " ", "[AutoPlace] couldn't pick up ", "BodyPosition", "แพงสุดก่อน", "[AutoPlace] ", "core/run_job", ", you have ", ")", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "GetCharacterSpeedUpgradeCost", "disabled", "PlayerGui", "Speed", "OwnerUserId", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", ")", "Auto Research: ซื้อสูงสุดต่อรอบ", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", ") on ", "Badge", "en", "unreadable", "CharSpeedMaxLevel", "Auto Upgrade: รอบเช็ค", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "x", "core/logger", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "GetCharacterMineSpeedUpgradeCost", "core/logger", "/", ") ", "table", "TextButton", "[AutoSell] MineSellItems ", "core/debug_decompile"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(684)] = function()
return function(import)
local log = import(S(936))
local Players = game:GetService(S(592))
local VirtualUser = game:GetService(S(342))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(134))
        connection = player.Idled:Connect(function()
            log(S(560))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(594))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(470)] = function()
return { Env = S(8), IsDev = true, Commit = S(111), BuiltAt = S(338) }
end
modules[S(861)] = function()
local VirtualInputManager = game:GetService(S(612))
local GuiService = game:GetService(S(716))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(140)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(370)] = function()
return function(import)
local HttpService = game:GetService(S(375))
local getPlaceId = import(S(616))
local log = import(S(416))
local ROOT_FOLDER = S(76)
local placeFolder = ROOT_FOLDER .. S(648) .. tostring(getPlaceId())
local filePath = placeFolder .. S(544)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(251) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(657) .. filePath .. S(395) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(333) .. filePath .. S(16) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(942)] = function()
return function(import)
local Players = game:GetService(S(201))
local log = import(S(691))
local CAPABILITIES = {
    S(220), S(820), S(863), S(272), S(103),
    S(66), S(628), S(79), S(833), S(169),
    S(844), S(237), S(180), S(392),
}
local OUT_FOLDER = S(310)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(747)) do
        if type(target) ~= S(624) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(579))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(90) .. name .. S(9) .. ((ok and type(value) == S(233)) and S(496) or S(577)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(54))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(261))
    if not playerScripts then
        log(S(231))
        return 0
    end
    pcall(makefolder, S(742))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(602)) or target:IsA(S(567)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(270) and #source > 0 then
                local name = target:GetFullName():gsub(S(198), S(280))
                if pcall(writefile, OUT_FOLDER .. S(110) .. name .. S(166), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(674) .. written .. S(129) .. OUT_FOLDER
        .. S(141) .. failed .. S(88))
    return written
end
local function run()
    log(S(825))
    reportCapabilities()
    local count = dumpAll()
    log(S(213))
    return count
end
return run
end
end
modules[S(710)] = function()
return function(import)
local Players = game:GetService(S(677))
local log = import(S(759))
local function dumpVisible(root, indent)
    indent = indent or S(807)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(671)) and child.Visible then
            local line = indent .. child.ClassName .. S(876) .. child.Name .. S(754)
            if child:IsA(S(115)) or child:IsA(S(462)) then
                line = line .. S(545) .. tostring(child.Text) .. S(216)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(485) .. attrName .. S(223) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(366))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(918))
    if not playerGui then
        log(S(800))
        return 0
    end
    local screens = 0
    log(S(364))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(575)) and screen.Enabled and screen.Name ~= S(207) then
            screens = screens + 1
            log(S(752) .. screen.ClassName .. S(267) .. screen.Name .. S(396))
            dumpVisible(screen, S(603))
        end
    end
    log(S(125) .. screens .. S(751))
    return screens
end
return dumpGui
end
end
modules[S(500)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(145))
local log = import(S(11))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(794)) or descendant:IsA(S(268)) then
            count = count + 1
            log(S(809) .. descendant.ClassName .. S(330) .. descendant:GetFullName())
        end
    end
    log(S(98) .. count .. S(571))
    return count
end
return dumpRemotes
end
end
modules[S(45)] = function()
return function(import)
local Players = game:GetService(S(412))
local Workspace = game:GetService(S(108))
local UserInputService = game:GetService(S(799))
local log = import(S(514))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(683) .. describe(prompt)
            .. S(840) .. prompt.ActionText .. S(15))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(50) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(789) .. describe(button)
            .. S(99) .. tostring(button.Text) .. S(501))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(476)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(163)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(510)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(858))
        return
    end
    connections = {}
    log(S(903))
    local playerGui = Players.LocalPlayer:WaitForChild(S(202))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(425)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(244)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(455)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(190),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(530)] = function()
return function(import)
local interactionLogger = import(S(554))(import)
local dumpRemotes = import(S(187))(import)
local dumpGui = import(S(620))(import)
local decompileScripts = import(S(598))(import)
local humanoid = import(S(451))
local function build(window, persist)
    local tab = window:AddTab(S(5), S(269))
    persist.Toggle(
        tab:AddCard(
            S(323),
            S(924)
        ),
        S(326),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(585),
        S(872)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(736), function()
        remoteBtn.Text = dumpRemotes() .. S(871)
    end)
    local guiCard = tab:AddCard(
        S(852),
        S(78)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(434), function()
        guiBtn.Text = dumpGui() .. S(889)
    end)
    local decompileCard = tab:AddCard(
        S(583),
        S(758)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(543), function()
        decompileBtn.Text = decompileScripts() .. S(224)
    end)
    humanoid.Start()
    persist.Slider(tab, S(877), S(171),
        8, 200, S(165), 16, humanoid.SetWalkSpeed, S(550))
    persist.Slider(tab, S(357), S(96),
        30, 300, S(580), 50, humanoid.SetJumpPower, S(607))
    persist.Toggle(
        tab:AddCard(S(735), S(661)),
        S(883), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(105), S(481)),
        S(26), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(43), S(413)),
        S(176), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(219)] = function()
return function(import)
    local getPlaceId = import(S(584))
    local registry = import(S(427))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(97))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(407)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(154))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(693)] = function()
local Players = game:GetService(S(780))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(723))
    local backpack = player:WaitForChild(S(629))
    return player, humanoid, backpack
end
return getMe
end
modules[S(369)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(474)] = function()
local Players = game:GetService(S(490))
local RunService = game:GetService(S(31))
local UserInputService = game:GetService(S(675))
local Workspace = game:GetService(S(540))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(456))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(285))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(899))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(327)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(297)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(909))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(299)] = function()
local Players = game:GetService(S(343))
local STRINGS = {
    en = {
        search_placeholder = S(475),
        open_menu_hint = S(417),
    },
    th = {
        search_placeholder = S(776),
        open_menu_hint = S(71),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(777)
    if locale:sub(1, 2):lower() == S(464) then
        return S(593)
    end
    return S(927)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(791) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(796)] = function()
local LOG_FILE = S(359)
local function log(message)
    local line = os.date(S(708)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(783))
            end
            appendfile(LOG_FILE, line .. S(242))
        elseif writefile then
            local existing = S(421)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(356))
        else
            error(S(329))
        end
        if not (isfile and readfile) then
            error(S(289))
        end
        if not isfile(LOG_FILE) then
            error(S(634) .. LOG_FILE .. S(741))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(653) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(151) .. LOG_FILE .. S(760) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(597)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(352) then
        return nil
    end
    local number, suffix = text:match(S(2))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(84), S(404))))
    if not value then
        return nil
    end
    if suffix ~= S(304) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(912)] = function()
return function(import)
local log = import(S(124))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(548) .. name .. S(734) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(460)] = function()
return function(import)
local RunService = game:GetService(S(408))
local log = import(S(878))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(92) .. task_.name .. S(787) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(667) .. task_.name .. S(156)
                    .. string.format(S(157), now - task_.busySince)
                    .. S(486))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(303) .. starved.name .. S(69)
            .. string.format(S(35), now - starved.waitingSince)
            .. S(279))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(67) .. (currentTask and currentTask.name or S(217)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(243)] = function()
local Players = game:GetService(S(21))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(524))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(27)] = function()
return function(import)
local i18n = import(S(20))
local UserInputService = game:GetService(S(728))
local THEME = {
    BrandName = S(48),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(646))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(649))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(349))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(940))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(59)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(94))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(489))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(394)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(469))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(513)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(606))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(839))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(647))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(390))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(831))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(449))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(39))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(521))
            if not valueLbl then
                valueLbl = Instance.new(S(114))
                valueLbl.Name = S(668)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(926))
            if not badge then
                badge = Instance.new(S(600))
                badge.Name = S(450)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(848))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(414))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(250))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(526))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(586)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(859)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(148))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(497))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(847))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(658)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(689))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(743))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(509))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(721)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(782))
    screenGui.Name = S(281)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(81))
    local main = Instance.new(S(262))
    main.Name = S(636)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(209))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(865))
    tabBar.Name = S(122)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(399))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(756))
    header.Name = S(703)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(457))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(639))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(389))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(764)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(527))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(761)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(368))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(143)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(68))
    searchBox.Name = S(713)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(291))
    searchBox.Text = S(218)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(132))
    content.Name = S(499)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(726))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(383))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(817))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(857))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(473)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(518) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(386) .. (item.Subtitle or S(85))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(804))
        footerLabel.Text = i18n.T(S(238))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(199)] = function()
return function(import)
local gameApi = import(S(845))(import)
local log = import(S(595))
local runGuarded = import(S(622))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(18) .. minItemsToCollect .. S(511))
    else
        log(S(33))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(928) then
            log(S(843))
            lastReported = S(874)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(897) then
            log(S(339))
            lastReported = S(126)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(177) then
            local capacity = gameApi.ChestCapacity()
            log(S(155) .. count .. S(328) .. want .. S(507)
                .. (capacity and (S(879) .. capacity .. S(379)) or S(749)))
            lastReported = S(842)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(642), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(372) .. count .. S(295) .. after .. S(922))
        else
            log(S(541) .. count
                .. S(381) .. tostring(after) .. S(465))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(755)] = function()
return function(import)
local gameApi = import(S(362))(import)
local log = import(S(902))
local runGuarded = import(S(615))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(346) .. (enabled and S(503) or S(311)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(670) .. reward.category .. S(239)
                .. tostring(reward.tierKey) .. S(164) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(248) .. #pending .. S(467))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(885), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(17)] = function()
return function(import)
local getCurrentPlot = import(S(504))
local getPlotParts = import(S(185))
local log = import(S(719))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(798), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(234) .. (enabled and S(429) or S(619)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(551)] = function()
return function(import)
local Players = game:GetService(S(426))
local getCurrentPlot = import(S(153))
local getPlotParts = import(S(341))
local getMinerSlots = import(S(314))(import)
local teleport = import(S(305))
local parseMoney = import(S(685))
local gameApi = import(S(609))(import)
local log = import(S(933))
local runGuarded = import(S(494))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(351)
local LOCKED_SLOT_TEXT = S(869)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(596))
    return pad and pad:FindFirstChildWhichIsA(S(162), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(116)) then
            local digits = descendant.Text:match(S(391))
            if digits then
                return tonumber((digits:gsub(S(699), S(552))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(183)) and descendant.Text:match(S(290)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(240) .. holder.Name .. S(273)
            .. gameApi.MinerSlots() .. S(100) .. gameApi.MaxMinerSlots() .. S(729)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(547)
    end
    if not prompt.Enabled then
        return true, S(641)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(655) .. price
    end
    local action = prompt.ActionText or S(498)
    if action:match(S(841)) or action:match(S(805)) or action:match(S(160)) then
        return true, S(363) .. action .. S(337)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(588))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(228) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(864) .. why .. S(914))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(717))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(47) .. prompt.ObjectText
                    .. S(6) .. shown .. S(866) .. math.floor(base) .. S(265))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(568))
    if line ~= lastSurvey then
        log(S(435) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(40), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(195), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(173), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(23) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(888))
    else
        lastAttempt = 0
        log(S(702))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(605),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(345)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(835)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(4)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(210))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(258) .. holder.Name .. S(663))
            return false
        end
        log(S(644) .. candidate.slot .. S(762) .. candidate.name
            .. S(632) .. tostring(why))
    end
    if not miner then
        log(S(589))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(911) .. holder.Name .. S(138) .. miner.name)
        return false
    end
    log(S(529) .. miner.name .. S(136) .. miner.power
        .. S(925) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(908) .. entry.name .. S(257) .. entry.holder.Name
            .. S(811) .. tostring(entry.prompt.ActionText) .. S(405))
        return false
    end
    log(S(101) .. entry.name .. S(471) .. entry.power
        .. S(380) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(520) .. pad.holder.Name .. S(468)
                .. tostring(pad.priceText))
        else
            log(S(128) .. pad.holder.Name
                .. S(29) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(896))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(60) then
        runGuarded(S(832), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(802) then
        log(S(19) .. target.holder.Name
            .. S(676) .. tostring(target.priceText))
        runGuarded(S(420), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(298) .. target.holder.Name .. S(745) .. target.name
        .. S(522) .. target.power .. S(367) .. best.name .. S(51) .. best.power .. S(463))
    runGuarded(S(436), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(810)] = function()
return function(import)
local gameApi = import(S(816))(import)
local parseMoney = import(S(41))
local log = import(S(36))
local runGuarded = import(S(64))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(846) .. (enabled and S(188) or S(525)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(355)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(768) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(336) .. deepestKey .. S(286))
        return true
    end
    log(S(542) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(781) .. gameApi.ZoneDisplayName(zoneKey) .. S(365)
                .. math.floor(price) .. S(913) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(440) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(137) .. zoneKey .. S(80))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(539) then
            reportedLocked = S(563)
            log(S(695) .. zoneKey .. S(308)
                .. gameApi.ZoneDisplayName(currentKey) .. S(225)
                .. done .. S(937) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(245), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(7)] = function()
return function(import)
local gameApi = import(S(730))(import)
local log = import(S(334))
local runGuarded = import(S(388))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(266) .. (enabled and S(191) or S(14)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(384) .. node.zoneKey .. S(107) .. node.id
                    .. S(664) .. node.family .. S(938) .. node.level .. S(226) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(445) .. node.id
                    .. S(397) .. node.family .. S(725) .. node.level .. S(680) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(890) .. blocked
                .. S(276))
        elseif powerBlocked then
            log(S(771) .. powerBlocked.zoneKey .. S(418) .. powerBlocked.id
                .. S(829) .. powerBlocked.family .. S(144)
                .. math.floor(powerBlocked.powerReq) .. S(795)
                .. math.floor(powerBlocked.power)
                .. S(700))
        else
            log(S(89)
                .. S(812) .. math.floor(gameApi.GetMoney()) .. S(744))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(93) .. nearest.zoneKey
                    .. S(709) .. nearest.id .. S(82) .. nearest.family .. S(853)
                    .. nearest.level .. S(109) .. nearest.max
                    .. S(249) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(376)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(769),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(479))
        end
        if not buyCore then
            table.insert(parts, S(681))
        end
        if #parts == 0 then
            return S(824)
        end
        return S(246) .. table.concat(parts, S(669)) .. S(284)
    end
    local why
    if nearest.cost > budget then
        why = S(484) .. string.format(S(184), nearest.cost)
            .. S(172) .. string.format(S(884), money)
    elseif nearest.powerOk == false then
        why = S(904) .. string.format(S(374), nearest.powerReq) .. S(849)
    else
        why = S(569)
    end
    return S(523) .. nearest.zoneKey .. S(907) .. tostring(nearest.id)
        .. S(385) .. tostring(nearest.family) .. S(316) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(855) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(113), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(601)] = function()
return function(import)
local Workspace = game:GetService(S(24))
local gameApi = import(S(891))(import)
local teleport = import(S(444))
local log = import(S(715))
local runGuarded = import(S(770))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(430))
    else
        lastAttempt = 0
        log(S(402))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(432))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(382), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(941)
                .. (sold and S(487)
                         or S(150)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(766) .. (sold and (S(321) .. #keys .. S(170)) or S(488)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(271), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(590)] = function()
return function(import)
local Workspace = game:GetService(S(315))
local gameApi = import(S(505))(import)
local teleport = import(S(834))
local log = import(S(508))
local runGuarded = import(S(260))(import)
local STATS = { S(733), S(919), S(287) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(247)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(688) .. (enabled and S(774) or S(917)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(704) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(706) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(635) or S(748)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(672) .. holderKey .. S(827) .. choice.stat .. S(459)
                        .. choice.info.level .. S(424) .. after.level
                        .. S(887) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(403) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(882))
    local shop = stand and stand:FindFirstChild(S(826))
    if not shop then
        return nil
    end
    if shop:IsA(S(823)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(788), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(232)
                .. (bought and S(229)
                           or S(406)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(851))
        end
    end
    if bought then
        log(S(167) .. pickaxe.index .. S(519) .. pickaxe.name
            .. S(302) .. pickaxe.multiplier .. S(778))
        return true
    end
    log(S(61) .. pickaxe.index .. S(458) .. pickaxe.name
        .. S(193))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(492) .. holderKey .. S(344)
                    .. tostring(worn or S(555)) .. S(478) .. wanted.name)
            else
                log(S(119) .. holderKey .. S(724)
                    .. wanted.name .. S(112) .. tostring(nowWorn or S(790)) .. S(581))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(532) .. holderKey .. S(808) .. variant
                .. S(360) .. after .. S(515) .. cost .. S(131))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(821) .. gameApi.OwnedPickaxeCount()
                    .. S(423) .. gameApi.MaxMinerSlots()
                    .. S(643))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(443))
        elseif maxed > 0 then
            log(S(870))
        else
            log(S(3))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(775), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(186)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(538))
local Workspace = game:GetService(S(854))
local log = import(S(613))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(410), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(123) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(815))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(309) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(256) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(582) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(763) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(493) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(331))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(203) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(211))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(95), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(353) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(939) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(400) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(562), keys)
end
function api.EquipSlot(slot)
    return send(S(512), slot == S(283) and S(127) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(431), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(32), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(477), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(401), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(495)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(860)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(697))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(254) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(312) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(608), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(118))
    plots = plots and plots:FindFirstChild(S(65))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(640)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(920)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(174))
    local gui = board and board:FindFirstChild(S(121))
    local root = gui and gui:FindFirstChild(S(765))
    local list = root and root:FindFirstChild(S(679))
    local row = list and list:FindFirstChild(S(189) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(294)) and descendant.Text ~= S(686) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(491) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(130), characterKey, stat == S(828) and S(49) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(275), max = S(892), cost = S(104) },
    Speed = { level = S(230), max = S(929), cost = S(916) },
    MineSpeed = { level = S(690), max = S(75), cost = S(935) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(252) or charName == S(205) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(611) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(516) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(822) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(746) and type(holder.CharName) == S(181) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(638) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(772) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(682)] = function()
return function(import)
local Players = game:GetService(S(773))
local gameApi = import(S(714))(import)
local log = import(S(645))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(117))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(738), S(753)))) or 0
    if suffix ~= S(264) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(433))
    local mineGui = playerGui and playerGui:FindFirstChild(S(731))
    local hotbar = mineGui and mineGui:FindFirstChild(S(711))
    local content = hotbar and hotbar:FindFirstChild(S(784))
    return content and content:FindFirstChild(S(757))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(720)) then
            local count = descendant.Text:match(S(573))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(722))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(599))
    local nameLabel = content and content:FindFirstChild(S(830))
    local damageLabel = content and content:FindFirstChild(S(662))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(37) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(204) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(221) .. slot .. S(712) .. info.name
                        .. S(422) .. info.power .. S(214) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(534)] = function()
local Players = game:GetService(S(528))
local Workspace = game:GetService(S(564))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(779))
    local plots = mineWorld and mineWorld:FindFirstChild(S(651))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(625)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(836)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(259)),
        Nodes = plot:FindFirstChild(S(767)),
        Chest = plot:FindFirstChild(S(25)),
        Zones = plot:FindFirstChild(S(197)),
        Crew = plot:FindFirstChild(S(572)),
        Loot = plot:FindFirstChild(S(806)),
        PlotIndex = plot:GetAttribute(S(70)),
        ChestCapacity = plot:GetAttribute(S(441)),
    }
end
return getPlotParts
end
modules[S(324)] = function()
return function(import)
    local createWindow = import(S(58))(import)
    local i18n = import(S(701))
    local antiAfk = import(S(38))(import)
    local buildEnv = import(S(696))
    local scheduler = import(S(633))(import)
    local config = import(S(30))(import)
    local log = import(S(428))
    log(S(196) .. buildEnv.Commit .. S(793) .. buildEnv.Env
        .. S(895) .. buildEnv.BuiltAt .. S(631))
    local window = createWindow({
        Subtitle = S(452),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(705))(import)
    local autoPlace = import(S(578))(import)
    local autoChest = import(S(502))(import)
    local autoSell = import(S(574))(import)
    local autoUpgrade = import(S(340))(import)
    local autoResearch = import(S(301))(import)
    local autoClaim = import(S(22))(import)
    local autoPlot = import(S(350))(import)
    local gameApi = import(S(411))(import)
    scheduler.RegisterStandalone(S(656), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(42), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(893), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(292), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(627), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(86), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(442), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(447), 20)
    scheduler.RegisterExclusive(S(618), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(637), S(727))
    persistentToggle(farmTab:AddCard(
        S(472),
        S(241)
    ), S(687), autoMine.SetEnabled)
    persistentSlider(farmTab, S(106), S(536),
        1, 30, S(55), 5, autoMine.SetCyclesPerSecond, S(886))
    persistentSlider(farmTab, S(307), S(621),
        1, 10, S(274), 1, autoMine.SetClicksPerCycle, S(932))
    persistentToggle(farmTab:AddCard(
        S(28),
        S(34)
    ), S(439), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(906), S(306),
        1, 60, S(698), 3, autoPlace.SetCheckInterval, S(630))
    persistentToggle(
        farmTab:AddCard(S(87),
            S(934)),
        S(73), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(561),
            S(44)),
        S(610), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(737), S(881),
        0, 500, S(850), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(466))
    persistentToggle(farmTab:AddCard(
        S(814),
        S(856)
    ), S(557), autoChest.SetEnabled)
    persistentSlider(farmTab, S(147), S(931),
        50, 3000, S(819), 100, autoChest.SetMinItems, S(168))
    persistentSlider(farmTab, S(72), S(652),
        1, 60, S(227), 3, autoChest.SetCheckInterval, S(438))
    persistentToggle(farmTab:AddCard(
        S(614),
        S(660)
    ), S(718), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(559),
            S(659)),
        S(296), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(13), S(623),
        5, 300, S(666), 20, autoSell.SetCheckInterval, S(149))
    farmTab:AddSlider(S(617),
        S(915),
        5, 120, sellMaxWait, function(value)
            config.Set(S(373), value)
        end, S(732))
    persistentToggle(farmTab:AddCard(
        S(446),
        S(587)
    ), S(650), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(930), S(320),
        5, 300, S(133), 20, autoUpgrade.SetInterval, S(448))
    persistentSlider(farmTab, S(740), S(387),
        1, 20, S(454), 5, autoUpgrade.SetMaxPerPass, S(894))
    persistentSegmented(
        farmTab,
        { S(546), S(707), S(318), S(152) },
        { S(901), S(235), S(263), S(553) },
        S(371), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(46), S(194),
        0, 500, S(558), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(57))
    persistentToggle(
        farmTab:AddCard(S(813),
            S(10)),
        S(288), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(398) .. pickaxe.multiplier .. S(900)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(319)
            end
            return S(537) .. index .. S(415) .. (pickaxeLabels[index] or S(335))
        end
        local savedPick = config.Get(S(654), 0)
        pickaxeCard = farmTab:AddSlider(
            S(377),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(750), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(53)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(253),
            S(62)),
        S(200), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(482),
            S(212)),
        S(556), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(255),
            S(182)),
        S(517), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(898), S(921),
        1, 20, S(838), 2, autoUpgrade.SetMutationTarget, S(282))
    persistentSlider(farmTab, S(102), S(591),
        0, 500, S(348), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(533))
    persistentToggle(farmTab:AddCard(
        S(785),
        S(739)
    ), S(453), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(1), S(673),
        10, 600, S(803), 30, autoResearch.SetCheckInterval, S(325))
    persistentSlider(farmTab, S(923), S(222),
        1, 20, S(604), 5, autoResearch.SetMaxPerPass, S(317))
    persistentSlider(farmTab, S(146), S(161),
        0, 500, S(868), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(531))
    persistentToggle(
        farmTab:AddCard(S(409),
            S(566)),
        S(905), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(875),
            S(576)),
        S(215), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(91),
            S(792)),
        S(461), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(818), S(910) },
        { true, false },
        S(797), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(120),
        S(293)
    ), S(158), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(867), S(206),
        5, 300, S(665), 30, autoClaim.SetCheckInterval, S(278))
    persistentSlider(farmTab, S(139), S(361),
        1, 30, S(873), 10, autoClaim.SetMaxPerPass, S(862))
    persistentToggle(farmTab:AddCard(
        S(483),
        S(570)
    ), S(694), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(347), S(77),
        5, 300, S(354), 30, autoPlot.SetCheckInterval, S(178))
    persistentToggle(
        farmTab:AddCard(S(678),
            S(159)),
        S(535), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(506),
            S(549)),
        S(74), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(626), S(208),
        0, 500, S(83), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(135))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(142))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(52), S(801))
    persistentToggle(
        settingsTab:AddCard(S(437), S(192)),
        S(236),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(393), 100)
    settingsTab:AddSlider(S(56), S(175), 50, 150, savedScale, function(value)
        config.Set(S(300), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(358), 1)
    settingsTab:AddSegmented({ S(880), S(322), S(480) }, savedLanguage, function(index, value)
        local code = ({ S(332), S(692), S(378) })[index]
        config.Set(S(786), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(277), S(419), S(12) })[savedLanguage] or S(63))
    log(S(565) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(313)] = function()
return {
    [79389059854988] = S(837),
}
end
import(S(179))(import)
