-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", ", you have ", "GetHolderMineSpeedLevel", "autoUpgradeEquipPickaxe", "table", " waited ", "string", "/", "0 = ไม่จำกัด", "แรงกระโดด", "%$", "[AutoUpgrade] ", "[BestMiner] slot ", "AutoPlot", "getscriptbytecode", "^%$", "cost", "UserInputService", "games/my_anime_mine/main", "autoPlaceUnlockBudget", "Auto Upgrade Plot (Zone)", " ชิ้น", "0fb5d9b+", "[AutoPlace] fireproximityprompt error: ", "empty", " -> x", "[MekPro] settings file: ", "%.0f", ",", "autoUpgradeInterval", "[Remotes] === total: ", "core/config", "TextLabel", " items (now ", "TextLabel", "autoResearchCore", "[AutoPlot] ", " (power ", "Auto Research: กันเงินไว้", "games/my_anime_mine/get_best_miner", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "debug.getproto", "Limited", "[GameAPI] SendEvent failed: ", "[GuiDump] === end (", " pickaxe(s) for ", "games/registry", "GuiService", "Auto Plot: ใช้โซนล่าสุดเสมอ", " (", "core/logger", "Workspace", "no mine data", "M", "Players", "may need us at the stand - trying that", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", " (", "table", "[AutoChest] collected ", "", "TextButton", "autoPlot", "core/anti_afk", "UICorner", "menuScale", "autoSellMaxWait", "AutoPlace", "games/my_anime_mine/auto_mine", "MineEquipSlot", "(", " (money ", " claimable but none could be resolved", "noclip", " x", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "getcallbackvalue", "Players", "core/debug_dump_gui", "Frame", "games/my_anime_mine/get_current_plot", "/s", "[AutoPlace] upgrading ", " - ", "_", "Text", "ScrollingFrame", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "autoResearchCheapestFirst", "autoPlotBuy", "table", "Auto Plot: กันเงินไว้", "OwnerUserId", "Character", " (", "[AutoPlot] ", "#", "Frame", "🏠", "Modules", "M", "games/my_anime_mine/auto_research", " (", "sold ", "jumpPower", "[Interaction] Key pressed: %s (gameProcessed=%s)", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "Workspace", "เดินทะลุกำแพงและพื้น", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "GetHolderSpeedLevel", "auto", " = ", "Auto Mine: ความเร็ว", "ตามเครื่อง", " -> ", "waiting", " -> ", "[AutoUpgrade] MineBuyPickaxe ", "[AutoChest] MineTakeChest had no effect (", ")", "BasePart", "([%d,%.]+)%s*(%a*)", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "Workspace", "List", "core/debug_dump_remotes", "Workspace", ".lua", " failed)", ") gave nothing", "^([%d,]+)$", "Auto Claim: เก็บสูงสุดต่อรอบ", "string", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", ") didn't take: ", "(nothing to do)", "fill", " '", " error: ", "[AutoSell] MineSellItems ", "TextLabel", "[Decompile] wrote ", "getconnections", "%.3g", "ChestCapacity", "PlayerGui", "UserInputService", " | ", "autoResearch", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Players", "", "write reported success but the line isn't in ", "%.0f", "Damage", "ZoneRow", "?", "s", "HotbarFrame", ") from ", "autoUpgradePickaxeIndex", " refused - ", "Header", "no", "VirtualUser", "prompt disabled", "none", "0 = ไม่จำกัด", "AutoClaim", "[Scheduler] turn: ", "[AutoPlace] disabled", "works from anywhere - dropping the teleport", "core/debug_interaction_logger", "M", " remotes", " team power", "autoClaim", "Players", ": ", "Auto Research (Plot)", "table", "table", "core/parse_money", "TextLabel", ": ", "TextLabel", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "", "TextLabel", "TextLabel", "ขายทุกกี่วินาที", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/auto_claim", " (Text=", "TextButton", "Exclusive", "TextLabel", "nothing claimable", "autoUpgradePickaxe", " while selecting", "autoSellMaxWait", "[AutoPlot] couldn't switch to ", ")", "AutoSell", "Auto Plot: ซื้อโซนถัดไป", "Root", "slot ", "unreadable", "[", "=empty", " (", "research", "Frame", " still empty after placing ", "%$%s*([%d,%.]+)%s*(%a*)", " possible holder(s) - not buying more", "MekPro", "[AutoUpgrade] bought pickaxe #", ": ", ": ", "')", "Locked miner slot", "games/my_anime_mine/auto_mine", "Mining ก่อน", "button has no size on screen", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "fill", "autoUpgradePickaxeIndex", "Auto Upgrade Unit", "en", "TextButton", "string", "[Decompile] === start ===", "[AutoPlace] couldn't pick up ", "Loot", "BodyVelocity", "x", "Auto Upgrade: รอบเช็ค", "[AutoSell] ", "[AutoUpgrade] ", "core/get_place_id", "M", "s - carrying on without it", " research is ", "Frame", "  ", ")", "nothing left to buy (", "Players", "CharMaxLevel", "waiting", "core/ui", "[AutoSell] enabled", "[AutoResearch] cheapest open node: ", "games/my_anime_mine/auto_sell", " for ", "แพงสุดก่อน", "Badge", "function", "มุดแมพ (Noclip)", "Damage", "%", "core/debug_tab", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "[AntiAFK] enabled - watching Player.Idled", "core/scheduler", " power ", "upgrade", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "languageIndex", "core/get_me", "needs ", "research", "Frame", " stack(s)", "core/parse_money", ")", "games/my_anime_mine/get_current_plot", "core/run_job", "ClickDetector", "[Decompile] ", "core/run_job", "Backpack", ") -> ", "[Decompile] === executor capabilities ===", "core/click", "autoMineSpeed", ")", "costs ", "AutoResearch", ")", "core/run_job", "=locked(", " owned)", "GetCharacterSpeedUpgradeCost", ")", "hookfunction", "[AutoResearch] Core ", "autoUpgradeMaxPerPass", "autoResearchInterval", "table", " from ", ")", ": ", "ตั้งค่า", "auto", "", "[AutoPlot] now mining ", "[AutoMine] ", " not bought yet (", "core/teleport", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "MinePower", "Damage ก่อน", "search_placeholder", "ฟาร์ม", "TextLabel", "autoUpgradePriority", "Nodes", " costs ", "PickaxesStand", "works from anywhere - no teleport needed", "core/debug_tab", "debug.getupvalues", "Workspace", "Frame", "debug.getconstants", "autoResearchKeepMoney", "", "ScreenGui", " ===", "Auto Plot: รอบเช็ค", "Run", "RunService", ": badge says ", "Decompile Client Scripts", "อังกฤษ", ")", "Dump Visible GUI", "  [attr] ", "leaderstats", "core/get_leaderstat", "games/my_anime_mine/game_api", "UIStroke", "autoUpgradeLimitPickaxes", "-", "disabled", "Auto Sell", "RunService", "MekPro/decompiled", "[Decompile] === end ===", "core/logger", "s", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "=", "index rewards are disabled in this game build", "Auto Chest: รอบเช็ค", ") needs ", "core/run_job", "games/my_anime_mine/auto_upgrade", ") on ", "Content", "RemoteFunction", ": State(", "table", "GetCharacterUpgradeCost", "core/logger", "[AutoChest] waiting: ", "MineSpeed", "autoUpgradeMutationTarget", "[AntiAFK] disabled", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "Humanoid", "core/build_env", ")", "TabBar", "TextButton", "lowest", "Humanoid", "core/run_job", "Chest", "] job failed: ", " ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "games/my_anime_mine/auto_upgrade", "Frame", "Auto Sell: รอคิวได้นานสุด", " (chest holds ", " screens", "table", "[AutoResearch] nothing open and affordable right now", ") ", "cost", "Frame", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", " costs ", "games/my_anime_mine/game_api", "Auto Claim (Index)", "Mutation: หยุดเมื่อได้ x", "Players", "disabled", " (x", " - upgrade the miners first", "Players", "GuiObject", "", "MineRollMutation", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "core/teleport", "getgc", "Auto Place: รอบเช็ค", "number", "decompile", "core/i18n", "autoPlace", "Workspace", "AutoMine", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[GuiDump] PlayerGui not found", "core/run_job", "UICorner", "TextLabel", "PlayerGui", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "autoSellInterval", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "Auto Place Miner", "TextButton", "UIPadding", "[AutoPlot] unlocked ", "[AutoResearch] idle: ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Open menu", " ", "Holders", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "autoClaimInterval", "HumanoidRootPart", " ", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "UserInputService", " -> ", " ", "open_menu_hint", "dev", "autoPlotKeepMoney", "core/logger", "core/dispatch", "AutoChest", "s", "Content", "/", "games/my_anime_mine/auto_chest", "BodyPosition", "AutoResearch", "autoPlaceInterval", "needs us at the stand - keeping the teleport", "BasePart", "MineBuyPickaxe", "[Bb]uy", "[AutoUpgrade] ", "core/debug_dump_gui", "AutoPlot", "autoClaimMaxPerPass", "Auto Claim: รอบเช็ค", "table", "JumpPower (ปกติ 50)", "[AutoChest] chest is empty", "en", "walkSpeed", "core/scheduler", "no isfile/readfile available to verify the write", "Speed", "Crew", "Auto Mine: คลิกต่อรอบ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[Config] couldn't read ", "disabled", "th", "MekPro.log", "[Remotes] ", "MineWorld", "ขนาดเมนู", ", built ", " (still ", "BasePart", "autoPlaceUnlock", "unreadable", "[Interaction] logger disabled", "My Anime Mine · Mining Hub", "core/humanoid", " #", "  ", "Hauler", "games/my_anime_mine/auto_sell", "Core", "table", "GuiButton", "search_placeholder", "", "core/logger", "[Logger] failed to write ", "%.3g", "/", "CoreGui", "CoreUI", "games/registry", "s", "%$", "float", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "MineSelectZone", "table", "ReplicatedStorage", "[AutoSell] disabled", "core/debug_interaction_logger", "", "[^%.]+", "[Config] couldn't write ", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "/", " (", "games/my_anime_mine/game_api", "/", "badge says ", " wouldn't take ", "Auto Research: ซื้อสาย Zone", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "MineEquipPickaxe", "Damage", "TextLabel", "fly", "[AutoClaim] ", "/config.json", "ปุ่มเปิดเมนู", "games/my_anime_mine/game_api", "[Decompile] decompile unavailable in this executor", "[AutoPlace] unlocked ", "ValueLabel", ")", "[AutoPlace] couldn't hold anything - not pressing E", "games/my_anime_mine/get_plot_parts", " but no unclaimed reward in its Track", "/", "th", "maxed", "TextLabel", "enabled", "[AutoUpgrade] ", "", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", " (cost ", "[AutoPlace] nothing in the hotbar to place", " ", "", "Auto Place: ปลดล็อกช่อง miner", "getsenv", "Badge", "interactionLogger", "ReplicatedStorage", "MekPro", "[%X] ", " mutation x", ", ", "core/logger", " (Text=", "[AutoClaim] ", "games/my_anime_mine/auto_place", "[AutoUpgrade] ", "core/humanoid", " ", ")", "neither appendfile nor writefile is available", "ProximityPrompt", " has been busy for ", "autoResearchMaxPerPass", "Main", "Core buying is off", "'", "autoPlotInterval", "s - jumping the queue", "ไล่อัปเกรด miner ทุกกี่วินาที", "core/logger", "core/config", "UIListLayout", "string", ") ", "[AutoPlace] ", "GuiButton", "function", "HumanoidRootPart", "[AutoResearch] ", "both Zone and Core buying are switched off", "core/logger", "table", ")", "needs ", "table", ",", "Frame", "Players", "lowest", " reward(s) looked claimable but none went through", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "TextLabel", "Pad", "", "[AutoChest] enabled (collects once >= ", ")", "UIListLayout", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "[Interaction] logger enabled - go click/interact with things now", "slot past the usable cap", "[AutoResearch] ", "GetCharacterMineSpeedUpgradeCost", "AutoUpgrade", " team power, you have ", "PlotIndex", "Auto Place: เพดานราคาปลดช่อง", "ย่อขยายเมนูทั้งชุด", "ไล่ซื้อทุกกี่วินาที", "MekPro", "LayerCollector", "[AutoPlace] ", "MineTakeChest", "", "MineSpeed", "MekPro", "games/my_anime_mine/auto_research", "ProximityPrompt", "[Interaction] ProximityPrompt triggered: ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "getloadedmodules", "enabled", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "disabled", "UIScale", ": ", "Auto Sell: รอบขาย", "core/run_job", "", "บิน (Fly)", "🐛", "^M(%d+)$", "", "Auto Research: รอบเช็ค", "autoUpgradeMutationBudget", ",", "", "2026-09-21 12:49:31", "[AutoUpgrade] ", "modules not loaded", "X", "ลอย (Float)", "Text", "Players", "LocalScript", "[GameAPI] couldn't load the game's modules: ", "", "core/logger", "core/run_job", "", "autoChestMinItems", "AutoUpgrade", "TextLabel", "", "games/my_anime_mine/game_api", "Auto Research: ซื้อสูงสุดต่อรอบ", "\n", "games/my_anime_mine/game_api", " (", "menuScale", "ZoneBoard", "autoSellProtectUnits", "[Scheduler] ", "Hauler", "HumanoidRootPart", "table", " (ActionText=", "ModuleScript", " -> ", "table", "/", "", "action '", "th", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", " (", " didn't go through (out of stock or too expensive)", "autoResearchCoreLast", "Content", "MineSellItems", "[AutoPlace] drifted out of range of ", "Auto Upgrade: สุ่ม Mutation", "[Interaction] ClickDetector clicked: ", "PlayerGui", " node(s) looked buyable but the server refused", "AutoClaim", "Auto Research: ซื้อสาย Core", "sale didn't go through", "Auto Upgrade: กันเงินไว้", "core/logger", " '", "MineBuySkill", "[Interaction] Button activated: ", "yes", "en", "core/logger", "Auto Chest", "string", "TextBox", "", "core/logger", ", base ", "Zones", " (x", "⚙", "Search functions...", " still locked after paying ", "อ่านป้ายบน chest ทุกกี่วินาที", "core/dispatch", "ItemFrame", "[AutoPlace] holders: ", "core/logger", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "getscripts", "games/my_anime_mine/main", " (", "games/my_anime_mine/auto_place", "auto", "MineGui", "core/debug_decompile", "nothing to place or upgrade", "x", "autoResearchZones", "en-us", "none", "server didn't equip it", " ", "MineUpgradeCharacter", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "autoChestInterval", "SellStand", " ", ")", "Anti-AFK", "core/ui", "[AutoPlace] slot ", "games/my_anime_mine/game_api", "BasePart", "TextLabel", "getrenv", "Empty miner slot", "games/my_anime_mine/game_api", "Search", " items)", "core/logger", "MineSpeed", "\n", "auto", "TextLabel", "AutoPlace", "core/logger", "autoSell", "'", "VirtualInputManager", "autoMineClicks", "s", " items", "unlock", "string", " screen(s)) ===", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "This is not a supported place.", "Auto Place: เปลี่ยนตัวอ่อนออก", " (", "s", "RemoteEvent", " ", "ZoneBoardGui", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/game_api", "Plots", "TextButton", "Interaction Logger", "autoMine", "games/my_anime_mine/get_plot_parts", "core/build_env", "AutoPlace", " (prompt says '", "ClickDetector", "All", "[AutoClaim] claimed ", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "table", ")", "%.3g", "autoUpgradeKeepMoney", "core/debug_dump_remotes", "write reported success but ", "core/logger", "TextLabel", "x", "OwnerUserId", "AutoSell", "Players", "[AutoChest] can't read the chest yet", "cheapest open node ", "ClickDetector", "[Scheduler] ", "[MekPro] build ", "Plots", ") ", "s", "[Uu]nlock", ")", "Auto Upgrade: Pickaxe ที่จะซื้อ", "AutoChest", "TextButton", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/logger", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "[AutoPlace] ", "Players", "s", " (power ", "core/get_place_id", "UIListLayout", "autoUpgradeRollMutation", "[AutoPlace] placed ", "core/logger", " script(s) to ", "Zone buying is off", "core/teleport", "antiAfk", "enabled", "games/my_anime_mine/game_api", "[AutoResearch] ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Shop Stand", "^x(%d+)$", "ดีบัก", "PlayerScripts", "AutoPlace", "TextButton", "CharMineSpeedMaxLevel", "games/my_anime_mine/auto_chest", "table", "games/my_anime_mine/auto_plot", "Dump", "core/logger", "WalkSpeed (ปกติ 16)", "Mining", "autoPlaceUpgradeWeak", "Speed ก่อน", "Dump", "ไทย", "ถูกสุดก่อน", " files", "languageIndex", "core/anti_afk", "slot not in the hotbar", ") ", "[AntiAFK] idle detected, simulating input", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "", "ProximityPrompt", "[AutoUpgrade] pickaxe #", "Dump All Remotes", "getnilinstances", " for ", "/", " ", "", " = ", " pickaxe ", "M", "ItemName", "enabled", "Players", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "'", ", have ", "TextButton", "", "core/logger", "core/debug_decompile", " ", "Frame", "ค้นหาฟังก์ชัน...", "autoPlotLatest", "locked slot", " (", "core/get_place_id", "empty", "affordable - waiting for a turn", "open_menu_hint", "autoChest", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "MineWorld", " (", "; ", "games/my_anime_mine/get_plot_parts", "CharSpeedMaxLevel", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[AutoPlot] ", "[^%w]+", "x", "[GuiDump] === visible PlayerGui tree ===", ")", " doesn't exist", "[AutoUpgrade] every stat on every miner is maxed", "ValueLabel", "Auto Mine", "HttpService", "Frame", "[AutoResearch] ", "[GuiDump] ", "Auto Upgrade: แจก Pickaxe ให้ miner", "", "every open node is maxed", "Mutation: เพดานราคาต่อครั้ง", "games/my_anime_mine/auto_claim", "[AutoPlace] unlocking ", "function", "x", "[AutoChest] disabled", "Players", "GetHolderLevel", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "[AutoClaim] idle: ", " -> ", "th", "ต่ำสุดก่อน", "เช็คโซนทุกกี่วินาที", "core/i18n", "games/my_anime_mine/auto_plot", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "[Decompile] PlayerScripts not found", "Container", "core/i18n", "", "Auto Chest: เกณฑ์เก็บ", "Speed", "[Scheduler] ", "ClaimableTiers failed", "[AutoPlace] picked up ", "enabled", "core/teleport", "", "unlock", "disabled", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "core/parse_money", "ความเร็ววิ่ง", "autoUpgrade", "ProximityPrompt"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(865)] = function()
return function(import)
local log = import(S(706))
local Players = game:GetService(S(932))
local VirtualUser = game:GetService(S(166))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(266))
        connection = player.Idled:Connect(function()
            log(S(868))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(372))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(792)] = function()
return { Env = S(449), IsDev = true, Commit = S(23), BuiltAt = S(654) }
end
modules[S(287)] = function()
local VirtualInputManager = game:GetService(S(770))
local GuiService = game:GetService(S(48))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(226)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(32)] = function()
return function(import)
local HttpService = game:GetService(S(919))
local getPlaceId = import(S(831))
local log = import(S(855))
local ROOT_FOLDER = S(218)
local placeFolder = ROOT_FOLDER .. S(687) .. tostring(getPlaceId())
local filePath = placeFolder .. S(538)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(302) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(481) .. filePath .. S(642) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(523) .. filePath .. S(180) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(891)] = function()
return function(import)
local Players = game:GetService(S(408))
local log = import(S(890))
local CAPABILITIES = {
    S(417), S(15), S(730), S(414), S(637),
    S(144), S(77), S(561), S(756), S(328),
    S(325), S(42), S(298), S(874),
}
local OUT_FOLDER = S(351)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(522)) do
        if type(target) ~= S(852) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(286))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(282) .. name .. S(879) .. ((ok and type(value) == S(929)) and S(710) or S(165)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(541))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(847))
    if not playerScripts then
        log(S(943))
        return 0
    end
    pcall(makefolder, S(626))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(661)) or target:IsA(S(684)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(590) and #source > 0 then
                local name = target:GetFullName():gsub(S(911), S(85))
                if pcall(writefile, OUT_FOLDER .. S(508) .. name .. S(129), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(143) .. written .. S(836) .. OUT_FOLDER
        .. S(780) .. failed .. S(130))
    return written
end
local function run()
    log(S(234))
    reportCapabilities()
    local count = dumpAll()
    log(S(352))
    return count
end
return run
end
end
modules[S(79)] = function()
return function(import)
local Players = game:GetService(S(78))
local log = import(S(368))
local function dumpVisible(root, indent)
    indent = indent or S(559)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(409)) and child.Visible then
            local line = indent .. child.ClassName .. S(139) .. child.Name .. S(769)
            if child:IsA(S(142)) or child:IsA(S(232)) then
                line = line .. S(570) .. tostring(child.Text) .. S(600)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(341) .. attrName .. S(113) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(497))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(147))
    if not playerGui then
        log(S(423))
        return 0
    end
    local screens = 0
    log(S(913))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(627)) and screen.Enabled and screen.Name ~= S(632) then
            screens = screens + 1
            log(S(922) .. screen.ClassName .. S(707) .. screen.Name .. S(886))
            dumpVisible(screen, S(247))
        end
    end
    log(S(45) .. screens .. S(776))
    return screens
end
return dumpGui
end
end
modules[S(127)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(518))
local log = import(S(805))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(782)) or descendant:IsA(S(364)) then
            count = count + 1
            log(S(485) .. descendant.ClassName .. S(221) .. descendant:GetFullName())
        end
    end
    log(S(31) .. count .. S(332))
    return count
end
return dumpRemotes
end
end
modules[S(520)] = function()
return function(import)
local Players = game:GetService(S(179))
local Workspace = game:GetService(S(128))
local UserInputService = game:GetService(S(445))
local log = import(S(569))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(635) .. describe(prompt)
            .. S(683) .. prompt.ActionText .. S(248))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(699) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(709) .. describe(button)
            .. S(195) .. tostring(button.Text) .. S(297))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(634)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(795)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(593)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(493))
        return
    end
    connections = {}
    log(S(616))
    local playerGui = Players.LocalPlayer:WaitForChild(S(700))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(961)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(813)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(502)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(106),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(264)] = function()
return function(import)
local interactionLogger = import(S(174))(import)
local dumpRemotes = import(S(803))(import)
local dumpGui = import(S(466))(import)
local decompileScripts = import(S(736))(import)
local humanoid = import(S(495))
local function build(window, persist)
    local tab = window:AddTab(S(846), S(647))
    persist.Toggle(
        tab:AddCard(
            S(789),
            S(639)
        ),
        S(563),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(873),
        S(227)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(860), function()
        remoteBtn.Text = dumpRemotes() .. S(176)
    end)
    local guiCard = tab:AddCard(
        S(340),
        S(745)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(854), function()
        guiBtn.Text = dumpGui() .. S(393)
    end)
    local decompileCard = tab:AddCard(
        S(337),
        S(385)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(334), function()
        decompileBtn.Text = decompileScripts() .. S(863)
    end)
    humanoid.Start()
    persist.Slider(tab, S(959), S(856),
        8, 200, S(474), 16, humanoid.SetWalkSpeed, S(666))
    persist.Slider(tab, S(10), S(471),
        30, 300, S(105), 50, humanoid.SetJumpPower, S(924))
    persist.Toggle(
        tab:AddCard(S(646), S(355)),
        S(536), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(261), S(109)),
        S(74), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(658), S(107)),
        S(514), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(452)] = function()
return function(import)
    local getPlaceId = import(S(898))
    local registry = import(S(47))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(778))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(343)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(342))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(272)] = function()
local Players = game:GetService(S(152))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(374))
    local backpack = player:WaitForChild(S(284))
    return player, humanoid, backpack
end
return getMe
end
modules[S(242)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(574)] = function()
local Players = game:GetService(S(810))
local RunService = game:GetService(S(335))
local UserInputService = game:GetService(S(18))
local Workspace = game:GetService(S(125))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(380))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(595))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(237))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(490)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(462)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(458))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(945)] = function()
local Players = game:GetService(S(55))
local STRINGS = {
    en = {
        search_placeholder = S(722),
        open_menu_hint = S(437),
    },
    th = {
        search_placeholder = S(894),
        open_menu_hint = S(539),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(740)
    if locale:sub(1, 2):lower() == S(690) then
        return S(937)
    end
    return S(473)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(734) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(451)] = function()
local LOG_FILE = S(484)
local function log(message)
    local line = os.date(S(566)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(554))
            end
            appendfile(LOG_FILE, line .. S(763))
        elseif writefile then
            local existing = S(870)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(673))
        else
            error(S(577))
        end
        if not (isfile and readfile) then
            error(S(476))
        end
        if not isfile(LOG_FILE) then
            error(S(804) .. LOG_FILE .. S(915))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(154) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(506) .. LOG_FILE .. S(305) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(958)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(7) then
        return nil
    end
    local number, suffix = text:match(S(216))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(603), S(504))))
    if not value then
        return nil
    end
    if suffix ~= S(386) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(360)] = function()
return function(import)
local log = import(S(712))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(210) .. name .. S(383) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(267)] = function()
return function(import)
local RunService = game:GetService(S(350))
local log = import(S(664))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(949) .. task_.name .. S(140) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(814) .. task_.name .. S(579)
                    .. string.format(S(155), now - task_.busySince)
                    .. S(244))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(679) .. starved.name .. S(6)
            .. string.format(S(28), now - starved.waitingSince)
            .. S(585))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(171) .. (currentTask and currentTask.name or S(137)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(953)] = function()
local Players = game:GetService(S(884))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(681))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(751)] = function()
return function(import)
local i18n = import(S(940))
local UserInputService = game:GetService(S(148))
local THEME = {
    BrandName = S(565),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(425))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(65))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(433))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(196))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(189)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(345))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(191))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(158)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(551))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(521)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(87))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(589))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(198))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(98))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(426))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(755))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(33))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(543))
            if not valueLbl then
                valueLbl = Instance.new(S(35))
                valueLbl.Name = S(917)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(562))
            if not badge then
                badge = Instance.new(S(609))
                badge.Name = S(259)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(378))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(80))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(604))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(432))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(330)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(263)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(327))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(390))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(888))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(653)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(275))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(614))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(849))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(659)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(331))
    screenGui.Name = S(510)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(509))
    local main = Instance.new(S(920))
    main.Name = S(581)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(641))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(893))
    tabBar.Name = S(377)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(832))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(398))
    header.Name = S(164)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(62))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(535))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(190))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(153)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(823))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(657)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(788))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(347)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(715))
    searchBox.Name = S(759)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(503))
    searchBox.Text = S(716)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(214))
    content.Name = S(695)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(246))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(318))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(448))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(806))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(86)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(645) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(447) .. (item.Subtitle or S(889))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(316))
        footerLabel.Text = i18n.T(S(901))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(851)] = function()
return function(import)
local gameApi = import(S(671))(import)
local log = import(S(767))
local runGuarded = import(S(293))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(612) .. minItemsToCollect .. S(760))
    else
        log(S(931))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(492) then
            log(S(811))
            lastReported = S(209)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(25) then
            log(S(472))
            lastReported = S(899)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(117) then
            local capacity = gameApi.ChestCapacity()
            log(S(369) .. count .. S(8) .. want .. S(773)
                .. (capacity and (S(392) .. capacity .. S(914)) or S(688)))
            lastReported = S(252)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(822), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(60) .. count .. S(34) .. after .. S(121))
        else
            log(S(120) .. count
                .. S(118) .. tostring(after) .. S(749))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(194)] = function()
return function(import)
local gameApi = import(S(527))(import)
local log = import(S(505))
local runGuarded = import(S(280))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(571) .. (enabled and S(840) or S(405)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(935) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(797) .. reward.category .. S(743)
                .. tostring(reward.tierKey) .. S(496) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(537) .. #pending .. S(607))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(702), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(224)] = function()
return function(import)
local getCurrentPlot = import(S(785))
local getPlotParts = import(S(546))
local log = import(S(761))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(281), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(310) .. (enabled and S(552) or S(482)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(572)] = function()
return function(import)
local Players = game:GetService(S(660))
local getCurrentPlot = import(S(81))
local getPlotParts = import(S(791))
local getMinerSlots = import(S(193))(import)
local teleport = import(S(838))
local parseMoney = import(S(277))
local gameApi = import(S(786))(import)
local log = import(S(587))
local runGuarded = import(S(665))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(757)
local LOCKED_SLOT_TEXT = S(223)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(610))
    return pad and pad:FindFirstChildWhichIsA(S(871), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(185)) then
            local digits = descendant.Text:match(S(132))
            if digits then
                return tonumber((digits:gsub(S(652), S(630))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(187)) and descendant.Text:match(S(16)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(208) .. holder.Name .. S(311)
            .. gameApi.MinerSlots() .. S(876) .. gameApi.MaxMinerSlots() .. S(295)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(896)
    end
    if not prompt.Enabled then
        return true, S(167)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(290) .. price
    end
    local action = prompt.ActionText or S(61)
    if action:match(S(819)) or action:match(S(464)) or action:match(S(11)) then
        return true, S(689) .. action .. S(583)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(442))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(24) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(294) .. why .. S(278))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(211))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(356) .. prompt.ObjectText
                    .. S(71) .. shown .. S(718) .. math.floor(base) .. S(376))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(149))
    if line ~= lastSurvey then
        log(S(727) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(228), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(269), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(955), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(592) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(172))
    else
        lastAttempt = 0
        log(S(1))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(903),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(866)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(617)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(742)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(557))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(697) .. holder.Name .. S(201))
            return false
        end
        log(S(752) .. candidate.slot .. S(692) .. candidate.name
            .. S(136) .. tostring(why))
    end
    if not miner then
        log(S(545))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(827) .. holder.Name .. S(215) .. miner.name)
        return false
    end
    log(S(834) .. miner.name .. S(38) .. miner.power
        .. S(362) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(235) .. entry.name .. S(303) .. entry.holder.Name
            .. S(794) .. tostring(entry.prompt.ActionText) .. S(222))
        return false
    end
    log(S(951) .. entry.name .. S(830) .. entry.power
        .. S(161) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(628) .. pad.holder.Name .. S(723)
                .. tostring(pad.priceText))
        else
            log(S(542) .. pad.holder.Name
                .. S(257) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(737))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(138) then
        runGuarded(S(766), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(774) then
        log(S(928) .. target.holder.Name
            .. S(875) .. tostring(target.priceText))
        runGuarded(S(68), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(83) .. target.holder.Name .. S(220) .. target.name
        .. S(897) .. target.power .. S(285) .. best.name .. S(212) .. best.power .. S(339))
    runGuarded(S(793), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(941)] = function()
return function(import)
local gameApi = import(S(753))(import)
local parseMoney = import(S(184))
local log = import(S(598))
local runGuarded = import(S(424))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(910) .. (enabled and S(883) or S(640)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(513)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(309) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(58) .. deepestKey .. S(820))
        return true
    end
    log(S(203) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(96) .. gameApi.ZoneDisplayName(zoneKey) .. S(400)
                .. math.floor(price) .. S(2) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(434) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(95) .. zoneKey .. S(800))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(274) then
            reportedLocked = S(213)
            log(S(37) .. zoneKey .. S(163)
                .. gameApi.ZoneDisplayName(currentKey) .. S(245)
                .. done .. S(525) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(467), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(633)] = function()
return function(import)
local gameApi = import(S(674))(import)
local log = import(S(835))
local runGuarded = import(S(644))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(618) .. (enabled and S(638) or S(348)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(842) .. node.zoneKey .. S(443) .. node.id
                    .. S(732) .. node.family .. S(396) .. node.level .. S(446) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(299) .. node.id
                    .. S(50) .. node.family .. S(591) .. node.level .. S(685) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(921) .. blocked
                .. S(701))
        elseif powerBlocked then
            log(S(596) .. powerBlocked.zoneKey .. S(384) .. powerBlocked.id
                .. S(103) .. powerBlocked.family .. S(359)
                .. math.floor(powerBlocked.powerReq) .. S(621)
                .. math.floor(powerBlocked.power)
                .. S(407))
        else
            log(S(395)
                .. S(72) .. math.floor(gameApi.GetMoney()) .. S(292))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(255) .. nearest.zoneKey
                    .. S(877) .. nearest.id .. S(905) .. nearest.family .. S(867)
                    .. nearest.level .. S(528) .. nearest.max
                    .. S(321) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(597)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(500),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(837))
        end
        if not buyCore then
            table.insert(parts, S(582))
        end
        if #parts == 0 then
            return S(925)
        end
        return S(249) .. table.concat(parts, S(568)) .. S(304)
    end
    local why
    if nearest.cost > budget then
        why = S(273) .. string.format(S(145), nearest.cost)
            .. S(887) .. string.format(S(507), money)
    elseif nearest.powerOk == false then
        why = S(601) .. string.format(S(801), nearest.powerReq) .. S(177)
    else
        why = S(900)
    end
    return S(812) .. nearest.zoneKey .. S(575) .. tostring(nearest.id)
        .. S(526) .. tostring(nearest.family) .. S(817) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(435) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(459), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(256)] = function()
return function(import)
local Workspace = game:GetService(S(108))
local gameApi = import(S(401))(import)
local teleport = import(S(413))
local log = import(S(51))
local runGuarded = import(S(283))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(519))
    else
        lastAttempt = 0
        log(S(254))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(747))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(578), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(141)
                .. (sold and S(173)
                         or S(461)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(240) .. (sold and (S(104) .. #keys .. S(276)) or S(704)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(205), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(361)] = function()
return function(import)
local Workspace = game:GetService(S(326))
local gameApi = import(S(758))(import)
local teleport = import(S(312))
local log = import(S(825))
local runGuarded = import(S(381))(import)
local STATS = { S(534), S(948), S(370) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(379)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(465) .. (enabled and S(952) or S(956)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(366) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(17) or S(550)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(241) .. holderKey .. S(748) .. choice.stat .. S(438)
                        .. choice.info.level .. S(116) .. after.level
                        .. S(548) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(397) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(322))
    local shop = stand and stand:FindFirstChild(S(844))
    if not shop then
        return nil
    end
    if shop:IsA(S(754)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(122), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(119)
                .. (bought and S(323)
                           or S(56)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(691))
        end
    end
    if bought then
        log(S(219) .. pickaxe.index .. S(783) .. pickaxe.name
            .. S(720) .. pickaxe.multiplier .. S(204))
        return true
    end
    log(S(872) .. pickaxe.index .. S(558) .. pickaxe.name
        .. S(693))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(655) .. holderKey .. S(880)
                    .. tostring(worn or S(168)) .. S(936) .. wanted.name)
            else
                log(S(553) .. holderKey .. S(530)
                    .. wanted.name .. S(489) .. tostring(nowWorn or S(741)) .. S(544))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(12) .. holderKey .. S(567) .. variant
                .. S(26) .. after .. S(556) .. cost .. S(613))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(573) .. gameApi.OwnedPickaxeCount()
                    .. S(46) .. gameApi.MaxMinerSlots()
                    .. S(217))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(532))
        elseif maxed > 0 then
            log(S(916))
        else
            log(S(636))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(668), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(540)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(564))
local Workspace = game:GetService(S(420))
local log = import(S(728))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(100), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(662) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(682))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(5) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(680) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(91) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(517) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(498) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(648))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(44) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(629))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(796), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(501) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(394) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(94) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(696), keys)
end
function api.EquipSlot(slot)
    return send(S(70), slot == S(649) and S(670) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(463), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(533), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(411), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(708), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(799)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(250)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(314))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(594) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(183) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(516), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(904))
    plots = plots and plots:FindFirstChild(S(816))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(605)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(93)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(677))
    local gui = board and board:FindFirstChild(S(784))
    local root = gui and gui:FindFirstChild(S(207))
    local list = root and root:FindFirstChild(S(126))
    local row = list and list:FindFirstChild(S(157) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(765)) and descendant.Text ~= S(663) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(260) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(744), characterKey, stat == S(857) and S(631) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(933), max = S(251), cost = S(367) },
    Speed = { level = S(111), max = S(908), cost = S(296) },
    MineSpeed = { level = S(3), max = S(850), cost = S(619) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(714) or charName == S(946) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(59) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(416) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(134) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(470) and type(holder.CharName) == S(775) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(43), S(197) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(686) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(656)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(53)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(357)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(602) then
        return {}, S(950)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(199)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(182) or type(state.Track) ~= S(599) then
                table.insert(notes, tabKey .. S(365) .. tostring(category) .. S(131))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(336) .. waiting
                        .. S(547))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(529) .. total .. S(73)
            .. (#notes > 0 and (S(84) .. table.concat(notes, S(906))) or S(308))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(233) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(40)] = function()
return function(import)
local Players = game:GetService(S(828))
local gameApi = import(S(841))(import)
local log = import(S(717))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(123))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(29), S(878)))) or 0
    if suffix ~= S(410) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(427))
    local mineGui = playerGui and playerGui:FindFirstChild(S(735))
    local hotbar = mineGui and mineGui:FindFirstChild(S(160))
    local content = hotbar and hotbar:FindFirstChild(S(455))
    return content and content:FindFirstChild(S(944))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(669)) then
            local count = descendant.Text:match(S(845))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(726))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(363))
    local nameLabel = content and content:FindFirstChild(S(882))
    local damageLabel = content and content:FindFirstChild(S(156))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(954) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(456) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(13) .. slot .. S(186) .. info.name
                        .. S(268) .. info.power .. S(75) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(279)] = function()
local Players = game:GetService(S(404))
local Workspace = game:GetService(S(52))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(486))
    local plots = mineWorld and mineWorld:FindFirstChild(S(787))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(808)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(907)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(439)),
        Nodes = plot:FindFirstChild(S(320)),
        Chest = plot:FindFirstChild(S(382)),
        Zones = plot:FindFirstChild(S(719)),
        Crew = plot:FindFirstChild(S(478)),
        Loot = plot:FindFirstChild(S(236)),
        PlotIndex = plot:GetAttribute(S(622)),
        ChestCapacity = plot:GetAttribute(S(146)),
    }
end
return getPlotParts
end
modules[S(731)] = function()
return function(import)
    local createWindow = import(S(253))(import)
    local i18n = import(S(418))
    local antiAfk = import(S(64))(import)
    local buildEnv = import(S(375))
    local scheduler = import(S(475))(import)
    local config = import(S(588))(import)
    local log = import(S(353))
    log(S(815) .. buildEnv.Commit .. S(675) .. buildEnv.Env
        .. S(488) .. buildEnv.BuiltAt .. S(576))
    local window = createWindow({
        Subtitle = S(494),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(69))(import)
    local autoPlace = import(S(733))(import)
    local autoChest = import(S(457))(import)
    local autoSell = import(S(499))(import)
    local autoUpgrade = import(S(389))(import)
    local autoResearch = import(S(102))(import)
    local autoClaim = import(S(927))(import)
    local autoPlot = import(S(853))(import)
    local gameApi = import(S(344))(import)
    scheduler.RegisterStandalone(S(421), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(453), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(848), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(620), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(291), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(170), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(14), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(202), 20)
    scheduler.RegisterExclusive(S(809), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(317), S(99))
    persistentToggle(farmTab:AddCard(
        S(918),
        S(555)
    ), S(790), autoMine.SetEnabled)
    persistentSlider(farmTab, S(114), S(430),
        1, 30, S(288), 5, autoMine.SetCyclesPerSecond, S(82))
    persistentSlider(farmTab, S(479), S(480),
        1, 10, S(771), 1, autoMine.SetClicksPerCycle, S(738))
    persistentToggle(farmTab:AddCard(
        S(431),
        S(843)
    ), S(419), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(415), S(41),
        1, 60, S(460), 3, autoPlace.SetCheckInterval, S(818))
    persistentToggle(
        farmTab:AddCard(S(779),
            S(188)),
        S(858), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(560),
            S(524)),
        S(491), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(623), S(9),
        0, 500, S(20), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(881))
    persistentToggle(farmTab:AddCard(
        S(713),
        S(444)
    ), S(902), autoChest.SetEnabled)
    persistentSlider(farmTab, S(947), S(151),
        50, 3000, S(667), 100, autoChest.SetMinItems, S(22))
    persistentSlider(farmTab, S(358), S(724),
        1, 60, S(746), 3, autoChest.SetCheckInterval, S(512))
    persistentToggle(farmTab:AddCard(
        S(349),
        S(57)
    ), S(768), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(942),
            S(124)),
        S(678), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(643), S(192),
        5, 300, S(429), 20, autoSell.SetCheckInterval, S(781))
    farmTab:AddSlider(S(391),
        S(515),
        5, 120, sellMaxWait, function(value)
            config.Set(S(67), value)
        end, S(772))
    persistentToggle(farmTab:AddCard(
        S(230),
        S(373)
    ), S(960), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(239), S(586),
        5, 300, S(30), 20, autoUpgrade.SetInterval, S(159))
    persistentSlider(farmTab, S(608), S(909),
        1, 20, S(300), 5, autoUpgrade.SetMaxPerPass, S(238))
    persistentSegmented(
        farmTab,
        { S(938), S(315), S(859), S(225) },
        { S(606), S(262), S(477), S(762) },
        S(319), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(705), S(313),
        0, 500, S(802), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(243))
    persistentToggle(
        farmTab:AddCard(S(422),
            S(824)),
        S(200), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(406) .. pickaxe.multiplier .. S(289)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(869)
            end
            return S(97) .. index .. S(892) .. (pickaxeLabels[index] or S(88))
        end
        local savedPick = config.Get(S(229), 0)
        pickaxeCard = farmTab:AddSlider(
            S(821),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(162), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(611)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(798),
            S(265)),
        S(346), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(923),
            S(399)),
        S(4), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(698),
            S(388)),
        S(833), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(403), S(729),
        1, 20, S(371), 2, autoUpgrade.SetMutationTarget, S(912))
    persistentSlider(farmTab, S(926), S(169),
        0, 500, S(651), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(101))
    persistentToggle(farmTab:AddCard(
        S(181),
        S(934)
    ), S(150), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(650), S(625),
        10, 600, S(301), 30, autoResearch.SetCheckInterval, S(454))
    persistentSlider(farmTab, S(672), S(777),
        1, 20, S(580), 5, autoResearch.SetMaxPerPass, S(930))
    persistentSlider(farmTab, S(39), S(428),
        0, 500, S(329), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(175))
    persistentToggle(
        farmTab:AddCard(S(531),
            S(957)),
        S(739), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(703),
            S(615)),
        S(36), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(412),
            S(387)),
        S(694), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(862), S(258) },
        { true, false },
        S(89), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(402),
        S(436)
    ), S(178), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(469), S(110),
        5, 300, S(441), 30, autoClaim.SetCheckInterval, S(829))
    persistentSlider(farmTab, S(133), S(440),
        1, 30, S(468), 10, autoClaim.SetMaxPerPass, S(807))
    persistentToggle(farmTab:AddCard(
        S(21),
        S(76)
    ), S(63), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(333), S(939),
        5, 300, S(584), 30, autoPlot.SetCheckInterval, S(354))
    persistentToggle(
        farmTab:AddCard(S(206),
            S(270)),
        S(90), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(49),
            S(826)),
        S(895), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(92), S(885),
        0, 500, S(450), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(54))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(324))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(306), S(721))
    persistentToggle(
        settingsTab:AddCard(S(750), S(135)),
        S(839),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(676), 100)
    settingsTab:AddSlider(S(487), S(624), 50, 150, savedScale, function(value)
        config.Set(S(66), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(271), 1)
    settingsTab:AddSegmented({ S(115), S(338), S(861) }, savedLanguage, function(index, value)
        local code = ({ S(112), S(711), S(549) })[index]
        config.Set(S(864), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(764), S(231), S(483) })[savedLanguage] or S(307))
    log(S(27) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(511)] = function()
return {
    [79389059854988] = S(19),
}
end
import(S(725))(import)
