-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Auto Research: กันเงินไว้", "Badge", " -> x", "locked slot", "Players", "TextLabel", "games/my_anime_mine/get_best_miner", "'", "MineUpgradeCharacter", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "[GuiDump] === end (", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "", "core/debug_tab", "[AutoResearch] Core ", "table", "Damage", "UICorner", "core/get_me", "  [attr] ", "Humanoid", " not bought yet (", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "debug.getupvalues", "[Decompile] PlayerScripts not found", "MekPro", "core/anti_afk", "ตามเครื่อง", "games/my_anime_mine/get_plot_parts", "Auto Research: ซื้อสาย Zone", " (prompt says '", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "getcallbackvalue", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "en", "", "TextLabel", "autoUpgradeEquipPickaxe", "Auto Chest: เกณฑ์เก็บ", "Search functions...", "Interaction Logger", "autoChest", "CoreUI", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "ItemFrame", " (cost ", "getrenv", "write reported success but the line isn't in ", "disabled", " script(s) to ", ") -> ", "[AutoSell] ", "ProximityPrompt", "[AutoPlace] slot ", "core/debug_dump_remotes", "Auto Upgrade: รอบเช็ค", " error: ", "core/debug_dump_gui", "yes", "[BestMiner] slot ", "auto", "core/logger", "SellStand", "%$%s*([%d,%.]+)%s*(%a*)", "🐛", "enabled", "TextLabel", "PlayerGui", "en", ") ", " power ", "[Interaction] ProximityPrompt triggered: ", "open_menu_hint", " ", "auto", "RemoteFunction", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "walkSpeed", " files", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Dump All Remotes", "core/logger", "core/teleport", "/", " = ", "Crew", ")", "table", "[Bb]uy", " (", "%", "disabled", "works from anywhere - dropping the teleport", "", "languageIndex", "Frame", "core/get_place_id", "write reported success but ", "[Scheduler] ", "MineEquipPickaxe", "[AutoChest] waiting: ", " ", "UserInputService", "/s", "'", "s", "games/my_anime_mine/auto_upgrade", "[AntiAFK] enabled - watching Player.Idled", "MekPro", "GetCharacterUpgradeCost", "function", "core/i18n", " (", "[Uu]nlock", "autoMineSpeed", "Players", "TextLabel", "games/my_anime_mine/get_current_plot", "RunService", "Place", ")", "autoResearchKeepMoney", "TextLabel", "ValueLabel", "lowest", "[AutoChest] can't read the chest yet", "Locked miner slot", "Content", "[AutoPlace] ", "CharMaxLevel", "[AutoResearch] ", "Mutation: หยุดเมื่อได้ x", "core/debug_decompile", "table", "ย่อขยายเมนูทั้งชุด", "อ่านป้ายบน chest ทุกกี่วินาที", "[AutoUpgrade] ", "games/my_anime_mine/auto_chest", "core/i18n", "games/my_anime_mine/main", "ProximityPrompt", "getscriptbytecode", "core/logger", "M", "fill", "core/debug_decompile", " -> ", "autoPlace", "\n", "core/debug_dump_gui", "games/my_anime_mine/game_api", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", " (x", ") from ", "Decompile Client Scripts", "Players", "Auto Upgrade: แจก Pickaxe ให้ miner", " items (now ", " ", ")", "BodyVelocity", "", "Holders", "[Scheduler] ", "", "search_placeholder", "ค้นหาฟังก์ชัน...", " '", " (power ", "0 = ไม่จำกัด", "core/scheduler", "[AutoPlace] upgrading ", " ", "TextButton", "ตั้งค่า", "autoUpgradeKeepMoney", "antiAfk", "Players", "[AntiAFK] idle detected, simulating input", "core/ui", ": ", "ปุ่มเปิดเมนู", "getloadedmodules", "[Interaction] Key pressed: %s (gameProcessed=%s)", "getconnections", "[AutoPlace] fireproximityprompt error: ", "Frame", "games/my_anime_mine/get_best_miner", "autoUpgradePriority", "TextLabel", "[^%w]+", "  ", "Auto Place: เปลี่ยนตัวอ่อนออก", "Mining", "core/logger", "MineSpeed", "ScreenGui", "Players", "autoSell", "core/logger", "HumanoidRootPart", "TextButton", "TextLabel", "ClickDetector", "Content", "slot ", "unreadable", "lowest", "getnilinstances", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "=locked(", "GetCharacterSpeedUpgradeCost", " items)", "core/logger", "", "table", "[AutoPlace] nothing in the hotbar to place", "MekPro", "[Decompile] decompile unavailable in this executor", "cost", "ขนาดเมนู", " ชิ้น", "Speed", "Workspace", "[Interaction] Button activated: ", "HumanoidRootPart", "core/humanoid", "TextButton", "TextLabel", "(", "?", "enabled", "core/build_env", "HttpService", " (Text=", "[GameAPI] couldn't load the game's modules: ", "Speed", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "HotbarFrame", "", "[AutoPlace] disabled", "RunService", "core/debug_dump_remotes", "TextBox", "ถูกสุดก่อน", "TextLabel", "autoUpgradePickaxeIndex", "บิน (Fly)", "TextButton", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "UIPadding", " -> ", "MineSpeed", "games/my_anime_mine/auto_research", "[AutoSell] enabled", "fill", "games/my_anime_mine/get_plot_parts", "neither appendfile nor writefile is available", "autoPlaceInterval", "getgc", "games/my_anime_mine/auto_mine", "core/config", "Zones", "Chest", "s - jumping the queue", "[GuiDump] === visible PlayerGui tree ===", "enabled", "UIScale", "TextLabel", "th", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "action '", " mutation x", "#", "GuiButton", ")", " node(s) looked buyable but the server refused", "[AutoChest] chest is empty", "autoResearchCore", "games/my_anime_mine/auto_place", "games/my_anime_mine/game_api", "Pad", "autoResearchZones", "VirtualInputManager", "[AntiAFK] disabled", "x", "core/scheduler", "[AutoResearch] ", "[AutoUpgrade] every stat on every miner is maxed", "RemoteEvent", "TextButton", "open_menu_hint", "en", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "ReplicatedStorage", "table", "[Decompile] wrote ", "autoChestInterval", "core/get_place_id", "Frame", "PlayerScripts", "Plots", "🏠", "BasePart", " x", "Frame", "UIListLayout", "UICorner", "core/logger", ")", "ไล่ซื้อทุกกี่วินาที", "games/my_anime_mine/game_api", "core/debug_interaction_logger", "autoMine", "core/parse_money", " | ", "TabBar", "Frame", "Loot", "BodyPosition", "empty", "Frame", "search_placeholder", "disabled", "CoreGui", "ไล่อัปเกรด miner ทุกกี่วินาที", "core/logger", "^%$", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "autoPlaceUpgradeWeak", "Auto Research (Plot)", "Workspace", " (", "no isfile/readfile available to verify the write", "Open menu", " (x", "", "Text", "เดินทะลุกำแพงและพื้น", " (ActionText=", "core/logger", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "=empty", "Mining", "prompt disabled", "Players", "[Interaction] ClickDetector clicked: ", " -> ", "autoMineClicks", "games/my_anime_mine/auto_upgrade", "function", "Players", "AutoMine", "TextLabel", "This is not a supported place.", "  ", "core/logger", "[AutoUpgrade] ", "leaderstats", "core/logger", "GetHolderSpeedLevel", ",", "TextLabel", ") didn't take, trying the next one", "ลอย (Float)", "debug.getproto", "table", "Nodes", "autoUpgrade", "ModuleScript", "[AutoUpgrade] ", "WalkSpeed (ปกติ 16)", "Auto Mine", "games/my_anime_mine/auto_place", "hookfunction", "Players", "TextLabel", "^x(%d+)$", " screen(s)) ===", "-", "[Decompile] === executor capabilities ===", "Damage", "games/my_anime_mine/main", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "[AutoUpgrade] ", " -> ", "Frame", "MineBuyPickaxe", "debug.getconstants", "autoResearchInterval", ")", "[AutoSell] MineSellItems ", "table", "MekPro/decompiled", "ดีบัก", ")", "Auto Chest", " items", "Workspace", "core/i18n", "Frame", "LocalScript", " (", "TextButton", " while selecting", "Main", "^M(%d+)$", "autoUpgradeMutationBudget", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "UIListLayout", "Humanoid", "table", "MineBuySkill", "Auto Research: ซื้อสูงสุดต่อรอบ", "%.0f", "waiting", "dev", " pickaxe ", "GuiButton", "[^%.]+", "waiting", "games/registry", "Search", "float", "ต่ำสุดก่อน", "ความเร็ววิ่ง", "core/dispatch", "MineSpeed", "%$", "Auto Research: รอบเช็ค", "')", "/", ": ", "TextButton", ": ", "Auto Research: ซื้อสาย Core", "games/my_anime_mine/game_api", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "s", "MineTakeChest", "My Anime Mine · Mining Hub", "core/debug_tab", "autoUpgradeMaxPerPass", "x", "games/my_anime_mine/auto_chest", "getsenv", "Backpack", "Damage", " screens", "button has no size on screen", " waited ", "games/my_anime_mine/auto_sell", "แพงสุดก่อน", "^([%d,]+)$", "lowest", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "[Interaction] logger enabled - go click/interact with things now", "Players", "[MekPro] settings file: ", "games/my_anime_mine/game_api", "", "games/my_anime_mine/auto_research", "[AutoPlace] picked up ", "M", "MineSellItems", " didn't go through (out of stock or too expensive)", "[Decompile] === end ===", "⚙", "HumanoidRootPart", "[GuiDump] ", "/config.json", "games/my_anime_mine/get_plot_parts", " failed)", "autoUpgradePickaxe", "UserInputService", "JumpPower (ปกติ 50)", "Text", "[AutoPlace] ", "core/ui", "Hauler", "table", "/", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[^%d%.]", "GuiService", "", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "en-us", "Dump", "core/logger", " ", "OwnerUserId", "Auto Place: รอบเช็ค", "/", "TextButton", "[Remotes] === total: ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "BasePart", "interactionLogger", " (", "", "[Decompile] ", "autoUpgradePickaxeIndex", "Auto Upgrade Unit", "Auto Upgrade: Pickaxe ที่จะซื้อ", "[AutoResearch] nothing open and affordable right now", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "M", "แรงกระโดด", "MineGui", " (power ", "fly", "GetCharacterMineSpeedUpgradeCost", "cost", "ProximityPrompt", ")", " from ", "games/my_anime_mine/game_api", "X", "Container", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "", "", "Badge", "x", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Auto Place Miner", "autoUpgradeMutationTarget", "ValueLabel", "[Scheduler] turn: ", "TextLabel", "th", ": ", " remotes", "", " ", "languageIndex", "ClickDetector", "Players", "autoChestMinItems", "VirtualUser", "[AutoPlace] placed ", ",", "menuScale", "", ")", " owned)", " ", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/auto_mine", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "", "string", "s", "autoSellInterval", "AutoSell", ".lua", "PlayerGui", "Auto Upgrade: สุ่ม Mutation", "games/my_anime_mine/game_api", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "jumpPower", "ItemName", "Header", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "unreadable", "core/logger", "", "LayerCollector", "อังกฤษ", "Dump Visible GUI", "All", "core/teleport", "Mutation: เพดานราคาต่อครั้ง", "TextLabel", " '", "s", "Auto Mine: คลิกต่อรอบ", "costs ", "[AutoPlace] holders: ", "none", "Auto Sell", ") ", "[AutoSell] disabled", "Players", "", "PlotIndex", "[Config] couldn't write ", "AutoChest", "มุดแมพ (Noclip)", "MekPro.log", "decompile", "ProximityPrompt", " (Text=", "[AutoUpgrade] pickaxe #", "MineEquipSlot", "autoResearchMaxPerPass", "Frame", "core/config", "core/build_env", "[AutoResearch] ", "[AutoChest] MineTakeChest had no effect (", "", " ===", "CharSpeedMaxLevel", "empty", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " (", "[AutoUpgrade] bought pickaxe #", " -> ", "games/registry", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "CharMineSpeedMaxLevel", "ReplicatedStorage", "Auto Chest: รอบเช็ค", " still empty after placing ", "noclip", " (chest holds ", "AutoUpgrade", "Empty miner slot", "", "getscripts", "Frame", "autoResearch", "[AutoChest] collected ", "Anti-AFK", "Damage", "core/humanoid", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "UserInputService", "core/get_leaderstat", "[AutoPlace] drifted out of range of ", ")", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "Auto Sell: รอบขาย", "auto", "'", "core/debug_interaction_logger", " = ", "games/my_anime_mine/get_current_plot", "MineRollMutation", "s", "/", "Dump", "ไทย", "MekPro", "=", "[AutoMine] ", "Modules", "Speed", "x", "_", "core/click", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Auto Mine: ความเร็ว", "TextLabel", "core/logger", "autoResearchCheapestFirst", "Workspace", "[GuiDump] PlayerGui not found", "GetHolderLevel", "\n", "[%X] ", "sold ", ": ", "ScrollingFrame", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", ")", "GetHolderMineSpeedLevel", "[GameAPI] SendEvent failed: ", "core/anti_afk", "[Interaction] logger disabled", "PlayerGui", "games/my_anime_mine/auto_sell", "ClickDetector", "", " stack(s)", "core/logger", "ChestCapacity", "th", "[AutoChest] disabled", "nothing to place or upgrade", "autoUpgradeRollMutation", "string", "UIStroke", "no", "(nothing to do)", "core/logger", "[AutoChest] enabled (collects once >= ", "", "[AutoPlace] couldn't pick up ", "needs us at the stand - keeping the teleport", "autoUpgradeInterval", "maxed", "Auto Upgrade: กันเงินไว้", "sale didn't go through", "core/teleport", "TextButton", " doesn't exist", "GuiObject", "UIListLayout", "Run", "AutoResearch", "table", ": ", "th", "[Remotes] ", "Frame", "core/dispatch", "auto", ")", ") on ", "MineWorld", "core/get_place_id", "core/logger", "[Config] couldn't read ", "[Logger] failed to write ", "[Decompile] === start ===", "/", "Content", "menuScale", "AutoPlace", "ฟาร์ม", "upgrade"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(673)] = function()
return function(import)
local log = import(S(569))
local Players = game:GetService(S(116))
local VirtualUser = game:GetService(S(543))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(108))
        connection = player.Idled:Connect(function()
            log(S(179))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(287))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(234)] = function()
return { Env = S(419), IsDev = true }
end
modules[S(655)] = function()
local VirtualInputManager = game:GetService(S(286))
local GuiService = game:GetService(S(487))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(452)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(601)] = function()
return function(import)
local HttpService = game:GetService(S(235))
local getPlaceId = import(S(97))
local log = import(S(311))
local ROOT_FOLDER = S(26)
local placeFolder = ROOT_FOLDER .. S(84) .. tostring(getPlaceId())
local filePath = placeFolder .. S(473)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(369) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(718) .. filePath .. S(437) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(590) .. filePath .. S(707) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(133)] = function()
return function(import)
local Players = game:GetService(S(5))
local log = import(S(329))
local CAPABILITIES = {
    S(594), S(142), S(624), S(262), S(183),
    S(185), S(33), S(448), S(47), S(391),
    S(24), S(368), S(377), S(209),
}
local OUT_FOLDER = S(396)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(422)) do
        if type(target) ~= S(217) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(383))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(504) .. name .. S(85) .. ((ok and type(value) == S(111)) and S(59) or S(688)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(220))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(303))
    if not playerScripts then
        log(S(25))
        return 0
    end
    pcall(makefolder, S(219))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(404)) or target:IsA(S(372)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(686) and #source > 0 then
                local name = target:GetFullName():gsub(S(191), S(654))
                if pcall(writefile, OUT_FOLDER .. S(496) .. name .. S(559), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(299) .. written .. S(50) .. OUT_FOLDER
        .. S(90) .. failed .. S(475))
    return written
end
local function run()
    log(S(720))
    reportCapabilities()
    local count = dumpAll()
    log(S(469))
    return count
end
return run
end
end
modules[S(58)] = function()
return function(import)
local Players = game:GetService(S(378))
local log = import(S(143))
local function dumpVisible(root, indent)
    indent = indent or S(503)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(702)) and child.Visible then
            local line = indent .. child.ClassName .. S(578) .. child.Name .. S(105)
            if child:IsA(S(117)) or child:IsA(S(174)) then
                line = line .. S(236) .. tostring(child.Text) .. S(393)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(20) .. attrName .. S(641) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(192))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(675))
    if not playerGui then
        log(S(662))
        return 0
    end
    local screens = 0
    log(S(268))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(571)) and screen.Enabled and screen.Name ~= S(109) then
            screens = screens + 1
            log(S(472) .. screen.ClassName .. S(168) .. screen.Name .. S(8))
            dumpVisible(screen, S(358))
        end
    end
    log(S(11) .. screens .. S(381))
    return screens
end
return dumpGui
end
end
modules[S(245)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(297))
local log = import(S(717))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(292)) or descendant:IsA(S(76)) then
            count = count + 1
            log(S(709) .. descendant.ClassName .. S(181) .. descendant:GetFullName())
        end
    end
    log(S(498) .. count .. S(606))
    return count
end
return dumpRemotes
end
end
modules[S(315)] = function()
return function(import)
local Players = game:GetService(S(587))
local Workspace = game:GetService(S(661))
local UserInputService = game:GetService(S(632))
local log = import(S(195))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(72) .. describe(prompt)
            .. S(342) .. prompt.ActionText .. S(518))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(349) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(226) .. describe(button)
            .. S(596) .. tostring(button.Text) .. S(87))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(517)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(540)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(277)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(674))
        return
    end
    connections = {}
    log(S(459))
    local playerGui = Players.LocalPlayer:WaitForChild(S(68))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(53)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(677)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(421)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(184),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(444)] = function()
return function(import)
local interactionLogger = import(S(640))(import)
local dumpRemotes = import(S(55))(import)
local dumpGui = import(S(150))(import)
local decompileScripts = import(S(146))(import)
local humanoid = import(S(228))
local function build(window, persist)
    local tab = window:AddTab(S(397), S(65))
    persist.Toggle(
        tab:AddCard(
            S(41),
            S(509)
        ),
        S(501),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(81),
        S(252)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(491), function()
        remoteBtn.Text = dumpRemotes() .. S(536)
    end)
    local guiCard = tab:AddCard(
        S(573),
        S(211)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(646), function()
        guiBtn.Text = dumpGui() .. S(451)
    end)
    local decompileCard = tab:AddCard(
        S(155),
        S(656)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(704), function()
        decompileBtn.Text = decompileScripts() .. S(79)
    end)
    humanoid.Start()
    persist.Slider(tab, S(428), S(374),
        8, 200, S(78), 16, humanoid.SetWalkSpeed, S(216))
    persist.Slider(tab, S(511), S(478),
        30, 300, S(564), 50, humanoid.SetJumpPower, S(537))
    persist.Toggle(
        tab:AddCard(S(250), S(44)),
        S(514), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(592), S(341)),
        S(619), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(367), S(631)),
        S(426), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(711)] = function()
return function(import)
    local getPlaceId = import(S(301))
    local registry = import(S(613))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(357))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(633)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(361))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(19)] = function()
local Players = game:GetService(S(178))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(413))
    local backpack = player:WaitForChild(S(449))
    return player, humanoid, backpack
end
return getMe
end
modules[S(716)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(630)] = function()
local Players = game:GetService(S(348))
local RunService = game:GetService(S(244))
local UserInputService = game:GetService(S(103))
local Workspace = game:GetService(S(225))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(21))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(201))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(161))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(306)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(500)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(322))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(139)] = function()
local Players = game:GetService(S(460))
local STRINGS = {
    en = {
        search_placeholder = S(40),
        open_menu_hint = S(337),
    },
    th = {
        search_placeholder = S(167),
        open_menu_hint = S(182),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(490)
    if locale:sub(1, 2):lower() == S(708) then
        return S(534)
    end
    return S(35)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(61) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(362)] = function()
local LOG_FILE = S(593)
local function log(message)
    local line = os.date(S(665)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(570))
            end
            appendfile(LOG_FILE, line .. S(664))
        elseif writefile then
            local existing = S(339)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(149))
        else
            error(S(260))
        end
        if not (isfile and readfile) then
            error(S(336))
        end
        if not isfile(LOG_FILE) then
            error(S(98) .. LOG_FILE .. S(701))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(48) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(719) .. LOG_FILE .. S(435) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(317)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(555) then
        return nil
    end
    local number, suffix = text:match(S(64))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(364), S(488))))
    if not value then
        return nil
    end
    if suffix ~= S(165) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(289)] = function()
return function(import)
local RunService = game:GetService(S(119))
local log = import(S(492))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(164) .. task_.name .. S(57) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(99) .. starved.name .. S(453)
            .. string.format(S(417), now - starved.waitingSince)
            .. S(267))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(532) .. (currentTask and currentTask.name or S(689)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(83)] = function()
local Players = game:GetService(S(354))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(471))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(180)] = function()
return function(import)
local i18n = import(S(402))
local UserInputService = game:GetService(S(477))
local THEME = {
    BrandName = S(648),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(18))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(310))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(253))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(436))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(605)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(687))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(203))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(232)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(356))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(524)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(668))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(412))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(365))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(308))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(248))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(123))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(230))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(531))
            if not valueLbl then
                valueLbl = Instance.new(S(271))
                valueLbl.Name = S(124)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(2))
            if not badge then
                badge = Instance.new(S(6))
                badge.Name = S(526)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(251))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(625))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(96))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(406))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(623)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(91)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(389))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(600))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(700))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(463)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(710))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(309))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(293))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(479)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(197))
    screenGui.Name = S(43)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(327))
    local main = Instance.new(S(320))
    main.Name = S(408)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(270))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(324))
    tabBar.Name = S(319)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(703))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(403))
    header.Name = S(566)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(229))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(379))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(190))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(36)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(497))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(521)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(202))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(382)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(246))
    searchBox.Name = S(425)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(166))
    searchBox.Text = S(588)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(302))
    content.Name = S(128)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(187))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(577))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(73))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(37))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(340)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(13) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(493) .. (item.Subtitle or S(554))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(325))
        footerLabel.Text = i18n.T(S(294))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(138)] = function()
return function(import)
local gameApi = import(S(520))(import)
local log = import(S(215))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(691) .. minItemsToCollect .. S(214))
    else
        log(S(683))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(207) then
            log(S(126))
            lastReported = S(568)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(608) then
            log(S(280))
            lastReported = S(323)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(418) then
            local capacity = gameApi.ChestCapacity()
            log(S(101) .. count .. S(434) .. want .. S(400)
                .. (capacity and (S(620) .. capacity .. S(160)) or S(692)))
            lastReported = S(423)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(627) .. count .. S(158) .. after .. S(635))
        else
            log(S(604) .. count
                .. S(388) .. tostring(after) .. S(121))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(263)] = function()
return function(import)
local getCurrentPlot = import(S(642))
local getPlotParts = import(S(259))
local log = import(S(659))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(204), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(650) .. (enabled and S(66) or S(92)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(282)] = function()
return function(import)
local Players = game:GetService(S(198))
local getCurrentPlot = import(S(551))
local getPlotParts = import(S(474))
local getMinerSlots = import(S(7))(import)
local teleport = import(S(699))
local gameApi = import(S(462))(import)
local log = import(S(62))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(622)
local LOCKED_SLOT_TEXT = S(127)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(284))
    return pad and pad:FindFirstChildWhichIsA(S(141), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(533)) then
            local digits = descendant.Text:match(S(456))
            if digits then
                return tonumber((digits:gsub(S(545), S(242))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(658)) and descendant.Text:match(S(330)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(206) .. holder.Name .. S(22)
            .. gameApi.MinerSlots() .. S(645) .. gameApi.MaxMinerSlots() .. S(549)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(4)
    end
    if not prompt.Enabled then
        return true, S(347)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(581) .. price
    end
    local action = prompt.ActionText or S(678)
    if action:match(S(114)) or action:match(S(89)) or action:match(S(431)) then
        return true, S(274) .. action .. S(639)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(227))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(186) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(212) .. why .. S(713))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(345))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(649) .. prompt.ObjectText .. S(231) .. power .. S(312))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(318))
    if line ~= lastSurvey then
        log(S(582) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(145), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(726), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(480) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(243))
    else
        lastAttempt = 0
        log(S(386))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(485),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(120), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(218))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(634) .. holder.Name .. S(407))
            return false
        end
        log(S(54) .. candidate.slot .. S(335) .. candidate.name
            .. S(366))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(129) .. holder.Name .. S(618) .. miner.name)
        return false
    end
    log(S(544) .. miner.name .. S(513) .. miner.power
        .. S(714) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(693) .. entry.name .. S(519) .. entry.holder.Name
            .. S(31) .. tostring(entry.prompt.ActionText) .. S(433))
        return false
    end
    log(S(465) .. entry.name .. S(169) .. entry.power
        .. S(154) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(684))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(258) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(172) .. target.holder.Name .. S(535) .. target.name
        .. S(610) .. target.power .. S(51) .. best.name .. S(502) .. best.power .. S(548))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(464)] = function()
return function(import)
local gameApi = import(S(439))(import)
local log = import(S(82))
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(603) .. (enabled and S(233) or S(49)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
            end
        end
    end
    return list
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    if buyZones then
        local nodes = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(131) .. node.zoneKey .. S(102) .. node.id
                    .. S(113) .. node.family .. S(70) .. node.level .. S(254) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if buyCore then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(15) .. node.id
                    .. S(405) .. node.family .. S(585) .. node.level .. S(147) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(290) .. blocked
                .. S(279))
        else
            log(S(508))
        end
    end
    busy = false
end
function autoResearch.HasWork()
    if buyZones and #buyableZoneNodes() > 0 then
        return true
    end
    return buyCore and #buyableCoreNodes() > 0
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(454)] = function()
return function(import)
local Workspace = game:GetService(S(401))
local gameApi = import(S(314))(import)
local teleport = import(S(575))
local log = import(S(343))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(586))
    else
        lastAttempt = 0
        log(S(257))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(63))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(595), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(394)
                .. (sold and S(93)
                         or S(694)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(52) .. (sold and (S(666) .. #keys .. S(679)) or S(698)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(107)] = function()
return function(import)
local gameApi = import(S(562))(import)
local log = import(S(690))
local STATS = { S(629), S(238), S(255) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(457)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(387) .. (enabled and S(269) or S(326)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(134) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(125) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(221) or S(696)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(360) .. holderKey .. S(173) .. choice.stat .. S(159)
                        .. choice.info.level .. S(612) .. after.level
                        .. S(484) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(516) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function findPickaxe()
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local before = pickaxe.ownedCount
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    if ownedCountOf(pickaxe.name) > before then
        log(S(611) .. pickaxe.index .. S(74) .. pickaxe.name
            .. S(153) .. pickaxe.multiplier .. S(670))
        return true
    end
    log(S(597) .. pickaxe.index .. S(538) .. pickaxe.name
        .. S(468))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(373) .. holderKey .. S(420)
                .. tostring(entry and entry.Pickaxe or S(583)) .. S(350) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(137) .. holderKey .. S(275) .. variant
                .. S(3) .. after .. S(46) .. cost .. S(278))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass and tryPickaxe() then
        bought = bought + 1
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(273))
        elseif maxed > 0 then
            log(S(291))
        else
            log(S(440))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(16) and entry.Pickaxe and entry.Pickaxe ~= S(525) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(151)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(616))
local log = import(S(359))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(651), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(237) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(483))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(414) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(482) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(88) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(409))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(672) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(442))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(574), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(395) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(467), keys)
end
function api.EquipSlot(key)
    return send(S(598), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(390), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(100), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(643), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(415), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(298)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.GetSkillNodes(zoneKey) or {}) do
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            table.insert(list, {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            })
        end
    end)
    return list
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(353) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(9), characterKey, stat == S(194) and S(430) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(663), max = S(130), cost = S(110) },
    Speed = { level = S(363), max = S(607), cost = S(213) },
    MineSpeed = { level = S(671), max = S(615), cost = S(515) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(706) then
        return 0
    end
    return tonumber(result.Total) or 0
end
return api
end
end
modules[S(188)] = function()
return function(import)
local Players = game:GetService(S(156))
local log = import(S(200))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(560))
    local mineGui = playerGui and playerGui:FindFirstChild(S(512))
    local hotbar = mineGui and mineGui:FindFirstChild(S(241))
    local content = hotbar and hotbar:FindFirstChild(S(722))
    return content and content:FindFirstChild(S(522))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(67)) then
            local count = descendant.Text:match(S(380))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(45))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(205))
    local nameLabel = content and content:FindFirstChild(S(565))
    local damageLabel = content and content:FindFirstChild(S(450))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(162) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(486), S(94)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(721) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(60) .. slot .. S(667) .. info.name
                        .. S(71) .. info.power .. S(307) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(118)] = function()
local Players = game:GetService(S(541))
local Workspace = game:GetService(S(334))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(715))
    local plots = mineWorld and mineWorld:FindFirstChild(S(304))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(494)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(29)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(163)),
        Nodes = plot:FindFirstChild(S(370)),
        Chest = plot:FindFirstChild(S(266)),
        Zones = plot:FindFirstChild(S(265)),
        Crew = plot:FindFirstChild(S(86)),
        Loot = plot:FindFirstChild(S(321)),
        PlotIndex = plot:GetAttribute(S(589)),
        ChestCapacity = plot:GetAttribute(S(681)),
    }
end
return getPlotParts
end
modules[S(140)] = function()
return function(import)
    local createWindow = import(S(481))(import)
    local i18n = import(S(112))
    local antiAfk = import(S(27))(import)
    local buildEnv = import(S(602))
    local scheduler = import(S(171))(import)
    local config = import(S(264))(import)
    local log = import(S(680))
    local window = createWindow({
        Subtitle = S(443),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(552))(import)
    local autoPlace = import(S(376))(import)
    local autoChest = import(S(447))(import)
    local autoSell = import(S(676))(import)
    local autoUpgrade = import(S(352))(import)
    local autoResearch = import(S(256))(import)
    local gameApi = import(S(283))(import)
    scheduler.RegisterStandalone(S(355), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(591), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(724), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(621), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(705), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(558), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, 90)
    scheduler.Start()
    local farmTab = window:AddTab(S(725), S(305))
    persistentToggle(farmTab:AddCard(
        S(375),
        S(499)
    ), S(316), autoMine.SetEnabled)
    persistentSlider(farmTab, S(657), S(210),
        1, 30, S(115), 5, autoMine.SetCyclesPerSecond, S(104))
    persistentSlider(farmTab, S(580), S(523),
        1, 10, S(351), 1, autoMine.SetClicksPerCycle, S(288))
    persistentToggle(farmTab:AddCard(
        S(529),
        S(458)
    ), S(148), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(495), S(489),
        1, 60, S(261), 3, autoPlace.SetCheckInterval, S(579))
    persistentToggle(
        farmTab:AddCard(S(193),
            S(10)),
        S(332), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(399),
        S(34)
    ), S(42), autoChest.SetEnabled)
    persistentSlider(farmTab, S(39), S(528),
        50, 3000, S(542), 100, autoChest.SetMinItems, S(223))
    persistentSlider(farmTab, S(617), S(136),
        1, 60, S(300), 3, autoChest.SetCheckInterval, S(106))
    persistentToggle(farmTab:AddCard(
        S(584),
        S(32)
    ), S(199), autoSell.SetEnabled)
    persistentSlider(farmTab, S(637), S(553),
        10, 300, S(557), 45, autoSell.SetCheckInterval, S(441))
    persistentToggle(farmTab:AddCard(
        S(506),
        S(239)
    ), S(371), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(56), S(328),
        5, 300, S(695), 20, autoUpgrade.SetInterval, S(556))
    persistentSlider(farmTab, S(636), S(77),
        1, 20, S(445), 5, autoUpgrade.SetMaxPerPass, S(446))
    persistentSegmented(
        farmTab,
        { S(427), S(384), S(224), S(346) },
        { S(208), S(17), S(652), S(196) },
        S(189), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(697), S(411),
        0, 500, S(176), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(466))
    persistentToggle(
        farmTab:AddCard(S(296),
            S(567)),
        S(476), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(338) .. pickaxe.multiplier .. S(398)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(240)
            end
            return S(276) .. index .. S(550) .. (pickaxeLabels[index] or S(609))
        end
        local savedPick = config.Get(S(505), 0)
        pickaxeCard = farmTab:AddSlider(
            S(507),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(249), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(547)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(157),
            S(563)),
        S(38), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(561),
            S(80)),
        S(685), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(132), S(23),
        1, 20, S(530), 2, autoUpgrade.SetMutationTarget, S(653))
    persistentSlider(farmTab, S(576), S(170),
        0, 500, S(410), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(144))
    persistentToggle(farmTab:AddCard(
        S(333),
        S(614)
    ), S(626), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(432), S(313),
        10, 600, S(392), 30, autoResearch.SetCheckInterval, S(644))
    persistentSlider(farmTab, S(416), S(344),
        1, 20, S(599), 5, autoResearch.SetMaxPerPass, S(527))
    persistentSlider(farmTab, S(1), S(331),
        0, 500, S(122), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(510))
    persistentToggle(
        farmTab:AddCard(S(30),
            S(12)),
        S(285), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(438),
            S(152)),
        S(281), autoResearch.SetBuyCore, true
    )
    persistentSegmented(
        farmTab,
        { S(247), S(455) },
        { true, false },
        S(660), 1, autoResearch.SetCheapestFirst
    )
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(14))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(175), S(470))
    persistentToggle(
        settingsTab:AddCard(S(628), S(669)),
        S(177),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(723), 100)
    settingsTab:AddSlider(S(222), S(135), 50, 150, savedScale, function(value)
        config.Set(S(546), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(539), 1)
    settingsTab:AddSegmented({ S(28), S(572), S(647) }, savedLanguage, function(index, value)
        local code = ({ S(638), S(295), S(272) })[index]
        config.Set(S(95), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(712), S(69), S(682) })[savedLanguage] or S(75))
    log(S(461) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(424)] = function()
return {
    [79389059854988] = S(385),
}
end
import(S(429))(import)
