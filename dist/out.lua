-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {" = ", "Close", "RemoteFunction", "no isfile/readfile available to verify the write", " ", "core/logger", "[AutoChest] fireproximityprompt error: ", "  [attr] ", "[AutoSell] disabled", "TextLabel", "disabled", "Frame", "", " = ", "[AutoSell] enabled (only sells when there's something in the scroll)", "write reported success but ", "Dump", "SelectAllButton", "games/my_anime_mine/get_current_plot", "Close", "Label", "ChestCapacity", "ClickDetector", "disabled", "core/turn_scheduler", "ValueLabel", "[MyAnimeMine] not standing in any plot", "RemoteFunction", ": ", "games/my_anime_mine/get_current_plot", "ฟาร์ม", " (ActionText=", "Badge", "th", "CoreUI", "[AutoUpgrade] ", " = ", "games/my_anime_mine/get_plot_parts", "core/ui", "ClickDetector", "Debug: Current Plot", "Players", "Check", "Content", "[AntiAFK] idle detected, simulating input", "core/logger", "[MyAnimeMine] === Tool: ", "en-us", "[AutoChest] click failed on ", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "CoreGui", "games/my_anime_mine/debug_dump_gui", "Select All", " ===", "Done", " ===", "leaderstats", ",", "Search", "[Interaction] logger enabled - go click/interact with things now", "core/dispatch", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", ": ", "[Scheduler] ", "[AutoPlace] ", "core/logger", " items, closing", "", "ปุ่มเปิดเมนู", "Anti-AFK", "en", "AutoMine", "[AutoSell] sell menu didn't open, aborting", "TextButton", "Players", "games/my_anime_mine/get_current_plot", "Backpack", "disabled", "[GuiDump] === visible MineGui tree ===", "UserInputService", "Frame", "ค้นหาฟังก์ชัน...", "'", "games/my_anime_mine/debug_dump_gui", "[AutoChest] only ", "Content", "?", "core/interaction_logger", "core/click", "ClickDetector", "Backpack", "Error: ", " remotes", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Badge", "core/click", "TextLabel", "games/my_anime_mine/auto_sell", "[AutoSell] nothing to sell, closing", "TextButton", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "games/my_anime_mine/get_plot_parts", "core/logger", "ChestFrame", "Content", "games/my_anime_mine/auto_chest", "[AutoChest] disabled", "s", "Plots", "no tool", "games/my_anime_mine/debug_dump", "AutoPlace", "ClickDetector", "SellFrame", "core/i18n", "  [attr] ", "Content", "GuiObject", "[SellCards] === total cards: ", "Frame", "Frame", "MekPro.log", "Auto Chest", "Workspace", "Card", "UserInputService", "TabBar", "TextLabel", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "ReplicatedStorage", " items", "open_menu_hint", "games/my_anime_mine/main", ": ", "Frame", "[Scheduler] turn: ", "TextLabel", "MineWorld", "enabled", "Players", "ScrollingFrame", "UIPadding", "'), backing off ", "Close", "Zones", "AutoUpgrade", "[AutoPlace] ", "core/teleport", "th", "ย่อขยายเมนูทั้งชุด", "Frame", "  ", ": ", "", "ValueLabel", "OwnerUserId", "Players", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "[GuiDump] === end ===", ")", "Empty miner slot", "MineGui", " ===", "[AutoChest] enabled (collects once >= ", "core/logger", "Workspace", "TextButton", "core/ui", " cards)", "[AutoSell] click failed on ", "AutoSell", "ตั้งค่า", ": ", "en", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "button has no size on screen", "RunService", "games/my_anime_mine/auto_mine", "TextButton", "core/logger", "Interaction Logger (debug)", "Content", "log failed", "TextButton", "TextButton", "not standing in any plot", "'", "no character/backpack", "games/my_anime_mine/get_current_plot", "-", "no tool in Backpack (all miners already placed?)", "HumanoidRootPart", "RemoteEvent", "ไทย", "[AntiAFK] disabled", "Loot", "Frame", "core/teleport", "This is not a supported place.", "Auto Upgrade", "ProximityPrompt", "plot has no Holders folder", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "th", "Card", "Players", "My Anime Mine · Mining Hub", "Sell for", "", "PlayerGui", "core/logger", "ProximityPrompt", "", "TextBox", "", "MekPro", "[AntiAFK] enabled - watching Player.Idled", "", "core/teleport", " = ", "[AutoSell] fireproximityprompt error: ", "UICorner", "\n", "core/get_place_id", "ตามเครื่อง", "ProximityPrompt", "%", "ProximityPrompt", " (Text=", " (MaxActivationDistance=%s)", " -> ", "games/my_anime_mine/debug_dump_sell_cards", " items)", "TakeButton", "[AutoPlace] fireproximityprompt error: ", "Dump", "Debug: Dump Backpack Tool", "", "", "core/dispatch", "core/logger", "Debug: Dump Visible GUI", "[GuiDump] MineGui not found", "Progress", "search_placeholder", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "UIScale", "no empty holder found (", "Crew", "[Interaction] ProximityPrompt triggered: ", "games/registry", "Humanoid", "", " '", "Header", "PlayerGui", "ProximityPrompt", "TextLabel", " cards", ")", "[Interaction] Key pressed: %s (gameProcessed=%s)", "Dump", "core/i18n", "Frame", " ===", "games/my_anime_mine/get_plot_parts", "Auto Sell", "PlayerGui", "no plot", "[AutoChest] Take All had NO effect (", "UpgradePart", "games/my_anime_mine/auto_upgrade", "TextButton", "MineGui", "search_placeholder", "games/my_anime_mine/debug_dump_remotes", "Players", "Done", "games/my_anime_mine/get_plot_parts", "Dump", "[MyAnimeMine] === Dump start ===", "PlayerGui", "Debug: Dump All Remotes", "VirtualUser", "'", "Scroll", "TextLabel", " <-- remote, likely fired by the real interaction", "[MyAnimeMine] no holders found", "Pad", "⚙", " '", "core/anti_afk", "UIListLayout", "PlayerGui", " -> ", "write reported success but the line isn't in ", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Nodes", "\n", "core/logger", "ProximityPrompt", "UIListLayout", "Main", "", "games/my_anime_mine/auto_place", "Scroll", "Tool", "games/my_anime_mine/get_plot_parts", "Frame", "Text", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "UICorner", "ValueBase", "%", "X", "VirtualInputManager", "Players", " doesn't exist", "[MyAnimeMine] === Holder: ", "Auto Mine", "core/get_leaderstat", " cards left, label = '", "Tool", "Players", "Backpack", "games/my_anime_mine/get_current_plot", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Open menu", "Debug: Dump Node/Holder", "Players", "Content", "auto", "not found", "Content", "TextLabel", "GuiService", "[SellCards] Card #", "neither appendfile nor writefile is available", "core/logger", "enabled", "TextLabel", "[Interaction] Button activated: ", "core/logger", "Label", "core/i18n", "games/my_anime_mine/main", "Sell for", " holders checked)", "[AutoMine] ", ")", "games/my_anime_mine/auto_sell", "[Remotes] === total: ", "[Interaction] logger disabled", "TextButton", "Auto Place Miner", "[AutoSell] clicked SelectAllButton (", "Done", "Dump", "  ", "enabled", "games/my_anime_mine/auto_chest", "Holders", "Players", "RemoteEvent", "[MyAnimeMine] no nodes present right now", " items)", " = ", "GuiButton", "TextLabel", "", "  [attr] ", "core/click", "", "Chest", "games/my_anime_mine/debug_dump_remotes", "Debug: Dump Sell Cards", "Humanoid", "SellStand", "games/my_anime_mine/get_current_plot", "games/registry", "Search functions...", "open_menu_hint", "core/logger", "[AutoPlace] placed ", "(none enabled)", "MineGui", " '", "[MyAnimeMine] === Node: ", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/anti_afk", " '", "Frame", "no humanoid", "core/get_me", "core/turn_scheduler", "GuiObject", "SellButton", "auto", "games/my_anime_mine/debug_dump_sell_cards", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", " (Text=", "Players", "[%X] ", "TextButton", "games/my_anime_mine/auto_mine", "^(%d+)", "[AutoChest] chest didn't open, aborting", "GuiButton", "ขนาดเมนู", "อังกฤษ", "SellFrame", "TextLabel", "core/interaction_logger", "core/get_place_id", " ===", "games/my_anime_mine/auto_place", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "games/my_anime_mine/get_plot_parts", "Frame", "core/logger", "AutoChest", "Text", "[AutoSell] nothing selectable (", "🏠", "", "TextLabel", "", "ScreenGui", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "TextLabel", "  ", "games/my_anime_mine/debug_dump", "[Remotes] ", "MineGui", " on ", "Workspace", "Actions", "[AutoSell] clicked SellButton", "PlotIndex", "[AutoChest] Take All worked: ", "UIStroke", "TextLabel", "UIListLayout", "games/my_anime_mine/auto_upgrade", "'", "[Interaction] ClickDetector clicked: ", "[Logger] failed to write ", " error: ", "Players", "ClickDetector", "  [attr] "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(391)] = function()
return function(import)
local log = import(S(241))
local Players = game:GetService(S(42))
local VirtualUser = game:GetService(S(284))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(217))
        connection = player.Idled:Connect(function()
            log(S(45))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(195))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(373)] = function()
local VirtualInputManager = game:GetService(S(317))
local GuiService = game:GetService(S(337))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(176)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(240)] = function()
return function(import)
    local getPlaceId = import(S(415))
    local registry = import(S(251))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(199))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(322)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(57))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(395)] = function()
local Players = game:GetService(S(403))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(378))
    local backpack = player:WaitForChild(S(91))
    return player, humanoid, backpack
end
return getMe
end
modules[S(224)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(263)] = function()
local Players = game:GetService(S(277))
local STRINGS = {
    en = {
        search_placeholder = S(382),
        open_menu_hint = S(329),
    },
    th = {
        search_placeholder = S(82),
        open_menu_hint = S(69),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(48)
    if locale:sub(1, 2):lower() == S(204) then
        return S(149)
    end
    return S(174)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(399) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(414)] = function()
return function(import)
local Players = game:GetService(S(318))
local Workspace = game:GetService(S(166))
local UserInputService = game:GetService(S(126))
local log = import(S(6))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(250) .. describe(prompt)
            .. S(32) .. prompt.ActionText .. S(160))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(447) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(343) .. describe(button)
            .. S(402) .. tostring(button.Text) .. S(351))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(201)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(90)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(409)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(354))
        return
    end
    connections = {}
    log(S(60))
    local playerGui = Players.LocalPlayer:WaitForChild(S(210))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(257)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(451)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(369)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(261),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(66)] = function()
local LOG_FILE = S(122)
local function log(message)
    local line = os.date(S(404)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(374))
            end
            appendfile(LOG_FILE, line .. S(300))
        elseif writefile then
            local existing = S(209)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(223))
        else
            error(S(339))
        end
        if not (isfile and readfile) then
            error(S(4))
        end
        if not isfile(LOG_FILE) then
            error(S(16) .. LOG_FILE .. S(319))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(297) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(448) .. LOG_FILE .. S(173) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(219)] = function()
local Players = game:GetService(S(157))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(192))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(25)] = function()
return function(import)
local RunService = game:GetService(S(177))
local log = import(S(384))
local SLICE_SECONDS = 3
local scheduler = {}
local tasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.Register(name, step, isEnabled, isBusy)
    table.insert(tasks, { name = name, step = step, isEnabled = isEnabled, isBusy = isBusy })
end
local function anyBusy()
    for _, task_ in ipairs(tasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickNextEnabled()
    if #tasks == 0 then
        return nil
    end
    for _ = 1, #tasks do
        rrIndex = (rrIndex % #tasks) + 1
        local candidate = tasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        if anyBusy() then
            return
        end
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(136) .. (currentTask and currentTask.name or S(386)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            local ok, err = pcall(currentTask.step)
            if not ok then
                log(S(64) .. currentTask.name .. S(449) .. tostring(err))
            end
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(168)] = function()
return function(import)
local i18n = import(S(115))
local UserInputService = game:GetService(S(80))
local THEME = {
    BrandName = S(216),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(313))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(222))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(142))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(273))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(13)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(442))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(443))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(87)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(342))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(239)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(141))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(303))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(128))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(12))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(258))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(287))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(137))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(26))
            if not valueLbl then
                valueLbl = Instance.new(S(431))
                valueLbl.Name = S(155)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(33))
            if not badge then
                badge = Instance.new(S(97))
                badge.Name = S(95)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(184))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(151))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(197))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(355))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(215)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(315))
        local track = Instance.new(S(264))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(121))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(405))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(305)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(227)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(120))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(294))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(185))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(423)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(429))
    screenGui.Name = S(35)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(51))
    local main = Instance.new(S(310))
    main.Name = S(304)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(247))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(420))
    tabBar.Name = S(127)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(444))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(393))
    header.Name = S(255)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(74))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(413))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(336))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(428)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(179))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(316)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(167))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(190)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(214))
    searchBox.Name = S(59)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(275))
    searchBox.Text = S(218)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(135))
    content.Name = S(182)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(81))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(370))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(383))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(427))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(311)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(154) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(5) .. (item.Subtitle or S(68))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(245))
        footerLabel.Text = i18n.T(S(132))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(106)] = function()
return function(import)
local Players = game:GetService(S(331))
local getCurrentPlot = import(S(380))
local getPlotParts = import(S(309))
local teleport = import(S(148))
local click = import(S(89))
local log = import(S(301))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(268))
    local mineGui = playerGui and playerGui:FindFirstChild(S(162))
    return mineGui and mineGui:FindFirstChild(S(104))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(332))
    local actions = content and content:FindFirstChild(S(438))
    return actions and actions:FindFirstChild(S(234))
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(44))
    local progress = content and content:FindFirstChild(S(244))
    local label = progress and progress:FindFirstChild(S(21))
    local text = label and label.Text or S(238)
    local digits = text:gsub(S(58), S(253)):match(S(407))
    return tonumber(digits)
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(49) .. button.Name .. S(153) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(302), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(107))
    else
        lastAttempt = 0
        log(S(164) .. MIN_ITEMS_TO_COLLECT .. S(367))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(7) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(408))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    if count and count < MIN_ITEMS_TO_COLLECT then
        log(S(85) .. count .. S(67))
        local closeButton = chestFrame:FindFirstChild(S(2))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(441) .. count .. S(231) .. after .. S(131))
        else
            log(S(270) .. tostring(count)
                .. S(296) .. tostring(after) .. S(233))
        end
    end
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(178)] = function()
return function(import)
local getCurrentPlot = import(S(189))
local getPlotParts = import(S(38))
local log = import(S(180))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(23), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(350) .. (enabled and S(341) or S(24)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(417)] = function()
return function(import)
local Players = game:GetService(S(364))
local getCurrentPlot = import(S(76))
local getPlotParts = import(S(419))
local log = import(S(344))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(290))
    return pad and pad:FindFirstChildWhichIsA(S(212), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(161)
end
local autoPlace = {}
local enabled = false
local lastFire = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(147) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    log(S(65) .. (enabled and S(139) or S(11)))
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local player = Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild(S(326))
    if not character or not backpack then
        reason(S(188))
        return
    end
    local humanoid = character:FindFirstChildOfClass(S(252))
    local tool = backpack:FindFirstChildWhichIsA(S(308))
    if not humanoid then
        reason(S(394))
        return
    end
    if not tool then
        reason(S(191))
        return
    end
    local plot = getCurrentPlot()
    if not plot then
        reason(S(186))
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        reason(S(202))
        return
    end
    local placed = false
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderEmpty(prompt) then
            humanoid:EquipTool(tool)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if ok then
                log(S(385) .. tool.Name .. S(436) .. holder.Name)
            else
                log(S(235) .. tostring(err))
            end
            placed = true
            break
        end
    end
    if not placed then
        reason(S(248) .. #parts.Holders:GetChildren() .. S(349))
    else
        lastReason = nil
    end
end
return autoPlace
end
end
modules[S(352)] = function()
return function(import)
local Players = game:GetService(S(206))
local Workspace = game:GetService(S(124))
local teleport = import(S(198))
local click = import(S(96))
local log = import(S(211))
local CHECK_INTERVAL = 3
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(295))
    local mineGui = playerGui and playerGui:FindFirstChild(S(274))
    return mineGui and mineGui:FindFirstChild(S(114))
end
local function labelText(button)
    local content = button:FindFirstChild(S(117))
    local label = content and content:FindFirstChild(S(345))
    return label and label.Text or S(371)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(335))
    local scroll = content and content:FindFirstChild(S(286))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(125) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(170) .. button.Name .. S(29) .. tostring(err))
    end
    return ok
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(379))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(226), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(9))
    else
        lastAttempt = 0
        log(S(15))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(221) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(73))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(99))
        local closeButton = sellFrame:FindFirstChild(S(20))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(105))
    local selectAllButton = content and content:FindFirstChild(S(18))
    local sellButton = content and content:FindFirstChild(S(398))
    if selectAllButton and labelText(selectAllButton) == S(53) then
        clickButton(selectAllButton)
        log(S(357) .. cardCount .. S(169))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(348), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(208), 1, true) then
        clickButton(sellButton)
        log(S(439))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(144))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(424) .. cardCount .. S(323)
            .. tostring(sawLabel) .. S(143) .. NOTHING_SELLABLE_COOLDOWN .. S(108))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(445)] = function()
return function(import)
local getCurrentPlot = import(S(30))
local getPlotParts = import(S(266))
local log = import(S(340))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(271))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(113))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(36) .. (enabled and S(361) or S(78)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(433)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(213)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(254) .. child.Name .. S(83)
        if child:IsA(S(228)) then
            line = line .. string.format(
                S(312),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(40)) then
            line = line .. string.format(S(230), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(193)) or child:IsA(S(3)) then
            line = line .. S(288)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(452) .. attrName .. S(220) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(152))
    end
end
return dumpInstance
end
modules[S(52)] = function()
return function(import)
local Players = game:GetService(S(140))
local log = import(S(421))
local function dumpVisible(root, indent)
    indent = indent or S(426)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(397)) and child.Visible then
            local line = indent .. child.ClassName .. S(292) .. child.Name .. S(285)
            if child:IsA(S(10)) or child:IsA(S(100)) then
                line = line .. S(229) .. tostring(child.Text) .. S(260)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(116) .. attrName .. S(37) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(360))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(282))
    local mineGui = playerGui and playerGui:FindFirstChild(S(387))
    if not mineGui then
        log(S(243))
        return
    end
    log(S(79))
    dumpVisible(mineGui)
    log(S(159))
end
return dumpMineGui
end
end
modules[S(376)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(130))
local log = import(S(103))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(365)) or descendant:IsA(S(28)) then
            count = count + 1
            log(S(434) .. descendant.ClassName .. S(134) .. descendant:GetFullName())
        end
    end
    log(S(353) .. count .. S(265))
    return count
end
return dumpRemotes
end
end
modules[S(232)] = function()
return function(import)
local Players = game:GetService(S(325))
local log = import(S(46))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(256))
    local mineGui = playerGui and playerGui:FindFirstChild(S(435))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(412))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(86)) and sellFrame.Content:FindFirstChild(S(307))
    if not scroll then
        log(S(298))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(205) or card:IsA(S(118)) then
            count = count + 1
            log(S(338) .. count .. S(63) .. card.ClassName .. S(392) .. card.Name .. S(446))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(8) .. attrName .. S(1) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(432) .. child.ClassName .. S(388) .. child.Name .. S(187)
                if child:IsA(S(314)) then
                    line = line .. S(14) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(119) .. count .. S(163))
    return count
end
return dumpSellCards
end
end
modules[S(327)] = function()
local Players = game:GetService(S(450))
local Workspace = game:GetService(S(437))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(138))
    local plots = mineWorld and mineWorld:FindFirstChild(S(109))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(156)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(102)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(363)),
        Nodes = plot:FindFirstChild(S(299)),
        Chest = plot:FindFirstChild(S(375)),
        Zones = plot:FindFirstChild(S(145)),
        Crew = plot:FindFirstChild(S(249)),
        Loot = plot:FindFirstChild(S(196)),
        PlotIndex = plot:GetAttribute(S(440)),
        ChestCapacity = plot:GetAttribute(S(22)),
    }
end
return getPlotParts
end
modules[S(347)] = function()
return function(import)
    local Players = game:GetService(S(75))
    local createWindow = import(S(39))(import)
    local i18n = import(S(346))
    local antiAfk = import(S(293))(import)
    local interactionLogger = import(S(88))(import)
    local scheduler = import(S(396))(import)
    local log = import(S(165))
    local window = createWindow({
        Subtitle = S(207),
    })
    local getCurrentPlot = import(S(19))
    local getPlotParts = import(S(279))
    local dumpInstance = import(S(111))
    local dumpRemotes = import(S(276))(import)
    local dumpSellCards = import(S(400))(import)
    local dumpGui = import(S(84))(import)
    local autoMine = import(S(406))(import)
    local autoPlace = import(S(306))(import)
    local autoChest = import(S(362))(import)
    local autoSell = import(S(98))(import)
    local autoUpgrade = import(S(272))(import)
    scheduler.Register(S(72), autoMine.Step, autoMine.IsEnabled)
    scheduler.Register(S(112), autoPlace.Step, autoPlace.IsEnabled)
    scheduler.Register(S(422), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy)
    scheduler.Register(S(171), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Register(S(146), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.Start()
    local farmTab = window:AddTab(S(31), S(425))
    farmTab:AddCard(
        S(321),
        S(94)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(356),
        S(390)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(123),
        S(246)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(267),
        S(175)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(200),
        S(129)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(41), S(101))
    local checkBtn
    checkBtn = debugCard:AddButton(S(43), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(334)
            log(S(27))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(50),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(330),
        S(158)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(17), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(269)
            return
        end
        local ok, err = log(S(281))
        if not ok then
            dumpBtn.Text = S(183)
            dumpCard:SetSubtitle(S(92) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(389) .. firstNode.Name .. S(416))
            dumpInstance(firstNode, log)
        else
            log(S(366))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(320) .. firstHolder.Name .. S(54))
            dumpInstance(firstHolder, log)
        else
            log(S(289))
        end
        dumpBtn.Text = S(358)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(237),
        S(418)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(262), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(77))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(324))
        if not tool then
            toolDumpBtn.Text = S(110)
            return
        end
        log(S(47) .. tool.Name .. S(56))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(372) .. attrName .. S(368) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(55)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(283),
        S(62)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(280), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(93)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(377),
        S(203)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(359), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(259)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(242),
        S(430)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(236), function()
        dumpGui()
        guiDumpBtn.Text = S(278)
    end)
    local settingsTab = window:AddTab(S(172), S(291))
    settingsTab:AddCard(S(70), S(328)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(181),
        S(401)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(410), S(150), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(225), S(411), S(194) }, 1, function(index, value)
        local code = ({ S(333), S(71), S(34) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(381)] = function()
return {
    [79389059854988] = S(133),
}
end
import(S(61))(import)
