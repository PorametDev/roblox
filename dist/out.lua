-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"  ", "[AutoPlace] placed ", "Workspace", "Check", "[AutoPlace] enabled", "core/i18n", "[Interaction] ClickDetector clicked: ", "ValueLabel", "games/my_anime_mine/debug_dump_remotes", " (ActionText=", "ProximityPrompt", "Debug: Dump All Remotes", "Done", "Badge", "Content", "ClickDetector", " (MaxActivationDistance=%s)", "RemoteFunction", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "en", " ===", "games/my_anime_mine/auto_upgrade", "ScrollingFrame", "[Interaction] logger enabled - go click/interact with things now", "TextButton", "[Interaction] logger disabled", "Holders", " ", "Players", "Plots", "[AutoOpenChest] enabled", "Text", "games/my_anime_mine/get_plot_parts", "Frame", "Tool", "TextButton", "ProximityPrompt", "Debug: Dump Node/Holder", "core/get_place_id", "Error: ", "TextLabel", "Players", "Tool", "ReplicatedStorage", "games/my_anime_mine/get_plot_parts", "Search", "[Remotes] === total: ", "RunService", "TextLabel", "ไทย", "Frame", "Debug: Current Plot", "Auto Place Miner", "GuiButton", "Search functions...", "Done", "Players", "Backpack", "[AutoSell] SellItems:FireServer() error: ", "TextButton", "TextButton", "Frame", "[MyAnimeMine] no nodes present right now", "open_menu_hint", "en", "games/my_anime_mine/get_plot_parts", "UIListLayout", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "[AutoMine] enabled", "search_placeholder", "games/my_anime_mine/get_current_plot", "Remotes", "UserInputService", " '", "Main", "games/registry", "Auto Mine", "games/my_anime_mine/auto_take_chest", "", "TextButton", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/logger", "Header", "Crew", "ค้นหาฟังก์ชัน...", "games/my_anime_mine/debug_dump", "TextLabel", "no isfile/readfile available to verify the write", "th", "[AutoPlace] disabled", "ClickDetector", "'", "[AutoTakeChest] disabled", "Dump", "Backpack", "PlayerGui", "", "UserInputService", "%", "auto", "RunService", "", "games/my_anime_mine/auto_sell", "[AutoTakeChest] enabled (ClaimOre=%s, CollectAll=%s)", "Frame", "TextButton", "?", "ProximityPrompt", "TextLabel", "ReplicatedStorage", "ปุ่มเปิดเมนู", "core/get_me", "core/anti_afk", "Players", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "", "TextLabel", "Open menu", "เดินไปกด SellStand แล้วกด SelectAllButton อัตโนมัติ (ยังไม่ยืนยันว่าขายจริงหรือแค่เลือก)", "TextLabel", "core/get_place_id", "games/my_anime_mine/main", "core/logger", "core/dispatch", " = ", "[AutoOpenChest] disabled", "[AntiAFK] disabled", "ScreenGui", "Anti-AFK", "", "games/my_anime_mine/get_current_plot", "Players", "", "UpgradePart", "core/interaction_logger", "Frame", "ตามเครื่อง", "core/ui", "MekPro.log", "games/my_anime_mine/auto_take_chest", "games/my_anime_mine/auto_mine", "ClaimOre", "TextBox", "games/my_anime_mine/main", "games/my_anime_mine/auto_place", "th", "TextButton", "games/my_anime_mine/debug_dump_remotes", "UIScale", "ValueLabel", "ProximityPrompt", "core/i18n", "CollectAll", "X", "TabBar", "Auto Open Chest", "core/logger", "TextLabel", "search_placeholder", "กด ChestPrompt ให้อัตโนมัติ เพื่อให้หน้า Take All โผล่ขึ้นมา", "core/ui", "Players", "Frame", ": ", "[Interaction] ProximityPrompt triggered: ", "core/anti_afk", "RunService", "games/my_anime_mine/get_current_plot", "OwnerUserId", " (Text=", "write reported success but ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", " ===", "[MyAnimeMine] === Node: ", "Workspace", "Humanoid", "Remotes", "games/my_anime_mine/auto_mine", "leaderstats", "Workspace", "  [attr] ", "", "games/my_anime_mine/auto_upgrade", "core/logger", " = ", "RunService", "[AntiAFK] enabled - watching Player.Idled", "ChestCapacity", "core/logger", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", " ===", "[MyAnimeMine] no holders found", "core/logger", "games/my_anime_mine/get_plot_parts", "core/dispatch", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Auto Upgrade", "%", "CoreUI", "-", "games/my_anime_mine/auto_sell", "TextButton", "", "[Remotes] ", "Frame", "write reported success but the line isn't in ", "Badge", "Empty miner slot", "games/my_anime_mine/get_current_plot", "core/i18n", "Pad", " doesn't exist", "[AutoUpgrade] disabled", "TextLabel", "Backpack", "ClickDetector", "Loot", "My Anime Mine · Mining Hub", "🏠", ")", "อังกฤษ", ")", "no plot", "[MyAnimeMine] === Dump start ===", "games/my_anime_mine/get_plot_parts", "\n", "games/my_anime_mine/get_plot_parts", " ===", "Debug: Dump Backpack Tool", "ClickDetector", " remotes", "  [attr] ", "core/logger", "CoreGui", "", "games/registry", ": ", "Dump", "Dump", "TextLabel", "MekPro", ")", "Chest", "TextLabel", "auto", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "⚙", "ขนาดเมนู", "[MyAnimeMine] === Tool: ", "VirtualUser", "[Interaction] Key pressed: %s (gameProcessed=%s)", "MineWorld", "[AutoUpgrade] enabled", "games/my_anime_mine/auto_open_chest", "ฟาร์ม", "en-us", "[MyAnimeMine] === Holder: ", "Frame", "RunService", "[Interaction] Button activated: ", "", "RemoteEvent", "core/logger", "SellItems", "UIListLayout", "UICorner", "games/my_anime_mine/get_current_plot", "RunService", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "\n", "ClickDetector", "SellStand", "core/logger", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/debug_dump", "[Logger] failed to write ", "Nodes", "ตั้งค่า", "", "th", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "Text", "TextLabel", "RemoteEvent", "Frame", "GuiButton", "ย่อขยายเมนูทั้งชุด", "UICorner", "Interaction Logger (debug)", "ProximityPrompt", "Frame", "RemoteFunction", "neither appendfile nor writefile is available", "[AutoMine] disabled", " <-- remote, likely fired by the real interaction", "Zones", "core/interaction_logger", "[%X] ", "not found", "UIStroke", "log failed", "games/my_anime_mine/auto_place", "Players", "[AutoSell] enabled (SellItems=", "Frame", "core/get_leaderstat", "TextLabel", " on ", "UIPadding", "games/my_anime_mine/auto_open_chest", "[MyAnimeMine] not standing in any plot", "ReplicatedStorage", "Auto Sell", "[AntiAFK] idle detected, simulating input", "[AutoSell] disabled", "core/logger", "Humanoid", "ProximityPrompt", "no tool", "open_menu_hint", "core/logger", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "This is not a supported place.", "UIListLayout", "PlotIndex"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(166)] = function()
return function(import)
local log = import(S(233))
local Players = game:GetService(S(57))
local VirtualUser = game:GetService(S(250))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(187))
        connection = player.Idled:Connect(function()
            log(S(314))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(127))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(124)] = function()
return function(import)
    local getPlaceId = import(S(121))
    local registry = import(S(236))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(323))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(306)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(179))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(112)] = function()
local Players = game:GetService(S(303))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(317))
    local backpack = player:WaitForChild(S(95))
    return player, humanoid, backpack
end
return getMe
end
modules[S(39)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(6)] = function()
local Players = game:GetService(S(29))
local STRINGS = {
    en = {
        search_placeholder = S(55),
        open_menu_hint = S(118),
    },
    th = {
        search_placeholder = S(85),
        open_menu_hint = S(111),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(256)
    if locale:sub(1, 2):lower() == S(280) then
        return S(146)
    end
    return S(65)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(100) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(135)] = function()
return function(import)
local Players = game:GetService(S(42))
local Workspace = game:GetService(S(175))
local UserInputService = game:GetService(S(73))
local log = import(S(316))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(165) .. describe(prompt)
            .. S(10) .. prompt.ActionText .. S(242))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(7) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(260) .. describe(button)
            .. S(170) .. tostring(button.Text) .. S(220))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(37)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(91)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(286)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(26))
        return
    end
    connections = {}
    log(S(24))
    local playerGui = Players.LocalPlayer:WaitForChild(S(96))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(151)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(216)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(54)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(251),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(184)] = function()
local LOG_FILE = S(139)
local function log(message)
    local line = os.date(S(298)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(133))
            end
            appendfile(LOG_FILE, line .. S(270))
        elseif writefile then
            local existing = S(130)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(226))
        else
            error(S(293))
        end
        if not (isfile and readfile) then
            error(S(88))
        end
        if not isfile(LOG_FILE) then
            error(S(171) .. LOG_FILE .. S(212))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(206) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(276) .. LOG_FILE .. S(237) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(138)] = function()
return function(import)
local i18n = import(S(152))
local UserInputService = game:GetService(S(98))
local THEME = {
    BrandName = S(241),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(288))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(266))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(309))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(60))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(261)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(300))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(244))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(107)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(109))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(102)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(23))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(324))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(117))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(305))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(283))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(49))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(87))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(150))
            if not valueLbl then
                valueLbl = Instance.new(S(240))
                valueLbl.Name = S(8)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(14))
            if not badge then
                badge = Instance.new(S(120))
                badge.Name = S(207)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(25))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(34))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(285))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(36))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(182)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(198))
        local track = Instance.new(S(62))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(163))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(80))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(79)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(99)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(258))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(67))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(202))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(282)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(128))
    screenGui.Name = S(199)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(234))
    local main = Instance.new(S(291))
    main.Name = S(75)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(149))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(105))
    tabBar.Name = S(155)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(265))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(51))
    header.Name = S(83)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(106))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(41))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(158))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(116)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(147))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(154)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(61))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(200)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(143))
    searchBox.Name = S(46)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(159))
    searchBox.Text = S(203)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(205))
    content.Name = S(15)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(136))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(214))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(64))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(307))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(32)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(235) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(28) .. (item.Subtitle or S(279))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(70))
        footerLabel.Text = i18n.T(S(320))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(141)] = function()
return function(import)
local RunService = game:GetService(S(167))
local getCurrentPlot = import(S(209))
local getPlotParts = import(S(33))
local log = import(S(263))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(16), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(294))
        return
    end
    log(S(69))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(254)] = function()
return function(import)
local RunService = game:GetService(S(268))
local getCurrentPlot = import(S(274))
local getPlotParts = import(S(66))
local log = import(S(273))
local FIRE_INTERVAL = 1
local autoOpenChest = {}
local connection = nil
function autoOpenChest.SetEnabled(enabled)
    if enabled == autoOpenChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(126))
        return
    end
    log(S(31))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Chest then
            return
        end
        local prompt = parts.Chest:FindFirstChildWhichIsA(S(318), true)
        if prompt then
            fireproximityprompt(prompt)
        end
    end)
end
function autoOpenChest.IsEnabled()
    return connection ~= nil
end
return autoOpenChest
end
end
modules[S(145)] = function()
return function(import)
local Players = game:GetService(S(162))
local RunService = game:GetService(S(186))
local getCurrentPlot = import(S(168))
local getPlotParts = import(S(225))
local log = import(S(321))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(211))
    return pad and pad:FindFirstChildWhichIsA(S(108), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(208)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(90))
        return
    end
    log(S(5))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(215))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(176))
        local tool = backpack:FindFirstChildWhichIsA(S(35))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(2) .. tool.Name .. S(308) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(201)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(44))
local Workspace = game:GetService(S(3))
local RunService = game:GetService(S(259))
local log = import(S(123))
local FIRE_INTERVAL = 2
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(315))
        return
    end
    local remotes = ReplicatedStorage:FindFirstChild(S(72))
    local sellItems = remotes and remotes:FindFirstChild(S(264))
    log(S(304) .. tostring(sellItems ~= nil) .. S(222))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local sellStand = Workspace:FindFirstChild(S(272))
        local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(290), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        if sellItems then
            local ok, err = pcall(function()
                sellItems:FireServer()
            end)
            if not ok then
                log(S(59) .. tostring(err))
            end
        end
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(78)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(312))
local RunService = game:GetService(S(48))
local log = import(S(189))
local FIRE_INTERVAL = 0.5
local function getRemote(name)
    local remotes = ReplicatedStorage:FindFirstChild(S(177))
    return remotes and remotes:FindFirstChild(name)
end
local autoTakeChest = {}
local connection = nil
function autoTakeChest.SetEnabled(enabled)
    if enabled == autoTakeChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(93))
        return
    end
    local claimOre = getRemote(S(142))
    local collectAll = getRemote(S(153))
    log(string.format(
        S(104),
        tostring(claimOre ~= nil), tostring(collectAll ~= nil)
    ))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        if claimOre then
            pcall(function()
                claimOre:FireServer()
            end)
        end
        if collectAll then
            pcall(function()
                collectAll:FireServer()
            end)
        end
    end)
end
function autoTakeChest.IsEnabled()
    return connection ~= nil
end
return autoTakeChest
end
end
modules[S(22)] = function()
return function(import)
local RunService = game:GetService(S(101))
local getCurrentPlot = import(S(71))
local getPlotParts = import(S(194))
local log = import(S(82))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(134))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(230))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(213))
        return
    end
    log(S(253))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(275)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(97)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(74) .. child.Name .. S(92)
        if child:IsA(S(11)) then
            line = line .. string.format(
                S(322),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(271)) then
            line = line .. string.format(S(17), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(262)) or child:IsA(S(292)) then
            line = line .. S(295)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(232) .. attrName .. S(185) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(1))
    end
end
return dumpInstance
end
modules[S(148)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(110))
local log = import(S(157))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(284)) or descendant:IsA(S(18)) then
            count = count + 1
            log(S(204) .. descendant.ClassName .. S(164) .. descendant:GetFullName())
        end
    end
    log(S(47) .. count .. S(191))
    return count
end
return dumpRemotes
end
end
modules[S(267)] = function()
local Players = game:GetService(S(132))
local Workspace = game:GetService(S(180))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(252))
    local plots = mineWorld and mineWorld:FindFirstChild(S(30))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(169)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(227)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(27)),
        Nodes = plot:FindFirstChild(S(277)),
        Chest = plot:FindFirstChild(S(243)),
        Zones = plot:FindFirstChild(S(296)),
        Crew = plot:FindFirstChild(S(84)),
        Loot = plot:FindFirstChild(S(217)),
        PlotIndex = plot:GetAttribute(S(325)),
        ChestCapacity = plot:GetAttribute(S(188)),
    }
end
return getPlotParts
end
modules[S(122)] = function()
return function(import)
    local Players = game:GetService(S(114))
    local createWindow = import(S(161))(import)
    local i18n = import(S(210))
    local antiAfk = import(S(113))(import)
    local interactionLogger = import(S(297))(import)
    local log = import(S(193))
    local window = createWindow({
        Subtitle = S(218),
    })
    local getCurrentPlot = import(S(131))
    local getPlotParts = import(S(45))
    local dumpInstance = import(S(86))
    local dumpRemotes = import(S(9))(import)
    local autoMine = import(S(178))(import)
    local autoPlace = import(S(302))(import)
    local autoTakeChest = import(S(140))(import)
    local autoOpenChest = import(S(310))(import)
    local autoSell = import(S(103))(import)
    local autoUpgrade = import(S(183))(import)
    local farmTab = window:AddTab(S(255), S(219))
    farmTab:AddCard(
        S(77),
        S(172)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(53),
        S(81)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(156),
        S(160)
    ):AddToggle(false, function(state)
        autoOpenChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(313),
        S(119)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(197),
        S(281)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(52), S(196))
    local checkBtn
    checkBtn = debugCard:AddButton(S(4), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(299)
            log(S(311))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(190),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(38),
        S(269)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(238), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(223)
            return
        end
        local ok, err = log(S(224))
        if not ok then
            dumpBtn.Text = S(301)
            dumpCard:SetSubtitle(S(40) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(174) .. firstNode.Name .. S(21))
            dumpInstance(firstNode, log)
        else
            log(S(63))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(257) .. firstHolder.Name .. S(228))
            dumpInstance(firstHolder, log)
        else
            log(S(192))
        end
        dumpBtn.Text = S(13)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(229),
        S(115)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(239), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(58))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(43))
        if not tool then
            toolDumpBtn.Text = S(319)
            return
        end
        log(S(249) .. tool.Name .. S(173))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(181) .. attrName .. S(125) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(56)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(12),
        S(246)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(94), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(231)
    end)
    local settingsTab = window:AddTab(S(278), S(247))
    settingsTab:AddCard(S(129), S(19)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(289),
        S(68)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(248), S(287), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(137), S(221), S(50) }, 1, function(index, value)
        local code = ({ S(245), S(20), S(89) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoTakeChest.SetEnabled(false)
        autoOpenChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(76)] = function()
return {
    [79389059854988] = S(144),
}
end
import(S(195))(import)
