-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/i18n", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "'", "(nothing to do)", "Anti-AFK", " (", "TextLabel", "", ": ", "core/logger", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "Locked miner slot", "not found", "write failed", "UIListLayout", "[AutoPlace] ", "/", "TextButton", "[MyAnimeMine] === Tool: ", "Workspace", "games/my_anime_mine/auto_research", " ===", "'", "[BestMiner] slot ", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", "neither appendfile nor writefile is available", "Modules", "", "disabled", "MineWorld", "[ChestDump] === ReplicatedStorage.", "core/config", "PlayerGui", "/s", " ===", " while selecting", ": ", "Pad", "ซื้อ research ทุก family ในโซนที่ปลดแล้วผ่าน remote ตรง (ไม่เปิด UI)", "StatsPanel", "เดินทะลุกำแพงและพื้น", "autoChest", ": ", "[AutoChest] collected ", "Scroll", ") from ", "MineSpeed", "", "Research BuyButton", "CharLevel", "[AutoPlace] fireproximityprompt error: ", "fill", "TextLabel", "unreadable", "core/teleport", "TextButton", "[Handlers]   ", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Players", ": ", "[Config] couldn't write ", "PlayerScripts", "table", "Players", "getconnections", "core/click", "s", "ProximityPrompt", " ", "^%$", "games/my_anime_mine/debug_inspect_handlers", "Workspace", "Debug: Dump All Remotes", "core/interaction_logger", "TextLabel", "autoSellInterval", "RemoteFunction", "มุดแมพ (Noclip)", "Players", "=", "Holders", "^x(%d+)$", "Damage", "[SellCards] Card #", "core/dispatch", "[MekPro] settings file: ", "Mining", "TextLabel", "th", "TextLabel", "[Interaction] logger enabled - go click/interact with things now", "AutoMine", "Frame", "TextButton", "แรงกระโดด", "ReplicatedStorage", "", "Container", ",", "ClickDetector", "Auto Mine", "[AutoUpgrade] nothing upgraded (maxed out or not enough cash)", "  [const] ", "s", "float", "Done", "core/logger", "  ", "languageIndex", " -> ", "GuiButton", "lowest", "Damage", " from ", "s", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "Badge", "[AutoPlace] slot ", "[Config] couldn't read ", "TakeButton", ": not found", "getscripts", "[Probe] decompile unavailable - can't read the game's source this way", "[AutoChest] waiting: ", "Sell SelectAllButton", "')", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[AutoSell] disabled", "Speed", "Auto Research (Plot)", "log failed", "ลอย (Float)", "[AutoPlace] couldn't pick up ", "UIScale", "autoPlace", "SelectAllButton", "Frame", "Content", "อังกฤษ", "games/my_anime_mine/get_plot_parts", "CoreUI", "games/my_anime_mine/game_api", "Chest TakeButton", "MineEquipSlot", "UpgradeButton", "[Probe] PlayerScripts.UI.MineGame not found", "menuScale", "autoResearchInterval", "table", "  (debug.getupvalues unavailable)", "Dump", "Workspace", "languageIndex", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[Uu]nlock", "Loot", ":", "Chest", "UIPadding", " -> ", ") -> ", "[SellCards] === total cards: ", "SellFrame", ": ", "", "Card", "en", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Frame", "GuiObject", "SkillTreeFullFrame", "waiting", "core/logger", "Debug: Dump Visible GUI", "games/my_anime_mine/game_api", ")", "function", "MineChestClient", "Check", "games/my_anime_mine/game_api", "search_placeholder", ")", "core/i18n", "ValueLabel", "games/my_anime_mine/get_best_miner", "core/logger", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "auto", "GuiObject", " doesn't exist", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "games/my_anime_mine/debug_dump_gui", "  ", "NodeCount", "TextButton", "core/scheduler", "Content", "enabled", "Open menu", " (", "Done", "autoUpgrade", "walkSpeed", "debug.getupvalues", "[AutoChest] enabled (collects once >= ", "core/ui", "0 = เลือกอัตโนมัติ (ตัวที่ดีที่สุดที่ซื้อไหว)", "Auto Place: เปลี่ยนตัวอ่อนออก", "Folder", ")", "games/my_anime_mine/game_api", " ===", "write reported success but the line isn't in ", "Auto Sell", "[AutoPlace] nothing in the hotbar to place", "TextButton", " -> ", "auto", ")", "RunService", "button has no size on screen", "MineSpeed", " = ", "MineEquipPickaxe", "[AutoSell] enabled", "HttpService", "Content", "NodeHealth", "s", "Frame", "MineBuySkill", "Plots", "sold ", "Dump", "[Probe] ", "UI", "games/my_anime_mine/game_api", "getcallbackvalue", " (", "'", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Zones", "PlayerGui", "locked slot", "lowest", "MekPro.log", "RemoteEvent", "Frame", "games/my_anime_mine/debug_inspect_handlers", "core/parse_money", ")", "AutoChest", "[Interaction] ClickDetector clicked: ", "BodyPosition", "TextLabel", "table", "Done", "works from anywhere - dropping the teleport", "HotbarFrame", "SellStand", "BasePart", " '", "[ChestDump] === plot ", "core/logger", "games/registry", "", "Debug: Dump Node/Holder", "autoChestInterval", "games/my_anime_mine/debug_dump_gui", "Frame", "autoChestMinItems", "lowest", "games/my_anime_mine/auto_chest", "autoUpgradeMaxPerPass", "Auto Place Miner", "games/my_anime_mine/get_plot_parts", ") ===", "Mining", "ProximityPrompt", "PlayerGui", "AutoSell", ": ", "core/ui", " items (now ", "เปิดหน้า research ทุกกี่วินาที", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "table", "[AutoChest] disabled", "[GameAPI] SendEvent failed: ", "BodyVelocity", "[Probe] decompiled ", " ชิ้น", "", "RemoteFunction", "ตามเครื่อง", "  ", "Search", " error: ", "", " | ", "core/logger", "TextLabel", "  (debug.getconstants unavailable)", "[Probe] ", "games/my_anime_mine/debug_dump_sell_cards", "OwnerUserId", " ===", ")", "Crew", "Debug: Probe Executor + Scripts", ": ", " = ", "[AutoUpgrade] ", "Main", "games/my_anime_mine/debug_probe_env", "⚙", "no plot", "Text", "Sell SellButton", "disabled", "games/registry", "string", "games/my_anime_mine/auto_sell", "TextButton", "th", " chars) -> ", "[AutoPlace] picked up ", "search_placeholder", "ScrollingFrame", "VirtualInputManager", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "games/my_anime_mine/get_current_plot", "TextLabel", "Content", "MekPro", "games/my_anime_mine/debug_dump", "autoUpgradePriority", "[attr] ", "ProximityPrompt", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", " = ", " <-- remote, likely fired by the real interaction", "[StateDump] no mine data (is the game API loaded?)", "Backpack", "core/get_place_id", "MouseButton1Click", "MineSellClient", " = ", "", "waiting", " items)", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "no", ")", " x", "hookfunction", "Workspace", "AutoResearch", "sl", "Auto Mine: ความเร็ว", "[AntiAFK] disabled", "MineBuyPickaxe", "Done", "SlotFocusFrame", "Debug: Dump Game State", "Players", " (Text=", "Speed", "ต่ำสุดก่อน", "Scroll", "games/my_anime_mine/game_api", "core/logger", "games/my_anime_mine/get_plot_parts", " '", " = ", "[AutoPlace] disabled", "PlayerGui", "Humanoid", "Text", " ===", "  ", "core/logger", "Interaction Logger", "Debug: Dump Chest Data", "games/my_anime_mine/debug_dump_remotes", "-", "[GuiDump] === visible MineGui tree ===", "", " ", "core/teleport", "core/dispatch", "ฟาร์ม", "TextButton", "=locked(", "UserInputService", "[Interaction] Button activated: ", "[AutoUpgrade] gave ", "Humanoid", "UserInputService", "core/humanoid", "Damage", "Auto Chest", "  [attr] ", "RemoteEvent", "'", "[Scheduler] ", "Tool", " still empty after placing ", "GuiService", "Players", "🐛", "MineTakeChest", "games/my_anime_mine/main", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "", "TextLabel", " power ", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "Done", "autoMineClicks", "ปิดไว้เป็นค่าเริ่มต้น - ไม่รู้ว่าการสุ่มหนึ่งครั้งเสียอะไรบ้าง", "no tool", "write reported success but ", "ClickDetector", "'", "Players", "autoPlaceInterval", "ดีบัก", "Frame", "[ChestDump] === end ===", "ReplicatedStorage", "core/anti_afk", " items", "games/my_anime_mine/get_current_plot", "TextLabel", "VirtualUser", "'", "Activated", "MineSlotFocusClient", "autoUpgradePickaxeIndex", "TabBar", "Damage", "number", "en", "core/humanoid", "games/my_anime_mine/auto_sell", "[AutoResearch] ", "Search functions...", "Instance", "  ", "Damage", "ตั้งค่า", "games/my_anime_mine/debug_dump_state", "core/logger", "  [upvalue] ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "CharSpeedLevel", "table", "UIListLayout", "en-us", "Players", "core/scheduler", " = ", "Backpack", "Auto Mine: คลิกต่อรอบ", "sale didn't go through", "core/logger", "[Scheduler] turn: ", "[AutoResearch] nothing bought (maxed out or not enough cash)", "Speed", "core/get_leaderstat", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "ค้นหาฟังก์ชัน...", "All", "games/my_anime_mine/auto_research", "string", " (power ", "ClickDetector", " = ", ") on ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "ClickDetector", "games/my_anime_mine/auto_place", "[Handlers] ", "autoMineSpeed", " (Text=", "games/my_anime_mine/debug_dump_state", "Players", "", "games/my_anime_mine/auto_mine", ",", " '", "", "#", "Dump", "core/config", ".lua", "ValueBase", "Players", "[%X] ", "[^%d%.]", " connection(s)", "upgrade", "%", "[AutoSell] ", "TextButton", "games/my_anime_mine/game_api", " ", " to ", "[GuiDump] MineGui not found", "TextLabel", "HumanoidRootPart", "Auto Upgrade: รอบเช็ค", "Auto Upgrade: แจก Pickaxe ให้ miner", "core/logger", "[Logger] failed to write ", "%$%s*([%d,%.]+)%s*(%a*)", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "UIStroke", " '", "Content", " (prompt says '", "(", "table", "[MyAnimeMine] === Holder: ", "My Anime Mine · Mining Hub", "table", "ปุ่มเปิดเมนู", " ", "[AutoUpgrade] bought pickaxe #", "string", "อ่านป้ายบน chest ทุกกี่วินาที", ") didn't take, trying the next one", "Players", "TextLabel", "BasePart", "MineSellItems", "Header", "%$", "autoUpgradeRollMutation", "Unit Damage UpgradeButton", "debug.getproto", ": ", "Nodes", "games/my_anime_mine/get_plot_parts", "MekPro/decompiled_", " not found", ": ", "core/get_place_id", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "x", "defined at ", "", " (power ", "TextLabel", "AutoPlace", "RemoteFunction", "decompile", " (", "[MyAnimeMine] === Node: ", " ", "menuScale", "Debug: Inspect Button Handlers", "antiAfk", "WalkSpeed (ปกติ 16)", "", "[Bb]uy", "[Handlers]   #", "auto", "autoSell", "games/my_anime_mine/debug_dump_chest", "MineHUDClient", "games/my_anime_mine/get_current_plot", "MineRollMutation", "costs ", "X", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "ย่อขยายเมนูทั้งชุด", "TextLabel", "Done", "  ", "[GuiDump] === end ===", "\n", "boolean", "[AutoChest] MineTakeChest had no effect (", "noclip", " ", "[StateDump] === end ===", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", "[AutoChest] can't read the chest yet", "[MyAnimeMine] not standing in any plot", "MineGui", "[AutoPlace] upgrading ", "games/my_anime_mine/get_best_miner", "[Probe] couldn't decompile ", "/config.json", "prompt disabled", "GuiButton", "Players", "[Handlers] ", "PlotIndex", "fill", "บิน (Fly)", "", "core/logger", "[AutoMine] ", "getscriptbytecode", "[Interaction] Key pressed: %s (gameProcessed=%s)", " ===", "", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "core/logger", "ให้ miner ทุกตัวถือ pickaxe ที่ดีที่สุดที่เรามี", "UIListLayout", "Debug: Current Plot", "Inspect", "RemoteEvent", "MineGui", "Actions", "SellFrame", " attributes ===", "games/my_anime_mine/debug_dump", "core/logger", "UserInputService", "autoUpgradeInterval", "autoUpgradePickaxeIndex", "no isfile/readfile available to verify the write", "AutoUpgrade", "MineGui", "core/get_place_id", "autoPlaceUpgradeWeak", "ChestFrame", "core/logger", "core/get_me", "ความเร็ววิ่ง", "[Handlers] === end ===", "games/my_anime_mine/get_current_plot", "autoResearchMaxPerPass", "Debug: Dump Backpack Tool", "games/my_anime_mine/debug_probe_env", "Auto Upgrade: สุ่ม Mutation", " = ", "x", "ChestCapacity", "nothing to place or upgrade", "", "games/my_anime_mine/auto_upgrade", "[ChestDump] plot has no Chest", "[AutoResearch] ", "games/my_anime_mine/auto_mine", "Instance", "Auto Chest: รอบเช็ค", "core/logger", "[ChestDump] === Chest subtree ===", "MineUpgradeCharacter", "", "ValueLabel", "table(", "getnilinstances", "enabled", "getgc", " remotes", "[Interaction] logger disabled", "Players", "  [attr] ", "games/my_anime_mine/debug_dump_sell_cards", "getrenv", "Players", "Auto Research: ซื้อสูงสุดต่อรอบ", " cards", " (ActionText=", "MineSpeed", "th", "core/i18n", "ขนาดเมนู", "en", "Auto Sell: รอบขาย", "Probe", "Auto Chest: เกณฑ์เก็บ", "TextButton", "[Remotes] ", "  [attr] ", "Error: ", "พิมพ์โครงสร้าง GameState.GetData2().MineGame เพื่อหา key ที่ remote ต้องใช้", " stack(s)", "core/logger", "Dump", " parts", "[Handlers] === start (open the relevant menus first) ===", "?", "autoUpgradeEquipPickaxe", "=empty", "[AntiAFK] idle detected, simulating input", "[AutoUpgrade] ", "^([%d,]+)$", "[Remotes] === total: ", "open_menu_hint", "[Probe] === start ===", "ไทย", "TextButton", " ", "[Handlers] === ", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "[AutoPlace] ", "  ", "CharMineSpeedLevel", "MekPro", "[AutoPlace] drifted out of range of ", "games/my_anime_mine/auto_chest", "Content", "[MyAnimeMine] === Dump start ===", "Badge", "Auto Upgrade: Pickaxe ที่จะซื้อ", "th", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "games/my_anime_mine/get_plot_parts", "SellButton", "auto", "MineSkillTreeClient", "yes", "MineGui", "UICorner", "[ChestDump] not standing in any plot", "function", "core/logger", "HumanoidRootPart", "games/my_anime_mine/debug_dump_chest", "SellFrame", "games/my_anime_mine/auto_upgrade", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "", "This is not a supported place.", "[^%.]+", "Content", "getloadedmodules", "/", " ", "[Probe] === end ===", "  [attr] ", "Frame", " = ", "🏠", "[StateDump] === MineGame ===", "Dump", " ", "[ChestDump] === Player subtree ===", "MineHotbarClient", "RunService", "Luck", "autoUpgradePickaxe", "Empty miner slot", "Players", "[AutoSell] MineSellItems ", " '", ")", "disabled", "[AntiAFK] enabled - watching Player.Idled", "HumanoidRootPart", "  ", "[MyAnimeMine] no holders found", "leaderstats", "core/teleport", "TextLabel", "TextBox", "[AutoUpgrade] rolled a mutation on ", " (Text=", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "fly", "CoreGui", " (MaxActivationDistance=%s)", "ScreenGui", "[Handlers] getconnections isn't available in this executor", "", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "getsenv", "debug.getconstants", ")", "x", "ซื้อ pickaxe ตามที่เลือกด้านล่าง", "Detail", "autoMine", "jumpPower", "action '", "[GameAPI] couldn't load the game's modules: ", "unreadable", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "TextLabel", "", "", "TextLabel", "MineGame", "[Interaction] ProximityPrompt triggered: ", "\n", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Frame", "ItemName", "Frame", "BottomPanels", "Place", "[MyAnimeMine] no nodes present right now", "core/anti_afk", "[AutoPlace] holders: ", "JumpPower (ปกติ 50)", "table", "Auto Place: รอบเช็ค", "Auto Research: รอบเช็ค", "ReplicatedStorage", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/logger", "needs us at the stand - keeping the teleport", "enabled", "[AutoPlace] placed ", "UICorner", "/", "games/my_anime_mine/game_api", "open_menu_hint", "ไล่อัปเกรด miner ทุกกี่วินาที", "table", "games/my_anime_mine/main", " (", "Dump", "games/my_anime_mine/debug_dump_remotes", "ProximityPrompt", " ", "[Probe] === executor capabilities ===", "ValueBase", "core/logger", "Players", "BuyButton", "Dump", "s", "PlayerGui", "Debug: Dump Sell Cards", "core/interaction_logger", "games/my_anime_mine/get_current_plot", "autoResearch", "function", "games/my_anime_mine/auto_place", "Frame", ")", "ProximityPrompt", "ItemFrame", "table", "core/logger", "Auto Upgrade Unit", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "interactionLogger", ""}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(432)] = function()
return function(import)
local log = import(S(382))
local Players = game:GetService(S(64))
local VirtualUser = game:GetService(S(436))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(757))
        connection = player.Idled:Connect(function()
            log(S(693))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(361))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(66)] = function()
local VirtualInputManager = game:GetService(S(330))
local GuiService = game:GetService(S(409))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(221)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(496)] = function()
return function(import)
local HttpService = game:GetService(S(226))
local getPlaceId = import(S(630))
local log = import(S(633))
local ROOT_FOLDER = S(335)
local placeFolder = ROOT_FOLDER .. S(17) .. tostring(getPlaceId())
local filePath = placeFolder .. S(596)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(527) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(119) .. filePath .. S(311) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(61) .. filePath .. S(9) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(391)] = function()
return function(import)
    local getPlaceId = import(S(345))
    local registry = import(S(321))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(732))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(471)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(761))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(634)] = function()
local Players = game:GetService(S(79))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(378))
    local backpack = player:WaitForChild(S(464))
    return player, humanoid, backpack
end
return getMe
end
modules[S(549)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(445)] = function()
local Players = game:GetService(S(461))
local RunService = game:GetService(S(220))
local UserInputService = game:GetService(S(395))
local Workspace = game:GetService(S(20))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(398))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(512))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(290))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(536)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(261)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(254))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(1)] = function()
local Players = game:GetService(S(668))
local STRINGS = {
    en = {
        search_placeholder = S(448),
        open_menu_hint = S(199),
    },
    th = {
        search_placeholder = S(473),
        open_menu_hint = S(528),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(460)
    if locale:sub(1, 2):lower() == S(673) then
        return S(714)
    end
    return S(444)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(218) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(834)] = function()
return function(import)
local Players = game:GetService(S(664))
local Workspace = game:GetService(S(72))
local UserInputService = game:GetService(S(399))
local log = import(S(372))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(792) .. describe(prompt)
            .. S(671) .. prompt.ActionText .. S(308))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(253) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(396) .. describe(button)
            .. S(486) .. tostring(button.Text) .. S(354))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(279)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(482)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(598)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(663))
        return
    end
    connections = {}
    log(S(91))
    local playerGui = Players.LocalPlayer:WaitForChild(S(832))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(841)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(100)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(111)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(608),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(301)] = function()
local LOG_FILE = S(246)
local function log(message)
    local line = os.date(S(500)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(165))
            end
            appendfile(LOG_FILE, line .. S(793))
        elseif writefile then
            local existing = S(789)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(583))
        else
            error(S(26))
        end
        if not (isfile and readfile) then
            error(S(627))
        end
        if not isfile(LOG_FILE) then
            error(S(423) .. LOG_FILE .. S(190))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(213) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(516) .. LOG_FILE .. S(60) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(250)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(476) then
        return nil
    end
    local number, suffix = text:match(S(517))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(99), S(293))))
    if not value then
        return nil
    end
    if suffix ~= S(646) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(196)] = function()
return function(import)
local RunService = game:GetService(S(748))
local log = import(S(827))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(406) .. task_.name .. S(298) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(468) .. (currentTask and currentTask.name or S(4)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(762)] = function()
local Players = game:GetService(S(752))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(726))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(206)] = function()
return function(import)
local i18n = import(S(183))
local UserInputService = game:GetService(S(624))
local THEME = {
    BrandName = S(707),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(722))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(813))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(159))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(216))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(415)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(519))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(7))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(690)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(302))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(848)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(329))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(459))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(416))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(270))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(535))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(511))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(53))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(184))
            if not valueLbl then
                valueLbl = Instance.new(S(88))
                valueLbl.Name = S(657)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(712))
            if not badge then
                badge = Instance.new(S(75))
                badge.Name = S(117)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(700))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(169))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(93))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(680))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(553)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(504)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(429))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(740))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(94))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(28)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(230))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(614))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(56))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(379)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(771))
    screenGui.Name = S(141)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(769))
    local main = Instance.new(S(137))
    main.Name = S(314)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(134))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(839))
    tabBar.Name = S(441)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(15))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(797))
    header.Name = S(538)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(393))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(763))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(787))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(731)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(324))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(576)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(195))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(386)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(764))
    searchBox.Name = S(297)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(181))
    searchBox.Text = S(773)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(248))
    content.Name = S(334)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(795))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(333))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(697))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(790))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(318)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(489) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(737) .. (item.Subtitle or S(349))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(328))
        footerLabel.Text = i18n.T(S(816))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(709)] = function()
return function(import)
local gameApi = import(S(175))(import)
local log = import(S(454))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(205) .. minItemsToCollect .. S(351))
    else
        log(S(288))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(785) then
            log(S(590))
            lastReported = S(54)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(172) then
            log(S(124) .. count .. S(736) .. minItemsToCollect .. S(433))
            lastReported = S(350)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(44) .. count .. S(284) .. after .. S(210))
        else
            log(S(585) .. count
                .. S(160) .. tostring(after) .. S(777))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(650)] = function()
return function(import)
local getCurrentPlot = import(S(332))
local getPlotParts = import(S(373))
local log = import(S(612))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(478), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(606) .. (enabled and S(198) or S(320)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(838)] = function()
return function(import)
local Players = game:GetService(S(599))
local getCurrentPlot = import(S(637))
local getPlotParts = import(S(140))
local getMinerSlots = import(S(185))(import)
local teleport = import(S(390))
local gameApi = import(S(237))(import)
local log = import(S(186))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(751)
local LOCKED_SLOT_TEXT = S(12)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(38))
    return pad and pad:FindFirstChildWhichIsA(S(68), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(579)) then
            local digits = descendant.Text:match(S(695))
            if digits then
                return tonumber((digits:gsub(S(491), S(48))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(255)) and descendant.Text:match(S(70)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(244)
    end
    if not prompt.Enabled then
        return true, S(597)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(575) .. price
    end
    local action = prompt.ActionText or S(299)
    if action:match(S(155)) or action:match(S(567)) or action:match(S(539)) then
        return true, S(783) .. action .. S(240)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(758))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(51) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(394) .. why .. S(251))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(692))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(80) .. prompt.ObjectText .. S(523) .. power .. S(840))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(300))
    if line ~= lastSurvey then
        log(S(802) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(602), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(503), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(704) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(376))
    else
        lastAttempt = 0
        log(S(456))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(472),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(799), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(215))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(708) .. holder.Name .. S(36))
            return false
        end
        log(S(118) .. candidate.slot .. S(559) .. candidate.name
            .. S(533))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(16) .. holder.Name .. S(408) .. miner.name)
        return false
    end
    log(S(812) .. miner.name .. S(554) .. miner.power
        .. S(480) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(133) .. entry.name .. S(114) .. entry.holder.Name
            .. S(522) .. tostring(entry.prompt.ActionText) .. S(126))
        return false
    end
    log(S(327) .. entry.name .. S(477) .. entry.power
        .. S(46) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(645))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(52) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(593) .. target.holder.Name .. S(164) .. target.name
        .. S(239) .. target.power .. S(161) .. best.name .. S(6) .. best.power .. S(755))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(475)] = function()
return function(import)
local gameApi = import(S(142))(import)
local log = import(S(10))
local KNOWN_FAMILIES = { S(194), S(749), S(228) }
local checkInterval = 30
local maxPerPass = 5
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(649) .. (enabled and S(811) or S(756)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
local function unlockedZones()
    local mine = gameApi.GetMineData()
    local zones = mine and mine.UnlockedZones or {}
    local list = {}
    for zoneKey, unlocked in pairs(zones) do
        if unlocked then
            table.insert(list, zoneKey)
        end
    end
    table.sort(list)
    return list
end
local function skillLevel(zoneKey, family)
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    return skills and skills[family] or 0
end
local function familiesFor(zoneKey)
    local seen = {}
    local list = {}
    for _, family in ipairs(KNOWN_FAMILIES) do
        seen[family] = true
        table.insert(list, family)
    end
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    if skills then
        for family in pairs(skills) do
            if not seen[family] then
                table.insert(list, family)
            end
        end
    end
    return list
end
local function runJob()
    busy = true
    local bought = 0
    for _, zoneKey in ipairs(unlockedZones()) do
        for _, family in ipairs(familiesFor(zoneKey)) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local before = skillLevel(zoneKey, family)
            gameApi.BuySkill(zoneKey, family)
            task.wait(0.4)
            local after = skillLevel(zoneKey, family)
            if after > before then
                log(S(447) .. zoneKey .. S(529) .. family .. S(508)
                    .. before .. S(217) .. after)
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(469))
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(323)] = function()
return function(import)
local Workspace = game:GetService(S(152))
local gameApi = import(S(371))(import)
local teleport = import(S(55))
local log = import(S(725))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(128))
    else
        lastAttempt = 0
        log(S(225))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(260))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(339), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(753)
                .. (sold and S(258)
                         or S(810)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(505) .. (sold and (S(233) .. #keys .. S(685)) or S(466)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(647)] = function()
return function(import)
local gameApi = import(S(211))(import)
local log = import(S(264))
local STATS = {
    { event = S(442), field = S(50) },
    { event = S(470), field = S(457) },
    { event = S(222), field = S(706) },
}
local checkInterval = 20
local maxPerPass = 5
local priority = S(272)
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local rollMutations = false
local equippedByHolder = {}
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(694) .. (enabled and S(660) or S(29)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
    if not state then
        table.clear(equippedByHolder)
    end
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function levelOf(holderKey, field)
    local entry = holders()[holderKey]
    return entry and entry[field] or nil
end
local function chooseStat(entry)
    if priority ~= S(112) then
        for _, stat in ipairs(STATS) do
            if stat.event == priority and entry[stat.field] then
                return { event = stat.event, field = stat.field, level = entry[stat.field] }
            end
        end
    end
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local level = entry[stat.field]
        if level and (not lowest or level < lowest.level) then
            lowest = { event = stat.event, field = stat.field, level = level }
        end
    end
    return lowest
end
local function findPickaxe()
    local pickaxes = gameApi.GetPickaxes()
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(pickaxes) do
            if pickaxe.index == pickaxeTarget and not pickaxe.owned then
                return pickaxe
            end
        end
        return nil
    end
    local money = gameApi.GetMoney()
    local best = nil
    for _, pickaxe in ipairs(pickaxes) do
        if not pickaxe.owned and pickaxe.cost <= money then
            if not best or pickaxe.cost > best.cost then
                best = pickaxe
            end
        end
    end
    return best
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    for _, current in ipairs(gameApi.GetPickaxes()) do
        if current.index == pickaxe.index and current.owned then
            log(S(530) .. pickaxe.index .. S(701) .. pickaxe.name)
            return true
        end
    end
    return false
end
local function equipBestPickaxe()
    local best = gameApi.BestOwnedPickaxe()
    if not best then
        return
    end
    for holderKey in pairs(holders()) do
        if equippedByHolder[holderKey] ~= best.name then
            gameApi.EquipPickaxe(holderKey, best.name)
            equippedByHolder[holderKey] = best.name
            log(S(397) .. best.name .. S(509) .. holderKey)
            task.wait(0.3)
        end
    end
end
local function runJob()
    busy = true
    local bought = 0
    for holderKey, entry in pairs(holders()) do
        if not enabled or bought >= maxPerPass then
            break
        end
        local stat = chooseStat(entry)
        if stat then
            gameApi.UpgradeCharacter(holderKey, stat.event)
            task.wait(0.4)
            local after = levelOf(holderKey, stat.field)
            if after and after > stat.level then
                log(S(313) .. holderKey .. S(824) .. stat.event .. S(561)
                    .. stat.level .. S(110) .. after)
                bought = bought + 1
            end
        end
    end
    if buyPickaxe and enabled and bought < maxPerPass and tryPickaxe() then
        bought = bought + 1
    end
    if equipPickaxes and enabled then
        equipBestPickaxe()
    end
    if rollMutations and enabled then
        for holderKey in pairs(holders()) do
            if bought >= maxPerPass then
                break
            end
            gameApi.RollMutation(holderKey)
            log(S(765) .. holderKey)
            bought = bought + 1
            task.wait(0.4)
        end
    end
    if bought == 0 then
        log(S(102))
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(336)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(266)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(262) .. child.Name .. S(3)
        if child:IsA(S(823)) then
            line = line .. string.format(
                S(794),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(424)) then
            line = line .. string.format(S(770), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(617)) or child:IsA(S(294)) then
            line = line .. S(342)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(403) .. attrName .. S(463) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(193))
    end
end
return dumpInstance
end
modules[S(727)] = function()
return function(import)
local Players = game:GetService(S(828))
local ReplicatedStorage = game:GetService(S(807))
local getCurrentPlot = import(S(573))
local getPlotParts = import(S(276))
local log = import(S(605))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(338) .. name .. S(312) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(705))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(754) .. child.Name .. S(23)
            if child:IsA(S(826)) then
                line = line .. S(341) .. tostring(child.Value)
            elseif child:IsA(S(435)) or child:IsA(S(506)) then
                line = line .. S(766) .. tostring(child.Text) .. S(219)
            end
            log(line)
            dumpSubtree(child, indent .. S(759))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(723))
        return 0
    end
    log(S(263) .. plot.Name .. S(621))
    dumpAttributes(plot, S(108))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(648))
        return 0
    end
    log(S(654))
    dumpSubtree(parts.Chest, S(566))
    local player = Players.LocalPlayer
    log(S(746))
    dumpSubtree(player, S(8))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(31) .. player.Name .. S(35))
        dumpSubtree(mine, S(788))
    end
    log(S(430))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(192)] = function()
return function(import)
local Players = game:GetService(S(426))
local log = import(S(107))
local function dumpVisible(root, indent)
    indent = indent or S(97)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(170)) and child.Visible then
            local line = indent .. child.ClassName .. S(374) .. child.Name .. S(405)
            if child:IsA(S(90)) or child:IsA(S(18)) then
                line = line .. S(367) .. tostring(child.Text) .. S(176)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(682) .. attrName .. S(741) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(296))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(377))
    local mineGui = playerGui and playerGui:FindFirstChild(S(592))
    if not mineGui then
        log(S(510))
        return
    end
    log(S(387))
    dumpVisible(mineGui)
    log(S(582))
end
return dumpMineGui
end
end
modules[S(822)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(96))
local log = import(S(467))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(247)) or descendant:IsA(S(557)) then
            count = count + 1
            log(S(681) .. descendant.ClassName .. S(43) .. descendant:GetFullName())
        end
    end
    log(S(696) .. count .. S(212))
    return count
end
return dumpRemotes
end
end
modules[S(305)] = function()
return function(import)
local Players = game:GetService(S(534))
local log = import(S(653))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(280))
    local mineGui = playerGui and playerGui:FindFirstChild(S(721))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(620))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(227)) and sellFrame.Content:FindFirstChild(S(45))
    if not scroll then
        log(S(611))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(166) or card:IsA(S(189)) then
            count = count + 1
            log(S(84) .. count .. S(548) .. card.ClassName .. S(520) .. card.Name .. S(425))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(665) .. attrName .. S(375) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(581) .. child.ClassName .. S(492) .. child.Name .. S(437)
                if child:IsA(S(498)) then
                    line = line .. S(479) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(162) .. count .. S(380))
    return count
end
return dumpSellCards
end
end
modules[S(453)] = function()
return function(import)
local gameApi = import(S(180))(import)
local log = import(S(686))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(287) then
        local count = 0
        for _ in pairs(value) do
            count = count + 1
        end
        return S(658) .. count .. S(182)
    end
    return valueType .. S(389) .. tostring(value)
end
local function dumpTable(value, indent, depth)
    for key, entry in pairs(value) do
        log(indent .. tostring(key) .. S(348) .. describe(entry))
        if type(entry) == S(63) and depth > 0 then
            dumpTable(entry, indent .. S(381), depth - 1)
        end
    end
end
local function dumpState()
    local mine = gameApi.GetMineData()
    if not mine then
        log(S(343))
        return 0
    end
    log(S(743))
    dumpTable(mine, S(450), 2)
    log(S(588))
    return 1
end
return dumpState
end
end
modules[S(71)] = function()
return function(import)
local Players = game:GetService(S(488))
local log = import(S(515))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(651) then
        return value.ClassName .. S(745) .. value:GetFullName()
    end
    if valueType == S(149) then
        return S(524)
    end
    if valueType == S(837) then
        return S(724)
    end
    return valueType .. S(69) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(449) then
        return value:IsA(S(404)) or value:IsA(S(77)) or value:IsA(S(209))
    end
    return valueType == S(322) or valueType == S(443) or valueType == S(584)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(359))
    end)
    if ok and source then
        log(indent .. S(552) .. tostring(source) .. S(157) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(818) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(103) .. describe(constant))
            end
        end
    else
        log(indent .. S(303))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(458) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(455) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(150))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(484) .. label .. S(121))
        return
    end
    log(S(702) .. label .. S(820) .. button:GetFullName() .. S(277))
    if not getconnections then
        log(S(772))
        return
    end
    for _, signalName in ipairs({ S(438), S(346) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(600) .. signalName .. S(282) .. #connections .. S(502))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(568) .. index)
                    inspectFunction(fn, S(57))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(243))
    local current = playerGui and playerGui:FindFirstChild(S(629))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(689))
    inspectButton(S(143), find({ S(632), S(138), S(619), S(120) }))
    inspectButton(S(125), find({ S(163), S(197), S(136) }))
    inspectButton(S(319), find({ S(728), S(521), S(717) }))
    inspectButton(S(49), find({ S(171), S(780), S(829) }))
    local statsScroll = find({ S(364), S(798), S(40), S(370) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(401))
    inspectButton(S(541), damageRow and damageRow:FindFirstChild(S(145)))
    log(S(636))
end
return inspectHandlers
end
end
modules[S(640)] = function()
return function(import)
local Players = game:GetService(S(410))
local log = import(S(844))
local CAPABILITIES = {
    S(558), S(607), S(122), S(661), S(735),
    S(65), S(238), S(775), S(667), S(776),
    S(204), S(542), S(356), S(659),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(733)) do
        if type(target) ~= S(804) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(825))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(235) .. name .. S(642) .. ((ok and type(value) == S(177)) and S(720) or S(353)))
    end
end
local WANTED_SCRIPTS = {
    S(178),
    S(347),
    S(719),
    S(439),
    S(747),
    S(572),
}
local function tryDecompile()
    if not decompile then
        log(S(123))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(62))
    local ui = playerScripts and playerScripts:FindFirstChild(S(236))
    local mineGame = ui and ui:FindFirstChild(S(791))
    if not mineGame then
        log(S(146))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(304) .. name .. S(547))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(531) and #source > 0 then
                local path = S(546) .. name .. S(497)
                local written = pcall(writefile, path, source)
                log(S(291) .. name .. S(200) .. #source .. S(326)
                    .. (written and path or S(14)))
            else
                log(S(595) .. name .. S(543) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(698))
    reportCapabilities()
    tryDecompile()
    log(S(738))
end
return probe
end
end
modules[S(507)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(431))
local log = import(S(173))
local api = {}
local gameState, mineInventory, mineConfig
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(27), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(784) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetMineData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return nil
    end
    return data.MineGame
end
function api.GetMoney()
    if not modules() then
        return 0
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return 0
    end
    return tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(843))
            if buyableWithCash then
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    owned = owned[def.Name] ~= nil,
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(289) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(412))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(474), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(256) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(537), keys)
end
function api.EquipSlot(key)
    return send(S(144), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(362), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(224), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(574), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(nodeId, family)
    return send(S(231), nodeId, family)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(655), characterKey, stat == S(278) and S(672) or stat)
end
return api
end
end
modules[S(594)] = function()
return function(import)
local Players = game:GetService(S(366))
local log = import(S(809))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(33))
    local mineGui = playerGui and playerGui:FindFirstChild(S(618))
    local hotbar = mineGui and mineGui:FindFirstChild(S(259))
    local content = hotbar and hotbar:FindFirstChild(S(710))
    return content and content:FindFirstChild(S(98))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(555)) then
            local count = descendant.Text:match(S(82))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(842))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(734))
    local nameLabel = content and content:FindFirstChild(S(796))
    local damageLabel = content and content:FindFirstChild(S(113))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(610) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(501), S(388)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(814) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(24) .. slot .. S(37) .. info.name
                        .. S(417) .. info.power .. S(355) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(835)] = function()
local Players = game:GetService(S(499))
local Workspace = game:GetService(S(357))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(30))
    local plots = mineWorld and mineWorld:FindFirstChild(S(232))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(306)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(716)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(81)),
        Nodes = plot:FindFirstChild(S(544)),
        Chest = plot:FindFirstChild(S(158)),
        Zones = plot:FindFirstChild(S(242)),
        Crew = plot:FindFirstChild(S(309)),
        Loot = plot:FindFirstChild(S(156)),
        PlotIndex = plot:GetAttribute(S(601)),
        ChestCapacity = plot:GetAttribute(S(644)),
    }
end
return getPlotParts
end
modules[S(819)] = function()
return function(import)
    local Players = game:GetService(S(59))
    local createWindow = import(S(283))(import)
    local i18n = import(S(674))
    local antiAfk = import(S(801))(import)
    local interactionLogger = import(S(74))(import)
    local scheduler = import(S(462))(import)
    local config = import(S(32))(import)
    local log = import(S(623))
    local window = createWindow({
        Subtitle = S(526),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local getCurrentPlot = import(S(434))
    local getPlotParts = import(S(545))
    local dumpInstance = import(S(622))
    local dumpRemotes = import(S(385))(import)
    local dumpSellCards = import(S(666))(import)
    local dumpGui = import(S(269))(import)
    local dumpChest = import(S(571))(import)
    local inspectHandlers = import(S(249))(import)
    local probeEnv = import(S(315))(import)
    local dumpState = import(S(487))(import)
    local humanoid = import(S(400))
    local autoMine = import(S(490))(import)
    local autoPlace = import(S(483))(import)
    local autoChest = import(S(273))(import)
    local autoSell = import(S(446))(import)
    local autoUpgrade = import(S(729))(import)
    local autoResearch = import(S(21))(import)
    local gameApi = import(S(815))(import)
    scheduler.RegisterStandalone(S(92), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(252), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(556), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(628), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(358), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(281), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(392), S(742))
    persistentToggle(farmTab:AddCard(
        S(101),
        S(481)
    ), S(781), autoMine.SetEnabled)
    persistentSlider(farmTab, S(360), S(550),
        1, 30, S(485), 5, autoMine.SetCyclesPerSecond, S(34))
    persistentSlider(farmTab, S(465), S(786),
        1, 10, S(420), 1, autoMine.SetClicksPerCycle, S(643))
    persistentToggle(farmTab:AddCard(
        S(275),
        S(808)
    ), S(135), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(805), S(340),
        1, 60, S(427), 3, autoPlace.SetCheckInterval, S(67))
    persistentToggle(
        farmTab:AddCard(S(208),
            S(331)),
        S(631), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(402),
        S(774)
    ), S(42), autoChest.SetEnabled)
    persistentSlider(farmTab, S(679), S(577),
        50, 3000, S(271), 100, autoChest.SetMinItems, S(292))
    persistentSlider(farmTab, S(652), S(532),
        1, 60, S(268), 3, autoChest.SetCheckInterval, S(229))
    persistentToggle(farmTab:AddCard(
        S(214),
        S(154)
    ), S(570), autoSell.SetEnabled)
    persistentSlider(farmTab, S(677), S(703),
        10, 300, S(76), 45, autoSell.SetCheckInterval, S(831))
    persistentToggle(farmTab:AddCard(
        S(845),
        S(286)
    ), S(202), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(513), S(817),
        5, 300, S(625), 20, autoUpgrade.SetInterval, S(115))
    persistentSlider(farmTab, S(352), S(127),
        1, 20, S(274), 5, autoUpgrade.SetMaxPerPass, S(778))
    persistentSegmented(
        farmTab,
        { S(369), S(83), S(368), S(87) },
        { S(245), S(451), S(129), S(47) },
        S(337), 1, autoUpgrade.SetPriority
    )
    persistentToggle(
        farmTab:AddCard(S(730),
            S(779)),
        S(750), autoUpgrade.SetBuyPickaxe
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeNames = {}
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeNames[pickaxe.index] = pickaxe.name
    end
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(207)
            end
            return S(494) .. index .. S(587) .. (pickaxeNames[index] or S(191))
        end
        local savedPick = config.Get(S(440), 0)
        pickaxeCard = farmTab:AddSlider(
            S(713),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(626), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(604)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(514),
            S(613)),
        S(691), autoUpgrade.SetEquipPickaxes
    )
    persistentToggle(
        farmTab:AddCard(S(641),
            S(421)),
        S(540), autoUpgrade.SetRollMutations
    )
    persistentToggle(farmTab:AddCard(
        S(130),
        S(39)
    ), S(836), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(806), S(285),
        10, 600, S(148), 30, autoResearch.SetCheckInterval, S(104))
    persistentSlider(farmTab, S(669), S(414),
        1, 20, S(638), 5, autoResearch.SetMaxPerPass, S(551))
    local debugTab = window:AddTab(S(428), S(411))
    persistentToggle(
        debugTab:AddCard(
            S(383),
            S(846)
        ),
        S(847),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(615), S(168))
    local checkBtn
    checkBtn = debugCard:AddButton(S(179), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(13)
            log(S(591))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(58),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(267),
        S(11)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(687), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(317)
            return
        end
        local ok, err = log(S(711))
        if not ok then
            dumpBtn.Text = S(131)
            dumpCard:SetSubtitle(S(683) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(560) .. firstNode.Name .. S(22))
            dumpInstance(firstNode, log)
        else
            log(S(800))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(525) .. firstHolder.Name .. S(609))
            dumpInstance(firstHolder, log)
        else
            log(S(760))
        end
        dumpBtn.Text = S(201)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(639),
        S(187)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(821), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(344))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(407))
        if not tool then
            toolDumpBtn.Text = S(422)
            return
        end
        log(S(19) .. tool.Name .. S(307))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(739) .. attrName .. S(223) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(363)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(73),
        S(2)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(744), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(662)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(833),
        S(767)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(495), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(670)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(174),
        S(241)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(234), function()
        dumpGui()
        guiDumpBtn.Text = S(257)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(384),
        S(116)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(151), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(688)
    end)
    local handlersCard = debugTab:AddCard(
        S(563),
        S(589)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(616), function()
        inspectHandlers()
        handlersBtn.Text = S(419)
    end)
    local probeCard = debugTab:AddCard(
        S(310),
        S(25)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(678), function()
        probeEnv()
        probeBtn.Text = S(580)
    end)
    local stateCard = debugTab:AddCard(
        S(365),
        S(684)
    )
    local stateBtn
    stateBtn = stateCard:AddButton(S(830), function()
        dumpState()
        stateBtn.Text = S(106)
    end)
    humanoid.Start()
    persistentSlider(debugTab, S(635), S(565),
        8, 200, S(203), 16, humanoid.SetWalkSpeed, S(656))
    persistentSlider(debugTab, S(95), S(803),
        30, 300, S(782), 50, humanoid.SetJumpPower, S(493))
    persistentToggle(
        debugTab:AddCard(S(603), S(518)),
        S(768), humanoid.SetFlying
    )
    persistentToggle(
        debugTab:AddCard(S(78), S(41)),
        S(586), humanoid.SetNoclip
    )
    persistentToggle(
        debugTab:AddCard(S(132), S(418)),
        S(105), humanoid.SetFloating
    )
    local settingsTab = window:AddTab(S(452), S(316))
    persistentToggle(
        settingsTab:AddCard(S(5), S(715)),
        S(564),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(147), 100)
    settingsTab:AddSlider(S(675), S(578), 50, 150, savedScale, function(value)
        config.Set(S(562), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(109), 1)
    settingsTab:AddSegmented({ S(295), S(139), S(699) }, savedLanguage, function(index, value)
        local code = ({ S(569), S(676), S(89) })[index]
        config.Set(S(153), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(718), S(167), S(325) })[savedLanguage] or S(188))
    log(S(86) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        humanoid.Stop()
    end)
end
end
modules[S(265)] = function()
return {
    [79389059854988] = S(413),
}
end
import(S(85))(import)
