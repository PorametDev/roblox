-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Auto Sell", "en", "disabled", "[AutoPlace] fireproximityprompt error: ", "core/logger", "core/click", "CoreUI", "core/logger", "TextButton", "search_placeholder", "UIListLayout", "Sell for", "log failed", "unreadable", "", "Close", "core/ui", "Open menu", "ตามเครื่อง", "GuiButton", "no tool", "table", "games/my_anime_mine/auto_sell", "Workspace", "Empty miner slot", "games/my_anime_mine/get_current_plot", "Done", "ProximityPrompt", "Debug: Current Plot", "", "core/anti_afk", "core/logger", "?", "core/teleport", "[Remotes] === total: ", "TextLabel", "core/teleport", "[MyAnimeMine] === Node: ", "", "Frame", "Frame", "Label", "core/config", "ScreenGui", "core/i18n", ": ", "core/i18n", " '", "[AutoChest] can't read the chest billboard, is the plot loaded?", "AutoUpgrade", "UserInputService", "Search", "[GuiDump] MineGui not found", " -> ", "", "ClickDetector", "Content", "MineGui", "PlayerGui", "games/my_anime_mine/auto_mine", " on ", "games/my_anime_mine/debug_dump_chest", "Search functions...", "Frame", "RunService", "ProximityPrompt", "Close", "SellFrame", "TextButton", "[Interaction] Button activated: ", "games/my_anime_mine/auto_place", "menuScale", "TextLabel", "Frame", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "Players", ": ", "ขนาดเมนู", "  [attr] ", "no humanoid", "games/my_anime_mine/auto_place", "Auto Mine", "disabled", "[AutoSell] nothing selectable (", "Frame", " (ActionText=", " = ", "Main", "core/teleport", "Pad", " items)", "Players", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "", "menuScale", "[Interaction] ProximityPrompt triggered: ", "Done", "enabled", "Actions", "[AntiAFK] idle detected, simulating input", "Content", "core/logger", "%", "  ", ": ", "core/logger", "\n", "PlayerGui", "", "TextLabel", "", "ย่อขยายเมนูทั้งชุด", "ValueBase", "[AutoChest] Take All worked: ", "[ChestDump] === Chest subtree ===", " '", "", "core/config", "Amount", "[Interaction] logger disabled", "TextLabel", "search_placeholder", ": ", "th", ": ", "core/get_place_id", "no plot", "RemoteFunction", "core/logger", "TextButton", "games/my_anime_mine/auto_mine", "Players", "enabled", "core/logger", "games/my_anime_mine/get_current_plot", "AutoPlace", "no character/backpack", "Plots", "  [attr] ", "TextButton", "open_menu_hint", "Players", "Loot", "TextButton", "My Anime Mine · Mining Hub", "MekPro", "  ", "Nodes", "Workspace", "SellButton", "[AutoSell] disabled", "core/ui", "games/my_anime_mine/debug_dump_sell_cards", "PlayerGui", "VirtualUser", "core/logger", "games/my_anime_mine/main", "Tool", "  ", "SelectAllButton", ")", "อังกฤษ", " remotes", "core/dispatch", "\n", "TextLabel", "games/my_anime_mine/get_plot_parts", "Frame", "Auto Chest", "leaderstats", "RemoteEvent", "ClickDetector", "Chest", "Card", "", "/", "HttpService", " = ", "en", "games/my_anime_mine/auto_upgrade", "/", "th", "[ChestDump] not standing in any plot", "UIListLayout", "ChestFrame", "games/my_anime_mine/get_plot_parts", "", "TextButton", "games/my_anime_mine/debug_dump", "autoUpgrade", "Done", "games/my_anime_mine/get_plot_parts", "(none enabled)", "Debug: Dump Node/Holder", "games/my_anime_mine/auto_chest", "games/my_anime_mine/get_plot_parts", "Content", "Debug: Dump All Remotes", "waiting", "TextBox", "[AutoSell] clicked SellButton", "MineGui", "open_menu_hint", "core/get_place_id", "Progress", "ProximityPrompt", " ===", "UIScale", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "'", "", "ClickDetector", " '", "TakeButton", "auto", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "'", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "[AutoPlace] ", "Backpack", "[Config] couldn't read ", ": ", "core/scheduler", "Debug: Dump Chest Data", "core/scheduler", "[AutoChest] disabled", "[ChestDump] === end ===", "languageIndex", "This is not a supported place.", "SellStand", " ===", "TextButton", "games/my_anime_mine/debug_dump_remotes", "MekPro.log", "[MyAnimeMine] no nodes present right now", "[AutoChest] Take All had NO effect (", "TextLabel", "interactionLogger", "[Config] couldn't write ", "Dump", "🏠", " ===", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", " attributes ===", "[AutoSell] fireproximityprompt error: ", "TextButton", " ===", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "games/my_anime_mine/get_plot_parts", "Auto Place Miner", "[ChestDump] plot has no Chest", "core/logger", "[Logger] failed to write ", "TextLabel", "ตั้งค่า", "Backpack", "Debug: Dump Visible GUI", "  ", "[AntiAFK] enabled - watching Player.Idled", ": ", "core/logger", "games/my_anime_mine/get_plot_parts", "ProximityPrompt", " '", "SellFrame", " items", "⚙", "ฟาร์ม", " items", "[MyAnimeMine] === Holder: ", "[AutoMine] ", "Badge", "GuiObject", "ValueBase", "Debug: Dump Sell Cards", "th", "TextLabel", "ChestCapacity", "no isfile/readfile available to verify the write", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[MyAnimeMine] no holders found", "auto", "Header", "TextLabel", "'), backing off ", "[AutoPlace] placed ", "games/my_anime_mine/get_current_plot", "[Interaction] logger enabled - go click/interact with things now", "games/my_anime_mine/get_current_plot", "ReplicatedStorage", "Sell for", "[AutoChest] ", "AutoSell", "TextLabel", "Players", " (Text=", "Scroll", "[MyAnimeMine] === Tool: ", " ===", "[AutoChest] fireproximityprompt error: ", " ", "Error: ", "autoSell", "AutoChest", "[AutoSell] click failed on ", "core/get_leaderstat", "write reported success but ", "write reported success but the line isn't in ", "no empty holder found (", "UserInputService", " doesn't exist", "'", "auto", "", "[GuiDump] === end ===", " holders checked)", "[SellCards] === total cards: ", "GuiService", "[MyAnimeMine] not standing in any plot", "Auto Upgrade", "RemoteFunction", "PlayerGui", "VirtualInputManager", "", "  [attr] ", "core/click", "  ", "^(%d+)", "[GuiDump] === visible MineGui tree ===", "en-us", "core/logger", "games/my_anime_mine/get_current_plot", "X", "[AutoChest] chest didn't open, aborting", "", "", " cards left, label = '", "core/anti_afk", "[Interaction] Key pressed: %s (gameProcessed=%s)", "th", "games/my_anime_mine/get_current_plot", "TextButton", "Tool", "Holders", "core/get_place_id", "UpgradePart", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "UIStroke", "[AutoChest] click failed on ", "Dump", "Players", "", "Text", "GuiButton", "autoPlace", "ค้นหาฟังก์ชัน...", ")", "games/my_anime_mine/debug_dump", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "core/logger", "Frame", "", "UIPadding", "[AutoPlace] ", ")", "Dump", "Zones", "s", "Text", "core/i18n", "games/my_anime_mine/debug_dump_remotes", " = ", "Content", "games/my_anime_mine/get_current_plot", "[AutoSell] clicked SelectAllButton (", "Scroll", "  ", "TextLabel", "  [attr] ", "core/logger", "games/my_anime_mine/get_plot_parts", "UICorner", "core/click", ")", "games/my_anime_mine/auto_sell", "TextButton", "AutoMine", "Debug: Dump Backpack Tool", "TabBar", "unreadable", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "MineWorld", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "RemoteEvent", "antiAfk", "Frame", "CoreGui", "TextLabel", "[MyAnimeMine] === Dump start ===", " <-- remote, likely fired by the real interaction", " (Text=", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", " cards)", "ProximityPrompt", "languageIndex", "PlayerGui", "plot has no Holders folder", "core/logger", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "enabled", "Workspace", "TextLabel", ",", " = ", "ProximityPrompt", "Backpack", "'", "", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[ChestDump] === Player subtree ===", " = ", "ClickDetector", "[SellCards] Card #", "waiting", "UICorner", "[AutoSell] nothing to sell, closing", "[AutoUpgrade] ", "[Interaction] ClickDetector clicked: ", "Humanoid", "ไทย", "PlotIndex", " = ", "/config.json", " '", "no tool in Backpack (all miners already placed?)", "ValueLabel", "TextLabel", "games/registry", "Badge", "autoChest", "Anti-AFK", "games/registry", " error: ", "-", "autoMine", "Dump", "[ChestDump] === plot ", "[%X] ", "UIListLayout", "HumanoidRootPart", "Players", "[attr] ", "button has no size on screen", "[AutoChest] waiting: ", "core/logger", "Card", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", " items, going to collect", "ValueLabel", "Players", "not found", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "ปุ่มเปิดเมนู", "ScrollingFrame", "not standing in any plot", "Players", "MekPro", " (MaxActivationDistance=%s)", " cards", "core/get_me", "[Remotes] ", "Crew", " (Text=", "core/interaction_logger", "Check", "Players", "Select All", " = ", "Interaction Logger (debug)", "OwnerUserId", "games/my_anime_mine/debug_dump_chest", "Players", "Dump", "Frame", "games/my_anime_mine/debug_dump_gui", " parts", "disabled", "Players", "games/my_anime_mine/auto_chest", "Frame", "games/my_anime_mine/main", "Frame", "games/my_anime_mine/debug_dump_sell_cards", "%", "neither appendfile nor writefile is available", "[Scheduler] turn: ", "[AutoSell] enabled (only sells when there's something in the scroll)", "[MekPro] settings file: ", "Content", "[AutoChest] enabled (collects once >= ", " -> ", "core/dispatch", "Players", "Humanoid", " items)", "Label", "[AutoSell] sell menu didn't open, aborting", "auto", "ClickDetector", "Content", "[AntiAFK] disabled", " ===", "games/my_anime_mine/auto_upgrade", "core/interaction_logger", "Content", "[ChestDump] === ReplicatedStorage.", "'", "MineGui", "ReplicatedStorage", "games/my_anime_mine/debug_dump_gui", "[Scheduler] ", "GuiObject", "en", "Dump", "MineGui"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(338)] = function()
return function(import)
local log = import(S(102))
local Players = game:GetService(S(142))
local VirtualUser = game:GetService(S(155))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(259))
        connection = player.Idled:Connect(function()
            log(S(100))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(511))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(326)] = function()
local VirtualInputManager = game:GetService(S(323))
local GuiService = game:GetService(S(318))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(453)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(118)] = function()
return function(import)
local HttpService = game:GetService(S(177))
local getPlaceId = import(S(345))
local log = import(S(252))
local ROOT_FOLDER = S(467)
local placeFolder = ROOT_FOLDER .. S(181) .. tostring(getPlaceId())
local filePath = placeFolder .. S(433)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(22) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(221) .. filePath .. S(123) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(239) .. filePath .. S(222) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(502)] = function()
return function(import)
    local getPlaceId = import(S(126))
    local registry = import(S(442))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(229))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(306)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(170))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(470)] = function()
local Players = game:GetService(S(92))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(429))
    local backpack = player:WaitForChild(S(220))
    return player, humanoid, backpack
end
return getMe
end
modules[S(204)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(47)] = function()
local Players = game:GetService(S(503))
local STRINGS = {
    en = {
        search_placeholder = S(63),
        open_menu_hint = S(18),
    },
    th = {
        search_placeholder = S(356),
        open_menu_hint = S(463),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(330)
    if locale:sub(1, 2):lower() == S(340) then
        return S(182)
    end
    return S(179)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(215) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(514)] = function()
return function(import)
local Players = game:GetService(S(295))
local Workspace = game:GetService(S(411))
local UserInputService = game:GetService(S(310))
local log = import(S(8))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(96) .. describe(prompt)
            .. S(86) .. prompt.ActionText .. S(161))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(428) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(70) .. describe(button)
            .. S(473) .. tostring(button.Text) .. S(384))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(404)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(212)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(20)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(120))
        return
    end
    connections = {}
    log(S(288))
    local playerGui = Players.LocalPlayer:WaitForChild(S(108))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(66)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(422)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(354)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(339),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(106)] = function()
local LOG_FILE = S(234)
local function log(message)
    local line = os.date(S(448)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(336))
            end
            appendfile(LOG_FILE, line .. S(165))
        elseif writefile then
            local existing = S(94)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(107))
        else
            error(S(495))
        end
        if not (isfile and readfile) then
            error(S(279))
        end
        if not isfile(LOG_FILE) then
            error(S(307) .. LOG_FILE .. S(311))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(308) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(253) .. LOG_FILE .. S(77) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(223)] = function()
return function(import)
local RunService = game:GetService(S(65))
local log = import(S(261))
local SLICE_SECONDS = 3
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy)
    table.insert(exclusiveTasks, { name = name, step = step, isEnabled = isEnabled, isBusy = isBusy })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(521) .. task_.name .. S(443) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickNextEnabled()
    if #exclusiveTasks == 0 then
        return nil
    end
    for _ = 1, #exclusiveTasks do
        rrIndex = (rrIndex % #exclusiveTasks) + 1
        local candidate = exclusiveTasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(496) .. (currentTask and currentTask.name or S(193)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(34)] = function()
local Players = game:GetService(S(460))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(450))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(17)] = function()
return function(import)
local i18n = import(S(45))
local UserInputService = game:GetService(S(51))
local THEME = {
    BrandName = S(146),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(382))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(425))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(363))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(144))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(30)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(348))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(284))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(33)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(36))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(211)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(464))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(449))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(378))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(484))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(437))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(110))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(237))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(436))
            if not valueLbl then
                valueLbl = Instance.new(S(277))
                valueLbl.Name = S(459)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(439))
            if not badge then
                badge = Instance.new(S(412))
                badge.Name = S(272)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(386))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(74))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(361))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(130))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(117)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(103))
        local track = Instance.new(S(64))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(41))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(342))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(314)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(494)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(85))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(184))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(246))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(353)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(44))
    screenGui.Name = S(7)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(397))
    local main = Instance.new(S(492))
    main.Name = S(88)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(208))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(396))
    tabBar.Name = S(389)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(11))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(40))
    header.Name = S(283)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(69))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(121))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(254))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(362)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(188))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(333)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(9))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(444)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(200))
    searchBox.Name = S(52)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(10))
    searchBox.Text = S(55)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(490))
    content.Name = S(373)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(168))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(73))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(141))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(294))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(369)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(39) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(301) .. (item.Subtitle or S(15))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(122))
        footerLabel.Text = i18n.T(S(203))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(489)] = function()
return function(import)
local Players = game:GetService(S(476))
local getCurrentPlot = import(S(332))
local getPlotParts = import(S(196))
local teleport = import(S(37))
local click = import(S(6))
local log = import(S(156))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(322))
    local mineGui = playerGui and playerGui:FindFirstChild(S(518))
    return mineGui and mineGui:FindFirstChild(S(185))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(57))
    local actions = content and content:FindFirstChild(S(99))
    return actions and actions:FindFirstChild(S(214))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(413), S(352)):match(S(328))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(119), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(101))
    local progress = content and content:FindFirstChild(S(205))
    local label = progress and progress:FindFirstChild(S(42))
    return parseCount(label and label.Text or S(335))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(349) .. button.Name .. S(260) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(206), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(226))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(500) .. MIN_ITEMS_TO_COLLECT .. S(505))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(300) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(334))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(114) .. count .. S(501) .. after .. S(266))
        else
            log(S(236) .. tostring(count)
                .. S(54) .. tostring(after) .. S(91))
        end
    end
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(390) then
            log(S(49))
            lastReportedCount = S(14)
        end
        return
    end
    if count < MIN_ITEMS_TO_COLLECT then
        if lastReportedCount ~= S(424) then
            log(S(454) .. count .. S(176) .. MIN_ITEMS_TO_COLLECT .. S(269))
            lastReportedCount = S(199)
        end
        return
    end
    lastReportedCount = nil
    log(S(292) .. count .. S(458))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(131)] = function()
return function(import)
local getCurrentPlot = import(S(135))
local getPlotParts = import(S(186))
local log = import(S(455))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(56), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(271) .. (enabled and S(410) or S(487)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(71)] = function()
return function(import)
local Players = game:GetService(S(76))
local getCurrentPlot = import(S(289))
local getPlotParts = import(S(249))
local log = import(S(129))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(90))
    return pad and pad:FindFirstChildWhichIsA(S(28), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(25)
end
local autoPlace = {}
local enabled = false
local lastFire = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(219) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    log(S(364) .. (enabled and S(98) or S(3)))
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local player = Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild(S(256))
    if not character or not backpack then
        reason(S(137))
        return
    end
    local humanoid = character:FindFirstChildOfClass(S(504))
    local tool = backpack:FindFirstChildWhichIsA(S(158))
    if not humanoid then
        reason(S(80))
        return
    end
    if not tool then
        reason(S(435))
        return
    end
    local plot = getCurrentPlot()
    if not plot then
        reason(S(465))
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        reason(S(407))
        return
    end
    local placed = false
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderEmpty(prompt) then
            humanoid:EquipTool(tool)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if ok then
                log(S(286) .. tool.Name .. S(61) .. holder.Name)
            else
                log(S(4) .. tostring(err))
            end
            placed = true
            break
        end
    end
    if not placed then
        reason(S(309) .. #parts.Holders:GetChildren() .. S(316))
    else
        lastReason = nil
    end
end
return autoPlace
end
end
modules[S(385)] = function()
return function(import)
local Players = game:GetService(S(488))
local Workspace = game:GetService(S(149))
local teleport = import(S(89))
local click = import(S(383))
local log = import(S(331))
local CHECK_INTERVAL = 45
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(59))
    local mineGui = playerGui and playerGui:FindFirstChild(S(202))
    return mineGui and mineGui:FindFirstChild(S(68))
end
local function labelText(button)
    local content = button:FindFirstChild(S(510))
    local label = content and content:FindFirstChild(S(506))
    return label and label.Text or S(175)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(515))
    local scroll = content and content:FindFirstChild(S(376))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(174) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(305) .. button.Name .. S(46) .. tostring(err))
    end
    return ok
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(230))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(415), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(151))
    else
        lastAttempt = 0
        log(S(497))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(245) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(507))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(426))
        local closeButton = sellFrame:FindFirstChild(S(67))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(197))
    local selectAllButton = content and content:FindFirstChild(S(160))
    local sellButton = content and content:FindFirstChild(S(150))
    if selectAllButton and labelText(selectAllButton) == S(477) then
        clickButton(selectAllButton)
        log(S(375) .. cardCount .. S(403))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(12), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(291), 1, true) then
        clickButton(sellButton)
        log(S(201))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(16))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(84) .. cardCount .. S(337)
            .. tostring(sawLabel) .. S(285) .. NOTHING_SELLABLE_COOLDOWN .. S(368))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(180)] = function()
return function(import)
local getCurrentPlot = import(S(341))
local getPlotParts = import(S(192))
local log = import(S(5))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(346))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(509))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(427) .. (enabled and S(133) or S(83)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(189)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(418)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(213) .. child.Name .. S(517)
        if child:IsA(S(263)) then
            line = line .. string.format(
                S(218),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(172)) then
            line = line .. string.format(S(468), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(171)) or child:IsA(S(321)) then
            line = line .. S(400)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(139) .. attrName .. S(178) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(327))
    end
end
return dumpInstance
end
modules[S(481)] = function()
return function(import)
local Players = game:GetService(S(132))
local ReplicatedStorage = game:GetService(S(290))
local getCurrentPlot = import(S(287))
local getPlotParts = import(S(167))
local log = import(S(380))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(452) .. name .. S(414) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(147))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(434) .. child.Name .. S(417)
            if child:IsA(S(274)) then
                line = line .. S(421) .. tostring(child.Value)
            elseif child:IsA(S(398)) or child:IsA(S(232)) then
                line = line .. S(296) .. tostring(child.Text) .. S(365)
            end
            log(line)
            dumpSubtree(child, indent .. S(104))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(183))
        return 0
    end
    log(S(447) .. plot.Name .. S(244))
    dumpAttributes(plot, S(377))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(251))
        return 0
    end
    log(S(115))
    dumpSubtree(parts.Chest, S(187))
    local player = Players.LocalPlayer
    log(S(420))
    dumpSubtree(player, S(324))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(516) .. player.Name .. S(247))
        dumpSubtree(mine, S(111))
    end
    log(S(227))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(485)] = function()
return function(import)
local Players = game:GetService(S(351))
local log = import(S(32))
local function dumpVisible(root, indent)
    indent = indent or S(109)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(273)) and child.Visible then
            local line = indent .. child.ClassName .. S(48) .. child.Name .. S(210)
            if child:IsA(S(166)) or child:IsA(S(140)) then
                line = line .. S(401) .. tostring(child.Text) .. S(357)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(325) .. attrName .. S(432) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(258))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(406))
    local mineGui = playerGui and playerGui:FindFirstChild(S(58))
    if not mineGui then
        log(S(53))
        return
    end
    log(S(329))
    dumpVisible(mineGui)
    log(S(315))
end
return dumpMineGui
end
end
modules[S(233)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(519))
local log = import(S(360))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(394)) or descendant:IsA(S(128)) then
            count = count + 1
            log(S(471) .. descendant.ClassName .. S(105) .. descendant:GetFullName())
        end
    end
    log(S(35) .. count .. S(242))
    return count
end
return dumpRemotes
end
end
modules[S(493)] = function()
return function(import)
local Players = game:GetService(S(482))
local log = import(S(134))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(154))
    local mineGui = playerGui and playerGui:FindFirstChild(S(525))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(265))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(499)) and sellFrame.Content:FindFirstChild(S(297))
    if not scroll then
        log(S(209))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(456) or card:IsA(S(522)) then
            count = count + 1
            log(S(423) .. count .. S(125) .. card.ClassName .. S(116) .. card.Name .. S(217))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(79) .. attrName .. S(478) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(159) .. child.ClassName .. S(264) .. child.Name .. S(312)
                if child:IsA(S(113)) then
                    line = line .. S(87) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(317) .. count .. S(512))
    return count
end
return dumpSellCards
end
end
modules[S(26)] = function()
local Players = game:GetService(S(451))
local Workspace = game:GetService(S(24))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(392))
    local plots = mineWorld and mineWorld:FindFirstChild(S(138))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(480)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(381)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(344)),
        Nodes = plot:FindFirstChild(S(148)),
        Chest = plot:FindFirstChild(S(173)),
        Zones = plot:FindFirstChild(S(367)),
        Crew = plot:FindFirstChild(S(472)),
        Loot = plot:FindFirstChild(S(143)),
        PlotIndex = plot:GetAttribute(S(431)),
        ChestCapacity = plot:GetAttribute(S(278)),
    }
end
return getPlotParts
end
modules[S(491)] = function()
return function(import)
    local Players = game:GetService(S(466))
    local createWindow = import(S(152))(import)
    local i18n = import(S(370))
    local antiAfk = import(S(31))(import)
    local interactionLogger = import(S(474))(import)
    local scheduler = import(S(225))(import)
    local config = import(S(43))(import)
    local log = import(S(408))
    local window = createWindow({
        Subtitle = S(145),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local getCurrentPlot = import(S(374))
    local getPlotParts = import(S(262))
    local dumpInstance = import(S(358))
    local dumpRemotes = import(S(371))(import)
    local dumpSellCards = import(S(153))(import)
    local dumpGui = import(S(520))(import)
    local dumpChest = import(S(62))(import)
    local autoMine = import(S(60))(import)
    local autoPlace = import(S(81))(import)
    local autoChest = import(S(195))(import)
    local autoSell = import(S(23))(import)
    local autoUpgrade = import(S(513))(import)
    scheduler.RegisterStandalone(S(387), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterStandalone(S(50), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.RegisterExclusive(S(136), autoPlace.Step, autoPlace.IsEnabled)
    scheduler.RegisterExclusive(S(304), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy)
    scheduler.RegisterExclusive(S(293), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(268), S(241))
    persistentToggle(farmTab:AddCard(
        S(82),
        S(280)
    ), S(445), autoMine.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(250),
        S(347)
    ), S(355), autoPlace.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(169),
        S(243)
    ), S(440), autoChest.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(1),
        S(419)
    ), S(303), autoSell.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(320),
        S(402)
    ), S(190), autoUpgrade.SetEnabled)
    local debugCard = farmTab:AddCard(S(29), S(93))
    local checkBtn
    checkBtn = debugCard:AddButton(S(475), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(461)
            log(S(319))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(248),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(194),
        S(391)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(446), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(127)
            return
        end
        local ok, err = log(S(399))
        if not ok then
            dumpBtn.Text = S(13)
            dumpCard:SetSubtitle(S(302) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(38) .. firstNode.Name .. S(299))
            dumpInstance(firstNode, log)
        else
            log(S(235))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(270) .. firstHolder.Name .. S(207))
            dumpInstance(firstHolder, log)
        else
            log(S(281))
        end
        dumpBtn.Text = S(27)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(388),
        S(75)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(240), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(416))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(343))
        if not tool then
            toolDumpBtn.Text = S(21)
            return
        end
        log(S(298) .. tool.Name .. S(231))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(379) .. attrName .. S(372) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(97)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(198),
        S(393)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(366), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(163)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(275),
        S(359)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(483), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(469)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(257),
        S(457)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(524), function()
        dumpGui()
        guiDumpBtn.Text = S(191)
    end)
    local chestDumpCard = farmTab:AddCard(
        S(224),
        S(216)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(350), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(486)
    end)
    local settingsTab = window:AddTab(S(255), S(267))
    persistentToggle(
        settingsTab:AddCard(S(441), S(409)),
        S(395),
        antiAfk.SetEnabled
    )
    persistentToggle(
        settingsTab:AddCard(
            S(479),
            S(462)
        ),
        S(238),
        interactionLogger.SetEnabled
    )
    local savedScale = config.Get(S(95), 100)
    settingsTab:AddSlider(S(78), S(112), 50, 150, savedScale, function(value)
        config.Set(S(72), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(405), 1)
    settingsTab:AddSegmented({ S(19), S(162), S(430) }, savedLanguage, function(index, value)
        local code = ({ S(313), S(523), S(124) })[index]
        config.Set(S(228), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(282), S(2), S(276) })[savedLanguage] or S(508))
    log(S(498) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(438)] = function()
return {
    [79389059854988] = S(157),
}
end
import(S(164))(import)
