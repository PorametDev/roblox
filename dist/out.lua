-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"MekPro", "GuiButton", "core/interaction_logger", "[%X] ", "games/my_anime_mine/debug_dump_sell_cards", "games/my_anime_mine/debug_dump_gui", "Debug: Dump Chest Data", "core/get_place_id", " -> ", " '", "ValueLabel", "  [attr] ", "TextLabel", "Auto Mine: ความเร็ว", "Auto Chest: รอบเช็ค", "TextButton", "core/teleport", "Auto Upgrade: รอบกด", "[AutoSell] clicked SellButton", " <-- remote, likely fired by the real interaction", "", "TextButton", "[Config] couldn't write ", "Sell for", "HotbarFrame", "=empty", " cards", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "[AntiAFK] disabled", "waiting", "games/my_anime_mine/debug_dump_remotes", "ClickDetector", "This is not a supported place.", "Debug: Dump Visible GUI", "UIStroke", "Auto Mine: คลิกต่อรอบ", "ย่อขยายเมนูทั้งชุด", "", "", "", "games/my_anime_mine/get_plot_parts", "Container", "Done", "en-us", "Close", "games/my_anime_mine/auto_chest", "Dump", "games/my_anime_mine/get_current_plot", "[BestMiner] slot ", "s", "auto", "ItemFrame", "games/my_anime_mine/get_current_plot", "Frame", "TextLabel", "Players", "อ่านป้ายบน chest ทุกกี่วินาที", " error: ", "[Interaction] Button activated: ", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "[Interaction] logger disabled", "TextLabel", "core/logger", "'", "ItemName", "nothing to place or upgrade", "UICorner", "[AutoPlace] drifted out of range of ", "autoPlaceInterval", "TextLabel", "[AntiAFK] idle detected, simulating input", "Players", "[AutoSell] nothing to sell, closing", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", " remotes", "fill", "[Bb]uy", "PlayerGui", "[AutoChest] can't read the chest billboard, is the plot loaded?", "  ", "Actions", "Workspace", ": ", ",", "ProximityPrompt", "core/logger", "waiting", " cards)", "games/my_anime_mine/debug_dump_chest", "core/anti_afk", "Text", "core/logger", " = ", "TextButton", "core/click", "games/my_anime_mine/get_plot_parts", "open_menu_hint", "Players", "autoChestMinItems", "Frame", ": ", "[Scheduler] turn: ", "Nodes", "Pad", "[AutoChest] Take All worked: ", "[AutoPlace] ", "core/get_leaderstat", "MineGui", "Locked miner slot", "", " (Text=", "Damage", "PlayerGui", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "Chest", "[AutoPlace] holders: ", "TextLabel", "'", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "games/my_anime_mine/auto_sell", "games/my_anime_mine/get_current_plot", "GuiObject", "Amount", "th", " ===", "UpgradePart", "games/my_anime_mine/auto_upgrade", "", "languageIndex", "auto", "", " (", "  ", "[Logger] failed to write ", "games/my_anime_mine/auto_place", "Done", "Frame", "MineWorld", "?", "SellStand", "PlayerGui", "'", "games/my_anime_mine/get_best_miner", "Content", ")", ")", "GuiButton", "UICorner", " ===", "Debug: Dump Sell Cards", "Content", "[AntiAFK] enabled - watching Player.Idled", "TextLabel", "core/logger", "s", "TextLabel", "[AutoSell] nothing selectable (", "Dump", "UIListLayout", "ProximityPrompt", " (power ", "write reported success but the line isn't in ", "", "Zones", "[AutoPlace] slot ", "", "ScrollingFrame", "antiAfk", "core/dispatch", "menuScale", "[MyAnimeMine] no nodes present right now", "  ", "games/my_anime_mine/main", "X", "no isfile/readfile available to verify the write", "TextLabel", "th", "games/registry", "table", "autoPlace", "กด UpgradeClick ทุกกี่วินาที", "  [attr] ", "autoMineSpeed", " items", "Frame", "Players", "Scroll", "^([%d,]+)$", "Content", "core/logger", "[MyAnimeMine] no holders found", "PlayerGui", "UIScale", "unreadable", "ดีบัก", "costs ", "", "Text", ") -> ", "Search functions...", "^x(%d+)$", ")", "Debug: Dump Backpack Tool", "[AutoUpgrade] ", "Anti-AFK", "Scroll", "[AutoSell] fireproximityprompt error: ", "=locked(", "x", "[GuiDump] MineGui not found", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "TextLabel", "TextBox", "=", "Content", "core/get_place_id", "th", "autoSell", "[MyAnimeMine] === Dump start ===", "[Scheduler] ", "core/dispatch", "core/config", "games/my_anime_mine/auto_mine", "Scroll", "Auto Place Miner", " items)", "prompt disabled", "autoSellInterval", "interactionLogger", "TextLabel", "AutoChest", "Tool", "[AutoChest] fireproximityprompt error: ", "Content", "[AutoSell] also selected ", "games/my_anime_mine/debug_dump", "core/config", ")", "/s", " doesn't exist", "core/get_me", "Auto Sell", "autoMine", " ===", " (ActionText=", "^(%d+)", " = ", "games/my_anime_mine/auto_place", "[AutoPlace] upgrading ", "AutoMine", "CoreGui", "games/my_anime_mine/get_best_miner", "[ChestDump] === ReplicatedStorage.", "core/i18n", ": ", "/", "Card", "[AutoChest] waiting: ", "Backpack", "[MyAnimeMine] not standing in any plot", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "RemoteEvent", "ตามเครื่อง", "MineGui", "Frame", "MineGui", "core/get_place_id", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "HumanoidRootPart", " unit card(s) Select All skipped", "\n", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "Progress", "[AutoSell] sell menu didn't open, aborting", "ValueBase", "Players", "[AutoChest] click failed on ", "locked slot", "autoChest", "Open menu", "[Interaction] ClickDetector clicked: ", "Empty miner slot", "leaderstats", "Card", "", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "Players", "games/my_anime_mine/auto_chest", "UserInputService", "SelectAllButton", "TextButton", "[^%d%.]", "ValueLabel", " -> ", "TextButton", "TextButton", "core/logger", "MekPro.log", "Crew", "ขนาดเมนู", "GuiService", "⚙", "[AutoPlace] picked up ", "Frame", "HumanoidRootPart", "core/teleport", "TabBar", "core/logger", "%", "[ChestDump] plot has no Chest", "Close", "Header", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", ": ", "ฟาร์ม", "core/anti_afk", "enabled", "languageIndex", " (prompt says '", "upgrade", "TextLabel", "🐛", "button has no size on screen", "UIListLayout", "GuiObject", "🏠", "Content", "no tool", "ProximityPrompt", " (", "", ": ", "Badge", "autoUpgradeInterval", "')", "[Interaction] ProximityPrompt triggered: ", "[Config] couldn't read ", "Workspace", "ค้นหาฟังก์ชัน...", " ชิ้น", "'", "PlayerGui", "Auto Chest", "Done", "[ChestDump] === Chest subtree ===", "Frame", "^%$", "log failed", "[AutoPlace] nothing in the hotbar to place", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Content", "ClickDetector", "Auto Chest: เกณฑ์เก็บ", "Sell for", "[AutoPlace] placed ", "[Interaction] Key pressed: %s (gameProcessed=%s)", "[AutoSell] clicked SelectAllButton (", "Main", "games/my_anime_mine/get_current_plot", " ", ": ", ": ", "core/logger", "games/my_anime_mine/debug_dump_chest", "ReplicatedStorage", "  ", "'), backing off ", "MineGui", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "ตั้งค่า", "auto", "[MyAnimeMine] === Tool: ", "[AutoChest] enabled (collects once >= ", "Interaction Logger", "games/my_anime_mine/get_plot_parts", "RunService", "Frame", "open_menu_hint", " (power ", "games/my_anime_mine/get_plot_parts", " items", " (", "Players", "action '", "AutoUpgrade", "TextLabel", "", " '", "[AutoPlace] disabled", ") from ", "games/my_anime_mine/auto_sell", "games/my_anime_mine/auto_mine", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "TextButton", "games/my_anime_mine/debug_dump", "core/ui", "", "\n", "ValueBase", "", "[MyAnimeMine] === Node: ", "core/scheduler", "%$", "VirtualInputManager", "[Remotes] ", "Debug: Dump All Remotes", "core/logger", "Loot", "ChestFrame", "fill", "[GuiDump] === visible MineGui tree ===", " ===", "Backpack", ",", "Label", "core/logger", "", "[AutoPlace] fireproximityprompt error: ", " | ", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", " ===", "core/logger", "(nothing to do)", "'", "ClickDetector", "Content", ")", "core/logger", "Dump", "[AutoPlace] ", "core/logger", " still empty after placing ", "TextLabel", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Players", "Label", "core/i18n", "[GuiDump] === end ===", " = ", "Select All", " from ", "  ", "not found", " (Text=", "PlotIndex", "TextButton", "core/logger", "Content", "Dump", "[AutoChest] ", "Frame", "SellButton", " cards left, label = '", "-", "TextLabel", "ไทย", "en", "MineGui", "Error: ", " parts", "games/my_anime_mine/get_plot_parts", "MekPro", "core/teleport", "ProximityPrompt", "games/my_anime_mine/get_plot_parts", "Plots", "autoUpgrade", "Auto Upgrade", "Workspace", ")", "[AutoSell] click failed on ", "games/registry", " '", " attributes ===", "ChestCapacity", "menuScale", "UserInputService", "auto", "TakeButton", "[AutoChest] chest didn't open, aborting", "SellFrame", " power ", "[ChestDump] === plot ", "ReplicatedStorage", "games/my_anime_mine/debug_dump_remotes", " = ", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "", "core/scheduler", "RemoteFunction", "autoMineClicks", "disabled", "Auto Place: รอบเช็ค", "[AutoPlace] couldn't pick up ", "AutoSell", "", "search_placeholder", "UIListLayout", " ===", "[ChestDump] not standing in any plot", "core/logger", "ProximityPrompt", "s", "core/i18n", "Auto Sell: รอบขาย", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "", "autoChestInterval", "write reported success but ", "[AutoSell] enabled (only sells when there's something in the scroll)", "Players", "TextLabel", "ปุ่มเปิดเมนู", "ProximityPrompt", "en", " '", "[MekPro] settings file: ", "Search", "  ", "[Interaction] logger enabled - go click/interact with things now", "/", "อังกฤษ", "neither appendfile nor writefile is available", " = ", "  [attr] ", "games/my_anime_mine/get_plot_parts", "[AutoMine] ", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "[Uu]nlock", "[ChestDump] === Player subtree ===", "games/my_anime_mine/get_current_plot", " (MaxActivationDistance=%s)", "Players", " items, going to collect", "Check", "unreadable", "ClickDetector", "HttpService", "core/teleport", "[AutoChest] Take All had NO effect (", ": ", "", "TextLabel", "[AutoChest] disabled", " = ", "search_placeholder", "RemoteEvent", "Holders", "TextButton", "Frame", "core/click", "[SellCards] Card #", "core/ui", "OwnerUserId", " items)", "Players", "Frame", "core/click", "[SellCards] === total cards: ", "Players", "no plot", "Content", "Debug: Dump Node/Holder", "games/my_anime_mine/main", "[attr] ", ") didn't take, trying the next one", "/config.json", "games/my_anime_mine/get_current_plot", "SellFrame", "enabled", "th", "SelectOverlay", "VirtualUser", "games/my_anime_mine/auto_upgrade", " x", "(", "My Anime Mine · Mining Hub", "Players", "disabled", "CoreUI", "[MyAnimeMine] === Holder: ", "RemoteFunction", "[AutoSell] disabled", "Humanoid", "Card", "s", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/debug_dump_gui", "ScreenGui", "s", "TextButton", "core/interaction_logger", "AutoPlace", ")", "PlayerGui", "Dump", "[Remotes] === total: ", "", "UIPadding", "[ChestDump] === end ===", "  [attr] ", "Place", "Debug: Current Plot", "Content", ") on ", "Badge", "en", "Players", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", ": ", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "ClickDetector", " = ", " while selecting", "Dump", "core/click", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "", "TextLabel", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "/", "Auto Mine", " (Text=", "games/my_anime_mine/debug_dump_sell_cards", "'", "core/logger", " '"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(317)] = function()
return function(import)
local log = import(S(429))
local Players = game:GetService(S(534))
local VirtualUser = game:GetService(S(574))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(152))
        connection = player.Idled:Connect(function()
            log(S(71))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(29))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(95)] = function()
local VirtualInputManager = game:GetService(S(405))
local GuiService = game:GetService(S(302))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(324)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(222)] = function()
return function(import)
local HttpService = game:GetService(S(539))
local getPlaceId = import(S(216))
local log = import(S(448))
local ROOT_FOLDER = S(1)
local placeFolder = ROOT_FOLDER .. S(256) .. tostring(getPlaceId())
local filePath = placeFolder .. S(568)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(179) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(338) .. filePath .. S(315) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(23) .. filePath .. S(255) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(221)] = function()
return function(import)
    local getPlaceId = import(S(268))
    local registry = import(S(178))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(33))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(107)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(284))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(241)] = function()
local Players = game:GetService(S(436))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(585))
    local backpack = player:WaitForChild(S(259))
    return player, humanoid, backpack
end
return getMe
end
modules[S(8)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(254)] = function()
local Players = game:GetService(S(98))
local STRINGS = {
    en = {
        search_placeholder = S(200),
        open_menu_hint = S(281),
    },
    th = {
        search_placeholder = S(340),
        open_menu_hint = S(514),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(44)
    if locale:sub(1, 2):lower() == S(177) then
        return S(217)
    end
    return S(608)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(51) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(593)] = function()
return function(import)
local Players = game:GetService(S(56))
local Workspace = game:GetService(S(470))
local UserInputService = game:GetService(S(478))
local log = import(S(298))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(337) .. describe(prompt)
            .. S(245) .. prompt.ActionText .. S(146))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(282) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(59) .. describe(button)
            .. S(445) .. tostring(button.Text) .. S(428))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(85)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(538)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(147)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(61))
        return
    end
    connections = {}
    log(S(521))
    local playerGui = Players.LocalPlayer:WaitForChild(S(192))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(330)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(426)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(2)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(357),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(417)] = function()
local LOG_FILE = S(299)
local function log(message)
    local line = os.date(S(4)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(497))
            end
            appendfile(LOG_FILE, line .. S(272))
        elseif writefile then
            local existing = S(398)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(399))
        else
            error(S(524))
        end
        if not (isfile and readfile) then
            error(S(175))
        end
        if not isfile(LOG_FILE) then
            error(S(510) .. LOG_FILE .. S(240))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(162) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(134) .. LOG_FILE .. S(101) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(490)] = function()
return function(import)
local RunService = game:GetService(S(377))
local log = import(S(92))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(220) .. task_.name .. S(58) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(102) .. (currentTask and currentTask.name or S(424)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(17)] = function()
local Players = game:GetService(S(288))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(306))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(554)] = function()
return function(import)
local i18n = import(S(438))
local UserInputService = game:GetService(S(290))
local THEME = {
    BrandName = S(463),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(148))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(67))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(600))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(550))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(166)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(35))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(387))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(139)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(322))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(286)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(167))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(325))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(176))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(452))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(620))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(434))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(212))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(294))
            if not valueLbl then
                valueLbl = Instance.new(S(456))
                valueLbl.Name = S(11)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(334))
            if not badge then
                badge = Instance.new(S(513))
                badge.Name = S(607)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(296))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(378))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(185))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(22))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(131)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(310)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(551))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(54))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(592))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(508)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(347))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(159))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(297))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(198)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(590))
    screenGui.Name = S(581)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(251))
    local main = Instance.new(S(100))
    main.Name = S(359)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(193))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(305))
    tabBar.Name = S(308)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(499))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(266))
    header.Name = S(313)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(16))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(544))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(117))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(128)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(94))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(174)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(395))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(455)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(213))
    searchBox.Name = S(519)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(498))
    searchBox.Text = S(401)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(137))
    content.Name = S(189)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(558))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(70))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(97))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(156))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(91)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(197) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(361) .. (item.Subtitle or S(388))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(547))
        footerLabel.Text = i18n.T(S(379))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(289)] = function()
return function(import)
local Players = game:GetService(S(579))
local getCurrentPlot = import(S(48))
local getPlotParts = import(S(381))
local teleport = import(S(540))
local click = import(S(552))
local log = import(S(190))
local checkInterval = 3
local minItemsToCollect = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(343))
    local mineGui = playerGui and playerGui:FindFirstChild(S(108))
    return mineGui and mineGui:FindFirstChild(S(410))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(234))
    local actions = content and content:FindFirstChild(S(81))
    return actions and actions:FindFirstChild(S(480))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(415), S(489)):match(S(246))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(123), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(328))
    local progress = content and content:FindFirstChild(S(274))
    local label = progress and progress:FindFirstChild(S(437))
    return parseCount(label and label.Text or S(21))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(278) .. button.Name .. S(333) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(515), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(545))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(374) .. minItemsToCollect .. S(226))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local cachedCount, cachedAt = nil, 0
function autoChest.HasWork()
    local now = tick()
    if now - cachedAt > 1 then
        cachedCount = getChestCount()
        cachedAt = now
    end
    return cachedCount ~= nil and cachedCount >= minItemsToCollect
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(233) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(481))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(105) .. count .. S(9) .. after .. S(382))
        else
            log(S(541) .. tostring(count)
                .. S(295) .. tostring(after) .. S(556))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(194) then
            log(S(79))
            lastReportedCount = S(537)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReportedCount ~= S(30) then
            log(S(258) .. count .. S(622) .. minItemsToCollect .. S(184))
            lastReportedCount = S(87)
        end
        return
    end
    lastReportedCount = nil
    log(S(451) .. count .. S(535))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(223)] = function()
return function(import)
local getCurrentPlot = import(S(53))
local getPlotParts = import(S(376))
local log = import(S(86))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(353), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(528) .. (enabled and S(318) or S(580)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(248)] = function()
return function(import)
local Players = game:GetService(S(277))
local getCurrentPlot = import(S(360))
local getPlotParts = import(S(466))
local getMinerSlots = import(S(143))(import)
local teleport = import(S(307))
local click = import(S(617))
local log = import(S(63))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(283)
local LOCKED_SLOT_TEXT = S(109)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(104))
    return pad and pad:FindFirstChildWhichIsA(S(465), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(55)) then
            local digits = descendant.Text:match(S(188))
            if digits then
                return tonumber((digits:gsub(S(84), S(418))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(153)) and descendant.Text:match(S(348)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(279)
    end
    if not prompt.Enabled then
        return true, S(227)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(196) .. price
    end
    local action = prompt.ActionText or S(332)
    if action:match(S(530)) or action:match(S(77)) or action:match(S(404)) then
        return true, S(385) .. action .. S(342)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(270))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(419) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(208) .. why .. S(595))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(26))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(214) .. prompt.ObjectText .. S(577) .. power .. S(238))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(420))
    if line ~= lastSurvey then
        log(S(116) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(76), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(321), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(431) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(390))
    else
        lastAttempt = 0
        log(S(261))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(610),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(603), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(350))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(68) .. holder.Name .. S(615))
            return false
        end
        log(S(165) .. candidate.slot .. S(383) .. candidate.name
            .. S(567))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(106) .. holder.Name .. S(433) .. miner.name)
        return false
    end
    log(S(356) .. miner.name .. S(161) .. miner.power
        .. S(606) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(495) .. entry.name .. S(442) .. entry.holder.Name
            .. S(320) .. tostring(entry.prompt.ActionText) .. S(336))
        return false
    end
    log(S(304) .. entry.name .. S(380) .. entry.power
        .. S(391) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(66))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(411) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(249) .. target.holder.Name .. S(362) .. target.name
        .. S(132) .. target.power .. S(199) .. best.name .. S(331) .. best.power .. S(145))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(392)] = function()
return function(import)
local Players = game:GetService(S(72))
local Workspace = game:GetService(S(339))
local teleport = import(S(464))
local click = import(S(559))
local log = import(S(309))
local checkInterval = 45
local nothingSellableCooldown = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(113))
    local mineGui = playerGui and playerGui:FindFirstChild(S(369))
    return mineGui and mineGui:FindFirstChild(S(570))
end
local function labelText(button)
    local content = button:FindFirstChild(S(144))
    local label = content and content:FindFirstChild(S(416))
    return label and label.Text or S(40)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(352))
    local scroll = content and content:FindFirstChild(S(187))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(285) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(472) .. button.Name .. S(83) .. tostring(err))
    end
    return ok
end
local function isCardSelected(card)
    local content = card:FindFirstChild(S(151))
    local overlay = content and content:FindFirstChild(S(573))
    return overlay ~= nil and overlay.Visible
end
local function isOnScreen(card, scroll)
    local top = scroll.AbsolutePosition.Y
    local bottom = top + scroll.AbsoluteSize.Y
    local cardTop = card.AbsolutePosition.Y
    local cardBottom = cardTop + card.AbsoluteSize.Y
    return cardTop >= top and cardBottom <= bottom
end
local function selectUnselectedCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(605))
    local scroll = content and content:FindFirstChild(S(224))
    if not scroll then
        return 0
    end
    local selected = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(257) and not isCardSelected(card) and isOnScreen(card, scroll) then
            clickButton(card)
            selected = selected + 1
            task.wait(0.1)
        end
    end
    return selected
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(140))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(160), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(584))
    else
        lastAttempt = 0
        log(S(511))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(207) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(275))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(73))
        local closeButton = sellFrame:FindFirstChild(S(45))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(449))
    local selectAllButton = content and content:FindFirstChild(S(291))
    local sellButton = content and content:FindFirstChild(S(453))
    if selectAllButton and labelText(selectAllButton) == S(441) then
        clickButton(selectAllButton)
        log(S(358) .. cardCount .. S(88))
        task.wait(0.2)
    end
    local selected = selectUnselectedCards(sellFrame)
    if selected > 0 then
        log(S(235) .. selected .. S(271))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(355), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(24), 1, true) then
        clickButton(sellButton)
        log(S(19))
    else
        lastAttempt = tick() + nothingSellableCooldown - checkInterval
        local closeButton = sellFrame:FindFirstChild(S(312))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(157) .. cardCount .. S(454)
            .. tostring(sawLabel) .. S(368) .. nothingSellableCooldown .. S(587))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(127)] = function()
return function(import)
local getCurrentPlot = import(S(569))
local getPlotParts = import(S(462))
local log = import(S(423))
local fireInterval = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(126))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(32))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(204) .. (enabled and S(571) or S(493)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.SetInterval(seconds)
    fireInterval = math.max(seconds, 1)
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < fireInterval then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(396)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(543)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(474) .. child.Name .. S(118)
        if child:IsA(S(503)) then
            line = line .. string.format(
                S(507),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(613)) then
            line = line .. string.format(S(533), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(263)) or child:IsA(S(491)) then
            line = line .. S(20)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(526) .. attrName .. S(525) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(520))
    end
end
return dumpInstance
end
modules[S(89)] = function()
return function(import)
local Players = game:GetService(S(186))
local ReplicatedStorage = game:GetService(S(485))
local getCurrentPlot = import(S(121))
local getPlotParts = import(S(96))
local log = import(S(154))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(566) .. name .. S(487) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(80))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(517) .. child.Name .. S(626)
            if child:IsA(S(400)) then
                line = line .. S(546) .. tostring(child.Value)
            elseif child:IsA(S(230)) or child:IsA(S(292)) then
                line = line .. S(624) .. tostring(child.Text) .. S(202)
            end
            log(line)
            dumpSubtree(child, indent .. S(443))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(501))
        return 0
    end
    log(S(484) .. plot.Name .. S(475))
    dumpAttributes(plot, S(172))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(311))
        return 0
    end
    log(S(346))
    dumpSubtree(parts.Chest, S(38))
    local player = Players.LocalPlayer
    log(S(531))
    dumpSubtree(player, S(163))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(253) .. player.Name .. S(413))
        dumpSubtree(mine, S(39))
    end
    log(S(601))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(589)] = function()
return function(import)
local Players = game:GetService(S(561))
local log = import(S(364))
local function dumpVisible(root, indent)
    indent = indent or S(599)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(326)) and child.Visible then
            local line = indent .. child.ClassName .. S(628) .. child.Name .. S(64)
            if child:IsA(S(13)) or child:IsA(S(447)) then
                line = line .. S(111) .. tostring(child.Text) .. S(471)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(182) .. attrName .. S(93) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(133))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(141))
    local mineGui = playerGui and playerGui:FindFirstChild(S(267))
    if not mineGui then
        log(S(210))
        return
    end
    log(S(412))
    dumpVisible(mineGui)
    log(S(439))
end
return dumpMineGui
end
end
modules[S(31)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(366))
local log = import(S(408))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(548)) or descendant:IsA(S(583)) then
            count = count + 1
            log(S(406) .. descendant.ClassName .. S(611) .. descendant:GetFullName())
        end
    end
    log(S(598) .. count .. S(500))
    return count
end
return dumpRemotes
end
end
modules[S(5)] = function()
return function(import)
local Players = game:GetService(S(512))
local log = import(S(432))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(596))
    local mineGui = playerGui and playerGui:FindFirstChild(S(265))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(482))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(563)) and sellFrame.Content:FindFirstChild(S(206))
    if not scroll then
        log(S(488))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(586) or card:IsA(S(122)) then
            count = count + 1
            log(S(553) .. count .. S(542) .. card.ClassName .. S(10) .. card.Name .. S(425))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(602) .. attrName .. S(440) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(367) .. child.ClassName .. S(389) .. child.Name .. S(142)
                if child:IsA(S(276)) then
                    line = line .. S(247) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(560) .. count .. S(125))
    return count
end
return dumpSellCards
end
end
modules[S(252)] = function()
return function(import)
local Players = game:GetService(S(609))
local log = import(S(502))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(78))
    local mineGui = playerGui and playerGui:FindFirstChild(S(459))
    local hotbar = mineGui and mineGui:FindFirstChild(S(25))
    local content = hotbar and hotbar:FindFirstChild(S(427))
    return content and content:FindFirstChild(S(42))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(62)) then
            local count = descendant.Text:match(S(201))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(52))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(215))
    local nameLabel = content and content:FindFirstChild(S(65))
    local damageLabel = content and content:FindFirstChild(S(112))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(110) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(293), S(619)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(522) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(49) .. slot .. S(363) .. info.name
                        .. S(483) .. info.power .. S(576) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(532)] = function()
local Players = game:GetService(S(384))
local Workspace = game:GetService(S(82))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(138))
    local plots = mineWorld and mineWorld:FindFirstChild(S(467))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(555)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(527)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(549)),
        Nodes = plot:FindFirstChild(S(103)),
        Chest = plot:FindFirstChild(S(115)),
        Zones = plot:FindFirstChild(S(164)),
        Crew = plot:FindFirstChild(S(300)),
        Loot = plot:FindFirstChild(S(409)),
        PlotIndex = plot:GetAttribute(S(446)),
        ChestCapacity = plot:GetAttribute(S(476)),
    }
end
return getPlotParts
end
modules[S(173)] = function()
return function(import)
    local Players = game:GetService(S(557))
    local createWindow = import(S(397))(import)
    local i18n = import(S(505))
    local antiAfk = import(S(90))(import)
    local interactionLogger = import(S(3))(import)
    local scheduler = import(S(403))(import)
    local config = import(S(237))(import)
    local log = import(S(627))
    local window = createWindow({
        Subtitle = S(578),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(588))
    local getPlotParts = import(S(41))
    local dumpInstance = import(S(236))
    local dumpRemotes = import(S(486))(import)
    local dumpSellCards = import(S(625))(import)
    local dumpGui = import(S(6))(import)
    local dumpChest = import(S(365))(import)
    local autoMine = import(S(393))(import)
    local autoPlace = import(S(135))(import)
    local autoChest = import(S(46))(import)
    local autoSell = import(S(120))(import)
    local autoUpgrade = import(S(575))(import)
    scheduler.RegisterStandalone(S(250), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterStandalone(S(386), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.RegisterExclusive(S(231), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(594), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(496), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(316), S(327))
    persistentToggle(farmTab:AddCard(
        S(623),
        S(618)
    ), S(243), autoMine.SetEnabled)
    persistentSlider(farmTab, S(14), S(211),
        1, 30, S(183), 5, autoMine.SetCyclesPerSecond, S(239))
    persistentSlider(farmTab, S(36), S(119),
        1, 10, S(492), 1, autoMine.SetClicksPerCycle, S(209))
    persistentToggle(farmTab:AddCard(
        S(225),
        S(262)
    ), S(180), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(494), S(28),
        1, 60, S(69), 3, autoPlace.SetCheckInterval, S(50))
    persistentToggle(farmTab:AddCard(
        S(344),
        S(287)
    ), S(280), autoChest.SetEnabled)
    persistentSlider(farmTab, S(354), S(314),
        50, 3000, S(99), 100, autoChest.SetMinItems, S(341))
    persistentSlider(farmTab, S(15), S(57),
        1, 60, S(509), 3, autoChest.SetCheckInterval, S(504))
    persistentToggle(farmTab:AddCard(
        S(242),
        S(351)
    ), S(218), autoSell.SetEnabled)
    persistentSlider(farmTab, S(506), S(612),
        10, 300, S(228), 45, autoSell.SetCheckInterval, S(155))
    persistentToggle(farmTab:AddCard(
        S(469),
        S(269)
    ), S(468), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(18), S(181),
        1, 30, S(335), 1, autoUpgrade.SetInterval, S(591))
    local debugTab = window:AddTab(S(195), S(323))
    persistentToggle(
        debugTab:AddCard(
            S(375),
            S(74)
        ),
        S(229),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(604), S(394))
    local checkBtn
    checkBtn = debugCard:AddButton(S(536), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(444)
            log(S(260))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(421),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(564),
        S(273)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(597), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(562)
            return
        end
        local ok, err = log(S(219))
        if not ok then
            dumpBtn.Text = S(349)
            dumpCard:SetSubtitle(S(460) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(402) .. firstNode.Name .. S(244))
            dumpInstance(firstNode, log)
        else
            log(S(171))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(582) .. firstHolder.Name .. S(422))
            dumpInstance(firstHolder, log)
        else
            log(S(191))
        end
        dumpBtn.Text = S(43)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(203),
        S(529)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(430), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(414))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(232))
        if not tool then
            toolDumpBtn.Text = S(329)
            return
        end
        log(S(373) .. tool.Name .. S(149))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(12) .. attrName .. S(614) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(136)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(407),
        S(370)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(47), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(75)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(150),
        S(621)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(158), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(27)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(34),
        S(435)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(450), function()
        dumpGui()
        guiDumpBtn.Text = S(345)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(7),
        S(60)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(616), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(461)
    end)
    local settingsTab = window:AddTab(S(371), S(303))
    persistentToggle(
        settingsTab:AddCard(S(205), S(114)),
        S(168),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(477), 100)
    settingsTab:AddSlider(S(301), S(37), 50, 150, savedScale, function(value)
        config.Set(S(170), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(319), 1)
    settingsTab:AddSegmented({ S(264), S(523), S(457) }, savedLanguage, function(index, value)
        local code = ({ S(479), S(458), S(572) })[index]
        config.Set(S(129), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(130), S(516), S(124) })[savedLanguage] or S(372))
    log(S(518) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(473)] = function()
return {
    [79389059854988] = S(565),
}
end
import(S(169))(import)
