-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"CoreUI", "UICorner", "games/my_anime_mine/get_current_plot", "core/interaction_logger", "Open menu", "games/my_anime_mine/auto_open_chest", "[%X] ", "games/my_anime_mine/auto_mine", "Main", "Debug: Dump Backpack Tool", "Badge", "Frame", "games/my_anime_mine/get_plot_parts", "core/logger", "Anti-AFK", "[MyAnimeMine] === Dump start ===", "UIScale", " '", "'", "", "games/my_anime_mine/auto_take_chest", "[AutoTakeChest] clicked Take All", "Auto Mine", "GuiButton", "Humanoid", "core/logger", "Interaction Logger (debug)", "UIStroke", "[AutoPlace] disabled", "not found", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "MekPro.log", "RemoteFunction", "TextButton", "core/ui", "[AntiAFK] idle detected, simulating input", "UICorner", "core/get_place_id", "", "ProximityPrompt", "?", "core/logger", "Tool", "UserInputService", "[AutoTakeChest] enabled", "games/my_anime_mine/get_plot_parts", "[AutoTakeChest] disabled", "[Interaction] ProximityPrompt triggered: ", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", ")", "games/my_anime_mine/get_plot_parts", "ProximityPrompt", "TextBox", " (MaxActivationDistance=%s)", "TextLabel", "กด ChestPrompt ให้อัตโนมัติ เพื่อให้หน้า Take All โผล่ขึ้นมา", ": ", "Frame", "\n", "[AutoOpenChest] enabled", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Crew", " (ActionText=", "Dump", "  ", "", "[AutoTakeChest] ", "-", "firesignal is not available on this executor", "games/my_anime_mine/auto_mine", "TextLabel", "[MyAnimeMine] not standing in any plot", "games/my_anime_mine/get_current_plot", "neither appendfile nor writefile is available", "th", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Empty miner slot", "Zones", "core/anti_afk", "Backpack", "core/i18n", "core/dispatch", "GuiButton", "th", "Error: ", "Players", "[AutoMine] disabled", "Frame", "RunService", "core/logger", ")", "Frame", "Backpack", "TextLabel", "Search functions...", "ProximityPrompt", "leaderstats", "Header", "", "Backpack", "no isfile/readfile available to verify the write", "Auto Open Chest", "RunService", "TextLabel", "core/interaction_logger", "search_placeholder", "TextLabel", "take all", "Players", "ตั้งค่า", "[AutoPlace] enabled", " ", "ClickDetector", "core/i18n", "Players", "Players", "  [attr] ", "Tool", "Badge", "My Anime Mine · Mining Hub", "core/logger", "core/get_me", "ClickDetector", "Done", "open_menu_hint", "RunService", "write reported success but the line isn't in ", "%", "Frame", "Debug: Current Plot", "games/my_anime_mine/main", "Done", "ไทย", "RemoteEvent", " <-- remote, likely fired by the real interaction", "⚙", "  [attr] ", "Search", "TextButton", "TextLabel", "games/my_anime_mine/get_current_plot", "TextLabel", "อังกฤษ", "ValueLabel", "[AutoPlace] placed ", "UIListLayout", "", "Content", "ProximityPrompt", "Humanoid", "", "TextButton", "MekPro", "core/logger", "write reported success but ", "", "TextLabel", "ตามเครื่อง", "games/registry", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "🏠", "TextLabel", "[Interaction] logger disabled", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "TextButton", "core/i18n", "ScreenGui", "VirtualUser", "This is not a supported place.", "core/logger", "Pad", "", "Debug: Dump Node/Holder", "[Interaction] ClickDetector clicked: ", "search_placeholder", "core/get_leaderstat", " = ", "TextButton", "CoreGui", "Frame", "Players", "Players", "OwnerUserId", "log failed", "ClickDetector", "ขนาดเมนู", "[Interaction] Key pressed: %s (gameProcessed=%s)", "Nodes", "Check", "Auto Place Miner", "TextButton", "UIListLayout", "%", " ===", "\n", "TabBar", "UIPadding", "", "TextLabel", "Plots", "[AutoOpenChest] disabled", "games/my_anime_mine/debug_dump", "games/my_anime_mine/get_current_plot", "Workspace", "games/my_anime_mine/auto_place", "TextLabel", "[MyAnimeMine] === Tool: ", "ClickDetector", "ย่อขยายเมนูทั้งชุด", " (Text=", "en", "Dump", "ProximityPrompt", "ปุ่มเปิดเมนู", "auto", "Players", "[Logger] failed to write ", "[MyAnimeMine] === Holder: ", "ValueLabel", "ฟาร์ม", "games/registry", "PlayerGui", "PlayerGui", "en-us", " = ", "[MyAnimeMine] === Node: ", "Frame", "Frame", "games/my_anime_mine/get_plot_parts", "auto", "[AutoMine] enabled", "games/my_anime_mine/main", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " ===", "MineWorld", "TextLabel", "UIListLayout", "games/my_anime_mine/get_current_plot", "TextButton", "Frame", "[AntiAFK] enabled - watching Player.Idled", "games/my_anime_mine/auto_take_chest", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "core/get_place_id", "Players", "", "th", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Text", "[Interaction] Button activated: ", "core/dispatch", "TextButton", "core/ui", "Loot", "ค้นหาฟังก์ชัน...", "no plot", "PlotIndex", " doesn't exist", "", "ChestCapacity", "games/my_anime_mine/auto_open_chest", "games/my_anime_mine/get_plot_parts", "X", "[MyAnimeMine] no holders found", "open_menu_hint", "ScrollingFrame", "games/my_anime_mine/debug_dump", " on ", "TextButton", "Text", "Workspace", "[MyAnimeMine] no nodes present right now", "[AntiAFK] disabled", "Chest", "[Interaction] logger enabled - go click/interact with things now", "Holders", "games/my_anime_mine/auto_place", " ===", "UserInputService", "Frame", "no tool", "Frame", "core/logger", "RunService", "en", "core/anti_afk"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(286)] = function()
return function(import)
local log = import(S(42))
local Players = game:GetService(S(182))
local VirtualUser = game:GetService(S(168))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(241))
        connection = player.Idled:Connect(function()
            log(S(36))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(273))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(251)] = function()
return function(import)
    local getPlaceId = import(S(244))
    local registry = import(S(159))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(169))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(176)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(97))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(122)] = function()
local Players = game:GetService(S(116))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(150))
    local backpack = player:WaitForChild(S(93))
    return player, humanoid, backpack
end
return getMe
end
modules[S(38)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(81)] = function()
local Players = game:GetService(S(245))
local STRINGS = {
    en = {
        search_placeholder = S(95),
        open_menu_hint = S(5),
    },
    th = {
        search_placeholder = S(255),
        open_menu_hint = S(214),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(224)
    if locale:sub(1, 2):lower() == S(84) then
        return S(75)
    end
    return S(211)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(215) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(4)] = function()
return function(import)
local Players = game:GetService(S(109))
local Workspace = game:GetService(S(271))
local UserInputService = game:GetService(S(44))
local log = import(S(121))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(48) .. describe(prompt)
            .. S(63) .. prompt.ActionText .. S(91))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(174) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(250) .. describe(button)
            .. S(210) .. tostring(button.Text) .. S(50))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(149)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(208)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(24)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(163))
        return
    end
    connections = {}
    log(S(275))
    local playerGui = Players.LocalPlayer:WaitForChild(S(223))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(40)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(123)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(83)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(187),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(170)] = function()
local LOG_FILE = S(32)
local function log(message)
    local line = os.date(S(7)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(39))
            end
            appendfile(LOG_FILE, line .. S(195))
        elseif writefile then
            local existing = S(20)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(59))
        else
            error(S(74))
        end
        if not (isfile and readfile) then
            error(S(101))
        end
        if not isfile(LOG_FILE) then
            error(S(155) .. LOG_FILE .. S(258))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(127) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(217) .. LOG_FILE .. S(57) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(253)] = function()
return function(import)
local i18n = import(S(166))
local UserInputService = game:GetService(S(279))
local THEME = {
    BrandName = S(153),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(2))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(37))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(197))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(165))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(99)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(28))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(94))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(41)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(71))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(151)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(266))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(146))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(107))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(12))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(142))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(236))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(140))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(144))
            if not valueLbl then
                valueLbl = Instance.new(S(199))
                valueLbl.Name = S(219)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(11))
            if not badge then
                badge = Instance.new(S(206))
                badge.Name = S(119)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(178))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(228))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(180))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(139))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(198)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(193))
        local track = Instance.new(S(92))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(282))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(34))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(156)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(128)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(58))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(237))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(239))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(270)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(167))
    screenGui.Name = S(1)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(179))
    local main = Instance.new(S(280))
    main.Name = S(9)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(17))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(227))
    tabBar.Name = S(196)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(192))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(129))
    header.Name = S(98)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(252))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(55))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(162))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(246)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(269))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(263)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(152))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(68)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(53))
    searchBox.Name = S(138)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(106))
    searchBox.Text = S(147)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(240))
    content.Name = S(148)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(88))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(104))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(265))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(157))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(249)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(66) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(112) .. (item.Subtitle or S(259))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(175))
        footerLabel.Text = i18n.T(S(125))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(8)] = function()
return function(import)
local RunService = game:GetService(S(126))
local getCurrentPlot = import(S(141))
local getPlotParts = import(S(13))
local log = import(S(90))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(113), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(87))
        return
    end
    log(S(231))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(6)] = function()
return function(import)
local RunService = game:GetService(S(89))
local getCurrentPlot = import(S(73))
local getPlotParts = import(S(229))
local log = import(S(26))
local FIRE_INTERVAL = 1
local autoOpenChest = {}
local connection = nil
function autoOpenChest.SetEnabled(enabled)
    if enabled == autoOpenChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(201))
        return
    end
    log(S(60))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Chest then
            return
        end
        local prompt = parts.Chest:FindFirstChildWhichIsA(S(96), true)
        if prompt then
            fireproximityprompt(prompt)
        end
    end)
end
function autoOpenChest.IsEnabled()
    return connection ~= nil
end
return autoOpenChest
end
end
modules[S(277)] = function()
return function(import)
local Players = game:GetService(S(216))
local RunService = game:GetService(S(284))
local getCurrentPlot = import(S(238))
local getPlotParts = import(S(46))
local log = import(S(283))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(171))
    return pad and pad:FindFirstChildWhichIsA(S(52), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(77)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(29))
        return
    end
    log(S(111))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(100))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(25))
        local tool = backpack:FindFirstChildWhichIsA(S(118))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(145) .. tool.Name .. S(268) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(242)] = function()
return function(import)
local Players = game:GetService(S(181))
local RunService = game:GetService(S(103))
local log = import(S(14))
local FIRE_INTERVAL = 0.5
local function findButtonByText(instance, text)
    for _, descendant in ipairs(instance:GetDescendants()) do
        if descendant:IsA(S(191)) and descendant.Visible then
            if descendant.Text:lower():find(text, 1, true) then
                return descendant
            end
        end
    end
    return nil
end
local function clickButton(button)
    if not firesignal then
        return false, S(69)
    end
    pcall(firesignal, button.MouseButton1Click)
    pcall(firesignal, button.Activated)
    return true
end
local autoTakeChest = {}
local connection = nil
function autoTakeChest.SetEnabled(enabled)
    if enabled == autoTakeChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(47))
        return
    end
    log(S(45))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local playerGui = Players.LocalPlayer:FindFirstChild(S(222))
        if not playerGui then
            return
        end
        local button = findButtonByText(playerGui, S(108))
        if not button then
            return
        end
        local ok, err = clickButton(button)
        if ok then
            log(S(22))
        else
            log(S(67) .. tostring(err))
        end
    end)
end
function autoTakeChest.IsEnabled()
    return connection ~= nil
end
return autoTakeChest
end
end
modules[S(267)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(172)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(18) .. child.Name .. S(19)
        if child:IsA(S(213)) then
            line = line .. string.format(
                S(61),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(185)) then
            line = line .. string.format(S(54), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(134)) or child:IsA(S(33)) then
            line = line .. S(135)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(137) .. attrName .. S(177) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(65))
    end
end
return dumpInstance
end
modules[S(203)] = function()
local Players = game:GetService(S(86))
local Workspace = game:GetService(S(204))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(235))
    local plots = mineWorld and mineWorld:FindFirstChild(S(200))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(183)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(51)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(276)),
        Nodes = plot:FindFirstChild(S(188)),
        Chest = plot:FindFirstChild(S(274)),
        Zones = plot:FindFirstChild(S(78)),
        Crew = plot:FindFirstChild(S(62)),
        Loot = plot:FindFirstChild(S(254)),
        PlotIndex = plot:GetAttribute(S(257)),
        ChestCapacity = plot:GetAttribute(S(260)),
    }
end
return getPlotParts
end
modules[S(232)] = function()
return function(import)
    local Players = game:GetService(S(115))
    local createWindow = import(S(35))(import)
    local i18n = import(S(114))
    local antiAfk = import(S(79))(import)
    local interactionLogger = import(S(105))(import)
    local log = import(S(154))
    local window = createWindow({
        Subtitle = S(120),
    })
    local getCurrentPlot = import(S(3))
    local getPlotParts = import(S(262))
    local dumpInstance = import(S(202))
    local autoMine = import(S(70))(import)
    local autoPlace = import(S(205))(import)
    local autoTakeChest = import(S(21))(import)
    local autoOpenChest = import(S(261))(import)
    local farmTab = window:AddTab(S(220), S(161))
    farmTab:AddCard(
        S(23),
        S(164)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(190),
        S(49)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(102),
        S(56)
    ):AddToggle(false, function(state)
        autoOpenChest.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(130), S(160))
    local checkBtn
    checkBtn = debugCard:AddButton(S(189), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(30)
            log(S(72))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(76),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(173),
        S(31)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(64), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(256)
            return
        end
        local ok, err = log(S(16))
        if not ok then
            dumpBtn.Text = S(184)
            dumpCard:SetSubtitle(S(85) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(226) .. firstNode.Name .. S(278))
            dumpInstance(firstNode, log)
        else
            log(S(272))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(218) .. firstHolder.Name .. S(194))
            dumpInstance(firstHolder, log)
        else
            log(S(264))
        end
        dumpBtn.Text = S(132)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(10),
        S(243)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(212), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(80))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(43))
        if not tool then
            toolDumpBtn.Text = S(281)
            return
        end
        log(S(207) .. tool.Name .. S(234))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(117) .. attrName .. S(225) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(124)
    end)
    local settingsTab = window:AddTab(S(110), S(136))
    settingsTab:AddCard(S(15), S(233)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(27),
        S(248)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(186), S(209), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(158), S(143), S(133) }, 1, function(index, value)
        local code = ({ S(230), S(285), S(247) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoTakeChest.SetEnabled(false)
        autoOpenChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
    end)
end
end
modules[S(221)] = function()
return {
    [79389059854988] = S(131),
}
end
import(S(82))(import)
