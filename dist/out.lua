-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"Players", "Dump Visible GUI", "Frame", "core/debug_dump_remotes", "แพงสุดก่อน", "auto", " (chest holds ", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "BasePart", "leaderstats", ": ", "slot not in the hotbar", "Players", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", ") ", "AutoUpgrade", "Dump All Remotes", "GetHolderMineSpeedLevel", "core/get_place_id", " x", " items)", "M", "[AutoPlot] ", "Auto Upgrade: กันเงินไว้", "M", "games/my_anime_mine/game_api", "[SpendPolicy] ", "[Config] couldn't read ", "", "autoPlaceUpgradeWeak", "games/my_anime_mine/get_current_plot", " stack(s)", "[SpendPolicy] ", "dearest", "languageIndex", "locked slot", ")", "ฟาร์ม", "Modules", "badge says ", "off", "ค้นหาฟังก์ชัน...", "[GuiDump] PlayerGui not found", ")", "MineBuyPickaxe", " mutation x", "[AutoClaim] idle: ", "autoPlotLatest", ")", "HumanoidRootPart", "[MekPro] settings file: ", "ตามเครื่อง", "costs ", "[GameAPI] SendEvent failed: ", "core/config", "core/dispatch", "Players", " refused - ", "RemoteFunction", "ต่ำสุดก่อน", "dearest", "AutoResearch", "[AutoResearch] idle: ", " while selecting", "'", " screens", "core/teleport", " ", "", "Auto Mine: ความเร็ว", "core/logger", "core/humanoid", "enabled", "/s", "'", "[Decompile] wrote ", ",", "This is not a supported place.", " '", " owned)", "games/my_anime_mine/get_current_plot", "autoMineSpeed", "เดินทะลุกำแพงและพื้น", "Auto Plot: ซื้อโซนถัดไป", "ต่างคนต่างซื้อ", "autoSell", "[AutoPlot] ", "TextButton", "[GuiDump] === end (", "", "autoUpgradeLimitPickaxes", "search_placeholder", "autoUpgradeEquipPickaxe", "Frame", "games/my_anime_mine/game_api", "games/my_anime_mine/auto_place", "games/my_anime_mine/auto_place", "Workspace", "empty", "", ") ", "games/my_anime_mine/auto_mine", "cheapest open node ", " (power ", "TextLabel", "core/i18n", "core/logger", "TextButton", "%.3g", "Anti-AFK", "TextLabel", "-", "x", "[AutoClaim] ", "debug.getupvalues", "s", "(", "upgrade", "Damage ก่อน", " ชิ้น", "Auto Place Miner", "MineSpeed", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[AutoResearch] ", "[AutoPlace] couldn't pick up ", " waited ", "autoResearchCore", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "[AutoChest] chest is empty", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "games/my_anime_mine/game_api", "BodyPosition", "games/my_anime_mine/auto_research", "GetCharacterMineSpeedUpgradeCost", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", " '", "core/spend_policy", "AutoPlace", "🏠", "TextLabel", "[AutoChest] disabled", "AutoPlot", "autoChestMinItems", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "TextLabel", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "Exclusive", ") didn't take: ", "VirtualInputManager", "CoreUI", "TextLabel", "^%$", "[^%.]+", "core/click", "TextLabel", "core/get_me", "Badge", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "core/logger", "core/debug_interaction_logger", "games/my_anime_mine/auto_plot", "Auto Research: ซื้อสาย Core", "autoUpgradeMutationBudget", "x", "autoResearchZones", ")", "[AutoPlace] disabled", "Run", "ถูกสุดก่อน", "TextButton", "Damage", "[AutoResearch] cheapest open node: ", "games/my_anime_mine/auto_sell", "affordable - waiting for a turn", "server didn't equip it", " (still ", "MineWorld", "Auto Claim: เก็บสูงสุดต่อรอบ", "walkSpeed", "\n", "Players", "Auto Place: เปลี่ยนตัวอ่อนออก", "games/my_anime_mine/main", "PlayerGui", "autoMine", " error: ", ")", "games/my_anime_mine/get_best_miner", "", "search_placeholder", "table", "dev", "core/ui", "autoUpgradeRollMutation", " -> x", "ChestCapacity", "games/my_anime_mine/auto_mine", "TextLabel", ")", "disabled", "fly", "TextLabel", "Pad", "UserInputService", "core/debug_decompile", "autoUpgradeMaxPerPass", "autoResearchMaxPerPass", "Players", "hookfunction", "JumpPower (ปกติ 50)", "core/anti_afk", "[AutoSell] enabled", "function", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "core/parse_money", "autoChest", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "MekPro.log", "getconnections", "[Scheduler] ", "HumanoidRootPart", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "M", " (", "core/run_job", "core/parse_money", "TextLabel", "ย่อขยายเมนูทั้งชุด", "table", "button has no size on screen", "[Interaction] Key pressed: %s (gameProcessed=%s)", "ถูกสุดก่อน", " (Text=", "spendOrder", "dearest", "en", "Limited", "core/run_job", "games/my_anime_mine/auto_upgrade", "⚙", "[AutoSell] disabled", "unlock", "Text", "Speed", "games/my_anime_mine/game_api", "", "autoClaim", "games/my_anime_mine/game_api", "autoPlotBuy", "AutoSell", "แรงกระโดด", "ยิง Equip Best ทุกกี่วินาที", "[AutoPlace] unlocked ", "Auto Upgrade: สุ่ม Mutation", " (Text=", "no", "TextLabel", "[Interaction] ProximityPrompt triggered: ", "action '", "", " screen(s)) ===", "Players", "", "/", "autoUpgradePickaxeIndex", "UIStroke", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "", "core/logger", "[AutoUpgrade] pickaxe #", "Chest", "string", "autoClaimMaxPerPass", "core/logger", "Frame", "MineEquipBest", "TextLabel", "cheapest", "[Scheduler] turn: ", "core/logger", "[AutoClaim] ", "GetCharacterSpeedUpgradeCost", "Players", "Players", "[Config] couldn't write ", "off - every automation spends on its own", "s", "GetHolderSpeedLevel", " files", "AutoPlace", "Auto Chest", "games/registry", " ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[AutoResearch] ", "off", "Container", "CoreGui", "AutoPlot", "en-us", "table", "core/humanoid", "Crew", "UIListLayout", "research", "autoSellMaxWait", " - upgrade the miners first", "getgc", " ===", "Auto Claim: รอบเช็ค", " (", "%$", "0 = ไม่จำกัด", "core/run_job", "อ่านป้ายบน chest ทุกกี่วินาที", "autoPlotKeepMoney", "ความเร็ววิ่ง", "Dump", "Auto Claim (Index)", "ModuleScript", ") gave nothing", "/", " remotes", "games/my_anime_mine/auto_sell", "ขายทุกกี่วินาที", "", "MekPro/decompiled", "autoResearchCheapestFirst", "Frame", "Auto Sell: รอคิวได้นานสุด", "MekPro", "", "[AutoChest] can't read the chest yet", "", "AutoUpgrade", "core/logger", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "LayerCollector", "Zone buying is off", "modules not loaded", "debug.getproto", "Auto Chest: รอบเช็ค", "Header", "games/my_anime_mine/auto_plot", "ClickDetector", "neither appendfile nor writefile is available", " but no unclaimed reward in its Track", ")", "getscripts", "Content", "UICorner", "core/teleport", "may need us at the stand - trying that", "nothing left to buy (", "[BestMiner] slot ", "[AutoUpgrade] ", " failed)", "WalkSpeed (ปกติ 16)", "core/logger", "TextButton", "[Interaction] ClickDetector clicked: ", "[Interaction] logger disabled", "UserInputService", "')", "[Bb]uy", "slot ", "%$", "games/my_anime_mine/game_api", "MineGui", "Players", ": ", "Open menu", "UIListLayout", "MineEquipPickaxe", "Workspace", ")", "autoSellProtectUnits", " -> ", "GuiService", "[AutoPlace] picked up ", "prompt disabled", "ValueLabel", " = ", "AutoPlot", "Hauler", "/config.json", "[Decompile] PlayerScripts not found", "X", " waited ", "OwnerUserId", "autoResearchKeepMoney", "interactionLogger", " (x", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "ไล่ซื้อทุกกี่วินาที", "Root", "[AutoUpgrade] every stat on every miner is maxed", "autoPlaceUnlockBudget", "Auto Sell: รอบขาย", ", you have ", "0 = ไม่จำกัด", "Auto Research: กันเงินไว้", "menuScale", "GetCharacterUpgradeCost", "getrenv", "unlock", "table", "TextButton", "[AutoResearch] ", ")", "[Interaction] logger enabled - go click/interact with things now", "Search", "=locked(", "Auto Upgrade Plot (Zone)", "ProximityPrompt", "[AutoPlace] Equip Best: hotbar's strongest ", "ClaimableTiers failed", "Auto Place: รอบดึงตัวเก่ง", "Auto Upgrade: Pickaxe ที่จะซื้อ", "x", "Auto Research (Plot)", "[^%w]+", "Search functions...", "%", "Players", "autoMineClicks", "Zones", "AutoPlot", "autoUpgradePriority", " (", "GuiService", "[GameAPI] couldn't load the game's modules: ", "[AutoPlot] couldn't switch to ", ")", "[AutoUpgrade] bought pickaxe #", "core/debug_dump_gui", "Frame", "ปุ่มเปิดเมนู", "", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/logger", "getcallbackvalue", "Frame", " costs ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "TextButton", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "%.3g", " (", "  [attr] ", " (power ", "languageIndex", ")", "AutoPlace", " claimable but none could be resolved", "table", "[AutoPlace] ", "ReplicatedStorage", "ItemName", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", ")", "Content", "AutoPlace", "string", "ScrollingFrame", "MineUpgradeCharacter", "core/debug_tab", "lowest", "^M(%d+)$", " ", "[AutoPlace] ", ") ", "games/my_anime_mine/game_api", "antiAfk", "games/my_anime_mine/auto_chest", "Hauler", "", "autoUpgradePickaxe", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "GuiButton", "off", "MinePower", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "Auto Research: ซื้อสูงสุดต่อรอบ", " (", "index rewards are disabled in this game build", "[AutoPlace] fireproximityprompt error: ", "%.3g", "core/config", "=", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "%$%s*([%d,%.]+)%s*(%a*)", "Interaction Logger", "ไทย", "/", "Speed ก่อน", "", "[AntiAFK] idle detected, simulating input", "MineBuySkill", "core/spend_policy", "🐛", "PlayerGui", "อังกฤษ", "games/my_anime_mine/get_plot_parts", " for ", "/", "table", "[AntiAFK] enabled - watching Player.Idled", "ValueLabel", "autoPlaceUnlock", "มุดแมพ (Noclip)", " (prompt says '", "[", "core/logger", "%.0f", "[Decompile] ", "core/scheduler", ")", "=empty", ".lua", "function", "yes", " didn't go through (out of stock or too expensive)", "BasePart", "table", " (ActionText=", "games/my_anime_mine/auto_upgrade", "core/dispatch", "MekPro", "Mutation: เพดานราคาต่อครั้ง", "%.0f", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "core/logger", ", have ", "Frame", "core/logger", ": ", "", "buying the ", "HttpService", "maxed", "Humanoid", "[MekPro] build ", "#", "games/my_anime_mine/auto_claim", "AutoChest", "games/my_anime_mine/get_plot_parts", " for ", "[AutoPlace] holders: ", "autoResearchInterval", "M", "บิน (Fly)", "TextLabel", "disabled", "[AutoResearch] ", " -> ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "jumpPower", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "Badge", "core/get_place_id", ",", "AutoPlace", "TextButton", " (", "All", "core/debug_decompile", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", ": badge says ", ": State(", "write reported success but the line isn't in ", "open_menu_hint", "UIListLayout", "MineRollMutation", "Auto Place: รอบเช็ค", "core/logger", "string", "[AutoPlace] placed ", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "TextLabel", "TextLabel", "TextButton", "  ", "AutoMine", "cost", "core/spend_policy", "PlayerGui", "Damage", "[AutoUpgrade] ", "core/ui", "AutoUpgrade", "ZoneRow", "AutoPlot", "VirtualUser", ")", "[AutoPlot] unlocked ", " (", "no isfile/readfile available to verify the write", "_", "Text", "empty", " (money ", "en", "Main", "GuiObject", "AutoResearch", " = ", "auto", "[AutoResearch] Core ", "autoSellMaxWait", ": ", "float", "Auto Chest: เกณฑ์เก็บ", " still empty after placing ", "; ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "s", ": ", "menuScale", "core/run_job", "PlotIndex", "Holders", "every open node is maxed", " (cost ", ", base ", "'", "[Logger] failed to write ", "[AntiAFK] disabled", "/", "autoUpgradeMutationTarget", "unreadable", " ", "games/my_anime_mine/auto_research", "[AutoSell] MineSellItems ", "Workspace", "[AutoUpgrade] ", "Frame", "open_menu_hint", "%.3g", "s behind a purchase that never happened - letting it spend", "MekPro", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "/", "disabled", "Damage", " (", "ClickDetector", "cheapest", "core/run_job", "s", "nothing claimable", "ProximityPrompt", "/", "Auto Place: เพดานราคาปลดช่อง", "Content", "[AutoPlace] drifted out of range of ", "autoPlotInterval", " ", "[AutoPlace] couldn't hold anything - not pressing E", "(nothing to do)", "core/logger", "PlayerScripts", "?", "", "[Decompile] === executor capabilities ===", "disabled", "Character", "fill", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", " ", ") ", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", " - ", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "Auto Mine: คลิกต่อรอบ", "", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoPlace] slot ", "both Zone and Core buying are switched off", ") from ", "", "core/get_place_id", "Auto Plot: กันเงินไว้", "[Decompile] === end ===", "th", "Workspace", "core/logger", " pickaxe(s) for ", " costs ", "ตั้งค่า", "core/run_job", "RunService", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " reward(s) looked claimable but none went through", "slot past the usable cap", " ", "[Decompile] === start ===", "s", "[Remotes] === total: ", "Auto Upgrade: แจก Pickaxe ให้ miner", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "core/run_job", "works from anywhere - no teleport needed", " doesn't exist", "s - carrying on without it", "AutoUpgrade", "ClickDetector", "BasePart", "^([%d,]+)$", "core/i18n", "table", "[Decompile] decompile unavailable in this executor", "TextBox", "needs ", "[AutoUpgrade] MineBuyPickaxe ", " still locked after paying ", "BodyVelocity", "[AutoPlace] unlocking ", "([%d,%.]+)%s*(%a*)", "string", "x", "table", " not bought yet (", "ProximityPrompt", "Workspace", "table", "Auto Research: ซื้อสาย Zone", "Empty miner slot", "disabled", "core/debug_interaction_logger", "ProximityPrompt", "", "getscriptbytecode", " has been busy for ", "x", ",", ", built ", "function", "nothing to place or upgrade", "] job failed: ", "AutoResearch", " (", "[Uu]nlock", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "Workspace", "Core buying is off", ", ", "core/teleport", "no mine data", " possible holder(s) - not buying more", "", "OwnerUserId", "[AutoChest] MineTakeChest had no effect (", "Core", "autoPlaceInterval", " #", " (", "^x(%d+)$", "s - jumping the queue", " (", "autoUpgradePickaxeIndex", " team power", " pickaxe ", "cost", "Dump", "games/my_anime_mine/auto_chest", "Players", "auto", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "dearest", "", "core/get_leaderstat", "[AutoUpgrade] ", " power ", "[AutoPlot] ", ") on ", "getloadedmodules", "core/build_env", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "games/my_anime_mine/main", "table", ") -> ", " wouldn't take ", "AutoPlace", "table", "Plots", "core/logger", "AutoClaim", "My Anime Mine · Mining Hub", "HumanoidRootPart", "core/scheduler", " team power, you have ", "core/logger", "games/my_anime_mine/get_plot_parts", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "table", "enabled", "เช็คโซนทุกกี่วินาที", "AutoUpgrade", "UserInputService", "Auto Mine", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "Speed", "ขนาดเมนู", "%.0f", "core/spend_policy", "Frame", " | ", "number", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "waiting", "getsenv", " -> ", "RemoteEvent", "Auto Plot: ใช้โซนล่าสุดเสมอ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "MineWorld", "MineTakeChest", "  ", " -> ", "[AutoChest] waiting: ", " items (now ", "games/my_anime_mine/get_best_miner", "[AutoPlace] nothing in the hotbar to place", "s", "autoPlaceEquipBestInterval", ")", " from ", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "string", "[AutoPlot] now mining ", "[AutoPlace] ", "ItemFrame", "Auto Upgrade Unit", ")", "waiting", " ", "BasePart", "core/logger", "Auto Research: รอบเช็ค", "ดีบัก", "table", "games/my_anime_mine/auto_claim", "autoSellInterval", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "core/teleport", "decompile", "autoPlot", "GetHolderLevel", "CharMaxLevel", "string", "dearest", "[AutoChest] collected ", "Shop Stand", "table", "ZoneBoardGui", "autoClaimInterval", "core/build_env", "UICorner", "off", " -> ", "AutoResearch", " ", "Players", "MineSelectZone", "needs ", " ", "core/run_job", "s", "1597966+", "MineSpeed", ")", "ZoneBoard", "Mining", "ลำดับการใช้เงินรวมทุกระบบ", "[AutoChest] enabled (collects once >= ", "autoResearchCoreLast", "core/i18n", "Loot", "games/my_anime_mine/game_api", "games/my_anime_mine/game_api", "[Remotes] ", "UIScale", "AutoPlace", "[Scheduler] ", ") needs ", ")", "/", "fill", "autoUpgradeInterval", "none", "core/debug_dump_gui", "AutoSell", "games/registry", "autoPlace", "core/parse_money", "Mutation: หยุดเมื่อได้ x", "", "th", "AutoChest", " node(s) looked buyable but the server refused", "%.3g", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Frame", "s", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "core/logger", "Players", "AutoClaim", "TextButton", "table", "autoUpgradeKeepMoney", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "ScreenGui", "Humanoid", "getnilinstances", " (x", "core/spend_policy", "SellStand", "core/anti_afk", "research", "works from anywhere - dropping the teleport", "[AutoUpgrade] ", "M", "[Scheduler] ", "Auto Plot: รอบเช็ค", "CharSpeedMaxLevel", "enabled", "Plots", "dearest", "TextLabel", "Mining ก่อน", " (", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "table", "UIPadding", "th", "Auto Place: ปลดล็อกช่อง miner", "Auto Upgrade: รอบเช็ค", "", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "core/debug_dump_remotes", "autoChestInterval", "แพงสุดก่อน", "th", "TabBar", "2026-09-21 13:01:32", "enabled", "[GuiDump] === visible PlayerGui tree ===", "needs us at the stand - keeping the teleport", "[AutoClaim] claimed ", "noclip", "RunService", "Auto Sell", " ", ": ", "\n", "PickaxesStand", "[AutoUpgrade] ", "Locked miner slot", "core/spend_policy", " -> ", "enabled", "autoUpgrade", "[GuiDump] ", "GuiButton", "s", "ReplicatedStorage", "Nodes", " script(s) to ", "core/debug_tab", "", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "", "sale didn't go through", "core/logger", "[AutoSell] ", "[AutoResearch] nothing open and affordable right now", " research is ", "LocalScript", "TextLabel", "MineEquipSlot", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Backpack", " thing available first", "unreadable", " items", "auto", "[AutoMine] ", "Frame", "core/logger", "games/my_anime_mine/game_api", "List", "autoPlaceEquipBest", "sold ", "HotbarFrame", "games/my_anime_mine/get_current_plot", "MekPro", "[Interaction] Button activated: ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "ลอย (Float)", "lowest", "MineSellItems", "[AutoPlace] upgrading ", "CharMineSpeedMaxLevel", "autoResearch", "MineSpeed", "", "TextLabel", "write reported success but ", "[%X] ", "Decompile Client Scripts", "debug.getconstants", "en", "none"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(922)] = function()
return function(import)
local log = import(S(281))
local Players = game:GetService(S(865))
local VirtualUser = game:GetService(S(595))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(509))
        connection = player.Idled:Connect(function()
            log(S(499))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(629))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(859)] = function()
return { Env = S(193), IsDev = true, Commit = S(871), BuiltAt = S(949) }
end
modules[S(155)] = function()
local VirtualInputManager = game:GetService(S(150))
local GuiService = game:GetService(S(380))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(231)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(490)] = function()
return function(import)
local HttpService = game:GetService(S(541))
local getPlaceId = import(S(562))
local log = import(S(577))
local ROOT_FOLDER = S(1000)
local placeFolder = ROOT_FOLDER .. S(630) .. tostring(getPlaceId())
local filePath = placeFolder .. S(387)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(302) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(28) .. filePath .. S(958) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(286) .. filePath .. S(372) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(568)] = function()
return function(import)
local Players = game:GetService(S(768))
local log = import(S(160))
local CAPABILITIES = {
    S(848), S(734), S(350), S(309), S(778),
    S(220), S(443), S(813), S(406), S(1015),
    S(115), S(342), S(210), S(918),
}
local OUT_FOLDER = S(328)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(154)) do
        if type(target) ~= S(192) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(666))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(517) .. name .. S(608) .. ((ok and type(value) == S(739)) and S(523) or S(257)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(713))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(663))
    if not playerScripts then
        log(S(388))
        return 0
    end
    pcall(makefolder, S(332))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(982)) or target:IsA(S(321)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(273) and #source > 0 then
                local name = target:GetFullName():gsub(S(423), S(600))
                if pcall(writefile, OUT_FOLDER .. S(323) .. name .. S(521), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(76) .. written .. S(972) .. OUT_FOLDER
        .. S(598) .. failed .. S(358))
    return written
end
local function run()
    log(S(698))
    reportCapabilities()
    local count = dumpAll()
    log(S(685))
    return count
end
return run
end
end
modules[S(893)] = function()
return function(import)
local Players = game:GetService(S(13))
local log = import(S(978))
local function dumpVisible(root, indent)
    indent = indent or S(90)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(606)) and child.Visible then
            local line = indent .. child.ClassName .. S(136) .. child.Name .. S(65)
            if child:IsA(S(228)) or child:IsA(S(108)) then
                line = line .. S(234) .. tostring(child.Text) .. S(435)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(451) .. attrName .. S(384) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(820))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(185))
    if not playerGui then
        log(S(43))
        return 0
    end
    local screens = 0
    log(S(951))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(339)) and screen.Enabled and screen.Name ~= S(530) then
            screens = screens + 1
            log(S(967) .. screen.ClassName .. S(79) .. screen.Name .. S(627))
            dumpVisible(screen, S(584))
        end
    end
    log(S(89) .. screens .. S(262))
    return screens
end
return dumpGui
end
end
modules[S(944)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(970))
local log = import(S(537))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(815)) or descendant:IsA(S(59)) then
            count = count + 1
            log(S(883) .. descendant.ClassName .. S(538) .. descendant:GetFullName())
        end
    end
    log(S(700) .. count .. S(310))
    return count
end
return dumpRemotes
end
end
modules[S(161)] = function()
return function(import)
local Players = game:GetService(S(1))
local Workspace = game:GetService(S(746))
local UserInputService = game:GetService(S(205))
local log = import(S(688))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(259) .. describe(prompt)
            .. S(527) .. prompt.ActionText .. S(462))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(362) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(1001) .. describe(button)
            .. S(256) .. tostring(button.Text) .. S(200))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(732)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(648)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(481)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(363))
        return
    end
    connections = {}
    log(S(412))
    local playerGui = Players.LocalPlayer:WaitForChild(S(588))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(653)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(708)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(968)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(232),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(973)] = function()
return function(import)
local interactionLogger = import(S(731))(import)
local dumpRemotes = import(S(4))(import)
local dumpGui = import(S(437))(import)
local decompileScripts = import(S(206))(import)
local humanoid = import(S(303))
local function build(window, persist)
    local tab = window:AddTab(S(842), S(502))
    persist.Toggle(
        tab:AddCard(
            S(494),
            S(569)
        ),
        S(393),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(17),
        S(673)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(319), function()
        remoteBtn.Text = dumpRemotes() .. S(324)
    end)
    local guiCard = tab:AddCard(
        S(2),
        S(915)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(766), function()
        guiBtn.Text = dumpGui() .. S(66)
    end)
    local decompileCard = tab:AddCard(
        S(1014),
        S(558)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(169), function()
        decompileBtn.Text = decompileScripts() .. S(290)
    end)
    humanoid.Start()
    persist.Slider(tab, S(318), S(359),
        8, 200, S(180), 16, humanoid.SetWalkSpeed, S(974))
    persist.Slider(tab, S(252), S(211),
        30, 300, S(559), 50, humanoid.SetJumpPower, S(327))
    persist.Toggle(
        tab:AddCard(S(553), S(770)),
        S(202), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(512), S(83)),
        S(954), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(1003), S(8)),
        S(613), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(56)] = function()
return function(import)
    local getPlaceId = import(S(19))
    local registry = import(S(895))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(78))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(773)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(10))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(157)] = function()
local Players = game:GetService(S(57))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(543))
    local backpack = player:WaitForChild(S(986))
    return player, humanoid, backpack
end
return getMe
end
modules[S(683)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(72)] = function()
local Players = game:GetService(S(284))
local RunService = game:GetService(S(693))
local UserInputService = game:GetService(S(801))
local Workspace = game:GetService(S(636))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(917))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(791))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(718))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(525)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(9)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(132))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(106)] = function()
local Players = game:GetService(S(209))
local STRINGS = {
    en = {
        search_placeholder = S(424),
        open_menu_hint = S(373),
    },
    th = {
        search_placeholder = S(42),
        open_menu_hint = S(439),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(301)
    if locale:sub(1, 2):lower() == S(947) then
        return S(939)
    end
    return S(1016)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(6) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(534)] = function()
local LOG_FILE = S(219)
local function log(message)
    local line = os.date(S(1013)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(190))
            end
            appendfile(LOG_FILE, line .. S(181))
        elseif writefile then
            local existing = S(539)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(959))
        else
            error(S(347))
        end
        if not (isfile and readfile) then
            error(S(599))
        end
        if not isfile(LOG_FILE) then
            error(S(1012) .. LOG_FILE .. S(705))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(572) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(628) .. LOG_FILE .. S(612) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(897)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(721) then
        return nil
    end
    local number, suffix = text:match(S(493))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(77), S(440))))
    if not value then
        return nil
    end
    if suffix ~= S(1010) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(650)] = function()
return function(import)
local log = import(S(909))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(514) .. name .. S(741) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(792)] = function()
return function(import)
local RunService = game:GetService(S(955))
local log = import(S(993))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(886) .. task_.name .. S(187) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(221) .. task_.name .. S(735)
                    .. string.format(S(806), now - task_.busySince)
                    .. S(706))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(927) .. starved.name .. S(126)
            .. string.format(S(532), now - starved.waitingSince)
            .. S(760))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(280) .. (currentTask and currentTask.name or S(661)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(137)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(794))
local policy = {}
local probes = {}
local order = {}
local mode = S(482)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(27) .. (mode == S(297)
        and S(287)
        or (S(540) .. mode .. S(987))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(861) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(279) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(61) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(33) .. name .. S(390)
            .. string.format(S(516), now - blockedSince[name])
            .. S(641))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(67)] = function()
local Players = game:GetService(S(371))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(222))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(194)] = function()
return function(import)
local i18n = import(S(879))
local UserInputService = game:GetService(S(364))
local GuiService = game:GetService(S(432))
local THEME = {
    BrandName = S(642),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(860))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(352))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(938))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(447))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(665)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(267))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(140))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(664)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(105))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(29)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(466))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(374))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(983))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(330))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(111))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(203))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(1011))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(383))
            if not valueLbl then
                valueLbl = Instance.new(S(152))
                valueLbl.Name = S(510)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(561))
            if not badge then
                badge = Instance.new(S(199))
                badge.Name = S(158)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(88))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(438))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(906))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(361))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(247)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(425)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(94))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(3))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(409))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(752)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(536))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(574))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(912))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(244)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(916))
    screenGui.Name = S(151)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(299))
    local main = Instance.new(S(444))
    main.Name = S(605)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(884))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(276))
    tabBar.Name = S(948)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(305))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(638))
    header.Name = S(344)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(565))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(146))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(933))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(261)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(583))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(389)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(171))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(112)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(714))
    searchBox.Name = S(413)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(191))
    searchBox.Text = S(335)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(992))
    content.Name = S(463)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(808))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(258))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(573))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(581))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(601)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(498) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(671) .. (item.Subtitle or S(100))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(92))
        footerLabel.Text = i18n.T(S(639))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(476)] = function()
return function(import)
local gameApi = import(S(882))(import)
local log = import(S(788))
local runGuarded = import(S(621))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(877) .. minItemsToCollect .. S(21))
    else
        log(S(141))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(988) then
            log(S(334))
            lastReported = S(632)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(602) then
            log(S(129))
            lastReported = S(99)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(812) then
            local capacity = gameApi.ChestCapacity()
            log(S(822) .. count .. S(644) .. want .. S(989)
                .. (capacity and (S(7) .. capacity .. S(519)) or S(333)))
            lastReported = S(837)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(901), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(854) .. count .. S(823) .. after .. S(873))
        else
            log(S(754) .. count
                .. S(557) .. tostring(after) .. S(167))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(844)] = function()
return function(import)
local gameApi = import(S(474))(import)
local log = import(S(337))
local runGuarded = import(S(315))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(282) .. (enabled and S(965) or S(555)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(47) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(953) .. reward.category .. S(957)
                .. tostring(reward.tierKey) .. S(757) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(114) .. #pending .. S(695))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(911), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(198)] = function()
return function(import)
local getCurrentPlot = import(S(81))
local getPlotParts = import(S(548))
local log = import(S(662))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(346), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(991) .. (enabled and S(798) or S(730)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(96)] = function()
return function(import)
local Players = game:GetService(S(182))
local getCurrentPlot = import(S(31))
local getPlotParts = import(S(795))
local getMinerSlots = import(S(824))(import)
local teleport = import(S(847))
local parseMoney = import(S(227))
local gameApi = import(S(246))(import)
local log = import(S(840))
local runGuarded = import(S(692))(import)
local spendPolicy = import(S(807))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(729)
local LOCKED_SLOT_TEXT = S(962)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(204))
    return pad and pad:FindFirstChildWhichIsA(S(725), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(554)) then
            local digits = descendant.Text:match(S(710))
            if digits then
                return tonumber((digits:gsub(S(563), S(69))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(278)) and descendant.Text:match(S(153)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(367) .. holder.Name .. S(724)
            .. gameApi.MinerSlots() .. S(265) .. gameApi.MaxMinerSlots() .. S(80)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(36)
    end
    if not prompt.Enabled then
        return true, S(382)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(53) .. price
    end
    local action = prompt.ActionText or S(976)
    if action:match(S(744)) or action:match(S(366)) or action:match(S(368)) then
        return true, S(260) .. action .. S(75)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(50))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(488) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(414) .. why .. S(836))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(520))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(491) .. prompt.ObjectText
                    .. S(117) .. shown .. S(626) .. math.floor(base) .. S(888))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(809))
    if line ~= lastSurvey then
        log(S(550) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(890), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(118), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(455)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(243), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(458) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(168))
    else
        lastAttempt = 0
        log(S(617))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(675),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(12)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(696)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(176)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(825))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(657) .. holder.Name .. S(64))
            return false
        end
        log(S(679) .. candidate.slot .. S(758) .. candidate.name
            .. S(149) .. tostring(why))
    end
    if not miner then
        log(S(660))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(833) .. holder.Name .. S(615) .. miner.name)
        return false
    end
    log(S(579) .. miner.name .. S(452) .. miner.power
        .. S(777) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(125) .. entry.name .. S(829) .. entry.holder.Name
            .. S(513) .. tostring(entry.prompt.ActionText) .. S(365))
        return false
    end
    log(S(381) .. entry.name .. S(104) .. entry.power
        .. S(681) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(472) .. pad.holder.Name .. S(717)
                .. tostring(pad.priceText))
        else
            log(S(254) .. pad.holder.Name
                .. S(549) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(417)
            .. string.format(S(449), bestBefore) .. S(379)
            .. string.format(S(109), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(464), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(740))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(669) then
        runGuarded(S(138), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(407) then
        log(S(719) .. target.holder.Name
            .. S(506) .. tostring(target.priceText))
        runGuarded(S(564), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(1006) .. target.holder.Name .. S(11) .. target.name
        .. S(312) .. target.power .. S(783) .. best.name .. S(761) .. best.power .. S(349))
    runGuarded(S(291), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(345)] = function()
return function(import)
local gameApi = import(S(369))(import)
local parseMoney = import(S(216))
local log = import(S(71))
local runGuarded = import(S(226))(import)
local spendPolicy = import(S(963))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(23) .. (enabled and S(950) or S(667)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(313)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(832) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(486) .. deepestKey .. S(596))
        return true
    end
    log(S(434) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(385)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(776) .. gameApi.ZoneDisplayName(zoneKey) .. S(445)
                .. math.floor(price) .. S(401) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(597) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(743) .. zoneKey .. S(44))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(923) then
            reportedLocked = S(306)
            log(S(87) .. zoneKey .. S(58)
                .. gameApi.ZoneDisplayName(currentKey) .. S(981)
                .. done .. S(889) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(594)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(429), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(133)] = function()
return function(import)
local gameApi = import(S(994))(import)
local log = import(S(270))
local runGuarded = import(S(703))(import)
local spendPolicy = import(S(587))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(296) .. (enabled and S(930) or S(645)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(556) .. node.zoneKey .. S(68) .. node.id
                    .. S(566) .. node.family .. S(15) .. node.level .. S(821) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(610) .. node.id
                    .. S(431) .. node.family .. S(672) .. node.level .. S(862) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(410) .. blocked
                .. S(902))
        elseif powerBlocked then
            log(S(124) .. powerBlocked.zoneKey .. S(868) .. powerBlocked.id
                .. S(647) .. powerBlocked.family .. S(887)
                .. math.floor(powerBlocked.powerReq) .. S(793)
                .. math.floor(powerBlocked.power)
                .. S(308))
        else
            log(S(980)
                .. S(603) .. math.floor(gameApi.GetMoney()) .. S(49))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(173) .. nearest.zoneKey
                    .. S(697) .. nearest.id .. S(450) .. nearest.family .. S(101)
                    .. nearest.level .. S(507) .. nearest.max
                    .. S(690) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(680)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(755),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(340))
        end
        if not buyCore then
            table.insert(parts, S(747))
        end
        if #parts == 0 then
            return S(624)
        end
        return S(355) .. table.concat(parts, S(748)) .. S(37)
    end
    local why
    if nearest.cost > budget then
        why = S(867) .. string.format(S(489), nearest.cost)
            .. S(535) .. string.format(S(903), money)
    elseif nearest.powerOk == false then
        why = S(715) .. string.format(S(640), nearest.powerReq) .. S(763)
    else
        why = S(175)
    end
    return S(103) .. nearest.zoneKey .. S(864) .. tostring(nearest.id)
        .. S(935) .. tostring(nearest.family) .. S(473) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(63) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(853) and cost > best)
            or (mode ~= S(932) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(607)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(62), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(174)] = function()
return function(import)
local Workspace = game:GetService(S(376))
local gameApi = import(S(95))(import)
local teleport = import(S(353))
local log = import(S(107))
local runGuarded = import(S(239))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(242))
    else
        lastAttempt = 0
        log(S(213))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(921))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(416), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(635)
                .. (sold and S(924)
                         or S(952)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(979) .. (sold and (S(997) .. #keys .. S(32)) or S(977)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(251), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(528)] = function()
return function(import)
local Workspace = game:GetService(S(726))
local gameApi = import(S(249))(import)
local teleport = import(S(749))
local log = import(S(360))
local runGuarded = import(S(869))(import)
local spendPolicy = import(S(920))(import)
local STATS = { S(172), S(804), S(1009) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(469)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(925) .. (enabled and S(73) or S(201)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(408) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(586) or S(542)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(357) .. holderKey .. S(633) .. choice.stat .. S(294)
                        .. choice.info.level .. S(964) .. after.level
                        .. S(654) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(765) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(960))
    local shop = stand and stand:FindFirstChild(S(855))
    if not shop then
        return nil
    end
    if shop:IsA(S(709)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(839), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(716)
                .. (bought and S(704)
                           or S(354)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(904))
        end
    end
    if bought then
        log(S(436) .. pickaxe.index .. S(838) .. pickaxe.name
            .. S(394) .. pickaxe.multiplier .. S(188))
        return true
    end
    log(S(271) .. pickaxe.index .. S(471) .. pickaxe.name
        .. S(524))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(637) .. holderKey .. S(764)
                    .. tostring(worn or S(892)) .. S(814) .. wanted.name)
            else
                log(S(774) .. holderKey .. S(784)
                    .. wanted.name .. S(177) .. tostring(nowWorn or S(1017)) .. S(454))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(590) .. holderKey .. S(46) .. variant
                .. S(196) .. after .. S(625) .. cost .. S(377))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(707))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(961) .. gameApi.OwnedPickaxeCount()
                    .. S(689) .. gameApi.MaxMinerSlots()
                    .. S(751))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(830))
        elseif maxed > 0 then
            log(S(398))
        else
            log(S(780))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(34) and price > best)
            or (mode ~= S(771) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(800))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(592), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(131)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(459))
local Workspace = game:GetService(S(687))
local log = import(S(515))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(39), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(433) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(937))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(727) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(386) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(508) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(856) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(477) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(470))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(54) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(819))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(567), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(782) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(457) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(668) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(1005), keys)
end
function api.EquipSlot(slot)
    return send(S(984), slot == S(733) and S(682) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(45), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(375), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(575), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(277))
end
function api.BuySkill(zoneKey, family)
    return send(S(500), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(797)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(426)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(483))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(214) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(723) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(866), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(818))
    plots = plots and plots:FindFirstChild(S(931))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(285)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(391)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(874))
    local gui = board and board:FindFirstChild(S(857))
    local root = gui and gui:FindFirstChild(S(397))
    local list = root and root:FindFirstChild(S(995))
    local row = list and list:FindFirstChild(S(593) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(582)) and descendant.Text ~= S(478) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(522) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(467), characterKey, stat == S(875) and S(872) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(850), max = S(851), cost = S(405) },
    Speed = { level = S(289), max = S(929), cost = S(283) },
    MineSpeed = { level = S(18), max = S(1007), cost = S(134) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(852) or charName == S(899) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(913) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(810) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(465) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(712) and type(holder.CharName) == S(831) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(238), S(148) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(526) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(341)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(750)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(487)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(786) then
        return {}, S(418)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(652)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(843) or type(state.Track) ~= S(230) then
                table.insert(notes, tabKey .. S(571) .. tostring(category) .. S(322))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(570) .. waiting
                        .. S(348))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(40) .. total .. S(456)
            .. (#notes > 0 and (S(674) .. table.concat(notes, S(616))) or S(772))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(578) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(189)] = function()
return function(import)
local Players = game:GetService(S(263))
local gameApi = import(S(26))(import)
local log = import(S(442))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(720))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(737), S(942))))
    if not value then
        return nil
    end
    if suffix ~= S(677) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(503))
    local mineGui = playerGui and playerGui:FindFirstChild(S(370))
    local hotbar = mineGui and mineGui:FindFirstChild(S(998))
    local content = hotbar and hotbar:FindFirstChild(S(656))
    return content and content:FindFirstChild(S(298))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(156)) then
            local count = descendant.Text:match(S(759))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(834))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(351))
    local nameLabel = content and content:FindFirstChild(S(460))
    local damageLabel = content and content:FindFirstChild(S(589))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(264) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(496) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(356) .. slot .. S(619) .. info.name
                        .. S(775) .. info.power .. S(20) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(999)] = function()
local Players = game:GetService(S(910))
local Workspace = game:GetService(S(98))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(178))
    local plots = mineWorld and mineWorld:FindFirstChild(S(787))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(753)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(505)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(623)),
        Nodes = plot:FindFirstChild(S(971)),
        Chest = plot:FindFirstChild(S(272)),
        Zones = plot:FindFirstChild(S(428)),
        Crew = plot:FindFirstChild(S(304)),
        Loot = plot:FindFirstChild(S(880)),
        PlotIndex = plot:GetAttribute(S(622)),
        ChestCapacity = plot:GetAttribute(S(197)),
    }
end
return getPlotParts
end
modules[S(184)] = function()
return function(import)
    local createWindow = import(S(591))(import)
    local i18n = import(S(711))
    local antiAfk = import(S(212))(import)
    local buildEnv = import(S(779))
    local scheduler = import(S(518))(import)
    local spendPolicy = import(S(501))(import)
    local config = import(S(55))(import)
    local log = import(S(275))
    log(S(544) .. buildEnv.Commit .. S(225) .. buildEnv.Env
        .. S(738) .. buildEnv.BuiltAt .. S(828))
    local window = createWindow({
        Subtitle = S(790),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(102))(import)
    local autoPlace = import(S(97))(import)
    local autoChest = import(S(767))(import)
    local autoSell = import(S(325))(import)
    local autoUpgrade = import(S(240))(import)
    local autoResearch = import(S(634))(import)
    local autoClaim = import(S(546))(import)
    local autoPlot = import(S(162))(import)
    local gameApi = import(S(881))(import)
    scheduler.RegisterStandalone(S(585), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(547), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(785), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(16), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(742), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(789), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(142), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(611), 20)
    scheduler.RegisterExclusive(S(894), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(863), autoResearch.NextPrice)
    spendPolicy.Register(S(336), autoUpgrade.NextPrice)
    spendPolicy.Register(S(300), autoPlot.NextPrice)
    spendPolicy.Register(S(885), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(38), S(139))
    farmTab:AddSectionHeader(S(876))
    persistentSegmented(
        farmTab,
        { S(85), S(233), S(946) },
        { S(41), S(649), S(236) },
        S(235), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(802),
        S(268)
    ), S(186), autoMine.SetEnabled)
    persistentSlider(farmTab, S(70), S(533),
        1, 30, S(82), 5, autoMine.SetCyclesPerSecond, S(74))
    persistentSlider(farmTab, S(676), S(295),
        1, 10, S(427), 1, autoMine.SetClicksPerCycle, S(722))
    persistentToggle(farmTab:AddCard(
        S(121),
        S(441)
    ), S(896), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(576), S(745),
        1, 60, S(756), 3, autoPlace.SetCheckInterval, S(699))
    persistentToggle(
        farmTab:AddCard(S(183),
            S(905)),
        S(30), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(975),
            S(215)),
        S(996), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(419), S(253),
        5, 300, S(827), 60, autoPlace.SetEquipBestInterval, S(907))
    persistentToggle(
        farmTab:AddCard(S(940),
            S(448)),
        S(511), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(655), S(402),
        0, 500, S(399), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(224))
    persistentToggle(farmTab:AddCard(
        S(292),
        S(14)
    ), S(217), autoChest.SetEnabled)
    persistentSlider(farmTab, S(614), S(144),
        50, 3000, S(143), 100, autoChest.SetMinItems, S(120))
    persistentSlider(farmTab, S(343), S(316),
        1, 60, S(945), 3, autoChest.SetCheckInterval, S(870))
    persistentToggle(farmTab:AddCard(
        S(956),
        S(123)
    ), S(86), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(147),
            S(811)),
        S(378), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(400), S(326),
        5, 300, S(845), 20, autoSell.SetCheckInterval, S(288))
    farmTab:AddSlider(S(331),
        S(480),
        5, 120, sellMaxWait, function(value)
            config.Set(S(307), value)
        end, S(826))
    persistentToggle(farmTab:AddCard(
        S(835),
        S(446)
    ), S(966), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(941), S(678),
        5, 300, S(891), 20, autoUpgrade.SetInterval, S(618))
    persistentSlider(farmTab, S(1002), S(702),
        1, 20, S(207), 5, autoUpgrade.SetMaxPerPass, S(165))
    persistentSegmented(
        farmTab,
        { S(60), S(119), S(497), S(934) },
        { S(1004), S(646), S(245), S(122) },
        S(430), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(24), S(135),
        0, 500, S(914), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(22))
    persistentToggle(
        farmTab:AddCard(S(580),
            S(803)),
        S(479), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(919) .. pickaxe.multiplier .. S(411)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(936)
            end
            return S(545) .. index .. S(659) .. (pickaxeLabels[index] or S(817))
        end
        local savedPick = config.Get(S(762), 0)
        pickaxeCard = farmTab:AddSlider(
            S(420),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(266), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(269)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(159),
            S(461)),
        S(91), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(701),
            S(145)),
        S(93), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(255),
            S(223)),
        S(195), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(898), S(484),
        1, 20, S(631), 2, autoUpgrade.SetMutationTarget, S(736))
    persistentSlider(farmTab, S(531), S(314),
        0, 500, S(164), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(552))
    persistentToggle(farmTab:AddCard(
        S(422),
        S(338)
    ), S(1008), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(841), S(396),
        10, 600, S(551), 30, autoResearch.SetCheckInterval, S(116))
    persistentSlider(farmTab, S(485), S(985),
        1, 20, S(208), 5, autoResearch.SetMaxPerPass, S(421))
    persistentSlider(farmTab, S(403), S(943),
        0, 500, S(392), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(25))
    persistentToggle(
        farmTab:AddCard(S(728),
            S(492)),
        S(166), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(163),
            S(846)),
        S(127), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(560),
            S(643)),
        S(878), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(170), S(5) },
        { true, false },
        S(329), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(320),
        S(130)
    ), S(248), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(311), S(908),
        5, 300, S(858), 30, autoClaim.SetCheckInterval, S(969))
    persistentSlider(farmTab, S(179), S(694),
        1, 30, S(274), 10, autoClaim.SetMaxPerPass, S(113))
    persistentToggle(farmTab:AddCard(
        S(415),
        S(218)
    ), S(849), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(928), S(799),
        5, 300, S(658), 30, autoPlot.SetCheckInterval, S(651))
    persistentToggle(
        farmTab:AddCard(S(84),
            S(670)),
        S(250), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(816),
            S(128)),
        S(48), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(684), S(395),
        0, 500, S(317), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(926))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(468))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(691), S(241))
    persistentToggle(
        settingsTab:AddCard(S(110), S(796)),
        S(475),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(620), 100)
    settingsTab:AddSlider(S(805), S(229), 50, 150, savedScale, function(value)
        config.Set(S(404), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(35), 1)
    settingsTab:AddSegmented({ S(52), S(504), S(495) }, savedLanguage, function(index, value)
        local code = ({ S(990), S(604), S(900) })[index]
        config.Set(S(453), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(769), S(237), S(686) })[savedLanguage] or S(609))
    log(S(51) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(293)] = function()
return {
    [79389059854988] = S(781),
}
end
import(S(529))(import)
