-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"waiting", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " ", "autoUpgradeRollMutation", "autoSellInterval", ",", " x", "([%d,%.]+)%s*(%a*)", " node(s) looked buyable but the server refused", "CoreGui", "auto", "VirtualInputManager", " (money ", "autoResearchInterval", "'", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", " - ", "disabled", "TextButton", "TextLabel", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "My Anime Mine · Mining Hub", "core/run_job", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "Workspace", "/", "core/i18n", "getrenv", "Auto Research: ซื้อสาย Core", "[AntiAFK] enabled - watching Player.Idled", "GuiButton", " -> ", "languageIndex", "core/get_me", "antiAfk", "getsenv", "games/my_anime_mine/auto_place", "write reported success but ", "Players", "[AutoResearch] idle: ", "disabled", " screens", "[Config] couldn't write ", "[AutoResearch] ", "[AutoPlace] placed ", "[Interaction] ClickDetector clicked: ", "[MekPro] build ", "PlotIndex", "Auto Research: กันเงินไว้", "core/ui", "GetCharacterMineSpeedUpgradeCost", "[AutoPlace] couldn't pick up ", "Auto Mine: ความเร็ว", "games/my_anime_mine/auto_mine", "[AutoUpgrade] ", "[AutoChest] waiting: ", "ปุ่มเปิดเมนู", "Nodes", "MineWorld", "UIListLayout", "ฟาร์ม", "TextLabel", "CharMaxLevel", "s - jumping the queue", "table", "", "Auto Claim (Index)", "[AutoClaim] claimed ", "Hauler", ") didn't take: ", " from ", "autoChestMinItems", "[AutoUpgrade] ", "Shop Stand", ") ", "button has no size on screen", " pickaxe ", "Damage", "games/my_anime_mine/auto_upgrade", "MekPro/decompiled", "cost", ".lua", "games/registry", "games/my_anime_mine/game_api", "Text", "table", "needs us at the stand - keeping the teleport", ")", "games/my_anime_mine/game_api", "เช็คโซนทุกกี่วินาที", "", ")", "ModuleScript", " ", "[AutoUpgrade] ", "Auto Research: รอบเช็ค", "[AutoSell] ", "PlayerGui", "[Decompile] === end ===", "core/i18n", "dev", "CharSpeedMaxLevel", "core/teleport", "'", "UIStroke", "core/dispatch", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "leaderstats", "needs ", "neither appendfile nor writefile is available", "HumanoidRootPart", "ClickDetector", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "Modules", "UserInputService", " (x", "x", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[AutoResearch] ", "menuScale", " possible holder(s) - not buying more", "GetHolderMineSpeedLevel", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "[Decompile] ", "Damage", "games/my_anime_mine/auto_chest", "autoPlace", "autoUpgradePickaxeIndex", "[AutoResearch] nothing open and affordable right now", "core/logger", "Players", "ดีบัก", "games/my_anime_mine/auto_upgrade", "autoPlot", "s", "List", "", "core/click", "%.3g", " (", "fill", "Frame", "nothing claimable", "BodyVelocity", "HotbarFrame", "(", "TextLabel", "disabled", "PickaxesStand", "x", "core/debug_dump_gui", "] job failed: ", "core/debug_decompile", "autoClaimMaxPerPass", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "TextLabel", "autoResearchCoreLast", "TextButton", "[AutoUpgrade] every stat on every miner is maxed", "research", "Mining", "^([%d,]+)$", "Workspace", "string", "/", "noclip", "ขายทุกกี่วินาที", ")", "ClickDetector", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", " (power ", "Crew", " ชิ้น", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", " waited ", "Open menu", "TextLabel", "Frame", ")", "", "table", "M", "[AutoResearch] ", "open_menu_hint", "All", "%.3g", "Auto Place: รอบเช็ค", "Auto Place: ปลดล็อกช่อง miner", "core/run_job", "Auto Mine", "enabled", "Humanoid", " (", "autoSell", "MineGui", ")", "th", " ", "", "", ", ", "[AutoClaim] ", "ProximityPrompt", "X", "Auto Place Miner", "games/my_anime_mine/game_api", "table", "[AutoChest] can't read the chest yet", "[AutoPlot] ", "float", "[Logger] failed to write ", "Players", "บิน (Fly)", "autoPlaceUpgradeWeak", "/", "autoMineClicks", " ", "games/my_anime_mine/game_api", " (", "core/logger", "ClaimableTiers failed", ": State(", ")", "core/debug_decompile", "Frame", ")", "hookfunction", " = ", "[Decompile] decompile unavailable in this executor", " ", "AutoClaim", " '", "core/run_job", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", ": ", "RunService", " ", "GetHolderLevel", "getscriptbytecode", "core/get_place_id", "TextLabel", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "Damage", "Frame", "BasePart", "AutoResearch", "ZoneRow", "?", "MineEquipSlot", "UIListLayout", "core/logger", "MineTakeChest", "no mine data", "core/logger", "[AutoPlace] couldn't hold anything - not pressing E", "[AutoPlace] disabled", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "s", "LayerCollector", "MineSpeed", "ไล่อัปเกรด miner ทุกกี่วินาที", "[AutoChest] MineTakeChest had no effect (", "Auto Sell: รอคิวได้นานสุด", "AutoPlace", "lowest", ") needs ", "MekPro", " while selecting", "AutoPlace", "Auto Place: เปลี่ยนตัวอ่อนออก", "=locked(", "auto", "[", "ItemName", " -> ", "TabBar", "Search functions...", "slot ", "function", "autoPlaceUnlockBudget", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "games/my_anime_mine/get_plot_parts", " (", "Auto Place: เพดานราคาปลดช่อง", ") from ", "[AutoClaim] ", " (Text=", "core/parse_money", "autoPlotLatest", "[AutoSell] disabled", ")", " for ", "UserInputService", "UIListLayout", " still locked after paying ", "Empty miner slot", "search_placeholder", "Players", "core/get_place_id", "table", "th", "core/run_job", "VirtualUser", "core/humanoid", "autoMineSpeed", "#", "needs ", "Search", "[Interaction] ProximityPrompt triggered: ", " remotes", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", ",", "core/debug_dump_remotes", "Workspace", "core/logger", "sold ", "AutoUpgrade", "2026-09-21 12:42:11", "Dump All Remotes", "[Decompile] PlayerScripts not found", "core/i18n", "autoUpgradeMaxPerPass", "autoClaimInterval", "Hauler", " team power, you have ", "⚙", "autoChestInterval", "RunService", "nothing left to buy (", "games/my_anime_mine/get_plot_parts", "M", "core/debug_tab", "[AutoPlot] ", "[Decompile] === executor capabilities ===", "[AutoClaim] idle: ", ",", ") on ", "Content", "AutoUpgrade", "')", "Speed", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "games/my_anime_mine/auto_plot", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "table", "This is not a supported place.", "ต่ำสุดก่อน", "core/debug_dump_remotes", " team power", "", "core/logger", "Mutation: หยุดเมื่อได้ x", "MekPro", "core/scheduler", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "/", "Auto Research: ซื้อสูงสุดต่อรอบ", "", "string", "^M(%d+)$", " failed)", "autoPlaceUnlock", "HttpService", " ", "PlayerGui", "UIPadding", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "no isfile/readfile available to verify the write", "action '", "Players", ") -> ", "[GuiDump] === end (", "Auto Research (Plot)", "Auto Claim: เก็บสูงสุดต่อรอบ", ")", "Workspace", "[AutoPlot] couldn't switch to ", "[GuiDump] === visible PlayerGui tree ===", "cheapest open node ", "AutoSell", "core/teleport", ")", "maxed", "[AutoChest] chest is empty", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "auto", "table", "[AutoResearch] Core ", "games/my_anime_mine/auto_sell", "core/debug_dump_gui", "enabled", "Frame", "games/my_anime_mine/auto_mine", " (cost ", " (", "core/run_job", "games/my_anime_mine/get_current_plot", "table", "search_placeholder", "[Scheduler] ", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "Chest", "AutoChest", " claimable but none could be resolved", "Auto Upgrade: Pickaxe ที่จะซื้อ", ")", "  [attr] ", " '", "Mining", "ตั้งค่า", "Auto Upgrade: ซื้อ Pickaxe ด้วย", " -> ", "AutoChest", "0 = ไม่จำกัด", " (x", "TextLabel", "ZoneBoardGui", "autoResearchMaxPerPass", "Players", "autoUpgradePriority", "ProximityPrompt", "walkSpeed", "games/my_anime_mine/get_best_miner", "", "", "table", ") ", "", "debug.getproto", "Plots", "en", "TextLabel", "Frame", "getscripts", "SellStand", "AutoPlace", "games/my_anime_mine/main", "[Interaction] Key pressed: %s (gameProcessed=%s)", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "[AutoPlot] ", ")", "UICorner", "Core buying is off", "[AutoPlace] nothing in the hotbar to place", "games/my_anime_mine/auto_place", "getnilinstances", "core/run_job", "TextButton", "UICorner", "Frame", "getloadedmodules", "ZoneBoard", "Exclusive", "/", "autoUpgradeKeepMoney", "enabled", "AutoPlot", "core/scheduler", "string", "OwnerUserId", "LocalScript", "", "core/logger", "Auto Chest: รอบเช็ค", " | ", "GuiObject", "nothing to place or upgrade", " for ", "core/logger", "ถูกสุดก่อน", "s - carrying on without it", "sale didn't go through", "no", "jumpPower", "[MekPro] settings file: ", "[AutoUpgrade] ", "=empty", "MineSpeed", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[Remotes] === total: ", ")", "อังกฤษ", "=", " (still ", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "Frame", " items (now ", "ValueLabel", "Pad", "s", "AutoMine", "MineBuyPickaxe", " ", ": ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "core/debug_interaction_logger", "Anti-AFK", " research is ", "ScreenGui", " screen(s)) ===", "MekPro.log", "MineRollMutation", "UserInputService", "Damage", "Mutation: เพดานราคาต่อครั้ง", "Auto Claim: รอบเช็ค", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "Container", "core/logger", "autoUpgradePickaxe", ": ", "autoUpgradeMutationTarget", "/config.json", "%", "[AutoUpgrade] pickaxe #", "%.3g", "cost", "TextLabel", "JumpPower (ปกติ 50)", "Auto Chest", " but no unclaimed reward in its Track", "ItemFrame", "Players", "", "Badge", "TextButton", ")", "^%$", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "games/my_anime_mine/auto_chest", "th", "[GameAPI] couldn't load the game's modules: ", "\n", "🐛", "Content", "", " files", "", "core/anti_afk", ") ", "[Config] couldn't read ", "ไล่ซื้อทุกกี่วินาที", "Zones", "core/teleport", "autoClaim", "", "  ", " (chest holds ", "autoUpgradeEquipPickaxe", " (power ", "MineSellItems", "TextLabel", "Decompile Client Scripts", " ", ")", "games/registry", "ย่อขยายเมนูทั้งชุด", "string", "index rewards are disabled in this game build", "games/my_anime_mine/game_api", "core/logger", "[AutoChest] enabled (collects once >= ", "autoUpgradeMutationBudget", "MineWorld", "autoUpgradeLimitPickaxes", "ProximityPrompt", "%$%s*([%d,%.]+)%s*(%a*)", "[Interaction] logger enabled - go click/interact with things now", " (", "core/logger", "ค้นหาฟังก์ชัน...", "Players", "Players", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "Players", "Players", "x", "unreadable", "[AutoSell] MineSellItems ", "M", "[AutoPlace] fireproximityprompt error: ", "core/debug_tab", "Dump", "Frame", "GuiButton", "Auto Upgrade: สุ่ม Mutation", "en", "AutoPlace", "games/my_anime_mine/get_best_miner", " power ", "table", "TextBox", "games/my_anime_mine/game_api", "", "Auto Chest: เกณฑ์เก็บ", "[Interaction] logger disabled", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "s", "", "GetCharacterSpeedUpgradeCost", "core/run_job", "Badge", "; ", "", "en-us", " pickaxe(s) for ", "none", "AutoResearch", "slot past the usable cap", "M", "server didn't equip it", "games/my_anime_mine/get_current_plot", " = ", "core/logger", "both Zone and Core buying are switched off", "Content", "costs ", "Backpack", "core/logger", "TextButton", "table", "every open node is maxed", "ChestCapacity", "TextLabel", "TextLabel", "games/my_anime_mine/main", "interactionLogger", ")", "disabled", "Workspace", "[AutoResearch] ", "[Scheduler] turn: ", "s", "unlock", ", have ", "core/config", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "[Bb]uy", "none", "[AutoPlace] upgrading ", "/", "research", "enabled", "  ", "[AutoPlace] unlocked ", "[AntiAFK] idle detected, simulating input", "autoUpgrade", "Character", ": ", "games/my_anime_mine/auto_claim", "auto", " -> ", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "WalkSpeed (ปกติ 16)", "Plots", "Main", ")", "autoSellMaxWait", "", "core/logger", "Auto Upgrade: แจก Pickaxe ให้ miner", "works from anywhere - no teleport needed", "M", "/", "x", " owned)", "number", ": badge says ", "table", "core/run_job", "TextButton", "OwnerUserId", "[AutoChest] collected ", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", " ", " - upgrade the miners first", "autoPlotInterval", "[AutoResearch] cheapest open node: ", "Auto Upgrade Plot (Zone)", "table", ")", "en", "[AutoPlot] now mining ", "MineSelectZone", "ความเร็ววิ่ง", "[AutoPlace] holders: ", " reward(s) looked claimable but none went through", "(nothing to do)", "PlayerScripts", "AutoClaim", "Frame", "[AutoPlace] ", "autoMine", " mutation x", ", built ", ": ", " refused - ", " (Text=", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "games/my_anime_mine/auto_research", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "%.0f", "[AutoUpgrade] MineBuyPickaxe ", "Speed", "function", "autoResearchKeepMoney", "BasePart", "[AntiAFK] disabled", "unreadable", ") ", "fly", "core/logger", "'", "string", "MineEquipPickaxe", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "MineBuySkill", "table", "0 = ไม่จำกัด", "MinePower", "HumanoidRootPart", "[Decompile] wrote ", "ลอย (Float)", "MekPro", "[GameAPI] SendEvent failed: ", "Auto Plot: ใช้โซนล่าสุดเสมอ", "autoChest", "autoPlotKeepMoney", "", "", "TextLabel", " (", "string", " (", "PlayerGui", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "th", " has been busy for ", " wouldn't take ", " -> x", "modules not loaded", "Root", " script(s) to ", "TextButton", "Loot", "core/anti_afk", "Auto Upgrade: รอบเช็ค", "", " error: ", "TextLabel", "yes", "\n", "works from anywhere - dropping the teleport", "core/logger", "getconnections", "[Remotes] ", "Holders", " not bought yet (", "Auto Sell", "[AutoUpgrade] bought pickaxe #", "core/parse_money", " (", "Players", " -> ", "[AutoPlot] unlocked ", "CharMineSpeedMaxLevel", "AutoPlot", "core/logger", "Header", "games/my_anime_mine/game_api", "Auto Upgrade: กันเงินไว้", "core/debug_interaction_logger", "waiting", "affordable - waiting for a turn", " costs ", " ===", "getgc", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", ", you have ", "Auto Research: ซื้อสาย Zone", " ", "TextButton", "enabled", " #", " (ActionText=", "autoResearch", "%$", "Workspace", "empty", "games/my_anime_mine/get_plot_parts", "ScrollingFrame", "Auto Plot: รอบเช็ค", "games/my_anime_mine/get_current_plot", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "BasePart", "core/humanoid", "may need us at the stand - trying that", "autoPlotBuy", "[AutoPlace] picked up ", " doesn't exist", "[GuiDump] PlayerGui not found", " didn't go through (out of stock or too expensive)", "_", "ReplicatedStorage", "MineSpeed", "Text", ") gave nothing", "MekPro", " costs ", "[AutoUpgrade] ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "ตามเครื่อง", "core/logger", "Dump Visible GUI", "Speed", "menuScale", "แรงกระโดด", "core/get_leaderstat", "games/my_anime_mine/auto_sell", "autoResearchCore", "function", "BasePart", "Frame", "decompile", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "MineUpgradeCharacter", "x", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "s", "Auto Mine: คลิกต่อรอบ", "[Decompile] === start ===", "Auto Plot: กันเงินไว้", "ClickDetector", "[BestMiner] slot ", "games/my_anime_mine/auto_plot", "core/get_place_id", "core/dispatch", "ReplicatedStorage", "languageIndex", "s", "HumanoidRootPart", "table", "core/logger", "-", "GetCharacterUpgradeCost", "[GuiDump] ", "slot not in the hotbar", "prompt disabled", "games/my_anime_mine/game_api", "Zone buying is off", "แพงสุดก่อน", "table", "games/my_anime_mine/auto_claim", "core/config", "lowest", "TextButton", "Humanoid", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "RemoteFunction", "d819037+", "empty", "core/logger", "", "[Scheduler] ", "[AutoPlace] slot ", "TextLabel", "Limited", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " (", "core/teleport", "TextLabel", " (", "open_menu_hint", "Players", "GetHolderSpeedLevel", "[Scheduler] ", " (", "TextLabel", "[AutoSell] enabled", "autoUpgradePickaxeIndex", "core/build_env", "core/ui", "s", "[%X] ", "GuiService", "ValueLabel", "[Interaction] Button activated: ", ", base ", "upgrade", "[Uu]nlock", "[AutoPlace] ", "disabled", "games/my_anime_mine/game_api", "ไทย", "อ่านป้ายบน chest ทุกกี่วินาที", "Interaction Logger", "%.0f", "fill", "Auto Plot: ซื้อโซนถัดไป", "write reported success but the line isn't in ", "Run", "autoSellMaxWait", " items", "[AutoPlace] drifted out of range of ", "/", "ProximityPrompt", " stack(s)", ": ", "debug.getconstants", "lowest", " still empty after placing ", "games/my_anime_mine/game_api", "unlock", "Dump", "ขนาดเมนู", "", "autoSellProtectUnits", "RemoteEvent", "มุดแมพ (Noclip)", "autoResearchCheapestFirst", "[^%w]+", "UIScale", " (prompt says '", "", "", "[AutoChest] disabled", "games/my_anime_mine/auto_research", "core/build_env", "table", "CoreUI", "autoUpgradeInterval", "Auto Sell: รอบขาย", "badge says ", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "core/parse_money", "debug.getupvalues", "[AutoUpgrade] ", "[AutoPlace] ", "[AutoMine] ", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "autoPlaceInterval", "%$", "autoResearchZones", "[^%.]+", "Locked miner slot", "เดินทะลุกำแพงและพื้น", "^x(%d+)$", "Auto Upgrade Unit", " items)", "/s", "[AutoPlace] unlocking ", "AutoSell", "getcallbackvalue", "🏠", "locked slot", "BodyPosition", "Core"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(748)] = function()
return function(import)
local log = import(S(351))
local Players = game:GetService(S(575))
local VirtualUser = game:GetService(S(303))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(30))
        connection = player.Idled:Connect(function()
            log(S(648))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(710))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(932)] = function()
return { Env = S(101), IsDev = true, Commit = S(864), BuiltAt = S(318) }
end
modules[S(138)] = function()
local VirtualInputManager = game:GetService(S(12))
local GuiService = game:GetService(S(889))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(76)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(638)] = function()
return function(import)
local HttpService = game:GetService(S(363))
local getPlaceId = import(S(840))
local log = import(S(770))
local ROOT_FOLDER = S(810)
local placeFolder = ROOT_FOLDER .. S(165) .. tostring(getPlaceId())
local filePath = placeFolder .. S(514)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(300) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(543) .. filePath .. S(495) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(43) .. filePath .. S(512) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(153)] = function()
return function(import)
local Players = game:GetService(S(212))
local log = import(S(756))
local CAPABILITIES = {
    S(826), S(239), S(434), S(779), S(452),
    S(757), S(958), S(36), S(28), S(913),
    S(940), S(429), S(227), S(447),
}
local OUT_FOLDER = S(80)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(949)) do
        if type(target) ~= S(426) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(334))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(124) .. name .. S(228) .. ((ok and type(value) == S(279)) and S(753) or S(474)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(229))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(691))
    if not playerScripts then
        log(S(320))
        return 0
    end
    pcall(makefolder, S(726))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(462)) or target:IsA(S(93)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(460) and #source > 0 then
                local name = target:GetFullName():gsub(S(925), S(805))
                if pcall(writefile, OUT_FOLDER .. S(215) .. name .. S(82), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(724) .. written .. S(745) .. OUT_FOLDER
        .. S(873) .. failed .. S(361))
    return written
end
local function run()
    log(S(835))
    reportCapabilities()
    local count = dumpAll()
    log(S(99))
    return count
end
return run
end
end
modules[S(151)] = function()
return function(import)
local Players = game:GetService(S(419))
local log = import(S(662))
local function dumpVisible(root, indent)
    indent = indent or S(91)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(467)) and child.Visible then
            local line = indent .. child.ClassName .. S(232) .. child.Name .. S(15)
            if child:IsA(S(432)) or child:IsA(S(622)) then
                line = line .. S(700) .. tostring(child.Text) .. S(659)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(407) .. attrName .. S(615) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(549))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(365))
    if not playerGui then
        log(S(803))
        return 0
    end
    local screens = 0
    log(S(378))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(259)) and screen.Enabled and screen.Name ~= S(267) then
            screens = screens + 1
            log(S(850) .. screen.ClassName .. S(408) .. screen.Name .. S(715))
            dumpVisible(screen, S(646))
        end
    end
    log(S(372) .. screens .. S(501))
    return screens
end
return dumpGui
end
end
modules[S(348)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(842))
local log = import(S(714))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(922)) or descendant:IsA(S(863)) then
            count = count + 1
            log(S(758) .. descendant.ClassName .. S(698) .. descendant:GetFullName())
        end
    end
    log(S(481) .. count .. S(778))
    return count
end
return dumpRemotes
end
end
modules[S(774)] = function()
return function(import)
local Players = game:GetService(S(765))
local Workspace = game:GetService(S(790))
local UserInputService = game:GetService(S(293))
local log = import(S(866))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(309) .. describe(prompt)
            .. S(787) .. prompt.ActionText .. S(406))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(46) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(891) .. describe(button)
            .. S(287) .. tostring(button.Text) .. S(223))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(568)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(112)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(587)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(598))
        return
    end
    connections = {}
    log(S(570))
    local playerGui = Players.LocalPlayer:WaitForChild(S(98))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(203)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(837)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(31)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(438),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(332)] = function()
return function(import)
local interactionLogger = import(S(497))(import)
local dumpRemotes = import(S(313))(import)
local dumpGui = import(S(390))(import)
local decompileScripts = import(S(224))(import)
local humanoid = import(S(798))
local function build(window, persist)
    local tab = window:AddTab(S(132), S(536))
    persist.Toggle(
        tab:AddCard(
            S(900),
            S(155)
        ),
        S(629),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(319),
        S(439)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(585), function()
        remoteBtn.Text = dumpRemotes() .. S(310)
    end)
    local guiCard = tab:AddCard(
        S(816),
        S(113)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(918), function()
        guiBtn.Text = dumpGui() .. S(42)
    end)
    local decompileCard = tab:AddCard(
        S(555),
        S(530)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(905), function()
        decompileBtn.Text = decompileScripts() .. S(539)
    end)
    humanoid.Start()
    persist.Slider(tab, S(687), S(656),
        8, 200, S(422), 16, humanoid.SetWalkSpeed, S(596))
    persist.Slider(tab, S(819), S(520),
        30, 300, S(475), 50, humanoid.SetJumpPower, S(358))
    persist.Toggle(
        tab:AddCard(S(213), S(401)),
        S(713), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(923), S(951)),
        S(166), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(725), S(718)),
        S(210), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(841)] = function()
return function(import)
    local getPlaceId = import(S(240))
    local registry = import(S(83))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(346))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(820)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(108))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(34)] = function()
local Players = game:GetService(S(298))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(861))
    local backpack = player:WaitForChild(S(620))
    return player, humanoid, backpack
end
return getMe
end
modules[S(299)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(304)] = function()
local Players = game:GetService(S(574))
local RunService = game:GetService(S(236))
local UserInputService = game:GetService(S(115))
local Workspace = game:GetService(S(314))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(192))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(845))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(144))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(245)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(797)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(961))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(321)] = function()
local Players = game:GetService(S(577))
local STRINGS = {
    en = {
        search_placeholder = S(277),
        open_menu_hint = S(176),
    },
    th = {
        search_placeholder = S(573),
        open_menu_hint = S(57),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(607)
    if locale:sub(1, 2):lower() == S(301) then
        return S(533)
    end
    return S(684)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(386) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(616)] = function()
local LOG_FILE = S(502)
local function log(message)
    local line = os.date(S(888)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(548))
            end
            appendfile(LOG_FILE, line .. S(535))
        elseif writefile then
            local existing = S(199)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(754))
        else
            error(S(110))
        end
        if not (isfile and readfile) then
            error(S(368))
        end
        if not isfile(LOG_FILE) then
            error(S(38) .. LOG_FILE .. S(802))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(904) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(211) .. LOG_FILE .. S(651) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(763)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(735) then
        return nil
    end
    local number, suffix = text:match(S(569))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(6), S(463))))
    if not value then
        return nil
    end
    if suffix ~= S(350) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(302)] = function()
return function(import)
local log = import(S(130))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(273) .. name .. S(152) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(354)] = function()
return function(import)
local RunService = game:GetService(S(328))
local log = import(S(815))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(868) .. task_.name .. S(751) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(880) .. task_.name .. S(740)
                    .. string.format(S(901), now - task_.busySince)
                    .. S(472))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(400) .. starved.name .. S(175)
            .. string.format(S(704), now - starved.waitingSince)
            .. S(64))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(634) .. (currentTask and currentTask.name or S(690)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(381)] = function()
local Players = game:GetService(S(524))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(111))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(886)] = function()
return function(import)
local i18n = import(S(100))
local UserInputService = game:GetService(S(504))
local THEME = {
    BrandName = S(353),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(443))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(450))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(366))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(449))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(538)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(105))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(554))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(248)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(519))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(750)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(793))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(294))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(627))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(487))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(752))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(733))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(870))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(890))
            if not valueLbl then
                valueLbl = Instance.new(S(875))
                valueLbl.Name = S(489)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(526))
            if not badge then
                badge = Instance.new(S(20))
                badge.Name = S(604)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(860))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(825))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(225))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(673))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(525)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(515)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(178))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(392))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(158))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(540)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(142))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(250))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(527))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(808)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(500))
    screenGui.Name = S(934)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(10))
    local main = Instance.new(S(244))
    main.Name = S(658)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(926))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(433))
    tabBar.Name = S(276)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(60))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(451))
    header.Name = S(771)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(784))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(882))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(177))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(137)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(19))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(204)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(746))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(848)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(594))
    searchBox.Name = S(308)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(399))
    searchBox.Text = S(928)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(693))
    content.Name = S(338)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(586))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(62))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(184))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(147))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(85)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(731) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(364) .. (item.Subtitle or S(180))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(297))
        footerLabel.Text = i18n.T(S(877))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(532)] = function()
return function(import)
local gameApi = import(S(84))(import)
local log = import(S(220))
local runGuarded = import(S(396))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(564) .. minItemsToCollect .. S(954))
    else
        log(S(930))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(580) then
            log(S(208))
            lastReported = S(711)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(865) then
            log(S(384))
            lastReported = S(791)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(775) then
            local capacity = gameApi.ChestCapacity()
            log(S(56) .. count .. S(356) .. want .. S(907)
                .. (capacity and (S(550) .. capacity .. S(442)) or S(200)))
            lastReported = S(1)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(403), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(675) .. count .. S(488) .. after .. S(168))
        else
            log(S(262) .. count
                .. S(32) .. tostring(after) .. S(179))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(857)] = function()
return function(import)
local gameApi = import(S(218))(import)
local log = import(S(847))
local runGuarded = import(S(603))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(202) .. (enabled and S(391) or S(631)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(335) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(68) .. reward.category .. S(3)
                .. tostring(reward.tierKey) .. S(786) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(286) .. #pending .. S(689))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(231), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(393)] = function()
return function(import)
local getCurrentPlot = import(S(795))
local getPlotParts = import(S(282))
local log = import(S(464))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(169), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(943) .. (enabled and S(457) or S(18)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(446)] = function()
return function(import)
local Players = game:GetService(S(878))
local getCurrentPlot = import(S(397))
local getPlotParts = import(S(792))
local getMinerSlots = import(S(591))(import)
local teleport = import(S(874))
local parseMoney = import(S(939))
local gameApi = import(S(595))(import)
local log = import(S(470))
local runGuarded = import(S(233))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(296)
local LOCKED_SLOT_TEXT = S(950)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(490))
    return pad and pad:FindFirstChildWhichIsA(S(421), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(156)) then
            local digits = descendant.Text:match(S(162))
            if digits then
                return tonumber((digits:gsub(S(336), S(425))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(626)) and descendant.Text:match(S(529)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(278) .. holder.Name .. S(760)
            .. gameApi.MinerSlots() .. S(909) .. gameApi.MaxMinerSlots() .. S(668)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(960)
    end
    if not prompt.Enabled then
        return true, S(852)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(619) .. price
    end
    local action = prompt.ActionText or S(867)
    if action:match(S(894)) or action:match(S(640)) or action:match(S(789)) then
        return true, S(369) .. action .. S(104)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(723))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(583) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(271) .. why .. S(382))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(478))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(484) .. prompt.ObjectText
                    .. S(146) .. shown .. S(892) .. math.floor(base) .. S(196))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(466))
    if line ~= lastSurvey then
        log(S(688) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(141), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(893), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(636), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(694) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(256))
    else
        lastAttempt = 0
        log(S(174))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(118),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(851)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(611)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(613)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(445))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(908) .. holder.Name .. S(268))
            return false
        end
        log(S(869) .. candidate.slot .. S(764) .. candidate.name
            .. S(70) .. tostring(why))
    end
    if not miner then
        log(S(255))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(895) .. holder.Name .. S(915) .. miner.name)
        return false
    end
    log(S(45) .. miner.name .. S(171) .. miner.power
        .. S(337) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(52) .. entry.name .. S(71) .. entry.holder.Name
            .. S(927) .. tostring(entry.prompt.ActionText) .. S(340))
        return false
    end
    log(S(801) .. entry.name .. S(552) .. entry.power
        .. S(285) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(942) .. pad.holder.Name .. S(295)
                .. tostring(pad.priceText))
        else
            log(S(647) .. pad.holder.Name
                .. S(292) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(468))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(902) then
        runGuarded(S(269), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(917) then
        log(S(956) .. target.holder.Name
            .. S(469) .. tostring(target.priceText))
        runGuarded(S(264), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(642) .. target.holder.Name .. S(235) .. target.name
        .. S(219) .. target.power .. S(371) .. best.name .. S(876) .. best.power .. S(291))
    runGuarded(S(590), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(343)] = function()
return function(import)
local gameApi = import(S(853))(import)
local parseMoney = import(S(288))
local log = import(S(510))
local runGuarded = import(S(448))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(333) .. (enabled and S(645) or S(896)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(947)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(685) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(395) .. deepestKey .. S(557))
        return true
    end
    log(S(377) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(441) .. gameApi.ZoneDisplayName(zoneKey) .. S(811)
                .. math.floor(price) .. S(781) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(767) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(193) .. zoneKey .. S(226))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(160) then
            reportedLocked = S(644)
            log(S(209) .. zoneKey .. S(699)
                .. gameApi.ZoneDisplayName(currentKey) .. S(499)
                .. done .. S(455) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(458), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(702)] = function()
return function(import)
local gameApi = import(S(89))(import)
local log = import(S(251))
local runGuarded = import(S(672))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(633) .. (enabled and S(191) or S(41)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(119) .. node.zoneKey .. S(198) .. node.id
                    .. S(734) .. node.family .. S(427) .. node.level .. S(654) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(388) .. node.id
                    .. S(140) .. node.family .. S(712) .. node.level .. S(412) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(183) .. blocked
                .. S(9))
        elseif powerBlocked then
            log(S(44) .. powerBlocked.zoneKey .. S(230) .. powerBlocked.id
                .. S(571) .. powerBlocked.family .. S(266)
                .. math.floor(powerBlocked.powerReq) .. S(325)
                .. math.floor(powerBlocked.power)
                .. S(678))
        else
            log(S(129)
                .. S(13) .. math.floor(gameApi.GetMoney()) .. S(528))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(680) .. nearest.zoneKey
                    .. S(494) .. nearest.id .. S(736) .. nearest.family .. S(75)
                    .. nearest.level .. S(26) .. nearest.max
                    .. S(777) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(617)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(962),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(854))
        end
        if not buyCore then
            table.insert(parts, S(444))
        end
        if #parts == 0 then
            return S(624)
        end
        return S(329) .. table.concat(parts, S(201)) .. S(482)
    end
    local why
    if nearest.cost > budget then
        why = S(109) .. string.format(S(139), nearest.cost)
            .. S(637) .. string.format(S(517), money)
    elseif nearest.powerOk == false then
        why = S(307) .. string.format(S(186), nearest.powerReq) .. S(349)
    else
        why = S(776)
    end
    return S(379) .. nearest.zoneKey .. S(237) .. tostring(nearest.id)
        .. S(881) .. tostring(nearest.family) .. S(542) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(40) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(246), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(389)] = function()
return function(import)
local Workspace = game:GetService(S(25))
local gameApi = import(S(206))(import)
local teleport = import(S(103))
local log = import(S(254))
local runGuarded = import(S(189))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(290))
    else
        lastAttempt = 0
        log(S(883))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(435))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(910), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(581)
                .. (sold and S(755)
                         or S(87)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(97) .. (sold and (S(316) .. #keys .. S(911)) or S(473)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(380), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(79)] = function()
return function(import)
local Workspace = game:GetService(S(376))
local gameApi = import(S(916))(import)
local teleport = import(S(546))
local log = import(S(572))
local runGuarded = import(S(23))(import)
local STATS = { S(243), S(706), S(260) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(914)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(477) .. (enabled and S(785) or S(148)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(345) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(859) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(518) or S(383)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(941) .. holderKey .. S(677) .. choice.stat .. S(94)
                        .. choice.info.level .. S(766) .. after.level
                        .. S(643) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(81) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(149))
    local shop = stand and stand:FindFirstChild(S(74))
    if not shop then
        return nil
    end
    if shop:IsA(S(824)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(709), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(705)
                .. (bought and S(664)
                           or S(799)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(639))
        end
    end
    if bought then
        log(S(762) .. pickaxe.index .. S(556) .. pickaxe.name
            .. S(116) .. pickaxe.multiplier .. S(92))
        return true
    end
    log(S(516) .. pickaxe.index .. S(217) .. pickaxe.name
        .. S(804))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(55) .. holderKey .. S(77)
                    .. tostring(worn or S(641)) .. S(275) .. wanted.name)
            else
                log(S(812) .. holderKey .. S(741)
                    .. wanted.name .. S(485) .. tostring(nowWorn or S(609)) .. S(375))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(95) .. holderKey .. S(696) .. variant
                .. S(742) .. after .. S(394) .. cost .. S(683))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(73) .. gameApi.OwnedPickaxeCount()
                    .. S(608) .. gameApi.MaxMinerSlots()
                    .. S(121))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(576))
        elseif maxed > 0 then
            log(S(159))
        else
            log(S(486))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(317), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(772)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(806))
local Workspace = game:GetService(S(163))
local log = import(S(315))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(114), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(534) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(682))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(623) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(69) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(856) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(933) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(324) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(360))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(727) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(252))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(185), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(181) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(65) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(650) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(553), keys)
end
function api.EquipSlot(slot)
    return send(S(249), slot == S(428) and S(66) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(493), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(717), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(503), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(719), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(593)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(131)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(722))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(707) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(86) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(686), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(566))
    plots = plots and plots:FindFirstChild(S(430))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(370)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(461)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(453))
    local gui = board and board:FindFirstChild(S(417))
    local root = gui and gui:FindFirstChild(S(744))
    local list = root and root:FindFirstChild(S(136))
    local row = list and list:FindFirstChild(S(247) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(241)) and descendant.Text ~= S(601) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(823) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(830), characterKey, stat == S(161) and S(807) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(238), max = S(63), cost = S(849) },
    Speed = { level = S(879), max = S(102), cost = S(602) },
    MineSpeed = { level = S(122), max = S(768), cost = S(51) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(164) or charName == S(661) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(846) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(669) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(716) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(398) and type(holder.CharName) == S(359) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(871), S(454) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(387) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(743)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(253)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(561)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(671) then
        return {}, S(221)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(143)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(720) or type(state.Track) ~= S(207) then
                table.insert(notes, tabKey .. S(222) .. tostring(category) .. S(809))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(670) .. waiting
                        .. S(522))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(937) .. total .. S(404)
            .. (#notes > 0 and (S(17) .. table.concat(notes, S(605))) or S(920))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(560) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(423)] = function()
return function(import)
local Players = game:GetService(S(578))
local gameApi = import(S(897))(import)
local log = import(S(563))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(8))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(312), S(424)))) or 0
    if suffix ~= S(929) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(737))
    local mineGui = playerGui and playerGui:FindFirstChild(S(195))
    local hotbar = mineGui and mineGui:FindFirstChild(S(145))
    local content = hotbar and hotbar:FindFirstChild(S(618))
    return content and content:FindFirstChild(S(509))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(416)) then
            local count = descendant.Text:match(S(952))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(523))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(537))
    local nameLabel = content and content:FindFirstChild(S(274))
    local damageLabel = content and content:FindFirstChild(S(505))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(606) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(666) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(838) .. slot .. S(912) .. info.name
                        .. S(592) .. info.power .. S(7) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(614)] = function()
local Players = game:GetService(S(39))
local Workspace = game:GetService(S(632))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(59))
    local plots = mineWorld and mineWorld:FindFirstChild(S(657))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(674)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(330)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(759)),
        Nodes = plot:FindFirstChild(S(58)),
        Chest = plot:FindFirstChild(S(402)),
        Zones = plot:FindFirstChild(S(545)),
        Crew = plot:FindFirstChild(S(172)),
        Loot = plot:FindFirstChild(S(747)),
        PlotIndex = plot:GetAttribute(S(48)),
        ChestCapacity = plot:GetAttribute(S(625)),
    }
end
return getPlotParts
end
modules[S(437)] = function()
return function(import)
    local createWindow = import(S(50))(import)
    local i18n = import(S(27))
    local antiAfk = import(S(541))(import)
    local buildEnv = import(S(885))
    local scheduler = import(S(459))(import)
    local config = import(S(858))(import)
    local log = import(S(621))
    log(S(47) .. buildEnv.Commit .. S(283) .. buildEnv.Env
        .. S(697) .. buildEnv.BuiltAt .. S(630))
    local window = createWindow({
        Subtitle = S(22),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(54))(import)
    local autoPlace = import(S(37))(import)
    local autoChest = import(S(126))(import)
    local autoSell = import(S(821))(import)
    local autoUpgrade = import(S(133))(import)
    local autoResearch = import(S(931))(import)
    local autoClaim = import(S(652))(import)
    local autoPlot = import(S(839))(import)
    local gameApi = import(S(562))(import)
    scheduler.RegisterStandalone(S(492), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(413), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(436), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(339), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(610), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(692), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(769), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(906), 20)
    scheduler.RegisterExclusive(S(957), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(61), S(959))
    persistentToggle(farmTab:AddCard(
        S(190),
        S(16)
    ), S(695), autoMine.SetEnabled)
    persistentSlider(farmTab, S(53), S(701),
        1, 30, S(305), 5, autoMine.SetCyclesPerSecond, S(955))
    persistentSlider(farmTab, S(834), S(480),
        1, 10, S(216), 1, autoMine.SetClicksPerCycle, S(150))
    persistentToggle(farmTab:AddCard(
        S(205),
        S(311)
    ), S(127), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(187), S(599),
        1, 60, S(946), 3, autoPlace.SetCheckInterval, S(258))
    persistentToggle(
        farmTab:AddCard(S(270),
            S(862)),
        S(214), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(188),
            S(123)),
        S(362), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(284), S(721),
        0, 500, S(280), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(582))
    persistentToggle(farmTab:AddCard(
        S(521),
        S(703)
    ), S(729), autoChest.SetEnabled)
    persistentSlider(farmTab, S(597), S(832),
        50, 3000, S(72), 100, autoChest.SetMinItems, S(173))
    persistentSlider(farmTab, S(465), S(899),
        1, 60, S(327), 3, autoChest.SetCheckInterval, S(635))
    persistentToggle(farmTab:AddCard(
        S(761),
        S(827)
    ), S(194), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(828),
            S(676)),
        S(921), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(936), S(167),
        5, 300, S(5), 20, autoSell.SetCheckInterval, S(491))
    farmTab:AddSlider(S(263),
        S(780),
        5, 120, sellMaxWait, function(value)
            config.Set(S(660), value)
        end, S(135))
    persistentToggle(farmTab:AddCard(
        S(953),
        S(872)
    ), S(649), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(749), S(261),
        5, 300, S(935), 20, autoUpgrade.SetInterval, S(600))
    persistentSlider(farmTab, S(257), S(2),
        1, 20, S(322), 5, autoUpgrade.SetMaxPerPass, S(117))
    persistentSegmented(
        farmTab,
        { S(347), S(78), S(341), S(409) },
        { S(265), S(125), S(817), S(479) },
        S(420), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(773), S(281),
        0, 500, S(456), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(612))
    persistentToggle(
        farmTab:AddCard(S(411),
            S(938)),
        S(511), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(415) .. pickaxe.multiplier .. S(88)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(796)
            end
            return S(306) .. index .. S(783) .. (pickaxeLabels[index] or S(355))
        end
        local savedPick = config.Get(S(128), 0)
        pickaxeCard = farmTab:AddSlider(
            S(405),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(884), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(732)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(344),
            S(21)),
        S(567), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(663),
            S(170)),
        S(551), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(588),
            S(342)),
        S(4), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(352), S(829),
        1, 20, S(513), 2, autoUpgrade.SetMutationTarget, S(579))
    persistentSlider(farmTab, S(506), S(414),
        0, 500, S(565), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(331))
    persistentToggle(farmTab:AddCard(
        S(373),
        S(738)
    ), S(788), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(96), S(544),
        10, 600, S(14), 30, autoResearch.SetCheckInterval, S(833))
    persistentSlider(farmTab, S(357), S(385),
        1, 20, S(418), 5, autoResearch.SetMaxPerPass, S(667))
    persistentSlider(farmTab, S(49), S(367),
        0, 500, S(708), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(182))
    persistentToggle(
        farmTab:AddCard(S(782),
            S(655)),
        S(948), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(29),
            S(531)),
        S(822), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(242),
            S(234)),
        S(157), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(471), S(855) },
        { true, false },
        S(924), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(67),
        S(813)
    ), S(547), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(507), S(508),
        5, 300, S(323), 30, autoClaim.SetCheckInterval, S(844))
    persistentSlider(farmTab, S(374), S(944),
        1, 30, S(154), 10, autoClaim.SetMaxPerPass, S(831))
    persistentToggle(farmTab:AddCard(
        S(681),
        S(440)
    ), S(134), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(794), S(90),
        5, 300, S(679), 30, autoPlot.SetCheckInterval, S(887))
    persistentToggle(
        farmTab:AddCard(S(903),
            S(945)),
        S(800), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(728),
            S(24)),
        S(289), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(836), S(107),
        0, 500, S(730), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(665))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(584))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(410), S(326))
    persistentToggle(
        settingsTab:AddCard(S(498), S(496)),
        S(35),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(818), 100)
    settingsTab:AddSlider(S(919), S(559), 50, 150, savedScale, function(value)
        config.Set(S(120), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(843), 1)
    settingsTab:AddSegmented({ S(814), S(483), S(898) }, savedLanguage, function(index, value)
        local code = ({ S(653), S(589), S(197) })[index]
        config.Set(S(33), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(272), S(431), S(739) })[savedLanguage] or S(11))
    log(S(476) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(558)] = function()
return {
    [79389059854988] = S(628),
}
end
import(S(106))(import)
