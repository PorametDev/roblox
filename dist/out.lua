-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {" remotes", "core/ui", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "core/anti_afk", ")", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Dump", "Text", "SellFrame", " ===", "TextButton", " on ", "ไทย", "ProximityPrompt", "Dump", "TextButton", "", "ClaimOre", "games/my_anime_mine/get_plot_parts", "th", ")", "ReplicatedStorage", "ValueLabel", "core/i18n", "core/dispatch", "PlayerGui", "%", "Check", "Tool", "no isfile/readfile available to verify the write", "Search functions...", "ClickDetector", "Chest", " '", "core/dispatch", "  [attr] ", "UserInputService", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "games/my_anime_mine/main", "TextBox", "Workspace", "UIPadding", "", "SellItems", "UIListLayout", "Auto Upgrade", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Frame", "core/get_me", "en-us", "TextLabel", "core/logger", "th", "no plot", "en", "en", " ===", "neither appendfile nor writefile is available", "[SellCards] Card #", "Open menu", "Badge", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "TextButton", "Debug: Dump Backpack Tool", "Plots", "Frame", "VirtualUser", "GuiButton", "games/my_anime_mine/auto_place", "[AntiAFK] idle detected, simulating input", "TextLabel", "UICorner", "games/my_anime_mine/get_plot_parts", "TextLabel", "Tool", "open_menu_hint", "Debug: Dump All Remotes", "%", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "search_placeholder", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/get_current_plot", "", "[MyAnimeMine] not standing in any plot", "Badge", "ตามเครื่อง", "TextLabel", "games/my_anime_mine/auto_chest", "[Interaction] logger enabled - go click/interact with things now", "", "ProximityPrompt", " = ", "Frame", "CoreUI", "", "TextButton", "Scroll", "Players", "ScrollingFrame", "ย่อขยายเมนูทั้งชุด", "MekPro.log", "[AutoUpgrade] disabled", "'", "TextLabel", "  [attr] ", "ClickDetector", "[Interaction] logger disabled", "Anti-AFK", "[AutoChest] disabled", "[AutoSell] SellItems:FireServer() error: ", "", "Search", "X", "GuiObject", "TextButton", "ProximityPrompt", "TabBar", "Debug: Dump Node/Holder", "Dump", "Players", "core/i18n", "TextLabel", " ===", ": ", "Dump", "⚙", "UICorner", "core/get_place_id", "Auto Place Miner", "[Logger] failed to write ", "games/my_anime_mine/get_plot_parts", "เปิด chest แล้วเก็บของอัตโนมัติในตัวเดียว (ยิง ChestPrompt + ClaimOre/CollectAll)", "Main", "RunService", "[MyAnimeMine] === Node: ", "games/registry", "CollectAll", "UpgradePart", "TextLabel", "ฟาร์ม", "TextLabel", "ChestCapacity", "Workspace", "core/logger", "ClickDetector", "SellStand", "TextLabel", "Pad", "Loot", "[MyAnimeMine] === Tool: ", "Backpack", "write reported success but the line isn't in ", "leaderstats", "[AutoMine] enabled", "Players", "Backpack", "games/my_anime_mine/main", "MekPro", "Frame", " (MaxActivationDistance=%s)", "core/interaction_logger", "core/ui", "Error: ", ": ", "auto", "games/my_anime_mine/auto_sell", "log failed", "core/logger", " ===", "[Remotes] ", "TextButton", "TextLabel", "[AutoChest] enabled (ClaimOre=%s, CollectAll=%s)", "[MyAnimeMine] no nodes present right now", "Frame", "games/my_anime_mine/debug_dump", "core/logger", "[AutoUpgrade] enabled", "\n", "games/my_anime_mine/get_plot_parts", "ValueBase", "Auto Chest", "[Interaction] ProximityPrompt triggered: ", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/get_current_plot", "TextButton", "'", "core/logger", "RunService", "Frame", "?", "  ", "UIListLayout", " doesn't exist", "games/my_anime_mine/debug_dump_sell_cards", "ปุ่มเปิดเมนู", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "RemoteEvent", "RunService", "games/my_anime_mine/auto_upgrade", " <-- remote, likely fired by the real interaction", "ClickDetector", "[AutoPlace] enabled", "games/my_anime_mine/get_plot_parts", "Text", "UIStroke", "PlotIndex", "\n", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Frame", "Players", "RunService", "MineWorld", " = ", "games/my_anime_mine/auto_mine", "core/logger", "ValueLabel", "[Interaction] Key pressed: %s (gameProcessed=%s)", " '", " ===", "games/my_anime_mine/auto_place", "Frame", "PlayerGui", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "[MyAnimeMine] no holders found", "core/logger", "core/logger", "[AntiAFK] enabled - watching Player.Idled", "games/my_anime_mine/get_current_plot", "[Interaction] Button activated: ", "Players", "core/interaction_logger", "[SellCards] === total cards: ", "This is not a supported place.", " ", "games/my_anime_mine/debug_dump_remotes", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "[AutoSell] enabled (SellItems=", " '", "auto", "Content", "Auto Mine", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "RemoteFunction", "ProximityPrompt", "เดินไปกด SellStand แล้วกด SelectAllButton อัตโนมัติ (ยังไม่ยืนยันว่าขายจริงหรือแค่เลือก)", "Frame", "Done", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "core/get_place_id", "Nodes", "", "Remotes", "Frame", "ReplicatedStorage", "Holders", "[MyAnimeMine] === Dump start ===", "-", "Players", "Players", "ReplicatedStorage", "core/logger", "", "core/i18n", "Frame", "Workspace", "games/my_anime_mine/auto_upgrade", "ค้นหาฟังก์ชัน...", "games/registry", "games/my_anime_mine/auto_mine", "Debug: Current Plot", "write reported success but ", "Empty miner slot", "  [attr] ", "TextLabel", "[%X] ", "Debug: Dump Sell Cards", "Humanoid", "RemoteFunction", "UserInputService", "RemoteEvent", " = ", "'", "ScreenGui", "[Interaction] ClickDetector clicked: ", "search_placeholder", "[AntiAFK] disabled", "games/my_anime_mine/debug_dump_sell_cards", "ตั้งค่า", "Header", "games/my_anime_mine/auto_chest", "Card", "core/anti_afk", "ขนาดเมนู", "OwnerUserId", "Content", "[AutoMine] disabled", "games/my_anime_mine/get_current_plot", "Backpack", "core/logger", "  ", "Humanoid", "Zones", " (ActionText=", "🏠", "Crew", "[Remotes] === total: ", "games/my_anime_mine/auto_sell", ": ", "games/my_anime_mine/debug_dump", "no tool", " cards", "TextButton", "th", "GuiButton", "Auto Sell", "open_menu_hint", "core/get_leaderstat", "Interaction Logger (debug)", "", "[AutoSell] disabled", "", " (Text=", ")", "not found", " = ", "Remotes", "[AutoChest] CollectAll:FireServer() error: ", "Players", "games/my_anime_mine/debug_dump_remotes", "RunService", "ProximityPrompt", "UIScale", "", "ProximityPrompt", "ClickDetector", "[MyAnimeMine] === Holder: ", "[AutoPlace] disabled", "games/my_anime_mine/get_plot_parts", "My Anime Mine · Mining Hub", "Done", "TextLabel", "UIListLayout", "CoreGui", "อังกฤษ", "core/logger", "[AutoChest] ClaimOre:FireServer() error: ", "MineGui", "[AutoPlace] placed "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(4)] = function()
return function(import)
local log = import(S(347))
local Players = game:GetService(S(212))
local VirtualUser = game:GetService(S(68))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(229))
        connection = player.Idled:Connect(function()
            log(S(71))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(288))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(26)] = function()
return function(import)
    local getPlaceId = import(S(251))
    local registry = import(S(137))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(235))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(319)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(154))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(50)] = function()
local Players = game:GetService(S(330))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(279))
    local backpack = player:WaitForChild(S(152))
    return player, humanoid, backpack
end
return getMe
end
modules[S(129)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(25)] = function()
local Players = game:GetService(S(232))
local STRINGS = {
    en = {
        search_placeholder = S(32),
        open_menu_hint = S(61),
    },
    th = {
        search_placeholder = S(269),
        open_menu_hint = S(197),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(51)
    if locale:sub(1, 2):lower() == S(54) then
        return S(315)
    end
    return S(57)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(166) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(233)] = function()
return function(import)
local Players = game:GetService(S(121))
local Workspace = game:GetService(S(42))
local UserInputService = game:GetService(S(38))
local log = import(S(263))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(184) .. describe(prompt)
            .. S(305) .. prompt.ActionText .. S(325))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(286) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(231) .. describe(button)
            .. S(324) .. tostring(button.Text) .. S(22))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(92)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(203)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(316)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(108))
        return
    end
    connections = {}
    log(S(90))
    local playerGui = Players.LocalPlayer:WaitForChild(S(224))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(333)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(337)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(69)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(219),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(228)] = function()
local LOG_FILE = S(102)
local function log(message)
    local line = os.date(S(277)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(18))
            end
            appendfile(LOG_FILE, line .. S(209))
        elseif writefile then
            local existing = S(323)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(180))
        else
            error(S(59))
        end
        if not (isfile and readfile) then
            error(S(31))
        end
        if not isfile(LOG_FILE) then
            error(S(273) .. LOG_FILE .. S(195))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(153) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(131) .. LOG_FILE .. S(165) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(2)] = function()
return function(import)
local i18n = import(S(122))
local UserInputService = game:GetService(S(281))
local THEME = {
    BrandName = S(159),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(73))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(128))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(43))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(64))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(84)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(207))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(142))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(192)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(75))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(44)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(100))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(46))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(173))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(94))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(140))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(276))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(343))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(24))
            if not valueLbl then
                valueLbl = Instance.new(S(105))
                valueLbl.Name = S(218)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(62))
            if not badge then
                badge = Instance.new(S(52))
                badge.Name = S(86)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(314))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(49))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(176))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(17))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(335)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(28))
        local track = Instance.new(S(191))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(67))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(97))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(253)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(79)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(266))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(344))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(116))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(9)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(285))
    screenGui.Name = S(95)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(345))
    local main = Instance.new(S(248))
    main.Name = S(134)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(334))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(211))
    tabBar.Name = S(118)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(194))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(255))
    header.Name = S(291)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(187))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(148))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(123))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(264)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(172))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(114)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(12))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(259)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(41))
    searchBox.Name = S(113)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(287))
    searchBox.Text = S(321)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(160))
    content.Name = S(297)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(223))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(72))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(77))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(88))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(206)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(91) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(236) .. (item.Subtitle or S(96))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(81))
        footerLabel.Text = i18n.T(S(318))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(89)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(23))
local RunService = game:GetService(S(332))
local getCurrentPlot = import(S(230))
local getPlotParts = import(S(340))
local log = import(S(53))
local FIRE_INTERVAL = 1
local function getRemote(name)
    local remotes = ReplicatedStorage:FindFirstChild(S(254))
    return remotes and remotes:FindFirstChild(name)
end
local autoChest = {}
local connection = nil
function autoChest.SetEnabled(enabled)
    if enabled == autoChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(110))
        return
    end
    local claimOre = getRemote(S(19))
    local collectAll = getRemote(S(138))
    log(string.format(
        S(174),
        tostring(claimOre ~= nil), tostring(collectAll ~= nil)
    ))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        local parts = plot and getPlotParts(plot)
        local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(117), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        if claimOre then
            local ok, err = pcall(function()
                claimOre:FireServer()
            end)
            if not ok then
                log(S(348) .. tostring(err))
            end
        end
        if collectAll then
            local ok, err = pcall(function()
                collectAll:FireServer()
            end)
            if not ok then
                log(S(329) .. tostring(err))
            end
        end
    end)
end
function autoChest.IsEnabled()
    return connection ~= nil
end
return autoChest
end
end
modules[S(216)] = function()
return function(import)
local RunService = game:GetService(S(135))
local getCurrentPlot = import(S(82))
local getPlotParts = import(S(205))
local log = import(S(301))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(33), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(298))
        return
    end
    log(S(155))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(70)] = function()
return function(import)
local Players = game:GetService(S(261))
local RunService = game:GetService(S(190))
local getCurrentPlot = import(S(299))
local getPlotParts = import(S(74))
local log = import(S(217))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(149))
    return pad and pad:FindFirstChildWhichIsA(S(246), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(274)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(339))
        return
    end
    log(S(204))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(157))
        if not character or not backpack then
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(303))
        local tool = backpack:FindFirstChildWhichIsA(S(76))
        if not humanoid or not tool then
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                fireproximityprompt(prompt)
                log(S(350) .. tool.Name .. S(13) .. holder.Name)
                break
            end
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(309)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(256))
local Workspace = game:GetService(S(267))
local RunService = game:GetService(S(213))
local log = import(S(145))
local FIRE_INTERVAL = 2
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(322))
        return
    end
    local remotes = ReplicatedStorage:FindFirstChild(S(328))
    local sellItems = remotes and remotes:FindFirstChild(S(45))
    log(S(239) .. tostring(sellItems ~= nil) .. S(5))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local sellStand = Workspace:FindFirstChild(S(147))
        local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(15), true)
        if prompt then
            fireproximityprompt(prompt)
        end
        if sellItems then
            local ok, err = pcall(function()
                sellItems:FireServer()
            end)
            if not ok then
                log(S(111) .. tostring(err))
            end
        end
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(268)] = function()
return function(import)
local RunService = game:GetService(S(200))
local getCurrentPlot = import(S(185))
local getPlotParts = import(S(181))
local log = import(S(169))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(139))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(107))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(103))
        return
    end
    log(S(179))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(177)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(112)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(220) .. child.Name .. S(104)
        if child:IsA(S(336)) then
            line = line .. string.format(
                S(48),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(146)) then
            line = line .. string.format(S(161), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(199)) or child:IsA(S(280)) then
            line = line .. S(202)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(37) .. attrName .. S(283) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(302))
    end
end
return dumpInstance
end
modules[S(331)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(262))
local log = import(S(227))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(282)) or descendant:IsA(S(245)) then
            count = count + 1
            log(S(171) .. descendant.ClassName .. S(310) .. descendant:GetFullName())
        end
    end
    log(S(308) .. count .. S(170))
    return count
end
return dumpRemotes
end
end
modules[S(289)] = function()
return function(import)
local Players = game:GetService(S(99))
local log = import(S(178))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(27))
    local mineGui = playerGui and playerGui:FindFirstChild(S(349))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(10))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(242)) and sellFrame.Content:FindFirstChild(S(98))
    if not scroll then
        log(S(210))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(293) or card:IsA(S(115)) then
            count = count + 1
            log(S(60) .. count .. S(125) .. card.ClassName .. S(35) .. card.Name .. S(188))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(275) .. attrName .. S(93) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(193) .. child.ClassName .. S(240) .. child.Name .. S(284)
                if child:IsA(S(182)) then
                    line = line .. S(327) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(234) .. count .. S(124))
    return count
end
return dumpSellCards
end
end
modules[S(186)] = function()
local Players = game:GetService(S(260))
local Workspace = game:GetService(S(144))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(214))
    local plots = mineWorld and mineWorld:FindFirstChild(S(66))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(296)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(20)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(257)),
        Nodes = plot:FindFirstChild(S(252)),
        Chest = plot:FindFirstChild(S(34)),
        Zones = plot:FindFirstChild(S(304)),
        Crew = plot:FindFirstChild(S(307)),
        Loot = plot:FindFirstChild(S(150)),
        PlotIndex = plot:GetAttribute(S(208)),
        ChestCapacity = plot:GetAttribute(S(143)),
    }
end
return getPlotParts
end
modules[S(40)] = function()
return function(import)
    local Players = game:GetService(S(156))
    local createWindow = import(S(163))(import)
    local i18n = import(S(265))
    local antiAfk = import(S(294))(import)
    local interactionLogger = import(S(162))(import)
    local log = import(S(189))
    local window = createWindow({
        Subtitle = S(341),
    })
    local getCurrentPlot = import(S(83))
    local getPlotParts = import(S(132))
    local dumpInstance = import(S(311))
    local dumpRemotes = import(S(237))(import)
    local dumpSellCards = import(S(196))(import)
    local autoMine = import(S(271))(import)
    local autoPlace = import(S(222))(import)
    local autoChest = import(S(292))(import)
    local autoSell = import(S(167))(import)
    local autoUpgrade = import(S(201))(import)
    local farmTab = window:AddTab(S(141), S(306))
    farmTab:AddCard(
        S(243),
        S(3)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(130),
        S(238)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(183),
        S(133)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(317),
        S(247)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(47),
        S(39)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(272), S(7))
    local checkBtn
    checkBtn = debugCard:AddButton(S(29), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(326)
            log(S(85))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(63),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(119),
        S(244)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(8), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(55)
            return
        end
        local ok, err = log(S(258))
        if not ok then
            dumpBtn.Text = S(168)
            dumpCard:SetSubtitle(S(164) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(136) .. firstNode.Name .. S(11))
            dumpInstance(firstNode, log)
        else
            log(S(175))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(338) .. firstHolder.Name .. S(221))
            dumpInstance(firstHolder, log)
        else
            log(S(226))
        end
        dumpBtn.Text = S(249)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(65),
        S(6)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(16), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(300))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(30))
        if not tool then
            toolDumpBtn.Text = S(312)
            return
        end
        log(S(151) .. tool.Name .. S(58))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(106) .. attrName .. S(215) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(342)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(78),
        S(198)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(120), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(1)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(278),
        S(250)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(126), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(313)
    end)
    local settingsTab = window:AddTab(S(290), S(127))
    settingsTab:AddCard(S(109), S(80)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(320),
        S(225)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(295), S(101), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(87), S(346), S(14) }, 1, function(index, value)
        local code = ({ S(241), S(56), S(21) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(270)] = function()
return {
    [79389059854988] = S(158),
}
end
import(S(36))(import)
