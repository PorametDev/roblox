-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"getcallbackvalue", "nothing to place or upgrade", "autoClaimMaxPerPass", "TextButton", "ขายทุกกี่วินาที", ") ", "autoUpgradePriority", "TextLabel", ") needs ", "[AutoSell] MineSellItems ", "ItemFrame", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "MekPro", "UIScale", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "GuiObject", "Workspace", "autoPlace", "ถูกสุดก่อน", "Auto Mine: คลิกต่อรอบ", "VirtualUser", "games/my_anime_mine/auto_place", "[Interaction] logger enabled - go click/interact with things now", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "MineSpeed", "Frame", "ChestCapacity", "M", "write reported success but ", " ", "ClickDetector", "disabled", "sold ", "x", "th", ": ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "core/logger", "Chest", "[Decompile] decompile unavailable in this executor", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "core/ui", "[AutoPlace] slot ", "works from anywhere - no teleport needed", "s", "บิน (Fly)", "core/parse_money", "core/debug_dump_remotes", "MineBuyPickaxe", "TextLabel", "อ่านป้ายบน chest ทุกกี่วินาที", "GetHolderMineSpeedLevel", "lowest", ") ", "ValueLabel", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "[AutoResearch] ", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "AutoMine", "MinePower", "BasePart", "getgc", ": ", "Container", "'", "languageIndex", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " from ", "Dump", "Text", "Auto Sell: รอคิวได้นานสุด", "[AutoPlace] holders: ", ")", "core/get_place_id", "TextButton", "Speed", " screen(s)) ===", "games/my_anime_mine/get_plot_parts", ")", " doesn't exist", "BasePart", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "autoUpgradeMutationBudget", " - upgrade the miners first", "games/my_anime_mine/game_api", "jumpPower", "autoResearch", "Hauler", " pickaxe ", "[AutoClaim] claimed ", " = ", "games/registry", "hookfunction", "core/get_place_id", "[Logger] failed to write ", "core/dispatch", "auto", "table", "function", "CoreUI", " reward(s) looked claimable but none went through", "", " (", "[Bb]uy", "[AutoResearch] ", "", " (chest holds ", " -> ", "/", "autoSell", "VirtualInputManager", "TextLabel", "core/logger", "PlotIndex", "Modules", "แพงสุดก่อน", "/", "autoResearchKeepMoney", " waited ", " ", "This is not a supported place.", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Main", "autoUpgrade", "may need us at the stand - trying that", "PlayerGui", "MineGui", "sale didn't go through", "autoClaim", "[AutoPlace] fireproximityprompt error: ", "s", " -> ", "Auto Claim: เก็บสูงสุดต่อรอบ", ")", "games/my_anime_mine/auto_mine", "ReplicatedStorage", "AutoClaim", "[AutoChest] MineTakeChest had no effect (", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "GetCharacterSpeedUpgradeCost", "M", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "[AutoUpgrade] ", "core/logger", "Holders", "", "getscripts", "games/my_anime_mine/auto_upgrade", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ")", "TextLabel", "ScreenGui", "GetCharacterUpgradeCost", "none", " ", " mutation x", "disabled", "LayerCollector", "Mining", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "ย่อขยายเมนูทั้งชุด", "core/humanoid", "games/my_anime_mine/get_current_plot", "[AntiAFK] idle detected, simulating input", "UICorner", "", "RunService", "no isfile/readfile available to verify the write", "TextButton", "core/debug_tab", " '", "[GameAPI] SendEvent failed: ", "Frame", "walkSpeed", "core/logger", "core/i18n", "HumanoidRootPart", "[AutoResearch] cheapest open node: ", "ProximityPrompt", "Auto Claim: รอบเช็ค", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "ScrollingFrame", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "Place", " (money ", "[Interaction] Button activated: ", "[^%d%.]", "[AutoChest] waiting: ", "fly", "languageIndex", " -> x", "TextLabel", "core/config", "UIStroke", "ไทย", "[Decompile] === start ===", " (Text=", "core/teleport", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "table", "BasePart", "games/my_anime_mine/auto_upgrade", "waiting", "[AutoPlace] ", "autoSellInterval", "autoClaimInterval", ")", "games/my_anime_mine/get_current_plot", "ขนาดเมนู", " ", "[AutoUpgrade] ", "%$%s*([%d,%.]+)%s*(%a*)", "", "ProximityPrompt", "Humanoid", "[Uu]nlock", " ", "string", ")", "yes", "s", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "[AutoUpgrade] pickaxe #", "autoUpgradeLimitPickaxes", "/", "menuScale", " (power ", "TextLabel", "Interaction Logger", "🏠", "", ") on ", "_", "All", "Auto Place Miner", "=", "CharMaxLevel", "\n", "Auto Research: ซื้อสาย Zone", "Mining", "neither appendfile nor writefile is available", "autoSellMaxWait", "GetHolderSpeedLevel", "slot ", "x", "%$", "autoResearchCore", ") ", "unreadable", "games/my_anime_mine/auto_mine", " stack(s)", "'", "getscriptbytecode", "UIListLayout", " = ", "core/anti_afk", "string", "games/my_anime_mine/auto_research", "AutoUpgrade", "/", "autoPlaceUpgradeWeak", "s", "fill", "", "s", "autoResearchCoreLast", "[GuiDump] === visible PlayerGui tree ===", "Anti-AFK", ": ", "MineSpeed", "core/teleport", " (power ", "PlayerScripts", "Auto Upgrade: รอบเช็ค", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "Auto Upgrade Unit", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "RemoteEvent", "Frame", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[Interaction] logger disabled", "Frame", "Auto Chest: เกณฑ์เก็บ", " -> ", "Zones", "  ", "x", "lowest", "ProximityPrompt", "core/logger", "[Interaction] ProximityPrompt triggered: ", ",", "TextButton", "อังกฤษ", "AutoResearch", "MineRollMutation", "[AutoResearch] ", "core/debug_decompile", "autoResearchZones", "core/dispatch", "(nothing to do)", "function", "core/scheduler", "autoUpgradeMutationTarget", "Crew", "Damage", "RunService", "core/i18n", "^%$", "Players", "Frame", "waiting", "auto", ")", " | ", "Auto Research (Plot)", "getloadedmodules", "[AutoPlace] ", "autoMine", "TextLabel", "autoChest", "core/config", "-", "autoUpgradePickaxeIndex", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "games/my_anime_mine/get_best_miner", "", "autoResearchInterval", "Players", " holder(s) - not buying more", "Auto Research: รอบเช็ค", "TextButton", "function", " not bought yet (", "core/debug_interaction_logger", "s - jumping the queue", "Text", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "Frame", "Frame", "[AutoUpgrade] bought pickaxe #", "games/my_anime_mine/game_api", "GuiService", ": ", "empty", "table", "[Interaction] ClickDetector clicked: ", "[Config] couldn't read ", " screens", "games/my_anime_mine/auto_chest", "dev", "Auto Sell: รอบขาย", "⚙", "[Scheduler] ", "เดินทะลุกำแพงและพื้น", "Auto Upgrade: สุ่ม Mutation", " (", "en", "HttpService", "[GuiDump] PlayerGui not found", "Nodes", " files", "CharMineSpeedMaxLevel", "Dump", "Dump All Remotes", "core/logger", "Auto Sell", " (cost ", "[^%.]+", "core/logger", " script(s) to ", " -> ", "Auto Upgrade: กันเงินไว้", "UserInputService", "search_placeholder", "[AutoUpgrade] ", "0 = ไม่จำกัด", "autoUpgradePickaxeIndex", "core/logger", "ตั้งค่า", "games/registry", " failed)", "interactionLogger", ": ", "th", "[MekPro] settings file: ", "table", " ===", "My Anime Mine · Mining Hub", "UserInputService", "GuiButton", "PlayerGui", "games/my_anime_mine/main", "Badge", "ModuleScript", "unreadable", "MekPro", "[Decompile] wrote ", ")", " x", "Workspace", "TextLabel", "", ": ", "no", "getsenv", "autoResearchMaxPerPass", "autoResearchCheapestFirst", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "🐛", "TextLabel", "table", "  [attr] ", "[Decompile] PlayerScripts not found", "TextLabel", "games/my_anime_mine/game_api", "[Decompile] === end ===", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "TextButton", "Content", " remotes", "/", " power ", "BodyVelocity", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "HotbarFrame", "?", "autoUpgradeMaxPerPass", "ClickDetector", ")", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "Content", "core/logger", "M", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "LocalScript", "[AutoClaim] ", "Workspace", "Damage", "auto", "Players", "x", "", "autoChestMinItems", "GetCharacterMineSpeedUpgradeCost", "Decompile Client Scripts", "cost", "games/my_anime_mine/auto_chest", "games/my_anime_mine/get_plot_parts", "x", "[Interaction] Key pressed: %s (gameProcessed=%s)", "write reported success but the line isn't in ", "[Decompile] ", "autoChestInterval", "MineTakeChest", " still empty after placing ", "", "", "TextButton", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "[GameAPI] couldn't load the game's modules: ", "UIListLayout", "core/logger", "/s", "core/humanoid", "(", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/game_api", "locked slot", "", " while selecting", "s", "AutoChest", "core/scheduler", ".lua", "core/debug_decompile", "#", "OwnerUserId", " node(s) looked buyable but the server refused", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "GetHolderLevel", "Mutation: หยุดเมื่อได้ x", "getnilinstances", "ต่ำสุดก่อน", "ตามเครื่อง", "maxed", "Badge", "core/logger", "ReplicatedStorage", "TextLabel", "Players", " (x", "[AutoClaim] ", "[AutoMine] ", "Players", "empty", "games/my_anime_mine/auto_sell", "table", "number", "Auto Place: รอบเช็ค", " ", "Auto Research: ซื้อสูงสุดต่อรอบ", "BodyPosition", "core/logger", "ปุ่มเปิดเมนู", "[AutoResearch] ", " (prompt says '", "auto", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "HumanoidRootPart", "cost", "disabled", "[AutoUpgrade] MineBuyPickaxe ", "menuScale", "core/get_me", "[AutoPlace] couldn't pick up ", " ", "costs ", "games/my_anime_mine/game_api", "Auto Place: เปลี่ยนตัวอ่อนออก", "Frame", "[AutoUpgrade] ", "TextBox", "UIPadding", "MineSpeed", " ", "en", "table", "core/logger", "", "/config.json", " -> ", " (", "autoMineSpeed", "[AutoChest] collected ", " (", " (", "TextLabel", "Frame", "JumpPower (ปกติ 50)", "games/my_anime_mine/auto_claim", "works from anywhere - dropping the teleport", "core/ui", ",", "Players", "'", "MineSellItems", "Speed", "leaderstats", "string", "enabled", "table", "s", "Damage", " ", "", "ClickDetector", "action '", "PickaxesStand", "core/logger", "Auto Research: กันเงินไว้", "core/logger", "MekPro", "debug.getupvalues", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "table", "core/debug_tab", "[Remotes] ", " (Text=", "debug.getproto", "ItemName", "^M(%d+)$", "MekPro/decompiled", "autoUpgradeRollMutation", "ProximityPrompt", "", "core/anti_afk", "noclip", "Loot", "%.0f", "th", "[%X] ", "CharSpeedMaxLevel", "core/debug_dump_gui", "=empty", "", "Run", "disabled", "\n", "en", "Open menu", "core/build_env", "decompile", "[GuiDump] === end (", "string", "ไล่อัปเกรด miner ทุกกี่วินาที", "search_placeholder", "float", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Auto Upgrade: Pickaxe ที่จะซื้อ", "games/my_anime_mine/main", "", "^([%d,]+)$", "enabled", "^x(%d+)$", "Players", "games/my_anime_mine/auto_place", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", ")", "Content", " (x", "open_menu_hint", "[AutoPlace] picked up ", "Auto Research: ซื้อสาย Core", "[AutoResearch] nothing open and affordable right now", "games/my_anime_mine/auto_sell", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "MineUpgradeCharacter", "Auto Claim (Index)", "Damage", "", "core/debug_interaction_logger", "antiAfk", "Players", "HumanoidRootPart", " #", "AutoSell", "autoUpgradeEquipPickaxe", "MineEquipSlot", "/", " error: ", "autoPlaceInterval", "needs us at the stand - keeping the teleport", "core/i18n", "Plots", "core/teleport", "autoMineClicks", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " (", "fill", " (ActionText=", "AutoPlace", "  ", "Frame", "=locked(", "games/my_anime_mine/auto_research", "games/my_anime_mine/game_api", "[AutoUpgrade] every stat on every miner is maxed", "TextButton", "table", "autoUpgradePickaxe", "games/my_anime_mine/get_plot_parts", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "", "TextLabel", "enabled", ")", "open_menu_hint", "Humanoid", "autoUpgradeInterval", "[GuiDump] ", "core/click", "table", "core/logger", "[Config] couldn't write ", " ชิ้น", "ไล่ซื้อทุกกี่วินาที", "Players", "Hauler", "Backpack", "[AutoPlace] disabled", ") -> ", "ฟาร์ม", "table", "SellStand", "Shop Stand", "[AutoPlace] upgrading ", " owned)", "Auto Upgrade: แจก Pickaxe ให้ miner", " pickaxe(s) for ", "Dump Visible GUI", "[AutoChest] disabled", "X", "')", "prompt disabled", "[AutoSell] enabled", "[AutoChest] chest is empty", "core/debug_dump_gui", "Players", "TextButton", "en-us", "Empty miner slot", "Pad", "[AutoResearch] Core ", "Auto Chest", "RemoteFunction", "games/my_anime_mine/get_current_plot", "PlayerGui", "Auto Chest: รอบเช็ค", "debug.getconstants", "[BestMiner] slot ", "core/teleport", "[AutoUpgrade] ", "Search", "[AutoSell] ", "autoSellMaxWait", "[Scheduler] turn: ", "table", "[AutoChest] can't read the chest yet", "button has no size on screen", "[AutoChest] enabled (collects once >= ", "Auto Mine", "/", "TextLabel", ")", "th", "Search functions...", "GuiButton", "Frame", "Workspace", "MekPro.log", "ดีบัก", "CoreGui", "[AntiAFK] disabled", "Speed", "[AutoSell] disabled", "", " (", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " '", "UIListLayout", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "[AutoPlace] placed ", "UserInputService", "TabBar", "[^%w]+", "[Remotes] === total: ", "UICorner", "WalkSpeed (ปกติ 16)", "string", "core/get_place_id", "Header", ") from ", " (", " items)", " didn't go through (out of stock or too expensive)", " costs ", "Players", "Mutation: เพดานราคาต่อครั้ง", "getconnections", "ความเร็ววิ่ง", "games/my_anime_mine/game_api", "TextLabel", "core/build_env", "enabled", "getrenv", "%", ")", "autoUpgradeKeepMoney", "games/my_anime_mine/game_api", "MineEquipPickaxe", "Workspace", "ค้นหาฟังก์ชัน...", "lowest", "core/debug_dump_remotes", "MineWorld", "core/logger", ") didn't take, trying the next one", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "ลอย (Float)", "BasePart", "[AutoPlace] nothing in the hotbar to place", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "core/get_leaderstat", "TextLabel", "[AntiAFK] enabled - watching Player.Idled", "[Decompile] === executor capabilities ===", "Auto Mine: ความเร็ว", " items", "upgrade", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", " ", "MekPro", "MineBuySkill", "มุดแมพ (Noclip)", "Locked miner slot", "games/my_anime_mine/auto_claim", "แรงกระโดด", "ValueLabel", " items (now ", "", " team power, you have ", "core/logger", "Players", "[AutoPlace] drifted out of range of ", "[Scheduler] ", "Auto Upgrade: ซื้อสูงสุดต่อรอบ"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(257)] = function()
return function(import)
local log = import(S(371))
local Players = game:GetService(S(796))
local VirtualUser = game:GetService(S(21))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(778))
        connection = player.Idled:Connect(function()
            log(S(165))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(725))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(756)] = function()
return { Env = S(352), IsDev = true }
end
modules[S(663)] = function()
local VirtualInputManager = game:GetService(S(111))
local GuiService = game:GetService(S(344))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(711)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(323)] = function()
return function(import)
local HttpService = game:GetService(S(360))
local getPlaceId = import(S(743))
local log = import(S(505))
local ROOT_FOLDER = S(398)
local placeFolder = ROOT_FOLDER .. S(226) .. tostring(getPlaceId())
local filePath = placeFolder .. S(532)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(499) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(349) .. filePath .. S(345) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(666) .. filePath .. S(36) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(477)] = function()
return function(import)
local Players = game:GetService(S(625))
local log = import(S(561))
local CAPABILITIES = {
    S(594), S(254), S(147), S(62), S(318),
    S(752), S(1), S(407), S(758), S(701),
    S(565), S(571), S(93), S(484),
}
local OUT_FOLDER = S(574)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(370)) do
        if type(target) ~= S(413) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(779))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(454) .. name .. S(256) .. ((ok and type(value) == S(303)) and S(221) or S(406)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(40))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(274))
    if not playerScripts then
        log(S(415))
        return 0
    end
    pcall(makefolder, S(13))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(437)) or target:IsA(S(396)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(596) and #source > 0 then
                local name = target:GetFullName():gsub(S(738), S(234))
                if pcall(writefile, OUT_FOLDER .. S(714) .. name .. S(476), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(399) .. written .. S(372) .. OUT_FOLDER
        .. S(534) .. failed .. S(383))
    return written
end
local function run()
    log(S(197))
    reportCapabilities()
    local count = dumpAll()
    log(S(418))
    return count
end
return run
end
end
modules[S(585)] = function()
return function(import)
local Players = game:GetService(S(442))
local log = import(S(113))
local function dumpVisible(root, indent)
    indent = indent or S(328)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(16)) and child.Visible then
            local line = indent .. child.ClassName .. S(731) .. child.Name .. S(65)
            if child:IsA(S(491)) or child:IsA(S(75)) then
                line = line .. S(198) .. tostring(child.Text) .. S(760)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(414) .. attrName .. S(91) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(287))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(126))
    if not playerGui then
        log(S(361))
        return 0
    end
    local screens = 0
    log(S(268))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(158)) and screen.Enabled and screen.Name ~= S(564) then
            screens = screens + 1
            log(S(662) .. screen.ClassName .. S(172) .. screen.Name .. S(253))
            dumpVisible(screen, S(644))
        end
    end
    log(S(595) .. screens .. S(77))
    return screens
end
return dumpGui
end
end
modules[S(48)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(136))
local log = import(S(367))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(279)) or descendant:IsA(S(697)) then
            count = count + 1
            log(S(569) .. descendant.ClassName .. S(405) .. descendant:GetFullName())
        end
    end
    log(S(739) .. count .. S(389))
    return count
end
return dumpRemotes
end
end
modules[S(336)] = function()
return function(import)
local Players = game:GetService(S(546))
local Workspace = game:GetService(S(17))
local UserInputService = game:GetService(S(736))
local log = import(S(795))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(292) .. describe(prompt)
            .. S(642) .. prompt.ActionText .. S(208))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(348) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(187) .. describe(button)
            .. S(570) .. tostring(button.Text) .. S(79))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(180)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(31)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(719)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(282))
        return
    end
    connections = {}
    log(S(23))
    local playerGui = Players.LocalPlayer:WaitForChild(S(393))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(290)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(430)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(392)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(452),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(171)] = function()
return function(import)
local interactionLogger = import(S(623))(import)
local dumpRemotes = import(S(767))(import)
local dumpGui = import(S(689))(import)
local decompileScripts = import(S(299))(import)
local humanoid = import(S(163))
local function build(window, persist)
    local tab = window:AddTab(S(723), S(411))
    persist.Toggle(
        tab:AddCard(
            S(230),
            S(182)
        ),
        S(384),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(366),
        S(200)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(69), function()
        remoteBtn.Text = dumpRemotes() .. S(422)
    end)
    local guiCard = tab:AddCard(
        S(682),
        S(184)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(365), function()
        guiBtn.Text = dumpGui() .. S(350)
    end)
    local decompileCard = tab:AddCard(
        S(447),
        S(37)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(588), function()
        decompileBtn.Text = decompileScripts() .. S(363)
    end)
    humanoid.Start()
    persist.Slider(tab, S(753), S(741),
        8, 200, S(175), 16, humanoid.SetWalkSpeed, S(232))
    persist.Slider(tab, S(790), S(541),
        30, 300, S(86), 50, humanoid.SetJumpPower, S(728))
    persist.Toggle(
        tab:AddCard(S(46), S(426)),
        S(190), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(787), S(356)),
        S(579), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(772), S(771)),
        S(599), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(96)] = function()
return function(import)
    local getPlaceId = import(S(94))
    local registry = import(S(382))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(121))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(776)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(550))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(516)] = function()
local Players = game:GetService(S(496))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(216))
    local backpack = player:WaitForChild(S(671))
    return player, humanoid, backpack
end
return getMe
end
modules[S(74)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(466)] = function()
local Players = game:GetService(S(311))
local RunService = game:GetService(S(168))
local UserInputService = game:GetService(S(391))
local Workspace = game:GetService(S(439))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(660))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(626))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(425))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(202)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(61)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(504))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(177)] = function()
local Players = game:GetService(S(330))
local STRINGS = {
    en = {
        search_placeholder = S(718),
        open_menu_hint = S(592),
    },
    th = {
        search_placeholder = S(765),
        open_menu_hint = S(506),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(692)
    if locale:sub(1, 2):lower() == S(717) then
        return S(582)
    end
    return S(528)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(509) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(563)] = function()
local LOG_FILE = S(722)
local function log(message)
    local line = os.date(S(583)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(622))
            end
            appendfile(LOG_FILE, line .. S(239))
        elseif writefile then
            local existing = S(404)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(590))
        else
            error(S(242))
        end
        if not (isfile and readfile) then
            error(S(169))
        end
        if not isfile(LOG_FILE) then
            error(S(29) .. LOG_FILE .. S(80))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(453) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(95) .. LOG_FILE .. S(385) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(47)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(742) then
        return nil
    end
    local number, suffix = text:match(S(213))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(293), S(471))))
    if not value then
        return nil
    end
    if suffix ~= S(102) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(475)] = function()
return function(import)
local RunService = game:GetService(S(308))
local log = import(S(144))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(355) .. task_.name .. S(632) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(798) .. starved.name .. S(119)
            .. string.format(S(581), now - starved.waitingSince)
            .. S(337))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(708) .. (currentTask and currentTask.name or S(302)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(272)] = function()
local Players = game:GetService(S(492))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(511))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(42)] = function()
return function(import)
local i18n = import(S(309))
local UserInputService = game:GetService(S(375))
local THEME = {
    BrandName = S(785),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(166))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(740))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(525))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(294))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(531)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(195))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(412))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(428)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(112))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(214)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(183))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(732))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(777))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(283))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(229))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(755))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(416))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(791))
            if not valueLbl then
                valueLbl = Instance.new(S(403))
                valueLbl.Name = S(55)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(488))
            if not badge then
                badge = Instance.new(S(151))
                badge.Name = S(395)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(460))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(720))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(341))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(333))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(459)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(759)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(26))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(645))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(650))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(167)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(522))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(255))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(691))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(338)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(152))
    screenGui.Name = S(100)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(724))
    local main = Instance.new(S(174))
    main.Name = S(123)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(14))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(312))
    tabBar.Name = S(737)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(463))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(280))
    header.Name = S(744)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(420))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(50))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(539))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(557)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(4))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(684)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(170))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(324)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(524))
    searchBox.Name = S(705)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(598))
    searchBox.Text = S(655)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(340))
    content.Name = S(421)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(540))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(193))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(659))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(656))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(70)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(146) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(218) .. (item.Subtitle or S(793))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(376))
        footerLabel.Text = i18n.T(S(613))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(351)] = function()
return function(import)
local gameApi = import(S(469))(import)
local log = import(S(665))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(712) .. minItemsToCollect .. S(747))
    else
        log(S(683))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(397) then
            log(S(710))
            lastReported = S(250)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(346) then
            log(S(688))
            lastReported = S(497)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(204) then
            local capacity = gameApi.ChestCapacity()
            log(S(189) .. count .. S(109) .. want .. S(781)
                .. (capacity and (S(107) .. capacity .. S(134)) or S(603)))
            lastReported = S(313)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(536) .. count .. S(792) .. after .. S(315))
        else
            log(S(138) .. count
                .. S(132) .. tostring(after) .. S(610))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(789)] = function()
return function(import)
local gameApi = import(S(648))(import)
local log = import(S(530))
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(494) .. (enabled and S(605) or S(32)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(90) .. reward.category .. S(211)
                .. tostring(reward.tierKey) .. S(627) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(438) .. #pending .. S(101))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    task.spawn(runJob)
end
return autoClaim
end
end
modules[S(251)] = function()
return function(import)
local getCurrentPlot = import(S(698))
local getPlotParts = import(S(450))
local log = import(S(380))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(558), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(495) .. (enabled and S(657) or S(157)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(608)] = function()
return function(import)
local Players = game:GetService(S(607))
local getCurrentPlot = import(S(164))
local getPlotParts = import(S(653))
local getMinerSlots = import(S(468))(import)
local teleport = import(S(199))
local gameApi = import(S(754))(import)
local log = import(S(291))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(693)
local LOCKED_SLOT_TEXT = S(788)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(694))
    return pad and pad:FindFirstChildWhichIsA(S(576), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(8)) then
            local digits = descendant.Text:match(S(604))
            if digits then
                return tonumber((digits:gsub(S(545), S(577))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(715)) and descendant.Text:match(S(310)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(245) .. holder.Name .. S(335)
            .. gameApi.MinerSlots() .. S(423) .. gameApi.MaxMinerSlots() .. S(679)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(470)
    end
    if not prompt.Enabled then
        return true, S(686)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(519) .. price
    end
    local action = prompt.ActionText or S(444)
    if action:match(S(217)) or action:match(S(104)) or action:match(S(247)) then
        return true, S(559) .. action .. S(547)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(178))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(130) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(646) .. why .. S(400))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(586))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(237) .. prompt.ObjectText .. S(467) .. power .. S(73))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(316))
    if line ~= lastSurvey then
        log(S(72) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(264), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(782), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(319) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(672))
    else
        lastAttempt = 0
        log(S(339))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(410),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(185), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(774))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(797) .. holder.Name .. S(472))
            return false
        end
        log(S(43) .. candidate.slot .. S(358) .. candidate.name
            .. S(770))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(205) .. holder.Name .. S(457) .. miner.name)
        return false
    end
    log(S(735) .. miner.name .. S(273) .. miner.power
        .. S(233) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(517) .. entry.name .. S(68) .. entry.holder.Name
            .. S(508) .. tostring(entry.prompt.ActionText) .. S(685))
        return false
    end
    log(S(614) .. entry.name .. S(228) .. entry.power
        .. S(745) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(2))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(641) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(678) .. target.holder.Name .. S(63) .. target.name
        .. S(640) .. target.power .. S(673) .. best.name .. S(746) .. best.power .. S(716))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(259)] = function()
return function(import)
local gameApi = import(S(520))(import)
local log = import(S(38))
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(105) .. (enabled and S(757) or S(513)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(507) .. node.zoneKey .. S(784) .. node.id
                    .. S(103) .. node.family .. S(54) .. node.level .. S(533) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(695) .. node.id
                    .. S(537) .. node.family .. S(6) .. node.level .. S(373) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(298) .. blocked
                .. S(480))
        elseif powerBlocked then
            log(S(57) .. powerBlocked.zoneKey .. S(518) .. powerBlocked.id
                .. S(538) .. powerBlocked.family .. S(9)
                .. math.floor(powerBlocked.powerReq) .. S(794)
                .. math.floor(powerBlocked.power)
                .. S(84))
        else
            log(S(616)
                .. S(186) .. math.floor(gameApi.GetMoney()) .. S(150))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(179) .. nearest.zoneKey
                    .. S(527) .. nearest.id .. S(729) .. nearest.family .. S(249)
                    .. nearest.level .. S(631) .. nearest.max
                    .. S(749) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    if #zoneNodes > 0 then
        return true
    end
    if not buyCore then
        return false
    end
    if coreLastResort and #zoneNodes > 0 then
        return false
    end
    return #buyableCoreNodes() > 0
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(617)] = function()
return function(import)
local Workspace = game:GetService(S(402))
local gameApi = import(S(762))(import)
local teleport = import(S(637))
local log = import(S(464))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(727))
    else
        lastAttempt = 0
        log(S(687))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(676))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(215), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(10)
                .. (sold and S(543)
                         or S(634)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(706) .. (sold and (S(33) .. #keys .. S(252)) or S(128)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(203)] = function()
return function(import)
local Workspace = game:GetService(S(721))
local gameApi = import(S(343))(import)
local teleport = import(S(703))
local log = import(S(176))
local STATS = { S(307), S(549), S(526) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(766)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(523) .. (enabled and S(552) or S(589)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(529) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(53) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(448) or S(487)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(377) .. holderKey .. S(502) .. choice.stat .. S(556)
                        .. choice.info.level .. S(285) .. after.level
                        .. S(117) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(512) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(560))
    local shop = stand and stand:FindFirstChild(S(677))
    if not shop then
        return nil
    end
    if shop:IsA(S(773)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(81), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(514)
                .. (bought and S(44)
                           or S(125)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(461))
        end
    end
    if bought then
        log(S(342) .. pickaxe.index .. S(155) .. pickaxe.name
            .. S(612) .. pickaxe.multiplier .. S(431))
        return true
    end
    log(S(224) .. pickaxe.index .. S(30) .. pickaxe.name
        .. S(748))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(212) .. holderKey .. S(89)
                .. tostring(entry and entry.Pickaxe or S(154)) .. S(108) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(143) .. holderKey .. S(156) .. variant
                .. S(192) .. after .. S(369) .. cost .. S(658))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(704) .. gameApi.OwnedPickaxeCount()
                    .. S(681) .. gameApi.MinerSlots()
                    .. S(331))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(432))
        elseif maxed > 0 then
            log(S(649))
        else
            log(S(654))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(664) and entry.Pickaxe and entry.Pickaxe ~= S(265) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(417)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(490))
local log = import(S(769))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(115), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(462) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(651))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(201) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(670) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(709) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(388) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(88) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(573))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(173) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(456))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(235), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(98) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(548), keys)
end
function api.EquipSlot(key)
    return send(S(630), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(49), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(763), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(297), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(786), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(675)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(750)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(60))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(334) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.GetSkillNodes(zoneKey) or {}) do
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            table.insert(list, {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            })
        end
    end)
    return list
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(99) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(619), characterKey, stat == S(159) and S(25) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(482), max = S(238), cost = S(153) },
    Speed = { level = S(244), max = S(584), cost = S(140) },
    MineSpeed = { level = S(52), max = S(364), cost = S(446) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(567) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(500) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(551) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(347) and type(holder.CharName) == S(219) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(553) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(258) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(327)] = function()
return function(import)
local Players = game:GetService(S(669))
local log = import(S(489))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(699))
    local mineGui = playerGui and playerGui:FindFirstChild(S(127))
    local hotbar = mineGui and mineGui:FindFirstChild(S(427))
    local content = hotbar and hotbar:FindFirstChild(S(611))
    return content and content:FindFirstChild(S(64))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(321)) then
            local count = descendant.Text:match(S(606))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(11))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(433))
    local nameLabel = content and content:FindFirstChild(S(572))
    local damageLabel = content and content:FindFirstChild(S(440))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(106) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(188), S(587)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(261) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(702) .. slot .. S(270) .. info.name
                        .. S(424) .. info.power .. S(401) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(209)] = function()
local Players = game:GetService(S(690))
local Workspace = game:GetService(S(764))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(768))
    local plots = mineWorld and mineWorld:FindFirstChild(S(636))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(479)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(78)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(145)),
        Nodes = plot:FindFirstChild(S(362)),
        Chest = plot:FindFirstChild(S(39)),
        Zones = plot:FindFirstChild(S(286)),
        Crew = plot:FindFirstChild(S(306)),
        Loot = plot:FindFirstChild(S(580)),
        PlotIndex = plot:GetAttribute(S(114)),
        ChestCapacity = plot:GetAttribute(S(27)),
    }
end
return getPlotParts
end
modules[S(394)] = function()
return function(import)
    local createWindow = import(S(544))(import)
    local i18n = import(S(635))
    local antiAfk = import(S(578))(import)
    local buildEnv = import(S(593))
    local scheduler = import(S(304))(import)
    local config = import(S(194))(import)
    local log = import(S(434))
    local window = createWindow({
        Subtitle = S(390),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(135))(import)
    local autoPlace = import(S(22))(import)
    local autoChest = import(S(449))(import)
    local autoSell = import(S(498))(import)
    local autoUpgrade = import(S(148))(import)
    local autoResearch = import(S(647))(import)
    local autoClaim = import(S(542))(import)
    local gameApi = import(S(85))(import)
    scheduler.RegisterStandalone(S(59), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(474), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(643), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(260), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(296), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(137), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(707), 20)
    scheduler.RegisterExclusive(S(628), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(674), S(231))
    persistentToggle(farmTab:AddCard(
        S(713),
        S(122)
    ), S(320), autoMine.SetEnabled)
    persistentSlider(farmTab, S(780), S(24),
        1, 30, S(535), 5, autoMine.SetCyclesPerSecond, S(465))
    persistentSlider(farmTab, S(20), S(609),
        1, 10, S(638), 1, autoMine.SetClicksPerCycle, S(246))
    persistentToggle(farmTab:AddCard(
        S(236),
        S(730)
    ), S(18), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(501), S(734),
        1, 60, S(633), 3, autoPlace.SetCheckInterval, S(131))
    persistentToggle(
        farmTab:AddCard(S(521),
            S(139)),
        S(262), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(696),
        S(67)
    ), S(322), autoChest.SetEnabled)
    persistentSlider(farmTab, S(284), S(326),
        50, 3000, S(445), 100, autoChest.SetMinItems, S(667))
    persistentSlider(farmTab, S(700), S(51),
        1, 60, S(455), 3, autoChest.SetCheckInterval, S(473))
    persistentToggle(farmTab:AddCard(
        S(368),
        S(618)
    ), S(110), autoSell.SetEnabled)
    persistentSlider(farmTab, S(353), S(5),
        5, 300, S(206), 20, autoSell.SetCheckInterval, S(222))
    farmTab:AddSlider(S(71),
        S(775),
        5, 120, sellMaxWait, function(value)
            config.Set(S(243), value)
        end, S(45))
    persistentToggle(farmTab:AddCard(
        S(277),
        S(41)
    ), S(124), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(275), S(597),
        5, 300, S(661), 20, autoUpgrade.SetInterval, S(554))
    persistentSlider(farmTab, S(799), S(15),
        1, 20, S(429), 5, autoUpgrade.SetMaxPerPass, S(288))
    persistentSegmented(
        farmTab,
        { S(485), S(555), S(726), S(241) },
        { S(289), S(621), S(76), S(271) },
        S(7), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(374), S(510),
        0, 500, S(761), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(28))
    persistentToggle(
        farmTab:AddCard(S(281),
            S(436)),
        S(652), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(493) .. pickaxe.multiplier .. S(220)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(161)
            end
            return S(478) .. index .. S(120) .. (pickaxeLabels[index] or S(639))
        end
        local savedPick = config.Get(S(379), 0)
        pickaxeCard = farmTab:AddSlider(
            S(601),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(325), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(458)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(142),
            S(58)),
        S(225), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(680),
            S(223)),
        S(629), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(357),
            S(733)),
        S(575), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(483), S(82),
        1, 20, S(305), 2, autoUpgrade.SetMutationTarget, S(34))
    persistentSlider(farmTab, S(751), S(378),
        0, 500, S(83), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(435))
    persistentToggle(farmTab:AddCard(
        S(317),
        S(276)
    ), S(87), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(332), S(668),
        10, 600, S(329), 30, autoResearch.SetCheckInterval, S(266))
    persistentSlider(farmTab, S(503), S(600),
        1, 20, S(408), 5, autoResearch.SetMaxPerPass, S(451))
    persistentSlider(farmTab, S(562), S(149),
        0, 500, S(118), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(141))
    persistentToggle(
        farmTab:AddCard(S(240),
            S(12)),
        S(300), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(615),
            S(566)),
        S(248), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(278),
            S(419)),
        S(267), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(19), S(116) },
        { true, false },
        S(409), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(620),
        S(783)
    ), S(129), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(181), S(481),
        5, 300, S(207), 30, autoClaim.SetCheckInterval, S(263))
    persistentSlider(farmTab, S(133), S(56),
        1, 30, S(3), 10, autoClaim.SetMaxPerPass, S(443))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(568))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(381), S(354))
    persistentToggle(
        settingsTab:AddCard(S(269), S(160)),
        S(624),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(515), 100)
    settingsTab:AddSlider(S(210), S(162), 50, 150, savedScale, function(value)
        config.Set(S(227), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(66), 1)
    settingsTab:AddSegmented({ S(486), S(295), S(196) }, savedLanguage, function(index, value)
        local code = ({ S(441), S(359), S(386) })[index]
        config.Set(S(191), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(97), S(591), S(35) })[savedLanguage] or S(314))
    log(S(387) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(92)] = function()
return {
    [79389059854988] = S(602),
}
end
import(S(301))(import)
