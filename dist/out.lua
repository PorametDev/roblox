-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"GuiObject", "Frame", "core/click", " items)", "games/my_anime_mine/get_current_plot", "Auto Chest", "[AutoChest] fireproximityprompt error: ", "core/teleport", "Players", "Auto Chest: เกณฑ์เก็บ", "en", "ProximityPrompt", "  (debug.getconstants unavailable)", " items", "Content", "Dump", "core/click", "Content", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", ": ", "[AutoResearch] nothing affordable under the spend cap", "UpgradePart", "Open menu", "Players", " remotes", "%$", "PlayerGui", "ClickDetector", "games/my_anime_mine/get_current_plot", "MekPro", "[AutoSell] clicked SellButton", "This is not a supported place.", "Content", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "games/my_anime_mine/get_plot_parts", "AutoMine", "Players", "TextButton", "/config.json", "[AutoPlace] ", "TextLabel", "[MyAnimeMine] no holders found", "autoResearch", "games/registry", "[MyAnimeMine] === Tool: ", "core/click", "/", " ชิ้น", "Crew", "[AutoResearch] upgrades menu didn't open", "RemoteEvent", ": ", "core/get_me", " = ", "Place", "Canvas", "th", "PlayerGui", "Label", "[SellCards] === total cards: ", "games/my_anime_mine/debug_dump_sell_cards", "Label", "Tree", "function", "$", "Players", "ValueLabel", " x", ": ", ",", "ReplicatedStorage", "core/logger", ") from ", "ดีบัก", "action '", "", "^x(%d+)$", "อังกฤษ", "[GuiDump] === end ===", "ProximityPrompt", " (prompt says '", "auto", "AutoUpgrade", "games/my_anime_mine/main", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/auto_chest", "Tool", "Scroll", "autoPlace", "Content", "core/scheduler", "-", "Frame", ",", "RemoteEvent", "", "core/get_place_id", "autoPlaceInterval", "TextButton", "", "Cost", "UserInputService", "SellFrame", "Dump", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "core/get_place_id", "HumanoidRootPart", "[Remotes] ", "", "disabled", "[AutoSell] disabled", "PlayerGui", "Auto Upgrade: งบต่อครั้ง", "search_placeholder", "games/my_anime_mine/auto_sell", "Auto Sell: รอบขาย", "games/my_anime_mine/debug_inspect_handlers", "Text", "Players", "Card", "ExitButton", "[AutoChest] waiting: ", "SelectOverlay", "[ChestDump] === plot ", "s", "Players", "core/anti_afk", "MineGui", "TextBox", "GuiButton", "BottomPanels", "[Uu]nlock", "LeftBar", "ScrollingFrame", "Card", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "RemoteFunction", "Close", "เปิดหน้าอัปเกรดของ miner ที่วางอยู่ แล้วซื้อ Damage/Speed/Mining ที่ราคาไม่เกินงบ", "UpgradeButton", "Frame", "games/my_anime_mine/debug_dump_chest", "Research BuyButton", "games/my_anime_mine/get_current_plot", "ย่อขยายเมนูทั้งชุด", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "Chest TakeButton", "UICorner", " (ActionText=", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[Handlers] ", "UIPadding", "games/my_anime_mine/auto_research", "games/my_anime_mine/auto_place", "UICorner", "Backpack", "AutoSell", "Card", "[Logger] failed to write ", " '", "games/my_anime_mine/debug_dump_remotes", " | ", "[Interaction] Key pressed: %s (gameProcessed=%s)", "write reported success but the line isn't in ", "UIListLayout", "core/logger", "", "TextLabel", "core/anti_afk", "games/my_anime_mine/auto_research", "th", "SellButton", "core/interaction_logger", "Detail", "TextLabel", "RunService", "  ", "SellFrame", ") -> ", "table", "MineGui", "", "[AutoPlace] fireproximityprompt error: ", "[AutoSell] fireproximityprompt error: ", "TextLabel", "Damage", " = ", "Label", "games/my_anime_mine/auto_upgrade", "[Handlers] === end ===", "%$%s*([%d,%.]+)%s*(%a*)", " '", "games/my_anime_mine/auto_place", "[Interaction] Button activated: ", "Speed", "PlayerGui", ": ", "games/my_anime_mine/auto_mine", "BuyButton", "TextLabel", "Close", "[Handlers] ", "ChestCapacity", "Auto Research (Plot)", "[AutoPlace] couldn't pick up ", " power ", "games/my_anime_mine/main", "SellFrame", "games/my_anime_mine/auto_sell", "ClickDetector", "core/teleport", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "Debug: Dump Backpack Tool", "open_menu_hint", "ไล่อัปเกรด miner ทุกกี่วินาที", "Frame", ": ", "[Interaction] ProximityPrompt triggered: ", " (", "games/my_anime_mine/debug_dump_gui", "อ่านป้ายบน chest ทุกกี่วินาที", " parts", "Content", "^([%d,]+)$", ")", "core/logger", "TextLabel", "Players", "Select All", "[AutoSell] sell menu didn't open, aborting", "Search", "UIListLayout", "antiAfk", "", "autoMineSpeed", "^Node_", "GuiButton", " '", "TextButton", "Search functions...", ") didn't take, trying the next one", "[AutoSell] click failed on ", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "[Interaction] ClickDetector clicked: ", "StatsPanel", "Chest", "RemoteFunction", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "Dump", " (MaxActivationDistance=%s)", "UIListLayout", "[AutoUpgrade] ", "Sell for", "Mining", "[AutoPlace] upgrading ", "ProximityPrompt", "Auto Mine: คลิกต่อรอบ", "[%X] ", "Header", "waiting", "core/logger", "", "\n", "SelectAllButton", "s", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "no isfile/readfile available to verify the write", "/s", ": ", "costs ", "no tool", " while selecting", "Scroll", "prompt disabled", "th", "", "unreadable", " on ", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "  [attr] ", "core/parse_money", "en", "[AntiAFK] enabled - watching Player.Idled", "games/my_anime_mine/get_plot_parts", " = ", "core/interaction_logger", ": ", "games/my_anime_mine/get_best_miner", "SellButton", "Content", "UserInputService", "HumanoidRootPart", "games/my_anime_mine/get_plot_parts", "defined at ", "TabBar", "ไม่ซื้อ node ที่แพงกว่านี้", "core/logger", "[ChestDump] === Player subtree ===", "TextLabel", "[AutoChest] Take All had NO effect (", "PlayerGui", "", "Done", "Content", "TextLabel", "TextButton", "Done", "core/logger", "fill", "games/my_anime_mine/debug_dump_gui", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "en-us", "Folder", "", "Empty miner slot", "[AutoPlace] nothing in the hotbar to place", " = ", "core/click", "th", "=locked(", "Frame", "languageIndex", " unit card(s) Select All skipped", "[Handlers] getconnections isn't available in this executor", "core/dispatch", "[Handlers] === ", "[AutoChest] Take All worked: ", "disabled", "menuScale", "Workspace", " (Text=", "boolean", " (", "core/parse_money", "[AutoPlace] slot ", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", " (", "[SellCards] Card #", "TextButton", "s", " ===", "ClickDetector", "core/dispatch", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "PlayerGui", "TextButton", "[AutoResearch] no BuyButton after selecting ", "Dump", "[Scheduler] turn: ", "Auto Sell", "PlayerGui", " (Text=", " (ActionText=%s ObjectText=%s HoldDuration=%s)", ")", "Frame", "X", "autoUpgrade", "ProximityPrompt", "SlotFocusFrame", "ProximityPrompt", "Anti-AFK", "leaderstats", "Inspect", "games/my_anime_mine/debug_dump_sell_cards", ")", "string", "menuScale", "disabled", "[Handlers] === start (open the relevant menus first) ===", " = ", "Content", "(", "Label", "Frame", "core/i18n", "Backpack", " items)", "ClickDetector", "number", "Error: ", "[AutoSell] clicked SelectAllButton (", "[AutoChest] disabled", "Scroll", "enabled", "unreadable", "core/i18n", "Label", "TextButton", "'", " attributes ===", "core/i18n", "Debug: Dump Chest Data", "MineGui", "RemoteFunction", "SellFrame", "core/teleport", "games/my_anime_mine/debug_dump", " = ", "GuiService", "[AutoPlace] ", "core/parse_money", "games/my_anime_mine/get_plot_parts", " (power ", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "core/logger", "ItemName", "auto", "Players", "autoChest", "  [attr] ", "(nothing to do)", "games/my_anime_mine/debug_dump", "SelectAllButton", " items, going to collect", "auto", " ", "Badge", "ตามเครื่อง", "Players", "[AutoPlace] holders: ", "Debug: Dump All Remotes", "TextLabel", "core/logger", " ===", "Players", "Text", "", " (Text=", "en", "Auto Research: งบต่อครั้ง", "Sell for", "[AutoSell] also selected ", "fill", "AutoResearch", "[Config] couldn't write ", "PlayerGui", "HttpService", "languageIndex", "เปิดหน้า research ทุกกี่วินาที", "TextLabel", "", "Players", "autoUpgradeInterval", "Auto Place: รอบเช็ค", ") on ", "games/my_anime_mine/auto_upgrade", "  (debug.getupvalues unavailable)", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "core/logger", "[AutoChest] click failed on ", "[GuiDump] MineGui not found", "Debug: Current Plot", "[MyAnimeMine] === Node: ", " '", "Instance", "[MyAnimeMine] no nodes present right now", "/", "ExitButton", "neither appendfile nor writefile is available", "[AutoMine] ", "core/ui", "Actions", "UIScale", "Loot", "search_placeholder", "Container", "PlotIndex", "[ChestDump] === ReplicatedStorage.", "autoSellInterval", "table", "games/my_anime_mine/get_plot_parts", "'", ") for ", "ตั้งค่า", "table", "autoUpgradeMaxSpend", "Done", "Frame", "[AutoPlace] placed ", "Locked miner slot", "MineGui", "button has no size on screen", "Zones", "^(%d+)", "Plots", "[MekPro] settings file: ", "core/logger", "  ", "Players", " ", "core/config", " ===", "", " error: ", "TakeButton", "", "Frame", "Instance", ":", " ===", "ValueLabel", "core/logger", "Players", " connection(s)", "core/logger", "ClickDetector", "%", "?", ": ", "core/ui", "enabled", "TextButton", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "ReplicatedStorage", "[BestMiner] slot ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Auto Mine", "Badge", "MineGui", "no plot", "games/my_anime_mine/get_current_plot", "RemoteEvent", "s", "x", "games/my_anime_mine/debug_dump_chest", "'", "Content", "Damage", "TakeButton", "Players", "[AutoPlace] disabled", "TextLabel", " -> ", "Content", " '", "TextButton", ")", "Interaction Logger", "upgrade", "  ", "", "  [attr] ", "SellStand", "", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "ItemFrame", "'", "  ", "MineGui", " items", "core/logger", "[Remotes] === total: ", "'", " (", "core/logger", ")", "CoreGui", "[AntiAFK] idle detected, simulating input", "Auto Research: รอบเช็ค", "Content", "ProximityPrompt", "My Anime Mine · Mining Hub", "autoResearchMaxSpend", "not found", "BottomPanels", "sl", "Dump", "games/my_anime_mine/get_current_plot", "ไม่ซื้ออะไรที่แพงกว่านี้", "ValueBase", ")", "Auto Upgrade Unit", "locked slot", "[AutoPlace] drifted out of range of ", "games/my_anime_mine/debug_dump_remotes", "[AutoChest] enabled (collects once >= ", "?", "AutoChest", "Content", "CoreUI", "autoChestMinItems", "[^%d%.]", "[Scheduler] ", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Debug: Dump Sell Cards", ") ===", ",", "')", "TextLabel", "ขนาดเมนู", "OwnerUserId", "Actions", "ChestFrame", "VirtualInputManager", "[AutoSell] nothing to sell, closing", "[MyAnimeMine] === Holder: ", "UpgradeButton", "UIStroke", "TextLabel", "nothing to place or upgrade", "TextLabel", "games/my_anime_mine/debug_inspect_handlers", "ScreenGui", "core/teleport", "autoResearchInterval", "🏠", " for ", "[AutoUpgrade] upgrade screen didn't open for ", "function", "[AutoSell] nothing selectable (", "games/my_anime_mine/get_plot_parts", "ปุ่มเปิดเมนู", "string", "[MyAnimeMine] not standing in any plot", "s", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", " still empty after placing ", "core/logger", "MineGui", "  ", "[attr] ", "ValueBase", "Main", "Debug: Dump Node/Holder", "[ChestDump] not standing in any plot", "TextButton", "TextButton", "log failed", "Content", "Content", "[AutoChest] can't read the chest billboard, is the plot loaded?", "Damage", "HotbarFrame", "autoSell", "List", "open_menu_hint", ": ", "SlotFocusFrame", "Sidebar", " -> ", "Container", "GuiObject", "core/click", "/", "waiting", "VirtualUser", "Content", "games/my_anime_mine/get_best_miner", " ===", " cards)", "games/my_anime_mine/auto_chest", " (power ", "", "[MyAnimeMine] === Dump start ===", "[ChestDump] === Chest subtree ===", "[GuiDump] === visible MineGui tree ===", "[AutoPlace] picked up ", "[AutoSell] enabled (only sells when there's something in the scroll)", "  [attr] ", "write reported success but ", "enabled", "Holders", "[Config] couldn't read ", "MekPro", "  [const] ", "core/click", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", " ", "Auto Mine: ความเร็ว", "\n", "autoMineClicks", "SkillTreeFullFrame", "StatsPanel", "autoChestInterval", "Progress", "Debug: Inspect Button Handlers", " = ", "[AntiAFK] disabled", "", "MineWorld", "⚙", "", "Frame", "เปิดหน้า Upgrades แล้วซื้อ node ที่ปลดได้ในทุกโซน เรียงจากถูกไปแพง", "SkillTreeFullFrame", "ChestFrame", "[Handlers]   ", "[Handlers]   #", "Detail", "", "🐛", "Pips", "TextLabel", "Lock", "^%$", "Players", "games/my_anime_mine/auto_mine", ": ", "MineGui", "[AutoChest] ", "Workspace", "table", "  ", "TextLabel", "Content", "Players", "[Interaction] logger enabled - go click/interact with things now", "[AutoChest] chest didn't open, aborting", "games/my_anime_mine/get_current_plot", " cards", "core/logger", "Sell SellButton", "BuyButton", "", " doesn't exist", " from ", "[ChestDump] === end ===", "core/logger", "AutoPlace", "Unit Damage UpgradeButton", "[ChestDump] plot has no Chest", "core/get_leaderstat", "Auto Place Miner", "games/registry", "auto", "[AutoResearch] bought ", "core/scheduler", "ไทย", "MouseButton1Click", "Workspace", "MekPro.log", "Close", "PlayerGui", "[AutoResearch] ", "core/get_place_id", "", "Nodes", "ค้นหาฟังก์ชัน...", "'), backing off ", "Humanoid", "", "table", " <-- remote, likely fired by the real interaction", "UpgradesButton", ": not found", "Content", "  [upvalue] ", "autoMine", "ฟาร์ม", "s", "core/config", "TextButton", "Pad", " (", "Amount", "Scroll", "core/logger", ")", "", "Content", "Sell SelectAllButton", "TextLabel", "Debug: Dump Visible GUI", "Frame", "=", "Content", "Check", "interactionLogger", "games/my_anime_mine/get_plot_parts", "Auto Chest: รอบเช็ค", "$", "[AutoUpgrade] bought ", "[Interaction] logger disabled", "Dump", "Done", "Max", " ===", "'", "[Bb]uy", "=empty", "Auto Upgrade: รอบเช็ค", "Scroll", " cards left, label = '", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Activated", "Players"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(127)] = function()
return function(import)
local log = import(S(502))
local Players = game:GetService(S(503))
local VirtualUser = game:GetService(S(646))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(283))
        connection = player.Idled:Connect(function()
            log(S(558))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(678))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(17)] = function()
local VirtualInputManager = game:GetService(S(594))
local GuiService = game:GetService(S(399))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(482)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(491)] = function()
return function(import)
local HttpService = game:GetService(S(437))
local getPlaceId = import(S(735))
local log = import(S(718))
local ROOT_FOLDER = S(30)
local placeFolder = ROOT_FOLDER .. S(644) .. tostring(getPlaceId())
local filePath = placeFolder .. S(39)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(742) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(663) .. filePath .. S(698) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(435) .. filePath .. S(69) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(325)] = function()
return function(import)
    local getPlaceId = import(S(97))
    local registry = import(S(44))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(32))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(722)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(362))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(53)] = function()
local Players = game:GetService(S(706))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(740))
    local backpack = player:WaitForChild(S(156))
    return player, humanoid, backpack
end
return getMe
end
modules[S(106)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(386)] = function()
local Players = game:GetService(S(786))
local STRINGS = {
    en = {
        search_placeholder = S(240),
        open_menu_hint = S(23),
    },
    th = {
        search_placeholder = S(738),
        open_menu_hint = S(612),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(312)
    if locale:sub(1, 2):lower() == S(275) then
        return S(319)
    end
    return S(282)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(407) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(286)] = function()
return function(import)
local Players = game:GetService(S(530))
local Workspace = game:GetService(S(330))
local UserInputService = game:GetService(S(291))
local log = import(S(551))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(218) .. describe(prompt)
            .. S(149) .. prompt.ActionText .. S(571))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(244) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(194) .. describe(button)
            .. S(428) .. tostring(button.Text) .. S(758))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(12)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(28)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(237)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(773))
        return
    end
    connections = {}
    log(S(707))
    local playerGui = Players.LocalPlayer:WaitForChild(S(112))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(256)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(210)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(130)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(163),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(618)] = function()
local LOG_FILE = S(731)
local function log(message)
    local line = os.date(S(258)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(427))
            end
            appendfile(LOG_FILE, line .. S(670))
        elseif writefile then
            local existing = S(234)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(263))
        else
            error(S(459))
        end
        if not (isfile and readfile) then
            error(S(267))
        end
        if not isfile(LOG_FILE) then
            error(S(660) .. LOG_FILE .. S(715))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(164) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(159) .. LOG_FILE .. S(52) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(281)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(366) then
        return nil
    end
    local number, suffix = text:match(S(191))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(70), S(314))))
    if not value then
        return nil
    end
    if suffix ~= S(96) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(727)] = function()
return function(import)
local RunService = game:GetService(S(176))
local log = import(S(711))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(583) .. task_.name .. S(494) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(349) .. (currentTask and currentTask.name or S(411)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(8)] = function()
local Players = game:GetService(S(489))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(292))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(461)] = function()
return function(import)
local i18n = import(S(375))
local UserInputService = game:GetService(S(102))
local THEME = {
    BrandName = S(664),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(155))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(148))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(152))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(512))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(541)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(598))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(227))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(508)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(422))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(441)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(134))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(165))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(704))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(93))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(532))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(200))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(599))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(67))
            if not valueLbl then
                valueLbl = Instance.new(S(762))
                valueLbl.Name = S(501)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(417))
            if not badge then
                badge = Instance.new(S(185))
                badge.Name = S(518)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(626))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(141))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(355))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(627))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(759)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(507)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(764))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(321))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(38))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(679)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(2))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(251))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(388))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(118)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(603))
    screenGui.Name = S(580)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(557))
    local main = Instance.new(S(683))
    main.Name = S(623)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(463))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(374))
    tabBar.Name = S(295)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(232))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(478))
    header.Name = S(259)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(752))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(41))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(440))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(741)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(536))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(356)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(99))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(92)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(129))
    searchBox.Name = S(231)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(465))
    searchBox.Text = S(302)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(216))
    content.Name = S(371)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(497))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(589))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(636))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(299))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(426)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(736) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(416) .. (item.Subtitle or S(653))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(114))
        footerLabel.Text = i18n.T(S(214))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(651)] = function()
return function(import)
local Players = game:GetService(S(228))
local getCurrentPlot = import(S(709))
local getPlotParts = import(S(293))
local teleport = import(S(604))
local click = import(S(46))
local log = import(S(297))
local checkInterval = 3
local minItemsToCollect = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(733))
    local mineGui = playerGui and playerGui:FindFirstChild(S(128))
    return mineGui and mineGui:FindFirstChild(S(593))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(760))
    local actions = content and content:FindFirstChild(S(592))
    return actions and actions:FindFirstChild(S(529))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(94), S(276)):match(S(484))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(755), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(18))
    local progress = content and content:FindFirstChild(S(675))
    local label = progress and progress:FindFirstChild(S(188))
    return parseCount(label and label.Text or S(714))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(450) .. button.Name .. S(269) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(561), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(382))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(576) .. minItemsToCollect .. S(377))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local cachedCount, cachedAt = nil, 0
function autoChest.HasWork()
    local now = tick()
    if now - cachedAt > 1 then
        cachedCount = getChestCount()
        cachedAt = now
    end
    return cachedCount ~= nil and cachedCount >= minItemsToCollect
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(7) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(708))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(327) .. count .. S(533) .. after .. S(14))
        else
            log(S(300) .. tostring(count)
                .. S(640) .. tostring(after) .. S(4))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(277) then
            log(S(631))
            lastReportedCount = S(385)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReportedCount ~= S(645) then
            log(S(122) .. count .. S(457) .. minItemsToCollect .. S(550))
            lastReportedCount = S(260)
        end
        return
    end
    lastReportedCount = nil
    log(S(700) .. count .. S(414))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(697)] = function()
return function(import)
local getCurrentPlot = import(S(568))
local getPlotParts = import(S(769))
local log = import(S(487))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(378), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(460) .. (enabled and S(384) or S(368)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(193)] = function()
return function(import)
local Players = game:GetService(S(419))
local getCurrentPlot = import(S(85))
local getPlotParts = import(S(35))
local getMinerSlots = import(S(648))(import)
local teleport = import(S(396))
local click = import(S(318))
local log = import(S(308))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(315)
local LOCKED_SLOT_TEXT = S(480)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(753))
    return pad and pad:FindFirstChildWhichIsA(S(360), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(168)) then
            local digits = descendant.Text:match(S(224))
            if digits then
                return tonumber((digits:gsub(S(587), S(496))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(305)) and descendant.Text:match(S(695)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(573)
    end
    if not prompt.Enabled then
        return true, S(274)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(270) .. price
    end
    local action = prompt.ActionText or S(76)
    if action:match(S(132)) or action:match(S(779)) or action:match(S(26)) then
        return true, S(75) .. action .. S(778)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(107))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(183) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(320) .. why .. S(365))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(780))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(765) .. prompt.ObjectText .. S(372) .. power .. S(225))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(162))
    if line ~= lastSurvey then
        log(S(420) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(433), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(539), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(40) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(531))
    else
        lastAttempt = 0
        log(S(136))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(248),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(55), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(316))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(574) .. holder.Name .. S(272))
            return false
        end
        log(S(335) .. candidate.slot .. S(333) .. candidate.name
            .. S(241))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(400) .. holder.Name .. S(617) .. miner.name)
        return false
    end
    log(S(479) .. miner.name .. S(652) .. miner.power
        .. S(445) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(205) .. entry.name .. S(716) .. entry.holder.Name
            .. S(81) .. tostring(entry.prompt.ActionText) .. S(588))
        return false
    end
    log(S(657) .. entry.name .. S(403) .. entry.power
        .. S(73) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(600))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(309) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(255) .. target.holder.Name .. S(20) .. target.name
        .. S(219) .. target.power .. S(179) .. best.name .. S(754) .. best.power .. S(556))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(170)] = function()
return function(import)
local Players = game:GetService(S(442))
local parseMoney = import(S(334))
local click = import(S(643))
local log = import(S(423))
local checkInterval = 30
local maxSpend = 100000
local function getMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(58))
    return playerGui and playerGui:FindFirstChild(S(549))
end
local function getTreeFrame()
    local mineGui = getMineGui()
    return mineGui and mineGui:FindFirstChild(S(685))
end
local function getUpgradesButton()
    local mineGui = getMineGui()
    local leftBar = mineGui and mineGui:FindFirstChild(S(133))
    local content = leftBar and leftBar:FindFirstChild(S(647))
    local container = content and content:FindFirstChild(S(641))
    return container and container:FindFirstChild(S(744))
end
local function zoneTabs(frame)
    local sidebar = frame:FindFirstChild(S(639))
    local list = sidebar and sidebar:FindFirstChild(S(635))
    if not list then
        return {}
    end
    local tabs = {}
    for _, tab in ipairs(list:GetChildren()) do
        if tab:IsA(S(346)) then
            local content = tab:FindFirstChild(S(534))
            local locked = content and content:FindFirstChild(S(694))
            if not locked then
                local label = content and content:FindFirstChild(S(59))
                table.insert(tabs, {
                    button = tab,
                    name = (label and label.Text ~= S(262) and label.Text) or tab.Name,
                })
            end
        end
    end
    return tabs
end
local function affordableNodes(frame)
    local tree = frame:FindFirstChild(S(63))
    local canvas = tree and tree:FindFirstChild(S(56))
    if not canvas then
        return {}
    end
    local nodes = {}
    for _, node in ipairs(canvas:GetChildren()) do
        if node:IsA(S(239)) and node.Name:match(S(236)) then
            local content = node:FindFirstChild(S(630))
            local cost = content and content:FindFirstChild(S(101))
            local price = cost and parseMoney(cost.Text)
            if price and price <= maxSpend then
                local pips = content:FindFirstChild(S(692))
                table.insert(nodes, {
                    button = node,
                    name = node.Name:sub(6),
                    price = price,
                    priceText = cost.Text,
                    pips = pips and pips.Text or S(577),
                })
            end
        end
    end
    table.sort(nodes, function(a, b)
        return a.price < b.price
    end)
    return nodes
end
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(734) .. (enabled and S(661) or S(110)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyNode(frame, node)
    click.Real(node.button)
    task.wait(0.4)
    local detail = frame:FindFirstChild(S(689))
    local buyButton = detail and detail:FindFirstChild(S(199))
    if not buyButton then
        log(S(347) .. node.name)
        return false
    end
    local content = buyButton:FindFirstChild(S(766))
    local label = content and content:FindFirstChild(S(62))
    if label and label.Text == S(776) then
        return false
    end
    click.Real(buyButton)
    task.wait(0.5)
    log(S(726) .. node.name .. S(337) .. node.pips .. S(473) .. node.priceText)
    return true
end
local function runJob()
    busy = true
    local button = getUpgradesButton()
    if not button then
        busy = false
        return
    end
    click.Real(button)
    task.wait(0.8)
    local frame = getTreeFrame()
    if not (frame and frame.Visible) then
        log(S(50))
        busy = false
        return
    end
    local bought = 0
    for _, tab in ipairs(zoneTabs(frame)) do
        if not enabled then
            break
        end
        click.Real(tab.button)
        task.wait(0.5)
        for _, node in ipairs(affordableNodes(frame)) do
            if not enabled then
                break
            end
            if buyNode(frame, node) then
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(21))
    end
    local close = frame:FindFirstChild(S(732)) or frame:FindFirstChild(S(121))
    if close then
        click.Real(close)
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(115)] = function()
return function(import)
local Players = game:GetService(S(425))
local Workspace = game:GetService(S(701))
local teleport = import(S(211))
local click = import(S(666))
local log = import(S(449))
local checkInterval = 45
local nothingSellableCooldown = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(27))
    local mineGui = playerGui and playerGui:FindFirstChild(S(181))
    return mineGui and mineGui:FindFirstChild(S(103))
end
local function labelText(button)
    local content = button:FindFirstChild(S(290))
    local label = content and content:FindFirstChild(S(387))
    return label and label.Text or S(167)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(746))
    local scroll = content and content:FindFirstChild(S(756))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(158) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(242) .. button.Name .. S(509) .. tostring(err))
    end
    return ok
end
local function isCardSelected(card)
    local content = card:FindFirstChild(S(33))
    local overlay = content and content:FindFirstChild(S(123))
    return overlay ~= nil and overlay.Visible
end
local function isOnScreen(card, scroll)
    local top = scroll.AbsolutePosition.Y
    local bottom = top + scroll.AbsoluteSize.Y
    local cardTop = card.AbsolutePosition.Y
    local cardBottom = cardTop + card.AbsoluteSize.Y
    return cardTop >= top and cardBottom <= bottom
end
local function selectUnselectedCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(629))
    local scroll = content and content:FindFirstChild(S(782))
    if not scroll then
        return 0
    end
    local selected = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(135) and not isCardSelected(card) and isOnScreen(card, scroll) then
            clickButton(card)
            selected = selected + 1
            task.wait(0.1)
        end
    end
    return selected
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(543))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(80), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(111))
    else
        lastAttempt = 0
        log(S(658))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(184) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(230))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(595))
        local closeButton = sellFrame:FindFirstChild(S(138))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(15))
    local selectAllButton = content and content:FindFirstChild(S(413))
    local sellButton = content and content:FindFirstChild(S(172))
    if selectAllButton and labelText(selectAllButton) == S(229) then
        clickButton(selectAllButton)
        log(S(381) .. cardCount .. S(650))
        task.wait(0.2)
    end
    local selected = selectUnselectedCards(sellFrame)
    if selected > 0 then
        log(S(432) .. selected .. S(323))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(431), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(253), 1, true) then
        clickButton(sellButton)
        log(S(31))
    else
        lastAttempt = tick() + nothingSellableCooldown - checkInterval
        local closeButton = sellFrame:FindFirstChild(S(201))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(610) .. cardCount .. S(783)
            .. tostring(sawLabel) .. S(739) .. nothingSellableCooldown .. S(750))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(189)] = function()
return function(import)
local Players = game:GetService(S(24))
local getCurrentPlot = import(S(29))
local getPlotParts = import(S(284))
local parseMoney = import(S(401))
local click = import(S(3))
local log = import(S(405))
local STAT_NAMES = { S(632), S(195), S(254) }
local checkInterval = 20
local maxSpend = 100000
local function getFocusFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(351))
    local mineGui = playerGui and playerGui:FindFirstChild(S(699))
    return mineGui and mineGui:FindFirstChild(S(359))
end
local function findUpgradeClick(holder)
    local upgradePart = holder:FindFirstChild(S(22))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(342))
end
local function occupiedHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return {}
    end
    local found = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClick(holder)
        if detector then
            table.insert(found, { holder = holder, detector = detector })
        end
    end
    return found
end
local function readStat(scroll, statName)
    local row = scroll:FindFirstChild(statName)
    local button = row and row:FindFirstChild(S(140))
    local content = button and button:FindFirstChild(S(304))
    local label = content and content:FindFirstChild(S(373))
    if not label then
        return nil
    end
    return {
        name = statName,
        button = button,
        price = parseMoney(label.Text),
        text = label.Text,
    }
end
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(252) .. (enabled and S(511) or S(328)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyAffordableStats(entry)
    local frame = getFocusFrame()
    local panels = frame and frame:FindFirstChild(S(565))
    local statsPanel = panels and panels:FindFirstChild(S(673))
    local scroll = statsPanel and statsPanel:FindFirstChild(S(273))
    if not scroll then
        log(S(608) .. entry.holder.Name)
        return 0
    end
    local bought = 0
    for _ = 1, #STAT_NAMES do
        local cheapest = nil
        for _, statName in ipairs(STAT_NAMES) do
            local stat = readStat(scroll, statName)
            if stat and stat.price and stat.price <= maxSpend then
                if not cheapest or stat.price < cheapest.price then
                    cheapest = stat
                end
            end
        end
        if not cheapest then
            break
        end
        click.Real(cheapest.button)
        log(S(772) .. cheapest.name .. S(607) .. cheapest.text
            .. S(278) .. entry.holder.Name)
        bought = bought + 1
        task.wait(0.4)
    end
    return bought
end
local function closeFrame()
    local frame = getFocusFrame()
    local exit = frame and frame:FindFirstChild(S(458))
    if exit then
        click.Real(exit)
        task.wait(0.3)
    end
end
local function runJob()
    busy = true
    for _, entry in ipairs(occupiedHolders()) do
        if not enabled then
            break
        end
        fireclickdetector(entry.detector)
        task.wait(0.8)
        local frame = getFocusFrame()
        if frame and frame.Visible then
            buyAffordableStats(entry)
            closeFrame()
        end
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(412)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(682)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(192) .. child.Name .. S(547)
        if child:IsA(S(358)) then
            line = line .. string.format(
                S(353),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(506)) then
            line = line .. string.format(S(250), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(95)) or child:IsA(S(137)) then
            line = line .. S(743)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(542) .. attrName .. S(370) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(703))
    end
end
return dumpInstance
end
modules[S(525)] = function()
return function(import)
local Players = game:GetService(S(37))
local ReplicatedStorage = game:GetService(S(71))
local getCurrentPlot = import(S(521))
local getPlotParts = import(S(471))
local log = import(S(226))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(621) .. name .. S(285) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(488))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(535) .. child.Name .. S(472)
            if child:IsA(S(570)) then
                line = line .. S(317) .. tostring(child.Value)
            elseif child:IsA(S(693)) or child:IsA(S(306)) then
                line = line .. S(331) .. tostring(child.Text) .. S(537)
            end
            log(line)
            dumpSubtree(child, indent .. S(177))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(625))
        return 0
    end
    log(S(124) .. plot.Name .. S(390))
    dumpAttributes(plot, S(620))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(721))
        return 0
    end
    log(S(655))
    dumpSubtree(parts.Chest, S(493))
    local player = Players.LocalPlayer
    log(S(298))
    dumpSubtree(player, S(690))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(468) .. player.Name .. S(649))
        dumpSubtree(mine, S(109))
    end
    log(S(717))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(310)] = function()
return function(import)
local Players = game:GetService(S(66))
local log = import(S(261))
local function dumpVisible(root, indent)
    indent = indent or S(182)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(642)) and child.Visible then
            local line = indent .. child.ClassName .. S(238) .. child.Name .. S(553)
            if child:IsA(S(601)) or child:IsA(S(339)) then
                line = line .. S(352) .. tostring(child.Text) .. S(354)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(659) .. attrName .. S(398) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(540))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(436))
    local mineGui = playerGui and playerGui:FindFirstChild(S(619))
    if not mineGui then
        log(S(451))
        return
    end
    log(S(656))
    dumpVisible(mineGui)
    log(S(79))
end
return dumpMineGui
end
end
modules[S(161)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(514))
local log = import(S(555))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(522)) or descendant:IsA(S(247)) then
            count = count + 1
            log(S(108) .. descendant.ClassName .. S(197) .. descendant:GetFullName())
        end
    end
    log(S(552) .. count .. S(777))
    return count
end
return dumpRemotes
end
end
modules[S(364)] = function()
return function(import)
local Players = game:GetService(S(126))
local log = import(S(757))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(196))
    local mineGui = playerGui and playerGui:FindFirstChild(S(481))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(395))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(579)) and sellFrame.Content:FindFirstChild(S(88))
    if not scroll then
        log(S(784))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(120) or card:IsA(S(1)) then
            count = count + 1
            log(S(338) .. count .. S(287) .. card.ClassName .. S(160) .. card.Name .. S(526))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(410) .. attrName .. S(187) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(548) .. child.ClassName .. S(454) .. child.Name .. S(389)
                if child:IsA(S(622)) then
                    line = line .. S(677) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(60) .. count .. S(341))
    return count
end
return dumpSellCards
end
end
modules[S(117)] = function()
return function(import)
local Players = game:GetService(S(408))
local log = import(S(72))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(455) then
        return value.ClassName .. S(490) .. value:GetFullName()
    end
    if valueType == S(470) then
        return S(475)
    end
    if valueType == S(609) then
        return S(64)
    end
    return valueType .. S(668) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(498) then
        return value:IsA(S(51)) or value:IsA(S(394)) or value:IsA(S(313))
    end
    return valueType == S(613) or valueType == S(379) or valueType == S(332)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(566))
    end)
    if ok and source then
        log(indent .. S(294) .. tostring(source) .. S(499) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(180) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(665) .. describe(constant))
            end
        end
    else
        log(indent .. S(13))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(702) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(747) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(447))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(202) .. label .. S(745))
        return
    end
    log(S(326) .. label .. S(554) .. button:GetFullName() .. S(586))
    if not getconnections then
        log(S(324))
        return
    end
    for _, signalName in ipairs({ S(785), S(729) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(151) .. signalName .. S(217) .. #connections .. S(504))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(688) .. index)
                    inspectFunction(fn, S(687))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(301))
    local current = playerGui and playerGui:FindFirstChild(S(393))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(369))
    inspectButton(S(147), find({ S(686), S(527), S(462), S(495) }))
    inspectButton(S(761), find({ S(208), S(223), S(264) }))
    inspectButton(S(712), find({ S(178), S(560), S(289) }))
    inspectButton(S(143), find({ S(672), S(174), S(713) }))
    local statsScroll = find({ S(638), S(131), S(245), S(383) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(186))
    inspectButton(S(720), damageRow and damageRow:FindFirstChild(S(597)))
    log(S(190))
end
return inspectHandlers
end
end
modules[S(288)] = function()
return function(import)
local Players = game:GetService(S(696))
local log = import(S(505))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(345))
    local mineGui = playerGui and playerGui:FindFirstChild(S(519))
    local hotbar = mineGui and mineGui:FindFirstChild(S(633))
    local content = hotbar and hotbar:FindFirstChild(S(705))
    return content and content:FindFirstChild(S(466))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(175)) then
            local count = descendant.Text:match(S(77))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(546))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(90))
    local nameLabel = content and content:FindFirstChild(S(406))
    local damageLabel = content and content:FindFirstChild(S(528))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(544) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(582), S(100)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(47) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(515) .. slot .. S(637) .. info.name
                        .. S(206) .. info.power .. S(68) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(5)] = function()
local Players = game:GetService(S(119))
local Workspace = game:GetService(S(730))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(680))
    local plots = mineWorld and mineWorld:FindFirstChild(S(485))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(591)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(611)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(662)),
        Nodes = plot:FindFirstChild(S(737)),
        Chest = plot:FindFirstChild(S(246)),
        Zones = plot:FindFirstChild(S(483)),
        Crew = plot:FindFirstChild(S(49)),
        Loot = plot:FindFirstChild(S(464)),
        PlotIndex = plot:GetAttribute(S(467)),
        ChestCapacity = plot:GetAttribute(S(203)),
    }
end
return getPlotParts
end
modules[S(207)] = function()
return function(import)
    local Players = game:GetService(S(9))
    local createWindow = import(S(510))(import)
    local i18n = import(S(391))
    local antiAfk = import(S(169))(import)
    local interactionLogger = import(S(173))(import)
    local scheduler = import(S(91))(import)
    local config = import(S(751))(import)
    local log = import(S(166))
    local window = createWindow({
        Subtitle = S(562),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(144))
    local getPlotParts = import(S(402))
    local dumpInstance = import(S(397))
    local dumpRemotes = import(S(575))(import)
    local dumpSellCards = import(S(61))(import)
    local dumpGui = import(S(220))(import)
    local dumpChest = import(S(142))(import)
    local inspectHandlers = import(S(602))(import)
    local autoMine = import(S(198))(import)
    local autoPlace = import(S(154))(import)
    local autoChest = import(S(86))(import)
    local autoSell = import(S(209))(import)
    local autoUpgrade = import(S(446))(import)
    local autoResearch = import(S(153))(import)
    scheduler.RegisterStandalone(S(36), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(578), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(719), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(83), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(434), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(157), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(749), S(606))
    persistentToggle(farmTab:AddCard(
        S(517),
        S(150)
    ), S(748), autoMine.SetEnabled)
    persistentSlider(farmTab, S(669), S(344),
        1, 30, S(235), 5, autoMine.SetCyclesPerSecond, S(268))
    persistentSlider(farmTab, S(257), S(146),
        1, 10, S(671), 1, autoMine.SetClicksPerCycle, S(524))
    persistentToggle(farmTab:AddCard(
        S(723),
        S(516)
    ), S(89), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(444), S(243),
        1, 60, S(98), 3, autoPlace.SetCheckInterval, S(340))
    persistentToggle(farmTab:AddCard(
        S(6),
        S(545)
    ), S(409), autoChest.SetEnabled)
    persistentSlider(farmTab, S(10), S(266),
        50, 3000, S(581), 100, autoChest.SetMinItems, S(48))
    persistentSlider(farmTab, S(770), S(221),
        1, 60, S(674), 3, autoChest.SetCheckInterval, S(265))
    persistentToggle(farmTab:AddCard(
        S(350),
        S(448)
    ), S(634), autoSell.SetEnabled)
    persistentSlider(farmTab, S(116), S(19),
        10, 300, S(469), 45, autoSell.SetCheckInterval, S(615))
    persistentToggle(farmTab:AddCard(
        S(572),
        S(139)
    ), S(357), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(781), S(215),
        5, 300, S(443), 20, autoUpgrade.SetInterval, S(125))
    persistentSlider(farmTab, S(113), S(569),
        1000, 1000000, S(476), 100000, autoUpgrade.SetMaxSpend, S(771))
    persistentToggle(farmTab:AddCard(
        S(204),
        S(684)
    ), S(43), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(559), S(439),
        10, 600, S(605), 30, autoResearch.SetCheckInterval, S(523))
    persistentSlider(farmTab, S(430), S(296),
        1000, 1000000, S(563), 100000, autoResearch.SetMaxSpend, S(65))
    local debugTab = window:AddTab(S(74), S(691))
    persistentToggle(
        debugTab:AddCard(
            S(538),
            S(336)
        ),
        S(768),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(452), S(584))
    local checkBtn
    checkBtn = debugCard:AddButton(S(767), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(564)
            log(S(614))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(279),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(624),
        S(311)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(567), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(520)
            return
        end
        local ok, err = log(S(654))
        if not ok then
            dumpBtn.Text = S(628)
            dumpCard:SetSubtitle(S(380) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(453) .. firstNode.Name .. S(492))
            dumpInstance(firstNode, log)
        else
            log(S(456))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(596) .. firstHolder.Name .. S(424))
            dumpInstance(firstHolder, log)
        else
            log(S(42))
        end
        dumpBtn.Text = S(477)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(213),
        S(404)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(16), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(376))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(87))
        if not tool then
            toolDumpBtn.Text = S(271)
            return
        end
        log(S(45) .. tool.Name .. S(500))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(280) .. attrName .. S(54) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(307)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(421),
        S(513)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(249), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(25)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(585),
        S(616)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(104), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(710)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(763),
        S(34)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(774), function()
        dumpGui()
        guiDumpBtn.Text = S(303)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(392),
        S(212)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(348), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(222)
    end)
    local handlersCard = debugTab:AddCard(
        S(676),
        S(667)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(363), function()
        inspectHandlers()
        handlersBtn.Text = S(775)
    end)
    local settingsTab = window:AddTab(S(474), S(681))
    persistentToggle(
        settingsTab:AddCard(S(361), S(105)),
        S(233),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(329), 100)
    settingsTab:AddSlider(S(590), S(145), 50, 150, savedScale, function(value)
        config.Set(S(367), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(438), 1)
    settingsTab:AddSegmented({ S(418), S(78), S(728) }, savedLanguage, function(index, value)
        local code = ({ S(725), S(11), S(171) })[index]
        config.Set(S(322), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(82), S(429), S(57) })[savedLanguage] or S(415))
    log(S(486) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
    end)
end
end
modules[S(724)] = function()
return {
    [79389059854988] = S(84),
}
end
import(S(343))(import)
