-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"MineSellItems", "interactionLogger", " ===", "ปิดไว้เป็นค่าเริ่มต้น - ไม่รู้ว่าการสุ่มหนึ่งครั้งเสียอะไรบ้าง", "BottomPanels", "Players", "ValueBase", "getgc", "ความเร็ววิ่ง", " doesn't exist", "games/my_anime_mine/debug_dump_sell_cards", "games/my_anime_mine/debug_probe_env", "MineGui", "core/logger", " ", "[BestMiner] slot ", "AutoPlace", "Auto Mine: ความเร็ว", " '", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "[AutoResearch] nothing bought (maxed out or not enough cash)", ")", "TextButton", "games/my_anime_mine/auto_sell", "  [const] ", "PlotIndex", " '", "ClickDetector", "[Interaction] ClickDetector clicked: ", "SlotFocusFrame", "MineEquipPickaxe", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", " '", "games/my_anime_mine/get_plot_parts", "menuScale", "Auto Mine: คลิกต่อรอบ", "[AutoPlace] ", "MouseButton1Click", "Open menu", "[MyAnimeMine] === Dump start ===", " cards", "autoUpgradePickaxeIndex", "MineTakeChest", "core/scheduler", "games/my_anime_mine/debug_probe_env", "Workspace", ": ", "[GuiDump] MineGui not found", "MineSpeed", "games/my_anime_mine/get_plot_parts", " -> ", "string", "TextLabel", "RunService", "UIPadding", "[SellCards] Card #", "TextLabel", "TextButton", "#", ") ===", "ProximityPrompt", "autoPlaceUpgradeWeak", " items", ")", "Done", "GuiButton", "games/my_anime_mine/auto_upgrade", "ให้ miner ทุกตัวถือ pickaxe ที่ดีที่สุดที่เรามี", "SellStand", "Damage", "Auto Research (Plot)", "", "CharLevel", "ChestFrame", "[Remotes] === total: ", "games/my_anime_mine/debug_inspect_handlers", "games/my_anime_mine/get_current_plot", " attributes ===", "ProximityPrompt", "needs us at the stand - keeping the teleport", "sale didn't go through", "[AutoSell] MineSellItems ", "en", "Dump", "Debug: Dump Game State", "GuiButton", "[AutoPlace] ", "fill", "autoPlaceInterval", "write reported success but the line isn't in ", " (Text=", "Auto Sell", "เดินทะลุกำแพงและพื้น", "CharMineSpeedLevel", "MineHotbarClient", "games/my_anime_mine/get_plot_parts", "[AutoUpgrade] rolled a mutation on ", "  (debug.getconstants unavailable)", "games/my_anime_mine/game_api", "costs ", "s", "Badge", "table", "autoUpgradeEquipPickaxe", "x", "Scroll", " from ", "[AutoChest] disabled", " -> ", "[AutoSell] ", "UICorner", "[Config] couldn't write ", " ", "\n", "getscripts", "/", "Workspace", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "  ", "ClickDetector", " = ", "'", "Auto Chest: รอบเช็ค", "UIListLayout", "lowest", "Players", "[^%d%.]", "[Probe] ", "ปุ่มเปิดเมนู", "[AutoChest] waiting: ", "[AutoSell] disabled", "autoUpgradeRollMutation", "Debug: Current Plot", "autoChestMinItems", "Container", "autoUpgrade", "VirtualInputManager", "MineChestClient", "core/teleport", "PlayerGui", "🐛", "RemoteFunction", "[Handlers] === start (open the relevant menus first) ===", "Locked miner slot", " parts", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "Debug: Dump Visible GUI", "games/my_anime_mine/debug_dump_chest", " remotes", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", " ", "[Probe] === start ===", "autoChestInterval", "autoUpgradePickaxeIndex", "[Probe] decompiled ", "/s", "MineGame", "[Handlers]   ", "[Probe] couldn't decompile ", "TextButton", "[ChestDump] === Player subtree ===", " (power ", "Done", "[AutoChest] collected ", "disabled", "core/config", "Activated", "core/i18n", " '", "HotbarFrame", "SellButton", "ReplicatedStorage", "Anti-AFK", "BasePart", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "Loot", "TextLabel", "leaderstats", "en-us", " ", "RemoteFunction", "TextLabel", ") on ", " ", " '", "", ": not found", "jumpPower", "ValueLabel", "[MyAnimeMine] no holders found", "core/config", "[AntiAFK] idle detected, simulating input", "Sell SelectAllButton", "Debug: Dump Backpack Tool", "MekPro", "games/my_anime_mine/game_api", "core/logger", "autoResearchInterval", "ไทย", "core/i18n", "Frame", "table", "MineUpgradeCharacter", "function", "JumpPower (ปกติ 50)", "Zones", "[SellCards] === total cards: ", "ProximityPrompt", "core/logger", " = ", "write reported success but ", "บิน (Fly)", "RemoteEvent", "", "ค้นหาฟังก์ชัน...", "[ChestDump] === Chest subtree ===", "[Probe] === end ===", "Text", "games/registry", "My Anime Mine · Mining Hub", "Text", "[Handlers] ", " = ", "  [attr] ", " = ", "Auto Upgrade: สุ่ม Mutation", "Frame", "core/logger", "[Scheduler] turn: ", "ฟาร์ม", "ต่ำสุดก่อน", "[Probe] PlayerScripts.UI.MineGame not found", "core/i18n", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "ReplicatedStorage", ": ", " = ", "Card", "Speed", "[^%.]+", "[MyAnimeMine] === Holder: ", " | ", "TextButton", "TextLabel", "All", "[MyAnimeMine] no nodes present right now", " x", "Chest", "games/my_anime_mine/auto_mine", "sl", "decompile", " ", "enabled", "Search functions...", "", "core/logger", "MekPro.log", "WalkSpeed (ปกติ 16)", "SellFrame", "%$%s*([%d,%.]+)%s*(%a*)", "Debug: Inspect Button Handlers", "sold ", "TextLabel", "[GameAPI] couldn't load the game's modules: ", "Pad", ",", "[MyAnimeMine] === Tool: ", "autoPlace", "action '", " (", "", "Folder", "button has no size on screen", " (ActionText=", "[Config] couldn't read ", "Dump", "languageIndex", "core/interaction_logger", "[Remotes] ", "PlayerScripts", "Content", "ValueLabel", "Content", "autoUpgradeMaxPerPass", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "  ", "TextButton", "Done", "UICorner", ": ", "Frame", "boolean", "MekPro/decompiled_", ")", "[AutoUpgrade] ", "games/my_anime_mine/debug_dump_chest", "-", "", "PlayerGui", "Content", "[Handlers] getconnections isn't available in this executor", "th", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "th", "table", "[MyAnimeMine] === Node: ", "X", "MineGui", "Debug: Dump All Remotes", "UserInputService", " ===", "[AutoPlace] disabled", "[Handlers] ", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "write failed", "enabled", "UserInputService", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "core/interaction_logger", "Error: ", "Players", "ไล่อัปเกรด miner ทุกกี่วินาที", "ScreenGui", "Instance", "[ChestDump] not standing in any plot", "locked slot", "GuiService", "Players", "core/anti_afk", "games/my_anime_mine/auto_research", " pickaxe ", "Players", "debug.getupvalues", "[Interaction] logger enabled - go click/interact with things now", "core/get_place_id", "Auto Research: ซื้อสูงสุดต่อรอบ", "  ", "Humanoid", "games/my_anime_mine/get_current_plot", "TabBar", "MineRollMutation", "MineBuyPickaxe", "antiAfk", "MineSlotFocusClient", "[Handlers] === end ===", " ===", "core/logger", "string", "[MekPro] settings file: ", "autoUpgradePriority", "[Interaction] logger disabled", ")", "Debug: Dump Chest Data", "core/ui", "Damage", "Plots", "ดีบัก", "Backpack", " = ", "", "ClickDetector", "Auto Upgrade: Pickaxe ที่จะซื้อ", "games/registry", "games/my_anime_mine/auto_research", "Check", "Holders", "ReplicatedStorage", "core/logger", "OwnerUserId", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Content", "[GameAPI] SendEvent failed: ", "BuyButton", "autoResearchMaxPerPass", "[Bb]uy", "TextLabel", "core/logger", "VirtualUser", "TextLabel", "[AutoUpgrade] bought pickaxe #", "[AutoPlace] placed ", " (", "s", "Backpack", "[AutoSell] enabled", ")", "lowest", " ===", "games/my_anime_mine/auto_chest", "", "en", "[MyAnimeMine] not standing in any plot", "debug.getconstants", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", "Chest TakeButton", "/", "getrenv", "table", "TextButton", "BodyVelocity", "Speed", "AutoResearch", "Workspace", " (", "TextLabel", "noclip", "  [attr] ", "CoreUI", "Auto Upgrade: รอบเช็ค", "", "autoMineClicks", "games/my_anime_mine/game_api", "table(", ") from ", "ProximityPrompt", "Tool", "core/teleport", "AutoChest", "getconnections", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "[AntiAFK] enabled - watching Player.Idled", "พิมพ์โครงสร้าง GameState.GetData2().MineGame เพื่อหา key ที่ remote ต้องใช้", ": ", " not found", "games/my_anime_mine/get_best_miner", " items)", "debug.getproto", "Frame", ") -> ", "", "[Handlers]   #", "ย่อขยายเมนูทั้งชุด", "core/get_me", "autoChest", " ", "MineSkillTreeClient", "table", ": ", "yes", "games/my_anime_mine/get_current_plot", "Players", " power ", "auto", "'", "Done", "s", "autoMineSpeed", "MineSpeed", "Unit Damage UpgradeButton", "HumanoidRootPart", "[StateDump] === MineGame ===", "AutoUpgrade", "core/logger", "games/my_anime_mine/debug_dump_sell_cards", "ขนาดเมนู", "⚙", "UIListLayout", "disabled", "[Handlers] === ", "core/click", "", "Speed", "[GuiDump] === visible MineGui tree ===", "walkSpeed", " ===", "defined at ", "autoSell", "table", "Modules", "getnilinstances", "Frame", "waiting", "[ChestDump] === ReplicatedStorage.", "fly", "autoResearch", "TextLabel", "RemoteEvent", ": ", "[AutoPlace] upgrading ", "Interaction Logger", ".lua", "core/humanoid", "CharSpeedLevel", "games/my_anime_mine/main", "[StateDump] no mine data (is the game API loaded?)", "TextButton", "neither appendfile nor writefile is available", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "Badge", "0 = เลือกอัตโนมัติ (ตัวที่ดีที่สุดที่ซื้อไหว)", "games/my_anime_mine/get_current_plot", "NodeCount", "อ่านป้ายบน chest ทุกกี่วินาที", "Damage", "UIScale", "ซื้อ pickaxe ตามที่เลือกด้านล่าง", "", "ClickDetector", "table", "function", "TextLabel", "%", "core/ui", "ValueBase", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "games/my_anime_mine/debug_inspect_handlers", "TextLabel", "^([%d,]+)$", " ", "open_menu_hint", "[AntiAFK] disabled", "ซื้อ research ทุก family ในโซนที่ปลดแล้วผ่าน remote ตรง (ไม่เปิด UI)", "TakeButton", "[Probe] === executor capabilities ===", "Dump", "[AutoUpgrade] ", "HumanoidRootPart", "Auto Chest: เกณฑ์เก็บ", "games/my_anime_mine/debug_dump_remotes", "SellFrame", "Debug: Dump Sell Cards", "Frame", "float", "x", "[StateDump] === end ===", "TextLabel", "  ", "TextLabel", " while selecting", "getloadedmodules", " (", "PlayerGui", "core/dispatch", "Dump", ")", "string", "enabled", "Luck", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " ", "Frame", "[ChestDump] === plot ", "'", "x", ": ", "games/my_anime_mine/auto_upgrade", "[AutoPlace] holders: ", "games/my_anime_mine/get_current_plot", "Humanoid", " = ", "search_placeholder", "autoUpgradePickaxe", "[AutoChest] enabled (collects once >= ", "  ", "games/my_anime_mine/game_api", "\n", "Done", "UI", "core/logger", "%$", "Frame", "[Probe] ", "Dump", "[AutoUpgrade] nothing upgraded (maxed out or not enough cash)", "auto", "fill", " still empty after placing ", " chars) -> ", "disabled", "[AutoResearch] ", "Players", "[AutoPlace] nothing in the hotbar to place", "Auto Chest", "Content", "games/my_anime_mine/debug_dump_gui", "MineGui", "core/anti_afk", "Research BuyButton", "Main", "[attr] ", "waiting", "unreadable", "/", "core/logger", " items (now ", "nothing to place or upgrade", "core/logger", "getcallbackvalue", "games/my_anime_mine/auto_chest", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "autoSellInterval", "AutoMine", "Players", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Dump", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "games/my_anime_mine/get_plot_parts", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "  [attr] ", "", "log failed", "[AutoPlace] fireproximityprompt error: ", "", "Damage", "TextBox", " stack(s)", "", "Players", "=", "Players", "RemoteFunction", "Auto Place: รอบเช็ค", "core/logger", "no plot", "ItemFrame", "games/my_anime_mine/game_api", "ProximityPrompt", "Auto Place: เปลี่ยนตัวอ่อนออก", ": ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "ScrollingFrame", "Scroll", " (Text=", "ตั้งค่า", ")", "[Probe] decompile unavailable - can't read the game's source this way", "'", "Auto Upgrade Unit", "auto", "มุดแมพ (Noclip)", "games/my_anime_mine/auto_place", "table", "games/my_anime_mine/debug_dump_remotes", "GuiObject", "CoreGui", "number", "", "')", "Players", "Debug: Dump Node/Holder", "", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "", "upgrade", "table", ":", "  [upvalue] ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", ") didn't take, trying the next one", "  [attr] ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[GuiDump] === end ===", "no isfile/readfile available to verify the write", "Detail", "no tool", "Debug: Probe Executor + Scripts", " connection(s)", "Players", "ChestCapacity", "Place", " (prompt says '", ")", "[ChestDump] plot has no Chest", "Content", "=empty", "table", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "MineWorld", "HumanoidRootPart", "^x(%d+)$", "games/my_anime_mine/auto_sell", "เปิดหน้า research ทุกกี่วินาที", "games/my_anime_mine/debug_dump_state", "en", "games/my_anime_mine/debug_dump_state", "[AutoChest] MineTakeChest had no effect (", "[AutoResearch] ", "TextLabel", "core/get_place_id", "NodeHealth", " -> ", "TextButton", "^%$", "core/logger", "=locked(", "MineEquipSlot", "languageIndex", "[Uu]nlock", "games/my_anime_mine/game_api", "PlayerGui", "This is not a supported place.", " <-- remote, likely fired by the real interaction", "th", "BodyPosition", "Frame", "Frame", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", ")", "  (debug.getupvalues unavailable)", "getscriptbytecode", "MineBuySkill", ")", "not found", "SelectAllButton", "[AutoPlace] slot ", "[Logger] failed to write ", "Players", "[Interaction] ProximityPrompt triggered: ", "Frame", "games/my_anime_mine/main", "AutoSell", "MineHUDClient", "Instance", "HttpService", "Players", "Dump", "Actions", "no", "'", "function", "autoMine", "ตามเครื่อง", "prompt disabled", " (", "Auto Place Miner", "menuScale", "games/my_anime_mine/game_api", "[AutoPlace] drifted out of range of ", "core/logger", "Search", ",", "core/logger", " = ", "games/my_anime_mine/debug_dump_gui", "แรงกระโดด", "core/logger", " ===", " -> ", "ItemName", "Mining", "core/get_leaderstat", "", "core/logger", "  ", "core/dispatch", "games/my_anime_mine/game_api", "auto", "none", "SellFrame", "core/teleport", "Empty miner slot", " ชิ้น", "TextButton", "StatsPanel", "Inspect", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Crew", "", "Done", "core/scheduler", "🏠", "s", "ลอย (Float)", "MineSellClient", "Players", "PlayerGui", "Mining", "[AutoPlace] couldn't pick up ", "getsenv", "MekPro", "  ", "games/my_anime_mine/debug_dump", "open_menu_hint", "Auto Sell: รอบขาย", "TextButton", "", "TextLabel", "games/my_anime_mine/debug_dump", "GuiObject", "  ", "th", "[AutoPlace] picked up ", "lowest", "'", " (MaxActivationDistance=%s)", ": ", "[AutoChest] can't read the chest yet", "(nothing to do)", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "games/my_anime_mine/get_best_miner", "[AutoMine] ", "core/logger", "UIStroke", "autoUpgradeInterval", "MineGui", "core/parse_money", "[ChestDump] === end ===", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "Damage", "UIListLayout", "core/logger", "Auto Mine", "Nodes", "games/my_anime_mine/auto_mine", "?", "UserInputService", "unreadable", "Auto Upgrade: แจก Pickaxe ให้ miner", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "hookfunction", "core/get_place_id", "Content", "Sell SellButton", "s", "[%X] ", "/config.json", "", "works from anywhere - dropping the teleport", "core/humanoid", "อังกฤษ", "games/my_anime_mine/get_plot_parts", " error: ", " (power ", "RemoteEvent", "RunService", "[AutoUpgrade] ", " (Text=", "TextLabel", "MineSpeed", "(", " = ", "[Interaction] Key pressed: %s (gameProcessed=%s)", " ", "UpgradeButton", "Header", "Probe", "search_placeholder", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "BasePart", "Workspace", "[Interaction] Button activated: ", "SkillTreeFullFrame", "", "Auto Research: รอบเช็ค", "", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "games/my_anime_mine/auto_place", "core/logger", "[Scheduler] "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(329)] = function()
return function(import)
local log = import(S(732))
local Players = game:GetService(S(126))
local VirtualUser = game:GetService(S(378))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(421))
        connection = player.Idled:Connect(function()
            log(S(192))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(511))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(460)] = function()
local VirtualInputManager = game:GetService(S(137))
local GuiService = game:GetService(S(327))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(273)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(191)] = function()
return function(import)
local HttpService = game:GetService(S(714))
local getPlaceId = import(S(679))
local log = import(S(377))
local ROOT_FOLDER = S(195)
local placeFolder = ROOT_FOLDER .. S(396) .. tostring(getPlaceId())
local filePath = placeFolder .. S(817)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(103) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(275) .. filePath .. S(478) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(112) .. filePath .. S(290) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(745)] = function()
return function(import)
    local getPlaceId = import(S(335))
    local registry = import(S(363))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(691))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(741)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(178))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(433)] = function()
local Players = game:GetService(S(639))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(338))
    local backpack = player:WaitForChild(S(358))
    return player, humanoid, backpack
end
return getMe
end
modules[S(812)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(820)] = function()
local Players = game:GetService(S(608))
local RunService = game:GetService(S(54))
local UserInputService = game:GetService(S(317))
local Workspace = game:GetService(S(841))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(549))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(669))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(400))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(174)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(840)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(694))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(200)] = function()
local Players = game:GetService(S(610))
local STRINGS = {
    en = {
        search_placeholder = S(254),
        open_menu_hint = S(39),
    },
    th = {
        search_placeholder = S(215),
        open_menu_hint = S(129),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(179)
    if locale:sub(1, 2):lower() == S(781) then
        return S(304)
    end
    return S(674)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(629) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(278)] = function()
return function(import)
local Players = game:GetService(S(6))
local Workspace = game:GetService(S(403))
local UserInputService = game:GetService(S(310))
local log = import(S(613))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(708) .. describe(prompt)
            .. S(274) .. prompt.ActionText .. S(625))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(29) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(842) .. describe(button)
            .. S(623) .. tostring(button.Text) .. S(22))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(61)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(28)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(86)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(351))
        return
    end
    connections = {}
    log(S(334))
    local playerGui = Players.LocalPlayer:WaitForChild(S(532))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(208)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(361)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(66)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(833),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(14)] = function()
local LOG_FILE = S(257)
local function log(message)
    local line = os.date(S(816)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(846))
            end
            appendfile(LOG_FILE, line .. S(114))
        elseif writefile then
            local existing = S(600)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(556))
        else
            error(S(487))
        end
        if not (isfile and readfile) then
            error(S(653))
        end
        if not isfile(LOG_FILE) then
            error(S(211) .. LOG_FILE .. S(10))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(90) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(706) .. LOG_FILE .. S(545) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(796)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(536) then
        return nil
    end
    local number, suffix = text:match(S(260))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(266), S(72))))
    if not value then
        return nil
    end
    if suffix ~= S(818) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(760)] = function()
return function(import)
local RunService = game:GetService(S(826))
local log = import(S(256))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(850) .. task_.name .. S(823) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(229) .. (currentTask and currentTask.name or S(788)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(750)] = function()
local Players = game:GetService(S(328))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(450))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(354)] = function()
return function(import)
local i18n = import(S(233))
local UserInputService = game:GetService(S(806))
local THEME = {
    BrandName = S(770),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(111))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(289))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(55))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(753))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(298)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(793))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(526))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(805)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(476))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(461)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(621))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(800))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(57))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(522))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(376))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(53))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(528))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(189))
            if not valueLbl then
                valueLbl = Instance.new(S(244))
                valueLbl.Name = S(282)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(489))
            if not badge then
                badge = Instance.new(S(182))
                badge.Name = S(102)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(486))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(471))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(428))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(58))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(641)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(502)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(541))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(227))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(775))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(214)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(696))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(457))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(287))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(218)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(323))
    screenGui.Name = S(408)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(635))
    local main = Instance.new(S(561))
    main.Name = S(579)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(495))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(709))
    tabBar.Name = S(340)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(124))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(201))
    header.Name = S(836)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(243))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(777))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(501))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(410)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(23))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(307)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(682))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(297)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(605))
    searchBox.Name = S(730)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(551))
    searchBox.Text = S(776)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(291))
    content.Name = S(813)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(695))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(405))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(510))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(263))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(221)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(607) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(435) .. (item.Subtitle or S(255))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(838))
        footerLabel.Text = i18n.T(S(773))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(589)] = function()
return function(import)
local gameApi = import(S(746))(import)
local log = import(S(849))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(553) .. minItemsToCollect .. S(426))
    else
        log(S(108))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(807) then
            log(S(787))
            lastReported = S(582)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(581) then
            log(S(130) .. count .. S(116) .. minItemsToCollect .. S(63))
            lastReported = S(472)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(164) .. count .. S(585) .. after .. S(698))
        else
            log(S(676) .. count
                .. S(681) .. tostring(after) .. S(352))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(249)] = function()
return function(import)
local getCurrentPlot = import(S(548))
local getPlotParts = import(S(96))
local log = import(S(197))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(120), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(791) .. (enabled and S(537) or S(569)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(848)] = function()
return function(import)
local Players = game:GetService(S(658))
local getCurrentPlot = import(S(491))
local getPlotParts = import(S(597))
local getMinerSlots = import(S(425))(import)
local teleport = import(S(417))
local gameApi = import(S(99))(import)
local log = import(S(743))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(751)
local LOCKED_SLOT_TEXT = S(144)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(265))
    return pad and pad:FindFirstChildWhichIsA(S(617), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(829)) then
            local digits = descendant.Text:match(S(508))
            if digits then
                return tonumber((digits:gsub(S(731), S(271))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(379)) and descendant.Text:match(S(683)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(326)
    end
    if not prompt.Enabled then
        return true, S(723)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(100) .. price
    end
    local action = prompt.ActionText or S(643)
    if action:match(S(688)) or action:match(S(375)) or action:match(S(560)) then
        return true, S(269) .. action .. S(122)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(517))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(602) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(685) .. why .. S(386))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(665))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(609) .. prompt.ObjectText .. S(831) .. power .. S(294))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(242))
    if line ~= lastSurvey then
        log(S(547) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(566), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(644), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(87) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(312))
    else
        lastAttempt = 0
        log(S(648))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(847),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(660), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(572))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(728) .. holder.Name .. S(529))
            return false
        end
        log(S(705) .. candidate.slot .. S(531) .. candidate.name
            .. S(649))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(37) .. holder.Name .. S(567) .. miner.name)
        return false
    end
    log(S(381) .. miner.name .. S(824) .. miner.power
        .. S(183) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(768) .. entry.name .. S(107) .. entry.holder.Name
            .. S(661) .. tostring(entry.prompt.ActionText) .. S(638))
        return false
    end
    log(S(782) .. entry.name .. S(162) .. entry.power
        .. S(414) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(586))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(88) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(479) .. target.holder.Name .. S(438) .. target.name
        .. S(404) .. target.power .. S(429) .. best.name .. S(270) .. best.power .. S(535))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(330)] = function()
return function(import)
local gameApi = import(S(196))(import)
local log = import(S(453))
local KNOWN_FAMILIES = { S(492), S(538), S(680) }
local checkInterval = 30
local maxPerPass = 5
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(677) .. (enabled and S(253) or S(458)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
local function unlockedZones()
    local mine = gameApi.GetMineData()
    local zones = mine and mine.UnlockedZones or {}
    local list = {}
    for zoneKey, unlocked in pairs(zones) do
        if unlocked then
            table.insert(list, zoneKey)
        end
    end
    table.sort(list)
    return list
end
local function skillLevel(zoneKey, family)
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    return skills and skills[family] or 0
end
local function familiesFor(zoneKey)
    local seen = {}
    local list = {}
    for _, family in ipairs(KNOWN_FAMILIES) do
        seen[family] = true
        table.insert(list, family)
    end
    local mine = gameApi.GetMineData()
    local skills = mine and mine.Skills and mine.Skills[zoneKey]
    if skills then
        for family in pairs(skills) do
            if not seen[family] then
                table.insert(list, family)
            end
        end
    end
    return list
end
local function runJob()
    busy = true
    local bought = 0
    for _, zoneKey in ipairs(unlockedZones()) do
        for _, family in ipairs(familiesFor(zoneKey)) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local before = skillLevel(zoneKey, family)
            gameApi.BuySkill(zoneKey, family)
            task.wait(0.4)
            local after = skillLevel(zoneKey, family)
            if after > before then
                log(S(570) .. zoneKey .. S(113) .. family .. S(252)
                    .. before .. S(738) .. after)
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(21))
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(671)] = function()
return function(import)
local Workspace = game:GetService(S(46))
local gameApi = import(S(616))(import)
local teleport = import(S(139))
local log = import(S(368))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(131))
    else
        lastAttempt = 0
        log(S(385))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(69))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(415), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(82)
                .. (sold and S(819)
                         or S(80)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(110) .. (sold and (S(262) .. #keys .. S(606)) or S(81)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(67)] = function()
return function(import)
local gameApi = import(S(412))(import)
local log = import(S(584))
local STATS = {
    { event = S(799), field = S(73) },
    { event = S(462), field = S(483) },
    { event = S(830), field = S(94) },
}
local checkInterval = 20
local maxPerPass = 5
local priority = S(387)
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local rollMutations = false
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(827) .. (enabled and S(316) or S(165)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function levelOf(holderKey, field)
    local entry = holders()[holderKey]
    return entry and entry[field] or nil
end
local function chooseStat(entry)
    if priority ~= S(783) then
        for _, stat in ipairs(STATS) do
            if stat.event == priority and entry[stat.field] then
                return { event = stat.event, field = stat.field, level = entry[stat.field] }
            end
        end
    end
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local level = entry[stat.field]
        if level and (not lowest or level < lowest.level) then
            lowest = { event = stat.event, field = stat.field, level = level }
        end
    end
    return lowest
end
local function findPickaxe()
    local pickaxes = gameApi.GetPickaxes()
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(pickaxes) do
            if pickaxe.index == pickaxeTarget and not pickaxe.owned then
                return pickaxe
            end
        end
        return nil
    end
    local money = gameApi.GetMoney()
    local best = nil
    for _, pickaxe in ipairs(pickaxes) do
        if not pickaxe.owned and pickaxe.cost <= money then
            if not best or pickaxe.cost > best.cost then
                best = pickaxe
            end
        end
    end
    return best
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    for _, current in ipairs(gameApi.GetPickaxes()) do
        if current.index == pickaxe.index and current.owned then
            log(S(380) .. pickaxe.index .. S(180) .. pickaxe.name)
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local owned = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.owned then
            table.insert(owned, pickaxe)
        end
    end
    if #owned == 0 then
        return
    end
    table.sort(owned, function(a, b)
        return a.cost > b.cost
    end)
    local holderKeys = {}
    for holderKey in pairs(holders()) do
        table.insert(holderKeys, holderKey)
    end
    table.sort(holderKeys)
    for position, holderKey in ipairs(holderKeys) do
        local wanted = owned[position]
        if not wanted then
            break
        end
        local entry = holders()[holderKey]
        if entry and entry.Pickaxe ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(295) .. holderKey .. S(331)
                .. tostring(entry.Pickaxe or S(748)) .. S(51) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function runJob()
    busy = true
    local bought = 0
    for holderKey, entry in pairs(holders()) do
        if not enabled or bought >= maxPerPass then
            break
        end
        local stat = chooseStat(entry)
        if stat then
            gameApi.UpgradeCharacter(holderKey, stat.event)
            task.wait(0.4)
            local after = levelOf(holderKey, stat.field)
            if after and after > stat.level then
                log(S(516) .. holderKey .. S(15) .. stat.event .. S(509)
                    .. stat.level .. S(109) .. after)
                bought = bought + 1
            end
        end
    end
    if buyPickaxe and enabled and bought < maxPerPass and tryPickaxe() then
        bought = bought + 1
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled then
        for holderKey in pairs(holders()) do
            if bought >= maxPerPass then
                break
            end
            gameApi.RollMutation(holderKey)
            log(S(97) .. holderKey)
            bought = bought + 1
            task.wait(0.4)
        end
    end
    if bought == 0 then
        log(S(564))
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(778)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(497)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(169) .. child.Name .. S(444)
        if child:IsA(S(79)) then
            line = line .. string.format(
                S(20),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(498)) then
            line = line .. string.format(S(785), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(477)) or child:IsA(S(611)) then
            line = line .. S(692)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(224) .. attrName .. S(237) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(554))
    end
end
return dumpInstance
end
modules[S(296)] = function()
return function(import)
local Players = game:GetService(S(765))
local ReplicatedStorage = game:GetService(S(367))
local getCurrentPlot = import(S(339))
local getPlotParts = import(S(50))
local log = import(S(347))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(580) .. name .. S(550) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(744))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(185) .. child.Name .. S(543)
            if child:IsA(S(504)) then
                line = line .. S(223) .. tostring(child.Value)
            elseif child:IsA(S(678)) or child:IsA(S(399)) then
                line = line .. S(828) .. tostring(child.Text) .. S(702)
            end
            log(line)
            dumpSubtree(child, indent .. S(780))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(325))
        return 0
    end
    log(S(542) .. plot.Name .. S(78))
    dumpAttributes(plot, S(337))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(663))
        return 0
    end
    log(S(216))
    dumpSubtree(parts.Chest, S(430))
    local player = Players.LocalPlayer
    log(S(161))
    dumpSubtree(player, S(742))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(473) .. player.Name .. S(311))
        dumpSubtree(mine, S(603))
    end
    log(S(797))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(734)] = function()
return function(import)
local Players = game:GetService(S(707))
local log = import(S(209))
local function dumpVisible(root, indent)
    indent = indent or S(637)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(634)) and child.Visible then
            local line = indent .. child.ClassName .. S(33) .. child.Name .. S(627)
            if child:IsA(S(177)) or child:IsA(S(160)) then
                line = line .. S(91) .. tostring(child.Text) .. S(662)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(407) .. attrName .. S(225) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(119))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(766))
    local mineGui = playerGui and playerGui:FindFirstChild(S(13))
    if not mineGui then
        log(S(48))
        return
    end
    log(S(463))
    dumpVisible(mineGui)
    log(S(652))
end
return dumpMineGui
end
end
modules[S(633)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(235))
local log = import(S(729))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(213)) or descendant:IsA(S(142)) then
            count = count + 1
            log(S(279) .. descendant.ClassName .. S(236) .. descendant:GetFullName())
        end
    end
    log(S(75) .. count .. S(3))
    return count
end
return dumpRemotes
end
end
modules[S(11)] = function()
return function(import)
local Players = game:GetService(S(332))
local log = import(S(559))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(299))
    local mineGui = playerGui and playerGui:FindFirstChild(S(308))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(749))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(283)) and sellFrame.Content:FindFirstChild(S(106))
    if not scroll then
        log(S(756))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(238) or card:IsA(S(779)) then
            count = count + 1
            log(S(56) .. count .. S(619) .. card.ClassName .. S(27) .. card.Name .. S(784))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(650) .. attrName .. S(359) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(286) .. child.ClassName .. S(19) .. child.Name .. S(719)
                if child:IsA(S(7)) then
                    line = line .. S(210) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(207) .. count .. S(346))
    return count
end
return dumpSellCards
end
end
modules[S(673)] = function()
return function(import)
local gameApi = import(S(727))(import)
local log = import(S(684))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(499) then
        local count = 0
        for _ in pairs(value) do
            count = count + 1
        end
        return S(413) .. count .. S(64)
    end
    return valueType .. S(151) .. tostring(value)
end
local function dumpTable(value, indent, depth)
    for key, entry in pairs(value) do
        log(indent .. tostring(key) .. S(121) .. describe(entry))
        if type(entry) == S(632) and depth > 0 then
            dumpTable(entry, indent .. S(527), depth - 1)
        end
    end
end
local function dumpState()
    local mine = gameApi.GetMineData()
    if not mine then
        log(S(485))
        return 0
    end
    log(S(451))
    dumpTable(mine, S(771), 2)
    log(S(525))
    return 1
end
return dumpState
end
end
modules[S(76)] = function()
return function(import)
local Players = game:GetService(S(571))
local log = import(S(792))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(713) then
        return value.ClassName .. S(184) .. value:GetFullName()
    end
    if valueType == S(645) then
        return S(398)
    end
    if valueType == S(720) then
        return S(204)
    end
    return valueType .. S(540) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(324) then
        return value:IsA(S(825)) or value:IsA(S(181)) or value:IsA(S(272))
    end
    return valueType == S(52) or valueType == S(636) or valueType == S(292)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(250))
    end)
    if ok and source then
        log(indent .. S(466) .. tostring(source) .. S(646) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(305) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(25) .. describe(constant))
            end
        end
    else
        log(indent .. S(98))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(468) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(647) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(699))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(222) .. label .. S(187))
        return
    end
    log(S(459) .. label .. S(382) .. button:GetFullName() .. S(60))
    if not getconnections then
        log(S(301))
        return
    end
    for _, signalName in ipairs({ S(167), S(38) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(313) .. signalName .. S(47) .. #connections .. S(657))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(431) .. index)
                    inspectFunction(fn, S(158))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(140))
    local current = playerGui and playerGui:FindFirstChild(S(795))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(143))
    inspectButton(S(395), find({ S(74), S(371), S(717), S(513) }))
    inspectButton(S(193), find({ S(520), S(574), S(704) }))
    inspectButton(S(814), find({ S(259), S(300), S(171) }))
    inspectButton(S(578), find({ S(843), S(654), S(373) }))
    local statsScroll = find({ S(30), S(5), S(754), S(622) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(494))
    inspectButton(S(449), damageRow and damageRow:FindFirstChild(S(835)))
    log(S(345))
end
return inspectHandlers
end
end
modules[S(45)] = function()
return function(import)
local Players = game:GetService(S(441))
local log = import(S(228))
local CAPABILITIES = {
    S(251), S(700), S(115), S(8), S(530),
    S(419), S(588), S(769), S(397), S(393),
    S(333), S(427), S(811), S(470),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(240)) do
        if type(target) ~= S(666) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(514))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(562) .. name .. S(733) .. ((ok and type(value) == S(500)) and S(439) or S(718)))
    end
end
local WANTED_SCRIPTS = {
    S(138),
    S(764),
    S(436),
    S(344),
    S(95),
    S(712),
}
local function tryDecompile()
    if not decompile then
        log(S(626))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(280))
    local ui = playerScripts and playerScripts:FindFirstChild(S(558))
    local mineGame = ui and ui:FindFirstChild(S(157))
    if not mineGame then
        log(S(232))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(128) .. name .. S(424))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(348) and #source > 0 then
                local path = S(293) .. name .. S(481)
                local written = pcall(writefile, path, source)
                log(S(155) .. name .. S(724) .. #source .. S(568)
                    .. (written and path or S(315)))
            else
                log(S(159) .. name .. S(423) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(152))
    reportCapabilities()
    tryDecompile()
    log(S(217))
end
return probe
end
end
modules[S(555)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(172))
local log = import(S(801))
local api = {}
local gameState, mineInventory, mineConfig
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(469), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(264) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetMineData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return nil
    end
    return data.MineGame
end
function api.GetMoney()
    if not modules() then
        return 0
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return 0
    end
    return tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(202))
            if buyableWithCash then
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    owned = owned[def.Name] ~= nil,
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(372) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(43))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(245), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(437) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(1), keys)
end
function api.EquipSlot(key)
    return send(S(686), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(342), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(31), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(341), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(nodeId, family)
    return send(S(701), nodeId, family)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(203), characterKey, stat == S(767) and S(49) or stat)
end
return api
end
end
modules[S(790)] = function()
return function(import)
local Players = game:GetService(S(715))
local log = import(S(587))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(690))
    local mineGui = playerGui and playerGui:FindFirstChild(S(576))
    local hotbar = mineGui and mineGui:FindFirstChild(S(170))
    local content = hotbar and hotbar:FindFirstChild(S(664))
    return content and content:FindFirstChild(S(135))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(507)) then
            local count = descendant.Text:match(S(670))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(615))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(281))
    local nameLabel = content and content:FindFirstChild(S(739))
    local damageLabel = content and content:FindFirstChild(S(604))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(390) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(127), S(844)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(583) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(16) .. slot .. S(786) .. info.name
                        .. S(442) .. info.power .. S(247) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(440)] = function()
local Players = game:GetService(S(593))
local Workspace = game:GetService(S(117))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(668))
    local plots = mineWorld and mineWorld:FindFirstChild(S(356))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(369)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(34)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(366)),
        Nodes = plot:FindFirstChild(S(803)),
        Chest = plot:FindFirstChild(S(248)),
        Zones = plot:FindFirstChild(S(206)),
        Crew = plot:FindFirstChild(S(757)),
        Loot = plot:FindFirstChild(S(176)),
        PlotIndex = plot:GetAttribute(S(26)),
        ChestCapacity = plot:GetAttribute(S(659)),
    }
end
return getPlotParts
end
modules[S(484)] = function()
return function(import)
    local Players = game:GetService(S(321))
    local createWindow = import(S(503))(import)
    local i18n = import(S(168))
    local antiAfk = import(S(577))(import)
    local interactionLogger = import(S(319))(import)
    local scheduler = import(S(44))(import)
    local config = import(S(166))(import)
    local log = import(S(736))
    local window = createWindow({
        Subtitle = S(220),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local getCurrentPlot = import(S(77))
    local getPlotParts = import(S(822))
    local dumpInstance = import(S(772))
    local dumpRemotes = import(S(519))(import)
    local dumpSellCards = import(S(454))(import)
    local dumpGui = import(S(575))(import)
    local dumpChest = import(S(148))(import)
    local inspectHandlers = import(S(506))(import)
    local probeEnv = import(S(12))(import)
    local dumpState = import(S(675))(import)
    local humanoid = import(S(482))
    local autoMine = import(S(804))(import)
    local autoPlace = import(S(631))(import)
    local autoChest = import(S(389))(import)
    local autoSell = import(S(24))(import)
    local autoUpgrade = import(S(546))(import)
    local autoResearch = import(S(364))(import)
    local gameApi = import(S(689))(import)
    scheduler.RegisterStandalone(S(592), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(418), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(17), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(452), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(402), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(711), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(230), S(761))
    persistentToggle(farmTab:AddCard(
        S(802),
        S(594)
    ), S(721), autoMine.SetEnabled)
    persistentSlider(farmTab, S(18), S(303),
        1, 30, S(447), 5, autoMine.SetCyclesPerSecond, S(156))
    persistentSlider(farmTab, S(36), S(420),
        1, 10, S(411), 1, autoMine.SetClicksPerCycle, S(524))
    persistentToggle(farmTab:AddCard(
        S(725),
        S(839)
    ), S(268), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(612), S(810),
        1, 60, S(89), 3, autoPlace.SetCheckInterval, S(762))
    persistentToggle(
        farmTab:AddCard(S(618),
            S(175)),
        S(62), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(573),
        S(539)
    ), S(434), autoChest.SetEnabled)
    persistentSlider(farmTab, S(518), S(234),
        50, 3000, S(134), 100, autoChest.SetMinItems, S(752))
    persistentSlider(farmTab, S(123), S(493),
        1, 60, S(153), 3, autoChest.SetCheckInterval, S(446))
    persistentToggle(farmTab:AddCard(
        S(92),
        S(620)
    ), S(467), autoSell.SetEnabled)
    persistentSlider(farmTab, S(774), S(667),
        10, 300, S(591), 45, autoSell.SetCheckInterval, S(383))
    persistentToggle(farmTab:AddCard(
        S(628),
        S(150)
    ), S(136), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(409), S(322),
        5, 300, S(794), 20, autoUpgrade.SetInterval, S(101))
    persistentSlider(farmTab, S(146), S(651),
        1, 20, S(284), 5, autoUpgrade.SetMaxPerPass, S(544))
    persistentSegmented(
        farmTab,
        { S(231), S(355), S(239), S(740) },
        { S(125), S(70), S(401), S(448) },
        S(350), 1, autoUpgrade.SetPriority
    )
    persistentToggle(
        farmTab:AddCard(S(285),
            S(496)),
        S(552), autoUpgrade.SetBuyPickaxe
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeNames = {}
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeNames[pickaxe.index] = pickaxe.name
    end
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(490)
            end
            return S(59) .. index .. S(834) .. (pickaxeNames[index] or S(697))
        end
        local savedPick = config.Get(S(154), 0)
        pickaxeCard = farmTab:AddSlider(
            S(362),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(42), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(360)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(808),
            S(68)),
        S(104), autoUpgrade.SetEquipPickaxes
    )
    persistentToggle(
        farmTab:AddCard(S(226),
            S(4)),
        S(132), autoUpgrade.SetRollMutations
    )
    persistentToggle(farmTab:AddCard(
        S(71),
        S(512)
    ), S(475), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(845), S(672),
        10, 600, S(198), 30, autoResearch.SetCheckInterval, S(815))
    persistentSlider(farmTab, S(336), S(598),
        1, 20, S(374), 5, autoResearch.SetMaxPerPass, S(105))
    local debugTab = window:AddTab(S(357), S(141))
    persistentToggle(
        debugTab:AddCard(
            S(480),
            S(789)
        ),
        S(2),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(133), S(370))
    local checkBtn
    checkBtn = debugCard:AddButton(S(365), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(703)
            log(S(392))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(118),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(640),
        S(488)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(84), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(614)
            return
        end
        local ok, err = log(S(40))
        if not ok then
            dumpBtn.Text = S(601)
            dumpCard:SetSubtitle(S(320) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(306) .. firstNode.Name .. S(737))
            dumpInstance(firstNode, log)
        else
            log(S(246))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(241) .. firstHolder.Name .. S(465))
            dumpInstance(firstHolder, log)
        else
            log(S(190))
        end
        dumpBtn.Text = S(759)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(194),
        S(642)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(595), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(384))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(416))
        if not tool then
            toolDumpBtn.Text = S(655)
            return
        end
        log(S(267) .. tool.Name .. S(388))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(599) .. attrName .. S(832) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(65)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(309),
        S(318)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(563), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(149)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(521),
        S(590)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(716), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(41)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(147),
        S(314)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(515), function()
        dumpGui()
        guiDumpBtn.Text = S(557)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(353),
        S(32)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(276), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(145)
    end)
    local handlersCard = debugTab:AddCard(
        S(261),
        S(809)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(755), function()
        inspectHandlers()
        handlersBtn.Text = S(445)
    end)
    local probeCard = debugTab:AddCard(
        S(656),
        S(394)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(837), function()
        probeEnv()
        probeBtn.Text = S(288)
    end)
    local stateCard = debugTab:AddCard(
        S(85),
        S(422)
    )
    local stateBtn
    stateBtn = stateCard:AddButton(S(534), function()
        dumpState()
        stateBtn.Text = S(163)
    end)
    humanoid.Start()
    persistentSlider(debugTab, S(9), S(258),
        8, 200, S(464), 16, humanoid.SetWalkSpeed, S(186))
    persistentSlider(debugTab, S(735), S(205),
        30, 300, S(188), 50, humanoid.SetJumpPower, S(758))
    persistentToggle(
        debugTab:AddCard(S(212), S(596)),
        S(474), humanoid.SetFlying
    )
    persistentToggle(
        debugTab:AddCard(S(630), S(93)),
        S(406), humanoid.SetNoclip
    )
    persistentToggle(
        debugTab:AddCard(S(763), S(798)),
        S(523), humanoid.SetFloating
    )
    local settingsTab = window:AddTab(S(624), S(456))
    persistentToggle(
        settingsTab:AddCard(S(173), S(505)),
        S(343),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(726), 100)
    settingsTab:AddSlider(S(455), S(432), 50, 150, savedScale, function(value)
        config.Set(S(35), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(277), 1)
    settingsTab:AddSegmented({ S(722), S(821), S(199) }, savedLanguage, function(index, value)
        local code = ({ S(565), S(391), S(693) })[index]
        config.Set(S(687), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(443), S(83), S(302) })[savedLanguage] or S(747))
    log(S(349) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        humanoid.Stop()
    end)
end
end
modules[S(219)] = function()
return {
    [79389059854988] = S(710),
}
end
import(S(533))(import)
