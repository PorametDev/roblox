-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"en", "ค้นหาฟังก์ชัน...", "UpgradePart", "th", "", "TextBox", "ProximityPrompt", "", "Plots", "[AutoChest] disabled", "games/my_anime_mine/get_plot_parts", "", "Pad", ")", "Label", " (MaxActivationDistance=%s)", "TextLabel", "write reported success but the line isn't in ", "Content", "RemoteEvent", "[AutoSell] enabled", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "ProximityPrompt", "games/my_anime_mine/auto_chest", "games/my_anime_mine/auto_upgrade", "MouseButton1Up", " cards", "ProximityPrompt", "CoreGui", "PlayerGui", "games/my_anime_mine/debug_dump", "ValueBase", "core/logger", "[AutoSell] SellFrame.Visible = ", ")", "Dump", "[AutoChest] clicked TakeButton", "'", "[Interaction] ProximityPrompt triggered: ", "Workspace", "Workspace", "th", "Players", "TabBar", "MekPro.log", "core/get_place_id", ": ", "VirtualUser", "[Interaction] ClickDetector clicked: ", "", "Scroll", "RemoteFunction", "Backpack", "games/my_anime_mine/debug_dump", "ขนาดเมนู", " holders checked)", "Anti-AFK", " ", "games/my_anime_mine/debug_dump_remotes", "core/get_me", "Backpack", "RemoteEvent", "[AutoChest] enabled", " = ", "RunService", "games/my_anime_mine/auto_chest", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Badge", "core/interaction_logger", " remotes", "Search", "Debug: Dump All Remotes", " ===", "games/my_anime_mine/get_plot_parts", "core/logger", "TextLabel", "core/anti_afk", "core/ui", "games/my_anime_mine/auto_mine", "[AutoSell] disabled", "[Interaction] Key pressed: %s (gameProcessed=%s)", "ClickDetector", "[AutoPlace] enabled", "[AutoPlace] fireproximityprompt error: ", "Dump", "[GuiDump] === visible MineGui tree ===", "[Interaction] Button activated: ", "[MyAnimeMine] === Node: ", "PlayerGui", " <-- remote, likely fired by the real interaction", "games/my_anime_mine/debug_dump_gui", "RunService", "games/my_anime_mine/get_current_plot", "[MyAnimeMine] === Tool: ", "Tool", "games/my_anime_mine/get_current_plot", "en-us", " (ActionText=", "core/logger", "SellFrame", "TextLabel", "TextLabel", "search_placeholder", ": ", "  [attr] ", "Loot", "SellStand", "Frame", "games/my_anime_mine/auto_place", "  [attr] ", "GuiObject", "Debug: Dump Sell Cards", "TextButton", "-", "games/my_anime_mine/auto_mine", "TextLabel", "Dump", "core/i18n", "Check", "not found", "", "games/my_anime_mine/get_current_plot", "\n", "MekPro", "UIScale", "X", "core/dispatch", "[AutoUpgrade] enabled", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "Content", "games/my_anime_mine/main", "Main", "core/ui", "[AntiAFK] disabled", "MekProOriginalPosition", "Auto Sell", "[MyAnimeMine] no holders found", "This is not a supported place.", "\n", "Search functions...", "PlotIndex", "⚙", "Activated", "TextButton", "MineGui", "Frame", "MouseButton1Up", "TextLabel", "no humanoid", "Auto Mine", "Holders", "%", " ===", "ChestCapacity", "core/interaction_logger", "[AutoSell] clicked SelectAllButton", "[AutoSell] fireproximityprompt error: ", "Nodes", "core/logger", "", "Humanoid", "core/i18n", "Debug: Current Plot", "TextLabel", "core/dispatch", "  [attr] ", "[AutoSell] clicked SellButton", "games/my_anime_mine/debug_dump_sell_cards", "", "MekProOriginalPosition", "Workspace", "MekProOriginalPosition", "TakeButton", ": ", "UserInputService", "UIStroke", "MineGui", "SellButton", "ProximityPrompt", "[MyAnimeMine] === Holder: ", "[MyAnimeMine] no nodes present right now", "TextButton", "Auto Upgrade", "games/my_anime_mine/get_plot_parts", "Sell for", "Tool", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "ValueLabel", "Frame", " on ", "games/my_anime_mine/debug_dump_sell_cards", "", "ClickDetector", "auto", "not standing in any plot", "ตั้งค่า", "Debug: Dump Visible GUI", "UIListLayout", "core/logger", "Humanoid", "[GuiDump] MineGui not found", "MouseButton1Click", "[AutoChest] fireproximityprompt error: ", "PlayerGui", "ย่อขยายเมนูทั้งชุด", "RunService", "Frame", "TextLabel", "MouseButton1Down", "[AutoMine] enabled", "core/i18n", "", "Badge", "TextButton", "Frame", "MineWorld", "Crew", "core/logger", "[AutoPlace] disabled", "core/get_leaderstat", "Players", "ScrollingFrame", "open_menu_hint", "'", "no plot", "games/my_anime_mine/auto_sell", "core/logger", "Backpack", "[Remotes] === total: ", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "ClickDetector", "open_menu_hint", "[Interaction] logger disabled", "OwnerUserId", "Frame", "UICorner", "TextButton", "  ", "[SellCards] === total cards: ", " ===", "ตามเครื่อง", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "UIListLayout", "[MyAnimeMine] === Dump start ===", "write reported success but ", "", "Activated", "en", "core/logger", "Empty miner slot", "neither appendfile nor writefile is available", "[Remotes] ", "core/logger", "RunService", "Debug: Dump Node/Holder", "ปุ่มเปิดเมนู", " = ", "[%X] ", "UIListLayout", "Players", "ScreenGui", "Players", "SelectAllButton", "ไทย", "MekProOriginalPosition", "Content", "Players", "games/my_anime_mine/auto_sell", "", "GuiButton", "Dump", "Players", "UserInputService", "no isfile/readfile available to verify the write", "HumanoidRootPart", "Frame", "no character/backpack", " doesn't exist", "Content", "TextLabel", "PlayerGui", " = ", "Chest", "ฟาร์ม", "PlayerGui", "no empty holder found (", "HumanoidRootPart", "plot has no Holders folder", "RunService", "[AutoChest] ChestFrame.Visible = ", "Auto Place Miner", "Frame", "Interaction Logger (debug)", "Players", "games/registry", "no tool in Backpack (all miners already placed?)", "UIPadding", "%", "core/logger", " '", "games/my_anime_mine/get_current_plot", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "TextButton", "ValueLabel", " (Text=", "SellFrame", "th", "[AntiAFK] enabled - watching Player.Idled", "'", "Done", "RemoteFunction", "core/logger", "My Anime Mine · Mining Hub", " '", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", " '", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "TextLabel", "MouseButton1Down", "games/my_anime_mine/get_current_plot", "TextLabel", "Debug: Dump Backpack Tool", "TextButton", "HumanoidRootPart", "GuiButton", "Error: ", "?", "Frame", "Text", "MineGui", "TextLabel", "[Logger] failed to write ", " '", "Players", "อังกฤษ", "TextButton", " ===", "GuiObject", "", "[AntiAFK] idle detected, simulating input", "games/my_anime_mine/auto_upgrade", "search_placeholder", " = ", "core/get_place_id", "[AutoMine] disabled", "games/my_anime_mine/debug_dump_remotes", "games/my_anime_mine/get_current_plot", "Select All", "Auto Chest", "no tool", "ClickDetector", "TextLabel", "leaderstats", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Frame", "ReplicatedStorage", "  [attr] ", "auto", "Text", "ClickDetector", "[Interaction] logger enabled - go click/interact with things now", "[AutoPlace] ", "Players", "ProximityPrompt", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "games/my_anime_mine/get_plot_parts", "Frame", "Content", "MekProOriginalPosition", "ProximityPrompt", "log failed", "Card", "Done", "Done", "core/logger", "core/anti_afk", "games/my_anime_mine/get_plot_parts", "[AutoPlace] placed ", "  ", "[MyAnimeMine] not standing in any plot", "Zones", " = ", " ===", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "UICorner", "MouseButton1Click", "ChestFrame", "TextButton", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "MekProOriginalPosition", "Players", "Header", "Dump", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "[AutoUpgrade] disabled", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "games/my_anime_mine/auto_place", "Players", "Open menu", "CoreUI", "games/my_anime_mine/debug_dump_gui", "🏠", "", "MineGui", "[SellCards] Card #", ")", "'", "games/registry", " (Text=", "games/my_anime_mine/main", "games/my_anime_mine/get_plot_parts", "Actions", "[GuiDump] === end ===", "  "}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(377)] = function()
return function(import)
local log = import(S(199))
local Players = game:GetService(S(267))
local VirtualUser = game:GetService(S(48))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(308))
        connection = player.Idled:Connect(function()
            log(S(341))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(134))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(165)] = function()
return function(import)
    local getPlaceId = import(S(345))
    local registry = import(S(295))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(138))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(220)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(354))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(60)] = function()
local Players = game:GetService(S(392))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(200))
    local backpack = player:WaitForChild(S(53))
    return player, humanoid, backpack
end
return getMe
end
modules[S(46)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(118)] = function()
local Players = game:GetService(S(43))
local STRINGS = {
    en = {
        search_placeholder = S(140),
        open_menu_hint = S(400),
    },
    th = {
        search_placeholder = S(2),
        open_menu_hint = S(256),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(97)
    if locale:sub(1, 2):lower() == S(4) then
        return S(42)
    end
    return S(248)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(194) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(69)] = function()
return function(import)
local Players = game:GetService(S(399))
local Workspace = game:GetService(S(41))
local UserInputService = game:GetService(S(175))
local log = import(S(312))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(39) .. describe(prompt)
            .. S(98) .. prompt.ActionText .. S(35))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(49) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(87) .. describe(button)
            .. S(305) .. tostring(button.Text) .. S(14))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(365)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(193)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(326)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(233))
        return
    end
    connections = {}
    log(S(362))
    local playerGui = Players.LocalPlayer:WaitForChild(S(89))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(28)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(352)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(270)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(81),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(218)] = function()
local LOG_FILE = S(45)
local function log(message)
    local line = os.date(S(258)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(192))
            end
            appendfile(LOG_FILE, line .. S(139))
        elseif writefile then
            local existing = S(404)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(123))
        else
            error(S(251))
        end
        if not (isfile and readfile) then
            error(S(274))
        end
        if not isfile(LOG_FILE) then
            error(S(245) .. LOG_FILE .. S(278))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(18) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(333) .. LOG_FILE .. S(174) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(78)] = function()
return function(import)
local i18n = import(S(211))
local UserInputService = game:GetService(S(273))
local THEME = {
    BrandName = S(124),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(236))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(386))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(297))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(237))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(121)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(176))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(148))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(328)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(17))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(8)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(222))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(259))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(101))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(146))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(322))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(332))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(280))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(304))
            if not valueLbl then
                valueLbl = Instance.new(S(76))
                valueLbl.Name = S(188)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(68))
            if not badge then
                badge = Instance.new(S(319))
                badge.Name = S(213)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(182))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(215))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(276))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(337))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(269)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(152))
        local track = Instance.new(S(368))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(207))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(389))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(212)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(298)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(189))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(198))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(214))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(360)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(261))
    screenGui.Name = S(401)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(29))
    local main = Instance.new(S(292))
    main.Name = S(132)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(125))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(235))
    tabBar.Name = S(44)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(243))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(108))
    header.Name = S(393)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(303))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(116))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(102))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(12)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(324))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(126)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(144))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(114)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(6))
    searchBox.Name = S(71)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(343))
    searchBox.Text = S(340)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(329))
    content.Name = S(130)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(356))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(353))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(223))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(208))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(330)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(169) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(58) .. (item.Subtitle or S(246))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(103))
        footerLabel.Text = i18n.T(S(232))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(66)] = function()
return function(import)
local Players = game:GetService(S(364))
local RunService = game:GetService(S(65))
local getCurrentPlot = import(S(93))
local getPlotParts = import(S(378))
local log = import(S(33))
local FIRE_INTERVAL = 0.5
local HIDDEN_POSITION = UDim2.new(3, 0, 3, 0)
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(204))
    local mineGui = playerGui and playerGui:FindFirstChild(S(405))
    return mineGui and mineGui:FindFirstChild(S(388))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(279))
    local actions = content and content:FindFirstChild(S(413))
    return actions and actions:FindFirstChild(S(173))
end
local function clickButton(button)
    for _, signalName in ipairs({ S(387), S(143), S(209), S(26) }) do
        local signal = button[signalName]
        if signal then
            pcall(function()
                firesignal(signal)
            end)
        end
    end
end
local autoChest = {}
local connection = nil
function autoChest.SetEnabled(enabled)
    if enabled == autoChest.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        local chestFrame = getChestFrame()
        if chestFrame then
            chestFrame.Position = chestFrame:GetAttribute(S(391)) or chestFrame.Position
        end
        log(S(10))
        return
    end
    log(S(63))
    local lastFire = 0
    local lastVisibleState = nil
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local chestFrame = getChestFrame()
        local visible = chestFrame ~= nil and chestFrame.Visible
        if visible ~= lastVisibleState then
            log(S(290) .. tostring(visible))
            lastVisibleState = visible
            if visible and chestFrame:GetAttribute(S(370)) == nil then
                chestFrame:SetAttribute(S(265), chestFrame.Position)
            end
        end
        if visible then
            chestFrame.Position = HIDDEN_POSITION
            local takeButton = getTakeButton(chestFrame)
            if takeButton then
                clickButton(takeButton)
                log(S(37))
            end
        else
            local plot = getCurrentPlot()
            local parts = plot and getPlotParts(plot)
            local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(371), true)
            if prompt then
                local ok, err = pcall(function()
                    fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
                end)
                if not ok then
                    log(S(203) .. tostring(err))
                end
            end
        end
    end)
end
function autoChest.IsEnabled()
    return connection ~= nil
end
return autoChest
end
end
modules[S(79)] = function()
return function(import)
local RunService = game:GetService(S(206))
local getCurrentPlot = import(S(301))
local getPlotParts = import(S(74))
local log = import(S(99))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(82), true)
end
local autoMine = {}
local connection = nil
function autoMine.SetEnabled(enabled)
    if enabled == autoMine.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(346))
        return
    end
    log(S(210))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Nodes then
            return
        end
        for _, node in ipairs(parts.Nodes:GetChildren()) do
            local detector = findClickDetector(node)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoMine.IsEnabled()
    return connection ~= nil
end
return autoMine
end
end
modules[S(398)] = function()
return function(import)
local Players = game:GetService(S(272))
local RunService = game:GetService(S(92))
local getCurrentPlot = import(S(348))
local getPlotParts = import(S(184))
local log = import(S(159))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(13))
    return pad and pad:FindFirstChildWhichIsA(S(7), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(250)
end
local autoPlace = {}
local connection = nil
function autoPlace.SetEnabled(enabled)
    if enabled == autoPlace.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(219))
        return
    end
    log(S(83))
    local lastFire = 0
    local lastReason = nil
    local function reason(text)
        if text ~= lastReason then
            log(S(363) .. text)
            lastReason = text
        end
    end
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local player = Players.LocalPlayer
        local character = player.Character
        local backpack = player:FindFirstChild(S(228))
        if not character or not backpack then
            reason(S(277))
            return
        end
        local humanoid = character:FindFirstChildOfClass(S(161))
        local tool = backpack:FindFirstChildWhichIsA(S(95))
        if not humanoid then
            reason(S(149))
            return
        end
        if not tool then
            reason(S(296))
            return
        end
        local plot = getCurrentPlot()
        if not plot then
            reason(S(195))
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            reason(S(288))
            return
        end
        local placed = false
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local prompt = findHolderPrompt(holder)
            if isHolderEmpty(prompt) then
                humanoid:EquipTool(tool)
                local ok, err = pcall(function()
                    fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
                end)
                if ok then
                    log(S(379) .. tool.Name .. S(190) .. holder.Name)
                else
                    log(S(84) .. tostring(err))
                end
                placed = true
                break
            end
        end
        if not placed then
            reason(S(286) .. #parts.Holders:GetChildren() .. S(56))
        else
            lastReason = nil
        end
    end)
end
function autoPlace.IsEnabled()
    return connection ~= nil
end
return autoPlace
end
end
modules[S(226)] = function()
return function(import)
local Players = game:GetService(S(260))
local Workspace = game:GetService(S(40))
local RunService = game:GetService(S(254))
local log = import(S(253))
local FIRE_INTERVAL = 0.5
local HIDDEN_POSITION = UDim2.new(3, 0, 3, 0)
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(281))
    local mineGui = playerGui and playerGui:FindFirstChild(S(331))
    return mineGui and mineGui:FindFirstChild(S(100))
end
local function labelText(button)
    local content = button:FindFirstChild(S(19))
    local label = content and content:FindFirstChild(S(15))
    return label and label.Text or S(50)
end
local function clickButton(button)
    for _, signalName in ipairs({ S(202), S(247), S(320), S(147) }) do
        local signal = button[signalName]
        if signal then
            pcall(function()
                firesignal(signal)
            end)
        end
    end
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(107))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(23), true)
    return prompt, prompt and prompt.Parent
end
local function teleportTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(275))
    if not (hrp and part) then
        return false
    end
    hrp.CFrame = CFrame.new(part.Position + Vector3.new(0, 3, 5))
    return true
end
local autoSell = {}
local connection = nil
function autoSell.SetEnabled(enabled)
    if enabled == autoSell.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        local sellFrame = getSellFrame()
        if sellFrame then
            sellFrame.Position = sellFrame:GetAttribute(S(172)) or sellFrame.Position
        end
        log(S(80))
        return
    end
    log(S(21))
    local lastFire = 0
    local lastVisibleState = nil
    local returnCFrame = nil
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local sellFrame = getSellFrame()
        local visible = sellFrame ~= nil and sellFrame.Visible
        if visible ~= lastVisibleState then
            log(S(34) .. tostring(visible))
            lastVisibleState = visible
            if visible and sellFrame:GetAttribute(S(135)) == nil then
                sellFrame:SetAttribute(S(170), sellFrame.Position)
            end
            if not visible and returnCFrame then
                local character = Players.LocalPlayer.Character
                local hrp = character and character:FindFirstChild(S(287))
                if hrp then
                    hrp.CFrame = returnCFrame
                end
                returnCFrame = nil
            end
        end
        if visible then
            sellFrame.Position = HIDDEN_POSITION
            local content = sellFrame:FindFirstChild(S(266))
            local selectAllButton = content and content:FindFirstChild(S(263))
            local sellButton = content and content:FindFirstChild(S(178))
            if selectAllButton and labelText(selectAllButton) == S(349) then
                clickButton(selectAllButton)
                log(S(156))
            elseif sellButton and labelText(sellButton):find(S(185), 1, true) then
                clickButton(sellButton)
                log(S(167))
            end
        else
            local prompt, promptPart = getSellStandPromptPart()
            if prompt and promptPart then
                if not returnCFrame then
                    local character = Players.LocalPlayer.Character
                    local hrp = character and character:FindFirstChild(S(325))
                    if hrp then
                        returnCFrame = hrp.CFrame
                    end
                end
                teleportTo(promptPart)
                local ok, err = pcall(function()
                    fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
                end)
                if not ok then
                    log(S(157) .. tostring(err))
                end
            end
        end
    end)
end
function autoSell.IsEnabled()
    return connection ~= nil
end
return autoSell
end
end
modules[S(25)] = function()
return function(import)
local RunService = game:GetService(S(289))
local getCurrentPlot = import(S(122))
local getPlotParts = import(S(412))
local log = import(S(75))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(3))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(231))
end
local autoUpgrade = {}
local connection = nil
function autoUpgrade.SetEnabled(enabled)
    if enabled == autoUpgrade.IsEnabled() then
        return
    end
    if not enabled then
        connection:Disconnect()
        connection = nil
        log(S(396))
        return
    end
    log(S(128))
    local lastFire = 0
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if now - lastFire < FIRE_INTERVAL then
            return
        end
        lastFire = now
        local plot = getCurrentPlot()
        if not plot then
            return
        end
        local parts = getPlotParts(plot)
        if not parts.Holders then
            return
        end
        for _, holder in ipairs(parts.Holders:GetChildren()) do
            local detector = findUpgradeClickDetector(holder)
            if detector then
                fireclickdetector(detector)
            end
        end
    end)
end
function autoUpgrade.IsEnabled()
    return connection ~= nil
end
return autoUpgrade
end
end
modules[S(54)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(5)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(314) .. child.Name .. S(38)
        if child:IsA(S(179)) then
            line = line .. string.format(
                S(187),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(361)) then
            line = line .. string.format(S(16), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(62)) or child:IsA(S(311)) then
            line = line .. S(90)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(166) .. attrName .. S(282) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(380))
    end
end
return dumpInstance
end
modules[S(91)] = function()
return function(import)
local Players = game:GetService(S(294))
local log = import(S(249))
local function dumpVisible(root, indent)
    indent = indent or S(160)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(339)) and child.Visible then
            local line = indent .. child.ClassName .. S(334) .. child.Name .. S(309)
            if child:IsA(S(164)) or child:IsA(S(113)) then
                line = line .. S(410) .. tostring(child.Text) .. S(407)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(110) .. attrName .. S(64) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(238))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(285))
    local mineGui = playerGui and playerGui:FindFirstChild(S(177))
    if not mineGui then
        log(S(201))
        return
    end
    log(S(86))
    dumpVisible(mineGui)
    log(S(414))
end
return dumpMineGui
end
end
modules[S(59)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(357))
local log = import(S(227))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(20)) or descendant:IsA(S(52)) then
            count = count + 1
            log(S(252) .. descendant.ClassName .. S(104) .. descendant:GetFullName())
        end
    end
    log(S(229) .. count .. S(338))
    return count
end
return dumpRemotes
end
end
modules[S(191)] = function()
return function(import)
local Players = game:GetService(S(221))
local log = import(S(376))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(30))
    local mineGui = playerGui and playerGui:FindFirstChild(S(145))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(306))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(369)) and sellFrame.Content:FindFirstChild(S(51))
    if not scroll then
        log(S(67))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(373) or card:IsA(S(111)) then
            count = count + 1
            log(S(406) .. count .. S(47) .. card.ClassName .. S(300) .. card.Name .. S(408))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(105) .. attrName .. S(383) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(415) .. child.ClassName .. S(317) .. child.Name .. S(224)
                if child:IsA(S(32)) then
                    line = line .. S(257) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(239) .. count .. S(73))
    return count
end
return dumpSellCards
end
end
modules[S(96)] = function()
local Players = game:GetService(S(262))
local Workspace = game:GetService(S(171))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(216))
    local plots = mineWorld and mineWorld:FindFirstChild(S(9))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(234)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(367)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(151)),
        Nodes = plot:FindFirstChild(S(158)),
        Chest = plot:FindFirstChild(S(283)),
        Zones = plot:FindFirstChild(S(382)),
        Crew = plot:FindFirstChild(S(217)),
        Loot = plot:FindFirstChild(S(106)),
        PlotIndex = plot:GetAttribute(S(141)),
        ChestCapacity = plot:GetAttribute(S(154)),
    }
end
return getPlotParts
end
modules[S(131)] = function()
return function(import)
    local Players = game:GetService(S(335))
    local createWindow = import(S(133))(import)
    local i18n = import(S(162))
    local antiAfk = import(S(77))(import)
    local interactionLogger = import(S(155))(import)
    local log = import(S(299))
    local window = createWindow({
        Subtitle = S(313),
    })
    local getCurrentPlot = import(S(321))
    local getPlotParts = import(S(11))
    local dumpInstance = import(S(31))
    local dumpRemotes = import(S(347))(import)
    local dumpSellCards = import(S(168))(import)
    local dumpGui = import(S(402))(import)
    local autoMine = import(S(115))(import)
    local autoPlace = import(S(109))(import)
    local autoChest = import(S(24))(import)
    local autoSell = import(S(268))(import)
    local autoUpgrade = import(S(342))(import)
    local farmTab = window:AddTab(S(284), S(403))
    farmTab:AddCard(
        S(150),
        S(355)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(291),
        S(385)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(350),
        S(230)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(136),
        S(366)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(183),
        S(397)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(163), S(318))
    local checkBtn
    checkBtn = debugCard:AddButton(S(119), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(120)
            log(S(381))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(129),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(255),
        S(242)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(271), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(225)
            return
        end
        local ok, err = log(S(244))
        if not ok then
            dumpBtn.Text = S(372)
            dumpCard:SetSubtitle(S(327) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(88) .. firstNode.Name .. S(153))
            dumpInstance(firstNode, log)
        else
            log(S(181))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(180) .. firstHolder.Name .. S(384))
            dumpInstance(firstHolder, log)
        else
            log(S(137))
        end
        dumpBtn.Text = S(375)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(323),
        S(316)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(117), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(61))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(186))
        if not tool then
            toolDumpBtn.Text = S(351)
            return
        end
        log(S(94) .. tool.Name .. S(240))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(358) .. attrName .. S(344) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(310)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(72),
        S(315)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(36), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(70)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(112),
        S(395)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(394), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(27)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(197),
        S(390)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(85), function()
        dumpGui()
        guiDumpBtn.Text = S(374)
    end)
    local settingsTab = window:AddTab(S(196), S(142))
    settingsTab:AddCard(S(57), S(302)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(293),
        S(22)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(55), S(205), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(241), S(336), S(264) }, 1, function(index, value)
        local code = ({ S(359), S(1), S(307) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(409)] = function()
return {
    [79389059854988] = S(411),
}
end
import(S(127))(import)
