-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/humanoid", "works from anywhere - dropping the teleport", "", "[AutoSell] enabled", "autoPlaceUnlockBudget", ") didn't take: ", "Auto Mine: ความเร็ว", "HumanoidRootPart", "core/scheduler", " (", "Frame", ")", "waiting", "UserInputService", " (", "enabled", ")", "core/debug_dump_remotes", "core/run_job", "Auto Plot: ซื้อโซนถัดไป", "", "lowest", "games/my_anime_mine/game_api", "getconnections", "", "unreadable", "cost", "getgc", "[GuiDump] === visible PlayerGui tree ===", " #", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[Scheduler] turn: ", "core/spend_policy", "[AutoPlace] placed ", "none", "/", "[AutoUpgrade] pickaxe #", "games/my_anime_mine/get_plot_parts", "core/logger", ") -> ", "unreadable", "[AutoChest] waiting: ", "/", "games/my_anime_mine/auto_research", "[Interaction] logger disabled", "Auto Chest", "table", "autoPlotKeepMoney", "TextLabel", "ScreenGui", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "spendOrder", "Speed", "", "GetHolderSpeedLevel", "[AutoPlot] now mining ", "Dump", "s behind a purchase that never happened - letting it spend", "Holders", "Zones", "?", "[Decompile] === end ===", "GuiButton", "Decompile Client Scripts", "[AutoUpgrade] ", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "JumpPower (ปกติ 50)", "Auto Claim: รอบเช็ค", "TextButton", "Auto Upgrade: กันเงินไว้", "core/teleport", "ขนาดเมนู", "BodyVelocity", "Frame", ")", "cost", "yes", "core/debug_decompile", "[AutoPlace] drifted out of range of ", "write reported success but ", "core/teleport", "[AutoPlace] disabled", "CharMaxLevel", "[SpendPolicy] ", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", " ", "open_menu_hint", " (x", "Auto Mine", "enabled", "no", "", "Workspace", " (", "%.3g", " = ", "[AutoResearch] ", "string", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "autoResearchCoreLast", "", "[Decompile] wrote ", "MekPro", "All", "Mining ก่อน", "Core", " from ", "Plots", "GuiButton", "[AutoClaim] ", "", "AutoSell", "ตามเครื่อง", "off", "AutoPlot", "core/logger", "Damage", " for ", "autoMine", " refused - ", "buying the ", "Auto Claim (Index)", "MineWorld", "ValueLabel", "", "AutoPlace", "autoChest", "Pad", ", base ", "autoUpgradePickaxeIndex", "Auto Chest: เกณฑ์เก็บ", ") ", "core/run_job", "OwnerUserId", "autoMineClicks", "auto", "", "Container", "⚙", "ProximityPrompt", "ClaimableTiers failed", "table", "Players", " screens", " items)", "TextButton", "games/my_anime_mine/auto_chest", "[AutoChest] can't read the chest yet", "Text", "AutoUpgrade", "[AutoPlace] holders: ", "off", "Auto Upgrade: Pickaxe ที่จะซื้อ", "X", "ความเร็ววิ่ง", "games/my_anime_mine/auto_research", "ไล่อัปเกรด miner ทุกกี่วินาที", "M", "modules not loaded", "MineGui", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "getnilinstances", "AutoResearch", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Speed ก่อน", "string", "games/my_anime_mine/get_best_miner", "games/my_anime_mine/game_api", "UIStroke", "Auto Place: เปลี่ยนตัวอ่อนออก", "[Interaction] logger enabled - go click/interact with things now", "core/run_job", "autoPlaceUnlock", ": ", "Auto Place: ปลดล็อกช่อง miner", ": ", "autoUpgrade", "Auto Research: กันเงินไว้", ")", "core/i18n", "[Decompile] ", "[Config] couldn't write ", "ZoneRow", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "BasePart", "", "Auto Research: ซื้อสาย Zone", "[AutoSell] ", "enabled", "อังกฤษ", "core/run_job", "AutoPlot", " has been busy for ", "autoClaimInterval", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "autoMineSpeed", "empty", "core/parse_money", "", " reward(s) looked claimable but none went through", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "Mutation: หยุดเมื่อได้ x", "[AutoPlace] Equip Best: hotbar's strongest ", ")", "[BestMiner] slot ", "M", "Players", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "games/my_anime_mine/get_plot_parts", "[Interaction] Key pressed: %s (gameProcessed=%s)", "en", "Auto Plot: รอบเช็ค", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "s", ") ", "games/my_anime_mine/main", "s", "games/my_anime_mine/auto_plot", "[GuiDump] PlayerGui not found", "core/logger", "Mining", "core/get_leaderstat", "UserInputService", "TextLabel", "AutoPlace", "games/my_anime_mine/game_api", " ", "auto", "th", ",", "Dump", "TextButton", "core/dispatch", "Plots", "core/get_place_id", "off - every automation spends on its own", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", " remotes", "MineSpeed", "PlayerScripts", "Auto Sell", "s - carrying on without it", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "Auto Place Miner", "every open node is maxed", "[Interaction] ProximityPrompt triggered: ", "ย่อขยายเมนูทั้งชุด", "table", ") gave nothing", "sold ", "RemoteFunction", "walkSpeed", "Auto Upgrade Unit", "Frame", " wouldn't take ", " -> ", "getrenv", "MineSellItems", "games/my_anime_mine/game_api", "AutoPlot", "th", "M", "MekPro.log", "^M(%d+)$", "getloadedmodules", "TextLabel", ")", "games/my_anime_mine/get_current_plot", ": ", "Auto Plot: กันเงินไว้", " research is ", "[AutoPlace] upgrading ", "autoSell", "MineBuyPickaxe", "[Decompile] === executor capabilities ===", "core/run_job", "Auto Sell: รอคิวได้นานสุด", ")", "Humanoid", "locked slot", "OwnerUserId", "/", ")", "en", "", "autoClaimMaxPerPass", "[Decompile] === start ===", "string", "Players", " script(s) to ", "Auto Place: รอบเช็ค", "[Scheduler] ", "[Bb]uy", " waited ", "Content", "Limited", "[AutoSell] MineSellItems ", ")", "[AutoUpgrade] ", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " still locked after paying ", "Workspace", "", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "Hauler", ")", "", "core/dispatch", "[^%w]+", "core/build_env", " files", "auto", ")", "slot ", "[AutoUpgrade] ", ", ", "AutoChest", "[AntiAFK] idle detected, simulating input", "fill", "Auto Mine: คลิกต่อรอบ", "AutoPlace", "core/logger", " ", "getsenv", "[%X] ", " costs ", ", have ", "UIListLayout", "AutoUpgrade", " (", " (cost ", "^Swap out ", "research", "Auto Place: เพดานราคาปลดช่อง", "LayerCollector", "autoUpgradeMutationBudget", "Text", "autoUpgradeRollMutation", "games/my_anime_mine/main", "ItemName", "games/registry", "table", "disabled", "AutoUpgrade", "search_placeholder", "%$", "search_placeholder", " while selecting", "core/teleport", "[MekPro] settings file: ", "", "[AutoUpgrade] bought pickaxe #", "ReplicatedStorage", "games/my_anime_mine/auto_upgrade", "Auto Upgrade: สุ่ม Mutation", "[AutoChest] chest is empty", "TextButton", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "core/debug_dump_gui", "autoClaim", "TextLabel", "Chest", "unlock", "Auto Chest: รอบเช็ค", "[AutoUpgrade] ", "Content", "x", "autoChestInterval", "/", "getscriptbytecode", "autoResearchInterval", "Auto Research: รอบเช็ค", "AutoResearch", "Header", "disabled", "games/my_anime_mine/auto_mine", "TextLabel", "/", "core/get_place_id", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "Interaction Logger", "[AutoPlace] ", "Frame", "th", ")", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "getscripts", "autoUpgradeEquipPickaxe", "[AutoPlace] ", "CoreUI", "[AutoPlace] unlocking ", "PlayerGui", "[GameAPI] SendEvent failed: ", "[AutoClaim] ", "games/my_anime_mine/game_api", "s - jumping the queue", ": badge says ", "[GameAPI] couldn't load the game's modules: ", " thing available first", "=", "", "HumanoidRootPart", ",", "ขายทุกกี่วินาที", " (power ", "ไทย", "/s", "[AutoPlace] slot ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "core/debug_interaction_logger", "en", "Search", "open_menu_hint", "slot not in the hotbar", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "core/debug_dump_remotes", ": ", "function", "[AutoResearch] idle: ", "autoPlotLatest", "Backpack", "nothing claimable", "HotbarFrame", "games/my_anime_mine/get_best_miner", "may need us at the stand - trying that", "[AutoPlace] fireproximityprompt error: ", "nothing left to buy (", "TextLabel", ") needs ", " ", ")", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "AutoPlace", "s", "Exclusive", "autoPlace", "leaderstats", "games/my_anime_mine/auto_sell", "core/teleport", "waiting", " not bought yet (", ")", "Workspace", "[AutoPlot] ", "disabled", " is no better after the swap (", "ProximityPrompt", "Crew", ") from ", "AutoClaim", "GuiService", "autoSellProtectUnits", "autoUpgradeKeepMoney", "BasePart", " claimable but none could be resolved", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", ": ", "[Interaction] ClickDetector clicked: ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Players", "x", "🏠", "GetHolderMineSpeedLevel", "", "Damage", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " mutation x", "", "autoPlot", "Frame", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "[AutoSell] disabled", "table", " -> ", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "antiAfk", "This is not a supported place.", "MinePower", "/config.json", "  ", "Zone buying is off", " -> ", "core/logger", "Frame", "noclip", "([%d,%.]+)%s*(%a*)", "[AutoResearch] ", " error: ", "AutoResearch", "table", "off", "List", " didn't go through (out of stock or too expensive)", "ItemFrame", "upgrade", "disabled", "Anti-AFK", "", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "VirtualUser", "0 = ไม่จำกัด", "%.3g", ": State(", "autoSellMaxWait", "autoUpgradeInterval", "^%$", " ", "AutoChest", "core/debug_dump_gui", "Players", "core/logger", "TextButton", "enabled", "s", "string", ")", "string", "cheapest", "[AutoResearch] ", "core/spend_policy", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "MineUpgradeCharacter", "core/logger", "Frame", "=", "Dump Visible GUI", "[Remotes] === total: ", "HumanoidRootPart", "; ", "Dump All Remotes", ", you have ", "2026-09-21 13:08:13", "function", "core/logger", "autoUpgradePriority", " (", "MineSpeed", "[AutoMine] ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", " (x", " owned)", " (", ") on ", "UIScale", "VirtualInputManager", "Frame", "Content", "games/my_anime_mine/auto_place", "autoPlotBuy", "AutoResearch", "interactionLogger", " team power, you have ", " (", "games/my_anime_mine/auto_sell", "cheapest open node ", "", "Players", "ModuleScript", "แพงสุดก่อน", ") - leaving it alone for ", "_", "Auto Research: ซื้อสาย Core", "%.3g", "PlotIndex", "GetCharacterMineSpeedUpgradeCost", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "GetHolderLevel", "TabBar", "disabled", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "languageIndex", "PlayerGui", "autoPlaceUpgradeWeak", "ไล่ซื้อทุกกี่วินาที", "[AutoUpgrade] ", "M", "auto", "research", "Nodes", "AutoUpgrade", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "^([%d,%.]+)%s*(%a*)$", " (chest holds ", "", "[AutoChest] MineTakeChest had no effect (", "^x(%d+)$", "ReplicatedStorage", "core/logger", " (ActionText=", "fill", "core/logger", "prompt disabled", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "ค้นหาฟังก์ชัน...", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "TextButton", "[AutoChest] enabled (collects once >= ", "debug.getupvalues", "dearest", "[AutoPlot] ", "AutoClaim", "[AutoClaim] claimed ", "core/logger", ")", "core/logger", "TextLabel", "Auto Upgrade Plot (Zone)", " but no unclaimed reward in its Track", "[AutoUpgrade] ", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "autoResearch", "TextLabel", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "no mine data", "Auto Upgrade: รอบเช็ค", "decompile", "ถูกสุดก่อน", "Mutation: เพดานราคาต่อครั้ง", "%$", "MineSelectZone", "write reported success but the line isn't in ", "[AutoPlace] couldn't hold anything - not pressing E", " ", "core/parse_money", "Workspace", "slot past the usable cap", "BasePart", " ", "table", "core/logger", "games/my_anime_mine/auto_mine", " items", "games/my_anime_mine/get_plot_parts", "[^%.]+", "Auto Research: ซื้อสูงสุดต่อรอบ", "unlock", "core/debug_interaction_logger", "Locked miner slot", "[AutoUpgrade] every stat on every miner is maxed", "AutoMine", "[Uu]nlock", "index rewards are disabled in this game build", "🐛", "[Interaction] Button activated: ", "เดินทะลุกำแพงและพื้น", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "Frame", "autoSellInterval", "ลำดับการใช้เงินรวมทุกระบบ", "/", "อ่านป้ายบน chest ทุกกี่วินาที", "core/run_job", " ", "บิน (Fly)", " (", "both Zone and Core buying are switched off", "[AutoPlace] couldn't pick up ", "ลอย (Float)", " team power", "-", "core/logger", "CharSpeedMaxLevel", " - ", "=empty", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "[AutoPlace] ", "=locked(", "CoreGui", "needs ", "MineSpeed", "neither appendfile nor writefile is available", "TextLabel", "core/logger", "Damage", "[GuiDump] ", "", " node(s) looked buyable but the server refused", "core/parse_money", "[AntiAFK] enabled - watching Player.Idled", "ต่ำสุดก่อน", "autoResearchZones", "%.3g", "SellStand", "Players", "AutoPlot", "TextLabel", "ฟาร์ม", "core/logger", "MekPro", "GetCharacterUpgradeCost", "TextLabel", "[AutoResearch] ", "[AntiAFK] disabled", " x", "core/i18n", " pickaxe(s) for ", "Workspace", "s", " items (now ", "sale didn't go through", "TextLabel", "table", " -> ", "'", "core/logger", "')", " (money ", "ต่างคนต่างซื้อ", "table", "Modules", ") ", "[AutoResearch] cheapest open node: ", "needs us at the stand - keeping the teleport", "s", " (Text=", "มุดแมพ (Noclip)", "core/click", " ", "TextButton", "games/my_anime_mine/auto_claim", "s", "  [attr] ", "debug.getproto", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "UIListLayout", "games/my_anime_mine/game_api", "%.0f", "Search functions...", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", " (", "getcallbackvalue", "[Decompile] PlayerScripts not found", " (still ", "autoUpgradeMutationTarget", "[AutoChest] collected ", "core/get_place_id", "fly", "MineBuySkill", "MekPro", ".lua", "TextButton", "needs ", "maxed", "", "TextLabel", " (Text=", "[SpendPolicy] ", "ChestCapacity", "TextBox", " stack(s)", "none", "[AutoChest] disabled", "autoResearchCheapestFirst", "UIPadding", "Hauler", "number", "UICorner", "core/anti_afk", "autoChestMinItems", "table", " '", "Humanoid", "costs ", "MineTakeChest", "  ", "%.3g", "games/my_anime_mine/game_api", "TextLabel", "TextLabel", ", built ", " (", "[AutoPlace] nothing in the hotbar to place", ",", "RunService", "[AutoUpgrade] MineBuyPickaxe ", "\n", "'", "AutoPlace", "Badge", " still empty after placing ", "core/spend_policy", "0 = ไม่จำกัด", "CharMineSpeedMaxLevel", " (power ", "(", "%.0f", "Auto Upgrade: แจก Pickaxe ให้ miner", "'", "Players", "Frame", "AutoPlace", "autoPlaceEquipBestInterval", "th", "autoPlotInterval", "autoUpgradeLimitPickaxes", "UserInputService", "badge says ", "แรงกระโดด", "games/my_anime_mine/game_api", "GetCharacterSpeedUpgradeCost", "#", "[Logger] failed to write ", "GuiService", " waited ", " failed)", "/", "table", "jumpPower", "Run", ")", "Auto Place: รอบดึงตัวเก่ง", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", " (prompt says '", "games/my_anime_mine/game_api", "action '", "dearest", "x", "core/run_job", "ดีบัก", "core/logger", "s", "UICorner", " -> ", " (", "languageIndex", "autoUpgradeMaxPerPass", " -> ", " ชิ้น", "table", "Players", "Root", " pickaxe ", " -> x", "ValueLabel", "affordable - waiting for a turn", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "enabled", "server didn't equip it", "core/humanoid", "", "", " = ", "MineEquipSlot", "dearest", "core/get_me", "works from anywhere - no teleport needed", "Character", "[Scheduler] ", "Auto Claim: เก็บสูงสุดต่อรอบ", "games/my_anime_mine/auto_chest", " power ", "core/config", "PickaxesStand", "Main", "] job failed: ", "table", "[MekPro] build ", "[Remotes] ", "autoPlaceEquipBest", "Players", "BasePart", " (", "table", "ProximityPrompt", "core/debug_decompile", "autoResearchKeepMoney", " | ", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "ตั้งค่า", "autoUpgradePickaxeIndex", "[GameAPI] holder ", "ClickDetector", "core/config", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "string", "Badge", "s", "\n", "GuiObject", "BodyPosition", " - upgrade the miners first", "M", "TextLabel", " screen(s)) ===", "AutoPlace", "core/run_job", " doesn't exist", "core/scheduler", "[AutoPlot] couldn't switch to ", " ", "[Scheduler] ", "Empty miner slot", "AutoUpgrade", "cheapest", "[AutoClaim] idle: ", "[Config] couldn't read ", " ", "RunService", "[AutoPlace] ", "core/logger", "games/my_anime_mine/auto_upgrade", "core/anti_afk", "MineEquipBest", "MekPro/decompiled", "AutoSell", "x", "", "UIListLayout", "AutoPlot", "MineWorld", "Core buying is off", "button has no size on screen", "s", "ZoneBoardGui", "autoResearchCore", "menuScale", "", ") ", "dearest", "HttpService", "313921c+", "ถูกสุดก่อน", "autoResearchMaxPerPass", "Loot", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "menuScale", "games/my_anime_mine/get_current_plot", "MekPro", "games/my_anime_mine/auto_claim", "autoUpgradePickaxe", "%$%s*([%d,%.]+)%s*(%a*)", " for ", "MineRollMutation", "games/my_anime_mine/auto_place", "%.3g", "", "RemoteEvent", "LocalScript", "ปุ่มเปิดเมนู", "[GuiDump] === end (", "nothing to place or upgrade", "ยิง Equip Best ทุกกี่วินาที", " ===", "Speed", "en-us", "table", "Players", "games/my_anime_mine/auto_plot", "function", "core/spend_policy", "core/logger", "table", "Auto Plot: ใช้โซนล่าสุดเสมอ", "[AutoPlace] unlocked ", "%.3g", "MineEquipPickaxe", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "games/registry", "Workspace", " possible holder(s) - not buying more", "empty", "no isfile/readfile available to verify the write", "ClickDetector", "core/i18n", "Players", "Damage ก่อน", "hookfunction", "dearest", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "ProximityPrompt", "Frame", "core/spend_policy", "table", "[AutoPlot] ", "Shop Stand", " -> ", "[AutoPlace] picked up ", ": ", "[AutoResearch] Core ", "Auto Research (Plot)", "dearest", "lowest", "core/ui", "ZoneBoard", "core/debug_tab", "off", "[AutoPlot] unlocked ", "dev", " costs ", "core/spend_policy", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "", "", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "[AutoResearch] nothing open and affordable right now", "แพงสุดก่อน", "autoSellMaxWait", "(nothing to do)", "WalkSpeed (ปกติ 16)", "เช็คโซนทุกกี่วินาที", "[", "%", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "core/build_env", "debug.getconstants", "TextLabel", "ScrollingFrame", ")", " '", "ClickDetector", "/", "autoPlaceInterval", " fields: ", "PlayerGui", " ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "core/debug_tab", "TextButton", "Players", "games/my_anime_mine/get_current_plot", "x", "%.0f", "games/my_anime_mine/game_api", "float", "core/ui", "Auto Sell: รอบขาย", "My Anime Mine · Mining Hub", "Open menu", "core/logger", "[Decompile] decompile unavailable in this executor"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(903)] = function()
return function(import)
local log = import(S(1031))
local Players = game:GetService(S(1021))
local VirtualUser = game:GetService(S(500))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(680))
        connection = player.Idled:Connect(function()
            log(S(318))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(694))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(1006)] = function()
return { Env = S(990), IsDev = true, Commit = S(922), BuiltAt = S(532) }
end
modules[S(718)] = function()
local VirtualInputManager = game:GetService(S(545))
local GuiService = game:GetService(S(451))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(913)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(853)] = function()
return function(import)
local HttpService = game:GetService(S(921))
local getPlaceId = import(S(737))
local log = import(S(662))
local ROOT_FOLDER = S(103)
local placeFolder = ROOT_FOLDER .. S(1013) .. tostring(getPlaceId())
local filePath = placeFolder .. S(479)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(947) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(897) .. filePath .. S(457) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(182) .. filePath .. S(269) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(78)] = function()
return function(import)
local Players = game:GetService(S(557))
local log = import(S(592))
local CAPABILITIES = {
    S(617), S(370), S(387), S(28), S(265),
    S(24), S(732), S(324), S(257), S(1007),
    S(599), S(724), S(969), S(162),
}
local OUT_FOLDER = S(905)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(635)) do
        if type(target) ~= S(710) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(275))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(181) .. name .. S(843) .. ((ok and type(value) == S(950)) and S(77) or S(91)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(1032))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(240))
    if not playerScripts then
        log(S(733))
        return 0
    end
    pcall(makefolder, S(690))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(939)) or target:IsA(S(558)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(98) and #source > 0 then
                local name = target:GetFullName():gsub(S(309), S(561))
                if pcall(writefile, OUT_FOLDER .. S(369) .. name .. S(741), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(102) .. written .. S(290) .. OUT_FOLDER
        .. S(656) .. failed .. S(806))
    return written
end
local function run()
    log(S(287))
    reportCapabilities()
    local count = dumpAll()
    log(S(62))
    return count
end
return run
end
end
modules[S(359)] = function()
return function(import)
local Players = game:GetService(S(289))
local log = import(S(674))
local function dumpVisible(root, indent)
    indent = indent or S(285)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(880)) and child.Visible then
            local line = indent .. child.ClassName .. S(1011) .. child.Name .. S(789)
            if child:IsA(S(361)) or child:IsA(S(720)) then
                line = line .. S(716) .. tostring(child.Text) .. S(179)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(723) .. attrName .. S(96) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(766))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(392))
    if not playerGui then
        log(S(219))
        return 0
    end
    local screens = 0
    log(S(29))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(335)) and screen.Enabled and screen.Name ~= S(740) then
            screens = screens + 1
            log(S(676) .. screen.ClassName .. S(762) .. screen.Name .. S(705))
            dumpVisible(screen, S(480))
        end
    end
    log(S(941) .. screens .. S(885))
    return screens
end
return dumpGui
end
end
modules[S(416)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(588))
local log = import(S(604))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(938)) or descendant:IsA(S(251)) then
            count = count + 1
            log(S(859) .. descendant.ClassName .. S(417) .. descendant:GetFullName())
        end
    end
    log(S(527) .. count .. S(944))
    return count
end
return dumpRemotes
end
end
modules[S(410)] = function()
return function(import)
local Players = game:GetService(S(460))
local Workspace = game:GetService(S(443))
local UserInputService = game:GetService(S(797))
local log = import(S(952))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(246) .. describe(prompt)
            .. S(590) .. prompt.ActionText .. S(313))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(458) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(645) .. describe(button)
            .. S(747) .. tostring(button.Text) .. S(12))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(972)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(873)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(63)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(45))
        return
    end
    connections = {}
    log(S(171))
    local playerGui = Players.LocalPlayer:WaitForChild(S(572))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(865)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(1012)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(109)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(210),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(987)] = function()
return function(import)
local interactionLogger = import(S(638))(import)
local dumpRemotes = import(S(18))(import)
local dumpGui = import(S(509))(import)
local decompileScripts = import(S(866))(import)
local humanoid = import(S(840))
local function build(window, persist)
    local tab = window:AddTab(S(820), S(644))
    persist.Toggle(
        tab:AddCard(
            S(381),
            S(475)
        ),
        S(551),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(530),
        S(521)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(57), function()
        remoteBtn.Text = dumpRemotes() .. S(238)
    end)
    local guiCard = tab:AddCard(
        S(526),
        S(581)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(231), function()
        guiBtn.Text = dumpGui() .. S(144)
    end)
    local decompileCard = tab:AddCard(
        S(64),
        S(971)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(810), function()
        decompileBtn.Text = decompileScripts() .. S(311)
    end)
    humanoid.Start()
    persist.Slider(tab, S(155), S(1001),
        8, 200, S(252), 16, humanoid.SetWalkSpeed, S(842))
    persist.Slider(tab, S(799), S(67),
        30, 300, S(809), 50, humanoid.SetJumpPower, S(111))
    persist.Toggle(
        tab:AddCard(S(655), S(386)),
        S(738), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(717), S(646)),
        S(485), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(659), S(99)),
        S(1026), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(308)] = function()
return function(import)
    local getPlaceId = import(S(379))
    local registry = import(S(960))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(477))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(222)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(437))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(846)] = function()
local Players = game:GetService(S(143))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(279))
    local backpack = player:WaitForChild(S(421))
    return player, humanoid, backpack
end
return getMe
end
modules[S(235)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(1)] = function()
local Players = game:GetService(S(967))
local RunService = game:GetService(S(899))
local UserInputService = game:GetService(S(223))
local Workspace = game:GetService(S(961))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(763))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(8))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(73))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(862)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(185)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(881))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(696)] = function()
local Players = game:GetService(S(831))
local STRINGS = {
    en = {
        search_placeholder = S(729),
        open_menu_hint = S(1030),
    },
    th = {
        search_placeholder = S(595),
        open_menu_hint = S(940),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(946)
    if locale:sub(1, 2):lower() == S(229) then
        return S(261)
    end
    return S(211)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(228) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(901)] = function()
local LOG_FILE = S(263)
local function log(message)
    local line = os.date(S(325)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(3))
            end
            appendfile(LOG_FILE, line .. S(777))
        elseif writefile then
            local existing = S(137)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(879))
        else
            error(S(672))
        end
        if not (isfile and readfile) then
            error(S(964))
        end
        if not isfile(LOG_FILE) then
            error(S(80) .. LOG_FILE .. S(888))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(622) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(803) .. LOG_FILE .. S(176) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(198)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(517) then
        return nil
    end
    local number, suffix = text:match(S(932))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(403), S(677))))
    if not value then
        return nil
    end
    if suffix ~= S(918) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(172)] = function()
return function(import)
local log = import(S(483))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(1003) .. name .. S(856) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(889)] = function()
return function(import)
local RunService = game:GetService(S(775))
local log = import(S(534))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(892) .. task_.name .. S(488) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(292) .. task_.name .. S(193)
                    .. string.format(S(1024), now - task_.busySince)
                    .. S(242))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(849) .. starved.name .. S(805)
            .. string.format(S(787), now - starved.waitingSince)
            .. S(396))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(32) .. (currentTask and currentTask.name or S(1000)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(782)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(606))
local policy = {}
local probes = {}
local order = {}
local mode = S(114)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(748) .. (mode == S(988)
        and S(236)
        or (S(121) .. mode .. S(399))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(152) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(895) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(970) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(84) .. name .. S(294)
            .. string.format(S(728), now - blockedSince[name])
            .. S(58))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(349)] = function()
local Players = game:GetService(S(685))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(402))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(985)] = function()
return function(import)
local i18n = import(S(966))
local UserInputService = game:GetService(S(14))
local GuiService = game:GetService(S(804))
local THEME = {
    BrandName = S(929),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(823))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(758))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(755))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(69))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(464)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(169))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(692))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(61)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(687))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(995)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(1009))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(726))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(613))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(973))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(770))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(746))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(428))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(835))
            if not valueLbl then
                valueLbl = Instance.new(S(266))
                valueLbl.Name = S(124)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(877))
            if not badge then
                badge = Instance.new(S(673))
                badge.Name = S(780)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(232))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(524))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(383))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(512))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(92)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(1004)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(791))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(11))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(597))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(21)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(470))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(328))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(146))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(149)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(50))
    screenGui.Name = S(390)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(669))
    local main = Instance.new(S(484))
    main.Name = S(855)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(544))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(74))
    tabBar.Name = S(568)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(909))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(648))
    header.Name = S(374)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(357))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(1008))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(49))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(401)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(1020))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(154)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(742))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(661)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(750))
    searchBox.Name = S(412)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(345))
    searchBox.Text = S(994)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(546))
    content.Name = S(366)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(254))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(884))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(87))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(769))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(337)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(25) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(430) .. (item.Subtitle or S(125))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(347))
        footerLabel.Text = i18n.T(S(413))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(147)] = function()
return function(import)
local gameApi = import(S(168))(import)
local log = import(S(220))
local runGuarded = import(S(819))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(598) .. minItemsToCollect .. S(145))
    else
        log(S(753))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(41) then
            log(S(148))
            lastReported = S(26)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(963) then
            log(S(356))
            lastReported = S(197)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(440) then
            local capacity = gameApi.ChestCapacity()
            log(S(42) .. count .. S(36) .. want .. S(633)
                .. (capacity and (S(584) .. capacity .. S(75)) or S(54)))
            lastReported = S(13)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(508), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(736) .. count .. S(700) .. after .. S(1010))
        else
            log(S(586) .. count
                .. S(482) .. tostring(after) .. S(442))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(930)] = function()
return function(import)
local gameApi = import(S(23))(import)
local log = import(S(39))
local runGuarded = import(S(887))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(110) .. (enabled and S(513) or S(496)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(896) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(603) .. reward.category .. S(86)
                .. tostring(reward.tierKey) .. S(30) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(394) .. #pending .. S(200))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(602), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(376)] = function()
return function(import)
local getCurrentPlot = import(S(268))
local getPlotParts = import(S(38))
local log = import(S(821))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(965), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(538) .. (enabled and S(189) or S(569)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(935)] = function()
return function(import)
local Players = game:GetService(S(861))
local getCurrentPlot = import(S(928))
local getPlotParts = import(S(634))
local getMinerSlots = import(S(424))(import)
local teleport = import(S(439))
local parseMoney = import(S(679))
local gameApi = import(S(226))(import)
local log = import(S(116))
local runGuarded = import(S(19))(import)
local spendPolicy = import(S(33))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(893)
local LOCKED_SLOT_TEXT = S(639)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(128))
    return pad and pad:FindFirstChildWhichIsA(S(140), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(224)) then
            local digits, suffix = descendant.Text:match(S(583))
            if digits then
                local value = tonumber((digits:gsub(S(230), S(303))))
                if value then
                    if suffix ~= S(101) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(307)
    return (text:gsub(S(332), S(841)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(607)) and descendant.Text:match(S(506)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(314) .. holder.Name .. S(441)
            .. gameApi.MinerSlots() .. S(807) .. gameApi.MaxMinerSlots() .. S(541)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(280)
    end
    if not prompt.Enabled then
        return true, S(593)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(764) .. price
    end
    local action = prompt.ActionText or S(745)
    if action:match(S(642)) or action:match(S(293)) or action:match(S(620)) then
        return true, S(816) .. action .. S(778)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(528))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(426) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(668) .. why .. S(283))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(665))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(525) .. prompt.ObjectText
                    .. S(786) .. shown .. S(129) .. math.floor(base) .. S(385))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(868))
    if line ~= lastSurvey then
        log(S(151) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(319), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(495), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(779)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(637), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(667) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(82))
    else
        lastAttempt = 0
        log(S(1005))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(31),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(414)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(627)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(839)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(773))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(79) .. holder.Name .. S(348))
            return false
        end
        log(S(408) .. candidate.slot .. S(731) .. candidate.name
            .. S(6) .. tostring(why))
    end
    if not miner then
        log(S(623))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(900) .. holder.Name .. S(781) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(34) .. miner.name .. S(405) .. miner.power
        .. S(543) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(658) .. entry.name .. S(107) .. entry.holder.Name
            .. S(814) .. tostring(entry.prompt.ActionText) .. S(707))
        return false
    end
    log(S(979) .. entry.name .. S(785) .. entry.power
        .. S(449) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(382) .. pad.holder.Name .. S(301)
                .. tostring(pad.priceText))
        else
            log(S(955) .. pad.holder.Name
                .. S(118) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(389) .. holderName .. S(446)
            .. string.format(S(502), before) .. S(256) .. string.format(S(563), after)
            .. S(560) .. SWAP_COOLDOWN .. S(722))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(203)
            .. string.format(S(95), bestBefore) .. S(704)
            .. string.format(S(683), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(225), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(942))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(591) then
        runGuarded(S(321), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(363) then
        log(S(391) .. target.holder.Name
            .. S(933) .. tostring(target.priceText))
        runGuarded(S(886), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(272) .. target.holder.Name .. S(174) .. target.name
        .. S(542) .. target.power .. S(40) .. best.name .. S(553) .. best.power .. S(811))
    runGuarded(S(126), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(218)] = function()
return function(import)
local gameApi = import(S(259))(import)
local parseMoney = import(S(625))
local log = import(S(631))
local runGuarded = import(S(133))(import)
local spendPolicy = import(S(974))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(601) .. (enabled and S(838) or S(375)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(346)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(56) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(15) .. deepestKey .. S(17))
        return true
    end
    log(S(890) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(192)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(444) .. gameApi.ZoneDisplayName(zoneKey) .. S(991)
                .. math.floor(price) .. S(531) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(989) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(10) .. zoneKey .. S(204))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(578) then
            reportedLocked = S(333)
            log(S(976) .. zoneKey .. S(120)
                .. gameApi.ZoneDisplayName(currentKey) .. S(271)
                .. done .. S(651) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(910)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(115), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(156)] = function()
return function(import)
local gameApi = import(S(800))(import)
local log = import(S(689))
local runGuarded = import(S(276))(import)
local spendPolicy = import(S(520))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(693) .. (enabled and S(16) or S(445)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(487) .. node.zoneKey .. S(898) .. node.id
                    .. S(330) .. node.family .. S(712) .. node.level .. S(824) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(981) .. node.id
                    .. S(536) .. node.family .. S(215) .. node.level .. S(978) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(97) .. blocked
                .. S(678))
        elseif powerBlocked then
            log(S(519) .. powerBlocked.zoneKey .. S(1017) .. powerBlocked.id
                .. S(94) .. powerBlocked.family .. S(429)
                .. math.floor(powerBlocked.powerReq) .. S(552)
                .. math.floor(powerBlocked.power)
                .. S(882))
        else
            log(S(997)
                .. S(708) .. math.floor(gameApi.GetMoney()) .. S(306))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(713) .. nearest.zoneKey
                    .. S(323) .. nearest.id .. S(825) .. nearest.family .. S(919)
                    .. nearest.level .. S(378) .. nearest.max
                    .. S(326) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(657)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(106),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(481))
        end
        if not buyCore then
            table.insert(parts, S(912))
        end
        if #parts == 0 then
            return S(245)
        end
        return S(427) .. table.concat(parts, S(316)) .. S(298)
    end
    local why
    if nearest.cost > budget then
        why = S(743) .. string.format(S(767), nearest.cost)
            .. S(327) .. string.format(S(956), money)
    elseif nearest.powerOk == false then
        why = S(670) .. string.format(S(936), nearest.powerReq) .. S(660)
    else
        why = S(836)
    end
    return S(555) .. nearest.zoneKey .. S(629) .. tostring(nearest.id)
        .. S(772) .. tostring(nearest.family) .. S(132) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(419) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(920) and cost > best)
            or (mode ~= S(845) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(163)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(373), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(554)] = function()
return function(import)
local Workspace = game:GetService(S(626))
local gameApi = import(S(727))(import)
local teleport = import(S(81))
local log = import(S(589))
local runGuarded = import(S(191))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(472))
    else
        lastAttempt = 0
        log(S(4))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(684))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(447), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(297)
                .. (sold and S(2)
                         or S(714)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(188) .. (sold and (S(250) .. #keys .. S(751)) or S(701)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(906), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(354)] = function()
return function(import)
local Workspace = game:GetService(S(302))
local gameApi = import(S(1025))(import)
local teleport = import(S(71))
local log = import(S(322))
local runGuarded = import(S(653))(import)
local spendPolicy = import(S(992))(import)
local STATS = { S(465), S(53), S(671) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(984)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(365) .. (enabled and S(90) or S(343)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(142) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(76) or S(744)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(299) .. holderKey .. S(719) .. choice.stat .. S(891)
                        .. choice.info.level .. S(474) .. after.level
                        .. S(282) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(27) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(854))
    local shop = stand and stand:FindFirstChild(S(977))
    if not shop then
        return nil
    end
    if shop:IsA(S(454)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(628), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(776)
                .. (bought and S(847)
                           or S(425)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(471))
        end
    end
    if bought then
        log(S(352) .. pickaxe.index .. S(227) .. pickaxe.name
            .. S(540) .. pickaxe.multiplier .. S(431))
        return true
    end
    log(S(37) .. pickaxe.index .. S(624) .. pickaxe.name
        .. S(493))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(575) .. holderKey .. S(833)
                    .. tostring(worn or S(35)) .. S(828) .. wanted.name)
            else
                log(S(610) .. holderKey .. S(255)
                    .. wanted.name .. S(734) .. tostring(nowWorn or S(752)) .. S(267))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(65) .. holderKey .. S(467) .. variant
                .. S(834) .. after .. S(331) .. cost .. S(516))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(344))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(315) .. gameApi.OwnedPickaxeCount()
                    .. S(697) .. gameApi.MaxMinerSlots()
                    .. S(962))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(869))
        elseif maxed > 0 then
            log(S(640))
        else
            log(S(959))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(600) and price > best)
            or (mode ~= S(983) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(150))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(580), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(395)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(353))
local Workspace = game:GetService(S(93))
local log = import(S(523))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(711), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(398) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(830))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(342) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(305) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(975) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(761) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(756) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(264))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(393) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(765))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(104), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(473) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(953) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(848) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(258), keys)
end
function api.EquipSlot(slot)
    return send(S(844), slot == S(498) and S(199) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(274), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(957), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(934), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(904))
end
function api.BuySkill(zoneKey, family)
    return send(S(739), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(808)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(510)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(478))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(533) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(490) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(621), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(123))
    plots = plots and plots:FindFirstChild(S(108))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(948)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(281)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(986))
    local gui = board and board:FindFirstChild(S(915))
    local root = gui and gui:FindFirstChild(S(832))
    local list = root and root:FindFirstChild(S(492))
    local row = list and list:FindFirstChild(S(183) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(377)) and descendant.Text ~= S(351) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(418) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(522), characterKey, stat == S(221) and S(239) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(567), max = S(83), cost = S(691) },
    Speed = { level = S(55), max = S(663), cost = S(801) },
    MineSpeed = { level = S(463), max = S(784), cost = S(565) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(288) or charName == S(908) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(400) .. tostring(value))
        end
        table.sort(fields)
        log(S(872) .. holderKey .. S(1015) .. table.concat(fields, S(654)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(857) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(757) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(166) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(864) and type(holder.CharName) == S(876) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(296), S(435) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(703) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(159)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(615)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(643)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(630) then
        return {}, S(141)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(422)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(248) or type(state.Track) ~= S(47) then
                table.insert(notes, tabKey .. S(503) .. tostring(category) .. S(249))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(397) .. waiting
                        .. S(609))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(798) .. total .. S(455)
            .. (#notes > 0 and (S(664) .. table.concat(notes, S(529))) or S(556))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(515) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(167)] = function()
return function(import)
local Players = game:GetService(S(207))
local gameApi = import(S(815))(import)
local log = import(S(511))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(486))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(774), S(186))))
    if not value then
        return nil
    end
    if suffix ~= S(468) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(1016))
    local mineGui = playerGui and playerGui:FindFirstChild(S(160))
    local hotbar = mineGui and mineGui:FindFirstChild(S(423))
    local content = hotbar and hotbar:FindFirstChild(S(295))
    return content and content:FindFirstChild(S(138))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(702)) then
            local count = descendant.Text:match(S(587))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(494))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(547))
    local nameLabel = content and content:FindFirstChild(S(340))
    local damageLabel = content and content:FindFirstChild(S(117))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(585) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(43) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(205) .. slot .. S(980) .. info.name
                        .. S(852) .. info.power .. S(695) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(1022)] = function()
local Players = game:GetService(S(790))
local Workspace = game:GetService(S(698))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(911))
    local plots = mineWorld and mineWorld:FindFirstChild(S(234))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(134)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(209)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(59)),
        Nodes = plot:FindFirstChild(S(579)),
        Chest = plot:FindFirstChild(S(362)),
        Zones = plot:FindFirstChild(S(60)),
        Crew = plot:FindFirstChild(S(448)),
        Loot = plot:FindFirstChild(S(925)),
        PlotIndex = plot:GetAttribute(S(564)),
        ChestCapacity = plot:GetAttribute(S(749)),
    }
end
return getPlotParts
end
modules[S(339)] = function()
return function(import)
    local createWindow = import(S(1027))(import)
    local i18n = import(S(180))
    local antiAfk = import(S(759))(import)
    local buildEnv = import(S(310))
    local scheduler = import(S(9))(import)
    local spendPolicy = import(S(951))(import)
    local config = import(S(874))(import)
    local log = import(S(706))
    log(S(858) .. buildEnv.Commit .. S(863) .. buildEnv.Env
        .. S(771) .. buildEnv.BuiltAt .. S(605))
    local window = createWindow({
        Subtitle = S(1029),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(632))(import)
    local autoPlace = import(S(548))(import)
    local autoChest = import(S(851))(import)
    local autoSell = import(S(438))(import)
    local autoUpgrade = import(S(902))(import)
    local autoResearch = import(S(44))(import)
    local autoClaim = import(S(721))(import)
    local autoPlot = import(S(949))(import)
    local gameApi = import(S(768))(import)
    scheduler.RegisterStandalone(S(641), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(317), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(433), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(329), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(489), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(450), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(686), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(504), 20)
    scheduler.RegisterExclusive(S(112), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(550), autoResearch.NextPrice)
    spendPolicy.Register(S(894), autoUpgrade.NextPrice)
    spendPolicy.Register(S(260), autoPlot.NextPrice)
    spendPolicy.Register(S(792), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(688), S(462))
    farmTab:AddSectionHeader(S(650))
    persistentSegmented(
        farmTab,
        { S(709), S(923), S(559) },
        { S(491), S(518), S(817) },
        S(52), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(89),
        S(499)
    ), S(119), autoMine.SetEnabled)
    persistentSlider(farmTab, S(7), S(996),
        1, 30, S(196), 5, autoMine.SetCyclesPerSecond, S(407))
    persistentSlider(farmTab, S(320), S(539),
        1, 10, S(135), 1, autoMine.SetClicksPerCycle, S(818))
    persistentToggle(farmTab:AddCard(
        S(244),
        S(432)
    ), S(436), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(291), S(993),
        1, 60, S(1014), 3, autoPlace.SetCheckInterval, S(434))
    persistentToggle(
        farmTab:AddCard(S(170),
            S(208)),
        S(573), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(161),
            S(596)),
        S(860), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(812), S(943),
        5, 300, S(793), 60, autoPlace.SetEquipBestInterval, S(822))
    persistentToggle(
        farmTab:AddCard(S(175),
            S(213)),
        S(173), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(334), S(501),
        0, 500, S(5), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(206))
    persistentToggle(farmTab:AddCard(
        S(46),
        S(594)
    ), S(127), autoChest.SetEnabled)
    persistentSlider(farmTab, S(131), S(184),
        50, 3000, S(760), 100, autoChest.SetMinItems, S(829))
    persistentSlider(farmTab, S(364), S(652),
        1, 60, S(368), 3, autoChest.SetCheckInterval, S(217))
    persistentToggle(farmTab:AddCard(
        S(241),
        S(813)
    ), S(273), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(415),
            S(51)),
        S(452), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(1028), S(404),
        5, 300, S(649), 20, autoSell.SetCheckInterval, S(214))
    farmTab:AddSlider(S(277),
        S(837),
        5, 120, sellMaxWait, function(value)
            config.Set(S(999), value)
        end, S(699))
    persistentToggle(farmTab:AddCard(
        S(253),
        S(875)
    ), S(177), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(616), S(157),
        5, 300, S(505), 20, autoUpgrade.SetInterval, S(878))
    persistentSlider(farmTab, S(570), S(566),
        1, 20, S(827), 5, autoUpgrade.SetMaxPerPass, S(461))
    persistentSegmented(
        farmTab,
        { S(681), S(968), S(165), S(105) },
        { S(22), S(675), S(945), S(537) },
        S(535), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(70), S(459),
        0, 500, S(453), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(883))
    persistentToggle(
        farmTab:AddCard(S(164),
            S(456)),
        S(931), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(88) .. pickaxe.multiplier .. S(278)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(926)
            end
            return S(802) .. index .. S(507) .. (pickaxeLabels[index] or S(300))
        end
        local savedPick = config.Get(S(130), 0)
        pickaxeCard = farmTab:AddSlider(
            S(153),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(871), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(937)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(358),
            S(725)),
        S(796), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(788),
            S(201)),
        S(388), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(355),
            S(195)),
        S(338), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(202), S(380),
        1, 20, S(735), 2, autoUpgrade.SetMutationTarget, S(907))
    persistentSlider(farmTab, S(619), S(783),
        0, 500, S(336), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(576))
    persistentToggle(farmTab:AddCard(
        S(982),
        S(304)
    ), S(612), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(372), S(574),
        10, 600, S(371), 30, autoResearch.SetCheckInterval, S(514))
    persistentSlider(farmTab, S(636), S(466),
        1, 20, S(924), 5, autoResearch.SetMaxPerPass, S(1023))
    persistentSlider(farmTab, S(178), S(958),
        0, 500, S(867), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(158))
    persistentToggle(
        farmTab:AddCard(S(187),
            S(647)),
        S(682), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(562),
            S(409)),
        S(916), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(666),
            S(730)),
        S(100), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(618), S(998) },
        { true, false },
        S(754), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(122),
        S(1018)
    ), S(360), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(68), S(582),
        5, 300, S(194), 30, autoClaim.SetCheckInterval, S(914))
    persistentSlider(farmTab, S(850), S(66),
        1, 30, S(286), 10, autoClaim.SetMaxPerPass, S(367))
    persistentToggle(farmTab:AddCard(
        S(608),
        S(237)
    ), S(469), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(212), S(1002),
        5, 300, S(795), 30, autoPlot.SetCheckInterval, S(715))
    persistentToggle(
        farmTab:AddCard(S(20),
            S(614)),
        S(549), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(954),
            S(243)),
        S(420), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(270), S(611),
        0, 500, S(48), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(262))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(1019))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(870), S(139))
    persistentToggle(
        settingsTab:AddCard(S(497), S(85)),
        S(476),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(917), 100)
    settingsTab:AddSlider(S(72), S(247), 50, 150, savedScale, function(value)
        config.Set(S(927), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(571), 1)
    settingsTab:AddSegmented({ S(113), S(190), S(406) }, savedLanguage, function(index, value)
        local code = ({ S(136), S(284), S(384) })[index]
        config.Set(S(826), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(577), S(411), S(794) })[savedLanguage] or S(312))
    log(S(350) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(341)] = function()
return {
    [79389059854988] = S(216),
}
end
import(S(233))(import)
