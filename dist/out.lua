-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {" (Text=", "buying the ", "GetCharacterMineSpeedUpgradeCost", "interactionLogger", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "neither appendfile nor writefile is available", " team power", "[Decompile] wrote ", "autoResearchCoreLast", "Workspace", "core/run_job", "core/logger", "string", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "unlock", "AutoPlot", "Players", "table", "table", "⚙", "Search functions...", "MekPro", " - ", " ชิ้น", "[AutoUpgrade] MineBuyPickaxe ", "[Remotes] === total: ", "autoPlaceUnlockBudget", "core/scheduler", "core/run_job", " (", "table", "core/logger", "", "ZoneRow", "autoPlotInterval", "AutoClaim", "decompile", " error: ", "games/my_anime_mine/game_api", "%.3g", "core/logger", "autoResearchInterval", "PlayerScripts", "getcallbackvalue", ") ", "", " -> ", "games/my_anime_mine/auto_claim", "autoPlot", " x", "slot past the usable cap", "แพงสุดก่อน", "off - every automation spends on its own", "เดินทะลุกำแพงและพื้น", "MekPro", "PickaxesStand", "debug.getconstants", "Frame", "empty", "core/debug_dump_gui", "M", "[BestMiner] slot ", " '", "dearest", "table", "core/logger", "อังกฤษ", " still empty after placing ", "[MekPro] build ", "[AutoChest] disabled", "Content", "AutoPlace", "getscriptbytecode", "autoPlotBuy", "TextLabel", " '", "auto", "0 = ไม่จำกัด", "Players", "games/my_anime_mine/auto_research", "auto", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "%.3g", ")", "dearest", "M", "core/config", "AutoClaim", " ", " (ActionText=", "TextLabel", "games/my_anime_mine/get_plot_parts", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "[GuiDump] ", "menuScale", "UIScale", "[AutoResearch] ", "core/run_job", "ZoneBoardGui", " ", "Auto Upgrade: Pickaxe ที่จะซื้อ", " ", "ตามเครื่อง", "games/my_anime_mine/game_api", "Open menu", "Auto Mine: ความเร็ว", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "games/my_anime_mine/auto_research", "core/logger", " ", "แพงสุดก่อน", " -> ", "Auto Upgrade: กันเงินไว้", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "[GameAPI] shapes: inventory entry ", "core/teleport", "([%d,%.]+)%s*(%a*)", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "[AutoResearch] Core ", "Frame", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "ValueLabel", "GuiObject", " (", " = ", "core/spend_policy", "none", " script(s) to ", " but no unclaimed reward in its Track", "Locked miner slot", "core/debug_decompile", "WalkSpeed (ปกติ 16)", "เช็คโซนทุกกี่วินาที", ")", "", "M", "needs ", "TextLabel", "every open node is maxed", "dearest", "Auto Upgrade: แจก Pickaxe ให้ miner", "ถูกสุดก่อน", "CharMaxLevel", "autoPlaceUnlock", " = ", "s", "HumanoidRootPart", "Auto Mine", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "Frame", "ต่ำสุดก่อน", "core/logger", "AutoUpgrade", "autoSell", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", " power ", ", you have ", "games/my_anime_mine/auto_upgrade", "disabled", "core/ui", "[AutoPlace] ", "search_placeholder", "", " ", "BodyPosition", "MineSpeed", "PlayerGui", " ", "needs us at the stand - keeping the teleport", "s - jumping the queue", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "ค้นหาฟังก์ชัน...", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "autoChestMinItems", "Workspace", "autoMine", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "] job failed: ", "🐛", "getscripts", "core/logger", "languageIndex", ") ", "ยิง Equip Best ทุกกี่วินาที", ", base ", "GetCharacterUpgradeCost", "getgc", "CharSpeedMaxLevel", "upgrade", ")", "/", "Auto Upgrade Plot (Zone)", " thing available first", "[AutoResearch] nothing open and affordable right now", " ", " for ", "[Decompile] PlayerScripts not found", "PlayerGui", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", " (chest holds ", "Speed", "GetCharacterSpeedUpgradeCost", "autoUpgradeKeepMoney", "[AutoUpgrade] ", "[AutoChest] can't read the chest yet", "[^%.]+", "maxed", "ปุ่มเปิดเมนู", "games/my_anime_mine/get_best_miner", "auto", "autoResearchKeepMoney", "Auto Claim: เก็บสูงสุดต่อรอบ", "enabled", " ", "Container", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "core/logger", "TextLabel", "Zones", "Frame", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "games/my_anime_mine/auto_plot", "Text", "Auto Chest: รอบเช็ค", "games/my_anime_mine/game_api", " (", "(", "ขนาดเมนู", "[AutoSell] ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "games/my_anime_mine/auto_chest", "core/scheduler", "Mutation: หยุดเมื่อได้ x", "UICorner", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "[AutoPlace] placed ", "", "%.3g", "no", "PlotIndex", "x", "MinePower", "ลอย (Float)", " (", "BasePart", "autoUpgradeMaxPerPass", " reward(s) looked claimable but none went through", " costs ", "core/humanoid", "cheapest", "fly", "core/spend_policy", "[MekPro] settings file: ", "core/logger", "action '", "unlock", "AutoUpgrade", "Auto Upgrade Unit", "/", "core/dispatch", " from ", ")", "[AutoPlace] disabled", "=", "MineEquipPickaxe", "M", "en", " waited ", "leaderstats", "getnilinstances", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", ")", ")", "core/debug_dump_remotes", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "games/my_anime_mine/game_api", "lowest", "games/my_anime_mine/auto_sell", "nothing claimable", "ProximityPrompt", "67cd763", "games/my_anime_mine/main", "prompt disabled", ") from ", "[GuiDump] PlayerGui not found", "RemoteEvent", "UserInputService", "[Scheduler] ", "Loot", "Auto Sell: รอคิวได้นานสุด", "MineEquipSlot", " (", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "AutoUpgrade", "off", "autoClaimMaxPerPass", "Players", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "none", "[AutoPlace] unlocking ", "autoUpgradePickaxeIndex", "autoUpgradeMutationTarget", " items", "[Interaction] ClickDetector clicked: ", "yes", "ZoneBoard", "ไล่อัปเกรด miner ทุกกี่วินาที", "Auto Research (Plot)", "[AutoUpgrade] pickaxe #", ") on ", "Auto Research: ซื้อสาย Core", "Run", "Mutation: เพดานราคาต่อครั้ง", " (", "core/run_job", "games/registry", "core/logger", "[Decompile] === executor capabilities ===", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "slot ", "core/dispatch", "core/logger", "Badge", "x", "%.3g", "modules not loaded", "BasePart", "[AutoPlace] holders: ", "autoPlotKeepMoney", "Zone buying is off", "core/config", "th", "LocalScript", "MineSpeed", "TextLabel", "ย่อขยายเมนูทั้งชุด", "Players", "s", "autoChestInterval", " (money ", "=empty", "string", "AutoPlace", "core/i18n", "", "en", "th", "Holders", "dearest", " waited ", " ", "อ่านป้ายบน chest ทุกกี่วินาที", "Mining", "Workspace", " (Text=", "AutoPlot", "Humanoid", "table", "[GuiDump] === end (", "table", "s", "autoPlaceInterval", "ขายทุกกี่วินาที", "MineGui", "", "autoUpgrade", "s - carrying on without it", "autoSellMaxWait", "ItemName", "games/my_anime_mine/auto_chest", "core/ui", "", ",", " ===", "ต่างคนต่างซื้อ", "[GameAPI] shapes:   GetHolderVariant=", "s", "autoMineSpeed", "table", "core/debug_tab", "Exclusive", "waiting", "SellStand", "", "[Decompile] === end ===", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "Frame", "[AutoSell] enabled", "MekPro.log", "%$", "core/get_place_id", "] = ", "AutoPlot", "autoPlaceEquipBestInterval", "games/my_anime_mine/get_current_plot", "/", "[Bb]uy", "ScreenGui", "Badge", "x", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "no mine data", "Auto Plot: กันเงินไว้", "[AutoClaim] claimed ", "Auto Research: ซื้อสูงสุดต่อรอบ", ")", "'", "List", "UIListLayout", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "MekPro/decompiled", "Damage", "games/my_anime_mine/main", "}", "[Interaction] Button activated: ", "TextLabel", "autoPlace", "unreadable", "autoUpgradeEquipPickaxe", ") - leaving it alone for ", "core/anti_afk", " GetHolderLevel=", "Auto Plot: รอบเช็ค", "Pad", ") ", "[Interaction] logger enabled - go click/interact with things now", "[Config] couldn't write ", "nothing to place or upgrade", ": ", "enabled", ")", "index rewards are disabled in this game build", " GetVariantMult=", "core/logger", "[GameAPI] SendEvent failed: ", "costs ", "0 = ไม่จำกัด", "", "Auto Research: กันเงินไว้", "GuiService", ") -> ", "ฟาร์ม", "table", "Anti-AFK", "autoSellInterval", " costs ", "MekPro", " -> ", "table", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[AutoClaim] ", "ProximityPrompt", "  [attr] ", "Interaction Logger", "autoChest", "string", "Shop Stand", "getsenv", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "games/registry", "ChestCapacity", "walkSpeed", "works from anywhere - dropping the teleport", " items (now ", "disabled", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "TextLabel", " (x", "Mining ก่อน", "[AutoPlot] ", "core/parse_money", "sold ", "Players", "[AutoPlace] couldn't hold anything - not pressing E", "ClickDetector", "Damage ก่อน", "(nothing to do)", "th", "Character", "", "BodyVelocity", "/", " (still ", "core/debug_interaction_logger", "GetHolderMineSpeedLevel", "HumanoidRootPart", "My Anime Mine · Mining Hub", " pickaxe ", "server didn't equip it", "[Decompile] ", " team power, you have ", "", "Auto Plot: ใช้โซนล่าสุดเสมอ", " while selecting", "CoreUI", "[AutoChest] waiting: ", "core/teleport", "core/i18n", "Auto Place: เปลี่ยนตัวอ่อนออก", "[AutoResearch] cheapest open node: ", "core/logger", "AutoUpgrade", "=", "", ": State(", "Header", "empty", "disabled", " for ", "badge says ", "s", "')", "AutoResearch", "core/debug_tab", "MineTakeChest", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "lowest", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "table", "spendOrder", "disabled", "[Uu]nlock", "[AutoPlace] picked up ", "Frame", "[^%w]+", "table", "core/logger", "[Scheduler] ", "🏠", " (", "TextLabel", "games/my_anime_mine/auto_mine", " refused - ", "ClickDetector", "nothing left to buy (", "", "table", "%$", "[AutoResearch] idle: ", "AutoChest", " items)", "[AutoSell] disabled", "sale didn't go through", "enabled", ")", "AutoPlace", "Auto Place Miner", "number", "", "/s", "games/my_anime_mine/game_api", "Auto Sell", "[AutoPlot] unlocked ", "Dump Visible GUI", "enabled", " didn't go through (out of stock or too expensive)", "cost", " remotes", "", "research", "[AutoPlace] upgrading ", "_", "languageIndex", "%.3g", "x", "[AutoClaim] ", "[AutoPlot] now mining ", ": badge says ", "PlayerGui", "table", "^%$", "core/logger", "core/logger", "games/my_anime_mine/auto_mine", "th", " research is ", "AutoResearch", "AutoResearch", "getloadedmodules", "core/logger", "debug.getupvalues", "X", "fill", "Root", "affordable - waiting for a turn", "[AutoPlace] nothing in the hotbar to place", "แรงกระโดด", "core/humanoid", "Frame", ", ", "slot not in the hotbar", "[AutoPlace] unlocked ", "autoSellProtectUnits", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "antiAfk", "debug.getproto", "Text", "MineSpeed", ": ", "-", "TextBox", ", built ", "open_menu_hint", "disabled", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "MineSelectZone", "GetHolderLevel", "core/spend_policy", "Frame", "[Interaction] ProximityPrompt triggered: ", " files", " ", "core/run_job", "float", "GuiService", "{", ") gave nothing", "Hauler", "Workspace", "ClickDetector", "ตั้งค่า", "Damage", "[Interaction] Key pressed: %s (gameProcessed=%s)", "[AutoUpgrade] ", "games/my_anime_mine/game_api", "core/get_place_id", "dearest", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "AutoChest", "Main", "^([%d,%.]+)%s*(%a*)$", "[AutoResearch] ", "?", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "ReplicatedStorage", "TextButton", "[AutoResearch] ", "[Remotes] ", "games/my_anime_mine/auto_place", "Speed", "This is not a supported place.", "[AutoUpgrade] every stat on every miner is maxed", "autoResearchMaxPerPass", "Empty miner slot", "ลำดับการใช้เงินรวมทุกระบบ", "autoResearchCheapestFirst", "[GuiDump] === visible PlayerGui tree ===", "Workspace", "auto", ",", "%.3g", "CharMineSpeedMaxLevel", "Chest", " #", "core/parse_money", "fill", "core/get_me", "^M(%d+)$", "core/debug_decompile", "Players", " claimable but none could be resolved", "Decompile Client Scripts", "s", "Players", "All", "TextLabel", "Auto Research: ซื้อสาย Zone", "autoUpgradeInterval", " | ", "\n", ", have ", "Search", "", "function", "[AutoPlot] ", " not bought yet (", "AutoSell", ")", "TextLabel", "Auto Place: รอบเช็ค", ")", " fields: ", " (", ")", "AutoPlot", " ", "noclip", "'", "function", "Dump All Remotes", "core/debug_dump_gui", "/", "TextLabel", "UserInputService", "[Scheduler] ", "/", "ValueLabel", "Damage", "[AutoMine] ", "core/spend_policy", "ไทย", "games/my_anime_mine/get_current_plot", "ModuleScript", "core/build_env", "Auto Chest: เกณฑ์เก็บ", "off", "MineEquipBest", " ", "[SpendPolicy] ", "", "[GameAPI] shapes: no Character entries in the inventory", "[AutoUpgrade] ", "autoUpgradePickaxeIndex", "jumpPower", "games/my_anime_mine/get_plot_parts", "TextLabel", "", " node(s) looked buyable but the server refused", "core/logger", "core/run_job", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "[AutoPlot] couldn't switch to ", "#", "มุดแมพ (Noclip)", " is no better after the swap (", "ProximityPrompt", "Dump", "getconnections", "UIStroke", "HttpService", "All", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", ": ", "Players", "core/teleport", "Auto Place: ปลดล็อกช่อง miner", " has been busy for ", " wouldn't take ", "[AutoChest] collected ", " screen(s)) ===", "RunService", "[AutoPlace] fireproximityprompt error: ", "table", "autoClaimInterval", "LayerCollector", "table", "AutoPlace", "Players", "Frame", "[GameAPI] holder ", "Players", "core/parse_money", "Players", "Auto Place: เพดานราคาปลดช่อง", "Auto Research: รอบเช็ค", "autoMineClicks", "games/my_anime_mine/auto_upgrade", "/", "games/my_anime_mine/get_plot_parts", " -> ", "games/my_anime_mine/auto_claim", "[AutoPlace] ", "[", "Hauler", "string", "autoPlotLatest", "%.3g", "[GameAPI] shapes: no mine data yet", "core/anti_afk", ": ", ")", "=locked(", "games/my_anime_mine/get_current_plot", "dearest", "Core", "GuiButton", " pickaxe(s) for ", "[GameAPI] couldn't load the game's modules: ", "ถูกสุดก่อน", "ดีบัก", "games/my_anime_mine/get_best_miner", "^Swap out ", "[AutoResearch] ", "autoPlaceEquipBest", ") ", "TextButton", "MineSellItems", "games/my_anime_mine/game_api", "off", "core/teleport", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "AutoMine", "MineWorld", "[AntiAFK] enabled - watching Player.Idled", "/", "cheapest", "ScrollingFrame", "UIListLayout", ": ", "cheapest open node ", "[GameAPI] shapes: Hotbar[", "[AutoUpgrade] bought pickaxe #", "Auto Mine: คลิกต่อรอบ", " -> ", " (", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "core/run_job", "Players", "MekPro", "UIListLayout", "en", "core/get_leaderstat", "needs ", " (power ", "Auto Place: รอบดึงตัวเก่ง", "[AutoPlace] couldn't pick up ", "2026-09-21 13:11:44", "ไล่ซื้อทุกกี่วินาที", "both Zone and Core buying are switched off", "[AutoPlace] ", "", "autoResearchCore", "write reported success but ", "core/build_env", "\n", ")", "MineWorld", ")", "TextLabel", "write reported success but the line isn't in ", "[GameAPI] shapes: Holders.", "[AutoUpgrade] ", " (power ", "AutoResearch", "", "", " ", "menuScale", "", "[Logger] failed to write ", "BasePart", "Plots", "autoPlaceUpgradeWeak", "unreadable", "autoResearch", " GetHolderDamage=", "Dump", "autoUpgradePickaxe", "function", "en-us", " owned)", "Frame", "TextLabel", ": ", "Core buying is off", " failed)", "[AutoClaim] idle: ", "ProximityPrompt", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "CoreGui", "", "Auto Upgrade: รอบเช็ค", "[AutoChest] MineTakeChest had no effect (", "enabled", "MineUpgradeCharacter", " still locked after paying ", "locked slot", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "button has no size on screen", "", "autoUpgradeRollMutation", "AutoPlot", " mutation x", "Backpack", "HumanoidRootPart", "/config.json", "games/my_anime_mine/auto_place", "autoResearchZones", ",", "[GameAPI] shapes: Hotbar is empty", "ClaimableTiers failed", "open_menu_hint", "waiting", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "s", "core/click", "works from anywhere - no teleport needed", "TextLabel", "AutoPlace", "[Decompile] === start ===", "%.0f", "core/spend_policy", " (", " - upgrade the miners first", "[AutoSell] MineSellItems ", "autoUpgradePriority", "autoUpgradeLimitPickaxes", "[AutoPlace] drifted out of range of ", "games/my_anime_mine/game_api", "[Decompile] decompile unavailable in this executor", "[%X] ", "ความเร็ววิ่ง", ") needs ", "Auto Claim: รอบเช็ค", "[AutoPlot] ", "", "string", " screens", "  ", "core/debug_dump_remotes", "TextButton", "[Scheduler] turn: ", "", "autoClaim", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "off", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", " possible holder(s) - not buying more", "[AutoPlace] ", "Workspace", "บิน (Fly)", "table", "MineBuySkill", "=", " ParseCharStack=", "no isfile/readfile available to verify the write", "กันไม่ให้ใช้เงินหมดในรอบเดียว", " (prompt says '", "GetHolderSpeedLevel", "MineBuyPickaxe", "Speed ก่อน", "[AutoChest] chest is empty", "x", "UserInputService", "BasePart", "s behind a purchase that never happened - letting it spend", "UIPadding", "s", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "[AntiAFK] idle detected, simulating input", " (x", "core/logger", "core/get_place_id", "AutoUpgrade", "s", ")", "games/my_anime_mine/game_api", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "dev", "autoSellMaxWait", " (", "Content", "MineRollMutation", "Plots", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "s", "Players", "Auto Plot: ซื้อโซนถัดไป", "AutoSell", ") didn't take: ", "research", "TextButton", "^x(%d+)$", "core/logger", "[SpendPolicy] ", "TextLabel", "games/my_anime_mine/auto_plot", "", "HotbarFrame", ")", "OwnerUserId", "  ", "string", "Modules", "cost", "; ", "core/run_job", "AutoPlace", "", ")", "[Config] couldn't read ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "UICorner", "TextButton", "search_placeholder", "table", "TextButton", "RemoteFunction", "hookfunction", " -> ", "TabBar", " -> ", " (cost ", "RunService", "games/my_anime_mine/auto_sell", "VirtualUser", "Crew", "[AutoUpgrade] ", "TextButton", " -> x", "%.0f", "TextButton", "%$%s*([%d,%.]+)%s*(%a*)", " (", "table", "TextLabel", "Auto Claim (Index)", " doesn't exist", "[AutoPlace] Equip Best: hotbar's strongest ", "Content", " stack(s)", "table", "getrenv", "[AutoUpgrade] ", "Nodes", "Auto Chest", "core/logger", "Humanoid", "core/debug_interaction_logger", "games/my_anime_mine/game_api", "Character", " ", "[AntiAFK] disabled", "core/spend_policy", ".lua", "Auto Sell: รอบขาย", "ReplicatedStorage", "M", "Limited", "[Interaction] logger disabled", "ItemFrame", "Frame", "'", "JumpPower (ปกติ 50)", "GuiButton", "AutoPlace", "[AutoChest] enabled (collects once >= ", "Auto Upgrade: สุ่ม Mutation", "autoUpgradeMutationBudget", "%.0f", "TextButton", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "[AutoPlace] slot ", "core/i18n", "OwnerUserId", "may need us at the stand - trying that", "VirtualInputManager", "%"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(423)] = function()
return function(import)
local log = import(S(12))
local Players = game:GetService(S(752))
local VirtualUser = game:GetService(S(1000))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(798))
        connection = player.Idled:Connect(function()
            log(S(944))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(1027))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(708)] = function()
return { Env = S(953), IsDev = true, Commit = S(283), BuiltAt = S(821) }
end
modules[S(890)] = function()
local VirtualInputManager = game:GetService(S(1052))
local GuiService = game:GetService(S(442))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(873)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(333)] = function()
return function(import)
local HttpService = game:GetService(S(734))
local getPlaceId = import(S(393))
local log = import(S(575))
local ROOT_FOLDER = S(22)
local placeFolder = ROOT_FOLDER .. S(700) .. tostring(getPlaceId())
local filePath = placeFolder .. S(880)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(1009) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(985) .. filePath .. S(431) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(429) .. filePath .. S(737) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(132)] = function()
return function(import)
local Players = game:GetService(S(664))
local log = import(S(529))
local CAPABILITIES = {
    S(37), S(73), S(181), S(188), S(581),
    S(732), S(44), S(460), S(1017), S(57),
    S(583), S(598), S(993), S(271),
}
local OUT_FOLDER = S(413)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(207)) do
        if type(target) ~= S(521) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(320))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(492) .. name .. S(126) .. ((ok and type(value) == S(678)) and S(307) or S(240)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(904))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(43))
    if not playerScripts then
        log(S(198))
        return 0
    end
    pcall(makefolder, S(449))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(335)) or target:IsA(S(707)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(769) and #source > 0 then
                local name = target:GetFullName():gsub(S(527), S(564))
                if pcall(writefile, OUT_FOLDER .. S(484) .. name .. S(1029), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(8) .. written .. S(129) .. OUT_FOLDER
        .. S(245) .. failed .. S(860))
    return written
end
local function run()
    log(S(894))
    reportCapabilities()
    local count = dumpAll()
    log(S(387))
    return count
end
return run
end
end
modules[S(60)] = function()
return function(import)
local Players = game:GetService(S(299))
local log = import(S(723))
local function dumpVisible(root, indent)
    indent = indent or S(714)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(124)) and child.Visible then
            local line = indent .. child.ClassName .. S(63) .. child.Name .. S(1037)
            if child:IsA(S(469)) or child:IsA(S(1003)) then
                line = line .. S(1) .. tostring(child.Text) .. S(433)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(455) .. attrName .. S(146) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(913))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(168))
    if not playerGui then
        log(S(287))
        return 0
    end
    local screens = 0
    log(S(651))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(749)) and screen.Enabled and screen.Name ~= S(55) then
            screens = screens + 1
            log(S(94) .. screen.ClassName .. S(76) .. screen.Name .. S(409))
            dumpVisible(screen, S(976))
        end
    end
    log(S(361) .. screens .. S(744))
    return screens
end
return dumpGui
end
end
modules[S(914)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(639))
local log = import(S(582))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(288)) or descendant:IsA(S(992)) then
            count = count + 1
            log(S(642) .. descendant.ClassName .. S(803) .. descendant:GetFullName())
        end
    end
    log(S(26) .. count .. S(376))
    return count
end
return dumpRemotes
end
end
modules[S(486)] = function()
return function(import)
local Players = game:GetService(S(17))
local Workspace = game:GetService(S(10))
local UserInputService = game:GetService(S(698))
local log = import(S(324))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(614) .. describe(prompt)
            .. S(90) .. prompt.ActionText .. S(191))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(306) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(417) .. describe(button)
            .. S(357) .. tostring(button.Text) .. S(408))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(282)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(536)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(1039)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(1034))
        return
    end
    connections = {}
    log(S(428))
    local playerGui = Players.LocalPlayer:WaitForChild(S(571))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(454)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(477)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(780)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(627),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(382)] = function()
return function(import)
local interactionLogger = import(S(1023))(import)
local dumpRemotes = import(S(275))(import)
local dumpGui = import(S(695))(import)
local decompileScripts = import(S(663))(import)
local humanoid = import(S(250))
local function build(window, persist)
    local tab = window:AddTab(S(784), S(180))
    persist.Toggle(
        tab:AddCard(
            S(456),
            S(114)
        ),
        S(4),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(694),
        S(863)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(851), function()
        remoteBtn.Text = dumpRemotes() .. S(560)
    end)
    local guiCard = tab:AddCard(
        S(556),
        S(277)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(731), function()
        guiBtn.Text = dumpGui() .. S(912)
    end)
    local decompileCard = tab:AddCard(
        S(666),
        S(725)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(314), function()
        decompileBtn.Text = decompileScripts() .. S(615)
    end)
    humanoid.Start()
    persist.Slider(tab, S(906), S(133),
        8, 200, S(464), 16, humanoid.SetWalkSpeed, S(865))
    persist.Slider(tab, S(589), S(1038),
        30, 300, S(718), 50, humanoid.SetJumpPower, S(136))
    persist.Toggle(
        tab:AddCard(S(925), S(638)),
        S(252), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(728), S(54)),
        S(691), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(244), S(276)),
        S(618), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(323)] = function()
return function(import)
    local getPlaceId = import(S(947))
    local registry = import(S(318))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(645))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(816)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(270))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(661)] = function()
local Players = game:GetService(S(668))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(1022))
    local backpack = player:WaitForChild(S(878))
    return player, humanoid, backpack
end
return getMe
end
modules[S(630)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(590)] = function()
local Players = game:GetService(S(961))
local RunService = game:GetService(S(745))
local UserInputService = game:GetService(S(938))
local Workspace = game:GetService(S(924))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(359))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(488))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(483))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(939)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(329)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(166))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(500)] = function()
local Players = game:GetService(S(757))
local STRINGS = {
    en = {
        search_placeholder = S(21),
        open_menu_hint = S(105),
    },
    th = {
        search_placeholder = S(173),
        open_menu_hint = S(209),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(854)
    if locale:sub(1, 2):lower() == S(334) then
        return S(349)
    end
    return S(348)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(653) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(32)] = function()
local LOG_FILE = S(391)
local function log(message)
    local line = os.date(S(905)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(608))
            end
            appendfile(LOG_FILE, line .. S(674))
        elseif writefile then
            local existing = S(238)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(829))
        else
            error(S(6))
        end
        if not (isfile and readfile) then
            error(S(930))
        end
        if not isfile(LOG_FILE) then
            error(S(827) .. LOG_FILE .. S(1012))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(834) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(844) .. LOG_FILE .. S(601) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(473)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(977) then
        return nil
    end
    local number, suffix = text:match(S(1007))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(375), S(494))))
    if not value then
        return nil
    end
    if suffix ~= S(561) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(317)] = function()
return function(import)
local log = import(S(574))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(767) .. name .. S(179) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(233)] = function()
return function(import)
local RunService = game:GetService(S(998))
local log = import(S(503))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(290) .. task_.name .. S(38) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(530) .. task_.name .. S(741)
                    .. string.format(S(895), now - task_.busySince)
                    .. S(369))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(699) .. starved.name .. S(352)
            .. string.format(S(1044), now - starved.waitingSince)
            .. S(171))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(916) .. (currentTask and currentTask.name or S(479)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(253)] = function()
local instance
return function(import)
if instance then
    return instance
end
local log = import(S(218))
local policy = {}
local probes = {}
local order = {}
local mode = S(297)
local MAX_BLOCK = 60
local blockedSince = {}
function policy.Register(name, probe)
    if not probes[name] then
        table.insert(order, name)
    end
    probes[name] = probe
    blockedSince[name] = nil
end
function policy.SetMode(value)
    if value == mode then
        return
    end
    mode = value
    blockedSince = {}
    log(S(713) .. (mode == S(920)
        and S(53)
        or (S(2) .. mode .. S(194))))
end
function policy.GetMode()
    return mode
end
local function priceFor(name)
    local probe = probes[name]
    if not probe then
        return nil
    end
    local ok, price = pcall(probe, mode)
    if not ok then
        return nil
    end
    return tonumber(price)
end
function policy.MaySpend(name)
    if mode == S(710) then
        return true
    end
    local price = priceFor(name)
    if not price then
        blockedSince[name] = nil
        return true
    end
    local beaten = false
    for _, other in ipairs(order) do
        if other ~= name then
            local theirs = priceFor(other)
            if theirs then
                if mode == S(251) and theirs < price then
                    beaten = true
                    break
                elseif mode == S(141) and theirs > price then
                    beaten = true
                    break
                end
            end
        end
    end
    if not beaten then
        blockedSince[name] = nil
        return true
    end
    local now = tick()
    blockedSince[name] = blockedSince[name] or now
    if now - blockedSince[name] >= MAX_BLOCK then
        log(S(969) .. name .. S(269)
            .. string.format(S(1005), now - blockedSince[name])
            .. S(940))
        blockedSince[name] = nil
        return true
    end
    return false
end
instance = policy
return instance
end
end
modules[S(794)] = function()
local Players = game:GetService(S(339))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(148))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(373)] = function()
return function(import)
local i18n = import(S(346))
local UserInputService = game:GetService(S(289))
local GuiService = game:GetService(S(619))
local THEME = {
    BrandName = S(813),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(235))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(987))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(941))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target)
    local dragging, dragInput, dragStart, startPos
    local state = { moved = false }
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            state.moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                state.moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections, state
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(988))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(33)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(733))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(75))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(637)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(697))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(721)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(801))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(802))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(1010))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(221))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(833))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(219))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(337))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(701))
            if not valueLbl then
                valueLbl = Instance.new(S(683))
                valueLbl.Name = S(123)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(325))
            if not badge then
                badge = Instance.new(S(970))
                badge.Name = S(401)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(1045))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(151))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(58))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(640))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(374)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(1053)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(526))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(856))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(915))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(506)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(121))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(411))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(790))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(599)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(400))
    screenGui.Name = S(497)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(864))
    local main = Instance.new(S(591))
    main.Name = S(634)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(96))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(753))
    tabBar.Name = S(995)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(814))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(389))
    header.Name = S(508)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(1006))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(857))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(91))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(46)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(966))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(584)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(991))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(602)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(603))
    searchBox.Name = S(676)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(163))
    searchBox.Text = S(825)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(1036))
    content.Name = S(956)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(613))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(892))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(886))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(670))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    local avatarConnections, avatarDrag = makeDraggable(avatar, main)
    for _, connection in ipairs(avatarConnections) do
        table.insert(connections, connection)
    end
    table.insert(connections, avatar.Activated:Connect(function()
        if minimized and not avatarDrag.moved then
            setMinimized(false)
        end
    end))
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(224)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(839) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(1026) .. (item.Subtitle or S(538))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(989))
        footerLabel.Text = i18n.T(S(605))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(372)] = function()
return function(import)
local gameApi = import(S(792))(import)
local log = import(S(1021))
local runGuarded = import(S(98))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(1041) .. minItemsToCollect .. S(543))
    else
        log(S(70))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(420) then
            log(S(206))
            lastReported = S(848)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(509) then
            log(S(936))
            lastReported = S(59)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(887) then
            local capacity = gameApi.ChestCapacity()
            log(S(498) .. count .. S(192) .. want .. S(305)
                .. (capacity and (S(201) .. capacity .. S(135)) or S(482)))
            lastReported = S(384)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(542), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(743) .. count .. S(466) .. after .. S(974))
        else
            log(S(867) .. count
                .. S(994) .. tostring(after) .. S(84))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(765)] = function()
return function(import)
local gameApi = import(S(951))(import)
local log = import(S(946))
local runGuarded = import(S(811))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(568) .. (enabled and S(557) or S(467)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(861) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(406) .. reward.category .. S(841)
                .. tostring(reward.tierKey) .. S(658) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(453) .. #pending .. S(248))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(36), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(576)] = function()
return function(import)
local getCurrentPlot = import(S(706))
local getPlotParts = import(S(719))
local log = import(S(968))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(624), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(703) .. (enabled and S(546) or S(160)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(643)] = function()
return function(import)
local Players = game:GetService(S(755))
local getCurrentPlot = import(S(777))
local getPlotParts = import(S(92))
local getMinerSlots = import(S(210))(import)
local teleport = import(S(117))
local parseMoney = import(S(659))
local gameApi = import(S(553))(import)
local log = import(S(255))
local runGuarded = import(S(981))(import)
local spendPolicy = import(S(1028))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(648)
local LOCKED_SLOT_TEXT = S(131)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(426))
    return pad and pad:FindFirstChildWhichIsA(S(730), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local POWER_SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(139)) then
            local digits, suffix = descendant.Text:match(S(635))
            if digits then
                local value = tonumber((digits:gsub(S(883), S(840))))
                if value then
                    if suffix ~= S(910) then
                        value = value * (POWER_SUFFIXES[suffix]
                            or POWER_SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
                            or 1)
                    end
                    return value
                end
            end
        end
    end
    return nil
end
local function promptCharName(prompt)
    local text = prompt.ObjectText or S(677)
    return (text:gsub(S(786), S(347)))
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(418)) and descendant.Text:match(S(573)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(322) .. holder.Name .. S(680)
            .. gameApi.MinerSlots() .. S(762) .. gameApi.MaxMinerSlots() .. S(855)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(871)
    end
    if not prompt.Enabled then
        return true, S(285)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(438) .. price
    end
    local action = prompt.ActionText or S(972)
    if action:match(S(524)) or action:match(S(399)) or action:match(S(392)) then
        return true, S(256) .. action .. S(692)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(879))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(746) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local swapCooldown = {}
local SWAP_COOLDOWN = 120
local placedByUs = {}
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(776) .. why .. S(832))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(343))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local charName = promptCharName(prompt)
                local base
                local remembered = placedByUs[holder.Name]
                if remembered and remembered.name == charName then
                    base = remembered.power
                else
                    base = gameApi.HolderBaseDamage(holder.Name)
                        or gameApi.CharacterBaseDamage(charName)
                        or shown
                end
                table.insert(summary, holder.Name .. S(928) .. prompt.ObjectText
                    .. S(228) .. shown .. S(186) .. math.floor(base) .. S(688))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = promptCharName(prompt),
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(673))
    if line ~= lastSurvey then
        log(S(330) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(660), survey.empty[1], candidates[1]
    end
    local weakest = nil
    if upgradeWeakMiners then
        local now = tick()
        for _, entry in ipairs(survey.occupied) do
            if (swapCooldown[entry.holder.Name] or 0) <= now then
                weakest = entry
                break
            end
        end
    end
    if weakest and candidates[1].power > weakest.power then
        return S(190), weakest, candidates[1]
    end
    if unlockSlots and spendPolicy.MaySpend(S(982)) then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(257), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(923) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(264))
    else
        lastAttempt = 0
        log(S(412))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.NextPrice()
    if not unlockSlots then
        return nil
    end
    local survey = (look())
    if not survey then
        return nil
    end
    local money = gameApi.GetMoney()
    for _, pad in ipairs(survey.locked or {}) do
        if pad.price <= money
            and (unlockBudget == 0 or pad.price <= unlockBudget)
        then
            return pad.price
        end
    end
    return nil
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(921),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(593)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(51)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(491)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(588))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(902) .. holder.Name .. S(496))
            return false
        end
        log(S(1048) .. candidate.slot .. S(687) .. candidate.name
            .. S(964) .. tostring(why))
    end
    if not miner then
        log(S(476))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(824) .. holder.Name .. S(68) .. miner.name)
        return false
    end
    placedByUs[holder.Name] = { name = miner.name, power = miner.power }
    log(S(237) .. miner.name .. S(837) .. miner.power
        .. S(312) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(820) .. entry.name .. S(262) .. entry.holder.Name
            .. S(932) .. tostring(entry.prompt.ActionText) .. S(514))
        return false
    end
    log(S(525) .. entry.name .. S(818) .. entry.power
        .. S(286) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(162) .. pad.holder.Name .. S(870)
                .. tostring(pad.priceText))
        else
            log(S(594) .. pad.holder.Name
                .. S(511) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    local holderName = target.holder.Name
    local before = target.power
    placedByUs[holderName] = nil
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    local remembered = placedByUs[holderName]
    local after = remembered and remembered.power or gameApi.HolderBaseDamage(holderName)
    if after and before and after <= before then
        swapCooldown[holderName] = tick() + SWAP_COOLDOWN
        log(S(766) .. holderName .. S(729)
            .. string.format(S(655), before) .. S(808) .. string.format(S(771), after)
            .. S(422) .. SWAP_COOLDOWN .. S(147))
    else
        swapCooldown[holderName] = nil
    end
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(1013)
            .. string.format(S(239), bestBefore) .. S(764)
            .. string.format(S(83), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(1040), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(430))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(585) then
        runGuarded(S(345), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(15) then
        log(S(302) .. target.holder.Name
            .. S(197) .. tostring(target.priceText))
        runGuarded(S(893), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(563) .. target.holder.Name .. S(858) .. target.name
        .. S(1008) .. target.power .. S(443) .. best.name .. S(897) .. best.power .. S(984))
    runGuarded(S(548), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(223)] = function()
return function(import)
local gameApi = import(S(39))(import)
local parseMoney = import(S(756))
local log = import(S(436))
local runGuarded = import(S(29))(import)
local spendPolicy = import(S(612))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(909) .. (enabled and S(432) or S(523)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(540)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(569) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(532) .. deepestKey .. S(950))
        return true
    end
    log(S(726) .. deepestKey)
    return false
end
function autoPlot.NextPrice()
    if not buyNextZone then
        return nil
    end
    local index = nextLockedZone()
    if not index then
        return nil
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        return nil
    end
    return price
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    if not spendPolicy.MaySpend(S(876)) then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(679) .. gameApi.ZoneDisplayName(zoneKey) .. S(448)
                .. math.floor(price) .. S(158) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(555) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(955) .. zoneKey .. S(685))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(965) then
            reportedLocked = S(562)
            log(S(472) .. zoneKey .. S(535)
                .. gameApi.ZoneDisplayName(currentKey) .. S(578)
                .. done .. S(799) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone and spendPolicy.MaySpend(S(689)) then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(16), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(80)] = function()
return function(import)
local gameApi = import(S(629))(import)
local log = import(S(41))
local runGuarded = import(S(11))(import)
local spendPolicy = import(S(896))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(97) .. (enabled and S(868) or S(606)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(636) .. node.zoneKey .. S(102) .. node.id
                    .. S(227) .. node.family .. S(45) .. node.level .. S(450) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(120) .. node.id
                    .. S(316) .. node.family .. S(184) .. node.level .. S(112) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(787) .. blocked
                .. S(722))
        elseif powerBlocked then
            log(S(641) .. powerBlocked.zoneKey .. S(353) .. powerBlocked.id
                .. S(125) .. powerBlocked.family .. S(907)
                .. math.floor(powerBlocked.powerReq) .. S(493)
                .. math.floor(powerBlocked.power)
                .. S(898))
        else
            log(S(195)
                .. S(342) .. math.floor(gameApi.GetMoney()) .. S(547))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(502) .. nearest.zoneKey
                    .. S(196) .. nearest.id .. S(809) .. nearest.family .. S(427)
                    .. nearest.level .. S(696) .. nearest.max
                    .. S(249) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(823)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(779),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(332))
        end
        if not buyCore then
            table.insert(parts, S(859))
        end
        if #parts == 0 then
            return S(140)
        end
        return S(537) .. table.concat(parts, S(592)) .. S(682)
    end
    local why
    if nearest.cost > budget then
        why = S(138) .. string.format(S(566), nearest.cost)
            .. S(675) .. string.format(S(40), money)
    elseif nearest.powerOk == false then
        why = S(817) .. string.format(S(327), nearest.powerReq) .. S(7)
    else
        why = S(587)
    end
    return S(804) .. nearest.zoneKey .. S(110) .. tostring(nearest.id)
        .. S(30) .. tostring(nearest.family) .. S(789) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(541) .. summary)
end
local lastIdleReport = 0
function autoResearch.NextPrice(mode)
    local candidates = {}
    if buyZones then
        for _, node in ipairs((buyableZoneNodes())) do
            table.insert(candidates, node.cost)
        end
    end
    if buyCore then
        for _, node in ipairs(buyableCoreNodes()) do
            table.insert(candidates, node.cost)
        end
    end
    local best = nil
    for _, cost in ipairs(candidates) do
        if not best
            or (mode == S(351) and cost > best)
            or (mode ~= S(64) and cost < best)
        then
            best = cost
        end
    end
    return best
end
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if has and not spendPolicy.MaySpend(S(580)) then
        return false
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(579), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(999)] = function()
return function(import)
local Workspace = game:GetService(S(652))
local gameApi = import(S(226))(import)
local teleport = import(S(739))
local log = import(S(319))
local runGuarded = import(S(724))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(544))
    else
        lastAttempt = 0
        log(S(390))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(385))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(862), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(899)
                .. (sold and S(465)
                         or S(170)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(230) .. (sold and (S(474) .. #keys .. S(1015)) or S(545)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(681), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(761)] = function()
return function(import)
local Workspace = game:GetService(S(623))
local gameApi = import(S(1024))(import)
local teleport = import(S(499))
local log = import(S(109))
local runGuarded = import(S(617))(import)
local spendPolicy = import(S(704))(import)
local STATS = { S(702), S(202), S(600) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(279)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(205) .. (enabled and S(214) or S(510)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(528) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(979) or S(208)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(628) .. holderKey .. S(100) .. choice.stat .. S(712)
                        .. choice.info.level .. S(47) .. after.level
                        .. S(260) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(559) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(56))
    local shop = stand and stand:FindFirstChild(S(459))
    if not shop then
        return nil
    end
    if shop:IsA(S(845)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(246), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(25)
                .. (bought and S(891)
                           or S(1051)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(872))
        end
    end
    if bought then
        log(S(806) .. pickaxe.index .. S(89) .. pickaxe.name
            .. S(945) .. pickaxe.multiplier .. S(830))
        return true
    end
    log(S(311) .. pickaxe.index .. S(690) .. pickaxe.name
        .. S(558))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(1002) .. holderKey .. S(490)
                    .. tostring(worn or S(128)) .. S(996) .. wanted.name)
            else
                log(S(836) .. holderKey .. S(742)
                    .. wanted.name .. S(485) .. tostring(nowWorn or S(301)) .. S(273))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(716) .. holderKey .. S(877) .. variant
                .. S(1004) .. after .. S(997) .. cost .. S(263))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local maySpend = spendPolicy.MaySpend(S(504))
    local bought, tooExpensive, maxed = 0, 0, 0
    if maySpend then
        bought, tooExpensive, maxed = upgradeStats()
    end
    if buyPickaxe and maySpend and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(1018) .. gameApi.OwnedPickaxeCount()
                    .. S(781) .. gameApi.MaxMinerSlots()
                    .. S(922))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(295))
        elseif maxed > 0 then
            log(S(646))
        else
            log(S(150))
        end
    end
    busy = false
end
function autoUpgrade.NextPrice(mode)
    local best = nil
    local function consider(price)
        if not price then
            return
        end
        if not best
            or (mode == S(631) and price > best)
            or (mode ~= S(778) and price < best)
        then
            best = price
        end
    end
    for _, holderKey in ipairs(holderKeys()) do
        local choice = chooseStat(holderKey)
        if choice then
            consider(choice.info.cost)
        end
    end
    if buyPickaxe then
        local pickaxe = findPickaxe()
        if pickaxe then
            consider(pickaxe.cost)
        end
    end
    return best
end
function autoUpgrade.HasWork()
    local maySpend = spendPolicy.MaySpend(S(296))
    if maySpend then
        for _, holderKey in ipairs(holderKeys()) do
            if chooseStat(holderKey) then
                return true
            end
        end
        if buyPickaxe and findPickaxe() then
            return true
        end
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(154), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(903)] = function()
local instance
return function(import)
if instance then
    return instance
end
local ReplicatedStorage = game:GetService(S(1031))
local Workspace = game:GetService(S(176))
local log = import(S(153))
local api = {}
local protectUsefulUnits = true
local reportedHolderShape = false
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(978), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(782) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(990))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(381) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(768) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(362) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(451) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(622) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(662))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(437) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(517))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(735), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(360) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(539) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(481) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(791), keys)
end
function api.EquipSlot(slot)
    return send(S(293), slot == S(843) and S(164) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(934), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(266), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(957), holderKey)
end
local describedShapes = false
function api.DescribeShapes()
    if describedShapes or not modules() then
        return
    end
    describedShapes = true
    local mine = api.GetMineData()
    if not mine then
        log(S(772))
        return
    end
    local function describe(value)
        if type(value) ~= S(445) then
            return tostring(value)
        end
        local parts = {}
        for key, item in pairs(value) do
            table.insert(parts, tostring(key) .. S(505) .. tostring(item))
        end
        table.sort(parts)
        return S(620) .. table.concat(parts, S(215)) .. S(416)
    end
    pcall(function()
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(19) and holder.CharName then
                log(S(835) .. holderKey .. S(616) .. describe(holder))
                log(S(378) ..
                    tostring(mineConfig.GetHolderVariant(holder))
                    .. S(435) ..
                    tostring(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
                    .. S(850) .. tostring(mineConfig.GetHolderDamage(holder))
                    .. S(424) .. tostring(mineConfig.GetHolderLevel(holder)))
                break
            end
        end
    end)
    pcall(function()
        local shown = 0
        for slot, key in pairs(mine.Hotbar or {}) do
            log(S(805) .. tostring(slot) .. S(394) .. tostring(key)
                .. S(929) .. tostring(mineConfig.ParseCharStack(key)))
            shown = shown + 1
            if shown >= 3 then
                break
            end
        end
        if shown == 0 then
            log(S(884))
        end
    end)
    pcall(function()
        local entries = mineInventory.Collect(mine, S(669), mineInventory.KindOrder.OreFirst)
        local shown = 0
        for _, entry in pairs(entries or {}) do
            if entry.Kind == S(1025) then
                log(S(116) .. describe(entry))
                shown = shown + 1
                if shown >= 2 then
                    break
                end
            end
        end
        if shown == 0 then
            log(S(715))
        end
    end)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(711))
end
function api.BuySkill(zoneKey, family)
    return send(S(927), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(1016)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(475)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(243))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(853) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(926) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(610), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(797))
    plots = plots and plots:FindFirstChild(S(846))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(812)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(975)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(308))
    local gui = board and board:FindFirstChild(S(99))
    local root = gui and gui:FindFirstChild(S(586))
    local list = root and root:FindFirstChild(S(410))
    local row = list and list:FindFirstChild(S(34) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(720)) and descendant.Text ~= S(917) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(693) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(869), characterKey, stat == S(355) and S(167) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(611), max = S(144), cost = S(187) },
    Speed = { level = S(933), max = S(189), cost = S(203) },
    MineSpeed = { level = S(487), max = S(656), cost = S(3) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(13) or charName == S(874) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local derived = nil
    pcall(function()
        local shown = tonumber(mineConfig.GetHolderDamage(holder))
        if not shown or shown <= 0 then
            return
        end
        local levelMult = tonumber(mineConfig.GetCharacterLevelMultiplier(
            mineConfig.GetHolderLevel(holder))) or 1
        local pickaxe = mineConfig.GetHolderPickaxe(holder)
        local pickMult = tonumber(pickaxe and pickaxe.Multiplier) or 1
        if levelMult > 0 and pickMult > 0 then
            derived = shown / (levelMult * pickMult)
        end
    end)
    if derived and derived > 0 then
        return derived
    end
    if not reportedHolderShape then
        reportedHolderShape = true
        local fields = {}
        for key, value in pairs(holder) do
            table.insert(fields, tostring(key) .. S(265) .. tostring(value))
        end
        table.sort(fields)
        log(S(754) .. holderKey .. S(686) .. table.concat(fields, S(165)))
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(747) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(550) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(911) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(572) and type(holder.CharName) == S(458) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(1033), S(383) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(65) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(328)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(404)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(434)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(750) then
        return {}, S(885)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(281)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(18) or type(state.Track) ~= S(31) then
                table.insert(notes, tabKey .. S(507) .. tostring(category) .. S(621))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(570) .. waiting
                        .. S(130))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(512) .. total .. S(665)
            .. (#notes > 0 and (S(23) .. table.concat(notes, S(980))) or S(983))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(344) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
instance = api
return instance
end
end
modules[S(785)] = function()
return function(import)
local Players = game:GetService(S(79))
local gameApi = import(S(278))(import)
local log = import(S(182))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(118))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(654), S(367))))
    if not value then
        return nil
    end
    if suffix ~= S(551) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(199))
    local mineGui = playerGui and playerGui:FindFirstChild(S(366))
    local hotbar = mineGui and mineGui:FindFirstChild(S(973))
    local content = hotbar and hotbar:FindFirstChild(S(71))
    return content and content:FindFirstChild(S(216))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(533)) then
            local count = descendant.Text:match(S(967))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(1035))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(1014))
    local nameLabel = content and content:FindFirstChild(S(371))
    local damageLabel = content and content:FindFirstChild(S(626))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(440) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(398) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(62) .. slot .. S(774) .. info.name
                        .. S(157) .. info.power .. S(50) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(397)] = function()
local Players = game:GetService(S(738))
local Workspace = game:GetService(S(356))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(831))
    local plots = mineWorld and mineWorld:FindFirstChild(S(958))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(1050)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(763)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(350)),
        Nodes = plot:FindFirstChild(S(1019)),
        Chest = plot:FindFirstChild(S(657)),
        Zones = plot:FindFirstChild(S(220)),
        Crew = plot:FindFirstChild(S(1001)),
        Loot = plot:FindFirstChild(S(291)),
        PlotIndex = plot:GetAttribute(S(241)),
        ChestCapacity = plot:GetAttribute(S(463)),
    }
end
return getPlotParts
end
modules[S(415)] = function()
return function(import)
    local createWindow = import(S(161))(import)
    local i18n = import(S(1049))
    local antiAfk = import(S(773))(import)
    local buildEnv = import(S(828))
    local scheduler = import(S(28))(import)
    local spendPolicy = import(S(127))(import)
    local config = import(S(87))(import)
    local log = import(S(66))
    log(S(69) .. buildEnv.Commit .. S(294) .. buildEnv.Env
        .. S(604) .. buildEnv.BuiltAt .. S(274))
    local window = createWindow({
        Subtitle = S(489),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(534))(import)
    local autoPlace = import(S(881))(import)
    local autoChest = import(S(232))(import)
    local autoSell = import(S(280))(import)
    local autoUpgrade = import(S(159))(import)
    local autoResearch = import(S(108))(import)
    local autoClaim = import(S(48))(import)
    local autoPlot = import(S(971))(import)
    local gameApi = import(S(104))(import)
    scheduler.RegisterStandalone(S(796), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(633), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(751), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(258), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(838), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(88), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(395), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(370), 20)
    scheduler.RegisterExclusive(S(963), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    spendPolicy.Register(S(515), autoResearch.NextPrice)
    spendPolicy.Register(S(948), autoUpgrade.NextPrice)
    spendPolicy.Register(S(358), autoPlot.NextPrice)
    spendPolicy.Register(S(72), autoPlace.NextPrice)
    scheduler.Start()
    local farmTab = window:AddTab(S(444), S(531))
    farmTab:AddSectionHeader(S(649))
    persistentSegmented(
        farmTab,
        { S(377), S(143), S(111) },
        { S(793), S(800), S(85) },
        S(522), 1, spendPolicy.SetMode
    )
    persistentToggle(farmTab:AddCard(
        S(149),
        S(82)
    ), S(177), autoMine.SetEnabled)
    persistentSlider(farmTab, S(106), S(5),
        1, 30, S(380), 5, autoMine.SetCyclesPerSecond, S(552))
    persistentSlider(farmTab, S(807), S(1046),
        1, 10, S(760), 1, autoMine.SetClicksPerCycle, S(937))
    persistentToggle(farmTab:AddCard(
        S(549),
        S(810)
    ), S(419), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(684), S(888),
        1, 60, S(364), 3, autoPlace.SetCheckInterval, S(340))
    persistentToggle(
        farmTab:AddCard(S(501),
            S(520)),
        S(847), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(632),
            S(107)),
        S(788), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(819), S(185),
        5, 300, S(396), 60, autoPlace.SetEquipBestInterval, S(949))
    persistentToggle(
        farmTab:AddCard(S(740),
            S(14)),
        S(145), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(758), S(439),
        0, 500, S(27), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(61))
    persistentToggle(farmTab:AddCard(
        S(1020),
        S(300)
    ), S(457), autoChest.SetEnabled)
    persistentSlider(farmTab, S(709), S(236),
        50, 3000, S(175), 100, autoChest.SetMinItems, S(24))
    persistentSlider(farmTab, S(225), S(354),
        1, 60, S(341), 3, autoChest.SetCheckInterval, S(363))
    persistentToggle(farmTab:AddCard(
        S(554),
        S(452)
    ), S(155), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(222),
            S(178)),
        S(595), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(1030), S(365),
        5, 300, S(447), 20, autoSell.SetCheckInterval, S(513))
    farmTab:AddSlider(S(292),
        S(119),
        5, 120, sellMaxWait, function(value)
            config.Set(S(954), value)
        end, S(942))
    persistentToggle(farmTab:AddCard(
        S(259),
        S(115)
    ), S(368), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(866), S(309),
        5, 300, S(672), 20, autoUpgrade.SetInterval, S(379))
    persistentSlider(farmTab, S(122), S(231),
        1, 20, S(247), 5, autoUpgrade.SetMaxPerPass, S(402))
    persistentSegmented(
        farmTab,
        { S(152), S(478), S(935), S(471) },
        { S(519), S(414), S(644), S(336) },
        S(900), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(113), S(609),
        0, 500, S(204), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(86))
    persistentToggle(
        farmTab:AddCard(S(607),
            S(795)),
        S(852), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(470) .. pickaxe.multiplier .. S(775)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(388)
            end
            return S(727) .. index .. S(169) .. (pickaxeLabels[index] or S(919))
        end
        local savedPick = config.Get(S(303), 0)
        pickaxeCard = farmTab:AddSlider(
            S(101),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(717), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(386)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(174),
            S(468)),
        S(901), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(142),
            S(952)),
        S(421), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(1042),
            S(321)),
        S(875), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(234), S(217),
        1, 20, S(304), 2, autoUpgrade.SetMutationTarget, S(326))
    persistentSlider(farmTab, S(315), S(78),
        0, 500, S(1043), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(137))
    persistentToggle(farmTab:AddCard(
        S(310),
        S(959)
    ), S(849), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(759), S(822),
        10, 600, S(42), 30, autoResearch.SetCheckInterval, S(667))
    persistentSlider(farmTab, S(407), S(931),
        1, 20, S(647), 5, autoResearch.SetMaxPerPass, S(567))
    persistentSlider(farmTab, S(441), S(172),
        0, 500, S(212), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(267))
    persistentToggle(
        farmTab:AddCard(S(671),
            S(1047)),
        S(882), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(313),
            S(986)),
        S(826), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(93),
            S(156)),
        S(9), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(783), S(52) },
        { true, false },
        S(650), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(1011),
        S(943)
    ), S(918), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(908), S(272),
        5, 300, S(748), 30, autoClaim.SetCheckInterval, S(960))
    persistentSlider(farmTab, S(213), S(461),
        1, 30, S(298), 10, autoClaim.SetMaxPerPass, S(242))
    persistentToggle(farmTab:AddCard(
        S(193),
        S(736)
    ), S(49), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(425), S(134),
        5, 300, S(35), 30, autoPlot.SetCheckInterval, S(889))
    persistentToggle(
        farmTab:AddCard(S(962),
            S(596)),
        S(74), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(495),
            S(403)),
        S(770), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(405), S(200),
        0, 500, S(331), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(1032))
    if buildEnv.IsDev then
        task.delay(8, gameApi.DescribeShapes)
    end
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(516))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(625), S(20))
    persistentToggle(
        settingsTab:AddCard(S(446), S(518)),
        S(597),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(95), 100)
    settingsTab:AddSlider(S(229), S(338), 50, 150, savedScale, function(value)
        config.Set(S(842), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(183), 1)
    settingsTab:AddSegmented({ S(103), S(67), S(705) }, savedLanguage, function(index, value)
        local code = ({ S(77), S(268), S(480) })[index]
        config.Set(S(565), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(211), S(815), S(577) })[savedLanguage] or S(81))
    log(S(254) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(462)] = function()
return {
    [79389059854988] = S(284),
}
end
import(S(261))(import)
