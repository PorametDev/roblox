-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"AutoUpgrade", " (", " (power ", "/", "List", "UIListLayout", "[Remotes] === total: ", "[AutoChest] disabled", ": ", "autoMine", ")", "core/debug_dump_gui", "core/parse_money", "games/registry", " items", "core/logger", "TextLabel", "M", "core/logger", "TextButton", "[AutoPlace] nothing in the hotbar to place", " ชิ้น", "%$", "write reported success but ", "Dump Visible GUI", "[AutoUpgrade] ", " ", "autoUpgradeLimitPickaxes", " - upgrade the miners first", "action '", "[AntiAFK] enabled - watching Player.Idled", "games/my_anime_mine/get_current_plot", ")", "TextLabel", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "/", "decompile", "autoMineClicks", " waited ", "AutoPlot", "MineSellItems", "sale didn't go through", "[Interaction] Button activated: ", "yes", "ClickDetector", "s - jumping the queue", "_", " (", "core/logger", ")", "th", "Mutation: เพดานราคาต่อครั้ง", "", "[AutoResearch] cheapest open node: ", "BasePart", "UIScale", "CharMaxLevel", "[Decompile] PlayerScripts not found", "[GuiDump] PlayerGui not found", "games/my_anime_mine/auto_claim", "autoMineSpeed", "[Interaction] Key pressed: %s (gameProcessed=%s)", "core/logger", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "costs ", "Workspace", "GetCharacterUpgradeCost", "[AutoResearch] nothing open and affordable right now", "Auto Place: รอบเช็ค", "/config.json", "table", "waiting", " ", ")", "prompt disabled", "ItemFrame", "table", "TextLabel", "unreadable", "autoUpgradePriority", "ขนาดเมนู", "string", "research", "autoSellMaxWait", ")", "jumpPower", "TextLabel", "Empty miner slot", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "games/my_anime_mine/auto_chest", "AutoClaim", "Auto Upgrade: สุ่ม Mutation", "My Anime Mine · Mining Hub", "autoPlaceUpgradeWeak", "UIListLayout", "autoResearchCoreLast", "Players", " node(s), ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "autoSellMaxWait", "core/get_place_id", "table", "core/parse_money", "Text", "", "[AutoUpgrade] ", ", you have ", "ChestCapacity", "UICorner", "en", " research is ", "no", "GetHolderLevel", "AutoClaim", "ความเร็ววิ่ง", "autoUpgradeInterval", "disabled", "Auto Research: ซื้อสาย Core", "core/humanoid", "Mutation: หยุดเมื่อได้ x", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Content", "disabled", "[AutoClaim] ", "Auto Chest: รอบเช็ค", "[GameAPI] SendEvent failed: ", "Auto Upgrade: Pickaxe ที่จะซื้อ", "TextLabel", "[AutoPlot] now mining ", "UICorner", "[AutoPlot] unlocked ", " buyZones=", "^M(%d+)$", "waiting", "CharSpeedMaxLevel", "[Decompile] wrote ", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/auto_plot", "core/logger", " pickaxe(s) for ", " power=", " ===", "/", "UIPadding", "needs us at the stand - keeping the teleport", "core/logger", "อังกฤษ", "UIListLayout", "MineSpeed", "games/my_anime_mine/auto_chest", "TextButton", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "core/scheduler", "games/my_anime_mine/main", "core/run_job", ")", "Zones", "M", "[AutoChest] can't read the chest yet", "Frame", "=empty", "TextLabel", "Frame", "TextLabel", "%", "GetHolderMineSpeedLevel", "This is not a supported place.", "8ca68a7+", "GetHolderSpeedLevel", "search_placeholder", "(none)", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "games/registry", "games/my_anime_mine/get_plot_parts", "x", "[AutoResearch] idle: money=", "TextLabel", "[Scheduler] ", "Main", "[AutoChest] MineTakeChest had no effect (", "'", "[AutoPlace] drifted out of range of ", "/", " (", "[Uu]nlock", " (Text=", "autoUpgradeMaxPerPass", "games/my_anime_mine/game_api", "autoUpgrade", "s", ": ", "Pad", "[AutoResearch] ", "[AutoPlot] ", "", "core/build_env", "core/run_job", "sold ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", ",", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "core/i18n", "fill", ") ", "Hauler", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "MekPro/decompiled", "Damage", "games/my_anime_mine/game_api", "debug.getconstants", "fill", "autoUpgradeMutationBudget", "enabled", "[AutoPlace] picked up ", " ", "MinePower", "Run", "TextBox", "Workspace", " (", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "Workspace", ".lua", "Badge", "[AutoPlace] fireproximityprompt error: ", "/", "autoClaimMaxPerPass", "s", "core/logger", "LayerCollector", "[MekPro] settings file: ", "button has no size on screen", "float", "cost", "core/logger", "[AutoUpgrade] ", "autoUpgradeKeepMoney", "[AutoUpgrade] bought pickaxe #", "", "[Interaction] ProximityPrompt triggered: ", "core/teleport", "games/my_anime_mine/auto_sell", " mutation x", "interactionLogger", " (ActionText=", "TextButton", "string", "games/my_anime_mine/main", " files", "en", "Backpack", "Auto Sell: รอคิวได้นานสุด", " not maxed", "no isfile/readfile available to verify the write", "", "[%X] ", "Players", "?", "Container", "ไทย", "[Decompile] decompile unavailable in this executor", "autoResearchCore", "Badge", "autoPlotBuy", "[AutoChest] chest is empty", "[GuiDump] === visible PlayerGui tree ===", "ClickDetector", "", "'", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "", " ", "ลอย (Float)", " -> ", "Frame", "autoUpgradeMutationTarget", "/", "maxed", "walkSpeed", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "Auto Sell", "TextLabel", "[AutoPlace] couldn't pick up ", "[AutoSell] ", "^([%d,]+)$", "All", "ReplicatedStorage", "Frame", "autoResearch", "Mining", "core/debug_interaction_logger", "MineEquipPickaxe", "MekPro", "core/run_job", " (chest holds ", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "[Bb]uy", "Auto Upgrade Unit", "core/get_me", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", " costs ", "[GuiDump] ", "core/ui", "AutoResearch", "autoPlace", "=locked(", "table", "", " costs ", "แรงกระโดด", "MineGui", "MineEquipSlot", "games/my_anime_mine/auto_research", "menuScale", "table", "core/logger", " (", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "autoClaim", "MineRollMutation", "function", "games/my_anime_mine/game_api", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "research", "s", "Search functions...", " node(s), ", "core/debug_decompile", " owned)", " -> ", "core/i18n", "core/build_env", "function", "TabBar", "X", "TextButton", "en", "", "number", "table", " failed)", "Open menu", "disabled", "HumanoidRootPart", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "core/logger", "AutoPlace", "open_menu_hint", "TextButton", "MineWorld", "th", "Decompile Client Scripts", "ScreenGui", "Auto Claim (Index)", ")", "core/config", " screens", "", "[AutoPlace] slot ", "table", " ", "Frame", "core/dispatch", "core/run_job", "%$%s*([%d,%.]+)%s*(%a*)", "disabled", "getconnections", "works from anywhere - dropping the teleport", "games/my_anime_mine/auto_plot", "core/get_leaderstat", ") on ", "ClickDetector", ": ", "อ่านป้ายบน chest ทุกกี่วินาที", " items (now ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ReplicatedStorage", "core/logger", "s", "Damage", "TextLabel", "core/teleport", "core/run_job", "เดินทะลุกำแพงและพื้น", "Auto Research: ซื้อสาย Zone", "Workspace", "Auto Mine", "[Interaction] logger enabled - go click/interact with things now", " refused - ", "core/logger", "[AutoClaim] claimed ", "MekPro", "th", "Auto Plot: กันเงินไว้", "ย่อขยายเมนูทั้งชุด", " zones=", "[AntiAFK] disabled", "autoChest", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "Holders", "upgrade", "Speed", "[AutoPlace] ", ") ", "ProximityPrompt", "Auto Claim: รอบเช็ค", " node(s) looked buyable but the server refused", "MineTakeChest", "Nodes", "table", "s - carrying on without it", "autoUpgradeEquipPickaxe", "Text", "AutoSell", ")", "[Interaction] logger disabled", "", "Crew", "Modules", "Auto Mine: คลิกต่อรอบ", "Frame", "enabled", "Speed", "[AutoSell] enabled", " (x", "')", "ค้นหาฟังก์ชัน...", "AutoChest", "AutoUpgrade", "s", "[Decompile] === start ===", "Plots", "🏠", " pickaxe ", "UserInputService", "TextLabel", "[Config] couldn't write ", "BasePart", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "[AutoResearch] ", "ProximityPrompt", "[AutoResearch] Core ", "  ", "enabled", "MekPro.log", "autoClaimInterval", " = ", "LocalScript", "autoChestInterval", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "AutoChest", "GuiService", "core/scheduler", "string", "RemoteEvent", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "บิน (Fly)", "table", "GuiButton", "HumanoidRootPart", "[AutoPlot] ", "UserInputService", "Shop Stand", "CoreUI", "MineSelectZone", " (power ", "GuiButton", "games/my_anime_mine/game_api", "Auto Plot: ซื้อโซนถัดไป", ") needs ", ")", " budget=", "Auto Upgrade: รอบเช็ค", "x", " (cost ", ") didn't take, trying the next one", " power ", " (", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "#", "core/debug_dump_remotes", " x", "CharMineSpeedMaxLevel", "Dump", "[AutoPlace] upgrading ", " -> ", "autoResearchMaxPerPass", "core/get_place_id", " cost=", "ZoneRow", "enabled", "getnilinstances", "core/logger", "[Decompile] === end ===", "core/config", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "autoPlotKeepMoney", "th", "[Scheduler] ", "core/logger", "-", "Players", "languageIndex", "autoResearchZones", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "JumpPower (ปกติ 50)", "TextButton", "Players", ", built ", ",", "[Scheduler] turn: ", " error: ", "s", "Dump", "autoUpgradePickaxeIndex", "Auto Plot: รอบเช็ค", "AutoPlot", "MekPro", "string", "Hauler", "[GuiDump] === end (", ")", "", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "Plots", "HumanoidRootPart", "enabled", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "core/run_job", "", "Damage", "leaderstats", " = ", "string", "GetCharacterSpeedUpgradeCost", "[AutoResearch] ", "/s", "2026-09-21 05:07:11", "games/my_anime_mine/get_current_plot", "Frame", "games/my_anime_mine/auto_research", "games/my_anime_mine/get_current_plot", "[AutoChest] waiting: ", "autoUpgradePickaxeIndex", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "ValueLabel", "[AutoMine] ", "Damage", "[Logger] failed to write ", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", " buyCore=", "getgc", " (Text=", "\n", " '", "nothing to place or upgrade", "games/my_anime_mine/auto_upgrade", "games/my_anime_mine/game_api", "x", "games/my_anime_mine/get_best_miner", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "lowest", ",", " ", "autoPlot", "PlayerGui", "AutoMine", "[Scheduler] ", " items)", "table", "[Interaction] ClickDetector clicked: ", "[AutoUpgrade] every stat on every miner is maxed", "(", "dev", " doesn't exist", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[AutoPlot] ", "/", "MineUpgradeCharacter", "'", "core/debug_dump_gui", " -> ", "autoPlaceInterval", "ModuleScript", "core/ui", ")", " (", "[AutoPlace] disabled", "] job failed: ", "MineSpeed", "ตั้งค่า", "core/debug_interaction_logger", "games/my_anime_mine/auto_mine", "en-us", "core/debug_tab", " team power, you have ", "games/my_anime_mine/get_plot_parts", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", " '", "RunService", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "Locked miner slot", "ถูกสุดก่อน", "games/my_anime_mine/auto_claim", "getcallbackvalue", "[Config] couldn't read ", "core/teleport", "Auto Upgrade: กันเงินไว้", " ", "[AutoPlace] ", "x", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "x", "core/logger", "Speed", "autoSell", "TextLabel", ") from ", "", " ", "core/anti_afk", "", "ItemName", "HttpService", "[AutoPlace] holders: ", "core/logger", " ", "ฟาร์ม", " (", "[Decompile] === executor capabilities ===", ": ", "RunService", "UserInputService", "autoSellInterval", "", ") -> ", "ScrollingFrame", "Chest", "autoResearchCheapestFirst", "autoUpgradeRollMutation", "[AutoPlace] placed ", "core/logger", " still empty after placing ", " ", "ปุ่มเปิดเมนู", "MekPro", " ", "[AutoChest] enabled (collects once >= ", "", "Root", "ไล่อัปเกรด miner ทุกกี่วินาที", "Place", "(nothing to do)", "VirtualInputManager", "core/run_job", "Frame", "Auto Claim: เก็บสูงสุดต่อรอบ", "works from anywhere - no teleport needed", "Players", "Players", "ZoneBoardGui", "Frame", "[^%.]+", "autoChestMinItems", "s", "hookfunction", ") ", "AutoPlace", "Players", "Dump All Remotes", "เช็คโซนทุกกี่วินาที", " script(s) to ", "core/i18n", "write reported success but the line isn't in ", ")", "MineWorld", "Auto Upgrade Plot (Zone)", "cost", "Frame", "getsenv", "GuiObject", "games/my_anime_mine/game_api", "locked slot", "unreadable", "autoResearchKeepMoney", " | ", "WalkSpeed (ปกติ 16)", "", "VirtualUser", "PlayerScripts", "core/debug_tab", "Players", "Players", "PickaxesStand", "TextLabel", " while selecting", "Players", "autoResearchInterval", "Humanoid", "antiAfk", "PlotIndex", " (", "PlayerGui", "TextLabel", " reward(s) looked claimable but none went through", "Auto Place: เปลี่ยนตัวอ่อนออก", ")", "[AutoSell] disabled", "[^%w]+", "MineBuyPickaxe", "0 = ไม่จำกัด", "core/teleport", "Frame", " powerOk=", "[AutoSell] MineSellItems ", "neither appendfile nor writefile is available", "core/humanoid", "[AutoUpgrade] pickaxe #", "Workspace", "Content", "Auto Research: กันเงินไว้", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "getrenv", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "games/my_anime_mine/auto_sell", " (prompt says '", "Content", "Players", " from ", " #", "TextLabel", "autoPlotLatest", " has been busy for ", "CoreGui", "🐛", "แพงสุดก่อน", "AutoPlace", " open=", "auto", ": ", "Humanoid", "games/my_anime_mine/auto_place", " screen(s)) ===", "none", "TextLabel", "core/get_place_id", "/", "auto", "M", "getscriptbytecode", " open and unmaxed", " (x", " not bought yet (", "[AutoResearch] ", "", "Workspace", " remotes", "Mining", "Loot", ": ", "BodyVelocity", "\n", "", "games/my_anime_mine/auto_mine", "lowest", "Auto Mine: ความเร็ว", "Auto Research: ซื้อสูงสุดต่อรอบ", "[AutoUpgrade] ", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "ดีบัก", "RemoteFunction", "noclip", ") ", "[AutoUpgrade] ", "มุดแมพ (Noclip)", "ต่ำสุดก่อน", "TextButton", "%$", "ขายทุกกี่วินาที", " (", "may need us at the stand - trying that", "games/my_anime_mine/game_api", "core/logger", "Interaction Logger", "Auto Plot: ใช้โซนล่าสุดเสมอ", "table", "Auto Research: รอบเช็ค", "core/dispatch", "getloadedmodules", "core/anti_afk", "empty", "[GameAPI] couldn't load the game's modules: ", "core/debug_dump_remotes", "Anti-AFK", "[^%d%.]", " stack(s)", "core/logger", "OwnerUserId", "MineBuySkill", "[AutoPlot] couldn't switch to ", ")", "games/my_anime_mine/game_api", "M", "Auto Chest", " (", "fly", "[Decompile] ", "[BestMiner] slot ", "core/click", "disabled", "", "Search", " holder(s) - not buying more", "core/run_job", "=", " -> x", "ZoneBoard", "[AutoResearch]   Core: ", "^%$", "lowest", "UIStroke", ")", "  [attr] ", "Auto Research (Plot)", "[AutoResearch]   ", "%.0f", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "Players", "[MekPro] build ", "ProximityPrompt", "languageIndex", "getscripts", "auto", "AutoSell", ": ", "table", "s", ")", "TextButton", "debug.getproto", "search_placeholder", "debug.getupvalues", "/", "TextLabel", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "table", "games/my_anime_mine/auto_place", "GetCharacterMineSpeedUpgradeCost", "[AutoClaim] ", "empty", "PlayerGui", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "core/debug_decompile", "Auto Chest: เกณฑ์เก็บ", "Auto Place Miner", "table", "BasePart", " req=", "⚙", " -> ", "", "games/my_anime_mine/get_best_miner", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " (", "games/my_anime_mine/get_plot_parts", "^x(%d+)$", "[", "HotbarFrame", "ValueLabel", "MineSpeed", "[AutoResearch]   ", "BodyPosition", "ตามเครื่อง", " (money ", "open_menu_hint", "ProximityPrompt", "Header", "TextButton", "slot ", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "", "core/logger", "Auto Sell: รอบขาย", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Auto Upgrade: แจก Pickaxe ให้ miner", "SellStand", "menuScale", "function", "[Remotes] ", "autoUpgradePickaxe", "[AntiAFK] idle detected, simulating input", "OwnerUserId", "%.0f", "[AutoUpgrade] MineBuyPickaxe ", " didn't go through (out of stock or too expensive)", "auto", "autoPlotInterval", "Players", "BasePart", "AutoResearch", "ไล่ซื้อทุกกี่วินาที", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "[AutoChest] collected ", "  ", "games/my_anime_mine/game_api"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(625)] = function()
return function(import)
local log = import(S(146))
local Players = game:GetService(S(503))
local VirtualUser = game:GetService(S(693))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(31))
        connection = player.Idled:Connect(function()
            log(S(895))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(397))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(196)] = function()
return { Env = S(578), IsDev = true, Commit = S(168), BuiltAt = S(542) }
end
modules[S(813)] = function()
local VirtualInputManager = game:GetService(S(658))
local GuiService = game:GetService(S(453))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(232)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(496)] = function()
return function(import)
local HttpService = game:GetService(S(628))
local getPlaceId = import(S(101))
local log = import(S(618))
local ROOT_FOLDER = S(293)
local placeFolder = ROOT_FOLDER .. S(226) .. tostring(getPlaceId())
local filePath = placeFolder .. S(70)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(102) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(610) .. filePath .. S(191) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(438) .. filePath .. S(839) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(328)] = function()
return function(import)
local Players = game:GetService(S(832))
local log = import(S(229))
local CAPABILITIES = {
    S(37), S(754), S(836), S(556), S(793),
    S(367), S(609), S(684), S(727), S(210),
    S(846), S(844), S(670), S(493),
}
local OUT_FOLDER = S(207)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(667)) do
        if type(target) ~= S(840) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(634))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(811) .. name .. S(537) .. ((ok and type(value) == S(333)) and S(44) or S(112)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(261))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(694))
    if not playerScripts then
        log(S(58))
        return 0
    end
    pcall(makefolder, S(392))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(449)) or target:IsA(S(588)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(521) and #source > 0 then
                local name = target:GetFullName():gsub(S(713), S(47))
                if pcall(writefile, OUT_FOLDER .. S(847) .. name .. S(223), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(136) .. written .. S(676) .. OUT_FOLDER
        .. S(809) .. failed .. S(341))
    return written
end
local function run()
    log(S(432))
    reportCapabilities()
    local count = dumpAll()
    log(S(495))
    return count
end
return run
end
end
modules[S(12)] = function()
return function(import)
local Players = game:GetService(S(97))
local log = import(S(139))
local function dumpVisible(root, indent)
    indent = indent or S(338)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(685)) and child.Visible then
            local line = indent .. child.ClassName .. S(559) .. child.Name .. S(181)
            if child:IsA(S(87)) or child:IsA(S(336)) then
                line = line .. S(186) .. tostring(child.Text) .. S(11)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(827) .. attrName .. S(448) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(908))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(855))
    if not playerGui then
        log(S(59))
        return 0
    end
    local screens = 0
    log(S(266))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(230)) and screen.Enabled and screen.Name ~= S(650) then
            screens = screens + 1
            log(S(302) .. screen.ClassName .. S(603) .. screen.Name .. S(269))
            dumpVisible(screen, S(444))
        end
    end
    log(S(523) .. screens .. S(747))
    return screens
end
return dumpGui
end
end
modules[S(482)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(377))
local log = import(S(316))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(456)) or descendant:IsA(S(775)) then
            count = count + 1
            log(S(893) .. descendant.ClassName .. S(744) .. descendant:GetFullName())
        end
    end
    log(S(7) .. count .. S(142))
    return count
end
return dumpRemotes
end
end
modules[S(291)] = function()
return function(import)
local Players = game:GetService(S(664))
local Workspace = game:GetService(S(723))
local UserInputService = game:GetService(S(463))
local log = import(S(801))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(240) .. describe(prompt)
            .. S(245) .. prompt.ActionText .. S(679))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(575) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(43) .. describe(button)
            .. S(557) .. tostring(button.Text) .. S(524))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(406)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(372)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(460)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(417))
        return
    end
    connections = {}
    log(S(388))
    local playerGui = Players.LocalPlayer:WaitForChild(S(570))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(834)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(45)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(468)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(62),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(599)] = function()
return function(import)
local interactionLogger = import(S(596))(import)
local dumpRemotes = import(S(797))(import)
local dumpGui = import(S(585))(import)
local decompileScripts = import(S(857))(import)
local humanoid = import(S(721))
local function build(window, persist)
    local tab = window:AddTab(S(774), S(739))
    persist.Toggle(
        tab:AddCard(
            S(788),
            S(121)
        ),
        S(244),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(674),
        S(318)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(485), function()
        remoteBtn.Text = dumpRemotes() .. S(761)
    end)
    local guiCard = tab:AddCard(
        S(25),
        S(726)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(516), function()
        guiBtn.Text = dumpGui() .. S(357)
    end)
    local decompileCard = tab:AddCard(
        S(352),
        S(849)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(217), function()
        decompileBtn.Text = decompileScripts() .. S(249)
    end)
    humanoid.Start()
    persist.Slider(tab, S(115), S(691),
        8, 200, S(279), 16, humanoid.SetWalkSpeed, S(195))
    persist.Slider(tab, S(310), S(508),
        30, 300, S(86), 50, humanoid.SetJumpPower, S(255))
    persist.Toggle(
        tab:AddCard(S(458), S(280)),
        S(810), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(779), S(384)),
        S(776), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(273), S(507)),
        S(233), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(792)] = function()
return function(import)
    local getPlaceId = import(S(489))
    local registry = import(S(173))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(167))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(370)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(536))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(299)] = function()
local Players = game:GetService(S(663))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(745))
    local backpack = player:WaitForChild(S(251))
    return player, humanoid, backpack
end
return getMe
end
modules[S(750)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(119)] = function()
local Players = game:GetService(S(510))
local RunService = game:GetService(S(604))
local UserInputService = game:GetService(S(436))
local Workspace = game:GetService(S(222))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(703))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(344))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(765))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(439)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(861)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(876))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(331)] = function()
local Players = game:GetService(S(257))
local STRINGS = {
    en = {
        search_placeholder = S(326),
        open_menu_hint = S(342),
    },
    th = {
        search_placeholder = S(428),
        open_menu_hint = S(649),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(598)
    if locale:sub(1, 2):lower() == S(351) then
        return S(51)
    end
    return S(337)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(837) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(63)] = function()
local LOG_FILE = S(446)
local function log(message)
    local line = os.date(S(256)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(308))
            end
            appendfile(LOG_FILE, line .. S(558))
        elseif writefile then
            local existing = S(759)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(766))
        else
            error(S(720))
        end
        if not (isfile and readfile) then
            error(S(254))
        end
        if not isfile(LOG_FILE) then
            error(S(24) .. LOG_FILE .. S(579))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(678) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(553) .. LOG_FILE .. S(764) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(13)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(455) then
        return nil
    end
    local number, suffix = text:match(S(365))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(567), S(358))))
    if not value then
        return nil
    end
    if suffix ~= S(534) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(383)] = function()
return function(import)
local log = import(S(346))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(871) .. name .. S(593) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(153)] = function()
return function(import)
local RunService = game:GetService(S(636))
local log = import(S(378))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(500) .. task_.name .. S(514) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(178) .. task_.name .. S(737)
                    .. string.format(S(897), now - task_.busySince)
                    .. S(412))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(572) .. starved.name .. S(39)
            .. string.format(S(830), now - starved.waitingSince)
            .. S(46))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(513) .. (currentTask and currentTask.name or S(657)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(716)] = function()
local Players = game:GetService(S(701))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(528))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(303)] = function()
return function(import)
local i18n = import(S(677))
local UserInputService = game:GetService(S(637))
local THEME = {
    BrandName = S(520),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(130))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(109))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(144))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(781))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(626)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(825))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(708))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(258)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(699))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(105)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(641))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(148))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(162))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(660))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(848))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(17))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(381))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(873))
            if not valueLbl then
                valueLbl = Instance.new(S(735))
                valueLbl.Name = S(550)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(263))
            if not badge then
                badge = Instance.new(S(437))
                badge.Name = S(224)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(882))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(544))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(683))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(151))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(639)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(165)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(422))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(362))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(20))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(239)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(160))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(6))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(843))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(414)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(353))
    screenGui.Name = S(465)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(738))
    local main = Instance.new(S(163))
    main.Name = S(179)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(56))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(275))
    tabBar.Name = S(334)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(95))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(666))
    header.Name = S(881)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(509))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(177))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(128))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(692)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(349))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(335)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(246))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(502)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(218))
    searchBox.Name = S(816)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(845))
    searchBox.Text = S(885)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(288))
    content.Name = S(724)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(717))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(164))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(879))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(78))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(104)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(525) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(613) .. (item.Subtitle or S(815))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(170))
        footerLabel.Text = i18n.T(S(348))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(150)] = function()
return function(import)
local gameApi = import(S(562))(import)
local log = import(S(501))
local runGuarded = import(S(155))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(652) .. minItemsToCollect .. S(573))
    else
        log(S(8))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(688) then
            log(S(159))
            lastReported = S(79)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(795) then
            log(S(265))
            lastReported = S(854)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(72) then
            local capacity = gameApi.ChestCapacity()
            log(S(547) .. count .. S(582) .. want .. S(15)
                .. (capacity and (S(295) .. capacity .. S(85)) or S(53)))
            lastReported = S(134)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(429), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(907) .. count .. S(375) .. after .. S(50))
        else
            log(S(180) .. count
                .. S(586) .. tostring(after) .. S(842))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(60)] = function()
return function(import)
local gameApi = import(S(786))(import)
local log = import(S(886))
local runGuarded = import(S(197))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(124) .. (enabled and S(213) or S(343)))
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    return #gameApi.ClaimableIndexRewards() > 0
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(391) .. reward.category .. S(27)
                .. tostring(reward.tierKey) .. S(734) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(853) .. #pending .. S(709))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(114), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(768)] = function()
return function(import)
local getCurrentPlot = import(S(32))
local getPlotParts = import(S(601))
local log = import(S(49))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(267), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(551) .. (enabled and S(423) or S(117)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(746)] = function()
return function(import)
local Players = game:GetService(S(697))
local getCurrentPlot = import(S(546))
local getPlotParts = import(S(174))
local getMinerSlots = import(S(866))(import)
local teleport = import(S(241))
local gameApi = import(S(909))(import)
local log = import(S(19))
local runGuarded = import(S(818))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(88)
local LOCKED_SLOT_TEXT = S(606)
local upgradeWeakMiners = true
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(192))
    return pad and pad:FindFirstChildWhichIsA(S(442), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(282)) then
            local digits = descendant.Text:match(S(285))
            if digits then
                return tonumber((digits:gsub(S(200), S(865))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(34)) and descendant.Text:match(S(823)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(883) .. holder.Name .. S(757)
            .. gameApi.MinerSlots() .. S(36) .. gameApi.MaxMinerSlots() .. S(329)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(687)
    end
    if not prompt.Enabled then
        return true, S(75)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(65) .. price
    end
    local action = prompt.ActionText or S(653)
    if action:match(S(185)) or action:match(S(297)) or action:match(S(782)) then
        return true, S(30) .. action .. S(584)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(461))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(225) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(306) .. why .. S(711))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(161))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(819) .. prompt.ObjectText .. S(577) .. power .. S(805))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(690))
    if line ~= lastSurvey then
        log(S(629) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(211), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(402), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(614) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(592))
    else
        lastAttempt = 0
        log(S(884))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(531),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    gameApi.EquipSlot(miner.slot)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(656), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(21))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(182) .. holder.Name .. S(700))
            return false
        end
        log(S(359) .. candidate.slot .. S(2) .. candidate.name
            .. S(477))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(404) .. holder.Name .. S(647) .. miner.name)
        return false
    end
    log(S(645) .. miner.name .. S(3) .. miner.power
        .. S(371) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(283) .. entry.name .. S(733) .. entry.holder.Name
            .. S(730) .. tostring(entry.prompt.ActionText) .. S(427))
        return false
    end
    log(S(214) .. entry.name .. S(467) .. entry.power
        .. S(622) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(560))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(203) then
        runGuarded(S(347), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(486) .. target.holder.Name .. S(635) .. target.name
        .. S(48) .. target.power .. S(640) .. best.name .. S(184) .. best.power .. S(826))
    runGuarded(S(672), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(138)] = function()
return function(import)
local gameApi = import(S(469))(import)
local parseMoney = import(S(103))
local log = import(S(16))
local runGuarded = import(S(659))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(462) .. (enabled and S(492) or S(123)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(23)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(129) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(317) .. deepestKey .. S(590))
        return true
    end
    log(S(804) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(194) .. gameApi.ZoneDisplayName(zoneKey) .. S(301)
                .. math.floor(price) .. S(107) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(131) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(706) .. zoneKey .. S(355))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(324) then
            reportedLocked = S(83)
            log(S(581) .. zoneKey .. S(389)
                .. gameApi.ZoneDisplayName(currentKey) .. S(111)
                .. done .. S(4) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(519), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(313)] = function()
return function(import)
local gameApi = import(S(686))(import)
local log = import(S(630))
local runGuarded = import(S(294))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(441) .. (enabled and S(445) or S(366)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(193) .. node.zoneKey .. S(361) .. node.id
                    .. S(868) .. node.family .. S(671) .. node.level .. S(274) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(443) .. node.id
                    .. S(784) .. node.family .. S(204) .. node.level .. S(330) .. after)
                bought = bought + 1
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(758) .. blocked
                .. S(408))
        elseif powerBlocked then
            log(S(540) .. powerBlocked.zoneKey .. S(568) .. powerBlocked.id
                .. S(633) .. powerBlocked.family .. S(471)
                .. math.floor(powerBlocked.powerReq) .. S(600)
                .. math.floor(powerBlocked.power)
                .. S(29))
        else
            log(S(68)
                .. S(878) .. math.floor(gameApi.GetMoney()) .. S(416))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(54) .. nearest.zoneKey
                    .. S(624) .. nearest.id .. S(220) .. nearest.family .. S(777)
                    .. nearest.level .. S(277) .. nearest.max
                    .. S(309) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
function reportIdle()
    local money = gameApi.GetMoney()
    local budget = spendable()
    local zones = gameApi.UnlockedZones()
    log(S(176) .. math.floor(money)
        .. S(473) .. math.floor(budget)
        .. S(396) .. (#zones > 0 and table.concat(zones, S(512)) or S(171))
        .. S(132) .. tostring(buyZones) .. S(555) .. tostring(buyCore))
    for _, zoneKey in ipairs(zones) do
        local nodes = gameApi.SkillNodes(zoneKey)
        local pending = 0
        for _, node in ipairs(nodes) do
            if not node.maxed then
                pending = pending + 1
                if pending <= 4 then
                    log(S(875) .. zoneKey .. S(272) .. tostring(node.id)
                        .. S(591) .. tostring(node.family) .. S(405)
                        .. node.level .. S(183) .. node.max
                        .. S(742) .. tostring(node.open)
                        .. S(490) .. tostring(node.cost)
                        .. S(718) .. tostring(node.powerOk)
                        .. S(862) .. tostring(node.powerReq)
                        .. S(141) .. tostring(node.power))
                end
            end
        end
        log(S(829) .. zoneKey .. S(373) .. #nodes
            .. S(98) .. pending .. S(253))
    end
    local coreNodes = gameApi.CoreNodes()
    local coreOpen = 0
    for _, node in ipairs(coreNodes) do
        if node.open and not node.maxed then
            coreOpen = coreOpen + 1
        end
    end
    log(S(822) .. #coreNodes .. S(327)
        .. coreOpen .. S(755))
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(304), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(242)] = function()
return function(import)
local Workspace = game:GetService(S(760))
local gameApi = import(S(209))(import)
local teleport = import(S(611))
local log = import(S(235))
local runGuarded = import(S(533))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(712))
    else
        lastAttempt = 0
        log(S(425))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(890))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(880), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(719)
                .. (sold and S(368)
                         or S(145)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(284) .. (sold and (S(198) .. #keys .. S(800)) or S(42)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(838), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(137)] = function()
return function(import)
local Workspace = game:GetService(S(386))
local gameApi = import(S(806))(import)
local teleport = import(S(382))
local log = import(S(390))
local runGuarded = import(S(364))(import)
local STATS = { S(380), S(424), S(874) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(769)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(106) .. (enabled and S(529) or S(814)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(315) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(824) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(234) or S(278)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(778) .. holderKey .. S(651) .. choice.stat .. S(73)
                        .. choice.info.level .. S(487) .. after.level
                        .. S(143) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(682) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MinerSlots()
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(698))
    local shop = stand and stand:FindFirstChild(S(464))
    if not shop then
        return nil
    end
    if shop:IsA(S(55)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(903), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(898)
                .. (bought and S(662)
                           or S(785)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(549))
        end
    end
    if bought then
        log(S(238) .. pickaxe.index .. S(648) .. pickaxe.name
            .. S(426) .. pickaxe.multiplier .. S(156))
        return true
    end
    log(S(722) .. pickaxe.index .. S(215) .. pickaxe.name
        .. S(899))
    return false
end
local function equipPickaxesBestFirst()
    local pool = {}
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        for _ = 1, pickaxe.ownedCount do
            table.insert(pool, pickaxe)
        end
    end
    if #pool == 0 then
        return
    end
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    local keys = holderKeys()
    local remaining = {}
    local settled = {}
    local current = holders()
    for _, pickaxe in ipairs(pool) do
        local claimed = false
        for _, holderKey in ipairs(keys) do
            local entry = current[holderKey]
            if not settled[holderKey] and entry and entry.Pickaxe == pickaxe.name then
                settled[holderKey] = true
                claimed = true
                break
            end
        end
        if not claimed then
            table.insert(remaining, pickaxe)
        end
    end
    local nextSpare = 1
    for _, holderKey in ipairs(keys) do
        if not settled[holderKey] then
            local wanted = remaining[nextSpare]
            if not wanted then
                break
            end
            nextSpare = nextSpare + 1
            local entry = current[holderKey]
            gameApi.EquipPickaxe(holderKey, wanted.name)
            log(S(26) .. holderKey .. S(435)
                .. tostring(entry and entry.Pickaxe or S(748)) .. S(864) .. wanted.name)
            task.wait(0.3)
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(772) .. holderKey .. S(243) .. variant
                .. S(820) .. after .. S(476) .. cost .. S(33))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(236) .. gameApi.OwnedPickaxeCount()
                    .. S(140) .. gameApi.MinerSlots()
                    .. S(817))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(565))
        elseif maxed > 0 then
            log(S(576))
        else
            log(S(530))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes then
        local owned = 0
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            owned = owned + pickaxe.ownedCount
        end
        if owned > 0 then
            local wearing = 0
            for _, entry in pairs(holders()) do
                if type(entry) == S(790) and entry.Pickaxe and entry.Pickaxe ~= S(623) then
                    wearing = wearing + 1
                end
            end
            if wearing < math.min(owned, #holderKeys()) then
                return true
            end
        end
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(1), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(322)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(287))
local Workspace = game:GetService(S(219))
local log = import(S(646))
local api = {}
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(420), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(796) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(307))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(411) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(205) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(850) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(77) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(522) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(133))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(126) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(409))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(286), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(860) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(41), keys)
end
function api.EquipSlot(key)
    return send(S(312), key)
end
function api.BuyPickaxe(tierIndex)
    return send(S(714), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(292), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(320), holderKey)
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(803), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(340)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(673)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(216))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(321) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(574) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(466), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(680))
    plots = plots and plots:FindFirstChild(S(527))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(696)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(896)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(821))
    local gui = board and board:FindFirstChild(S(665))
    local root = gui and gui:FindFirstChild(S(654))
    local list = root and root:FindFirstChild(S(5))
    local row = list and list:FindFirstChild(S(491) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(749)) and descendant.Text ~= S(767) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(892) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(583), characterKey, stat == S(290) and S(149) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(113), max = S(57), cost = S(67) },
    Speed = { level = S(169), max = S(135), cost = S(539) },
    MineSpeed = { level = S(166), max = S(484), cost = S(852) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(459) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(339) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(538) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(71) and type(holder.CharName) == S(82) then
            held[holder.CharName] = true
        end
    end
    return held
end
local function indexCategoryKeys()
    local keys = {}
    local seen = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        local ok, category = pcall(indexRewards.Category, zoneKey)
        if ok and type(category) == S(360) and category.Key and not seen[category.Key] then
            seen[category.Key] = true
            table.insert(keys, category.Key)
        end
    end
    return keys
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine or indexRewards.Enabled ~= true then
        return {}
    end
    local list = {}
    pcall(function()
        local held = heldCharacterNames(mine)
        for _, category in ipairs(indexCategoryKeys()) do
            local total = tonumber(indexRewards.Total(category)) or 0
            if total > 0 then
                local state = indexRewards.State(mine, category, held)
                for _, tier in ipairs((state and state.Track) or {}) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
            end
        end
    end)
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(247) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(564)] = function()
return function(import)
local Players = game:GetService(S(902))
local log = import(S(787))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(707))
    local mineGui = playerGui and playerGui:FindFirstChild(S(311))
    local hotbar = mineGui and mineGui:FindFirstChild(S(872))
    local content = hotbar and hotbar:FindFirstChild(S(731))
    return content and content:FindFirstChild(S(259))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(621)) then
            local count = descendant.Text:match(S(870))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(76))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(122))
    local nameLabel = content and content:FindFirstChild(S(627))
    local damageLabel = content and content:FindFirstChild(S(535))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(418) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(799), S(268)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(751) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(812) .. slot .. S(9) .. info.name
                        .. S(478) .. info.power .. S(483) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(543)] = function()
local Players = game:GetService(S(732))
local Workspace = game:GetService(S(66))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(350))
    local plots = mineWorld and mineWorld:FindFirstChild(S(433))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(802)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(869)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(401)),
        Nodes = plot:FindFirstChild(S(410)),
        Chest = plot:FindFirstChild(S(642)),
        Zones = plot:FindFirstChild(S(157)),
        Crew = plot:FindFirstChild(S(419)),
        Loot = plot:FindFirstChild(S(763)),
        PlotIndex = plot:GetAttribute(S(705)),
        ChestCapacity = plot:GetAttribute(S(108)),
    }
end
return getPlotParts
end
modules[S(248)] = function()
return function(import)
    local createWindow = import(S(589))(import)
    local i18n = import(S(202))
    local antiAfk = import(S(794))(import)
    local buildEnv = import(S(332))
    local scheduler = import(S(454))(import)
    local config = import(S(356))(import)
    local log = import(S(494))
    log(S(833) .. buildEnv.Commit .. S(479) .. buildEnv.Env
        .. S(511) .. buildEnv.BuiltAt .. S(74))
    local window = createWindow({
        Subtitle = S(93),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(597))(import)
    local autoPlace = import(S(851))(import)
    local autoChest = import(S(90))(import)
    local autoSell = import(S(729))(import)
    local autoUpgrade = import(S(561))(import)
    local autoResearch = import(S(545))(import)
    local autoClaim = import(S(608))(import)
    local autoPlot = import(S(369))(import)
    local gameApi = import(S(188))(import)
    scheduler.RegisterStandalone(S(571), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(452), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(741), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(430), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(904), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(91), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(40), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(84), 20)
    scheduler.RegisterExclusive(S(415), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(632), S(434))
    persistentToggle(farmTab:AddCard(
        S(387),
        S(831)
    ), S(10), autoMine.SetEnabled)
    persistentSlider(farmTab, S(770), S(605),
        1, 30, S(61), 5, autoMine.SetCyclesPerSecond, S(541))
    persistentSlider(farmTab, S(421), S(99),
        1, 10, S(38), 1, autoMine.SetClicksPerCycle, S(475))
    persistentToggle(farmTab:AddCard(
        S(859),
        S(270)
    ), S(305), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(69), S(906),
        1, 60, S(587), 3, autoPlace.SetCheckInterval, S(190))
    persistentToggle(
        farmTab:AddCard(S(710),
            S(206)),
        S(94), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(farmTab:AddCard(
        S(808),
        S(554)
    ), S(398), autoChest.SetEnabled)
    persistentSlider(farmTab, S(858), S(152),
        50, 3000, S(668), 100, autoChest.SetMinItems, S(22))
    persistentSlider(farmTab, S(125), S(374),
        1, 60, S(450), 3, autoChest.SetCheckInterval, S(669))
    persistentToggle(farmTab:AddCard(
        S(281),
        S(221)
    ), S(620), autoSell.SetEnabled)
    persistentSlider(farmTab, S(887), S(783),
        5, 300, S(638), 20, autoSell.SetCheckInterval, S(515))
    farmTab:AddSlider(S(252),
        S(345),
        5, 120, sellMaxWait, function(value)
            config.Set(S(100), value)
        end, S(431))
    persistentToggle(farmTab:AddCard(
        S(298),
        S(616)
    ), S(189), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(474), S(655),
        5, 300, S(116), 20, autoUpgrade.SetInterval, S(325))
    persistentSlider(farmTab, S(856), S(580),
        1, 20, S(187), 5, autoUpgrade.SetMaxPerPass, S(617))
    persistentSegmented(
        farmTab,
        { S(780), S(208), S(619), S(762) },
        { S(566), S(552), S(403), S(594) },
        S(80), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(612), S(64),
        0, 500, S(237), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(753))
    persistentToggle(
        farmTab:AddCard(S(172),
            S(506)),
        S(894), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(756) .. pickaxe.multiplier .. S(472)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(497)
            end
            return S(481) .. index .. S(631) .. (pickaxeLabels[index] or S(532))
        end
        local savedPick = config.Get(S(517), 0)
        pickaxeCard = farmTab:AddSlider(
            S(127),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(548), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(271)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(399),
            S(300)),
        S(28), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(889),
            S(526)),
        S(413), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(92),
            S(888)),
        S(644), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(120), S(400),
        1, 20, S(276), 2, autoUpgrade.SetMutationTarget, S(175))
    persistentSlider(farmTab, S(52), S(715),
        0, 500, S(212), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(807))
    persistentToggle(farmTab:AddCard(
        S(828),
        S(728)
    ), S(289), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(791), S(905),
        10, 600, S(702), 30, autoResearch.SetCheckInterval, S(228))
    persistentSlider(farmTab, S(771), S(376),
        1, 20, S(488), 5, autoResearch.SetMaxPerPass, S(563))
    persistentSlider(farmTab, S(725), S(296),
        0, 500, S(689), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(158))
    persistentToggle(
        farmTab:AddCard(S(385),
            S(89)),
        S(505), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(118),
            S(201)),
        S(262), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(602),
            S(773)),
        S(96), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(607), S(740) },
        { true, false },
        S(643), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(354),
        S(199)
    ), S(319), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(407), S(440),
        5, 300, S(447), 30, autoClaim.SetCheckInterval, S(379))
    persistentSlider(farmTab, S(661), S(867),
        1, 30, S(227), 10, autoClaim.SetMaxPerPass, S(615))
    persistentToggle(farmTab:AddCard(
        S(681),
        S(480)
    ), S(569), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(518), S(675),
        5, 300, S(901), 30, autoPlot.SetCheckInterval, S(841))
    persistentToggle(
        farmTab:AddCard(S(470),
            S(451)),
        S(264), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(789),
            S(323)),
        S(736), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(394), S(457),
        0, 500, S(498), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(18))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(695))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(595), S(863))
    persistentToggle(
        settingsTab:AddCard(S(798), S(35)),
        S(704),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(314), 100)
    settingsTab:AddSlider(S(81), S(395), 50, 150, savedScale, function(value)
        config.Set(S(891), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(504), 1)
    settingsTab:AddSegmented({ S(877), S(147), S(260) }, savedLanguage, function(index, value)
        local code = ({ S(743), S(110), S(499) })[index]
        config.Set(S(835), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(900), S(250), S(393) })[savedLanguage] or S(752))
    log(S(231) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(14)] = function()
return {
    [79389059854988] = S(154),
}
end
import(S(363))(import)
