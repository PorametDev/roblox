-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"", "Auto Upgrade: กันเงินไว้", ": State(", " research is ", "This is not a supported place.", "disabled", "", "[AutoUpgrade] bought pickaxe #", "/", "ไทย", "needs ", "core/logger", "/", "[AutoPlace] unlocked ", "Damage", "en-us", "getscriptbytecode", "CoreGui", "Auto Chest: รอบเช็ค", "UICorner", "x", "[AutoUpgrade] pickaxe #", " (", "TabBar", "autoUpgradeInterval", "[AutoUpgrade] ", "List", "'", "ChestCapacity", " script(s) to ", "[AutoPlace] couldn't pick up ", "disabled", "TextLabel", "[Bb]uy", "core/ui", "HumanoidRootPart", "MekPro", "table", "getconnections", "Damage", " still locked after paying ", "\n", "core/logger", " (", "TextLabel", "MineBuyPickaxe", "Auto Mine: คลิกต่อรอบ", "[AntiAFK] disabled", " pickaxe ", "[Decompile] decompile unavailable in this executor", "Auto Research (Plot)", "Speed", "Auto Chest: เกณฑ์เก็บ", "core/humanoid", "HumanoidRootPart", "Holders", "Auto Sell", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "UIStroke", "RunService", "x", "  [attr] ", "core/logger", "Auto Upgrade: สุ่ม Mutation", "action '", "", "modules not loaded", "games/my_anime_mine/game_api", "games/my_anime_mine/auto_sell", "table", "jumpPower", "fill", ")", "[", "getscripts", "ProximityPrompt", "[AutoClaim] claimed ", "table", "Players", "Plots", "core/teleport", "antiAfk", "TextLabel", "", "games/my_anime_mine/auto_plot", ")", " waited ", "[Logger] failed to write ", "", "/", "Root", "[AutoChest] MineTakeChest had no effect (", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/auto_mine", "en", "core/get_me", "table", "Zone buying is off", "Auto Mine", "Humanoid", "games/my_anime_mine/auto_claim", "MineEquipSlot", "UserInputService", "autoPlotKeepMoney", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "GuiButton", "unreadable", "TextLabel", "%$", "games/my_anime_mine/auto_chest", "table", "games/my_anime_mine/game_api", "Anti-AFK", "Auto Research: รอบเช็ค", "games/my_anime_mine/game_api", "string", "บิน (Fly)", "server didn't equip it", "AutoSell", "[AutoClaim] ", "[AutoChest] can't read the chest yet", "PlayerGui", "Frame", "autoPlaceUpgradeWeak", "^x(%d+)$", "CharSpeedMaxLevel", "auto", " -> ", "[AutoPlace] couldn't hold anything - not pressing E", "s", " ", "core/debug_dump_remotes", ") needs ", "#", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", "=empty", "Frame", " costs ", "enabled", "ตามเครื่อง", "", " items", "autoUpgradeMutationTarget", "Dump", "Limited", "Backpack", "Auto Mine: ความเร็ว", "write reported success but the line isn't in ", "Players", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "games/my_anime_mine/auto_place", "games/my_anime_mine/main", "[AutoChest] chest is empty", " -> x", "Speed", "%", "Locked miner slot", "core/logger", "games/my_anime_mine/auto_chest", "slot past the usable cap", "autoUpgradeLimitPickaxes", "ClickDetector", "core/run_job", "ฟาร์ม", "core/ui", "เช็คโซนทุกกี่วินาที", "[AutoPlot] now mining ", "Damage", "autoPlaceUnlockBudget", "games/my_anime_mine/auto_research", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "core/i18n", "[AutoUpgrade] MineBuyPickaxe ", "TextButton", "[AutoMine] ", "core/logger", "Hauler", "ReplicatedStorage", "Workspace", "ClaimableTiers failed", "", "games/registry", ",", "ValueLabel", "ItemFrame", "Text", ")", ": ", "core/config", "Mining", "core/build_env", "[Decompile] ", "ขนาดเมนู", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "ย่อขยายเมนูทั้งชุด", "JumpPower (ปกติ 50)", "TextLabel", " (cost ", "Crew", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "แรงกระโดด", " -> ", "AutoPlace", "table", "%$%s*([%d,%.]+)%s*(%a*)", "function", " reward(s) looked claimable but none went through", "Auto Research: ซื้อสาย Zone", "search_placeholder", "autoSellMaxWait", "Players", "", "[AutoPlace] placed ", "games/my_anime_mine/get_current_plot", "Auto Chest", ")", "autoResearch", "GuiService", " - upgrade the miners first", "core/teleport", "none", "", "affordable - waiting for a turn", "TextButton", "core/debug_tab", "Modules", "", "core/logger", " (", "autoChest", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "BasePart", "[AutoPlace] ", "Humanoid", "TextLabel", "Badge", "autoUpgradePriority", "disabled", ") ", "function", "MineBuySkill", " ", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "auto", "Interaction Logger", "table", "autoSell", "[AutoResearch] Core ", "[Scheduler] turn: ", "[Config] couldn't read ", "[AutoResearch] ", "autoUpgradeKeepMoney", "core/logger", "games/my_anime_mine/get_plot_parts", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", " (x", "[AutoPlace] unlocking ", " ชิ้น", "empty", "auto", "[AutoClaim] idle: ", "Open menu", "enabled", " didn't go through (out of stock or too expensive)", "/", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", " (Text=", "TextButton", "string", "Exclusive", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "TextLabel", "en", ")", "Auto Upgrade: แจก Pickaxe ให้ miner", " screens", ": ", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "ValueLabel", " (power ", "core/get_leaderstat", "[AutoPlot] ", ", built ", "GuiButton", "[AutoResearch] nothing open and affordable right now", "th", "HotbarFrame", "BasePart", "unlock", "core/logger", "%.3g", "cheapest open node ", "TextLabel", "ขายทุกกี่วินาที", "cost", "autoClaimInterval", "waiting", "games/my_anime_mine/game_api", "number", " not bought yet (", "GetHolderMineSpeedLevel", "[AutoUpgrade] ", "[Decompile] wrote ", " claimable but none could be resolved", "[Interaction] Button activated: ", "Players", " -> ", "[AutoSell] disabled", "ProximityPrompt", "slot not in the hotbar", "Workspace", "')", "HttpService", ")", "core/debug_dump_remotes", "[Decompile] === executor capabilities ===", "s", ")", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "0 = ไม่จำกัด", "Auto Research: ซื้อสูงสุดต่อรอบ", "BasePart", "s", "PickaxesStand", "[AutoPlace] upgrading ", "[Interaction] ClickDetector clicked: ", " ", "games/registry", "[Interaction] ProximityPrompt triggered: ", "core/logger", "MinePower", " power ", "TextLabel", "-", "core/logger", "core/logger", "core/run_job", "Workspace", " (Text=", "Damage", ") from ", "no", "Auto Place: เปลี่ยนตัวอ่อนออก", "table", " but no unclaimed reward in its Track", "core/click", " ===", "/", "Frame", "core/run_job", "dev", ")", "[AutoSell] enabled", "TextBox", "TextLabel", " error: ", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "ProximityPrompt", "[AutoPlace] holders: ", "TextButton", "no isfile/readfile available to verify the write", "TextButton", "Frame", "GetCharacterSpeedUpgradeCost", "^([%d,]+)$", "VirtualUser", "games/my_anime_mine/game_api", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "Auto Upgrade Plot (Zone)", "string", "[Remotes] ", " = ", ")", "hookfunction", "lowest", " = ", "Players", "enabled", "s", "RemoteEvent", "nothing to place or upgrade", " doesn't exist", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "ถูกสุดก่อน", ") ", "ลอย (Float)", " (ActionText=", "", "MineUpgradeCharacter", "needs ", "TextLabel", "lowest", "table", "th", "getcallbackvalue", "Dump", "", "Frame", " pickaxe(s) for ", "มุดแมพ (Noclip)", "disabled", "Plots", "Auto Research: ซื้อสาย Core", "[AutoPlace] nothing in the hotbar to place", "autoResearchKeepMoney", "[AutoPlace] ", "[AutoResearch] ", "string", " '", "ตั้งค่า", "[GameAPI] SendEvent failed: ", "MineSelectZone", "games/my_anime_mine/get_best_miner", "core/debug_interaction_logger", "table", " (still ", "[AutoUpgrade] every stat on every miner is maxed", "[BestMiner] slot ", "2026-09-21 12:42:30", "autoChestInterval", " (", "core/run_job", ")", "cost", "s", "enabled", " costs ", "TextButton", "[Scheduler] ", " -> ", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "core/anti_afk", "games/my_anime_mine/auto_place", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "\n", "Players", "sale didn't go through", "_", "upgrade", " ", " screen(s)) ===", "VirtualInputManager", "", ": ", " for ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "Auto Place Miner", "games/my_anime_mine/auto_sell", "ScreenGui", "Auto Claim: เก็บสูงสุดต่อรอบ", ",", "float", "disabled", "both Zone and Core buying are switched off", "[Remotes] === total: ", "/config.json", "Workspace", "LayerCollector", "ItemName", "games/my_anime_mine/main", " failed)", "HumanoidRootPart", "Decompile Client Scripts", "Search", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "string", "ProximityPrompt", "AutoResearch", "autoUpgradePickaxe", " items (now ", "TextButton", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "debug.getconstants", "every open node is maxed", " (", "core/debug_decompile", "fly", ")", "MineSpeed", ")", "core/build_env", "[AutoPlace] fireproximityprompt error: ", "games/my_anime_mine/get_best_miner", "GetHolderSpeedLevel", " ", "UIListLayout", "%.3g", "Auto Plot: กันเงินไว้", "nothing claimable", "ReplicatedStorage", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "[^%.]+", "autoUpgradeRollMutation", "MekPro", "AutoMine", "autoSellInterval", "walkSpeed", " (", "autoPlaceUnlock", "core/parse_money", "games/my_anime_mine/auto_upgrade", "Run", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "[AutoPlot] ", "Auto Plot: ซื้อโซนถัดไป", "s - jumping the queue", "search_placeholder", "AutoUpgrade", "autoMine", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "PlayerGui", "core/logger", "", " for ", "[AutoResearch] ", "ปุ่มเปิดเมนู", "ความเร็ววิ่ง", ") ", "Chest", " '", "core/anti_afk", "table", "Core", "core/parse_money", ") ", "autoPlotLatest", "menuScale", "BodyVelocity", "M", "[AutoPlot] ", "", "ScrollingFrame", "", "Auto Plot: ใช้โซนล่าสุดเสมอ", "autoResearchMaxPerPass", "Frame", "PlayerScripts", "Auto Claim (Index)", "leaderstats", "autoClaimMaxPerPass", ") didn't take: ", " from ", "Frame", "AutoPlot", "s - carrying on without it", "ดีบัก", "games/my_anime_mine/auto_plot", " (", "neither appendfile nor writefile is available", "[GuiDump] ", "Core buying is off", "ไล่ซื้อทุกกี่วินาที", "Auto Plot: รอบเช็ค", "TextLabel", "s", "s", "/s", "/", "Content", "Dump Visible GUI", "open_menu_hint", "[MekPro] settings file: ", ")", "⚙", "PlayerGui", " (", "", "UIListLayout", "[AutoChest] collected ", "TextButton", " ", "unreadable", "autoResearchInterval", "TextButton", " files", "AutoSell", " refused - ", "GetHolderLevel", "Character", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "[%X] ", " - ", "Auto Research: กันเงินไว้", "PlotIndex", "core/logger", "getrenv", "autoSellMaxWait", "index rewards are disabled in this game build", "M", ",", ": ", "MekPro.log", "AutoPlace", "([%d,%.]+)%s*(%a*)", "อ่านป้ายบน chest ทุกกี่วินาที", "noclip", "works from anywhere - no teleport needed", "[AutoPlace] slot ", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "?", "[AutoUpgrade] ", "/", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "[GuiDump] === end (", "My Anime Mine · Mining Hub", "Frame", "=locked(", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "nothing left to buy (", " (money ", "[AutoUpgrade] ", "AutoChest", "Speed", "Container", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "; ", "AutoPlot", "Auto Claim: รอบเช็ค", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", ", ", " mutation x", " node(s) looked buyable but the server refused", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "[AutoPlace] ", "MekPro", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "[AutoClaim] ", "button has no size on screen", "autoPlotBuy", "/", "table", "write reported success but ", "  ", "core/dispatch", "core/dispatch", "'", " team power, you have ", "fill", "ไล่อัปเกรด miner ทุกกี่วินาที", " (x", "MineWorld", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "core/parse_money", "[AutoChest] enabled (collects once >= ", ")", " ", " ", "locked slot", "TextLabel", "getloadedmodules", "MineSellItems", "autoResearchZones", "ModuleScript", "Text", "Mutation: เพดานราคาต่อครั้ง", "Empty miner slot", "%$", "[AutoResearch] idle: ", "autoPlotInterval", "[AutoResearch] cheapest open node: ", "languageIndex", "LocalScript", "autoUpgradePickaxeIndex", "prompt disabled", "", "core/run_job", "  ", "Header", "SellStand", " (chest holds ", "games/my_anime_mine/auto_claim", "Hauler", "waiting", "core/logger", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "slot ", " (", "AutoChest", "MineTakeChest", "MekPro/decompiled", "[AutoUpgrade] ", " wouldn't take ", "AutoPlace", "Frame", "[AutoPlot] couldn't switch to ", ")", "auto", "Shop Stand", "debug.getupvalues", "RunService", "CoreUI", "MineWorld", "games/my_anime_mine/game_api", "[MekPro] build ", "Auto Upgrade: รอบเช็ค", "lowest", "research", "core/scheduler", ", you have ", "core/i18n", "GuiObject", "M", "core/config", "core/debug_decompile", "games/my_anime_mine/get_plot_parts", "[AutoChest] waiting: ", "menuScale", "autoPlaceInterval", "[AutoSell] ", "unlock", "Frame", "ZoneBoardGui", "autoResearchCheapestFirst", "x", "core/logger", "getgc", "[Decompile] === end ===", " ", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "interactionLogger", "[Decompile] PlayerScripts not found", "core/debug_interaction_logger", " items)", "no mine data", " team power", "", "MineEquipPickaxe", "BasePart", "[GameAPI] couldn't load the game's modules: ", "[Decompile] === start ===", "Loot", ")", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "games/my_anime_mine/game_api", " (power ", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "s", "x", " possible holder(s) - not buying more", "core/logger", "games/my_anime_mine/auto_research", " -> ", "[Interaction] logger enabled - go click/interact with things now", "autoResearchCoreLast", "works from anywhere - dropping the teleport", ", base ", "core/scheduler", "GetCharacterUpgradeCost", "Players", "Pad", "research", "อังกฤษ", "MineRollMutation", "table", "MineGui", "Auto Sell: รอบขาย", "Dump All Remotes", " | ", "[Uu]nlock", "autoMineClicks", "core/logger", " has been busy for ", "M", "^%$", "OwnerUserId", "core/i18n", " (", "🏠", "แพงสุดก่อน", "table", "CharMaxLevel", "core/logger", "UICorner", "", "", "M", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", ")", "languageIndex", "autoUpgradeMutationBudget", "games/my_anime_mine/game_api", "[Config] couldn't write ", "open_menu_hint", "games/my_anime_mine/game_api", "All", "getsenv", "UIPadding", "Auto Place: เพดานราคาปลดช่อง", "table", " (", "ค้นหาฟังก์ชัน...", "costs ", "[^%w]+", "Badge", "GetCharacterMineSpeedUpgradeCost", " #", "none", "autoUpgradeMaxPerPass", "badge says ", "autoClaim", "x", "BodyPosition", "Content", "", "Players", "autoUpgrade", "autoPlot", "[Scheduler] ", "Auto Place: ปลดล็อกช่อง miner", "TextLabel", "th", "", "[GuiDump] === visible PlayerGui tree ===", "th", "Mining", "(", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", "ClickDetector", "AutoClaim", "[Interaction] logger disabled", ") -> ", "[AntiAFK] idle detected, simulating input", "Frame", "function", "(nothing to do)", "core/logger", ", have ", "MineSpeed", "core/run_job", "[AutoPlace] picked up ", "[AutoChest] disabled", "core/humanoid", "%.0f", " ", "autoUpgradePickaxeIndex", "table", "AutoClaim", "string", "RemoteFunction", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ".lua", "core/get_place_id", "", "Players", " stack(s)", "0 = ไม่จำกัด", "[GuiDump] PlayerGui not found", "games/my_anime_mine/get_plot_parts", "OwnerUserId", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "may need us at the stand - trying that", "sold ", "=", "autoSellProtectUnits", ": ", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "Nodes", " remotes", "Players", "", "Auto Upgrade Unit", "core/teleport", "table", "autoChestMinItems", "core/get_place_id", "Search functions...", "getnilinstances", " still empty after placing ", "UserInputService", "ZoneRow", "core/run_job", "games/my_anime_mine/auto_mine", "decompile", " x", ": badge says ", "Zones", "'", "Auto Upgrade: Pickaxe ที่จะซื้อ", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "[AutoPlace] drifted out of range of ", "[AutoSell] MineSellItems ", "autoUpgradeEquipPickaxe", "MekPro", "X", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "MineSpeed", "Players", "5571fad", "[Scheduler] ", " owned)", "debug.getproto", "Mutation: หยุดเมื่อได้ x", "empty", "WalkSpeed (ปกติ 16)", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", ": ", "", "en", " while selecting", " ", "[Interaction] Key pressed: %s (gameProcessed=%s)", "games/my_anime_mine/auto_upgrade", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "TextLabel", "core/debug_dump_gui", "core/debug_tab", "UIListLayout", "TextLabel", " (", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", ") on ", "[AutoUpgrade] ", "Main", "ClickDetector", "ต่ำสุดก่อน", "%.3g", "[AutoPlace] disabled", "🐛", "Workspace", "^M(%d+)$", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "AutoUpgrade", "core/run_job", "TextLabel", "Players", "UIScale", "[AutoPlot] unlocked ", " (prompt says '", "AutoPlace", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "Auto Sell: รอคิวได้นานสุด", "needs us at the stand - keeping the teleport", "autoPlace", "autoMineSpeed", "Workspace", "", "AutoResearch", "[AntiAFK] enabled - watching Player.Idled", "เดินทะลุกำแพงและพื้น", "core/debug_dump_gui", "CharMineSpeedMaxLevel", "Auto Place: รอบเช็ค", "[AutoResearch] ", "core/logger", "enabled", "autoResearchCore", "Players", ") gave nothing", "core/teleport", "core/get_place_id", "yes", "Content", "maxed", "games/my_anime_mine/get_current_plot", "%.0f", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "ZoneBoard", "UserInputService", "games/my_anime_mine/game_api", "] job failed: ", ")"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(432)] = function()
return function(import)
local log = import(S(336))
local Players = game:GetService(S(436))
local VirtualUser = game:GetService(S(366))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(939))
        connection = player.Idled:Connect(function()
            log(S(823))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(48))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(191)] = function()
return { Env = S(351), IsDev = true, Commit = S(889), BuiltAt = S(419) }
end
modules[S(346)] = function()
local VirtualInputManager = game:GetService(S(442))
local GuiService = game:GetService(S(218))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(629)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(704)] = function()
return function(import)
local HttpService = game:GetService(S(313))
local getPlaceId = import(S(843))
local log = import(S(945))
local ROOT_FOLDER = S(494)
local placeFolder = ROOT_FOLDER .. S(631) .. tostring(getPlaceId())
local filePath = placeFolder .. S(456)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(246) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(250) .. filePath .. S(444) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(783) .. filePath .. S(591) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(476)] = function()
return function(import)
local Players = game:GetService(S(149))
local log = import(S(773))
local CAPABILITIES = {
    S(874), S(17), S(75), S(717), S(651),
    S(39), S(395), S(787), S(586), S(473),
    S(690), S(892), S(374), S(868),
}
local OUT_FOLDER = S(681)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(492)) do
        if type(target) ~= S(111) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(316))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(192) .. name .. S(372) .. ((ok and type(value) == S(825)) and S(952) or S(342)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(50))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(537))
    if not playerScripts then
        log(S(722))
        return 0
    end
    pcall(makefolder, S(37))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(663)) or target:IsA(S(654)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(269) and #source > 0 then
                local name = target:GetFullName():gsub(S(794), S(438))
                if pcall(writefile, OUT_FOLDER .. S(602) .. name .. S(842), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(303) .. written .. S(30) .. OUT_FOLDER
        .. S(498) .. failed .. S(461))
    return written
end
local function run()
    log(S(731))
    reportCapabilities()
    local count = dumpAll()
    log(S(718))
    return count
end
return run
end
end
modules[S(906)] = function()
return function(import)
local Players = game:GetService(S(888))
local log = import(S(330))
local function dumpVisible(root, indent)
    indent = indent or S(227)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(702)) and child.Visible then
            local line = indent .. child.ClassName .. S(409) .. child.Name .. S(637)
            if child:IsA(S(909)) or child:IsA(S(570)) then
                line = line .. S(339) .. tostring(child.Text) .. S(478)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(62) .. attrName .. S(376) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(668))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(565))
    if not playerGui then
        log(S(848))
        return 0
    end
    local screens = 0
    log(S(814))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(458)) and screen.Enabled and screen.Name ~= S(884) then
            screens = screens + 1
            log(S(550) .. screen.ClassName .. S(520) .. screen.Name .. S(878))
            dumpVisible(screen, S(634))
        end
    end
    log(S(605) .. screens .. S(441))
    return screens
end
return dumpGui
end
end
modules[S(315)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(178))
local log = import(S(176))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(380)) or descendant:IsA(S(840)) then
            count = count + 1
            log(S(371) .. descendant.ClassName .. S(277) .. descendant:GetFullName())
        end
    end
    log(S(455) .. count .. S(347))
    return count
end
return dumpRemotes
end
end
modules[S(723)] = function()
return function(import)
local Players = game:GetService(S(845))
local Workspace = game:GetService(S(920))
local UserInputService = game:GetService(S(959))
local log = import(S(675))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(329) .. describe(prompt)
            .. S(387) .. prompt.ActionText .. S(216))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(326) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(305) .. describe(button)
            .. S(267) .. tostring(button.Text) .. S(423))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(76)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(162)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(106)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(821))
        return
    end
    connections = {}
    log(S(744))
    local playerGui = Players.LocalPlayer:WaitForChild(S(122))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(467)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(819)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(284)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(902),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(907)] = function()
return function(import)
local interactionLogger = import(S(414))(import)
local dumpRemotes = import(S(132))(import)
local dumpGui = import(S(941))(import)
local decompileScripts = import(S(705))(import)
local humanoid = import(S(54))
local function build(window, persist)
    local tab = window:AddTab(S(546), S(919))
    persist.Toggle(
        tab:AddCard(
            S(245),
            S(105)
        ),
        S(721),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(758),
        S(676)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(396), function()
        remoteBtn.Text = dumpRemotes() .. S(859)
    end)
    local guiCard = tab:AddCard(
        S(560),
        S(880)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(144), function()
        guiBtn.Text = dumpGui() .. S(276)
    end)
    local decompileCard = tab:AddCard(
        S(463),
        S(271)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(502), function()
        decompileBtn.Text = decompileScripts() .. S(575)
    end)
    humanoid.Start()
    persist.Slider(tab, S(517), S(895),
        8, 200, S(497), 16, humanoid.SetWalkSpeed, S(531))
    persist.Slider(tab, S(201), S(196),
        30, 300, S(71), 50, humanoid.SetJumpPower, S(7))
    persist.Toggle(
        tab:AddCard(S(117), S(604)),
        S(477), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(400), S(940)),
        S(596), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(386), S(643)),
        S(452), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(635)] = function()
return function(import)
    local getPlaceId = import(S(951))
    local registry = import(S(328))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(5))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(281)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(539))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(96)] = function()
local Players = game:GetService(S(926))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(100))
    local backpack = player:WaitForChild(S(146))
    return player, humanoid, backpack
end
return getMe
end
modules[S(866)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(833)] = function()
local Players = game:GetService(S(948))
local RunService = game:GetService(S(60))
local UserInputService = game:GetService(S(103))
local Workspace = game:GetService(S(936))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(234))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(462))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(528))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(288)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(322)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(803))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(767)] = function()
local Players = game:GetService(S(377))
local STRINGS = {
    en = {
        search_placeholder = S(867),
        open_menu_hint = S(262),
    },
    th = {
        search_placeholder = S(792),
        open_menu_hint = S(516),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(16)
    if locale:sub(1, 2):lower() == S(812) then
        return S(394)
    end
    return S(95)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(127) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(43)] = function()
local LOG_FILE = S(592)
local function log(message)
    local line = os.date(S(581)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(813))
            end
            appendfile(LOG_FILE, line .. S(42))
        elseif writefile then
            local existing = S(727)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(435))
        else
            error(S(549))
        end
        if not (isfile and readfile) then
            error(S(361))
        end
        if not isfile(LOG_FILE) then
            error(S(633) .. LOG_FILE .. S(382))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(148) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(88) .. LOG_FILE .. S(188) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(524)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(116) then
        return nil
    end
    local number, suffix = text:match(S(205))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(451), S(567))))
    if not value then
        return nil
    end
    if suffix ~= S(775) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(830)] = function()
return function(import)
local log = import(S(762))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(74) .. name .. S(961) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(748)] = function()
return function(import)
local RunService = game:GetService(S(691))
local log = import(S(741))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(809) .. task_.name .. S(356) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(890) .. task_.name .. S(763)
                    .. string.format(S(834), now - task_.busySince)
                    .. S(545))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(429) .. starved.name .. S(87)
            .. string.format(S(956), now - starved.waitingSince)
            .. S(506))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(249) .. (currentTask and currentTask.name or S(826)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(950)] = function()
local Players = game:GetService(S(306))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(55))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(165)] = function()
return function(import)
local i18n = import(S(172))
local UserInputService = game:GetService(S(870))
local THEME = {
    BrandName = S(626),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(20))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(774))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(788))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(471))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(861)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(59))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(925))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(600)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(83))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(898)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(532))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(486))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(333))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(607))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(905))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(811))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(197))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(184))
            if not valueLbl then
                valueLbl = Instance.new(S(272))
                valueLbl.Name = S(279)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(236))
            if not badge then
                badge = Instance.new(S(650))
                badge.Name = S(795)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(224))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(543))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(824))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(362))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(181)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(156)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(536))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(712))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(174))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(533)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(398))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(908))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(428))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(655)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(449))
    screenGui.Name = S(692)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(18))
    local main = Instance.new(S(349))
    main.Name = S(914)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(927))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(137))
    tabBar.Name = S(24)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(568))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(685))
    header.Name = S(669)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(360))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(355))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(554))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(937)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(574))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(885)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(268))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(334)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(354))
    searchBox.Name = S(464)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(507))
    searchBox.Text = S(443)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(123))
    content.Name = S(804)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(363))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(108))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(561))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(235))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(186)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(666) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(719) .. (item.Subtitle or S(776))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(209))
        footerLabel.Text = i18n.T(S(784))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(110)] = function()
return function(import)
local gameApi = import(S(785))(import)
local log = import(S(335))
local runGuarded = import(S(337))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(645) .. minItemsToCollect .. S(724))
    else
        log(S(832))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(107) then
            log(S(121))
            lastReported = S(572)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(259) then
            log(S(153))
            lastReported = S(894)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(297) then
            local capacity = gameApi.ChestCapacity()
            log(S(707) .. count .. S(265) .. want .. S(142)
                .. (capacity and (S(671) .. capacity .. S(962)) or S(397)))
            lastReported = S(674)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(613), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(569) .. count .. S(470) .. after .. S(73))
        else
            log(S(92) .. count
                .. S(307) .. tostring(after) .. S(187))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(101)] = function()
return function(import)
local gameApi = import(S(367))(import)
local log = import(S(290))
local runGuarded = import(S(667))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(120) .. (enabled and S(263) or S(32)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(261) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(77) .. reward.category .. S(648)
                .. tostring(reward.tierKey) .. S(797) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(628) .. #pending .. S(207))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(820), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(873)] = function()
return function(import)
local getCurrentPlot = import(S(214))
local getPlotParts = import(S(849))
local log = import(S(512))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(915), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(175) .. (enabled and S(426) or S(238)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(151)] = function()
return function(import)
local Players = game:GetService(S(211))
local getCurrentPlot = import(S(955))
local getPlotParts = import(S(706))
local getMinerSlots = import(S(413))(import)
local teleport = import(S(81))
local parseMoney = import(S(644))
local gameApi = import(S(112))(import)
local log = import(S(253))
local runGuarded = import(S(350))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(657)
local LOCKED_SLOT_TEXT = S(157)
local upgradeWeakMiners = true
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(751))
    return pad and pad:FindFirstChildWhichIsA(S(358), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(33)) then
            local digits = descendant.Text:match(S(365))
            if digits then
                return tonumber((digits:gsub(S(183), S(212))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(293)) and descendant.Text:match(S(765)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(677) .. holder.Name .. S(300)
            .. gameApi.MinerSlots() .. S(558) .. gameApi.MaxMinerSlots() .. S(891)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(649)
    end
    if not prompt.Enabled then
        return true, S(665)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(793) .. price
    end
    local action = prompt.ActionText or S(388)
    if action:match(S(760)) or action:match(S(34)) or action:match(S(658)) then
        return true, S(65) .. action .. S(28)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(36))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(482) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(608) .. why .. S(733))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(136))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(854) .. prompt.ObjectText
                    .. S(817) .. shown .. S(747) .. math.floor(base) .. S(646))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(759))
    if line ~= lastSurvey then
        log(S(359) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(639), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(439), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(289), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(233) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(918))
    else
        lastAttempt = 0
        log(S(580))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(624),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(310)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(160)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(118)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(404))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(881) .. holder.Name .. S(900))
            return false
        end
        log(S(598) .. candidate.slot .. S(44) .. candidate.name
            .. S(541) .. tostring(why))
    end
    if not miner then
        log(S(129))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(406) .. holder.Name .. S(869) .. miner.name)
        return false
    end
    log(S(213) .. miner.name .. S(736) .. miner.power
        .. S(912) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(31) .. entry.name .. S(542) .. entry.holder.Name
            .. S(929) .. tostring(entry.prompt.ActionText) .. S(312))
        return false
    end
    log(S(831) .. entry.name .. S(280) .. entry.power
        .. S(341) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(625) .. pad.holder.Name .. S(41)
                .. tostring(pad.priceText))
        else
            log(S(14) .. pad.holder.Name
                .. S(514) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(381))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(72) then
        runGuarded(S(593), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(711) then
        log(S(257) .. target.holder.Name
            .. S(445) .. tostring(target.priceText))
        runGuarded(S(930), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(325) .. target.holder.Name .. S(856) .. target.name
        .. S(548) .. target.power .. S(822) .. best.name .. S(910) .. best.power .. S(314))
    runGuarded(S(203), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(547)] = function()
return function(import)
local gameApi = import(S(115))(import)
local parseMoney = import(S(500))
local log = import(S(228))
local runGuarded = import(S(872))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(282) .. (enabled and S(946) or S(453)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(109)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(167) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(475) .. deepestKey .. S(373))
        return true
    end
    log(S(686) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(530) .. gameApi.ZoneDisplayName(zoneKey) .. S(138)
                .. math.floor(price) .. S(700) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(928) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(23) .. zoneKey .. S(563))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(752) then
            reportedLocked = S(698)
            log(S(504) .. zoneKey .. S(577)
                .. gameApi.ZoneDisplayName(currentKey) .. S(4)
                .. done .. S(9) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(544), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(742)] = function()
return function(import)
local gameApi = import(S(782))(import)
local log = import(S(716))
local runGuarded = import(S(163))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(251) .. (enabled and S(378) or S(401)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(515) .. node.zoneKey .. S(485) .. node.id
                    .. S(791) .. node.family .. S(385) .. node.level .. S(743) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(248) .. node.id
                    .. S(768) .. node.family .. S(239) .. node.level .. S(128) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(407) .. blocked
                .. S(623))
        elseif powerBlocked then
            log(S(944) .. powerBlocked.zoneKey .. S(440) .. powerBlocked.id
                .. S(566) .. powerBlocked.family .. S(133)
                .. math.floor(powerBlocked.powerReq) .. S(638)
                .. math.floor(powerBlocked.power)
                .. S(219))
        else
            log(S(285)
                .. S(611) .. math.floor(gameApi.GetMoney()) .. S(352))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(661) .. nearest.zoneKey
                    .. S(647) .. nearest.id .. S(421) .. nearest.family .. S(525)
                    .. nearest.level .. S(348) .. nearest.max
                    .. S(427) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(454)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(523),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(98))
        end
        if not buyCore then
            table.insert(parts, S(551))
        end
        if #parts == 0 then
            return S(474)
        end
        return S(610) .. table.concat(parts, S(621)) .. S(274)
    end
    local why
    if nearest.cost > budget then
        why = S(390) .. string.format(S(487), nearest.cost)
            .. S(828) .. string.format(S(291), money)
    elseif nearest.powerOk == false then
        why = S(11) .. string.format(S(917), nearest.powerReq) .. S(726)
    else
        why = S(223)
    end
    return S(292) .. nearest.zoneKey .. S(571) .. tostring(nearest.id)
        .. S(678) .. tostring(nearest.family) .. S(518) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(659) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(468), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(448)] = function()
return function(import)
local Workspace = game:GetService(S(457))
local gameApi = import(S(694))(import)
local teleport = import(S(863))
local log = import(S(827))
local runGuarded = import(S(924))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(308))
    else
        lastAttempt = 0
        log(S(353))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(670))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(309), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(882)
                .. (sold and S(746)
                         or S(933)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(710) .. (sold and (S(853) .. #keys .. S(846)) or S(437)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(119), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(903)] = function()
return function(import)
local Workspace = game:GetService(S(338))
local gameApi = import(S(68))(import)
local teleport = import(S(220))
local log = import(S(12))
local runGuarded = import(S(422))(import)
local STATS = { S(15), S(614), S(829) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(392)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(682) .. (enabled and S(139) or S(6)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(632) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local best = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if priority ~= S(697) then
                    if stat == priority then
                        return { stat = stat, info = info }
                    end
                elseif not best or info.level < best.info.level then
                    best = { stat = stat, info = info }
                end
            end
        end
    end
    if best then
        return best
    end
    return nil, anyLeft and S(424) or S(954)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(302) .. holderKey .. S(242) .. choice.stat .. S(327)
                        .. choice.info.level .. S(430) .. after.level
                        .. S(13) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(295) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(324))
    local shop = stand and stand:FindFirstChild(S(689))
    if not shop then
        return nil
    end
    if shop:IsA(S(232)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(729), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(173)
                .. (bought and S(597)
                           or S(852)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(194))
        end
    end
    if bought then
        log(S(8) .. pickaxe.index .. S(131) .. pickaxe.name
            .. S(256) .. pickaxe.multiplier .. S(779))
        return true
    end
    log(S(22) .. pickaxe.index .. S(901) .. pickaxe.name
        .. S(264))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(26) .. holderKey .. S(49)
                    .. tostring(worn or S(221)) .. S(202) .. wanted.name)
            else
                log(S(601) .. holderKey .. S(683)
                    .. wanted.name .. S(416) .. tostring(nowWorn or S(798)) .. S(86))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(612) .. holderKey .. S(622) .. variant
                .. S(154) .. after .. S(198) .. cost .. S(480))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(913) .. gameApi.OwnedPickaxeCount()
                    .. S(399) .. gameApi.MaxMinerSlots()
                    .. S(740))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(368))
        elseif maxed > 0 then
            log(S(417))
        else
            log(S(503))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(508), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(735)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(490))
local Workspace = game:GetService(S(311))
local log = import(S(585))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(226), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(730) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(522))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(771) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(177) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(393) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(97) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(673) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(921))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(411) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(680))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(786), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(415) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(864) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(579) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(652), keys)
end
function api.EquipSlot(slot)
    return send(S(102), slot == S(66) and S(1) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(46), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(728), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(754), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.BuySkill(zoneKey, family)
    return send(S(241), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(790)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(750)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(331))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(206) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(70) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(412), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(642))
    plots = plots and plots:FindFirstChild(S(402))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(79)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(850)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(958))
    local gui = board and board:FindFirstChild(S(713))
    local root = gui and gui:FindFirstChild(S(91))
    local list = root and root:FindFirstChild(S(27))
    local row = list and list:FindFirstChild(S(871) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(391)) and descendant.Text ~= S(84) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(240) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(389), characterKey, stat == S(816) and S(887) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(578), max = S(772), cost = S(749) },
    Speed = { level = S(484), max = S(126), cost = S(364) },
    MineSpeed = { level = S(301), max = S(942), cost = S(796) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(466) or charName == S(141) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    return api.CharacterBaseDamage(holder.CharName)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(204) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(299) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(408) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(38) and type(holder.CharName) == S(370) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(145), S(270) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(837) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(67)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(725)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(588)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(344) then
        return {}, S(180)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(489)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(755) or type(state.Track) ~= S(78) then
                table.insert(notes, tabKey .. S(3) .. tostring(category) .. S(949))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(876) .. waiting
                        .. S(345))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(800) .. total .. S(304)
            .. (#notes > 0 and (S(582) .. table.concat(notes, S(617))) or S(844))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(839) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(483)] = function()
return function(import)
local Players = game:GetService(S(806))
local gameApi = import(S(298))(import)
local log = import(S(63))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(594))
    if not number then
        return 0
    end
    local value = tonumber((number:gsub(S(590), S(805)))) or 0
    if suffix ~= S(222) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(511))
    local mineGui = playerGui and playerGui:FindFirstChild(S(756))
    local hotbar = mineGui and mineGui:FindFirstChild(S(287))
    local content = hotbar and hotbar:FindFirstChild(S(953))
    return content and content:FindFirstChild(S(615))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(45)) then
            local count = descendant.Text:match(S(125))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(185))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(559))
    local nameLabel = content and content:FindFirstChild(S(459))
    local damageLabel = content and content:FindFirstChild(S(40))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(89) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = gameApi.CharacterBaseDamage(name) or parsePower(damageLabel.Text),
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(90) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(418) .. slot .. S(897) .. info.name
                        .. S(332) .. info.power .. S(875) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(93)] = function()
local Players = game:GetService(S(860))
local Workspace = game:GetService(S(179))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(693))
    local plots = mineWorld and mineWorld:FindFirstChild(S(80))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(766)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(254)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(56)),
        Nodes = plot:FindFirstChild(S(858)),
        Chest = plot:FindFirstChild(S(519)),
        Zones = plot:FindFirstChild(S(877)),
        Crew = plot:FindFirstChild(S(199)),
        Loot = plot:FindFirstChild(S(732)),
        PlotIndex = plot:GetAttribute(S(584)),
        ChestCapacity = plot:GetAttribute(S(29)),
    }
end
return getPlotParts
end
modules[S(460)] = function()
return function(import)
    local createWindow = import(S(35))(import)
    local i18n = import(S(701))
    local antiAfk = import(S(521))(import)
    local buildEnv = import(S(481))
    local scheduler = import(S(699))(import)
    local config = import(S(189))(import)
    local log = import(S(158))
    log(S(695) .. buildEnv.Commit .. S(229) .. buildEnv.Env
        .. S(283) .. buildEnv.BuiltAt .. S(687))
    local window = createWindow({
        Subtitle = S(606),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(94))(import)
    local autoPlace = import(S(433))(import)
    local autoChest = import(S(159))(import)
    local autoSell = import(S(69))(import)
    local autoUpgrade = import(S(501))(import)
    local autoResearch = import(S(170))(import)
    local autoClaim = import(S(672))(import)
    local autoPlot = import(S(85))(import)
    local gameApi = import(S(960))(import)
    scheduler.RegisterStandalone(S(495), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(679), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(684), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(923), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(938), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(838), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(618), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(587), 20)
    scheduler.RegisterExclusive(S(576), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(164), S(769))
    persistentToggle(farmTab:AddCard(
        S(99),
        S(510)
    ), S(509), autoMine.SetEnabled)
    persistentSlider(farmTab, S(147), S(319),
        1, 30, S(935), 5, autoMine.SetCyclesPerSecond, S(557))
    persistentSlider(farmTab, S(47), S(599),
        1, 10, S(761), 1, autoMine.SetClicksPerCycle, S(802))
    persistentToggle(farmTab:AddCard(
        S(447),
        S(58)
    ), S(934), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(943), S(778),
        1, 60, S(709), 3, autoPlace.SetCheckInterval, S(379))
    persistentToggle(
        farmTab:AddCard(S(343),
            S(171)),
        S(124), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(810),
            S(465)),
        S(499), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(789), S(847),
        0, 500, S(169), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(589))
    persistentToggle(farmTab:AddCard(
        S(215),
        S(627)
    ), S(230), autoChest.SetEnabled)
    persistentSlider(farmTab, S(53), S(255),
        50, 3000, S(865), 100, autoChest.SetMinItems, S(258))
    persistentSlider(farmTab, S(19), S(595),
        1, 60, S(420), 3, autoChest.SetCheckInterval, S(556))
    persistentToggle(farmTab:AddCard(
        S(57),
        S(616)
    ), S(247), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(720),
            S(200)),
        S(855), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(757), S(294),
        5, 300, S(496), 20, autoSell.SetCheckInterval, S(425))
    farmTab:AddSlider(S(932),
        S(603),
        5, 120, sellMaxWait, function(value)
            config.Set(S(210), value)
        end, S(317))
    persistentToggle(farmTab:AddCard(
        S(862),
        S(243)
    ), S(807), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(696), S(640),
        5, 300, S(25), 20, autoUpgrade.SetInterval, S(323))
    persistentSlider(farmTab, S(911), S(737),
        1, 20, S(799), 5, autoUpgrade.SetMaxPerPass, S(715))
    persistentSegmented(
        farmTab,
        { S(916), S(168), S(155), S(190) },
        { S(375), S(340), S(52), S(479) },
        S(237), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(2), S(904),
        0, 500, S(252), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(529))
    persistentToggle(
        farmTab:AddCard(S(857),
            S(231)),
        S(469), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(641) .. pickaxe.multiplier .. S(318)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(278)
            end
            return S(134) .. index .. S(835) .. (pickaxeLabels[index] or S(266))
        end
        local savedPick = config.Get(S(836), 0)
        pickaxeCard = farmTab:AddSlider(
            S(879),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(664), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(513)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(886),
            S(896)),
        S(161), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(275),
            S(957)),
        S(883), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(64),
            S(931)),
        S(493), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(893), S(431),
        1, 20, S(143), 2, autoUpgrade.SetMutationTarget, S(61))
    persistentSlider(farmTab, S(656), S(320),
        0, 500, S(781), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(777))
    persistentToggle(farmTab:AddCard(
        S(51),
        S(922)
    ), S(217), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(114), S(552),
        10, 600, S(573), 30, autoResearch.SetCheckInterval, S(130))
    persistentSlider(farmTab, S(321), S(491),
        1, 20, S(535), 5, autoResearch.SetMaxPerPass, S(21))
    persistentSlider(farmTab, S(583), S(841),
        0, 500, S(405), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(703))
    persistentToggle(
        farmTab:AddCard(S(208),
            S(383)),
        S(653), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(403),
            S(150)),
        S(947), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(609),
            S(472)),
        S(745), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(384), S(770) },
        { true, false },
        S(714), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(538),
        S(446)
    ), S(801), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(619), S(135),
        5, 300, S(296), 30, autoClaim.SetCheckInterval, S(738))
    persistentSlider(farmTab, S(450), S(818),
        1, 30, S(540), 10, autoClaim.SetMaxPerPass, S(739))
    persistentToggle(farmTab:AddCard(
        S(369),
        S(851)
    ), S(808), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(553), S(166),
        5, 300, S(660), 30, autoPlot.SetCheckInterval, S(555))
    persistentToggle(
        farmTab:AddCard(S(505),
            S(734)),
        S(630), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(534),
            S(357)),
        S(526), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(488), S(620),
        0, 500, S(104), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(764))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(225))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(410), S(564))
    persistentToggle(
        settingsTab:AddCard(S(113), S(434)),
        S(82),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(527), 100)
    settingsTab:AddSlider(S(193), S(195), 50, 150, savedScale, function(value)
        config.Set(S(708), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(662), 1)
    settingsTab:AddSegmented({ S(140), S(753), S(10) }, savedLanguage, function(index, value)
        local code = ({ S(260), S(273), S(815) })[index]
        config.Set(S(780), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(244), S(899), S(286) })[savedLanguage] or S(688))
    log(S(562) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(182)] = function()
return {
    [79389059854988] = S(152),
}
end
import(S(636))(import)
