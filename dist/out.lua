-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"", "Dump", "Pad", "[^%d%.]", "ProximityPrompt", "MineGui", "[AutoSell] click failed on ", "Players", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "Frame", "[AutoPlace] placed ", "Frame", ") didn't take, trying the next one", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "games/my_anime_mine/get_best_miner", "TextButton", "Close", " is still empty after placing ", "PlayerGui", "MineGui", "no plot", "  [attr] ", " = ", "games/my_anime_mine/get_plot_parts", "Players", "TextLabel", "MekPro", "UIStroke", "Players", "", "Auto Mine", "ClickDetector", "", "core/logger", "", "th", "core/config", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", " cards left, label = '", "Backpack", "Loot", "neither appendfile nor writefile is available", "[AutoChest] fireproximityprompt error: ", " (power ", "Frame", "TextLabel", "games/my_anime_mine/debug_dump_sell_cards", "core/get_place_id", "X", "TextButton", "games/my_anime_mine/get_current_plot", "TextButton", "games/my_anime_mine/get_plot_parts", "autoPlace", "core/interaction_logger", "TextButton", "TabBar", "[MyAnimeMine] not standing in any plot", "core/logger", "games/my_anime_mine/get_current_plot", "core/anti_afk", " ===", "ตามเครื่อง", "no isfile/readfile available to verify the write", "?", "write reported success but ", "Debug: Dump All Remotes", "games/my_anime_mine/auto_sell", "games/my_anime_mine/debug_dump_gui", "RunService", "Frame", " remotes", "Plots", "[AutoMine] ", "Container", "core/scheduler", "auto", "button has no size on screen", "Text", "Holders", "This is not a supported place.", "TextButton", "Players", "RemoteEvent", "'", "[AutoPlace] teleport didn't land: %.1f studs from %s (max %s), skipping", "[AutoChest] can't read the chest billboard, is the plot loaded?", " cards", "ขนาดเมนู", "RemoteEvent", "SellButton", "อังกฤษ", "SellFrame", "[AutoSell] sell menu didn't open, aborting", "Open menu", "^(%d+)", "[MyAnimeMine] === Holder: ", "core/logger", "core/logger", "Players", "unreadable", "[AutoPlace] drifted %.1f studs from %s while selecting, aborting", "ReplicatedStorage", "ย่อขยายเมนูทั้งชุด", "Players", "[AutoChest] waiting: ", "Workspace", "  ", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", " error: ", "", "[AutoUpgrade] ", "[AutoChest] chest didn't open, aborting", "games/my_anime_mine/get_plot_parts", "[Scheduler] turn: ", "Label", "PlotIndex", "TextLabel", "auto", "Humanoid", "[Interaction] ClickDetector clicked: ", ")", "games/my_anime_mine/debug_dump_remotes", "core/logger", "", "Check", "", "Badge", "GuiObject", "[ChestDump] === end ===", ": ", "[Interaction] logger enabled - go click/interact with things now", "core/click", "MekPro.log", "core/logger", "RemoteFunction", "TextLabel", "ปุ่มเปิดเมนู", "Debug: Dump Chest Data", "enabled", "UIListLayout", " (ActionText=", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "VirtualInputManager", "games/my_anime_mine/get_current_plot", "[MyAnimeMine] no nodes present right now", "TextLabel", "Card", "games/my_anime_mine/debug_dump_sell_cards", " items", "[AutoPlace] ", "games/my_anime_mine/get_plot_parts", "Auto Sell", "languageIndex", "[MyAnimeMine] no holders found", " attributes ===", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "  [attr] ", "[AutoSell] disabled", "ProximityPrompt", "[ChestDump] === ReplicatedStorage.", "TextButton", "ClickDetector", "Empty miner slot", "TextLabel", "[AutoChest] Take All had NO effect (", "MineGui", ": ", "auto", "TextLabel", ")", "TextButton", "ProximityPrompt", "core/dispatch", "Sell for", "UserInputService", "Done", "disabled", " items)", "s", "UIListLayout", "core/config", "[attr] ", "ฟาร์ม", "Interaction Logger (debug)", "Frame", "", "core/logger", "[AutoPlace] disabled", "Header", "  [attr] ", "enabled", "[Scheduler] ", "ReplicatedStorage", "[Config] couldn't write ", "Content", "Debug: Dump Visible GUI", " ", "ValueLabel", "🏠", "/", "Chest", "[ChestDump] === plot ", "PlayerGui", "games/my_anime_mine/get_plot_parts", "ClickDetector", " doesn't exist", "GuiObject", "[AntiAFK] disabled", " '", "log failed", "/", "'", "[ChestDump] === Chest subtree ===", "search_placeholder", "  ", "[BestMiner] slot ", "en-us", "games/my_anime_mine/get_current_plot", "Auto Chest", "core/click", "'", "/config.json", "UICorner", "Scroll", "GuiService", "core/get_place_id", "games/my_anime_mine/get_plot_parts", " '", "en", " items", "HttpService", "RemoteFunction", "Backpack", "core/logger", "core/i18n", "menuScale", "(none enabled)", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "core/teleport", "OwnerUserId", "hotbar has no miner to place", " = ", "GuiButton", "en", "core/i18n", "MekPro", "ค้นหาฟังก์ชัน...", "[Interaction] Button activated: ", "waiting", " (", "Sell for", "[MyAnimeMine] === Dump start ===", "ScreenGui", "UIPadding", "Badge", "core/logger", "⚙", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "ProximityPrompt", "Error: ", "[AutoSell] fireproximityprompt error: ", "Players", "AutoSell", " cards)", "[AntiAFK] enabled - watching Player.Idled", "Crew", "table", "core/scheduler", "My Anime Mine · Mining Hub", ": ", "th", "", "[AutoChest] disabled", "HumanoidRootPart", "unreadable", " ===", "UIListLayout", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "[Interaction] ProximityPrompt triggered: ", "Done", "[%X] ", "ChestFrame", "Players", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Players", "games/registry", "HotbarFrame", "\n", "AutoUpgrade", "Tool", "core/anti_afk", "Progress", "CoreUI", "ScrollingFrame", "Place", "leaderstats", "UICorner", "auto", ": ", "Label", "Workspace", "Dump", " = ", "Players", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "'", "Dump", "[AutoChest] click failed on ", "", " '", "ProximityPrompt", "[MekPro] settings file: ", "Main", "games/my_anime_mine/auto_sell", "[AutoChest] Take All worked: ", "[AutoPlace] enabled (places the strongest miner it can find)", "search_placeholder", "AutoPlace", "", " parts", "", "[AutoSell] nothing selectable (", "/", "'), backing off ", ") on ", "'", "games/my_anime_mine/debug_dump", "games/my_anime_mine/auto_upgrade", "TextLabel", "MineGui", " x", "Zones", "  ", "th", "Amount", "Content", "en", "?", " (Text=", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "[Remotes] === total: ", "core/logger", "Debug: Current Plot", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", " power ", "core/dispatch", "core/teleport", " '", "[Logger] failed to write ", "Content", "open_menu_hint", "Players", "[ChestDump] === Player subtree ===", "Frame", "ChestCapacity", "[ChestDump] not standing in any plot", "UserInputService", "games/my_anime_mine/auto_upgrade", "games/registry", "core/logger", "languageIndex", "Content", "ProximityPrompt", "games/my_anime_mine/auto_place", "[AutoSell] enabled (only sells when there's something in the scroll)", "Card", "Dump", "Anti-AFK", "antiAfk", "[ChestDump] plot has no Chest", "Search", " <-- remote, likely fired by the real interaction", ": ", "no empty holder right now", "games/my_anime_mine/get_plot_parts", "Dump", "not found", "core/get_place_id", "TextLabel", ": ", "PlayerGui", "th", "waiting", "[SellCards] === total cards: ", " = ", "Scroll", " -> ", ",", "core/interaction_logger", " (MaxActivationDistance=%s)", ": ", "no tool", "Search functions...", "SellStand", "%.1f", ")", "ไทย", "UIScale", "TextButton", "core/get_leaderstat", "[MyAnimeMine] === Node: ", "core/logger", "AutoMine", "Players", "[GuiDump] === end ===", "Players", "[AutoSell] nothing to sell, closing", "", "PlayerGui", "core/teleport", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "games/my_anime_mine/get_current_plot", "TakeButton", "ValueLabel", "SelectAllButton", "", "ItemName", " = ", "HumanoidRootPart", "[AutoPlace] ", "  ", " ===", "MineGui", "Content", "TextLabel", "TextBox", "[AutoPlace] no slot reported ready (prompt '%s', enabled=%s, %s studs away) - trying %s anyway", "core/click", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Frame", "  [attr] ", "autoMine", "autoChest", "Content", "Done", "ItemFrame", "PlayerGui", "games/my_anime_mine/auto_chest", "[Interaction] Key pressed: %s (gameProcessed=%s)", "\n", "disabled", "autoUpgrade", "Debug: Dump Sell Cards", "core/teleport", "", "MineWorld", "Nodes", "[Remotes] ", "[AntiAFK] idle detected, simulating input", "[AutoPlace] fireproximityprompt error: ", "TextLabel", "write reported success but the line isn't in ", "[MyAnimeMine] === Tool: ", "ClickDetector", "[GuiDump] MineGui not found", "Content", "core/get_me", "core/i18n", "core/logger", "Content", "ตั้งค่า", "Frame", "Debug: Dump Backpack Tool", "CoreGui", "[AutoPlace] slot ", "core/click", "TextButton", "Frame", "games/my_anime_mine/debug_dump_chest", "menuScale", "Content", "SellFrame", "  ", "Frame", "%", "ValueBase", "core/ui", "  ", "games/my_anime_mine/get_current_plot", "TextLabel", " (Text=", "games/my_anime_mine/debug_dump_chest", "Debug: Dump Node/Holder", "games/my_anime_mine/get_best_miner", "TextLabel", "GuiButton", "autoSell", "Dump", "interactionLogger", "^x(%d+)$", "games/my_anime_mine/debug_dump_remotes", "Damage", "games/my_anime_mine/main", "[AutoChest] enabled (collects once >= ", "TextButton", "-", "Actions", "TextLabel", "", "ClickDetector", " ===", " '", "core/ui", " items, going to collect", "UpgradePart", "games/my_anime_mine/debug_dump", "PlayerGui", "games/my_anime_mine/auto_mine", "", " ===", "AutoChest", " items)", " -> ", "core/logger", "", "Auto Place Miner", "[Interaction] logger disabled", "%", "core/logger", "", "TextLabel", "Players", "Text", " = ", " ===", "games/my_anime_mine/auto_chest", "[AutoChest] ", "Frame", "[SellCards] Card #", "core/logger", "games/my_anime_mine/get_current_plot", "Auto Upgrade", "games/my_anime_mine/debug_dump_gui", ": ", "[AutoSell] clicked SellButton", "games/my_anime_mine/main", "open_menu_hint", "[GuiDump] === visible MineGui tree ===", ")", "[Config] couldn't read ", "Select All", " = ", "", "Close", "[AutoSell] clicked SelectAllButton (", "ValueBase", "VirtualUser", "games/my_anime_mine/auto_mine", "Workspace", " (Text=", "games/my_anime_mine/auto_place"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(293)] = function()
return function(import)
local log = import(S(533))
local Players = game:GetService(S(26))
local VirtualUser = game:GetService(S(550))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(267))
        connection = player.Idled:Connect(function()
            log(S(452))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(210))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(469)] = function()
local VirtualInputManager = game:GetService(S(145))
local GuiService = game:GetService(S(227))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(79)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(38)] = function()
return function(import)
local HttpService = game:GetService(S(233))
local getPlaceId = import(S(228))
local log = import(S(35))
local ROOT_FOLDER = S(28)
local placeFolder = ROOT_FOLDER .. S(325) .. tostring(getPlaceId())
local filePath = placeFolder .. S(224)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(269) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(543) .. filePath .. S(382) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(196) .. filePath .. S(272) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(175)] = function()
return function(import)
    local getPlaceId = import(S(49))
    local registry = import(S(288))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(82))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(402)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(298))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(460)] = function()
local Players = game:GetService(S(264))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(121))
    local backpack = player:WaitForChild(S(41))
    return player, humanoid, backpack
end
return getMe
end
modules[S(380)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(237)] = function()
local Players = game:GetService(S(306))
local STRINGS = {
    en = {
        search_placeholder = S(395),
        open_menu_hint = S(96),
    },
    th = {
        search_placeholder = S(249),
        open_menu_hint = S(139),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(219)
    if locale:sub(1, 2):lower() == S(336) then
        return S(37)
    end
    return S(231)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(120) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(391)] = function()
return function(import)
local Players = game:GetService(S(101))
local Workspace = game:GetService(S(552))
local UserInputService = game:GetService(S(177))
local log = import(S(100))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(281) .. describe(prompt)
            .. S(143) .. prompt.ActionText .. S(398))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(122) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(250) .. describe(button)
            .. S(553) .. tostring(button.Text) .. S(542))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(161)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(457)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(245)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(520))
        return
    end
    connections = {}
    log(S(133))
    local playerGui = Players.LocalPlayer:WaitForChild(S(510))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(365)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(207)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(489)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(442),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(462)] = function()
local LOG_FILE = S(135)
local function log(message)
    local line = os.date(S(283)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(523))
            end
            appendfile(LOG_FILE, line .. S(290))
        elseif writefile then
            local existing = S(188)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(443))
        else
            error(S(43))
        end
        if not (isfile and readfile) then
            error(S(65))
        end
        if not isfile(LOG_FILE) then
            error(S(67) .. LOG_FILE .. S(208))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(455) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(351) .. LOG_FILE .. S(301) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(77)] = function()
return function(import)
local RunService = game:GetService(S(71))
local log = import(S(522))
local SLICE_SECONDS = 3
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy)
    table.insert(exclusiveTasks, { name = name, step = step, isEnabled = isEnabled, isBusy = isBusy })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(194) .. task_.name .. S(111) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickNextEnabled()
    if #exclusiveTasks == 0 then
        return nil
    end
    for _ = 1, #exclusiveTasks do
        rrIndex = (rrIndex % #exclusiveTasks) + 1
        local candidate = exclusiveTasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(116) .. (currentTask and currentTask.name or S(239)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(241)] = function()
local Players = game:GetService(S(285))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(422))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(480)] = function()
return function(import)
local i18n = import(S(461))
local UserInputService = game:GetService(S(359))
local THEME = {
    BrandName = S(248),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(225))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(299))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(256))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(498))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(448)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(29))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(47))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(340)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(27))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(419)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(296))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(142))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(138))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(471))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(331))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(483))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(524))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(417))
            if not valueLbl then
                valueLbl = Instance.new(S(428))
                valueLbl.Name = S(200)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(257))
            if not badge then
                badge = Instance.new(S(488))
                badge.Name = S(129)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(173))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(477))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(72))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(163))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(34)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(521))
        local track = Instance.new(S(46))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(531))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(470))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(311)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(478)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(433))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(182))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(53))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(526)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(255))
    screenGui.Name = S(295)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(467))
    local main = Instance.new(S(356))
    main.Name = S(315)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(400))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(12))
    tabBar.Name = S(58)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(279))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(465))
    header.Name = S(191)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(51))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(454))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(381))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(1)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(401))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(50)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(57))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(499)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(429))
    searchBox.Name = S(373)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(216))
    searchBox.Text = S(323)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(10))
    content.Name = S(364)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(187))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(501))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(353))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(166))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(80)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(546) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(199) .. (item.Subtitle or S(502))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(319))
        footerLabel.Text = i18n.T(S(540))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(529)] = function()
return function(import)
local Players = game:GetService(S(106))
local getCurrentPlot = import(S(482))
local getPlotParts = import(S(54))
local teleport = import(S(447))
local click = import(S(431))
local log = import(S(404))
local CHECK_INTERVAL = 3
local MIN_ITEMS_TO_COLLECT = 100
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(383))
    local mineGui = playerGui and playerGui:FindFirstChild(S(6))
    return mineGui and mineGui:FindFirstChild(S(284))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(437))
    local actions = content and content:FindFirstChild(S(500))
    return actions and actions:FindFirstChild(S(416))
end
local function parseCount(text)
    local digits = tostring(text):gsub(S(390), S(126)):match(S(97))
    return tonumber(digits)
end
local function getChestCount()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    local chest = parts and parts.Chest
    local amount = chest and chest:FindFirstChild(S(337), true)
    return amount and parseCount(amount.Text)
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(352))
    local progress = content and content:FindFirstChild(S(294))
    local label = progress and progress:FindFirstChild(S(302))
    return parseCount(label and label.Text or S(31))
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(310) .. button.Name .. S(169) .. tostring(err))
    end
    return ok
end
local function getChestPrompt()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    return parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(313), true)
end
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReportedCount = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(275))
    else
        lastAttempt = 0
        lastReportedCount = nil
        log(S(497) .. MIN_ITEMS_TO_COLLECT .. S(180))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt = getChestPrompt()
    if not prompt then
        busy = false
        return
    end
    teleport.To(prompt.Parent)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(44) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local chestFrame = getChestFrame()
    if not (chestFrame and chestFrame.Visible) then
        log(S(114))
        teleport.Return()
        busy = false
        return
    end
    local count = getItemCount(chestFrame)
    local takeButton = getTakeButton(chestFrame)
    if takeButton then
        clickButton(takeButton)
        task.wait(0.6)
        local after = getItemCount(chestFrame)
        if after and count and after < count then
            log(S(317) .. count .. S(516) .. after .. S(232))
        else
            log(S(167) .. tostring(count)
                .. S(389) .. tostring(after) .. S(515))
        end
    end
    teleport.Return()
    busy = false
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local count = getChestCount()
    if not count then
        if lastReportedCount ~= S(277) then
            log(S(88))
            lastReportedCount = S(102)
        end
        return
    end
    if count < MIN_ITEMS_TO_COLLECT then
        if lastReportedCount ~= S(251) then
            log(S(107) .. count .. S(213) .. MIN_ITEMS_TO_COLLECT .. S(151))
            lastReportedCount = S(385)
        end
        return
    end
    lastReportedCount = nil
    log(S(530) .. count .. S(507))
    task.spawn(runJob)
end
return autoChest
end
end
modules[S(551)] = function()
return function(import)
local getCurrentPlot = import(S(52))
local getPlotParts = import(S(206))
local log = import(S(136))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(33), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(75) .. (enabled and S(141) or S(444)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(554)] = function()
return function(import)
local Players = game:GetService(S(406))
local getCurrentPlot = import(S(220))
local getPlotParts = import(S(25))
local getMinerSlots = import(S(16))(import)
local teleport = import(S(412))
local click = import(S(134))
local log = import(S(236))
local CHECK_INTERVAL = 3
local EMPTY_SLOT_TEXT = S(165)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(3))
    return pad and pad:FindFirstChildWhichIsA(S(5), true)
end
local function isHolderUsable(prompt)
    return prompt ~= nil and prompt.Enabled and prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(276))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function findEmptyHolder()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderUsable(prompt) then
            return holder, prompt
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(423) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(190))
    else
        lastAttempt = 0
        log(S(318))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
local function isPromptReady(prompt)
    return prompt.ActionText ~= nil and prompt.ActionText:find(S(297), 1, true) ~= nil
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if isPromptReady(prompt) then
            return true
        end
    end
    return false
end
local function runJob(holder, prompt, candidates)
    busy = true
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    local arrived = distanceTo(prompt.Parent)
    if arrived and arrived > prompt.MaxActivationDistance then
        log(string.format(S(87),
            arrived, holder.Name, tostring(prompt.MaxActivationDistance)))
        teleport.Return()
        busy = false
        return
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        local distance = distanceTo(prompt.Parent)
        if distance and distance > prompt.MaxActivationDistance then
            log(string.format(S(103),
                distance, holder.Name))
            teleport.Return()
            busy = false
            return
        end
        log(S(468) .. candidate.slot .. S(252) .. candidate.name
            .. S(13))
    end
    if not miner then
        miner = candidates[1]
        log(string.format(
            S(430),
            tostring(prompt.ActionText), tostring(prompt.Enabled),
            distanceTo(prompt.Parent) and string.format(S(397), distanceTo(prompt.Parent)) or S(66),
            miner.name
        ))
    end
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(453) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.5)
    if isHolderEmpty(prompt) then
        log(S(152) .. holder.Name .. S(19) .. miner.name)
    else
        log(S(11) .. miner.name .. S(45) .. miner.power
            .. S(327) .. holder.Name)
    end
    teleport.Return()
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    local holder, prompt = findEmptyHolder()
    if not holder then
        reason(S(376))
        return
    end
    local candidates = getMinerSlots()
    if #candidates == 0 then
        reason(S(243))
        return
    end
    lastReason = nil
    task.spawn(function()
        runJob(holder, prompt, candidates)
    end)
end
return autoPlace
end
end
modules[S(316)] = function()
return function(import)
local Players = game:GetService(S(525))
local Workspace = game:GetService(S(303))
local teleport = import(S(349))
local click = import(S(222))
local log = import(S(344))
local CHECK_INTERVAL = 45
local NOTHING_SELLABLE_COOLDOWN = 60
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(411))
    local mineGui = playerGui and playerGui:FindFirstChild(S(426))
    return mineGui and mineGui:FindFirstChild(S(94))
end
local function labelText(button)
    local content = button:FindFirstChild(S(427))
    local label = content and content:FindFirstChild(S(117))
    return label and label.Text or S(512)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(459))
    local scroll = content and content:FindFirstChild(S(226))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(368) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    local ok, err = click.Real(button)
    if not ok then
        log(S(7) .. button.Name .. S(537) .. tostring(err))
    end
    return ok
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(396))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(174), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(160))
    else
        lastAttempt = 0
        log(S(367))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
local function runJob()
    busy = true
    local prompt, promptPart = getSellStandPromptPart()
    if not (prompt and promptPart) then
        busy = false
        return
    end
    teleport.To(promptPart)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(263) .. tostring(err))
        teleport.Return()
        busy = false
        return
    end
    task.wait(0.8)
    if not enabled then
        teleport.Return()
        busy = false
        return
    end
    local sellFrame = getSellFrame()
    if not (sellFrame and sellFrame.Visible) then
        log(S(95))
        teleport.Return()
        busy = false
        return
    end
    local cardCount = countCards(sellFrame)
    if cardCount == 0 then
        log(S(409))
        local closeButton = sellFrame:FindFirstChild(S(547))
        if closeButton then
            clickButton(closeButton)
        end
        teleport.Return()
        busy = false
        return
    end
    local content = sellFrame:FindFirstChild(S(463))
    local selectAllButton = content and content:FindFirstChild(S(418))
    local sellButton = content and content:FindFirstChild(S(92))
    if selectAllButton and labelText(selectAllButton) == S(544) then
        clickButton(selectAllButton)
        log(S(548) .. cardCount .. S(266))
    end
    local sawLabel = nil
    for _ = 1, 5 do
        task.wait(0.3)
        sawLabel = sellButton and labelText(sellButton) or nil
        if sawLabel and sawLabel:find(S(176), 1, true) then
            break
        end
    end
    if sawLabel and sawLabel:find(S(253), 1, true) then
        clickButton(sellButton)
        log(S(538))
    else
        lastAttempt = tick() + NOTHING_SELLABLE_COOLDOWN - CHECK_INTERVAL
        local closeButton = sellFrame:FindFirstChild(S(18))
        if closeButton then
            clickButton(closeButton)
        end
        log(S(324) .. cardCount .. S(40)
            .. tostring(sawLabel) .. S(326) .. NOTHING_SELLABLE_COOLDOWN .. S(181))
    end
    task.wait(0.5)
    teleport.Return()
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < CHECK_INTERVAL then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(330)] = function()
return function(import)
local getCurrentPlot = import(S(415))
local getPlotParts = import(S(229))
local log = import(S(189))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(508))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(503))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(113) .. (enabled and S(193) or S(179)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(329)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(36)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(350) .. child.Name .. S(223)
        if child:IsA(S(261)) then
            line = line .. string.format(
                S(413),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(164)) then
            line = line .. string.format(S(392), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(91)) or child:IsA(S(137)) then
            line = line .. S(374)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(434) .. attrName .. S(387) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(424))
    end
end
return dumpInstance
end
modules[S(485)] = function()
return function(import)
local Players = game:GetService(S(287))
local ReplicatedStorage = game:GetService(S(195))
local getCurrentPlot = import(S(61))
local getPlotParts = import(S(377))
local log = import(S(517))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(184) .. name .. S(305) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(109))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(505) .. child.Name .. S(86)
            if child:IsA(S(549)) then
                line = line .. S(545) .. tostring(child.Value)
            elseif child:IsA(S(148)) or child:IsA(S(83)) then
                line = line .. S(484) .. tostring(child.Text) .. S(172)
            end
            log(line)
            dumpSubtree(child, indent .. S(335))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(358))
        return 0
    end
    log(S(204) .. plot.Name .. S(157))
    dumpAttributes(plot, S(217))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(372))
        return 0
    end
    log(S(215))
    dumpSubtree(parts.Chest, S(518))
    local player = Players.LocalPlayer
    log(S(355))
    dumpSubtree(player, S(112))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(162) .. player.Name .. S(504))
        dumpSubtree(mine, S(274))
    end
    log(S(131))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(70)] = function()
return function(import)
local Players = game:GetService(S(8))
local log = import(S(99))
local function dumpVisible(root, indent)
    indent = indent or S(410)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(209)) and child.Visible then
            local line = indent .. child.ClassName .. S(312) .. child.Name .. S(308)
            if child:IsA(S(171)) or child:IsA(S(17)) then
                line = line .. S(341) .. tostring(child.Text) .. S(123)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(23) .. attrName .. S(244) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(481))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(205))
    local mineGui = playerGui and playerGui:FindFirstChild(S(21))
    if not mineGui then
        log(S(458))
        return
    end
    log(S(541))
    dumpVisible(mineGui)
    log(S(407))
end
return dumpMineGui
end
end
modules[S(124)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(104))
local log = import(S(125))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(85)) or descendant:IsA(S(234)) then
            count = count + 1
            log(S(451) .. descendant.ClassName .. S(393) .. descendant:GetFullName())
        end
    end
    log(S(343) .. count .. S(528))
    return count
end
return dumpRemotes
end
end
modules[S(150)] = function()
return function(import)
local Players = game:GetService(S(354))
local log = import(S(362))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(20))
    local mineGui = playerGui and playerGui:FindFirstChild(S(168))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(475))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(197)) and sellFrame.Content:FindFirstChild(S(388))
    if not scroll then
        log(S(342))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(149) or card:IsA(S(130)) then
            count = count + 1
            log(S(532) .. count .. S(375) .. card.ClassName .. S(230) .. card.Name .. S(328))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(159) .. attrName .. S(24) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(476) .. child.ClassName .. S(211) .. child.Name .. S(214)
                if child:IsA(S(479)) then
                    line = line .. S(527) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(386) .. count .. S(278))
    return count
end
return dumpSellCards
end
end
modules[S(487)] = function()
return function(import)
local Players = game:GetService(S(84))
local log = import(S(60))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(440))
    local mineGui = playerGui and playerGui:FindFirstChild(S(332))
    local hotbar = mineGui and mineGui:FindFirstChild(S(289))
    local content = hotbar and hotbar:FindFirstChild(S(474))
    return content and content:FindFirstChild(S(76))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(119)) then
            local count = descendant.Text:match(S(493))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(439))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(338))
    local nameLabel = content and content:FindFirstChild(S(420))
    local damageLabel = content and content:FindFirstChild(S(495))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(321) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(4), S(128)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(202) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(218) .. slot .. S(132) .. info.name
                        .. S(347) .. info.power .. S(333) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(146)] = function()
local Players = game:GetService(S(30))
local Workspace = game:GetService(S(108))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(449))
    local plots = mineWorld and mineWorld:FindFirstChild(S(74))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(242)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(153)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(81)),
        Nodes = plot:FindFirstChild(S(450)),
        Chest = plot:FindFirstChild(S(203)),
        Zones = plot:FindFirstChild(S(334)),
        Crew = plot:FindFirstChild(S(268)),
        Loot = plot:FindFirstChild(S(42)),
        PlotIndex = plot:GetAttribute(S(118)),
        ChestCapacity = plot:GetAttribute(S(357)),
    }
end
return getPlotParts
end
modules[S(496)] = function()
return function(import)
    local Players = game:GetService(S(408))
    local createWindow = import(S(506))(import)
    local i18n = import(S(247))
    local antiAfk = import(S(62))(import)
    local interactionLogger = import(S(56))(import)
    local scheduler = import(S(270))(import)
    local config = import(S(183))(import)
    local log = import(S(258))
    local window = createWindow({
        Subtitle = S(271),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local getCurrentPlot = import(S(534))
    local getPlotParts = import(S(115))
    local dumpInstance = import(S(509))
    local dumpRemotes = import(S(494))(import)
    local dumpSellCards = import(S(48))(import)
    local dumpGui = import(S(536))(import)
    local dumpChest = import(S(472))(import)
    local autoMine = import(S(511))(import)
    local autoPlace = import(S(366))(import)
    local autoChest = import(S(441))(import)
    local autoSell = import(S(69))(import)
    local autoUpgrade = import(S(360))(import)
    scheduler.RegisterStandalone(S(405), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterStandalone(S(291), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.RegisterExclusive(S(320), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy)
    scheduler.RegisterExclusive(S(514), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy)
    scheduler.RegisterExclusive(S(265), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(185), S(201))
    persistentToggle(farmTab:AddCard(
        S(32),
        S(280)
    ), S(435), autoMine.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(519),
        S(39)
    ), S(55), autoPlace.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(221),
        S(144)
    ), S(436), autoChest.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(154),
        S(14)
    ), S(490), autoSell.SetEnabled)
    persistentToggle(farmTab:AddCard(
        S(535),
        S(9)
    ), S(445), autoUpgrade.SetEnabled)
    local debugCard = farmTab:AddCard(S(345), S(110))
    local checkBtn
    checkBtn = debugCard:AddButton(S(127), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(379)
            log(S(59))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(346),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(486),
        S(15)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(491), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(22)
            return
        end
        local ok, err = log(S(254))
        if not ok then
            dumpBtn.Text = S(212)
            dumpCard:SetSubtitle(S(262) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(403) .. firstNode.Name .. S(513))
            dumpInstance(firstNode, log)
        else
            log(S(147))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(98) .. firstHolder.Name .. S(425))
            dumpInstance(firstHolder, log)
        else
            log(S(156))
        end
        dumpBtn.Text = S(282)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(466),
        S(307)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(304), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(235))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(292))
        if not tool then
            toolDumpBtn.Text = S(394)
            return
        end
        log(S(456) .. tool.Name .. S(63))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(192) .. attrName .. S(421) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(178)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(68),
        S(414)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(378), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(73)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(446),
        S(260)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(369), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(89)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(198),
        S(432)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(2), function()
        dumpGui()
        guiDumpBtn.Text = S(438)
    end)
    local chestDumpCard = farmTab:AddCard(
        S(140),
        S(158)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(309), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(322)
    end)
    local settingsTab = window:AddTab(S(464), S(259))
    persistentToggle(
        settingsTab:AddCard(S(370), S(240)),
        S(371),
        antiAfk.SetEnabled
    )
    persistentToggle(
        settingsTab:AddCard(
            S(186),
            S(286)
        ),
        S(492),
        interactionLogger.SetEnabled
    )
    local savedScale = config.Get(S(238), 100)
    settingsTab:AddSlider(S(90), S(105), 50, 150, savedScale, function(value)
        config.Set(S(473), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(155), 1)
    settingsTab:AddSegmented({ S(64), S(93), S(399) }, savedLanguage, function(index, value)
        local code = ({ S(78), S(339), S(384) })[index]
        config.Set(S(363), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(300), S(246), S(273) })[savedLanguage] or S(170))
    log(S(314) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(361)] = function()
return {
    [79389059854988] = S(539),
}
end
import(S(348))(import)
