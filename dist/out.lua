-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"needs us at the stand - keeping the teleport", "core/get_place_id", "[AutoUpgrade] upgrade screen didn't open for ", "Done", "yes", "games/my_anime_mine/auto_upgrade", "MineGui", " <-- remote, likely fired by the real interaction", "neither appendfile nor writefile is available", "Players", "open_menu_hint", "waiting", "Damage", "Text", "Players", "พิมพ์โครงสร้าง GameState.GetData2().MineGame เพื่อหา key ที่ remote ต้องใช้", "[Remotes] === total: ", " parts", "[Handlers] === start (open the relevant menus first) ===", "ExitButton", "ItemName", "core/logger", "Label", "autoSell", "[GuiDump] === visible MineGui tree ===", "", "core/logger", "Modules", "languageIndex", "VirtualUser", ")", "ProximityPrompt", "ความเร็ววิ่ง", "core/parse_money", "AutoPlace", " ชิ้น", "games/my_anime_mine/auto_mine", "[AutoPlace] placed ", "[AntiAFK] enabled - watching Player.Idled", "  (debug.getupvalues unavailable)", "autoChestMinItems", "Players", "games/my_anime_mine/debug_dump_sell_cards", "MineBuySkill", "Frame", "games/my_anime_mine/get_best_miner", "UIListLayout", "ไปขายทุกกี่วินาที (ต้องเปิดเมนูทุกครั้ง)", "MineEquipSlot", "Done", "antiAfk", "[AutoUpgrade] bought ", "Auto Upgrade: รอบเช็ค", "[Probe] === end ===", "/config.json", "Inspect", "UpgradeButton", "PlayerGui", "games/my_anime_mine/game_api", "core/dispatch", "core/humanoid", "มุดแมพ (Noclip)", ") ===", " chars) -> ", "PlayerGui", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "CoreGui", "TextLabel", "[^%d%.]", "')", "games/my_anime_mine/main", "Detail", ") didn't take, trying the next one", "X", "TextLabel", "ขนาดเมนู", "TextButton", "อ่านว่าปุ่มพวกนี้ยิง remote ไหนด้วย arg อะไร (เปิดเมนูที่จะตรวจค้างไว้ก่อนกด)", "Content", "TextButton", "leaderstats", "", "ProximityPrompt", ": ", "[AutoPlace] ", "core/get_place_id", "TextLabel", "Auto Mine: ความเร็ว", "PlayerGui", "core/logger", "^Node_", "core/teleport", "ฟาร์ม", "auto", "core/teleport", "Label", "ไล่อัปเกรด miner ทุกกี่วินาที", " (power ", "TextLabel", "Backpack", "fill", " | ", "games/my_anime_mine/debug_dump_sell_cards", "[AntiAFK] disabled", "[Interaction] ClickDetector clicked: ", "core/click", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "[Probe] === start ===", "Card", "Workspace", "HttpService", "TextLabel", "ScreenGui", " ===", "table", ": ", "noclip", "TextLabel", "เปิดหน้า Upgrades แล้วซื้อ node ที่ปลดได้ในทุกโซน เรียงจากถูกไปแพง", "Badge", "Auto Chest: รอบเช็ค", "autoMineClicks", "[ChestDump] plot has no Chest", "TextLabel", "TextButton", "nothing to place or upgrade", "Done", "games/my_anime_mine/auto_research", "costs ", "getgc", ": ", "core/interaction_logger", "Instance", "games/my_anime_mine/debug_dump_remotes", "MineSellItems", "en", "open_menu_hint", "table", "[AutoPlace] ", "upgrade", "Frame", "GuiObject", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "HumanoidRootPart", "", "Container", ".lua", "[Handlers] ", "MekPro/decompiled_", "core/click", "core/anti_afk", "[Handlers] getconnections isn't available in this executor", "[AutoChest] disabled", "ProximityPrompt", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "Scroll", "Pad", ")", "Players", "  ", "TextButton", "  ", " = ", "[Interaction] logger disabled", "Auto Place: รอบเช็ค", "Tree", "=", "[Bb]uy", "[Probe] ", "PlayerGui", " '", "defined at ", "TakeButton", "🏠", "Speed", "no plot", "MekPro", "decompile", "[AutoChest] enabled (collects once >= ", "  [upvalue] ", "Players", "core/logger", " items (now ", "หาว่าจำนวนของใน chest ถูกเก็บไว้ที่ไหน จะได้เช็คได้โดยไม่ต้องเปิด UI", "[Interaction] ProximityPrompt triggered: ", "Humanoid", "Players", "sl", "", " ===", "core/interaction_logger", "[AutoChest] waiting: ", "ClickDetector", "[ChestDump] === end ===", "PlayerScripts", "[AutoResearch] ", "ลอย (Float)", "CoreUI", "[AutoPlace] nothing in the hotbar to place", "", "walkSpeed", ",", "Nodes", "[MyAnimeMine] === Node: ", "MouseButton1Click", "number", "Humanoid", "SkillTreeFullFrame", "auto", "core/ui", " items)", "core/logger", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "", "games/my_anime_mine/debug_probe_env", "เช็คจำนวนของจากป้ายบน chest เงียบๆ ถึงเกณฑ์แล้วค่อยไปเก็บ (ไม่รบกวนตอนยังไม่ถึง)", "[AutoPlace] disabled", "autoSellInterval", "[Probe] === executor capabilities ===", "TextLabel", "This is not a supported place.", "ไม่ซื้อ node ที่แพงกว่านี้", "games/my_anime_mine/get_current_plot", "?", "BasePart", "Frame", "interactionLogger", "autoPlaceInterval", "บิน (Fly)", "Auto Chest", "'", "Cost", "[ChestDump] not standing in any plot", "SlotFocusFrame", "TextLabel", "Tool", "ChestCapacity", "[MyAnimeMine] === Dump start ===", "[^%.]+", "Players", "locked slot", "Done", "core/logger", "Search functions...", "[Handlers] === ", "UpgradePart", "Header", "  (debug.getconstants unavailable)", "core/parse_money", "เปิดหน้า research ทุกกี่วินาที", "เปิดหน้าอัปเกรดของ miner ที่วางอยู่ แล้วซื้อ Damage/Speed/Mining ที่ราคาไม่เกินงบ", "unreadable", ")", " (ActionText=", "games/my_anime_mine/debug_inspect_handlers", "Auto Mine", "อังกฤษ", "[Scheduler] turn: ", "StatsPanel", "Unit Damage UpgradeButton", " not found", "[Handlers] ", "Close", "games/my_anime_mine/get_current_plot", "RemoteEvent", "ปุ่มเปิดเมนู", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "Auto Place Miner", "", " (", "[AutoUpgrade] ", "Content", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "th", "", "core/config", "GuiService", "[Probe] ", "Pips", "games/my_anime_mine/auto_chest", "Canvas", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "AutoSell", "getrenv", ")", "[GameAPI] couldn't load the game's modules: ", "ValueLabel", "autoResearch", " ===", ")", "[MyAnimeMine] no nodes present right now", " = ", "getloadedmodules", "Anti-AFK", "[ChestDump] === ReplicatedStorage.", "VirtualInputManager", "games/my_anime_mine/get_plot_parts", "autoMineSpeed", "games/my_anime_mine/get_plot_parts", "Content", "[Probe] PlayerScripts.UI.MineGame not found", "", "PlayerGui", "SelectAllButton", "no", "", " = ", " items", "BasePart", "MineGame", "Dump", "enabled", "AutoMine", "[Interaction] Button activated: ", "core/teleport", "menuScale", "UpgradesButton", "UICorner", "MineUpgradeCharacter", "TextButton", "/", "core/anti_afk", "[AutoSell] disabled", "[AutoChest] MineTakeChest had no effect (", "Sidebar", "games/registry", " (", "[SellCards] === total cards: ", " attributes ===", "games/my_anime_mine/debug_dump_remotes", "Dump", "Max", " ", " ===", " stack(s)", "GuiObject", "  [attr] ", "Content", "[Config] couldn't read ", "[MyAnimeMine] no holders found", "[BestMiner] slot ", "Place", "core/logger", "Done", "core/logger", "core/click", "", " = ", "/", "prompt disabled", "Badge", "My Anime Mine · Mining Hub", "^x(%d+)$", "Debug: Dump Backpack Tool", "MineGui", "[Remotes] ", "?", "[Logger] failed to write ", "Chest", "ScrollingFrame", " '", "TextButton", "core/get_leaderstat", "Main", "LeftBar", "  [attr] ", "GuiButton", " (MaxActivationDistance=%s)", "Zones", "menuScale", "write reported success but ", "StatsPanel", "ValueLabel", "Players", "table", " (", "games/my_anime_mine/main", "write reported success but the line isn't in ", "[AutoPlace] fireproximityprompt error: ", ": ", " -> ", "core/i18n", ") for ", "core/logger", "Sell SelectAllButton", "PlotIndex", "core/config", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "Holders", " = ", "ClickDetector", "MekPro", "", "Crew", "enabled", " (power ", " still empty after placing ", "Check", "TextLabel", "", "getscripts", "Damage", "autoChestInterval", " ", "Done", "core/logger", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "Auto Chest: เกณฑ์เก็บ", "Auto Sell", ": ", "⚙", "games/my_anime_mine/game_api", "HumanoidRootPart", "Players", "Debug: Probe Executor + Scripts", ")", "core/get_me", " connection(s)", "no isfile/readfile available to verify the write", "en-us", "string", " ===", "MekPro.log", "search_placeholder", "TabBar", "Instance", "autoMine", "[StateDump] === end ===", "'", "core/get_place_id", " on ", "[AntiAFK] idle detected, simulating input", "games/my_anime_mine/auto_place", "[AutoMine] ", ": ", "%$%s*([%d,%.]+)%s*(%a*)", "string", "RemoteEvent", "ClickDetector", " (", "MineSlotFocusClient", "PlayerGui", "UserInputService", "Empty miner slot", "core/parse_money", "Open menu", "Workspace", "  [attr] ", "OwnerUserId", "games/my_anime_mine/debug_dump_chest", "TextButton", " '", " '", "[StateDump] === MineGame ===", "[Handlers]   ", "Chest TakeButton", "en", "core/logger", ": ", "JumpPower (ปกติ 50)", "  ", "SkillTreeFullFrame", "[Interaction] Key pressed: %s (gameProcessed=%s)", " while selecting", "Locked miner slot", "ตั้งค่า", "s", "[GuiDump] === end ===", "/s", "MineSellClient", "[AutoPlace] upgrading ", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", "UIStroke", "AutoResearch", "%$", "Frame", "[GuiDump] MineGui not found", "TextLabel", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "Dump", "button has no size on screen", " (", "ย่อขยายเมนูทั้งชุด", "BuyButton", "  ", "TextLabel", "getnilinstances", "AutoChest", "core/dispatch", "games/my_anime_mine/game_api", "RemoteEvent", "getconnections", "", "", "games/my_anime_mine/get_current_plot", "^([%d,]+)$", "BodyVelocity", "=empty", "Damage", "Dump", "'", "ReplicatedStorage", "TextButton", "function", "[AutoResearch] nothing affordable under the spend cap", "Interaction Logger", "Search", "games/my_anime_mine/get_plot_parts", " doesn't exist", "Backpack", "[Interaction] logger enabled - go click/interact with things now", "games/my_anime_mine/auto_research", "[Probe] decompile unavailable - can't read the game's source this way", "Actions", "[AutoPlace] drifted out of range of ", "MineGui", "games/my_anime_mine/auto_sell", "SellFrame", "action '", "ProximityPrompt", "[Probe] couldn't decompile ", "disabled", ":", "debug.getupvalues", "  ", " for ", "'", "Sell SellButton", "games/my_anime_mine/auto_upgrade", "HumanoidRootPart", "waiting", "TextButton", ": not found", "Debug: Current Plot", " (", "games/registry", "Detail", "Content", "table", "TextLabel", "Frame", "games/my_anime_mine/get_plot_parts", "^%$", "TextLabel", "/", "games/my_anime_mine/get_best_miner", "core/logger", "UserInputService", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "s", "", "=locked(", "", "games/my_anime_mine/debug_dump", "TextLabel", "[ChestDump] === Player subtree ===", "[StateDump] no mine data (is the game API loaded?)", "games/my_anime_mine/debug_dump_state", "(nothing to do)", "Players", " power ", "games/my_anime_mine/auto_chest", " (Text=", "Auto Research (Plot)", "PlayerGui", "auto", "อ่านป้ายบน chest ทุกกี่วินาที", " x", "แรงกระโดด", "core/i18n", "UI", "ValueBase", "Mining", "UIPadding", ": ", " ", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "games/my_anime_mine/auto_mine", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "TextButton", "Folder", "MineGui", "", "  ", "Content", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "TextButton", " '", "เดินทะลุกำแพงและพื้น", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "[Scheduler] ", "[MyAnimeMine] === Tool: ", "Frame", "jumpPower", "[AutoPlace] couldn't pick up ", "[AutoChest] collected ", "en", " ===", "[ChestDump] === Chest subtree ===", "Dump", "table(", "Auto Research: รอบเช็ค", "MineChestClient", "UICorner", "UIListLayout", "s", "games/my_anime_mine/debug_dump_state", "  [const] ", "ไทย", "ไม่ซื้ออะไรที่แพงกว่านี้", "x", "SlotFocusFrame", "AutoUpgrade", "RemoteFunction", "[attr] ", "UIScale", "Players", "core/click", " = ", "MineSpeed", "", "getcallbackvalue", "table", "SellFrame", "ดีบัก", "[Handlers]   #", "TextButton", "disabled", "Content", "ตามเครื่อง", "core/scheduler", "th", "", "GuiButton", "games/my_anime_mine/debug_dump", "", "SellStand", "[AutoResearch] no BuyButton after selecting ", "debug.getproto", "Mining", "hookfunction", "[Config] couldn't write ", "works from anywhere - dropping the teleport", " (prompt says '", "RunService", "th", " (Text=", " error: ", "Probe", "  [attr] ", "[Handlers] === end ===", "auto", "Content", "table", ")", "sale didn't go through", "Frame", "Frame", "MineHUDClient", "Auto Mine: คลิกต่อรอบ", "MineGui", "Content", "Frame", "[AutoChest] can't read the chest yet", "Debug: Dump Visible GUI", "ReplicatedStorage", "Frame", "Dump", "UpgradeButton", "enabled", "[MyAnimeMine] === Holder: ", "core/ui", "  ", "[AutoPlace] holders: ", "[AutoSell] ", " from ", "เช็คว่า Potassium ทำอะไรได้บ้าง และลอง decompile script ของเกมเพื่อหา remote จริง", "MineSkillTreeClient", "[AutoSell] enabled", "core/logger", "\n", "fill", "SellFrame", "UserInputService", "[Probe] decompiled ", "Debug: Dump Node/Holder", "[MekPro] settings file: ", "unreadable", "-", "autoUpgradeMaxSpend", ") from ", "Workspace", "ClickDetector", "RemoteFunction", " remotes", "MineTakeChest", "UIListLayout", "Error: ", "ValueBase", "disabled", "table", ")", "ItemFrame", "[AutoResearch] upgrades menu didn't open", "[%X] ", "games/my_anime_mine/get_current_plot", "Debug: Inspect Button Handlers", "games/my_anime_mine/debug_dump_gui", "WalkSpeed (ปกติ 16)", "Scroll", "TextBox", "[GameAPI] SendEvent failed: ", "core/logger", "[AutoPlace] picked up ", "write failed", "List", "games/my_anime_mine/debug_inspect_handlers", "core/logger", " ", " = ", "HotbarFrame", "Text", "games/my_anime_mine/auto_place", "\n", "MineHotbarClient", "BodyPosition", "$", ") -> ", "games/my_anime_mine/debug_dump_chest", " = ", "Auto Research: งบต่อครั้ง", "Auto Upgrade: งบต่อครั้ง", " cards", "Lock", "core/logger", "$", "ค้นหาฟังก์ชัน...", "games/my_anime_mine/game_api", "(", "function", "", "autoChest", "MineGui", "games/my_anime_mine/get_current_plot", "TextLabel", "SellButton", "Debug: Dump All Remotes", "s", "Players", "Players", "Debug: Dump Chest Data", "[AutoSell] MineSellItems ", "games/my_anime_mine/get_current_plot", "fly", "games/my_anime_mine/auto_sell", "core/logger", "Debug: Dump Sell Cards", "core/humanoid", "Container", "table", "🐛", "search_placeholder", "core/scheduler", "RemoteFunction", "autoUpgradeInterval", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "Auto Sell: รอบขาย", "core/i18n", " (Text=", "All", "Players", "core/logger", "", ": ", "Players", "Content", ") on ", "Label", "Scroll", "autoResearchInterval", "ExitButton", "s", "autoUpgrade", "games/my_anime_mine/debug_probe_env", "autoResearchMaxSpend", "float", "Content", "[ChestDump] === plot ", "Frame", "debug.getconstants", "games/my_anime_mine/get_plot_parts", "no tool", "BottomPanels", "Debug: Dump Game State", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "Dump", "Loot", "", "games/my_anime_mine/debug_dump_gui", "Workspace", "autoPlace", "games/my_anime_mine/get_plot_parts", "%", "core/logger", "TextLabel", "ClickDetector", "string", "Plots", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "  ", "ReplicatedStorage", "BottomPanels", "log failed", "Auto Upgrade Unit", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "[SellCards] Card #", "th", "core/logger", "table", "'", "[AutoPlace] slot ", "getscriptbytecode", "MineWorld", "boolean", "Research BuyButton", ")", ",", "Activated", "core/logger", "Content", "ChestFrame", "BuyButton", "[Uu]nlock", ")", "Players", "[MyAnimeMine] not standing in any plot", "ProximityPrompt", " = ", "[AutoResearch] bought ", "getsenv", "'", "Players", "sold ", "RunService", "not found", "languageIndex", "function"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(322)] = function()
return function(import)
local log = import(S(212))
local Players = game:GetService(S(187))
local VirtualUser = game:GetService(S(30))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(39))
        connection = player.Idled:Connect(function()
            log(S(432))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(104))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(150)] = function()
local VirtualInputManager = game:GetService(S(296))
local GuiService = game:GetService(S(277))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(481)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(387)] = function()
return function(import)
local HttpService = game:GetService(S(111))
local getPlaceId = import(S(430))
local log = import(S(805))
local ROOT_FOLDER = S(392)
local placeFolder = ROOT_FOLDER .. S(545) .. tostring(getPlaceId())
local filePath = placeFolder .. S(55)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(375) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(339) .. filePath .. S(131) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(643) .. filePath .. S(380) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(60)] = function()
return function(import)
    local getPlaceId = import(S(86))
    local registry = import(S(536))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(221))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(363)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(81))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(417)] = function()
local Players = game:GetService(S(15))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(207))
    local backpack = player:WaitForChild(S(100))
    return player, humanoid, backpack
end
return getMe
end
modules[S(2)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(61)] = function()
local Players = game:GetService(S(42))
local RunService = game:GetService(S(845))
local UserInputService = game:GetService(S(443))
local Workspace = game:GetService(S(110))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(186))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(144))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(497))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(309)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(225)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(727))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(382)] = function()
local Players = game:GetService(S(159))
local STRINGS = {
    en = {
        search_placeholder = S(244),
        open_menu_hint = S(446),
    },
    th = {
        search_placeholder = S(738),
        open_menu_hint = S(266),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(420)
    if locale:sub(1, 2):lower() == S(274) then
        return S(633)
    end
    return S(136)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(209) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(132)] = function()
return function(import)
local Players = game:GetService(S(750))
local Workspace = game:GetService(S(801))
local UserInputService = game:GetService(S(685))
local log = import(S(27))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(185) .. describe(prompt)
            .. S(254) .. prompt.ActionText .. S(31))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(105) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(314) .. describe(button)
            .. S(648) .. tostring(button.Text) .. S(253))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(32)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(391)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(635)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(164))
        return
    end
    connections = {}
    log(S(511))
    local playerGui = Players.LocalPlayer:WaitForChild(S(89))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(83)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(694)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(367)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(463),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(458)] = function()
local LOG_FILE = S(423)
local function log(message)
    local line = os.date(S(706)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(145))
            end
            appendfile(LOG_FILE, line .. S(682))
        elseif writefile then
            local existing = S(494)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(725))
        else
            error(S(9))
        end
        if not (isfile and readfile) then
            error(S(419))
        end
        if not isfile(LOG_FILE) then
            error(S(371) .. LOG_FILE .. S(509))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(378) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(358) .. LOG_FILE .. S(84) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(445)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(421) then
        return nil
    end
    local number, suffix = text:match(S(436))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(828), S(200))))
    if not value then
        return nil
    end
    if suffix ~= S(393) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(632)] = function()
return function(import)
local RunService = game:GetService(S(646))
local log = import(S(343))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork)
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(592) .. task_.name .. S(649) .. tostring(err))
    end
end
local function anyExclusiveBusy()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            return true
        end
    end
    return false
end
local function pickExclusive()
    for _, candidate in ipairs(exclusiveTasks) do
        if candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork()) then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(258) .. (currentTask and currentTask.name or S(559)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(95)] = function()
local Players = game:GetService(S(181))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(413))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(210)] = function()
return function(import)
local i18n = import(S(570))
local UserInputService = game:GetService(S(548))
local THEME = {
    BrandName = S(177),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(318))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(605))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(574))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(77))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(551)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(473))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(124))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(357)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(555))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(306)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(360))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(47))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(87))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(658))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(399))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(220))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(118))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(287))
            if not valueLbl then
                valueLbl = Instance.new(S(235))
                valueLbl.Name = S(373)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(351))
            if not badge then
                badge = Instance.new(S(75))
                badge.Name = S(120)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(362))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(664))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(45))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(532))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(799)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(804)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(541))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(226))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(161))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(214)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(668))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(698))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(588))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(14)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(113))
    screenGui.Name = S(198)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(67))
    local main = Instance.new(S(659))
    main.Name = S(364)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(617))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(594))
    tabBar.Name = S(425)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(606))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(790))
    header.Name = S(247)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(628))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(544))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(486))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(275)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(80))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(74)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(125))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(690)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(712))
    searchBox.Name = S(507)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(763))
    searchBox.Text = S(189)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(476))
    content.Name = S(788)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(141))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(68))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(137))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(806))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(723)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(637) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(720) .. (item.Subtitle or S(347))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(424))
        footerLabel.Text = i18n.T(S(11))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(562)] = function()
return function(import)
local gameApi = import(S(59))(import)
local log = import(S(719))
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(179) .. minItemsToCollect .. S(211))
    else
        log(S(153))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoChest.HasWork()
    local count = gameApi.ChestCount()
    return count ~= nil and count >= minItemsToCollect
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(689) then
            log(S(665))
            lastReported = S(252)
        end
        return
    end
    if count < minItemsToCollect then
        if lastReported ~= S(12) then
            log(S(192) .. count .. S(321) .. minItemsToCollect .. S(308))
            lastReported = S(531)
        end
        return
    end
    lastReported = nil
    busy = true
    task.spawn(function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(597) .. count .. S(183) .. after .. S(835))
        else
            log(S(324) .. count
                .. S(381) .. tostring(after) .. S(656))
        end
        busy = false
    end)
end
return autoChest
end
end
modules[S(37)] = function()
return function(import)
local getCurrentPlot = import(S(223))
local getPlotParts = import(S(542))
local log = import(S(345))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(807), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(434) .. (enabled and S(395) or S(701)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(433)] = function()
return function(import)
local Players = game:GetService(S(414))
local getCurrentPlot = import(S(264))
local getPlotParts = import(S(508))
local getMinerSlots = import(S(46))(import)
local teleport = import(S(315))
local click = import(S(346))
local log = import(S(830))
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(444)
local LOCKED_SLOT_TEXT = S(465)
local SLOT_KEYS = {
    Enum.KeyCode.One, Enum.KeyCode.Two, Enum.KeyCode.Three,
    Enum.KeyCode.Four, Enum.KeyCode.Five, Enum.KeyCode.Six,
    Enum.KeyCode.Seven, Enum.KeyCode.Eight, Enum.KeyCode.Nine,
}
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(157))
    return pad and pad:FindFirstChildWhichIsA(S(520), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(478)) then
            local digits = descendant.Text:match(S(496))
            if digits then
                return tonumber((digits:gsub(S(202), S(742))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(112)) and descendant.Text:match(S(543)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(241)
    end
    if not prompt.Enabled then
        return true, S(350)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(129) .. price
    end
    local action = prompt.ActionText or S(584)
    if action:match(S(834)) or action:match(S(168)) or action:match(S(475)) then
        return true, S(519) .. action .. S(821)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(530))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(379) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied = {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(552) .. why .. S(416))
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(498))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local power = readPlacedPower(holder) or 0
                table.insert(summary, holder.Name .. S(167) .. prompt.ObjectText .. S(740) .. power .. S(290))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = power,
                })
            end
        end
    end
    local line = table.concat(summary, S(102))
    if line ~= lastSurvey then
        log(S(675) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    return { empty = empty, occupied = occupied }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(101), survey.empty[1], candidates[1]
    end
    local weakest = survey.occupied[1]
    if weakest and candidates[1].power > weakest.power then
        return S(140), weakest, candidates[1]
    end
    return nil
end
local autoPlace = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(85) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(217))
    else
        lastAttempt = 0
        log(S(66))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.HasWork()
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(388),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(prompt, miner)
    local key = SLOT_KEYS[miner.slot]
    if not key then
        return false
    end
    click.Key(key)
    for _ = 1, 8 do
        task.wait(0.1)
        if prompt.ActionText and prompt.ActionText:find(S(342), 1, true) then
            return true
        end
    end
    return false
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(199))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        if holdMiner(prompt, candidate) then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(515) .. holder.Name .. S(464))
            return false
        end
        log(S(822) .. candidate.slot .. S(376) .. candidate.name
            .. S(73))
    end
    miner = miner or candidates[1]
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(139) .. holder.Name .. S(397) .. miner.name)
        return false
    end
    log(S(38) .. miner.name .. S(396) .. miner.power
        .. S(778) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(596) .. entry.name .. S(677) .. entry.holder.Name
            .. S(645) .. tostring(entry.prompt.ActionText) .. S(70))
        return false
    end
    log(S(715) .. entry.name .. S(98) .. entry.power
        .. S(692) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local action, target, best = findNextAction()
    if not action then
        reason(S(126))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(683) then
        task.spawn(function()
            runFillJob(target)
        end)
        return
    end
    log(S(471) .. target.holder.Name .. S(410) .. target.name
        .. S(270) .. target.power .. S(729) .. best.name .. S(440) .. best.power .. S(703))
    task.spawn(function()
        runUpgradeJob(target)
    end)
end
return autoPlace
end
end
modules[S(128)] = function()
return function(import)
local Players = game:GetService(S(776))
local parseMoney = import(S(34))
local click = import(S(619))
local log = import(S(547))
local checkInterval = 30
local maxSpend = 100000
local function getMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(442))
    return playerGui and playerGui:FindFirstChild(S(583))
end
local function getTreeFrame()
    local mineGui = getMineGui()
    return mineGui and mineGui:FindFirstChild(S(208))
end
local function getUpgradesButton()
    local mineGui = getMineGui()
    local leftBar = mineGui and mineGui:FindFirstChild(S(365))
    local content = leftBar and leftBar:FindFirstChild(S(79))
    local container = content and content:FindFirstChild(S(760))
    return container and container:FindFirstChild(S(317))
end
local function zoneTabs(frame)
    local sidebar = frame:FindFirstChild(S(325))
    local list = sidebar and sidebar:FindFirstChild(S(717))
    if not list then
        return {}
    end
    local tabs = {}
    for _, tab in ipairs(list:GetChildren()) do
        if tab:IsA(S(451)) then
            local content = tab:FindFirstChild(S(654))
            local locked = content and content:FindFirstChild(S(735))
            if not locked then
                local label = content and content:FindFirstChild(S(96))
                table.insert(tabs, {
                    button = tab,
                    name = (label and label.Text ~= S(302) and label.Text) or tab.Name,
                })
            end
        end
    end
    return tabs
end
local function affordableNodes(frame)
    local tree = frame:FindFirstChild(S(166))
    local canvas = tree and tree:FindFirstChild(S(281))
    if not canvas then
        return {}
    end
    local nodes = {}
    for _, node in ipairs(canvas:GetChildren()) do
        if node:IsA(S(320)) and node.Name:match(S(91)) then
            local content = node:FindFirstChild(S(777))
            local cost = content and content:FindFirstChild(S(232))
            local price = cost and parseMoney(cost.Text)
            if price and price <= maxSpend then
                local pips = content:FindFirstChild(S(279))
                table.insert(nodes, {
                    button = node,
                    name = node.Name:sub(6),
                    price = price,
                    priceText = cost.Text,
                    pips = pips and pips.Text or S(224),
                })
            end
        end
    end
    table.sort(nodes, function(a, b)
        return a.price < b.price
    end)
    return nodes
end
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(196) .. (enabled and S(671) or S(522)))
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyNode(frame, node)
    click.Real(node.button)
    task.wait(0.4)
    local detail = frame:FindFirstChild(S(537))
    local buyButton = detail and detail:FindFirstChild(S(484))
    if not buyButton then
        log(S(639) .. node.name)
        return false
    end
    local content = buyButton:FindFirstChild(S(272))
    local label = content and content:FindFirstChild(S(23))
    if label and label.Text == S(332) then
        return false
    end
    click.Real(buyButton)
    task.wait(0.5)
    log(S(840) .. node.name .. S(327) .. node.pips .. S(383) .. node.priceText)
    return true
end
local function runJob()
    busy = true
    local button = getUpgradesButton()
    if not button then
        busy = false
        return
    end
    click.Real(button)
    task.wait(0.8)
    local frame = getTreeFrame()
    if not (frame and frame.Visible) then
        log(S(705))
        busy = false
        return
    end
    local bought = 0
    for _, tab in ipairs(zoneTabs(frame)) do
        if not enabled then
            break
        end
        click.Real(tab.button)
        task.wait(0.5)
        for _, node in ipairs(affordableNodes(frame)) do
            if not enabled then
                break
            end
            if buyNode(frame, node) then
                bought = bought + 1
            end
        end
    end
    if bought == 0 then
        log(S(505))
    end
    local close = frame:FindFirstChild(S(263)) or frame:FindFirstChild(S(782))
    if close then
        click.Real(close)
    end
    busy = false
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoResearch
end
end
modules[S(517)] = function()
return function(import)
local Workspace = game:GetService(S(693))
local gameApi = import(S(490))(import)
local teleport = import(S(92))
local log = import(S(182))
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(323))
    else
        lastAttempt = 0
        log(S(680))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.HasWork()
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(638))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(154), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(753)
                .. (sold and S(644)
                         or S(1)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(676) .. (sold and (S(844) .. #keys .. S(335)) or S(657)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    task.spawn(runJob)
end
return autoSell
end
end
modules[S(529)] = function()
return function(import)
local Players = game:GetService(S(10))
local getCurrentPlot = import(S(495))
local getPlotParts = import(S(803))
local parseMoney = import(S(249))
local click = import(S(106))
local log = import(S(736))
local STAT_NAMES = { S(13), S(175), S(573) }
local checkInterval = 20
local maxSpend = 100000
local function getFocusFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(65))
    local mineGui = playerGui and playerGui:FindFirstChild(S(355))
    return mineGui and mineGui:FindFirstChild(S(234))
end
local function findUpgradeClick(holder)
    local upgradePart = holder:FindFirstChild(S(246))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(193))
end
local function occupiedHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return {}
    end
    local found = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClick(holder)
        if detector then
            table.insert(found, { holder = holder, detector = detector })
        end
    end
    return found
end
local function readStat(scroll, statName)
    local row = scroll:FindFirstChild(statName)
    local button = row and row:FindFirstChild(S(57))
    local content = button and button:FindFirstChild(S(630))
    local label = content and content:FindFirstChild(S(779))
    if not label then
        return nil
    end
    return {
        name = statName,
        button = button,
        price = parseMoney(label.Text),
        text = label.Text,
    }
end
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(271) .. (enabled and S(312) or S(629)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxSpend(amount)
    maxSpend = math.max(amount, 0)
end
local function buyAffordableStats(entry)
    local frame = getFocusFrame()
    local panels = frame and frame:FindFirstChild(S(794))
    local statsPanel = panels and panels:FindFirstChild(S(372))
    local scroll = statsPanel and statsPanel:FindFirstChild(S(711))
    if not scroll then
        log(S(3) .. entry.holder.Name)
        return 0
    end
    local bought = 0
    for _ = 1, #STAT_NAMES do
        local cheapest = nil
        for _, statName in ipairs(STAT_NAMES) do
            local stat = readStat(scroll, statName)
            if stat and stat.price and stat.price <= maxSpend then
                if not cheapest or stat.price < cheapest.price then
                    cheapest = stat
                end
            end
        end
        if not cheapest then
            break
        end
        click.Real(cheapest.button)
        log(S(52) .. cheapest.name .. S(526) .. cheapest.text
            .. S(431) .. entry.holder.Name)
        bought = bought + 1
        task.wait(0.4)
    end
    return bought
end
local function closeFrame()
    local frame = getFocusFrame()
    local exit = frame and frame:FindFirstChild(S(20))
    if exit then
        click.Real(exit)
        task.wait(0.3)
    end
end
local function runJob()
    busy = true
    for _, entry in ipairs(occupiedHolders()) do
        if not enabled then
            break
        end
        fireclickdetector(entry.detector)
        task.wait(0.8)
        local frame = getFocusFrame()
        if frame and frame.Visible then
            buyAffordableStats(entry)
            closeFrame()
        end
    end
    busy = false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    task.spawn(runJob)
end
return autoUpgrade
end
end
modules[S(554)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(400)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(171) .. child.Name .. S(231)
        if child:IsA(S(838)) then
            line = line .. string.format(
                S(587),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(439)) then
            line = line .. string.format(S(368), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(265)) or child:IsA(S(615)) then
            line = line .. S(8)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(651) .. attrName .. S(620) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(485))
    end
end
return dumpInstance
end
modules[S(730)] = function()
return function(import)
local Players = game:GetService(S(374))
local ReplicatedStorage = game:GetService(S(812))
local getCurrentPlot = import(S(754))
local getPlotParts = import(S(792))
local log = import(S(773))
local function dumpAttributes(instance, indent)
    for name, value in pairs(instance:GetAttributes()) do
        log(indent .. S(616) .. name .. S(721) .. tostring(value))
    end
end
local SKIP_NAMES = {
    PlayerGui = true,
    PlayerScripts = true,
    StarterGear = true,
}
local function dumpSubtree(root, indent)
    dumpAttributes(root, indent .. S(674))
    for _, child in ipairs(root:GetChildren()) do
        if not SKIP_NAMES[child.Name] then
            local line = indent .. child.ClassName .. S(361) .. child.Name .. S(501)
            if child:IsA(S(572)) then
                line = line .. S(307) .. tostring(child.Value)
            elseif child:IsA(S(746)) or child:IsA(S(503)) then
                line = line .. S(770) .. tostring(child.Text) .. S(158)
            end
            log(line)
            dumpSubtree(child, indent .. S(525))
        end
    end
end
local function dumpChest()
    local plot = getCurrentPlot()
    if not plot then
        log(S(233))
        return 0
    end
    log(S(789) .. plot.Name .. S(329))
    dumpAttributes(plot, S(585))
    local parts = getPlotParts(plot)
    if not parts.Chest then
        log(S(123))
        return 0
    end
    log(S(600))
    dumpSubtree(parts.Chest, S(26))
    local player = Players.LocalPlayer
    log(S(556))
    dumpSubtree(player, S(493))
    local mine = ReplicatedStorage:FindFirstChild(player.Name)
    if mine then
        log(S(295) .. player.Name .. S(190))
        dumpSubtree(mine, S(634))
    end
    log(S(194))
    return #parts.Chest:GetDescendants()
end
return dumpChest
end
end
modules[S(709)] = function()
return function(import)
local Players = game:GetService(S(836))
local log = import(S(90))
local function dumpVisible(root, indent)
    indent = indent or S(82)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(336)) and child.Visible then
            local line = indent .. child.ClassName .. S(453) .. child.Name .. S(527)
            if child:IsA(S(540)) or child:IsA(S(581)) then
                line = line .. S(563) .. tostring(child.Text) .. S(827)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(448) .. attrName .. S(292) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(461))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(303))
    local mineGui = playerGui and playerGui:FindFirstChild(S(744))
    if not mineGui then
        log(S(477))
        return
    end
    log(S(25))
    dumpVisible(mineGui)
    log(S(468))
end
return dumpMineGui
end
end
modules[S(330)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(502))
local log = import(S(406))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(491)) or descendant:IsA(S(695)) then
            count = count + 1
            log(S(356) .. descendant.ClassName .. S(435) .. descendant:GetFullName())
        end
    end
    log(S(17) .. count .. S(289))
    return count
end
return dumpRemotes
end
end
modules[S(103)] = function()
return function(import)
local Players = game:GetService(S(560))
local log = import(S(714))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(58))
    local mineGui = playerGui and playerGui:FindFirstChild(S(7))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(625))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(300)) and sellFrame.Content:FindFirstChild(S(780))
    if not scroll then
        log(S(267))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(109) or card:IsA(S(142)) then
            count = count + 1
            log(S(817) .. count .. S(459) .. card.ClassName .. S(452) .. card.Name .. S(429))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(337) .. attrName .. S(731) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(811) .. child.ClassName .. S(589) .. child.Name .. S(842)
                if child:IsA(S(700)) then
                    line = line .. S(163) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(328) .. count .. S(599))
    return count
end
return dumpSellCards
end
end
modules[S(558)] = function()
return function(import)
local gameApi = import(S(412))(import)
local log = import(S(243))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(624) then
        local count = 0
        for _ in pairs(value) do
            count = count + 1
        end
        return S(602) .. count .. S(285)
    end
    return valueType .. S(333) .. tostring(value)
end
local function dumpTable(value, indent, depth)
    for key, entry in pairs(value) do
        log(indent .. tostring(key) .. S(348) .. describe(entry))
        if type(entry) == S(761) and depth > 0 then
            dumpTable(entry, indent .. S(162), depth - 1)
        end
    end
end
local function dumpState()
    local mine = gameApi.GetMineData()
    if not mine then
        log(S(557))
        return 0
    end
    log(S(454))
    dumpTable(mine, S(160), 2)
    log(S(428))
    return 1
end
return dumpState
end
end
modules[S(255)] = function()
return function(import)
local Players = game:GetService(S(772))
local log = import(S(384))
local function describe(value)
    local valueType = typeof(value)
    if valueType == S(133) then
        return value.ClassName .. S(576) .. value:GetFullName()
    end
    if valueType == S(138) then
        return S(539)
    end
    if valueType == S(848) then
        return S(741)
    end
    return valueType .. S(404) .. tostring(value)
end
local function isInteresting(value)
    local valueType = typeof(value)
    if valueType == S(426) then
        return value:IsA(S(438)) or value:IsA(S(765)) or value:IsA(S(582))
    end
    return valueType == S(808) or valueType == S(206) or valueType == S(825)
end
local function inspectFunction(fn, indent)
    local ok, source, line = pcall(function()
        return debug.info(fn, S(188))
    end)
    if ok and source then
        log(indent .. S(172) .. tostring(source) .. S(523) .. tostring(line))
    end
    local okConstants, constants = pcall(debug.getconstants, fn)
    if okConstants and type(constants) == S(115) then
        for _, constant in pairs(constants) do
            if isInteresting(constant) then
                log(indent .. S(609) .. describe(constant))
            end
        end
    else
        log(indent .. S(248))
    end
    local okUpvalues, upvalues = pcall(debug.getupvalues, fn)
    if okUpvalues and type(upvalues) == S(655) then
        for _, upvalue in pairs(upvalues) do
            if isInteresting(upvalue) then
                log(indent .. S(180) .. describe(upvalue))
            end
        end
    else
        log(indent .. S(40))
    end
end
local function inspectButton(label, button)
    if not button then
        log(S(148) .. label .. S(533))
        return
    end
    log(S(245) .. label .. S(535) .. button:GetFullName() .. S(63))
    if not getconnections then
        log(S(152))
        return
    end
    for _, signalName in ipairs({ S(829), S(205) }) do
        local okSignal, connections = pcall(getconnections, button[signalName])
        if okSignal and connections then
            log(S(262) .. signalName .. S(116) .. #connections .. S(418))
            for index, connection in ipairs(connections) do
                local fn = connection.Function
                if fn then
                    log(S(627) .. index)
                    inspectFunction(fn, S(455))
                end
            end
        end
    end
end
local function find(path)
    local playerGui = Players.LocalPlayer:FindFirstChild(S(565))
    local current = playerGui and playerGui:FindFirstChild(S(662))
    for _, name in ipairs(path) do
        if not current then
            return nil
        end
        current = current:FindFirstChild(name)
    end
    return current
end
local function inspectHandlers()
    log(S(19))
    inspectButton(S(456), find({ S(832), S(831), S(514), S(173) }))
    inspectButton(S(385), find({ S(518), S(586), S(304) }))
    inspectButton(S(528), find({ S(684), S(538), S(747) }))
    inspectButton(S(826), find({ S(462), S(72), S(833) }))
    local statsScroll = find({ S(613), S(813), S(259), S(156) })
    local damageRow = statsScroll and statsScroll:FindFirstChild(S(402))
    inspectButton(S(260), damageRow and damageRow:FindFirstChild(S(670)))
    log(S(652))
end
return inspectHandlers
end
end
modules[S(215)] = function()
return function(import)
local Players = game:GetService(S(618))
local log = import(S(22))
local CAPABILITIES = {
    S(178), S(823), S(401), S(130), S(293),
    S(492), S(623), S(841), S(284), S(791),
    S(524), S(640), S(642), S(487),
}
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(239)) do
        if type(target) ~= S(820) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(219))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(278) .. name .. S(390) .. ((ok and type(value) == S(504)) and S(5) or S(305)))
    end
end
local WANTED_SCRIPTS = {
    S(604),
    S(470),
    S(679),
    S(441),
    S(726),
    S(660),
}
local function tryDecompile()
    if not decompile then
        log(S(513))
        return
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(195))
    local ui = playerScripts and playerScripts:FindFirstChild(S(571))
    local mineGame = ui and ui:FindFirstChild(S(310))
    if not mineGame then
        log(S(301))
        return
    end
    for _, name in ipairs(WANTED_SCRIPTS) do
        local target = mineGame:FindFirstChild(name)
        if not target then
            log(S(169) .. name .. S(261))
        else
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(437) and #source > 0 then
                local path = S(149) .. name .. S(147)
                local written = pcall(writefile, path, source)
                log(S(686) .. name .. S(482) .. #source .. S(64)
                    .. (written and path or S(716)))
            else
                log(S(521) .. name .. S(775) .. tostring(source))
            end
        end
    end
end
local function probe()
    log(S(108))
    reportCapabilities()
    tryDecompile()
    log(S(54))
end
return probe
end
end
modules[S(739)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(667))
local log = import(S(757))
local api = {}
local gameState, mineInventory
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(28), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
    end)
    if not ok then
        loadFailed = true
        log(S(286) .. tostring(err))
        return false
    end
    return true
end
function api.IsReady()
    return modules()
end
function api.GetMineData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok or not data then
        return nil
    end
    return data.MineGame
end
function api.ChestCount()
    local mine = api.GetMineData()
    local chest = mine and mine.Chest
    if not chest then
        return nil
    end
    local total = 0
    for _, entry in pairs(chest) do
        total = total + (tonumber(entry) or entry.Count or entry.SellCount or 0)
    end
    return total
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(713) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(697))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(771), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(702) then
        return {}
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            table.insert(keys, entry.Key)
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(135), keys)
end
function api.EquipSlot(key)
    return send(S(49), key)
end
function api.BuySkill(nodeId, family)
    return send(S(44), nodeId, family)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(319), characterKey, stat == S(641) and S(621) or stat)
end
return api
end
end
modules[S(546)] = function()
return function(import)
local Players = game:GetService(S(843))
local log = import(S(681))
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(170))
    local mineGui = playerGui and playerGui:FindFirstChild(S(516))
    local hotbar = mineGui and mineGui:FindFirstChild(S(722))
    local content = hotbar and hotbar:FindFirstChild(S(338))
    return content and content:FindFirstChild(S(146))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(99)) then
            local count = descendant.Text:match(S(353))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(704))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(663))
    local nameLabel = content and content:FindFirstChild(S(21))
    local damageLabel = content and content:FindFirstChild(S(499))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(553) then
        return nil
    end
    return {
        name = nameLabel.Text,
        power = tonumber((damageLabel.Text:gsub(S(69), S(774)))) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(349) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(341) .. slot .. S(575) .. info.name
                        .. S(561) .. info.power .. S(568) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(707)] = function()
local Players = game:GetService(S(240))
local Workspace = game:GetService(S(447))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(824))
    local plots = mineWorld and mineWorld:FindFirstChild(S(809))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(449)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(299)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(389)),
        Nodes = plot:FindFirstChild(S(203)),
        Chest = plot:FindFirstChild(S(359)),
        Zones = plot:FindFirstChild(S(369)),
        Crew = plot:FindFirstChild(S(394)),
        Loot = plot:FindFirstChild(S(798)),
        PlotIndex = plot:GetAttribute(S(386)),
        ChestCapacity = plot:GetAttribute(S(237)),
    }
end
return getPlotParts
end
modules[S(377)] = function()
return function(import)
    local Players = game:GetService(S(751))
    local createWindow = import(S(673))(import)
    local i18n = import(S(769))
    local antiAfk = import(S(151))(import)
    local interactionLogger = import(S(191))(import)
    local scheduler = import(S(764))(import)
    local config = import(S(276))(import)
    local log = import(S(819))
    local window = createWindow({
        Subtitle = S(352),
    })
    local function persistentToggle(card, key, apply)
        local saved = config.Get(key, false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        if saved then
            apply(true)
        end
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local getCurrentPlot = import(S(745))
    local getPlotParts = import(S(297))
    local dumpInstance = import(S(636))
    local dumpRemotes = import(S(134))(import)
    local dumpSellCards = import(S(43))(import)
    local dumpGui = import(S(800))(import)
    local dumpChest = import(S(450))(import)
    local inspectHandlers = import(S(718))(import)
    local probeEnv = import(S(785))(import)
    local dumpState = import(S(608))(import)
    local humanoid = import(S(759))
    local autoMine = import(S(579))(import)
    local autoPlace = import(S(724))(import)
    local autoChest = import(S(280))(import)
    local autoSell = import(S(756))(import)
    local autoUpgrade = import(S(6))(import)
    local autoResearch = import(S(512))(import)
    scheduler.RegisterStandalone(S(313), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(488), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(35), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(614), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy)
    scheduler.RegisterExclusive(S(474), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy)
    scheduler.RegisterExclusive(S(283), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy)
    scheduler.Start()
    local farmTab = window:AddTab(S(93), S(174))
    persistentToggle(farmTab:AddCard(
        S(256),
        S(580)
    ), S(427), autoMine.SetEnabled)
    persistentSlider(farmTab, S(88), S(273),
        1, 30, S(298), 5, autoMine.SetCyclesPerSecond, S(469))
    persistentSlider(farmTab, S(661), S(578),
        1, 10, S(122), 1, autoMine.SetClicksPerCycle, S(612))
    persistentToggle(farmTab:AddCard(
        S(268),
        S(591)
    ), S(802), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(165), S(407),
        1, 60, S(228), 3, autoPlace.SetCheckInterval, S(607))
    persistentToggle(farmTab:AddCard(
        S(230),
        S(216)
    ), S(743), autoChest.SetEnabled)
    persistentSlider(farmTab, S(408), S(479),
        50, 3000, S(41), 100, autoChest.SetMinItems, S(36))
    persistentSlider(farmTab, S(121), S(567),
        1, 60, S(403), 3, autoChest.SetCheckInterval, S(550))
    persistentToggle(farmTab:AddCard(
        S(409),
        S(577)
    ), S(24), autoSell.SetEnabled)
    persistentSlider(farmTab, S(768), S(48),
        10, 300, S(218), 45, autoSell.SetCheckInterval, S(467))
    persistentToggle(farmTab:AddCard(
        S(815),
        S(251)
    ), S(784), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(53), S(97),
        5, 300, S(766), 20, autoUpgrade.SetInterval, S(749))
    persistentSlider(farmTab, S(733), S(611),
        1000, 1000000, S(691), 100000, autoUpgrade.SetMaxSpend, S(737))
    persistentToggle(farmTab:AddCard(
        S(564),
        S(119)
    ), S(288), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(603), S(250),
        10, 600, S(781), 30, autoResearch.SetCheckInterval, S(783))
    persistentSlider(farmTab, S(732), S(222),
        1000, 1000000, S(786), 100000, autoResearch.SetMaxSpend, S(728))
    local debugTab = window:AddTab(S(626), S(762))
    persistentToggle(
        debugTab:AddCard(
            S(506),
            S(810)
        ),
        S(227),
        interactionLogger.SetEnabled
    )
    local debugCard = debugTab:AddCard(S(534), S(155))
    local checkBtn
    checkBtn = debugCard:AddButton(S(398), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(846)
            log(S(837))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(213),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = debugTab:AddCard(
        S(687),
        S(143)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(601), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(176)
            return
        end
        local ok, err = log(S(238))
        if not ok then
            dumpBtn.Text = S(814)
            dumpCard:SetSubtitle(S(699) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(204) .. firstNode.Name .. S(422))
            dumpInstance(firstNode, log)
        else
            log(S(291))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(672) .. firstHolder.Name .. S(334))
            dumpInstance(firstHolder, log)
        else
            log(S(340))
        end
        dumpBtn.Text = S(242)
    end)
    local toolDumpCard = debugTab:AddCard(
        S(354),
        S(816)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(331), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(510))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(236))
        if not tool then
            toolDumpBtn.Text = S(793)
            return
        end
        log(S(593) .. tool.Name .. S(114))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(366) .. attrName .. S(839) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(50)
    end)
    local remoteDumpCard = debugTab:AddCard(
        S(748),
        S(796)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(500), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(696)
    end)
    local sellCardsDumpCard = debugTab:AddCard(
        S(758),
        S(107)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(311), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(734)
    end)
    local guiDumpCard = debugTab:AddCard(
        S(666),
        S(767)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(797), function()
        dumpGui()
        guiDumpBtn.Text = S(405)
    end)
    local chestDumpCard = debugTab:AddCard(
        S(752),
        S(184)
    )
    local chestDumpBtn
    chestDumpBtn = chestDumpCard:AddButton(S(480), function()
        local count = dumpChest()
        chestDumpBtn.Text = count .. S(18)
    end)
    local handlersCard = debugTab:AddCard(
        S(708),
        S(78)
    )
    local handlersBtn
    handlersBtn = handlersCard:AddButton(S(56), function()
        inspectHandlers()
        handlersBtn.Text = S(4)
    end)
    local probeCard = debugTab:AddCard(
        S(415),
        S(678)
    )
    local probeBtn
    probeBtn = probeCard:AddButton(S(650), function()
        probeEnv()
        probeBtn.Text = S(127)
    end)
    local stateCard = debugTab:AddCard(
        S(795),
        S(16)
    )
    local stateBtn
    stateBtn = stateCard:AddButton(S(669), function()
        dumpState()
        stateBtn.Text = S(344)
    end)
    humanoid.Start()
    persistentSlider(debugTab, S(33), S(710),
        8, 200, S(201), 16, humanoid.SetWalkSpeed, S(269))
    persistentSlider(debugTab, S(569), S(460),
        30, 300, S(595), 50, humanoid.SetJumpPower, S(622))
    persistentToggle(
        debugTab:AddCard(S(229), S(549)),
        S(755), humanoid.SetFlying
    )
    persistentToggle(
        debugTab:AddCard(S(62), S(590)),
        S(117), humanoid.SetNoclip
    )
    persistentToggle(
        debugTab:AddCard(S(197), S(472)),
        S(787), humanoid.SetFloating
    )
    local settingsTab = window:AddTab(S(466), S(411))
    persistentToggle(
        settingsTab:AddCard(S(294), S(282)),
        S(51),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(316), 100)
    settingsTab:AddSlider(S(76), S(483), 50, 150, savedScale, function(value)
        config.Set(S(370), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(29), 1)
    settingsTab:AddSegmented({ S(631), S(257), S(610) }, savedLanguage, function(index, value)
        local code = ({ S(653), S(598), S(647) })[index]
        config.Set(S(847), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(94), S(457), S(818) })[savedLanguage] or S(566))
    log(S(688) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        humanoid.Stop()
    end)
end
end
modules[S(326)] = function()
return {
    [79389059854988] = S(71),
}
end
import(S(489))(import)
