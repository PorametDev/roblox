-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"core/debug_tab", "ต่ำสุดก่อน", "core/debug_dump_remotes", "Auto Research: Core ต่อเมื่อสาย Zone ตันแล้ว", "x", "", "autoPlaceUnlockBudget", " (Text=", "[MekPro] build ", " files", "games/my_anime_mine/auto_upgrade", ")", "Auto Sell", "UIScale", "open_menu_hint", "Speed", "empty", " for ", "autoPlaceEquipBest", "AutoClaim", "ไล่ซื้อทุกกี่วินาที", "index rewards are disabled in this game build", "core/logger", "[AutoClaim] idle: ", "Speed", "core/parse_money", "write reported success but the line isn't in ", "MineEquipPickaxe", "", "MineBuyPickaxe", "AutoUpgrade", "ขนาดเมนู", " has been busy for ", "autoResearchKeepMoney", "เช็ครางวัลที่เก็บได้ทุกกี่วินาที", " still empty after placing ", ") gave nothing", "slot ", "unlock", "research", "core/dispatch", "getscripts", "Auto Place: รอบดึงตัวเก่ง", "[Scheduler] turn: ", "unlock", "[AutoPlace] nothing in the hotbar to place", " (", "games/my_anime_mine/get_plot_parts", "UserInputService", "Frame", "autoMineClicks", "hookfunction", "", "autoUpgradePickaxe", " (power ", "getscriptbytecode", "TextButton", ")", "[Interaction] Key pressed: %s (gameProcessed=%s)", "table", ")", "", "Mutation: หยุดเมื่อได้ x", "JumpPower (ปกติ 50)", "Run", "ถูกสุดก่อน", "core/teleport", "[AutoClaim] claimed ", "[AutoUpgrade] bought pickaxe #", "')", "core/debug_dump_gui", "ValueLabel", ")", "[AutoPlace] ", "HumanoidRootPart", "games/my_anime_mine/auto_mine", "", "ItemFrame", "games/my_anime_mine/get_best_miner", ") ", "[AutoSell] disabled", "เช็คช่องว่าง/ตัวที่ควรเปลี่ยนทุกกี่วินาที", "อ่านป้ายบน chest ทุกกี่วินาที", "Auto Chest", " ", "AutoResearch", "อังกฤษ", "AutoSell", "[AutoUpgrade] nothing to upgrade (no miners placed?)", "games/my_anime_mine/main", "Auto Place: รอบเช็ค", "lowest", "ความเร็ววิ่ง", "languageIndex", "both Zone and Core buying are switched off", "autoResearch", "HumanoidRootPart", "Workspace", "Main", "s", "; ", "[AutoPlot] ", "แท็บ Core ใช้ remote คนละตัวต่อหนึ่งอัปเกรด - เมื่อก่อนไม่ได้ซื้อเลย", "=empty", "core/run_job", "MineEquipBest", "My Anime Mine · Mining Hub", "รู้ราคาต่อครั้งแล้ว (GetMutationRollCost) แต่ยังเป็นการพนัน - ปิดไว้เป็นค่าเริ่มต้น", "", "^([%d,]+)$", "ClickDetector", "Limited", "Container", "decompile", "MineWorld", "GetHolderLevel", "[Config] couldn't write ", "autoChestInterval", "noclip", "Zones", "maxed", "[AutoSell] ", " (", "cost", "core/teleport", "en", "autoResearchMaxPerPass", "Players", "TextLabel", "ตัวที่ base damage สูงกว่าตัวที่อ่อนที่สุดบน holder = ของไว้อัปเกรด ไม่ใช่ของเหลือ", "AutoUpgrade", "ล็อกความสูงไว้ที่ระดับปัจจุบัน", " failed)", "no isfile/readfile available to verify the write", ": ", "open_menu_hint", "autoResearchCore", "0 = เลือกอัตโนมัติ (ตามสูตรของเกมเอง)", "UIListLayout", "", ")", "no mine data", "อัปเกรด Damage/Speed/Mining ของ miner ทุกตัวผ่าน remote ตรง (ไม่เปิด UI)", "บิน (Fly)", "AutoMine", "debug.getupvalues", "AutoPlot", "core/teleport", "core/logger", " '", "Hauler", "[Interaction] logger disabled", "locked slot", "PlayerGui", "%.0f", "AutoPlace", " ", "table", "none", "MekPro/decompiled", "โซนแพงกว่า research มาก จึงมีเงินสำรองของตัวเอง", "[AutoPlace] upgrading ", "MineSpeed", "UIStroke", "ไทย", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "ปุ่มเปิดเมนู", "[Decompile] === start ===", " -> ", "MekPro", "[Interaction] ClickDetector clicked: ", "", "TextLabel", "core/debug_decompile", " ", "/", "ฟาร์ม", "core/anti_afk", "server didn't equip it", "affordable - waiting for a turn", "MineSpeed", " node(s) looked buyable but the server refused", "GetCharacterUpgradeCost", "core/get_me", "[BestMiner] slot ", "[GameAPI] SendEvent failed: ", "AutoResearch", "autoSell", "[AutoChest] enabled (collects once >= ", "/s", "Auto Mine: คลิกต่อรอบ", " (power ", "Frame", "games/registry", " stack(s)", "ProximityPrompt", "", "🏠", "[AutoResearch] ", "Players", " -> ", "s - carrying on without it", "[AutoResearch] cheapest open node: ", "autoPlotLatest", " = ", "CharMaxLevel", " error: ", "core/config", "[AutoClaim] ", "games/my_anime_mine/game_api", "Auto Place: ดึงตัวเก่งขึ้น hotbar", "Frame", "[Bb]uy", "วาปไปกด E ที่แท่นที่ขึ้น 'Locked miner slot / Unlock' - ทำหลังสุดเมื่อช่องที่มีเต็มแล้ว", "ZoneBoardGui", "[AutoChest] can't read the chest yet", "UIListLayout", "games/my_anime_mine/auto_research", "ค้นหาฟังก์ชัน...", "core/run_job", " from ", "เลือก miner ที่พลังสูงสุดจาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "games/my_anime_mine/game_api", "auto", "Players", "Text", "Auto Chest: เกณฑ์เก็บ", "", "TextLabel", "ถ้าเผลอย้ายไปโซนเก่า จะย้ายกลับมาโซนที่ลึกที่สุดให้", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", "Frame", "M", "BodyPosition", "none", "Auto Research: ซื้อสาย Core", "core/logger", "[AutoPlace] couldn't hold anything - not pressing E", "Mutation: เพดานราคาต่อครั้ง", "[AutoPlace] unlocked ", " costs ", "RunService", "fly", "TabBar", "LocalScript", "ยิง MineEquipBest ของเกม - Auto Place วางได้เฉพาะตัวที่อยู่บน hotbar เท่านั้น", "ตั้งค่า", "getsenv", " -> ", " = ", "เก็บเมื่อของใน chest ถึงกี่ชิ้น", "บันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "RunService", "autoPlaceUpgradeWeak", "core/logger", "games/my_anime_mine/auto_sell", " ", " items", "ย่อขยายเมนูทั้งชุด", "Auto Upgrade: Pickaxe ที่จะซื้อ", "table", "UICorner", "string", "function", "core/debug_decompile", "TextLabel", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "Workspace", "core/scheduler", "[AutoPlot] ", "/", ")", " ", "[AutoResearch] ", "games/my_anime_mine/game_api", "Search functions...", " -> ", "Holders", "games/my_anime_mine/game_api", " (chest holds ", "Modules", "%", "ยิงซ้ำกี่ครั้งต่อ Node ในแต่ละรอบ", "%.3g", "core/logger", "M", "x", " costs ", "[Remotes] ", "[Config] couldn't read ", "getconnections", "Damage", "table", "empty", "GuiButton", "%.3g", "fill", " didn't go through (out of stock or too expensive)", "autoClaim", "debug.getproto", "Auto Plot: รอบเช็ค", "แพงสุดก่อน", ")", "AutoPlace", "needs us at the stand - keeping the teleport", " power ", "", "s", " (x", ") on ", "Crew", "/", "core/debug_dump_gui", "Frame", "Speed ก่อน", "s", "[AutoUpgrade] MineBuyPickaxe ", " (", "games/my_anime_mine/auto_research", " team power", "=", "LayerCollector", "autoSellMaxWait", "autoUpgradeMaxPerPass", "table", "ไม่ใช้เงินต่ำกว่าจำนวนนี้", ", you have ", "[Scheduler] ", "ซื้อเฉพาะ node ที่เปิดแล้วและยังไม่เต็ม ตามกฎเดียวกับปุ่ม Buy ในเกม", "[AntiAFK] enabled - watching Player.Idled", "CoreGui", " - upgrade the miners first", "[Logger] failed to write ", ")", "autoMineSpeed", "ไปที่ SellStand แล้วกด Select All + Sell อัตโนมัติทุก 45 วินาที", "[Decompile] === end ===", "Auto Research: ซื้อสาย Zone", "[AutoPlot] now mining ", "s", "x", "UIPadding", "[Interaction] Button activated: ", "BasePart", ") needs ", "Players", "Backpack", "Auto Claim (Index)", "AutoPlace", "table", "getgc", "Content", "(nothing to do)", "[Decompile] PlayerScripts not found", "[^%.]+", "games/my_anime_mine/auto_upgrade", "core/config", "Mining ก่อน", "number", "sale didn't go through", " (cost ", "TextButton", ")", ")", "core/scheduler", ",", "[AntiAFK] idle detected, simulating input", " ", "Pad", "HttpService", "autoUpgradeKeepMoney", "TextLabel", "PlayerGui", "=locked(", "TextButton", " not bought yet (", "games/my_anime_mine/auto_plot", "unreadable", " -> x", "ต้นไม้ของแต่ละโซน (The Underground ฯลฯ)", "/", "[AutoResearch] Core ", "core/logger", "MineWorld", ", ", "MekPro", "disabled", " ", "เช็คความสามารถของ executor แล้ว decompile ทุก script ใน PlayerScripts ลงไฟล์", "Exclusive", "ReplicatedStorage", "[AutoChest] waiting: ", "/", "[AutoResearch] ", "jumpPower", "Players", "PlayerGui", "core/logger", "TextLabel", "", "/", "  ", ", have ", "CoreUI", "Auto Sell: รอบขาย", "may need us at the stand - trying that", "autoClaimMaxPerPass", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "[AutoMine] ", "ตามเครื่อง", "", "[GuiDump] PlayerGui not found", "TextLabel", "core/parse_money", "", "autoUpgradeInterval", "[AutoPlace] drifted out of range of ", "games/my_anime_mine/auto_chest", "04ddda6", "MekPro", "core/logger", "  [attr] ", "badge says ", " (", "[AutoPlace] slot ", "Empty miner slot", " mutation x", "th", "PlotIndex", "costs ", "core/run_job", ": State(", "slot not in the hotbar", "TextButton", " for ", "core/logger", "Core", "th", "[AutoSell] enabled", "บินด้วย WASD + Space/Ctrl ตามทิศกล้อง", "table", " (", "core/debug_dump_remotes", "core/logger", "[Scheduler] ", "This is not a supported place.", "] job failed: ", "core/get_place_id", "string", "เดินทะลุกำแพงและพื้น", " (", "table", "(ไม่มีตัวนี้ให้ซื้อด้วยเงิน)", "Workspace", "menuScale", "ProximityPrompt", "TextLabel", "[AutoPlace] unlocking ", "TextButton", " items)", "autoPlotInterval", "GuiButton", "autoResearchZones", "[AutoPlace] holders: ", "ChestCapacity", "core/i18n", "core/i18n", "PlayerScripts", ")", "GuiService", "auto", "yes", "core/get_leaderstat", "debug.getconstants", "[Interaction] ProximityPrompt triggered: ", "TextButton", "[^%w]+", "auto", "Auto Claim: รอบเช็ค", "core/logger", "[AutoPlace] Equip Best: hotbar's strongest ", "สุ่มจนกว่าตัวคูณจะถึงค่านี้แล้วเลิกยุ่งกับตัวนั้น", "SellStand", "core/run_job", "Players", "core/humanoid", "neither appendfile nor writefile is available", "Content", "HotbarFrame", ")", "autoPlot", "autoPlace", "string", "s", "[AutoUpgrade] ", "core/logger", "2026-09-21 12:56:55", " doesn't exist", "Auto Place Miner", "AutoSell", "Auto Place: ปลดล็อกช่อง miner", "core/run_job", " (Text=", " claimable but none could be resolved", "\n", "Players", "function", "enabled", "nothing to place or upgrade", "cost", "ปุ่มบนป้ายใช้ remote เดียวกับการสลับโซน - server เป็นคนตัดสินว่าซื้อหรือย้าย", "table", " -> ", "ลอย (Float)", "Auto Plot: ซื้อโซนถัดไป", "disabled", "enabled", "^x(%d+)$", "OwnerUserId", "CharMineSpeedMaxLevel", "Players", "getloadedmodules", "", "[AutoPlot] couldn't switch to ", "core/logger", "sold ", "search_placeholder", ")", "autoUpgradeMutationTarget", "th", "GetCharacterMineSpeedUpgradeCost", "BodyVelocity", "[AutoClaim] ", "s", "getnilinstances", "อ่านจำนวนจาก state ตรงๆ ถึงเกณฑ์แล้วยิง remote เก็บ (ไม่วาป ไม่เปิด UI)", "games/my_anime_mine/get_current_plot", "ดีบัก", "%$%s*([%d,%.]+)%s*(%a*)", "[AutoUpgrade] ", "table", "กันไม่ให้ใช้เงินหมดในรอบเดียว", "Auto Upgrade: แจก Pickaxe ให้ miner", "Core ราคาหลักล้าน สาย Zone หลักหมื่น - ถ้าใช้เงินก้อนเดียวกัน Core จะกินเงินที่ Zone รออยู่", "core/dispatch", "M", "GetCharacterSpeedUpgradeCost", "#", "fill", "autoSellMaxWait", "[AutoResearch] ", "ItemName", "games/my_anime_mine/auto_claim", ") ", " reward(s) looked claimable but none went through", "autoResearchCoreLast", "มุดแมพ (Noclip)", "  ", "AutoChest", "pickaxe มีค่าเฉพาะตอนมีคนถือ - ซื้อเกินจำนวนช่องที่ปลดแล้วก็เปลืองเปล่า", "autoUpgradeEquipPickaxe", "เช็คโซนทุกกี่วินาที", "autoResearchInterval", ")", "core/run_job", ", built ", " team power, you have ", "?", "search_placeholder", "core/i18n", "TextLabel", "Auto Upgrade: ซื้อสูงสุดต่อรอบ", "0 = ไม่จำกัด", "AutoChest", "Workspace", "BasePart", "", "[AutoUpgrade] can't find workspace.PickaxesStand['Shop Stand']", "en", "Badge", "Players", "disabled", "core/ui", "core/logger", "prompt disabled", "Decompile Client Scripts", "[Scheduler] ", "เก็บรางวัล Index ที่ถึงเกณฑ์แล้วผ่าน remote ตรง (ไม่เปิด UI)", "core/logger", "[AutoChest] chest is empty", " x", "TextLabel", ")", "core/logger", "Players", "Damage", "Auto Place: เปลี่ยนตัวอ่อนออก", "core/logger", "RemoteFunction", "เกมเว้น 0.6 วิต่อการเก็บหนึ่งครั้ง", " ===", "enabled", "Dump", "Frame", "(", "M", "^%$", "%$", "Nodes", ": ", "core/get_place_id", "autoSellInterval", " (money ", "table", "core/logger", " waited ", "autoUpgradeMutationBudget", "GetHolderSpeedLevel", "MinePower", " (", "Plots", "nothing left to buy (", "core/run_job", "core/humanoid", " screen(s)) ===", " ชิ้น", "autoMine", " pickaxe(s) for ", "ClaimableTiers failed", "x", "/config.json", "ValueLabel", "nothing claimable", "[AutoResearch] idle: ", " still locked after paying ", "string", "s", "GuiService", "Auto Mine: ความเร็ว", "MineUpgradeCharacter", " (prompt says '", "[AutoPlace] enabled (fills empty holders, then upgrades weak ones)", "MineRollMutation", "⚙", "dev", "Mining", "[GameAPI] couldn't load the game's modules: ", "games/my_anime_mine/game_api", "slot past the usable cap", "needs ", "MineGui", "[Decompile] wrote ", "autoUpgradeRollMutation", "Auto Upgrade: รอบเช็ค", "[Decompile] decompile unavailable in this executor", "getrenv", "leaderstats", "[AutoPlace] couldn't pick up ", ") didn't take: ", "ถ้าโดนงานอื่นแซงเกินเวลานี้จะแทรกคิวเอง (มีผลหลังโหลดใหม่)", "\n", "table", "MineSelectZone", "List", "VirtualInputManager", " items (now ", "action '", "ซื้อโซนถัดไปและย้ายไปขุดโซนที่ลึกที่สุดที่มีอยู่เสมอ", "[AutoChest] collected ", "[AutoPlace] ", "[AutoChest] disabled", "core/anti_afk", "", "Frame", "Anti-AFK", "%.3g", "autoResearchCheapestFirst", "จำนวนรอบต่อวินาที (ยิ่งสูงยิ่งถี่)", "CharSpeedMaxLevel", ": ", "games/my_anime_mine/auto_chest", "games/my_anime_mine/auto_plot", "ร้าน pickaxe มีสต็อกจำกัดและรีสต็อกเป็นรอบ - ระบบจะซื้อเฉพาะตอนที่มีของจริง", "", "core/build_env", "%$", "[AutoPlace] picked up ", "[MekPro] settings file: ", "Frame", "Frame", "Search", " ", "Humanoid", "GuiObject", "games/my_anime_mine/get_best_miner", " but no unclaimed reward in its Track", "TextBox", "ยิง Equip Best ทุกกี่วินาที", "Workspace", "core/debug_interaction_logger", "[GuiDump] ", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "🐛", "Auto Research: รอบเช็ค", "disabled", "[AutoSell] MineSellItems ", "[AutoResearch] nothing open and affordable right now", "UserInputService", " #", "RemoteEvent", "Locked miner slot", " ", "games/my_anime_mine/get_current_plot", "games/my_anime_mine/get_current_plot", "All", "interactionLogger", "TextButton", "Text", "[Uu]nlock", "'", "modules not loaded", " ", "core/build_env", "Plots", "TextLabel", "autoPlotBuy", "Frame", "[AutoPlace] disabled", "string", "Auto Research (Plot)", "[AutoPlace] teleport didn't land: %.1f studs away (max %s)", "ReplicatedStorage", "table", "ไล่อัปเกรด miner ทุกกี่วินาที", ",", "TextButton", "string", "Players", "MineTakeChest", "autoUpgradePriority", " | ", "games/my_anime_mine/game_api", "%.0f", "[AutoUpgrade] nothing bought - can't afford the next upgrade yet", "games/my_anime_mine/auto_place", "%.3g", "^M(%d+)$", ": ", " (", " (", "PickaxesStand", "s", "th", "core/parse_money", ")", "autoChest", ",", "button has no size on screen", "TextLabel", "Dump Visible GUI", "MekPro", "Auto Chest: รอบเช็ค", "ZoneRow", "/", "%.3g", "TextLabel", "'", "games/my_anime_mine/game_api", "แจกตัวที่ multiplier สูงสุดก่อน ถือซ้ำได้ถ้ามีหลายอัน", "core/get_place_id", "upgrade", "table", "0 = ไม่จำกัด", "games/my_anime_mine/game_api", "[AutoChest] MineTakeChest had no effect (", "Shop Stand", "autoUpgradeLimitPickaxes", "autoChestMinItems", "Auto Place: เพดานราคาปลดช่อง", " '", "ScreenGui", "core/logger", "core/teleport", "games/registry", "ZoneBoard", "core/ui", " while selecting", "Auto Upgrade: ซื้อ Pickaxe ด้วย", "[AutoPlace] ", "TextLabel", "cheapest open node ", " wouldn't take ", "Auto Research: กันเงินไว้", "_", "[%X] ", "BasePart", "M", "disabled", "games/my_anime_mine/auto_claim", "games/my_anime_mine/auto_place", "[GuiDump] === end (", "core/logger", "autoPlaceUnlock", "Auto Sell: ไม่ขาย unit ที่ดีพอจะวาง", "Auto Upgrade: ไม่ซื้อเกินจำนวน holder", "([%d,%.]+)%s*(%a*)", "auto", "Root", " (x", "walkSpeed", "[GuiDump] === visible PlayerGui tree ===", "Badge", "ขายทุกกี่วินาที", "core/click", "VirtualUser", "", " refused - ", " possible holder(s) - not buying more", "autoPlaceInterval", "UserInputService", "", "Auto Plot: กันเงินไว้", ") ", "-", "enabled", "'", "BasePart", "Players", "function", "waiting", " -> ", "getcallbackvalue", "MekPro.log", "AutoClaim", "MineEquipSlot", "games/my_anime_mine/auto_sell", "core/run_job", "Content", "TextButton", "Loot", "OwnerUserId", "MineSpeed", "AutoPlace", "Frame", "autoUpgrade", " (", "Dump All Remotes", "[AutoUpgrade] pickaxe #", " (still ", "[AutoUpgrade] ", "Core buying is off", "/", ") -> ", "Auto Claim: เก็บสูงสุดต่อรอบ", "works from anywhere - dropping the teleport", "[Remotes] === total: ", ") ", " - ", "Hauler", "games/my_anime_mine/get_plot_parts", " remotes", "x", "[AutoPlot] unlocked ", "Chest", " script(s) to ", "ScrollingFrame", "ModuleScript", "Damage ก่อน", "autoUpgradePickaxeIndex", "[AutoUpgrade] ", "[", "autoPlaceEquipBestInterval", "AutoPlot", "autoPlotKeepMoney", "table", "needs ", "Humanoid", "UIListLayout", " ", "Header", "[AutoPlace] fireproximityprompt error: ", "Workspace", "X", "en", "TextLabel", "antiAfk", "", "s", "Auto Upgrade: สุ่ม Mutation", ".lua", "[AntiAFK] disabled", "works from anywhere - no teleport needed", "Auto Sell: รอคิวได้นานสุด", "games/my_anime_mine/main", "[AutoPlot] ", ") from ", "TextLabel", "", "[AutoUpgrade] every stat on every miner is maxed", " research is ", "float", "ปิด = เติมเฉพาะช่องว่าง ไม่ยุ่งกับตัวที่วางอยู่แล้ว", "HumanoidRootPart", "UICorner", " pickaxe ", "Dump", "Auto Upgrade Plot (Zone)", "", "games/my_anime_mine/auto_mine", "MineSellItems", "", "no", "[AutoPlace] placed ", "games/my_anime_mine/get_plot_parts", ")", "write reported success but ", "Interaction Logger", "ClickDetector", "menuScale", "core/debug_tab", "MineBuySkill", " (ActionText=", "research", "enabled", "core/debug_interaction_logger", "[Decompile] === executor capabilities ===", " screens", "autoSellProtectUnits", "WalkSpeed (ปกติ 16)", "ClickDetector", "languageIndex", "Auto Mine", "Players", "every open node is maxed", " (", ": ", "Damage", ", base ", "Character", ": ", "แรงกระโดด", "[Decompile] ", "ProximityPrompt", ")", "autoClaimInterval", "Zone buying is off", "table", "ProximityPrompt", " owned)", "[AutoUpgrade] ", "Auto Upgrade: กันเงินไว้", ": badge says ", "Open menu", "en-us", "TextLabel", "GetHolderMineSpeedLevel", "เปิดหน้าจอที่อยากดูค้างไว้ก่อน แล้วกดปุ่มนี้ - ไล่ทุก ScreenGui ที่เปิดอยู่", "autoUpgradePickaxeIndex", "[AutoUpgrade] ", "", " (", "table", "unreadable", "", "games/my_anime_mine/game_api", "lowest", "Auto Upgrade Unit", "AutoPlace", "[Interaction] logger enabled - go click/interact with things now", "Auto Research: ซื้อสูงสุดต่อรอบ", "s - jumping the queue", "waiting", "", "games/my_anime_mine/game_api", "Auto Plot: ใช้โซนล่าสุดเสมอ"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(178)] = function()
return function(import)
local log = import(S(802))
local Players = game:GetService(S(225))
local VirtualUser = game:GetService(S(815))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(330))
        connection = player.Idled:Connect(function()
            log(S(367))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(891))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(685)] = function()
return { Env = S(645), IsDev = true, Commit = S(419), BuiltAt = S(497) }
end
modules[S(814)] = function()
local VirtualInputManager = game:GetService(S(665))
local GuiService = game:GetService(S(470))
local click = {}
function click.Signal(button)
    return pcall(function()
        firesignal(button.Activated)
    end)
end
function click.Key(keyCode)
    return pcall(function()
        VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
    end)
end
function click.Real(button)
    local position = button.AbsolutePosition
    local size = button.AbsoluteSize
    if size.X <= 0 or size.Y <= 0 then
        return false, S(758)
    end
    local inset = GuiService:GetGuiInset()
    local x = position.X + size.X / 2 + inset.X
    local y = position.Y + size.Y / 2 + inset.Y
    return pcall(function()
        VirtualInputManager:SendMouseMoveEvent(x, y, game)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait(0.05)
        VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
end
return click
end
modules[S(357)] = function()
return function(import)
local HttpService = game:GetService(S(370))
local getPlaceId = import(S(770))
local log = import(S(615))
local ROOT_FOLDER = S(761)
local placeFolder = ROOT_FOLDER .. S(401) .. tostring(getPlaceId())
local filePath = placeFolder .. S(631)
local values = {}
local loaded = false
local function ensureFolders()
    if not (isfolder and makefolder) then
        return
    end
    if not isfolder(ROOT_FOLDER) then
        makefolder(ROOT_FOLDER)
    end
    if not isfolder(placeFolder) then
        makefolder(placeFolder)
    end
end
local function load()
    if loaded then
        return
    end
    loaded = true
    local ok, err = pcall(function()
        if isfile and isfile(filePath) then
            local decoded = HttpService:JSONDecode(readfile(filePath))
            if type(decoded) == S(158) then
                values = decoded
            end
        end
    end)
    if not ok then
        log(S(290) .. filePath .. S(748) .. tostring(err))
    end
end
local function save()
    local ok, err = pcall(function()
        ensureFolders()
        writefile(filePath, HttpService:JSONEncode(values))
    end)
    if not ok then
        log(S(117) .. filePath .. S(936) .. tostring(err))
    end
    return ok
end
local config = {}
function config.Get(key, default)
    load()
    local value = values[key]
    if value == nil then
        return default
    end
    return value
end
function config.Set(key, value)
    load()
    if values[key] == value then
        return
    end
    values[key] = value
    save()
end
function config.Path()
    return filePath
end
return config
end
end
modules[S(174)] = function()
return function(import)
local Players = game:GetService(S(396))
local log = import(S(237))
local CAPABILITIES = {
    S(114), S(56), S(42), S(351), S(522),
    S(291), S(832), S(248), S(656), S(474),
    S(146), S(300), S(52), S(535),
}
local OUT_FOLDER = S(160)
local function resolve(path)
    local target = getfenv and getfenv(0) or _G
    for part in path:gmatch(S(355)) do
        if type(target) ~= S(662) then
            return nil
        end
        target = rawget(target, part) or target[part]
    end
    return target
end
local function reportCapabilities()
    log(S(926))
    for _, name in ipairs(CAPABILITIES) do
        local ok, value = pcall(resolve, name)
        log(S(942) .. name .. S(205) .. ((ok and type(value) == S(507)) and S(472) or S(912)))
    end
end
local function dumpAll()
    if not decompile then
        log(S(655))
        return 0
    end
    local playerScripts = Players.LocalPlayer:FindFirstChild(S(468))
    if not playerScripts then
        log(S(354))
        return 0
    end
    pcall(makefolder, S(386))
    pcall(makefolder, OUT_FOLDER)
    local written = 0
    local failed = 0
    for _, target in ipairs(playerScripts:GetDescendants()) do
        if target:IsA(S(245)) or target:IsA(S(867)) then
            local ok, source = pcall(decompile, target)
            if ok and type(source) == S(729) and #source > 0 then
                local name = target:GetFullName():gsub(S(477), S(794))
                if pcall(writefile, OUT_FOLDER .. S(176) .. name .. S(890), source) then
                    written = written + 1
                else
                    failed = failed + 1
                end
            else
                failed = failed + 1
            end
        end
    end
    log(S(652) .. written .. S(865) .. OUT_FOLDER
        .. S(442) .. failed .. S(133))
    return written
end
local function run()
    log(S(168))
    reportCapabilities()
    local count = dumpAll()
    log(S(337))
    return count
end
return run
end
end
modules[S(313)] = function()
return function(import)
local Players = game:GetService(S(485))
local log = import(S(584))
local function dumpVisible(root, indent)
    indent = indent or S(887)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(694)) and child.Visible then
            local line = indent .. child.ClassName .. S(150) .. child.Name .. S(767)
            if child:IsA(S(592)) or child:IsA(S(476)) then
                line = line .. S(8) .. tostring(child.Text) .. S(528)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(422) .. attrName .. S(250) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(558))
        end
    end
end
local function dumpGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(397))
    if not playerGui then
        log(S(412))
        return 0
    end
    local screens = 0
    log(S(811))
    for _, screen in ipairs(playerGui:GetChildren()) do
        if screen:IsA(S(322)) and screen.Enabled and screen.Name ~= S(420) then
            screens = screens + 1
            log(S(701) .. screen.ClassName .. S(780) .. screen.Name .. S(720))
            dumpVisible(screen, S(402))
        end
    end
    log(S(801) .. screens .. S(625))
    return screens
end
return dumpGui
end
end
modules[S(443)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(732))
local log = import(S(255))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(710)) or descendant:IsA(S(599)) then
            count = count + 1
            log(S(289) .. descendant.ClassName .. S(680) .. descendant:GetFullName())
        end
    end
    log(S(856) .. count .. S(601))
    return count
end
return dumpRemotes
end
end
modules[S(700)] = function()
return function(import)
local Players = game:GetService(S(200))
local Workspace = game:GetService(S(699))
local UserInputService = game:GetService(S(820))
local log = import(S(598))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(475) .. describe(prompt)
            .. S(922) .. prompt.ActionText .. S(469))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(171) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(343) .. describe(button)
            .. S(503) .. tostring(button.Text) .. S(61))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(943)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(918)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(462)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(152))
        return
    end
    connections = {}
    log(S(969))
    local playerGui = Players.LocalPlayer:WaitForChild(S(154))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(196)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(111)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(295)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(59),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(1)] = function()
return function(import)
local interactionLogger = import(S(925))(import)
local dumpRemotes = import(S(3))(import)
local dumpGui = import(S(71))(import)
local decompileScripts = import(S(265))(import)
local humanoid = import(S(624))
local function build(window, persist)
    local tab = window:AddTab(S(538), S(703))
    persist.Toggle(
        tab:AddCard(
            S(917),
            S(267)
        ),
        S(716),
        interactionLogger.SetEnabled
    )
    local remoteCard = tab:AddCard(
        S(847),
        S(252)
    )
    local remoteBtn
    remoteBtn = remoteCard:AddButton(S(603), function()
        remoteBtn.Text = dumpRemotes() .. S(861)
    end)
    local guiCard = tab:AddCard(
        S(760),
        S(957)
    )
    local guiBtn
    guiBtn = guiCard:AddButton(S(906), function()
        guiBtn.Text = dumpGui() .. S(927)
    end)
    local decompileCard = tab:AddCard(
        S(586),
        S(389)
    )
    local decompileBtn
    decompileBtn = decompileCard:AddButton(S(65), function()
        decompileBtn.Text = decompileScripts() .. S(10)
    end)
    humanoid.Start()
    persist.Slider(tab, S(93), S(929),
        8, 200, S(810), 16, humanoid.SetWalkSpeed, S(960))
    persist.Slider(tab, S(941), S(64),
        30, 300, S(395), 50, humanoid.SetJumpPower, S(62))
    persist.Toggle(
        tab:AddCard(S(144), S(440)),
        S(243), humanoid.SetFlying
    )
    persist.Toggle(
        tab:AddCard(S(557), S(450)),
        S(119), humanoid.SetNoclip
    )
    persist.Toggle(
        tab:AddCard(S(514), S(132)),
        S(901), humanoid.SetFloating
    )
    return function()
        interactionLogger.SetEnabled(false)
        humanoid.Stop()
    end
end
return build
end
end
modules[S(41)] = function()
return function(import)
    local getPlaceId = import(S(611))
    local registry = import(S(784))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(446))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(473)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(657))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(184)] = function()
local Players = game:GetService(S(933))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(693))
    local backpack = player:WaitForChild(S(347))
    return player, humanoid, backpack
end
return getMe
end
modules[S(448)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(486)] = function()
local Players = game:GetService(S(595))
local RunService = game:GetService(S(253))
local UserInputService = game:GetService(S(49))
local Workspace = game:GetService(S(268))
local FLY_SPEED = 60
local humanoid = {}
local walkSpeed = nil
local jumpPower = nil
local flying = false
local noclip = false
local floating = false
local flyConnection, noclipConnection, floatConnection
local flyMover, floatMover
local respawnConnection
local function getCharacter()
    return Players.LocalPlayer.Character
end
local function getHumanoid()
    local character = getCharacter()
    return character and character:FindFirstChildOfClass(S(877))
end
local function getRoot()
    local character = getCharacter()
    return character and character:FindFirstChild(S(97))
end
function humanoid.SetWalkSpeed(value)
    walkSpeed = value
    local h = getHumanoid()
    if h then
        h.WalkSpeed = value
    end
end
function humanoid.SetJumpPower(value)
    jumpPower = value
    local h = getHumanoid()
    if h then
        h.UseJumpPower = true
        h.JumpPower = value
    end
end
local function stopFly()
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if flyMover then
        flyMover:Destroy()
        flyMover = nil
    end
    local h = getHumanoid()
    if h then
        h.PlatformStand = false
    end
end
local function startFly()
    stopFly()
    local root = getRoot()
    if not root then
        return
    end
    flyMover = Instance.new(S(532))
    flyMover.MaxForce = Vector3.new(1e5, 1e5, 1e5)
    flyMover.Velocity = Vector3.zero
    flyMover.Parent = root
    flyConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if not (currentRoot and flyMover) then
            return
        end
        local camera = Workspace.CurrentCamera
        local direction = Vector3.zero
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            direction = direction + camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            direction = direction - camera.CFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            direction = direction - camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            direction = direction + camera.CFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            direction = direction + Vector3.yAxis
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then
            direction = direction - Vector3.yAxis
        end
        flyMover.Velocity = direction.Magnitude > 0
            and direction.Unit * FLY_SPEED
            or Vector3.zero
    end)
end
function humanoid.SetFlying(state)
    flying = state
    if state then
        startFly()
    else
        stopFly()
    end
end
local function stopNoclip()
    if noclipConnection then
        noclipConnection:Disconnect()
        noclipConnection = nil
    end
    local character = getCharacter()
    if character then
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(796)) then
                part.CanCollide = true
            end
        end
    end
end
local function startNoclip()
    stopNoclip()
    noclipConnection = RunService.Stepped:Connect(function()
        local character = getCharacter()
        if not character then
            return
        end
        for _, part in ipairs(character:GetDescendants()) do
            if part:IsA(S(827)) and part.CanCollide then
                part.CanCollide = false
            end
        end
    end)
end
function humanoid.SetNoclip(state)
    noclip = state
    if state then
        startNoclip()
    else
        stopNoclip()
    end
end
local function stopFloat()
    if floatConnection then
        floatConnection:Disconnect()
        floatConnection = nil
    end
    if floatMover then
        floatMover:Destroy()
        floatMover = nil
    end
end
local function startFloat()
    stopFloat()
    local root = getRoot()
    if not root then
        return
    end
    local height = root.Position.Y
    floatMover = Instance.new(S(234))
    floatMover.MaxForce = Vector3.new(0, 1e5, 0)
    floatMover.P = 10000
    floatMover.D = 500
    floatMover.Position = Vector3.new(0, height, 0)
    floatMover.Parent = root
    floatConnection = RunService.Heartbeat:Connect(function()
        local currentRoot = getRoot()
        if currentRoot and floatMover then
            floatMover.Position = Vector3.new(
                currentRoot.Position.X, height, currentRoot.Position.Z)
        end
    end)
end
function humanoid.SetFloating(state)
    floating = state
    if state then
        startFloat()
    else
        stopFloat()
    end
end
function humanoid.IsFlying()
    return flying
end
function humanoid.IsNoclip()
    return noclip
end
function humanoid.IsFloating()
    return floating
end
local function reapply()
    task.wait(0.5)
    if walkSpeed then
        humanoid.SetWalkSpeed(walkSpeed)
    end
    if jumpPower then
        humanoid.SetJumpPower(jumpPower)
    end
    if flying then
        startFly()
    end
    if noclip then
        startNoclip()
    end
    if floating then
        startFloat()
    end
end
function humanoid.Start()
    if respawnConnection then
        return
    end
    respawnConnection = Players.LocalPlayer.CharacterAdded:Connect(reapply)
end
function humanoid.Stop()
    if respawnConnection then
        respawnConnection:Disconnect()
        respawnConnection = nil
    end
    stopFly()
    stopNoclip()
    stopFloat()
end
return humanoid
end
modules[S(570)] = function()
local Players = game:GetService(S(738))
local STRINGS = {
    en = {
        search_placeholder = S(276),
        open_menu_hint = S(953),
    },
    th = {
        search_placeholder = S(219),
        open_menu_hint = S(167),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(954)
    if locale:sub(1, 2):lower() == S(438) then
        return S(428)
    end
    return S(126)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(471) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(149)] = function()
local LOG_FILE = S(833)
local function log(message)
    local line = os.date(S(795)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(911))
            end
            appendfile(LOG_FILE, line .. S(505))
        elseif writefile then
            local existing = S(6)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(661))
        else
            error(S(487))
        end
        if not (isfile and readfile) then
            error(S(134))
        end
        if not isfile(LOG_FILE) then
            error(S(916) .. LOG_FILE .. S(498))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(27) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(333) .. LOG_FILE .. S(610) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(754)] = function()
local SUFFIXES = {
    K = 1e3,
    M = 1e6,
    B = 1e9,
    T = 1e12,
    Qa = 1e15,
    Qi = 1e18,
}
return function(text)
    if type(text) ~= S(636) then
        return nil
    end
    local number, suffix = text:match(S(539))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(366), S(577))))
    if not value then
        return nil
    end
    if suffix ~= S(400) then
        local multiplier = SUFFIXES[suffix] or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        if not multiplier then
            return math.huge
        end
        value = value * multiplier
    end
    return value
end
end
modules[S(484)] = function()
return function(import)
local log = import(S(782))
return function(name, job, clear)
    task.spawn(function()
        local ok, err = pcall(job)
        if not ok then
            log(S(871) .. name .. S(447) .. tostring(err))
        end
        clear()
    end)
end
end
end
modules[S(365)] = function()
return function(import)
local RunService = game:GetService(S(242))
local log = import(S(285))
local scheduler = {}
local standaloneTasks = {}
local exclusiveTasks = {}
local connection = nil
local currentTask = nil
local starvedOverride = nil
local overrideRan = false
function scheduler.RegisterStandalone(name, step, isEnabled)
    table.insert(standaloneTasks, { name = name, step = step, isEnabled = isEnabled })
end
local DEFAULT_MAX_WAIT = 45
function scheduler.RegisterExclusive(name, step, isEnabled, isBusy, hasWork, maxWait)
    if maxWait == nil then
        maxWait = DEFAULT_MAX_WAIT
    elseif maxWait == false then
        maxWait = nil
    end
    table.insert(exclusiveTasks, {
        name = name,
        step = step,
        isEnabled = isEnabled,
        isBusy = isBusy,
        hasWork = hasWork,
        maxWait = maxWait,
        waitingSince = nil,
    })
end
local function run(task_)
    local ok, err = pcall(task_.step)
    if not ok then
        log(S(445) .. task_.name .. S(207) .. tostring(err))
    end
end
local STUCK_AFTER = 25
local function anyExclusiveBusy()
    local now = tick()
    for _, task_ in ipairs(exclusiveTasks) do
        if task_.isBusy and task_.isBusy() then
            task_.busySince = task_.busySince or now
            if now - task_.busySince < STUCK_AFTER then
                return true
            end
            if not task_.reportedStuck then
                task_.reportedStuck = true
                log(S(587) .. task_.name .. S(33)
                    .. string.format(S(743), now - task_.busySince)
                    .. S(202))
            end
        else
            task_.busySince = nil
            task_.reportedStuck = nil
        end
    end
    return false
end
local function isReady(candidate)
    return candidate.isEnabled() and (not candidate.hasWork or candidate.hasWork())
end
local function pickExclusive()
    local now = tick()
    local ready = {}
    local starved = nil
    for _, candidate in ipairs(exclusiveTasks) do
        if isReady(candidate) then
            candidate.waitingSince = candidate.waitingSince or now
            table.insert(ready, candidate)
            if not starved and candidate.maxWait
                and now - candidate.waitingSince >= candidate.maxWait
            then
                starved = candidate
            end
        else
            candidate.waitingSince = nil
        end
    end
    if starvedOverride and (overrideRan or not isReady(starvedOverride)) then
        starvedOverride = nil
        overrideRan = false
    end
    if not starvedOverride and starved then
        starvedOverride = starved
        log(S(328) .. starved.name .. S(616)
            .. string.format(S(155), now - starved.waitingSince)
            .. S(971))
    end
    local chosen = starvedOverride or ready[1]
    if chosen then
        chosen.waitingSince = nil
    end
    return chosen
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        for _, task_ in ipairs(standaloneTasks) do
            if task_.isEnabled() then
                run(task_)
            end
        end
        if anyExclusiveBusy() then
            if starvedOverride and starvedOverride.isBusy
                and starvedOverride.isBusy()
            then
                overrideRan = true
            end
            return
        end
        local next_ = pickExclusive()
        if next_ ~= currentTask then
            currentTask = next_
            log(S(44) .. (currentTask and currentTask.name or S(353)))
        end
        if currentTask then
            run(currentTask)
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
    starvedOverride = nil
    overrideRan = false
    for _, task_ in ipairs(exclusiveTasks) do
        task_.waitingSince = nil
    end
end
return scheduler
end
end
modules[S(125)] = function()
local Players = game:GetService(S(521))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(75))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
modules[S(583)] = function()
return function(import)
local i18n = import(S(467))
local UserInputService = game:GetService(S(708))
local GuiService = game:GetService(S(638))
local THEME = {
    BrandName = S(170),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(262))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(904))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(342))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function containsPoint(element, position)
    local inset = GuiService:GetGuiInset()
    local topLeft = element.AbsolutePosition + inset
    local size = element.AbsoluteSize
    return position.X >= topLeft.X and position.X <= topLeft.X + size.X
        and position.Y >= topLeft.Y and position.Y <= topLeft.Y + size.Y
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local endConnection
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            if endConnection then
                endConnection:Disconnect()
            end
            endConnection = input.Changed:Connect(function()
                if input.UserInputState ~= Enum.UserInputState.End then
                    return
                end
                endConnection:Disconnect()
                endConnection = nil
                dragging = false
                if onClick and not moved and containsPoint(handle, input.Position) then
                    onClick()
                end
            end)
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    table.insert(connections, {
        Disconnect = function()
            if endConnection then
                endConnection:Disconnect()
                endConnection = nil
            end
        end,
    })
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(375))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(415)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(164))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(129))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(568)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(457))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(908)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(866))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(217))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(229))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(727))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(759))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(790))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(885))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(72))
            if not valueLbl then
                valueLbl = Instance.new(S(897))
                valueLbl.Name = S(632)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(812))
            if not badge then
                badge = Instance.new(S(266))
                badge.Name = S(580)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(736))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(212))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(844))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(362))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(411)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback, suffix)
        suffix = suffix or S(282)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. suffix)
        local track = Instance.new(S(604))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(50))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(839))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(821)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. suffix
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(689))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(878))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(434))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(226)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(781))
    screenGui.Name = S(404)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(331))
    local main = Instance.new(S(314))
    main.Name = S(99)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(14))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(674))
    tabBar.Name = S(244)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(139))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(232))
    header.Name = S(880)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(57))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(399))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(413))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(816)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(459))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(883)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(717))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(824)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(697))
    searchBox.Name = S(691)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(569))
    searchBox.Text = S(898)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(193))
    content.Name = S(352)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(690))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(955))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(15))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(372))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(718)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(172) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(388) .. (item.Subtitle or S(523))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(527))
        footerLabel.Text = i18n.T(S(136))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(418)] = function()
return function(import)
local gameApi = import(S(768))(import)
local log = import(S(383))
local runGuarded = import(S(431))(import)
local checkInterval = 3
local minItemsToCollect = 100
local autoChest = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastReported = nil
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReported = nil
    if enabled then
        lastAttempt = 0
        log(S(189) .. minItemsToCollect .. S(460))
    else
        log(S(671))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.IsBusy()
    return busy
end
function autoChest.SetMinItems(value)
    minItemsToCollect = math.max(value, 1)
end
function autoChest.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
local function threshold()
    local capacity = gameApi.ChestCapacity()
    if capacity and capacity > 0 then
        return math.min(minItemsToCollect, capacity)
    end
    return minItemsToCollect
end
function autoChest.HasWork()
    if not gameApi.ChestHasItems() then
        return false
    end
    local count = gameApi.ChestCount()
    return count ~= nil and count >= threshold()
end
function autoChest.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    local count = gameApi.ChestCount()
    if not count then
        if lastReported ~= S(963) then
            log(S(216))
            lastReported = S(378)
        end
        return
    end
    if not gameApi.ChestHasItems() then
        if lastReported ~= S(17) then
            log(S(590))
            lastReported = S(294)
        end
        return
    end
    local want = threshold()
    if count < want then
        if lastReported ~= S(830) then
            local capacity = gameApi.ChestCapacity()
            log(S(392) .. count .. S(271) .. want .. S(258)
                .. (capacity and (S(280) .. capacity .. S(755)) or S(228)))
            lastReported = S(972)
        end
        return
    end
    lastReported = nil
    busy = true
    runGuarded(S(559), function()
        gameApi.TakeChest()
        task.wait(0.5)
        local after = gameApi.ChestCount()
        if after and after < count then
            log(S(669) .. count .. S(666) .. after .. S(303))
        else
            log(S(775) .. count
                .. S(831) .. tostring(after) .. S(490))
        end
    end, function()
        busy = false
    end)
end
return autoChest
end
end
modules[S(799)] = function()
return function(import)
local gameApi = import(S(210))(import)
local log = import(S(589))
local runGuarded = import(S(623))(import)
local checkInterval = 30
local maxPerPass = 10
local autoClaim = {}
local enabled = false
local lastNote = nil
local busy = false
local lastAttempt = 0
function autoClaim.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastNote = nil
    log(S(209) .. (enabled and S(508) or S(798)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                autoClaim.HasWork()
            end
        end)
    end
end
function autoClaim.IsEnabled()
    return enabled
end
function autoClaim.IsBusy()
    return busy
end
function autoClaim.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoClaim.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoClaim.HasWork()
    local rewards, why = gameApi.ClaimableIndexRewards()
    if #rewards > 0 then
        lastNote = nil
        return true
    end
    if enabled and why and why ~= lastNote then
        lastNote = why
        log(S(24) .. why)
    end
    return false
end
local function stillClaimable(reward, list)
    for _, current in ipairs(list) do
        if current.category == reward.category
            and current.tierKey == reward.tierKey
            and current.index == reward.index
        then
            return true
        end
    end
    return false
end
local function runJob()
    busy = true
    local pending = gameApi.ClaimableIndexRewards()
    local claimed = 0
    for _, reward in ipairs(pending) do
        if not enabled or claimed >= maxPerPass then
            break
        end
        gameApi.ClaimIndexReward(reward)
        task.wait(0.7)
        if not stillClaimable(reward, gameApi.ClaimableIndexRewards()) then
            log(S(68) .. reward.category .. S(273)
                .. tostring(reward.tierKey) .. S(709) .. reward.index)
            claimed = claimed + 1
        end
    end
    if claimed == 0 and #pending > 0 then
        log(S(533) .. #pending .. S(555))
    end
    busy = false
end
function autoClaim.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if not autoClaim.HasWork() then
        return
    end
    runGuarded(S(834), runJob, function()
        busy = false
    end)
end
return autoClaim
end
end
modules[S(909)] = function()
return function(import)
local getCurrentPlot = import(S(537))
local getPlotParts = import(S(860))
local log = import(S(444))
local DEFAULT_CYCLES_PER_SECOND = 5
local DEFAULT_CLICKS_PER_CYCLE = 1
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(930), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
local cyclesPerSecond = DEFAULT_CYCLES_PER_SECOND
local clicksPerCycle = DEFAULT_CLICKS_PER_CYCLE
function autoMine.SetCyclesPerSecond(value)
    cyclesPerSecond = math.clamp(value, 1, 30)
end
function autoMine.SetClicksPerCycle(value)
    clicksPerCycle = math.clamp(value, 1, 10)
end
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(409) .. (enabled and S(825) or S(705)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < 1 / cyclesPerSecond then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            for _ = 1, clicksPerCycle do
                fireclickdetector(detector)
            end
        end
    end
end
return autoMine
end
end
modules[S(800)] = function()
return function(import)
local Players = game:GetService(S(828))
local getCurrentPlot = import(S(714))
local getPlotParts = import(S(914))
local getMinerSlots = import(S(695))(import)
local teleport = import(S(148))
local parseMoney = import(S(414))
local gameApi = import(S(279))(import)
local log = import(S(525))
local runGuarded = import(S(502))(import)
local checkInterval = 3
local EMPTY_SLOT_TEXT = S(426)
local LOCKED_SLOT_TEXT = S(711)
local upgradeWeakMiners = true
local useEquipBest = true
local equipBestInterval = 60
local unlockSlots = false
local unlockBudget = 0
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(369))
    return pad and pad:FindFirstChildWhichIsA(S(948), true)
end
local function isEmpty(prompt)
    return prompt.ObjectText == EMPTY_SLOT_TEXT
end
local function readPlacedPower(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(725)) then
            local digits = descendant.Text:match(S(110))
            if digits then
                return tonumber((digits:gsub(S(757), S(307))))
            end
        end
    end
    return nil
end
local function findPriceTag(holder)
    for _, descendant in ipairs(holder:GetDescendants()) do
        if descendant:IsA(S(571)) and descendant.Text:match(S(607)) then
            return descendant.Text
        end
    end
    return nil
end
local function isLocked(prompt, holder)
    local unlocked = gameApi.IsSlotUnlocked(holder.Name)
    if unlocked == false then
        return true, S(38) .. holder.Name .. S(376)
            .. gameApi.MinerSlots() .. S(312) .. gameApi.MaxMinerSlots() .. S(949)
    end
    if prompt.ObjectText == LOCKED_SLOT_TEXT then
        return true, S(153)
    end
    if not prompt.Enabled then
        return true, S(585)
    end
    local price = findPriceTag(holder)
    if price then
        return true, S(430) .. price
    end
    local action = prompt.ActionText or S(140)
    if action:match(S(719)) or action:match(S(213)) or action:match(S(608)) then
        return true, S(667) .. action .. S(826)
    end
    return false
end
local function distanceTo(part)
    local character = Players.LocalPlayer.Character
    local hrp = character and character:FindFirstChild(S(903))
    if not (hrp and part) then
        return nil
    end
    return (hrp.Position - part.Position).Magnitude
end
local function inRange(prompt)
    local distance = distanceTo(prompt.Parent)
    return distance == nil or distance <= prompt.MaxActivationDistance
end
local function firePrompt(prompt)
    local ok, err = pcall(function()
        fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
    end)
    if not ok then
        log(S(881) .. tostring(err))
    end
    return ok
end
local lastSurvey = nil
local function surveyHolders()
    local plot = getCurrentPlot()
    local parts = plot and getPlotParts(plot)
    if not (parts and parts.Holders) then
        return nil
    end
    local empty, occupied, lockedPads = {}, {}, {}
    local summary = {}
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if prompt then
            local locked, why = isLocked(prompt, holder)
            if locked then
                table.insert(summary, holder.Name .. S(374) .. why .. S(915))
                local priceText = findPriceTag(holder)
                local price = priceText and parseMoney(priceText) or nil
                if prompt.Enabled and price and price ~= math.huge then
                    table.insert(lockedPads, {
                        holder = holder,
                        prompt = prompt,
                        price = price,
                        priceText = priceText,
                    })
                end
            elseif isEmpty(prompt) then
                table.insert(summary, holder.Name .. S(104))
                table.insert(empty, { holder = holder, prompt = prompt })
            else
                local shown = readPlacedPower(holder) or 0
                local base = gameApi.HolderBaseDamage(holder.Name)
                    or gameApi.CharacterBaseDamage(prompt.ObjectText)
                    or shown
                table.insert(summary, holder.Name .. S(321) .. prompt.ObjectText
                    .. S(605) .. shown .. S(938) .. math.floor(base) .. S(272))
                table.insert(occupied, {
                    holder = holder,
                    prompt = prompt,
                    name = prompt.ObjectText,
                    power = base,
                    shown = shown,
                })
            end
        end
    end
    local line = table.concat(summary, S(741))
    if line ~= lastSurvey then
        log(S(464) .. line)
        lastSurvey = line
    end
    table.sort(occupied, function(a, b)
        return a.power < b.power
    end)
    table.sort(lockedPads, function(a, b)
        return a.price < b.price
    end)
    return { empty = empty, occupied = occupied, locked = lockedPads }
end
local cachedSurvey, cachedCandidates, cachedAt = nil, nil, 0
local function look()
    local now = tick()
    if now - cachedAt > 1 then
        cachedSurvey = surveyHolders()
        cachedCandidates = getMinerSlots()
        cachedAt = now
    end
    return cachedSurvey, cachedCandidates
end
local function findNextAction()
    local survey, candidates = look()
    if not survey or not candidates or #candidates == 0 then
        return nil
    end
    if #survey.empty > 0 then
        return S(549), survey.empty[1], candidates[1]
    end
    local weakest = upgradeWeakMiners and survey.occupied[1] or nil
    if weakest and candidates[1].power > weakest.power then
        return S(771), weakest, candidates[1]
    end
    if unlockSlots then
        local money = gameApi.GetMoney()
        for _, pad in ipairs(survey.locked or {}) do
            if pad.price <= money
                and (unlockBudget == 0 or pad.price <= unlockBudget)
            then
                return S(39), pad, nil
            end
        end
    end
    return nil
end
local autoPlace = {}
local enabled = false
local lastEquipBest = 0
local busy = false
local lastAttempt = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(74) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(728))
    else
        lastAttempt = 0
        log(S(642))
    end
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.IsBusy()
    return busy
end
function autoPlace.SetUseEquipBest(state)
    useEquipBest = state
end
function autoPlace.SetEquipBestInterval(seconds)
    equipBestInterval = math.max(seconds, 5)
end
function autoPlace.SetUnlockSlots(state)
    unlockSlots = state
end
function autoPlace.SetUnlockBudget(amount)
    unlockBudget = math.max(amount, 0)
end
function autoPlace.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoPlace.SetUpgradeWeakMiners(state)
    upgradeWeakMiners = state
end
function autoPlace.HasWork()
    if useEquipBest and tick() - lastEquipBest >= equipBestInterval then
        return true
    end
    return (findNextAction()) ~= nil
end
local function goTo(prompt)
    teleport.To(prompt.Parent, Vector3.new(0, 3, 0))
    task.wait(0.3)
    if inRange(prompt) then
        return true
    end
    local distance = distanceTo(prompt.Parent)
    log(string.format(S(731),
        distance or -1, tostring(prompt.MaxActivationDistance)))
    return false
end
local function holdMiner(miner)
    if not gameApi.HotbarHasSlot(miner.slot) then
        return false, S(433)
    end
    if not gameApi.IsSlotWithinCap(miner.slot) then
        return false, S(649)
    end
    gameApi.EquipSlot(miner.slot)
    local wanted = tostring(miner.slot)
    for _ = 1, 10 do
        task.wait(0.1)
        if tostring(gameApi.EquippedSlot()) == wanted then
            return true
        end
    end
    return false, S(179)
end
local function placeBest(holder, prompt)
    local candidates = getMinerSlots()
    if #candidates == 0 then
        log(S(46))
        return false
    end
    local miner = nil
    for _, candidate in ipairs(candidates) do
        local held, why = holdMiner(candidate)
        if held then
            miner = candidate
            break
        end
        if not inRange(prompt) then
            log(S(417) .. holder.Name .. S(787))
            return false
        end
        log(S(425) .. candidate.slot .. S(318) .. candidate.name
            .. S(659) .. tostring(why))
    end
    if not miner then
        log(S(238))
        return false
    end
    firePrompt(prompt)
    task.wait(0.5)
    if isEmpty(prompt) then
        log(S(670) .. holder.Name .. S(36) .. miner.name)
        return false
    end
    log(S(913) .. miner.name .. S(192) .. miner.power
        .. S(310) .. holder.Name)
    return true
end
local function pickUp(entry)
    firePrompt(entry.prompt)
    task.wait(0.5)
    if not isEmpty(entry.prompt) then
        log(S(658) .. entry.name .. S(221) .. entry.holder.Name
            .. S(641) .. tostring(entry.prompt.ActionText) .. S(70))
        return false
    end
    log(S(687) .. entry.name .. S(55) .. entry.power
        .. S(896) .. entry.holder.Name)
    return true
end
local function runFillJob(target)
    busy = true
    if goTo(target.prompt) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUnlockJob(pad)
    busy = true
    if goTo(pad.prompt) then
        firePrompt(pad.prompt)
        task.wait(0.8)
        local stillLocked = isLocked(pad.prompt, pad.holder)
        if stillLocked then
            log(S(789) .. pad.holder.Name .. S(635)
                .. tostring(pad.priceText))
        else
            log(S(240) .. pad.holder.Name
                .. S(18) .. tostring(pad.priceText))
        end
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function runUpgradeJob(target)
    busy = true
    if goTo(target.prompt) and pickUp(target) then
        placeBest(target.holder, target.prompt)
    end
    teleport.Return()
    cachedAt = 0
    busy = false
end
local function refreshHotbar()
    if not useEquipBest then
        return
    end
    local now = tick()
    if now - lastEquipBest < equipBestInterval then
        return
    end
    lastEquipBest = now
    local before = getMinerSlots()
    local bestBefore = before[1] and before[1].power or 0
    gameApi.EquipBest()
    task.wait(0.6)
    cachedAt = 0
    local after = getMinerSlots()
    local bestAfter = after[1] and after[1].power or 0
    if bestAfter > bestBefore then
        log(S(481)
            .. string.format(S(284), bestBefore) .. S(513)
            .. string.format(S(746), bestAfter))
    end
end
function autoPlace.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if useEquipBest and now - lastEquipBest >= equipBestInterval then
        busy = true
        runGuarded(S(304), refreshHotbar, function()
            busy = false
        end)
        return
    end
    local action, target, best = findNextAction()
    if not action then
        reason(S(509))
        return
    end
    lastReason = nil
    cachedAt = 0
    if action == S(297) then
        runGuarded(S(349), function()
            runFillJob(target)
        end, function()
            busy = false
        end)
        return
    end
    if action == S(45) then
        log(S(458) .. target.holder.Name
            .. S(435) .. tostring(target.priceText))
        runGuarded(S(843), function()
            runUnlockJob(target)
        end, function()
            busy = false
        end)
        return
    end
    log(S(162) .. target.holder.Name .. S(940) .. target.name
        .. S(47) .. target.power .. S(853) .. best.name .. S(749) .. best.power .. S(944))
    runGuarded(S(968), function()
        runUpgradeJob(target)
    end, function()
        busy = false
    end)
end
return autoPlace
end
end
modules[S(377)] = function()
return function(import)
local gameApi = import(S(223))(import)
local parseMoney = import(S(26))
local log = import(S(594))
local runGuarded = import(S(837))(import)
local checkInterval = 30
local buyNextZone = true
local useLatestZone = true
local keepMoney = 0
local autoPlot = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedLocked = nil
function autoPlot.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    reportedLocked = nil
    log(S(270) .. (enabled and S(924) or S(582)))
end
function autoPlot.IsEnabled()
    return enabled
end
function autoPlot.IsBusy()
    return busy
end
function autoPlot.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoPlot.SetBuyNextZone(state)
    buyNextZone = state
end
function autoPlot.SetUseLatestZone(state)
    useLatestZone = state
end
function autoPlot.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function deepestUnlocked()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local bestIndex, bestKey = nil, nil
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if unlocked[zoneKey] then
            bestIndex, bestKey = index, zoneKey
        end
    end
    return bestIndex, bestKey
end
local function nextLockedZone()
    local mine = gameApi.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    for index, zoneKey in ipairs(gameApi.ZoneKeys()) do
        if not unlocked[zoneKey] then
            return index, zoneKey
        end
    end
    return nil
end
local function priceOf(zoneIndex)
    local text = gameApi.ZoneBoardPriceText(zoneIndex)
    if not text or not text:find(S(686)) then
        return nil
    end
    local amount = parseMoney(text)
    if amount == math.huge then
        return nil
    end
    return amount
end
local function switchToDeepest()
    if not useLatestZone then
        return false
    end
    local deepestIndex, deepestKey = deepestUnlocked()
    if not deepestIndex or gameApi.CurrentZoneIndex() == deepestIndex then
        return false
    end
    gameApi.SelectZone(deepestKey)
    task.wait(0.5)
    if gameApi.CurrentZoneIndex() == deepestIndex then
        log(S(339) .. gameApi.ZoneDisplayName(deepestKey)
            .. S(620) .. deepestKey .. S(364))
        return true
    end
    log(S(524) .. deepestKey)
    return false
end
local function tryBuyNextZone()
    if not buyNextZone then
        return false
    end
    local index, zoneKey = nextLockedZone()
    if not index then
        return false
    end
    local price = priceOf(index)
    if price and price > (gameApi.GetMoney() - keepMoney) then
        if reportedLocked ~= price then
            reportedLocked = price
            log(S(895) .. gameApi.ZoneDisplayName(zoneKey) .. S(241)
                .. math.floor(price) .. S(327) .. math.floor(gameApi.GetMoney()))
        end
        return false
    end
    gameApi.SelectZone(zoneKey)
    task.wait(0.6)
    local mine = gameApi.GetMineData()
    if mine and (mine.UnlockedZones or {})[zoneKey] then
        log(S(863) .. gameApi.ZoneDisplayName(zoneKey)
            .. S(846) .. zoneKey .. S(73))
        reportedLocked = nil
        return true
    end
    local _, currentKey = deepestUnlocked()
    if currentKey then
        local done, total = gameApi.ZoneResearchProgress(currentKey)
        if reportedLocked ~= S(40) then
            reportedLocked = S(923)
            log(S(102) .. zoneKey .. S(817)
                .. gameApi.ZoneDisplayName(currentKey) .. S(900)
                .. done .. S(852) .. total)
        end
    end
    return false
end
local function runJob()
    busy = true
    local bought = tryBuyNextZone()
    if bought then
        task.wait(0.4)
    end
    switchToDeepest()
    busy = false
end
function autoPlot.HasWork()
    if useLatestZone then
        local deepestIndex = deepestUnlocked()
        if deepestIndex and gameApi.CurrentZoneIndex() ~= deepestIndex then
            return true
        end
    end
    if buyNextZone then
        local index = nextLockedZone()
        if index then
            local price = priceOf(index)
            if not price or price <= (gameApi.GetMoney() - keepMoney) then
                return true
            end
        end
    end
    return false
end
function autoPlot.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(147), runJob, function()
        busy = false
    end)
end
return autoPlot
end
end
modules[S(319)] = function()
return function(import)
local gameApi = import(S(648))(import)
local log = import(S(23))
local runGuarded = import(S(105))(import)
local checkInterval = 30
local maxPerPass = 5
local buyZones = true
local buyCore = true
local coreLastResort = true
local cheapestFirst = true
local keepMoney = 0
local autoResearch = {}
local enabled = false
local busy = false
local lastAttempt = 0
local lastIdleSummary = nil
local reportIdle
function autoResearch.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(199) .. (enabled and S(517) or S(516)))
    if enabled then
        task.spawn(function()
            task.wait(1)
            if enabled then
                reportIdle()
            end
        end)
    end
end
function autoResearch.IsEnabled()
    return enabled
end
function autoResearch.IsBusy()
    return busy
end
function autoResearch.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoResearch.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoResearch.SetBuyZones(state)
    buyZones = state
end
function autoResearch.SetBuyCore(state)
    buyCore = state
end
function autoResearch.SetCoreLastResort(state)
    coreLastResort = state
end
function autoResearch.SetCheapestFirst(state)
    cheapestFirst = state
end
function autoResearch.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function buyableZoneNodes()
    local budget = spendable()
    local list = {}
    local powerBlocked = nil
    for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
        for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
            if node.open and not node.maxed and node.cost and node.cost <= budget then
                table.insert(list, node)
                if not node.powerOk then
                    if not powerBlocked or node.powerReq < powerBlocked.powerReq then
                        powerBlocked = node
                    end
                end
            end
        end
    end
    return list, powerBlocked
end
local function buyableCoreNodes()
    local budget = spendable()
    local list = {}
    for _, node in ipairs(gameApi.CoreNodes()) do
        if node.open and not node.maxed and node.event
            and node.cost and node.cost <= budget
        then
            table.insert(list, node)
        end
    end
    return list
end
local function sortNodes(list)
    table.sort(list, function(a, b)
        local aPower = a.powerOk ~= false
        local bPower = b.powerOk ~= false
        if aPower ~= bPower then
            return aPower
        end
        if a.cost ~= b.cost then
            if cheapestFirst then
                return a.cost < b.cost
            end
            return a.cost > b.cost
        end
        return tostring(a.id) < tostring(b.id)
    end)
end
local function levelOfZoneNode(node)
    for _, current in ipairs(gameApi.SkillNodes(node.zoneKey)) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function levelOfCoreNode(node)
    for _, current in ipairs(gameApi.CoreNodes()) do
        if current.id == node.id then
            return current.level
        end
    end
    return node.level
end
local function runJob()
    busy = true
    local bought = 0
    local blocked = 0
    local powerBlocked = nil
    if buyZones then
        local nodes
        nodes, powerBlocked = buyableZoneNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuySkill(node.zoneKey, node.family)
            task.wait(0.4)
            local after = levelOfZoneNode(node)
            if after > node.level then
                log(S(394) .. node.zoneKey .. S(157) .. node.id
                    .. S(424) .. node.family .. S(80) .. node.level .. S(277) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    local zonesStillWant = coreLastResort and #(buyableZoneNodes()) > 0
    if buyCore and not zonesStillWant then
        local nodes = buyableCoreNodes()
        sortNodes(nodes)
        for _, node in ipairs(nodes) do
            if not enabled or bought >= maxPerPass then
                break
            end
            gameApi.BuyCore(node.event)
            task.wait(0.4)
            local after = levelOfCoreNode(node)
            if after > node.level then
                log(S(382) .. node.id
                    .. S(961) .. node.family .. S(554) .. node.level .. S(249) .. after)
                bought = bought + 1
                lastIdleSummary = nil
            else
                blocked = blocked + 1
            end
        end
    end
    if bought == 0 then
        if blocked > 0 then
            log(S(551) .. blocked
                .. S(182))
        elseif powerBlocked then
            log(S(274) .. powerBlocked.zoneKey .. S(85) .. powerBlocked.id
                .. S(123) .. powerBlocked.family .. S(345)
                .. math.floor(powerBlocked.powerReq) .. S(567)
                .. math.floor(powerBlocked.power)
                .. S(332))
        else
            log(S(707)
                .. S(613) .. math.floor(gameApi.GetMoney()) .. S(363))
            local nearest = nil
            for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
                for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                    if node.open and not node.maxed and node.cost
                        and (not nearest or node.cost < nearest.cost)
                    then
                        nearest = node
                    end
                end
            end
            if nearest then
                log(S(203) .. nearest.zoneKey
                    .. S(692) .. nearest.id .. S(750) .. nearest.family .. S(857)
                    .. nearest.level .. S(381) .. nearest.max
                    .. S(288) .. math.floor(nearest.cost))
            end
        end
    end
    busy = false
end
local function idleSummary()
    if not buyZones and not buyCore then
        return S(95)
    end
    local money = gameApi.GetMoney()
    local budget = spendable()
    local nearest = nil
    if buyZones then
        for _, zoneKey in ipairs(gameApi.UnlockedZones()) do
            for _, node in ipairs(gameApi.SkillNodes(zoneKey)) do
                if node.open and not node.maxed and node.cost
                    and (not nearest or node.cost < nearest.cost)
                then
                    nearest = node
                end
            end
        end
    end
    if buyCore then
        for _, node in ipairs(gameApi.CoreNodes()) do
            if node.open and not node.maxed and node.cost
                and (not nearest or node.cost < nearest.cost)
            then
                nearest = {
                    zoneKey = S(437),
                    id = node.id,
                    family = node.family,
                    level = node.level,
                    max = node.level + 1,
                    cost = node.cost,
                    powerOk = true,
                }
            end
        end
    end
    if not nearest then
        local parts = {}
        if not buyZones then
            table.insert(parts, S(946))
        end
        if not buyCore then
            table.insert(parts, S(851))
        end
        if #parts == 0 then
            return S(934)
        end
        return S(622) .. table.concat(parts, S(385)) .. S(58)
    end
    local why
    if nearest.cost > budget then
        why = S(876) .. string.format(S(676), nearest.cost)
            .. S(403) .. string.format(S(765), money)
    elseif nearest.powerOk == false then
        why = S(650) .. string.format(S(296), nearest.powerReq) .. S(320)
    else
        why = S(180)
    end
    return S(791) .. nearest.zoneKey .. S(712) .. tostring(nearest.id)
        .. S(935) .. tostring(nearest.family) .. S(823) .. why
end
function reportIdle()
    local summary = idleSummary()
    if summary == lastIdleSummary then
        return
    end
    lastIdleSummary = summary
    log(S(634) .. summary)
end
local lastIdleReport = 0
function autoResearch.HasWork()
    local zoneNodes = buyZones and buyableZoneNodes() or {}
    local has
    if #zoneNodes > 0 then
        has = true
    elseif not buyCore then
        has = false
    else
        has = #buyableCoreNodes() > 0
    end
    if not has and enabled then
        local now = tick()
        if now - lastIdleReport >= math.max(checkInterval, 15) then
            lastIdleReport = now
            reportIdle()
        end
    end
    return has
end
function autoResearch.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(187), runJob, function()
        busy = false
    end)
end
return autoResearch
end
end
modules[S(836)] = function()
return function(import)
local Workspace = game:GetService(S(454))
local gameApi = import(S(742))(import)
local teleport = import(S(783))
local log = import(S(496))
local runGuarded = import(S(220))(import)
local checkInterval = 45
local autoSell = {}
local enabled = false
local busy = false
local lastAttempt = 0
local holdWhile = nil
local needsProximity = nil
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if not busy then
            teleport.Return()
        end
        log(S(81))
    else
        lastAttempt = 0
        log(S(439))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.IsBusy()
    return busy
end
function autoSell.SetCheckInterval(seconds)
    checkInterval = math.max(seconds, 5)
end
function autoSell.SetHoldWhile(predicate)
    holdWhile = predicate
end
function autoSell.HasWork()
    if holdWhile and holdWhile() then
        return false
    end
    return #gameApi.SellableKeys() > 0
end
local function getSellStandPart()
    local sellStand = Workspace:FindFirstChild(S(483))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(456), true)
    return prompt and prompt.Parent
end
local function sell(keys)
    gameApi.SellItems(keys)
    task.wait(0.6)
    return #gameApi.SellableKeys() < #keys
end
local function runJob()
    busy = true
    local keys = gameApi.SellableKeys()
    if #keys == 0 then
        busy = false
        return
    end
    local sold = false
    if needsProximity ~= true then
        sold = sell(keys)
        if needsProximity == nil then
            needsProximity = not sold
            log(S(706)
                .. (sold and S(855)
                         or S(305)))
        end
    end
    if not sold then
        local part = getSellStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            sold = sell(gameApi.SellableKeys())
            teleport.Return()
        end
    end
    log(S(122) .. (sold and (S(526) .. #keys .. S(195)) or S(360)))
    busy = false
end
function autoSell.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    if #gameApi.SellableKeys() == 0 then
        return
    end
    runGuarded(S(500), runJob, function()
        busy = false
    end)
end
return autoSell
end
end
modules[S(356)] = function()
return function(import)
local Workspace = game:GetService(S(98))
local gameApi = import(S(974))(import)
local teleport = import(S(67))
local log = import(S(421))
local runGuarded = import(S(565))(import)
local STATS = { S(596), S(25), S(163) }
local checkInterval = 20
local maxPerPass = 5
local priority = S(966)
local keepMoney = 0
local buyPickaxe = true
local pickaxeTarget = 0
local equipPickaxes = true
local limitToHolders = true
local rollMutations = false
local mutationTarget = 2
local mutationBudget = 0
local autoUpgrade = {}
local enabled = false
local busy = false
local lastAttempt = 0
local reportedCap = false
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(950) .. (enabled and S(602) or S(387)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.IsBusy()
    return busy
end
function autoUpgrade.SetInterval(seconds)
    checkInterval = math.max(seconds, 1)
end
function autoUpgrade.SetMaxPerPass(count)
    maxPerPass = math.max(count, 1)
end
function autoUpgrade.SetPriority(value)
    priority = value
end
function autoUpgrade.SetKeepMoney(amount)
    keepMoney = math.max(amount, 0)
end
function autoUpgrade.SetBuyPickaxe(state)
    buyPickaxe = state
end
function autoUpgrade.SetPickaxeTarget(index)
    pickaxeTarget = index
end
function autoUpgrade.SetEquipPickaxes(state)
    equipPickaxes = state
end
function autoUpgrade.SetLimitPickaxesToHolders(state)
    limitToHolders = state
end
function autoUpgrade.SetRollMutations(state)
    rollMutations = state
end
function autoUpgrade.SetMutationTarget(multiplier)
    mutationTarget = math.max(multiplier, 1)
end
function autoUpgrade.SetMutationBudget(amount)
    mutationBudget = math.max(amount, 0)
end
local function holders()
    local mine = gameApi.GetMineData()
    return mine and mine.Holders or {}
end
local function holderKeys()
    local keys = {}
    for holderKey, entry in pairs(holders()) do
        if type(entry) == S(293) and entry.CharName then
            table.insert(keys, holderKey)
        end
    end
    table.sort(keys)
    return keys
end
local function spendable()
    return gameApi.GetMoney() - keepMoney
end
local function chooseStat(holderKey)
    local budget = spendable()
    local anyLeft = false
    local preferred = nil
    local lowest = nil
    for _, stat in ipairs(STATS) do
        local info = gameApi.StatInfo(holderKey, stat)
        if info and not info.maxed and info.cost then
            anyLeft = true
            if info.cost <= budget then
                if stat == priority then
                    preferred = { stat = stat, info = info }
                end
                if not lowest or info.level < lowest.info.level then
                    lowest = { stat = stat, info = info }
                end
            end
        end
    end
    local choice = preferred or lowest
    if choice then
        return choice
    end
    return nil, anyLeft and S(124) or S(121)
end
local function upgradeStats()
    local bought = 0
    local tooExpensive = 0
    local maxed = 0
    while enabled and bought < maxPerPass do
        local boughtThisLap = 0
        for _, holderKey in ipairs(holderKeys()) do
            if not enabled or bought >= maxPerPass then
                break
            end
            local choice, reason = chooseStat(holderKey)
            if choice then
                gameApi.UpgradeCharacter(holderKey, choice.stat)
                task.wait(0.35)
                local after = gameApi.StatInfo(holderKey, choice.stat)
                if after and after.level > choice.info.level then
                    log(S(959) .. holderKey .. S(879) .. choice.stat .. S(257)
                        .. choice.info.level .. S(201) .. after.level
                        .. S(764) .. after.max)
                    bought = bought + 1
                    boughtThisLap = boughtThisLap + 1
                end
            elseif reason == S(510) then
                tooExpensive = tooExpensive + 1
            else
                maxed = maxed + 1
            end
        end
        if boughtThisLap == 0 then
            break
        end
    end
    return bought, tooExpensive, maxed
end
local function pickaxeCapReached()
    if not limitToHolders then
        return false
    end
    local cap = gameApi.MaxMinerSlots()
    if cap <= 0 then
        cap = gameApi.MinerSlots()
    end
    if cap <= 0 then
        return false
    end
    return gameApi.OwnedPickaxeCount() >= cap
end
local function findPickaxe()
    if pickaxeCapReached() then
        return nil
    end
    if pickaxeTarget > 0 then
        for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
            if pickaxe.index == pickaxeTarget then
                if pickaxe.zoneUnlocked and pickaxe.inStock
                    and pickaxe.cost <= spendable()
                then
                    return pickaxe
                end
                return nil
            end
        end
        return nil
    end
    local wanted = gameApi.WantedPickaxe()
    if wanted and wanted.cost <= spendable() then
        return wanted
    end
    return nil
end
local function ownedCountOf(name)
    for _, pickaxe in ipairs(gameApi.GetPickaxes()) do
        if pickaxe.name == name then
            return pickaxe.ownedCount
        end
    end
    return 0
end
local function getPickaxeStandPart()
    local stand = Workspace:FindFirstChild(S(751))
    local shop = stand and stand:FindFirstChild(S(776))
    if not shop then
        return nil
    end
    if shop:IsA(S(576)) then
        return shop
    end
    return shop:FindFirstChildWhichIsA(S(344), true)
end
local needsProximity = nil
local function sendBuy(pickaxe)
    local before = ownedCountOf(pickaxe.name)
    gameApi.BuyPickaxe(pickaxe.index)
    task.wait(0.4)
    return ownedCountOf(pickaxe.name) > before
end
local function tryPickaxe()
    local pickaxe = findPickaxe()
    if not pickaxe then
        return false
    end
    local bought = false
    if needsProximity ~= true then
        bought = sendBuy(pickaxe)
        if needsProximity == nil then
            needsProximity = not bought
            log(S(317)
                .. (bought and S(892)
                           or S(406)))
        end
    end
    if not bought then
        local part = getPickaxeStandPart()
        if part then
            teleport.To(part)
            task.wait(0.4)
            bought = sendBuy(pickaxe)
            teleport.Return()
            if not bought then
                needsProximity = nil
            end
        else
            log(S(578))
        end
    end
    if bought then
        log(S(69) .. pickaxe.index .. S(175) .. pickaxe.name
            .. S(809) .. pickaxe.multiplier .. S(141))
        return true
    end
    log(S(848) .. pickaxe.index .. S(722) .. pickaxe.name
        .. S(298))
    return false
end
local function plannedPickaxes()
    local pool = gameApi.OwnedPickaxePool()
    local keys = holderKeys()
    if #pool == 0 or #keys == 0 then
        return {}
    end
    table.sort(keys, function(a, b)
        local da = gameApi.HolderBaseDamage(a) or 0
        local db = gameApi.HolderBaseDamage(b) or 0
        if da ~= db then
            return da > db
        end
        return a < b
    end)
    local plan = {}
    for position, holderKey in ipairs(keys) do
        local wanted = pool[position]
        if not wanted then
            break
        end
        table.insert(plan, { holderKey = holderKey, pickaxe = wanted })
    end
    return plan
end
local function pickaxeAssignmentPending()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local entry = current[step.holderKey]
        if (entry and entry.Pickaxe or nil) ~= step.pickaxe.name then
            return true
        end
    end
    return false
end
local function equipPickaxesBestFirst()
    local current = holders()
    for _, step in ipairs(plannedPickaxes()) do
        local holderKey, wanted = step.holderKey, step.pickaxe
        local entry = current[holderKey]
        local worn = entry and entry.Pickaxe or nil
        if worn ~= wanted.name then
            gameApi.EquipPickaxe(holderKey, wanted.name)
            task.wait(0.3)
            local after = holders()[holderKey]
            local nowWorn = after and after.Pickaxe or nil
            if nowWorn == wanted.name then
                log(S(495) .. holderKey .. S(905)
                    .. tostring(worn or S(235)) .. S(169) .. wanted.name)
            else
                log(S(870) .. holderKey .. S(792)
                    .. wanted.name .. S(849) .. tostring(nowWorn or S(159)) .. S(12))
            end
        end
    end
end
local function rollMutationsPass(remaining)
    local rolled = 0
    for _, holderKey in ipairs(holderKeys()) do
        if not enabled or rolled >= remaining then
            break
        end
        local variant = gameApi.HolderVariantMult(holderKey)
        local cost = gameApi.MutationRollCost(holderKey)
        if variant < mutationTarget and cost
            and cost <= spendable()
            and (mutationBudget == 0 or cost <= mutationBudget)
        then
            gameApi.RollMutation(holderKey)
            task.wait(0.5)
            local after = gameApi.HolderVariantMult(holderKey)
            log(S(540) .. holderKey .. S(427) .. variant
                .. S(379) .. after .. S(361) .. cost .. S(593))
            rolled = rolled + 1
        end
    end
    return rolled
end
local function runJob()
    busy = true
    local bought, tooExpensive, maxed = upgradeStats()
    if buyPickaxe and enabled and bought < maxPerPass then
        if pickaxeCapReached() then
            if not reportedCap then
                log(S(850) .. gameApi.OwnedPickaxeCount()
                    .. S(628) .. gameApi.MaxMinerSlots()
                    .. S(818))
                reportedCap = true
            end
        elseif tryPickaxe() then
            bought = bought + 1
            reportedCap = false
        end
    end
    if equipPickaxes and enabled then
        equipPickaxesBestFirst()
    end
    if rollMutations and enabled and bought < maxPerPass then
        bought = bought + rollMutationsPass(maxPerPass - bought)
    end
    if bought == 0 then
        if tooExpensive > 0 then
            log(S(744))
        elseif maxed > 0 then
            log(S(899))
        else
            log(S(89))
        end
    end
    busy = false
end
function autoUpgrade.HasWork()
    for _, holderKey in ipairs(holderKeys()) do
        if chooseStat(holderKey) then
            return true
        end
    end
    if buyPickaxe and findPickaxe() then
        return true
    end
    if equipPickaxes and pickaxeAssignmentPending() then
        return true
    end
    if rollMutations then
        for _, holderKey in ipairs(holderKeys()) do
            local cost = gameApi.MutationRollCost(holderKey)
            if gameApi.HolderVariantMult(holderKey) < mutationTarget
                and cost and cost <= spendable()
                and (mutationBudget == 0 or cost <= mutationBudget)
            then
                return true
            end
        end
    end
    return false
end
function autoUpgrade.Step()
    if not enabled or busy then
        return
    end
    local now = tick()
    if now - lastAttempt < checkInterval then
        return
    end
    lastAttempt = now
    runGuarded(S(31), runJob, function()
        busy = false
    end)
end
return autoUpgrade
end
end
modules[S(774)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(391))
local Workspace = game:GetService(S(882))
local log = import(S(480))
local api = {}
local protectUsefulUnits = true
local gameState, mineInventory, mineConfig, mineStock, items, indexRewards
local loadFailed = false
local function modules()
    if gameState or loadFailed then
        return gameState ~= nil
    end
    local ok, err = pcall(function()
        local moduleFolder = ReplicatedStorage:WaitForChild(S(281), 5)
        gameState = require(moduleFolder.GameState)
        mineInventory = require(moduleFolder.MineInventory)
        mineConfig = require(moduleFolder.MineConfig)
        mineStock = require(moduleFolder.MineStock)
        items = require(moduleFolder.Items)
        indexRewards = require(moduleFolder.MineIndexRewardsConfig)
    end)
    if not ok then
        loadFailed = true
        log(S(647) .. tostring(err))
        return false
    end
    return true
end
function api.SetProtectUsefulUnits(state)
    protectUsefulUnits = state
end
function api.IsReady()
    return modules()
end
function api.GetData()
    if not modules() then
        return nil
    end
    local ok, data = pcall(gameState.GetData2)
    if not ok then
        return nil
    end
    return data
end
function api.GetMineData()
    local data = api.GetData()
    return data and data.MineGame or nil
end
function api.GetMoney()
    local data = api.GetData()
    return data and tonumber(data.Money) or 0
end
function api.GetPickaxes()
    if not modules() then
        return {}
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    local owned = (mine and mine.Pickaxes) or {}
    local unlocked = (mine and mine.UnlockedZones) or {}
    local list = {}
    local ok = pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local buyableWithCash = not (mineConfig.IsPickaxeFree(def)
                or def.RobuxOnly or type(def.Stock) ~= S(541))
            if buyableWithCash then
                local remaining = nil
                if data then
                    local gotStock, value = pcall(mineStock.GetRemaining, data, def.Name)
                    remaining = gotStock and value or nil
                end
                local zoneOpen = unlocked[def.ZoneReq] and true or false
                table.insert(list, {
                    index = index,
                    name = def.Name,
                    cost = tonumber(def.Cost) or math.huge,
                    multiplier = tonumber(def.Multiplier) or 1,
                    zoneReq = def.ZoneReq,
                    zoneUnlocked = zoneOpen,
                    ownedCount = math.floor(tonumber(owned[def.Name]) or 0),
                    stock = remaining,
                    inStock = (remaining == nil) or (remaining > 0),
                })
            end
        end
    end)
    if not ok then
        return {}
    end
    return list
end
function api.WantedPickaxe()
    if not modules() then
        return nil
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return nil
    end
    local wanted = nil
    pcall(function()
        local minerCount = 0
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(947) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(859) then
                    minerCount = minerCount + 1
                end
            end
        end
        if minerCount <= 0 then
            return
        end
        local inUse = {}
        for _, def in ipairs(mineConfig.Pickaxes) do
            if not mineConfig.IsPickaxeFree(def) then
                local count = math.floor(tonumber((mine.Pickaxes or {})[def.Name]) or 0)
                for _ = 1, math.min(count, minerCount) do
                    table.insert(inUse, tonumber(def.Multiplier) or 1)
                end
            end
        end
        table.sort(inUse, function(a, b) return a > b end)
        local weakestInUse = minerCount <= #inUse
            and inUse[minerCount]
            or (tonumber(mineConfig.Pickaxes[1].Multiplier) or 1)
        local money = tonumber(data.Money) or 0
        for _, pickaxe in ipairs(api.GetPickaxes()) do
            if pickaxe.zoneUnlocked and pickaxe.inStock
                and pickaxe.multiplier > weakestInUse
                and pickaxe.cost <= money
            then
                if not wanted or pickaxe.multiplier > wanted.multiplier then
                    wanted = pickaxe
                end
            end
        end
    end)
    return wanted
end
function api.ChestCount()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, entries = pcall(mineInventory.ChestEntries, mine)
    if not ok or type(entries) ~= S(962) then
        return nil
    end
    local total = 0
    for _, entry in ipairs(entries) do
        total = total + (tonumber(entry.Count) or 0)
    end
    return total
end
function api.ChestCapacity()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, capacity = pcall(mineConfig.GetChestCapacityFor, mine, 0)
    return ok and tonumber(capacity) or nil
end
function api.ChestHasItems()
    local mine = api.GetMineData()
    return mine ~= nil and next(mine.Chest or {}) ~= nil
end
function api.MinerSlots()
    if not modules() then
        return 0
    end
    local data = api.GetData()
    local mine = data and data.MineGame
    if not mine then
        return 0
    end
    local ok, count = pcall(function()
        return tonumber(mine.MinerSlots or data.MinerSlots or mineConfig.BaseMinerSlots) or 0
    end)
    return (ok and count) or 0
end
function api.PlacedMinerCount()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local count = 0
    pcall(function()
        for _, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(512) and holder.CharName then
                local itemData = items.GetItemData(holder.CharName)
                if itemData and itemData.MineRole ~= S(151) then
                    count = count + 1
                end
            end
        end
    end)
    return count
end
function api.OwnedPickaxeCount()
    local total = 0
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        total = total + pickaxe.ownedCount
    end
    return total
end
function api.MaxMinerSlots()
    if not modules() then
        return 0
    end
    local ok, count = pcall(function() return tonumber(mineConfig.MaxMinerSlots) or 0 end)
    return (ok and count) or 0
end
function api.IsSlotUnlocked(holderKey)
    local index = tostring(holderKey):match(S(747))
    if not index then
        return nil
    end
    return tonumber(index) <= api.MinerSlots()
end
local function send(...)
    if not modules() then
        return false
    end
    local ok, err = pcall(gameState.SendEvent, ...)
    if not ok then
        log(S(186) .. tostring(err))
    end
    return ok
end
function api.TakeChest()
    return send(S(739))
end
function api.SellableKeys()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local ok, entries = pcall(mineInventory.Collect, mine, S(715), mineInventory.KindOrder.OreFirst)
    if not ok or type(entries) ~= S(772) then
        return {}
    end
    local floorDamage = nil
    if protectUsefulUnits then
        for holderKey, holder in pairs(mine.Holders or {}) do
            if type(holder) == S(733) and holder.CharName then
                local base = api.HolderBaseDamage(holderKey)
                if base and (not floorDamage or base < floorDamage) then
                    floorDamage = base
                end
            end
        end
    end
    local keys = {}
    for _, entry in pairs(entries) do
        local count = entry.SellCount or entry.Count or 0
        if count > 0 and (entry.Worth or 0) > 0 and entry.Key then
            local spare = false
            if floorDamage and entry.Kind == S(939) then
                local base = api.CharacterBaseDamage(entry.Name)
                spare = base ~= nil and base >= floorDamage
            end
            if not spare then
                table.insert(keys, entry.Key)
            end
        end
    end
    return keys
end
function api.SellItems(keys)
    if #keys == 0 then
        return false
    end
    return send(S(910), keys)
end
function api.EquipSlot(slot)
    return send(S(835), slot == S(673) and S(964) or tostring(slot))
end
function api.EquippedSlot()
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, slot = pcall(mineInventory.EquippedSlot, mine)
    return ok and slot or nil
end
function api.HotbarHasSlot(slot)
    local mine = api.GetMineData()
    return mine ~= nil and (mine.Hotbar or {})[tostring(slot)] ~= nil
end
function api.IsSlotWithinCap(slot)
    if not modules() then
        return true
    end
    local ok, within = pcall(mineInventory.SlotWithinCap, tostring(slot))
    if not ok then
        return true
    end
    return within and true or false
end
function api.BuyPickaxe(tierIndex)
    return send(S(30), tierIndex)
end
function api.EquipPickaxe(holderKey, pickaxeName)
    return send(S(28), holderKey, pickaxeName)
end
function api.RollMutation(holderKey)
    return send(S(643), holderKey)
end
function api.OwnedPickaxePool()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local owned = (mine and mine.Pickaxes) or {}
    local pool = {}
    pcall(function()
        for index, def in ipairs(mineConfig.Pickaxes) do
            local count = math.floor(tonumber(owned[def.Name]) or 0)
            local free = mineConfig.IsPickaxeFree(def) and true or false
            if free and count <= 0 then
                count = api.MaxMinerSlots()
            end
            for _ = 1, count do
                table.insert(pool, {
                    index = index,
                    name = def.Name,
                    multiplier = tonumber(def.Multiplier) or 1,
                    free = free,
                })
            end
        end
    end)
    table.sort(pool, function(a, b)
        if a.multiplier ~= b.multiplier then
            return a.multiplier > b.multiplier
        end
        return a.name < b.name
    end)
    return pool
end
function api.BestOwnedPickaxe()
    local best = nil
    for _, pickaxe in ipairs(api.GetPickaxes()) do
        if pickaxe.owned and (not best or pickaxe.cost > best.cost) then
            best = pickaxe
        end
    end
    return best
end
function api.EquipBest()
    return send(S(106))
end
function api.BuySkill(zoneKey, family)
    return send(S(921), zoneKey, family)
end
function api.ZoneKeys()
    if not modules() then
        return {}
    end
    local ok, keys = pcall(function() return mineConfig.ZoneKeys end)
    return (ok and type(keys) == S(350)) and keys or {}
end
function api.UnlockedZones()
    local mine = api.GetMineData()
    local unlocked = mine and mine.UnlockedZones or {}
    local list = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        if unlocked[zoneKey] then
            table.insert(list, zoneKey)
        end
    end
    return list
end
function api.MinePower()
    local player = game:GetService(S(128)).LocalPlayer
    return tonumber(player and player:GetAttribute(S(619))) or 0
end
local function skillPowerReq(zoneKey, family, nextLevel)
    if type(mineConfig.GetSkillPowerReq) ~= S(264) then
        return nil
    end
    local ok, req = pcall(mineConfig.GetSkillPowerReq, zoneKey, family, nextLevel)
    return ok and tonumber(req) or nil
end
function api.SkillNodes(zoneKey)
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    local skills = (mine and mine.Skills and mine.Skills[zoneKey]) or {}
    local power = api.MinePower()
    local list = {}
    local ok, nodes = pcall(mineConfig.GetSkillNodes, zoneKey)
    if not ok or type(nodes) ~= S(261) then
        return list
    end
    for _, node in ipairs(nodes) do
        local built
        local gotNode = pcall(function()
            local level = tonumber(skills[node.Family]) or 0
            local max = tonumber(mineConfig.GetSkillMax(zoneKey, node.Family)) or 0
            local cost = nil
            if level < max then
                cost = tonumber(mineConfig.GetSkillCost(zoneKey, level, node.Family))
            end
            local powerReq = skillPowerReq(zoneKey, node.Family, level + 1)
            built = {
                zoneKey = zoneKey,
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                col = node.Col or 0,
                level = level,
                max = max,
                cost = cost,
                powerReq = powerReq,
                power = power,
                powerOk = (powerReq == nil) or (power >= powerReq),
                maxed = mineConfig.IsSkillNodeMaxed(skills, node) and true or false,
                open = mineConfig.IsSkillNodeOpen(skills, node) and true or false,
            }
        end)
        if gotNode and built then
            table.insert(list, built)
        end
    end
    return list
end
function api.CurrentZoneIndex()
    local mine = api.GetMineData()
    return mine and tonumber(mine.CurrentZone) or 1
end
function api.CurrentZoneKey()
    local keys = api.ZoneKeys()
    return keys[api.CurrentZoneIndex()] or keys[1]
end
function api.SelectZone(zoneKey)
    return send(S(663), zoneKey)
end
function api.ZoneDisplayName(zoneKey)
    if not modules() then
        return zoneKey
    end
    local ok, zone = pcall(function() return mineConfig.Zones[zoneKey] end)
    return (ok and zone and zone.DisplayName) or zoneKey
end
function api.ZoneResearchProgress(zoneKey)
    if not modules() then
        return 0, 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0, 0
    end
    local ok, done, total = pcall(mineConfig.GetZoneResearchProgress, mine.Skills, zoneKey)
    if not ok then
        return 0, 0
    end
    return tonumber(done) or 0, tonumber(total) or 0
end
function api.ZoneBoardPriceText(zoneIndex)
    local plots = Workspace:FindFirstChild(S(115))
    plots = plots and plots:FindFirstChild(S(724))
    if not plots then
        return nil
    end
    local userId = game:GetService(S(506)).LocalPlayer.UserId
    local myPlot = nil
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(841)) == userId then
            myPlot = plot
            break
        end
    end
    if not myPlot then
        return nil
    end
    local board = myPlot:FindFirstChild(S(785))
    local gui = board and board:FindFirstChild(S(215))
    local root = gui and gui:FindFirstChild(S(808))
    local list = root and root:FindFirstChild(S(664))
    local row = list and list:FindFirstChild(S(763) .. zoneIndex)
    if not row then
        return nil
    end
    for _, descendant in ipairs(row:GetDescendants()) do
        if descendant:IsA(S(766)) and descendant.Text ~= S(29) then
            return descendant.Text
        end
    end
    return nil
end
function api.NodeLevel(zoneKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    if not mine then
        return nil
    end
    local ok, level = pcall(mineConfig.GetNodeLevel, mine.Skills, zoneKey)
    return ok and tonumber(level) or nil
end
function api.CoreNodes()
    if not modules() then
        return {}
    end
    local mine = api.GetMineData()
    if not mine then
        return {}
    end
    local list = {}
    pcall(function()
        for _, node in ipairs(mineConfig.CoreNodes or {}) do
            local upgrade = mineConfig.CoreUpgradeById[node.Family]
            local level = tonumber(mineConfig.GetCoreLevel(mine, node.Family)) or 0
            local cost = nil
            if upgrade and type(upgrade.Cost) == S(829) then
                local ok, value = pcall(upgrade.Cost, level)
                cost = ok and tonumber(value) or nil
            end
            table.insert(list, {
                id = node.Id,
                family = node.Family,
                row = node.Row or 0,
                level = level,
                cost = cost,
                event = upgrade and upgrade.Event or nil,
                maxed = mineConfig.IsCoreNodeMaxed(mine, node) and true or false,
                open = mineConfig.IsCoreNodeOpen(mine, node) and true or false,
            })
        end
    end)
    return list
end
function api.BuyCore(eventName)
    return send(eventName)
end
function api.UpgradeCharacter(characterKey, stat)
    return send(S(640), characterKey, stat == S(646) and S(181) or stat)
end
local STAT_FIELDS = {
    Damage = { level = S(116), max = S(206), cost = S(183) },
    Speed = { level = S(618), max = S(679), cost = S(547) },
    MineSpeed = { level = S(956), max = S(520), cost = S(531) },
}
function api.StatInfo(holderKey, stat)
    if not modules() then
        return nil
    end
    local field = STAT_FIELDS[stat]
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (field and holder and holder.CharName) then
        return nil
    end
    local info = nil
    pcall(function()
        local itemData = items.GetItemData(holder.CharName)
        if not itemData then
            return
        end
        local level = tonumber(mineConfig[field.level](holder)) or 0
        local max = tonumber(mineConfig[field.max]) or 0
        info = {
            level = level,
            max = max,
            maxed = level >= max,
            cost = level < max
                and tonumber(mineConfig[field.cost](level, itemData))
                or nil,
        }
    end)
    return info
end
function api.CharacterBaseDamage(charName)
    if not modules() or type(charName) ~= S(737) or charName == S(77) then
        return nil
    end
    local ok, damage = pcall(function()
        local itemData = items.GetItemData(charName)
        if not itemData then
            return nil
        end
        return tonumber(mineConfig.GetCharacterDamage(itemData))
    end)
    return ok and damage or nil
end
function api.HolderBaseDamage(holderKey)
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local base = api.CharacterBaseDamage(holder.CharName)
    if not base then
        return nil
    end
    return base * (api.HolderVariantMult(holderKey) or 1)
end
function api.MutationRollCost(holderKey)
    if not modules() then
        return nil
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not (holder and holder.CharName) then
        return nil
    end
    local ok, cost = pcall(function()
        return tonumber(mineConfig.GetMutationRollCost(items.GetItemData(holder.CharName), mine))
    end)
    return ok and cost or nil
end
function api.HolderVariantMult(holderKey)
    if not modules() then
        return 1
    end
    local mine = api.GetMineData()
    local holder = mine and (mine.Holders or {})[holderKey]
    if not holder then
        return 1
    end
    local ok, mult = pcall(function()
        return tonumber(mineConfig.GetVariantMult(mineConfig.GetHolderVariant(holder)))
    end)
    return (ok and mult) or 1
end
function api.ClaimableIndexTiers()
    if not modules() then
        return 0
    end
    local mine = api.GetMineData()
    if not mine then
        return 0
    end
    local ok, result = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(result) ~= S(875) then
        return 0
    end
    return tonumber(result.Total) or 0
end
local function heldCharacterNames(mine)
    local held = {}
    for stackKey, count in pairs(mine.Characters or {}) do
        if type(count) == S(359) and count > 0 then
            local ok, name = pcall(mineConfig.ParseCharStack, stackKey)
            if ok and type(name) == S(493) then
                held[name] = true
            end
        end
    end
    for _, holder in pairs(mine.Holders or {}) do
        if type(holder) == S(441) and type(holder.CharName) == S(449) then
            held[holder.CharName] = true
        end
    end
    return held
end
local EXTRA_INDEX_TABS = { S(112), S(390) }
local function indexTabKeys()
    local tabs = {}
    for _, zoneKey in ipairs(api.ZoneKeys()) do
        table.insert(tabs, zoneKey)
    end
    for _, extra in ipairs(EXTRA_INDEX_TABS) do
        table.insert(tabs, extra)
    end
    return tabs
end
local function indexCategoryKey(tabKey)
    local ok, category = pcall(indexRewards.Category, tabKey)
    if ok and type(category) == S(325) and category.Key then
        return category.Key
    end
    return tabKey
end
function api.ClaimableIndexRewards()
    if not modules() then
        return {}, S(721)
    end
    local mine = api.GetMineData()
    if not mine then
        return {}, S(142)
    end
    if indexRewards.Enabled ~= true then
        return {}, S(22)
    end
    local ok, tiers = pcall(indexRewards.ClaimableTiers, mine)
    if not ok or type(tiers) ~= S(452) then
        return {}, S(629)
    end
    local total = tonumber(tiers.Total) or 0
    if total <= 0 then
        return {}, S(633)
    end
    local byKey = tiers.ByKey or {}
    local list = {}
    local notes = {}
    local held = heldCharacterNames(mine)
    for _, tabKey in ipairs(indexTabKeys()) do
        local waiting = tonumber(byKey[tabKey]) or 0
        if waiting > 0 then
            local category = indexCategoryKey(tabKey)
            local gotState, state = pcall(indexRewards.State, mine, category, held)
            if not gotState or type(state) ~= S(614) or type(state.Track) ~= S(60) then
                table.insert(notes, tabKey .. S(432) .. tostring(category) .. S(37))
            else
                local before = #list
                for _, tier in ipairs(state.Track) do
                    if tier.Reached and not tier.Claimed then
                        local claimed = tier.ClaimedRewards or {}
                        for index in ipairs(tier.Rewards or {}) do
                            if not claimed[index] then
                                table.insert(list, {
                                    category = category,
                                    tierKey = tier.Key,
                                    index = index,
                                })
                            end
                        end
                    end
                end
                if #list == before then
                    table.insert(notes, tabKey .. S(952) .. waiting
                        .. S(696))
                end
            end
        end
    end
    if #list == 0 then
        return {}, S(423) .. total .. S(504)
            .. (#notes > 0 and (S(858) .. table.concat(notes, S(101))) or S(684))
    end
    return list
end
function api.ClaimIndexReward(reward)
    if not modules() then
        return false
    end
    local ok, eventName = pcall(function()
        return indexRewards.Events.Claim
    end)
    if not ok or type(eventName) ~= S(263) then
        return false
    end
    return send(eventName, reward.category, reward.tierKey, reward.index)
end
return api
end
end
modules[S(79)] = function()
return function(import)
local Players = game:GetService(S(581))
local gameApi = import(S(965))(import)
local log = import(S(436))
local SUFFIXES = {
    K = 1e3, M = 1e6, B = 1e9, T = 1e12, Qa = 1e15, Qi = 1e18,
}
local function parsePower(text)
    local number, suffix = text:match(S(806))
    if not number then
        return nil
    end
    local value = tonumber((number:gsub(S(735), S(53))))
    if not value then
        return nil
    end
    if suffix ~= S(973) then
        local multiplier = SUFFIXES[suffix]
            or SUFFIXES[suffix:sub(1, 1):upper() .. suffix:sub(2)]
        value = value * (multiplier or 1)
    end
    return value
end
local reported = {}
local function getContainer()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(373))
    local mineGui = playerGui and playerGui:FindFirstChild(S(651))
    local hotbar = mineGui and mineGui:FindFirstChild(S(489))
    local content = hotbar and hotbar:FindFirstChild(S(488))
    return content and content:FindFirstChild(S(113))
end
local function readCount(slotFrame)
    for _, descendant in ipairs(slotFrame:GetDescendants()) do
        if descendant:IsA(S(173)) then
            local count = descendant.Text:match(S(518))
            if count then
                return tonumber(count)
            end
        end
    end
    return nil
end
local function readSlot(slotFrame)
    local itemFrame = slotFrame:FindFirstChild(S(78))
    if not (itemFrame and itemFrame.Visible) then
        return nil
    end
    local content = itemFrame:FindFirstChild(S(838))
    local nameLabel = content and content:FindFirstChild(S(552))
    local damageLabel = content and content:FindFirstChild(S(937))
    if not (nameLabel and damageLabel) or nameLabel.Text == S(109) then
        return nil
    end
    local name = nameLabel.Text
    return {
        name = name,
        power = parsePower(damageLabel.Text) or gameApi.CharacterBaseDamage(name) or 0,
        count = readCount(slotFrame),
    }
end
local function getMinerSlots()
    local container = getContainer()
    if not container then
        return {}
    end
    local slots = {}
    for _, slotFrame in ipairs(container:GetChildren()) do
        local slot = tonumber(slotFrame.Name)
        if slot and slot >= 1 and slot <= 9 then
            local info = readSlot(slotFrame)
            if info and (info.count == nil or info.count > 0) then
                info.slot = slot
                local key = info.name .. S(393) .. slot
                if not reported[key] then
                    reported[key] = true
                    log(S(185) .. slot .. S(135) .. info.name
                        .. S(306) .. info.power .. S(591) .. tostring(info.count))
                end
                table.insert(slots, info)
            end
        end
    end
    table.sort(slots, function(a, b)
        return a.power > b.power
    end)
    return slots
end
return getMinerSlots
end
end
modules[S(713)] = function()
local Players = game:GetService(S(346))
local Workspace = game:GetService(S(575))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(384))
    local plots = mineWorld and mineWorld:FindFirstChild(S(621))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(519)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(48)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(278)),
        Nodes = plot:FindFirstChild(S(609)),
        Chest = plot:FindFirstChild(S(864)),
        Zones = plot:FindFirstChild(S(120)),
        Crew = plot:FindFirstChild(S(311)),
        Loot = plot:FindFirstChild(S(840)),
        PlotIndex = plot:GetAttribute(S(429)),
        ChestCapacity = plot:GetAttribute(S(465)),
    }
end
return getPlotParts
end
modules[S(894)] = function()
return function(import)
    local createWindow = import(S(786))(import)
    local i18n = import(S(466))
    local antiAfk = import(S(672))(import)
    local buildEnv = import(S(723))
    local scheduler = import(S(269))(import)
    local config = import(S(208))(import)
    local log = import(S(398))
    log(S(9) .. buildEnv.Commit .. S(451) .. buildEnv.Env
        .. S(566) .. buildEnv.BuiltAt .. S(334))
    local window = createWindow({
        Subtitle = S(107),
    })
    local function persistentToggle(card, key, apply, default)
        local saved = config.Get(key, default or false)
        card:AddToggle(saved, function(state)
            config.Set(key, state)
            apply(state)
        end)
        apply(saved)
    end
    local function persistentSlider(tab, title, subtitle, min, max, key, default, apply, suffix)
        local saved = config.Get(key, default)
        tab:AddSlider(title, subtitle, min, max, saved, function(value)
            config.Set(key, value)
            apply(value)
        end, suffix)
        apply(saved)
    end
    local function persistentSegmented(tab, options, values, key, default, apply)
        local savedIndex = config.Get(key, default)
        tab:AddSegmented(options, savedIndex, function(index)
            config.Set(key, index)
            apply(values[index])
        end)
        apply(values[savedIndex] or values[default])
    end
    local autoMine = import(S(76))(import)
    local autoPlace = import(S(745))(import)
    local autoChest = import(S(681))(import)
    local autoSell = import(S(256))(import)
    local autoUpgrade = import(S(11))(import)
    local autoResearch = import(S(218))(import)
    local autoClaim = import(S(553))(import)
    local autoPlot = import(S(682))(import)
    local gameApi = import(S(275))(import)
    scheduler.RegisterStandalone(S(145), autoMine.Step, autoMine.IsEnabled)
    scheduler.RegisterExclusive(S(574), autoChest.Step, autoChest.IsEnabled, autoChest.IsBusy, autoChest.HasWork)
    scheduler.RegisterExclusive(S(156), autoPlace.Step, autoPlace.IsEnabled, autoPlace.IsBusy, autoPlace.HasWork)
    scheduler.RegisterExclusive(S(131), autoUpgrade.Step, autoUpgrade.IsEnabled, autoUpgrade.IsBusy, autoUpgrade.HasWork)
    scheduler.RegisterExclusive(S(86), autoResearch.Step, autoResearch.IsEnabled, autoResearch.IsBusy, autoResearch.HasWork)
    scheduler.RegisterExclusive(S(20), autoClaim.Step, autoClaim.IsEnabled, autoClaim.IsBusy, autoClaim.HasWork)
    scheduler.RegisterExclusive(S(873), autoPlot.Step, autoPlot.IsEnabled, autoPlot.IsBusy, autoPlot.HasWork)
    autoSell.SetHoldWhile(function()
        return autoPlace.IsEnabled() and autoPlace.HasWork()
    end)
    local sellMaxWait = config.Get(S(550), 20)
    scheduler.RegisterExclusive(S(88), autoSell.Step, autoSell.IsEnabled, autoSell.IsBusy, autoSell.HasWork, sellMaxWait)
    scheduler.Start()
    local farmTab = window:AddTab(S(177), S(198))
    persistentToggle(farmTab:AddCard(
        S(932),
        S(702)
    ), S(627), autoMine.SetEnabled)
    persistentSlider(farmTab, S(639), S(678),
        1, 30, S(335), 5, autoMine.SetCyclesPerSecond, S(190))
    persistentSlider(farmTab, S(191), S(283),
        1, 10, S(51), 1, autoMine.SetClicksPerCycle, S(341))
    persistentToggle(farmTab:AddCard(
        S(499),
        S(222)
    ), S(492), autoPlace.SetEnabled)
    persistentSlider(farmTab, S(91), S(82),
        1, 60, S(819), 3, autoPlace.SetCheckInterval, S(752))
    persistentToggle(
        farmTab:AddCard(S(597),
            S(902)),
        S(254), autoPlace.SetUpgradeWeakMiners
    )
    persistentToggle(
        farmTab:AddCard(S(211),
            S(246)),
        S(19), autoPlace.SetUseEquipBest, true
    )
    persistentSlider(farmTab, S(43), S(698),
        5, 300, S(872), 60, autoPlace.SetEquipBestInterval, S(534))
    persistentToggle(
        farmTab:AddCard(S(501),
            S(214)),
        S(803), autoPlace.SetUnlockSlots
    )
    persistentSlider(farmTab, S(779), S(773),
        0, 500, S(7), 0,
        function(value) autoPlace.SetUnlockBudget(value * 1e6) end, S(546))
    persistentToggle(farmTab:AddCard(
        S(84),
        S(536)
    ), S(756), autoChest.SetEnabled)
    persistentSlider(farmTab, S(227), S(251),
        50, 3000, S(778), 100, autoChest.SetMinItems, S(626))
    persistentSlider(farmTab, S(762), S(83),
        1, 60, S(118), 3, autoChest.SetCheckInterval, S(494))
    persistentToggle(farmTab:AddCard(
        S(13),
        S(336)
    ), S(188), autoSell.SetEnabled)
    persistentToggle(
        farmTab:AddCard(S(804),
            S(130)),
        S(928), gameApi.SetProtectUsefulUnits, true
    )
    persistentSlider(farmTab, S(405), S(813),
        5, 300, S(612), 20, autoSell.SetCheckInterval, S(316))
    farmTab:AddSlider(S(893),
        S(660),
        5, 120, sellMaxWait, function(value)
            config.Set(S(323), value)
        end, S(100))
    persistentToggle(farmTab:AddCard(
        S(967),
        S(143)
    ), S(845), autoUpgrade.SetEnabled)
    persistentSlider(farmTab, S(654), S(734),
        5, 300, S(416), 20, autoUpgrade.SetInterval, S(308))
    persistentSlider(farmTab, S(572), S(166),
        1, 20, S(324), 5, autoUpgrade.SetMaxPerPass, S(630))
    persistentSegmented(
        farmTab,
        { S(2), S(868), S(315), S(358) },
        { S(92), S(292), S(16), S(842) },
        S(740), 1, autoUpgrade.SetPriority
    )
    persistentSlider(farmTab, S(951), S(326),
        0, 500, S(371), 0,
        function(value) autoUpgrade.SetKeepMoney(value * 1e6) end, S(797))
    persistentToggle(
        farmTab:AddCard(S(788),
            S(683)),
        S(54), autoUpgrade.SetBuyPickaxe, true
    )
    local pickaxes = gameApi.GetPickaxes()
    local pickaxeLabels = {}
    local highestIndex = 0
    for _, pickaxe in ipairs(pickaxes) do
        pickaxeLabels[pickaxe.index] = pickaxe.name .. S(309) .. pickaxe.multiplier .. S(564)
        highestIndex = math.max(highestIndex, pickaxe.index)
    end
    if highestIndex > 0 then
        local pickaxeCard
        local function describePick(index)
            if index == 0 then
                return S(138)
            end
            return S(548) .. index .. S(368) .. (pickaxeLabels[index] or S(453))
        end
        local savedPick = config.Get(S(869), 0)
        pickaxeCard = farmTab:AddSlider(
            S(260),
            describePick(savedPick),
            0, highestIndex, savedPick,
            function(value)
                config.Set(S(958), value)
                autoUpgrade.SetPickaxeTarget(value)
                pickaxeCard:SetSubtitle(describePick(value))
            end,
            S(197)
        )
        autoUpgrade.SetPickaxeTarget(savedPick)
    end
    persistentToggle(
        farmTab:AddCard(S(805),
            S(560)),
        S(777), autoUpgrade.SetLimitPickaxesToHolders, true
    )
    persistentToggle(
        farmTab:AddCard(S(543),
            S(769)),
        S(561), autoUpgrade.SetEquipPickaxes, true
    )
    persistentToggle(
        farmTab:AddCard(S(889),
            S(108)),
        S(653), autoUpgrade.SetRollMutations
    )
    persistentSlider(farmTab, S(63), S(482),
        1, 20, S(529), 2, autoUpgrade.SetMutationTarget, S(5))
    persistentSlider(farmTab, S(239), S(573),
        0, 500, S(617), 0,
        function(value) autoUpgrade.SetMutationBudget(value * 1e6) end, S(286))
    persistentToggle(farmTab:AddCard(
        S(730),
        S(329)
    ), S(96), autoResearch.SetEnabled)
    persistentSlider(farmTab, S(704), S(21),
        10, 600, S(563), 30, autoResearch.SetCheckInterval, S(340))
    persistentSlider(farmTab, S(970), S(542),
        1, 20, S(127), 5, autoResearch.SetMaxPerPass, S(862))
    persistentSlider(farmTab, S(793), S(231),
        0, 500, S(34), 0,
        function(value) autoResearch.SetKeepMoney(value * 1e6) end, S(606))
    persistentToggle(
        farmTab:AddCard(S(338),
            S(380)),
        S(463), autoResearch.SetBuyZones, true
    )
    persistentToggle(
        farmTab:AddCard(S(236),
            S(103)),
        S(137), autoResearch.SetBuyCore, true
    )
    persistentToggle(
        farmTab:AddCard(S(4),
            S(544)),
        S(556), autoResearch.SetCoreLastResort, true
    )
    persistentSegmented(
        farmTab,
        { S(66), S(302) },
        { true, false },
        S(677), 1, autoResearch.SetCheapestFirst
    )
    persistentToggle(farmTab:AddCard(
        S(348),
        S(588)
    ), S(299), autoClaim.SetEnabled)
    persistentSlider(farmTab, S(479), S(35),
        5, 300, S(945), 30, autoClaim.SetCheckInterval, S(888))
    persistentSlider(farmTab, S(854), S(600),
        1, 30, S(407), 10, autoClaim.SetMaxPerPass, S(287))
    persistentToggle(farmTab:AddCard(
        S(907),
        S(668)
    ), S(491), autoPlot.SetEnabled)
    persistentSlider(farmTab, S(301), S(562),
        5, 300, S(461), 30, autoPlot.SetCheckInterval, S(637))
    persistentToggle(
        farmTab:AddCard(S(515),
            S(511)),
        S(726), autoPlot.SetBuyNextZone, true
    )
    persistentToggle(
        farmTab:AddCard(S(975),
            S(230)),
        S(204), autoPlot.SetUseLatestZone, true
    )
    persistentSlider(farmTab, S(822), S(161),
        0, 500, S(874), 0,
        function(value) autoPlot.SetKeepMoney(value * 1e6) end, S(233))
    local stopDebug = nil
    if buildEnv.IsDev then
        stopDebug = import(S(920))(import)(window, {
            Toggle = persistentToggle,
            Slider = persistentSlider,
        })
    end
    local settingsTab = window:AddTab(S(247), S(644))
    persistentToggle(
        settingsTab:AddCard(S(675), S(408)),
        S(886),
        antiAfk.SetEnabled
    )
    local savedScale = config.Get(S(455), 100)
    settingsTab:AddSlider(S(32), S(259), 50, 150, savedScale, function(value)
        config.Set(S(919), value)
        window:SetScale(value)
    end)
    window:SetScale(savedScale)
    local savedLanguage = config.Get(S(94), 1)
    settingsTab:AddSegmented({ S(410), S(87), S(165) }, savedLanguage, function(index, value)
        local code = ({ S(224), S(579), S(530) })[index]
        config.Set(S(931), index)
        i18n.SetLanguage(code)
    end)
    i18n.SetLanguage(({ S(807), S(884), S(753) })[savedLanguage] or S(478))
    log(S(688) .. config.Path())
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
        autoResearch.SetEnabled(false)
        autoClaim.SetEnabled(false)
        autoPlot.SetEnabled(false)
        if stopDebug then
            stopDebug()
        end
    end)
end
end
modules[S(194)] = function()
return {
    [79389059854988] = S(90),
}
end
import(S(545))(import)
