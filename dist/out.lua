-- AUTO-GENERATED by scripts/build.py. Do not edit directly;
-- edit the source files under core/ and games/, then rebuild.
local STR = {"", " = ", "games/my_anime_mine/debug_dump", "กดเพื่อบันทึกโครงสร้างข้างใน Node แรกกับ Holder แรกลง MekPro.log", "[MyAnimeMine] no holders found", "games/my_anime_mine/main", "เปิดหน้าจอที่อยากดูก่อน (เช่นหน้า Take All ตอนเปิด chest) แล้วกดปุ่มนี้", "games/my_anime_mine/auto_chest", "disabled", "Dump", "MineGui", "PlayerGui", "SellFrame", "core/dispatch", "Tool", "Frame", "TextBox", " (MaxActivationDistance=%s)", "  [attr] ", "ย่อขยายเมนูทั้งชุด", " = ", "Text", "TextLabel", "Sell for", "Holders", "🏠", " (Text=", "Players", "[AutoChest] enabled (collects once >= ", "games/my_anime_mine/get_plot_parts", "เปิดแล้วไปกด/แตะอะไรก็ได้ในเกม ทุก prompt/click/ปุ่มจะถูกบันทึกลง MekPro.log", "games/my_anime_mine/get_plot_parts", "AutoUpgrade", "TextButton", "core/ui", "[AutoPlace] placed ", "MouseButton1Down", " (ActionText=", "[MyAnimeMine] === Node: ", "open_menu_hint", "core/logger", "TextButton", "core/i18n", "games/my_anime_mine/get_plot_parts", "TakeButton", "Done", "MouseButton1Click", "ค้นหาฟังก์ชัน...", "  ", " remotes", "", "Frame", "[MyAnimeMine] no nodes present right now", "Loot", "[AutoSell] fireproximityprompt error: ", "[AntiAFK] disabled", "Frame", "core/logger", "MouseButton1Up", "Auto Chest", "core/anti_afk", "ClickDetector", "TextLabel", "write reported success but ", "RunService", "games/my_anime_mine/debug_dump_remotes", "Scroll", "games/my_anime_mine/debug_dump_sell_cards", "Frame", "core/logger", "games/my_anime_mine/auto_upgrade", "Plots", "[SellCards] === total cards: ", "GuiObject", "games/my_anime_mine/auto_mine", "GuiButton", "Players", "Humanoid", "auto", "กดเพื่อบันทึก attribute/โครงสร้างของ miner ตัวแรกใน Backpack ลง MekPro.log", "Players", "UIListLayout", "Interaction Logger (debug)", "[SellCards] Card #", "core/i18n", " ===", "", "Players", "[AutoSell] clicked SelectAllButton (", "[Scheduler] ", "TextLabel", "Content", "Auto Sell", "ProximityPrompt", " error: ", " cards)", "[AutoUpgrade] ", "ScrollingFrame", "core/ui", "MekProOriginalPosition", "ValueLabel", "leaderstats", "games/my_anime_mine/get_current_plot", "Chest", "neither appendfile nor writefile is available", " items, waiting for ", "MouseButton1Down", ": ", "Dump", "RunService", "Open menu", "TextLabel", "ProximityPrompt", "Text", "UIPadding", "[MyAnimeMine] === Holder: ", "", "Frame", "RemoteFunction", "MekProOriginalPosition", "เปิด chest แล้วกดปุ่ม Take All อัตโนมัติ", "'", "HumanoidRootPart", "search_placeholder", " ===", "Label", "Workspace", "Content", "no character/backpack", "Content", "RemoteEvent", "TextButton", "games/my_anime_mine/debug_dump_remotes", ": ", "[MyAnimeMine] === Dump start ===", " (Text=", "Auto Place Miner", "Activated", "games/my_anime_mine/debug_dump_sell_cards", "Debug: Dump Node/Holder", " items)", "core/logger", "PlotIndex", "[MyAnimeMine] not standing in any plot", "GuiButton", "-", "Frame", "Error: ", "'", "AutoChest", " on ", "core/anti_afk", "", "games/my_anime_mine/get_current_plot", "X", "Close", "[AutoPlace] fireproximityprompt error: ", "Anti-AFK", "[Interaction] Key pressed: %s (gameProcessed=%s)", "กดเพื่อบันทึกรายชื่อ RemoteEvent/RemoteFunction ทั้งหมดใน ReplicatedStorage ลง MekPro.log", "TextLabel", "core/teleport", "Players", "TextLabel", "Content", "TextButton", "'", "ChestFrame", "no isfile/readfile available to verify the write", "TextLabel", "games/my_anime_mine/auto_mine", "core/get_leaderstat", "Zones", "Debug: Dump Sell Cards", "RunService", "search_placeholder", "enabled", "games/my_anime_mine/get_current_plot", "ProximityPrompt", "เปิดหน้าขายของ (SellStand) ก่อน แล้วกดปุ่มนี้เพื่อดูว่าแต่ละ Card ระบุไอเทมด้วยอะไร", "core/get_me", " ===", "disabled", "Content", "open_menu_hint", "^(%d+)", "[AutoSell] nothing to sell, closing", "not found", "PlayerGui", "AutoPlace", "games/my_anime_mine/get_plot_parts", "games/my_anime_mine/debug_dump", "games/my_anime_mine/auto_place", "MineGui", "UserInputService", "[MyAnimeMine] === Tool: ", " = ", "no tool in Backpack (all miners already placed?)", "not standing in any plot", "core/i18n", " holders checked)", "Header", "enabled", "Workspace", "Debug: Dump Backpack Tool", "Debug: Current Plot", "[AutoChest] only ", "UIScale", "Frame", "games/my_anime_mine/get_plot_parts", " (ActionText=%s ObjectText=%s HoldDuration=%s)", "Empty miner slot", "Frame", "%", "'", "[AutoSell] enabled (only sells when there's something in the scroll)", "disabled", "Search", "[AntiAFK] enabled - watching Player.Idled", "plot has no Holders folder", "TextButton", "TextButton", "games/registry", "ตั้งค่า", "Players", "Card", "Players", "ProximityPrompt", "Backpack", "games/my_anime_mine/get_current_plot", "[AutoChest] disabled", "", "CoreUI", "TextButton", "", "SelectAllButton", "กดหินอัตโนมัติทุก Node ใน plot ปัจจุบัน (ยิง ClickDetector 'NodeClick')", "หยิบ miner จาก Backpack ไปวางลง Holder ที่ว่างอัตโนมัติ", "Debug: Dump Visible GUI", "UIStroke", "games/my_anime_mine/auto_upgrade", "[GuiDump] MineGui not found", "MouseButton1Click", ": ", "", "Frame", "ขนาดเมนู", "Actions", "games/my_anime_mine/auto_sell", "อังกฤษ", "TextButton", "core/logger", "  ", "games/my_anime_mine/auto_place", "core/logger", "Backpack", "Badge", "UserInputService", "PlayerGui", "Nodes", "Frame", "[%X] ", "[AutoSell] disabled", "Humanoid", "Crew", " <-- remote, likely fired by the real interaction", "  ", "games/my_anime_mine/main", "TextLabel", "ฟาร์ม", "[Remotes] ", "core/logger", "กด UpgradeClick ทุก Holder อัตโนมัติเมื่อมีเงินพอ", "Done", "MekProOriginalPosition", "ปุ่มเปิดเมนู", "Frame", "core/logger", " '", "This is not a supported place.", "[AutoChest] ChestFrame.Visible = ", "games/my_anime_mine/get_current_plot", "ตามเครื่อง", "games/my_anime_mine/debug_dump_gui", ")", "en", " '", "games/my_anime_mine/auto_chest", "th", "write reported success but the line isn't in ", "Tool", "Debug: Dump All Remotes", "Players", "log failed", "ClickDetector", "TextLabel", "MekProOriginalPosition", "ValueBase", "Workspace", "MineWorld", "Players", "en-us", "enabled", "Dump", "[AutoChest] fireproximityprompt error: ", "Players", "", "MekPro", "⚙", "TextButton", "Dump", "core/dispatch", "", ")", "[Remotes] === total: ", "Done", "[Interaction] logger enabled - go click/interact with things now", "auto", "ScreenGui", "[GuiDump] === visible MineGui tree ===", "Backpack", "MineGui", "TextLabel", "MekProOriginalPosition", "games/registry", "", "core/get_place_id", "[AutoPlace] ", "th", "core/logger", "ไทย", "no empty holder found (", "ValueLabel", "Card", " '", "[Scheduler] turn: ", "Check", "Progress", "Players", "\n", "[GuiDump] === end ===", "core/interaction_logger", "core/interaction_logger", "[AutoPlace] ", "", "SellButton", "(none enabled)", "games/my_anime_mine/get_current_plot", "เดินไปกด SellStand แล้วกด Select All + Sell อัตโนมัติ (ยืนยันจาก log ว่าขายได้จริง)", "core/turn_scheduler", "", "%", "UICorner", "core/teleport", "ClickDetector", "core/logger", "Auto Upgrade", "AutoMine", " ", "MineGui", "Select All", "TabBar", "Badge", "Pad", "MouseButton1Up", "[AntiAFK] idle detected, simulating input", "PlayerGui", "UpgradePart", "Scroll", "no tool", " '", "VirtualUser", "SellFrame", "", "ป้องกันถูกเตะออกจากห้องเวลาไม่ได้ขยับตัว", "ClickDetector", "กดเพื่อเช็ค plot ปัจจุบัน (ผลขึ้นตรงบนปุ่มเอง ไม่ต้องพึ่ง console)", "[Interaction] logger disabled", "MekPro.log", "RemoteFunction", "TextLabel", "games/my_anime_mine/debug_dump_gui", "TextLabel", "th", "  [attr] ", "MekProOriginalPosition", " doesn't exist", "GuiObject", ")", "SellStand", "core/logger", "CoreGui", "?", "games/my_anime_mine/get_plot_parts", "Content", "UICorner", "Label", "Players", "PlayerGui", "[AutoSell] SellFrame.Visible = ", "Search functions...", "core/logger", " items)", " cards", "Content", "ProximityPrompt", "RemoteEvent", "[AutoMine] ", "[MyAnimeMine] current plot: %s | Nodes: %d | Holders: %d", "[Interaction] ProximityPrompt triggered: ", "Close", "[Logger] failed to write ", "UIListLayout", "", "  [attr] ", "[SellCards] SellFrame.Content.Scroll not found - open the sell menu first", "ClickDetector", ",", "[AutoChest] clicked TakeButton (", "Dump", "Activated", "Main", "no humanoid", " = ", " = ", "[Interaction] ClickDetector clicked: ", "core/turn_scheduler", "UIListLayout", "core/logger", " ===", "TextLabel", "[Interaction] Button activated: ", "Auto Mine", "ReplicatedStorage", "My Anime Mine · Mining Hub", "en", "[AutoSell] clicked SellButton", "OwnerUserId", "  [attr] ", "ChestCapacity", "games/my_anime_mine/auto_sell", "AutoSell", "\n", "ProximityPrompt", " ===", "no plot", "core/get_place_id"}
local function S(i) return STR[i] end
local modules = {}
local cache = {}
local function import(path)
    if cache[path] == nil then
        cache[path] = modules[path]()
    end
    return cache[path]
end
modules[S(152)] = function()
return function(import)
local log = import(S(278))
local Players = game:GetService(S(301))
local VirtualUser = game:GetService(S(372))
local antiAfk = {}
local connection = nil
function antiAfk.SetEnabled(enabled)
    if enabled == antiAfk.IsEnabled() then
        return
    end
    if enabled then
        local player = Players.LocalPlayer
        log(S(219))
        connection = player.Idled:Connect(function()
            log(S(366))
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    else
        log(S(56))
        connection:Disconnect()
        connection = nil
    end
end
function antiAfk.IsEnabled()
    return connection ~= nil
end
return antiAfk
end
end
modules[S(312)] = function()
return function(import)
    local getPlaceId = import(S(327))
    local registry = import(S(223))
    local scriptPath = registry[getPlaceId()]
    if not scriptPath then
        print(S(280))
        return
    end
    import(scriptPath)(import)
end
end
modules[S(172)] = function()
local function getLeaderstat(player, statName, timeout)
    local leaderstats = player:FindFirstChild(S(102))
    if not leaderstats then
        return nil
    end
    local stat = leaderstats:FindFirstChild(statName)
    if not stat and timeout then
        stat = leaderstats:WaitForChild(statName, timeout)
    end
    return stat
end
return getLeaderstat
end
modules[S(181)] = function()
local Players = game:GetService(S(77))
local function getMe()
    local player = Players.LocalPlayer
    local character = player.Character or player.CharacterAdded:Wait()
    local humanoid = character:WaitForChild(S(264))
    local backpack = player:WaitForChild(S(321))
    return player, humanoid, backpack
end
return getMe
end
modules[S(447)] = function()
local function getPlaceId()
    return game.PlaceId
end
return getPlaceId
end
modules[S(85)] = function()
local Players = game:GetService(S(225))
local STRINGS = {
    en = {
        search_placeholder = S(401),
        open_menu_hint = S(111),
    },
    th = {
        search_placeholder = S(48),
        open_menu_hint = S(276),
    },
}
local function detectSystemLanguage()
    local player = Players.LocalPlayer
    local locale = player and player.LocaleId or S(302)
    if locale:sub(1, 2):lower() == S(289) then
        return S(384)
    end
    return S(286)
end
local i18n = {}
i18n.current = detectSystemLanguage()
local listeners = {}
function i18n.SetLanguage(code)
    if code == S(79) then
        code = detectSystemLanguage()
    end
    i18n.current = code
    for _, listener in ipairs(listeners) do
        listener(code)
    end
end
function i18n.T(key)
    local dict = STRINGS[i18n.current] or STRINGS.en
    return dict[key] or STRINGS.en[key] or key
end
function i18n.OnChanged(callback)
    table.insert(listeners, callback)
    return function()
        local index = table.find(listeners, callback)
        if index then
            table.remove(listeners, index)
        end
    end
end
return i18n
end
modules[S(343)] = function()
return function(import)
local Players = game:GetService(S(163))
local Workspace = game:GetService(S(204))
local UserInputService = game:GetService(S(258))
local log = import(S(356))
local interactionLogger = {}
local connections = nil
local function describe(instance)
    local ok, path = pcall(function()
        return instance:GetFullName()
    end)
    return ok and path or instance.Name
end
local function watchPrompt(prompt, list)
    table.insert(list, prompt.Triggered:Connect(function()
        log(S(410) .. describe(prompt)
            .. S(38) .. prompt.ActionText .. S(285))
    end))
end
local function watchClickDetector(detector, list)
    table.insert(list, detector.MouseClick:Connect(function()
        log(S(426) .. describe(detector))
    end))
end
local function watchButton(button, list)
    table.insert(list, button.Activated:Connect(function()
        log(S(432) .. describe(button)
            .. S(27) .. tostring(button.Text) .. S(389))
    end))
end
local function scan(root, list)
    for _, descendant in ipairs(root:GetDescendants()) do
        if descendant:IsA(S(113)) then
            watchPrompt(descendant, list)
        elseif descendant:IsA(S(295)) then
            watchClickDetector(descendant, list)
        elseif descendant:IsA(S(145)) then
            watchButton(descendant, list)
        end
    end
end
function interactionLogger.SetEnabled(enabled)
    if enabled == interactionLogger.IsEnabled() then
        return
    end
    if not enabled then
        for _, connection in ipairs(connections) do
            connection:Disconnect()
        end
        connections = nil
        log(S(378))
        return
    end
    connections = {}
    log(S(317))
    local playerGui = Players.LocalPlayer:WaitForChild(S(259))
    scan(Workspace, connections)
    scan(playerGui, connections)
    table.insert(connections, Workspace.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(444)) then
            watchPrompt(descendant, connections)
        elseif descendant:IsA(S(355)) then
            watchClickDetector(descendant, connections)
        end
    end))
    table.insert(connections, playerGui.DescendantAdded:Connect(function(descendant)
        if descendant:IsA(S(76)) then
            watchButton(descendant, connections)
        end
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.UserInputType == Enum.UserInputType.Keyboard then
            log(string.format(
                S(159),
                input.KeyCode.Name, tostring(gameProcessed)
            ))
        end
    end))
end
function interactionLogger.IsEnabled()
    return connections ~= nil
end
return interactionLogger
end
end
modules[S(142)] = function()
local LOG_FILE = S(379)
local function log(message)
    local line = os.date(S(262)) .. message
    print(line)
    local ok, err = pcall(function()
        if appendfile then
            if isfile and not isfile(LOG_FILE) then
                writefile(LOG_FILE, S(235))
            end
            appendfile(LOG_FILE, line .. S(443))
        elseif writefile then
            local existing = S(345)
            if isfile and isfile(LOG_FILE) then
                existing = readfile(LOG_FILE)
            end
            writefile(LOG_FILE, existing .. line .. S(340))
        else
            error(S(105))
        end
        if not (isfile and readfile) then
            error(S(169))
        end
        if not isfile(LOG_FILE) then
            error(S(64) .. LOG_FILE .. S(387))
        end
        if not readfile(LOG_FILE):find(line, 1, true) then
            error(S(290) .. LOG_FILE)
        end
    end)
    if not ok then
        print(S(412) .. LOG_FILE .. S(108) .. tostring(err))
    end
    return ok, err
end
return log
end
modules[S(162)] = function()
return function(import)
local Players = game:GetService(S(227))
local teleport = {}
local savedCFrame = nil
local function getHumanoidRootPart()
    local character = Players.LocalPlayer.Character
    return character and character:FindFirstChild(S(123))
end
function teleport.To(part, offset)
    local hrp = getHumanoidRootPart()
    if not (hrp and part) then
        return false
    end
    if not savedCFrame then
        savedCFrame = hrp.CFrame
    end
    hrp.CFrame = CFrame.new(part.Position + (offset or Vector3.new(0, 3, 5)))
    return true
end
function teleport.Return()
    local hrp = getHumanoidRootPart()
    if hrp and savedCFrame then
        hrp.CFrame = savedCFrame
    end
    savedCFrame = nil
end
function teleport.IsAway()
    return savedCFrame ~= nil
end
return teleport
end
end
modules[S(350)] = function()
return function(import)
local RunService = game:GetService(S(175))
local log = import(S(330))
local SLICE_SECONDS = 3
local scheduler = {}
local tasks = {}
local connection = nil
local rrIndex = 0
local currentTask = nil
local sliceEnd = 0
function scheduler.Register(name, step, isEnabled)
    table.insert(tasks, { name = name, step = step, isEnabled = isEnabled })
end
local function pickNextEnabled()
    if #tasks == 0 then
        return nil
    end
    for _ = 1, #tasks do
        rrIndex = (rrIndex % #tasks) + 1
        local candidate = tasks[rrIndex]
        if candidate.isEnabled() then
            return candidate
        end
    end
    return nil
end
function scheduler.Start()
    if connection then
        return
    end
    connection = RunService.Heartbeat:Connect(function()
        local now = tick()
        if not currentTask or not currentTask.isEnabled() or now > sliceEnd then
            local next_ = pickNextEnabled()
            if next_ ~= currentTask then
                currentTask = next_
                log(S(336) .. (currentTask and currentTask.name or S(347)))
            end
            sliceEnd = now + SLICE_SECONDS
        end
        if currentTask then
            local ok, err = pcall(currentTask.step)
            if not ok then
                log(S(90) .. currentTask.name .. S(95) .. tostring(err))
            end
        end
    end)
end
function scheduler.Stop()
    if connection then
        connection:Disconnect()
        connection = nil
    end
    currentTask = nil
end
return scheduler
end
end
modules[S(35)] = function()
return function(import)
local i18n = import(S(43))
local UserInputService = game:GetService(S(195))
local THEME = {
    BrandName = S(308),
    Background = Color3.fromRGB(0xF1, 0xF2, 0xF6), 
    Card = Color3.fromRGB(0xFE, 0xFE, 0xFE), 
    CardHover = Color3.fromRGB(0xE9, 0xEA, 0xF0), 
    Accent = Color3.fromRGB(0x1C, 0xAF, 0x82), 
    ToggleOff = Color3.fromRGB(0xE3, 0xE7, 0xEA), 
    Text = Color3.fromRGB(0x25, 0x25, 0x25), 
    SubText = Color3.fromRGB(0x5F, 0x71, 0x7B), 
    Premium = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Badge = Color3.fromRGB(0xFD, 0x64, 0x12), 
    Font = Enum.Font.GothamMedium,
}
local RADIUS = {
    Small = 8,
    Control = 16,
    Card = 24,
}
local function corner(instance, radius)
    local c = Instance.new(S(353))
    c.CornerRadius = UDim.new(0, radius or RADIUS.Control)
    c.Parent = instance
    return c
end
local function pill(instance)
    local c = Instance.new(S(396))
    c.CornerRadius = UDim.new(0.5, 0)
    c.Parent = instance
    return c
end
local function padding(instance, amount)
    local p = Instance.new(S(115))
    p.PaddingLeft = UDim.new(0, amount)
    p.PaddingRight = UDim.new(0, amount)
    p.PaddingTop = UDim.new(0, amount)
    p.PaddingBottom = UDim.new(0, amount)
    p.Parent = instance
    return p
end
local function makeDraggable(handle, target, onClick)
    local dragging, dragInput, dragStart, startPos, moved
    local connections = {}
    table.insert(connections, handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            moved = false
            dragStart = input.Position
            startPos = target.Position
            table.insert(connections, input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                    if onClick and not moved then
                        onClick()
                    end
                end
            end))
        end
    end))
    table.insert(connections, handle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end))
    table.insert(connections, UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            if delta.Magnitude > 3 then
                moved = true
            end
            target.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end))
    return connections
end
local UI = {}
UI.__index = UI
function UI:AddTab(name, icon, opts)
    opts = opts or {}
    local window = self
    local tabButton = Instance.new(S(34))
    tabButton.Name = name
    tabButton.Size = UDim2.new(0, 44, 0, 44)
    tabButton.BackgroundColor3 = THEME.Card
    tabButton.AutoButtonColor = false
    tabButton.Text = S(117)
    tabButton.Parent = self.TabBar
    corner(tabButton, RADIUS.Control)
    local tabStroke = Instance.new(S(240))
    tabStroke.Color = THEME.ToggleOff
    tabStroke.Thickness = 1
    tabStroke.Parent = tabButton
    local icoLabel = Instance.new(S(431))
    icoLabel.BackgroundTransparency = 1
    icoLabel.Size = UDim2.new(1, 0, 1, 0)
    icoLabel.Text = icon or S(393)
    icoLabel.TextSize = 18
    icoLabel.Font = THEME.Font
    icoLabel.TextColor3 = THEME.SubText
    icoLabel.Parent = tabButton
    if opts.Premium then
        local tag = Instance.new(S(161))
        tag.Size = UDim2.new(0, 16, 0, 10)
        tag.Position = UDim2.new(1, -14, 0, -4)
        tag.BackgroundColor3 = THEME.Premium
        tag.Text = S(326)
        tag.Parent = tabButton
        corner(tag, 4)
    end
    local page = Instance.new(S(98))
    page.Name = name
    page.Size = UDim2.new(1, 0, 1, 0)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 3
    page.CanvasSize = UDim2.new(0, 0, 0, 0)
    page.AutomaticCanvasSize = Enum.AutomaticSize.Y
    page.Visible = next(self.Tabs) == nil
    page.Parent = self.Content
    local layout = Instance.new(S(82))
    layout.Padding = UDim.new(0, 10)
    layout.Parent = page
    local function setActive(activeName)
        for tabName, tab in pairs(self.Tabs) do
            tab.Button.BackgroundColor3 = tabName == activeName and THEME.Accent or THEME.Card
            tab.Icon.TextColor3 = tabName == activeName and THEME.Background or THEME.SubText
            tab.Page.Visible = tabName == activeName
        end
    end
    tabButton.MouseButton1Click:Connect(function()
        setActive(name)
    end)
    local tab = {}
    tab.Button = tabButton
    tab.Icon = icoLabel
    tab.Page = page
    function tab:AddSectionHeader(text)
        local lbl = Instance.new(S(381))
        lbl.Size = UDim2.new(1, 0, 0, 20)
        lbl.BackgroundTransparency = 1
        lbl.Text = text
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.TextColor3 = THEME.SubText
        lbl.TextSize = 13
        lbl.Font = THEME.Font
        lbl.Parent = page
        return lbl
    end
    function tab:AddCard(title, subtitle)
        local card = Instance.new(S(52))
        card.Size = UDim2.new(1, 0, 0, 0)
        card.AutomaticSize = Enum.AutomaticSize.Y
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Card)
        padding(card, 12)
        local titleLbl = Instance.new(S(164))
        titleLbl.Size = UDim2.new(1, -80, 0, 20)
        titleLbl.BackgroundTransparency = 1
        titleLbl.Text = title
        titleLbl.Font = THEME.Font
        titleLbl.TextSize = 15
        titleLbl.TextColor3 = THEME.Text
        titleLbl.TextXAlignment = Enum.TextXAlignment.Left
        titleLbl.Parent = card
        local subtitleLbl
        if subtitle then
            subtitleLbl = Instance.new(S(296))
            subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
            subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
            subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
            subtitleLbl.BackgroundTransparency = 1
            subtitleLbl.Text = subtitle
            subtitleLbl.Font = Enum.Font.Gotham
            subtitleLbl.TextSize = 12
            subtitleLbl.TextWrapped = true
            subtitleLbl.TextColor3 = THEME.SubText
            subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
            subtitleLbl.Parent = card
        end
        table.insert(window.SearchIndex, { Frame = card, Title = title, Subtitle = subtitle })
        local cardApi = {}
        cardApi.Frame = card
        function cardApi:SetSubtitle(text)
            if not subtitleLbl then
                subtitleLbl = Instance.new(S(269))
                subtitleLbl.Size = UDim2.new(1, -80, 0, 0)
                subtitleLbl.AutomaticSize = Enum.AutomaticSize.Y
                subtitleLbl.Position = UDim2.new(0, 0, 0, 20)
                subtitleLbl.BackgroundTransparency = 1
                subtitleLbl.Font = Enum.Font.Gotham
                subtitleLbl.TextSize = 12
                subtitleLbl.TextWrapped = true
                subtitleLbl.TextColor3 = THEME.SubText
                subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
                subtitleLbl.Parent = card
            end
            subtitleLbl.Text = text
        end
        function cardApi:SetValue(text)
            local valueLbl = card:FindFirstChild(S(333))
            if not valueLbl then
                valueLbl = Instance.new(S(323))
                valueLbl.Name = S(101)
                valueLbl.Size = UDim2.new(0, 70, 0, 20)
                valueLbl.Position = UDim2.new(1, -70, 0, 0)
                valueLbl.BackgroundTransparency = 1
                valueLbl.Font = THEME.Font
                valueLbl.TextSize = 14
                valueLbl.TextColor3 = THEME.SubText
                valueLbl.TextXAlignment = Enum.TextXAlignment.Right
                valueLbl.Parent = card
            end
            valueLbl.Text = text
            return valueLbl
        end
        function cardApi:SetBadge(text, color)
            local badge = card:FindFirstChild(S(257))
            if not badge then
                badge = Instance.new(S(112))
                badge.Name = S(363)
                badge.Size = UDim2.new(0, 70, 0, 18)
                badge.Position = UDim2.new(1, -70, 0, 0)
                badge.Font = THEME.Font
                badge.TextSize = 11
                badge.TextColor3 = THEME.Text
                badge.Parent = card
                corner(badge, RADIUS.Small)
            end
            badge.Text = text
            badge.BackgroundColor3 = color or THEME.Badge
            return badge
        end
        function cardApi:AddButton(text, callback)
            local btn = Instance.new(S(221))
            btn.Size = UDim2.new(0, 70, 0, 28)
            btn.Position = UDim2.new(1, -70, 0, -4)
            btn.BackgroundColor3 = THEME.Accent
            btn.AutoButtonColor = false
            btn.Text = text
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.Background
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(callback)
            return btn
        end
        function cardApi:AddToggle(default, callback)
            local state = default or false
            local track = Instance.new(S(209))
            track.Size = UDim2.new(0, 40, 0, 22)
            track.Position = UDim2.new(1, -40, 0, 0)
            track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
            track.Parent = card
            pill(track)
            local knob = Instance.new(S(16))
            knob.Size = UDim2.new(0, 18, 0, 18)
            knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
            knob.BackgroundColor3 = THEME.Text
            knob.Parent = track
            pill(knob)
            local clicker = Instance.new(S(234))
            clicker.Size = UDim2.new(1, 0, 1, 0)
            clicker.BackgroundTransparency = 1
            clicker.Text = S(153)
            clicker.Parent = track
            clicker.MouseButton1Click:Connect(function()
                state = not state
                track.BackgroundColor3 = state and THEME.Accent or THEME.ToggleOff
                knob.Position = state and UDim2.new(1, -20, 0, 2) or UDim2.new(0, 2, 0, 2)
                callback(state)
            end)
            return track
        end
        return cardApi
    end
    function tab:AddSlider(title, subtitle, min, max, default, callback)
        local card = self:AddCard(title, subtitle)
        local valueLbl = card:SetValue(tostring(default) .. S(352))
        local track = Instance.new(S(246))
        track.Size = UDim2.new(1, 0, 0, 4)
        track.Position = UDim2.new(0, 0, 0, 45)
        track.BackgroundColor3 = THEME.ToggleOff
        track.Parent = card.Frame
        corner(track, 2)
        local fill = Instance.new(S(147))
        fill.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
        fill.BackgroundColor3 = THEME.Accent
        fill.Parent = track
        corner(fill, 2)
        local knob = Instance.new(S(166))
        knob.Size = UDim2.new(0, 14, 0, 14)
        knob.Position = UDim2.new(fill.Size.X.Scale, -7, 0.5, -7)
        knob.BackgroundColor3 = THEME.Text
        knob.Text = S(245)
        knob.Parent = track
        pill(knob)
        local dragging = false
        knob.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = false
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local relative = math.clamp((input.Position.X - track.AbsolutePosition.X) / track.AbsoluteSize.X, 0, 1)
                fill.Size = UDim2.new(relative, 0, 1, 0)
                knob.Position = UDim2.new(relative, -7, 0.5, -7)
                local value = math.floor(min + (max - min) * relative)
                valueLbl.Text = tostring(value) .. S(214)
                callback(value)
            end
        end)
        return card
    end
    function tab:AddSegmented(options, defaultIndex, callback)
        local card = Instance.new(S(213))
        card.Size = UDim2.new(1, 0, 0, 36)
        card.BackgroundColor3 = THEME.Card
        card.Parent = page
        corner(card, RADIUS.Control)
        local layout = Instance.new(S(428))
        layout.FillDirection = Enum.FillDirection.Horizontal
        layout.Padding = UDim.new(0, 4)
        layout.Parent = card
        padding(card, 4)
        local buttons = {}
        local function setActive(index)
            for i, btn in ipairs(buttons) do
                btn.BackgroundColor3 = i == index and THEME.Accent or THEME.Card
                btn.TextColor3 = i == index and THEME.Background or THEME.SubText
            end
        end
        for i, optionText in ipairs(options) do
            local btn = Instance.new(S(310))
            btn.Size = UDim2.new(1 / #options, -4, 1, 0)
            btn.BackgroundColor3 = THEME.Card
            btn.AutoButtonColor = false
            btn.Text = optionText
            btn.Font = THEME.Font
            btn.TextSize = 13
            btn.TextColor3 = THEME.SubText
            btn.Parent = card
            corner(btn, RADIUS.Small)
            btn.MouseButton1Click:Connect(function()
                setActive(i)
                callback(i, optionText)
            end)
            buttons[i] = btn
        end
        setActive(defaultIndex or 1)
        return card
    end
    self.Tabs[name] = tab
    return tab
end
function UI:SetSearchCallback(callback)
    self.SearchBox:GetPropertyChangedSignal(S(22)):Connect(function()
        callback(self.SearchBox.Text)
    end)
end
local function createWindow(config)
    config = config or {}
    local env = getgenv and getgenv() or _G
    if env.MekProWindow then
        pcall(function()
            env.MekProWindow:Destroy()
        end)
    end
    local screenGui = Instance.new(S(319))
    screenGui.Name = S(233)
    screenGui.ResetOnSpawn = false
    screenGui.Parent = game:GetService(S(392))
    local main = Instance.new(S(57))
    main.Name = S(422)
    main.Size = UDim2.new(0, 430, 0, 620)
    main.Position = UDim2.new(0.5, -215, 0.5, -310)
    main.BackgroundColor3 = THEME.Background
    main.Active = true
    main.Parent = screenGui
    corner(main, RADIUS.Card)
    local expandedSize = main.Size
    local collapsedSize = UDim2.new(0, 64, 0, 64)
    local uiScale = Instance.new(S(208))
    uiScale.Scale = 1
    uiScale.Parent = main
    local tabBar = Instance.new(S(69))
    tabBar.Name = S(362)
    tabBar.Size = UDim2.new(1, -24, 0, 44)
    tabBar.Position = UDim2.new(0, 12, 0, 12)
    tabBar.BackgroundTransparency = 1
    tabBar.Parent = main
    local tabBarLayout = Instance.new(S(413))
    tabBarLayout.FillDirection = Enum.FillDirection.Horizontal
    tabBarLayout.Padding = UDim.new(0, 6)
    tabBarLayout.Parent = tabBar
    local header = Instance.new(S(261))
    header.Name = S(202)
    header.Size = UDim2.new(1, -24, 0, 48)
    header.Position = UDim2.new(0, 12, 0, 64)
    header.BackgroundTransparency = 1
    header.Parent = main
    local avatarExpandedPos = UDim2.new(0, 12, 0, 64)
    local avatarCollapsedPos = UDim2.new(0, 12, 0, 12)
    local avatar = Instance.new(S(222))
    avatar.Size = UDim2.new(0, 40, 0, 40)
    avatar.Position = avatarExpandedPos
    avatar.BackgroundColor3 = THEME.Accent
    avatar.AutoButtonColor = false
    avatar.Text = config.Avatar or THEME.BrandName:sub(1, 1)
    avatar.Font = THEME.Font
    avatar.TextSize = 18
    avatar.TextColor3 = THEME.Background
    avatar.Parent = main
    corner(avatar, RADIUS.Control)
    local titleLbl = Instance.new(S(91))
    titleLbl.Size = UDim2.new(1, -160, 0, 20)
    titleLbl.Position = UDim2.new(0, 50, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = config.Title or THEME.BrandName
    titleLbl.Font = THEME.Font
    titleLbl.TextSize = 16
    titleLbl.TextColor3 = THEME.Text
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left
    titleLbl.Parent = header
    local subtitleLbl = Instance.new(S(383))
    subtitleLbl.Size = UDim2.new(1, -160, 0, 16)
    subtitleLbl.Position = UDim2.new(0, 50, 0, 20)
    subtitleLbl.BackgroundTransparency = 1
    subtitleLbl.Text = config.Subtitle or S(414)
    subtitleLbl.Font = Enum.Font.Gotham
    subtitleLbl.TextSize = 12
    subtitleLbl.TextColor3 = THEME.SubText
    subtitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    subtitleLbl.Parent = header
    local closeBtn = Instance.new(S(251))
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -28, 0, 6)
    closeBtn.BackgroundColor3 = THEME.Card
    closeBtn.Text = S(155)
    closeBtn.Font = THEME.Font
    closeBtn.TextSize = 14
    closeBtn.TextColor3 = THEME.Text
    closeBtn.Parent = header
    corner(closeBtn, RADIUS.Small)
    local minimizeBtn = Instance.new(S(42))
    minimizeBtn.Size = UDim2.new(0, 28, 0, 28)
    minimizeBtn.Position = UDim2.new(1, -62, 0, 6)
    minimizeBtn.BackgroundColor3 = THEME.Card
    minimizeBtn.Text = S(146)
    minimizeBtn.Font = THEME.Font
    minimizeBtn.TextSize = 16
    minimizeBtn.TextColor3 = THEME.Text
    minimizeBtn.Parent = header
    corner(minimizeBtn, RADIUS.Small)
    local searchBox = Instance.new(S(17))
    searchBox.Name = S(218)
    searchBox.Size = UDim2.new(1, -24, 0, 36)
    searchBox.Position = UDim2.new(0, 12, 0, 118)
    searchBox.BackgroundColor3 = THEME.Card
    searchBox.PlaceholderText = i18n.T(S(124))
    searchBox.Text = S(307)
    searchBox.Font = Enum.Font.Gotham
    searchBox.TextSize = 13
    searchBox.TextColor3 = THEME.Text
    searchBox.PlaceholderColor3 = THEME.SubText
    searchBox.ClearTextOnFocus = false
    searchBox.Parent = main
    corner(searchBox, RADIUS.Control)
    padding(searchBox, 10)
    local content = Instance.new(S(118))
    content.Name = S(395)
    content.Size = UDim2.new(1, -24, 1, -220)
    content.Position = UDim2.new(0, 12, 0, 164)
    content.BackgroundTransparency = 1
    content.Parent = main
    local footer = Instance.new(S(277))
    footer.Size = UDim2.new(1, -24, 0, 30)
    footer.Position = UDim2.new(0, 12, 1, -42)
    footer.BackgroundTransparency = 1
    footer.Parent = main
    local footerLabel = Instance.new(S(63))
    footerLabel.Size = UDim2.new(1, -80, 1, 0)
    footerLabel.BackgroundTransparency = 1
    footerLabel.Text = i18n.T(S(40))
    footerLabel.Font = Enum.Font.Gotham
    footerLabel.TextSize = 12
    footerLabel.TextColor3 = THEME.SubText
    footerLabel.TextXAlignment = Enum.TextXAlignment.Left
    footerLabel.Parent = footer
    local keybind = config.Keybind or Enum.KeyCode.RightShift
    local keybindLabel = Instance.new(S(170))
    keybindLabel.Size = UDim2.new(0, 80, 1, 0)
    keybindLabel.Position = UDim2.new(1, -80, 0, 0)
    keybindLabel.BackgroundColor3 = THEME.Card
    keybindLabel.Text = keybind.Name
    keybindLabel.Font = THEME.Font
    keybindLabel.TextSize = 12
    keybindLabel.TextColor3 = THEME.Text
    keybindLabel.Parent = footer
    corner(keybindLabel, RADIUS.Small)
    local minimized = false
    local function setMinimized(state)
        minimized = state
        tabBar.Visible = not minimized
        header.Visible = not minimized
        searchBox.Visible = not minimized
        content.Visible = not minimized
        footer.Visible = not minimized
        main.Size = minimized and collapsedSize or expandedSize
        avatar.Position = minimized and avatarCollapsedPos or avatarExpandedPos
    end
    local connections = makeDraggable(main, main)
    for _, connection in ipairs(makeDraggable(avatar, main, function()
        if minimized then
            setMinimized(false)
        end
    end)) do
        table.insert(connections, connection)
    end
    local self = setmetatable({}, UI)
    self.ScreenGui = screenGui
    self.Main = main
    self.TabBar = tabBar
    self.Content = content
    self.SearchBox = searchBox
    self.Tabs = {}
    self.Connections = connections
    self.UIScale = uiScale
    self.SearchIndex = {}
    self.DestroyListeners = {}
    table.insert(connections, minimizeBtn.MouseButton1Click:Connect(function()
        setMinimized(not minimized)
    end))
    table.insert(connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end
        if input.KeyCode == keybind then
            screenGui.Enabled = not screenGui.Enabled
        end
    end))
    table.insert(connections, closeBtn.MouseButton1Click:Connect(function()
        self:Destroy()
    end))
    table.insert(connections, searchBox:GetPropertyChangedSignal(S(114)):Connect(function()
        local query = searchBox.Text:lower()
        for _, item in ipairs(self.SearchIndex) do
            if query == S(374) then
                item.Frame.Visible = true
            else
                local haystack = (item.Title .. S(359) .. (item.Subtitle or S(313))):lower()
                item.Frame.Visible = haystack:find(query, 1, true) ~= nil
            end
        end
    end))
    local unsubscribeLanguage = i18n.OnChanged(function()
        searchBox.PlaceholderText = i18n.T(S(176))
        footerLabel.Text = i18n.T(S(185))
    end)
    self.UnsubscribeLanguage = unsubscribeLanguage
    env.MekProWindow = self
    return self
end
function UI:OnDestroy(callback)
    table.insert(self.DestroyListeners, callback)
end
function UI:Destroy()
    for _, connection in ipairs(self.Connections) do
        connection:Disconnect()
    end
    for _, listener in ipairs(self.DestroyListeners) do
        pcall(listener)
    end
    self.UnsubscribeLanguage()
    self.ScreenGui:Destroy()
end
function UI:SetScale(percent)
    self.UIScale.Scale = percent / 100
end
return createWindow
end
end
modules[S(288)] = function()
return function(import)
local Players = game:GetService(S(398))
local RunService = game:GetService(S(110))
local getCurrentPlot = import(S(154))
local getPlotParts = import(S(210))
local log = import(S(252))
local FIRE_INTERVAL = 0.5
local RECHECK_COOLDOWN = 5
local MIN_ITEMS_TO_COLLECT = 100
local HIDDEN_POSITION = UDim2.new(3, 0, 3, 0)
local function getChestFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(367))
    local mineGui = playerGui and playerGui:FindFirstChild(S(360))
    return mineGui and mineGui:FindFirstChild(S(168))
end
local function getTakeButton(chestFrame)
    local content = chestFrame and chestFrame:FindFirstChild(S(405))
    local actions = content and content:FindFirstChild(S(248))
    return actions and actions:FindFirstChild(S(45))
end
local function getItemCount(chestFrame)
    local content = chestFrame:FindFirstChild(S(165))
    local progress = content and content:FindFirstChild(S(338))
    local label = progress and progress:FindFirstChild(S(126))
    local text = label and label.Text or S(232)
    local digits = text:gsub(S(418), S(1)):match(S(186))
    return tonumber(digits)
end
local function clickButton(button)
    for _, signalName in ipairs({ S(47), S(138), S(37), S(365) }) do
        local signal = button[signalName]
        if signal then
            pcall(function()
                firesignal(signal)
            end)
        end
    end
end
local autoChest = {}
local enabled = false
local lastFire = 0
local lastVisibleState = nil
local nextOpenCheck = 0
local hideConnection = nil
local function restoreFramePosition()
    local chestFrame = getChestFrame()
    if chestFrame then
        chestFrame.Position = chestFrame:GetAttribute(S(324)) or chestFrame.Position
    end
end
function autoChest.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if hideConnection then
            hideConnection:Disconnect()
            hideConnection = nil
        end
        restoreFramePosition()
        log(S(231))
    else
        nextOpenCheck = 0
        hideConnection = RunService.Heartbeat:Connect(function()
            local chestFrame = getChestFrame()
            if chestFrame and chestFrame.Visible then
                if chestFrame:GetAttribute(S(386)) == nil then
                    chestFrame:SetAttribute(S(297), chestFrame.Position)
                end
                chestFrame.Position = HIDDEN_POSITION
            end
        end)
        log(S(29) .. MIN_ITEMS_TO_COLLECT .. S(403))
    end
end
function autoChest.IsEnabled()
    return enabled
end
function autoChest.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local chestFrame = getChestFrame()
    local visible = chestFrame ~= nil and chestFrame.Visible
    if visible ~= lastVisibleState then
        log(S(281) .. tostring(visible))
        lastVisibleState = visible
    end
    if visible then
        local count = getItemCount(chestFrame)
        if count and count < MIN_ITEMS_TO_COLLECT then
            log(S(207) .. count .. S(106) .. MIN_ITEMS_TO_COLLECT)
            local closeButton = chestFrame:FindFirstChild(S(156))
            if closeButton then
                clickButton(closeButton)
            end
            nextOpenCheck = now + RECHECK_COOLDOWN
            return
        end
        local takeButton = getTakeButton(chestFrame)
        if takeButton then
            clickButton(takeButton)
            log(S(419) .. tostring(count) .. S(141))
        end
    elseif now >= nextOpenCheck then
        local plot = getCurrentPlot()
        local parts = plot and getPlotParts(plot)
        local prompt = parts and parts.Chest and parts.Chest:FindFirstChildWhichIsA(S(94), true)
        if prompt then
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if not ok then
                log(S(305) .. tostring(err))
            end
        end
    end
end
return autoChest
end
end
modules[S(171)] = function()
return function(import)
local getCurrentPlot = import(S(178))
local getPlotParts = import(S(44))
local log = import(S(272))
local FIRE_INTERVAL = 0.2
local function findClickDetector(instance)
    return instance:FindFirstChildWhichIsA(S(62), true)
end
local autoMine = {}
local enabled = false
local lastFire = 0
function autoMine.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(408) .. (enabled and S(177) or S(9)))
end
function autoMine.IsEnabled()
    return enabled
end
function autoMine.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Nodes then
        return
    end
    for _, node in ipairs(parts.Nodes:GetChildren()) do
        local detector = findClickDetector(node)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoMine
end
end
modules[S(254)] = function()
return function(import)
local Players = game:GetService(S(293))
local getCurrentPlot = import(S(103))
local getPlotParts = import(S(30))
local log = import(S(391))
local FIRE_INTERVAL = 1
local function findHolderPrompt(holder)
    local pad = holder:FindFirstChild(S(364))
    return pad and pad:FindFirstChildWhichIsA(S(406), true)
end
local function isHolderEmpty(prompt)
    return prompt ~= nil and prompt.ObjectText == S(212)
end
local autoPlace = {}
local enabled = false
local lastFire = 0
local lastReason = nil
local function reason(text)
    if text ~= lastReason then
        log(S(344) .. text)
        lastReason = text
    end
end
function autoPlace.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    lastReason = nil
    log(S(328) .. (enabled and S(203) or S(183)))
end
function autoPlace.IsEnabled()
    return enabled
end
function autoPlace.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local player = Players.LocalPlayer
    local character = player.Character
    local backpack = player:FindFirstChild(S(229))
    if not character or not backpack then
        reason(S(129))
        return
    end
    local humanoid = character:FindFirstChildOfClass(S(78))
    local tool = backpack:FindFirstChildWhichIsA(S(15))
    if not humanoid then
        reason(S(423))
        return
    end
    if not tool then
        reason(S(198))
        return
    end
    local plot = getCurrentPlot()
    if not plot then
        reason(S(199))
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        reason(S(220))
        return
    end
    local placed = false
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local prompt = findHolderPrompt(holder)
        if isHolderEmpty(prompt) then
            humanoid:EquipTool(tool)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if ok then
                log(S(36) .. tool.Name .. S(151) .. holder.Name)
            else
                log(S(157) .. tostring(err))
            end
            placed = true
            break
        end
    end
    if not placed then
        reason(S(332) .. #parts.Holders:GetChildren() .. S(201))
    else
        lastReason = nil
    end
end
return autoPlace
end
end
modules[S(441)] = function()
return function(import)
local Players = game:GetService(S(339))
local Workspace = game:GetService(S(127))
local RunService = game:GetService(S(65))
local teleport = import(S(354))(import)
local log = import(S(402))
local FIRE_INTERVAL = 0.5
local RECHECK_COOLDOWN = 5
local HIDDEN_POSITION = UDim2.new(3, 0, 3, 0)
local function getSellFrame()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(399))
    local mineGui = playerGui and playerGui:FindFirstChild(S(322))
    return mineGui and mineGui:FindFirstChild(S(373))
end
local function labelText(button)
    local content = button:FindFirstChild(S(130))
    local label = content and content:FindFirstChild(S(397))
    return label and label.Text or S(51)
end
local function countCards(sellFrame)
    local content = sellFrame:FindFirstChild(S(128))
    local scroll = content and content:FindFirstChild(S(67))
    if not scroll then
        return 0
    end
    local count = 0
    for _, child in ipairs(scroll:GetChildren()) do
        if child.Name == S(334) then
            count = count + 1
        end
    end
    return count
end
local function clickButton(button)
    for _, signalName in ipairs({ S(243), S(421), S(107), S(59) }) do
        local signal = button[signalName]
        if signal then
            pcall(function()
                firesignal(signal)
            end)
        end
    end
end
local function getSellStandPromptPart()
    local sellStand = Workspace:FindFirstChild(S(390))
    local prompt = sellStand and sellStand:FindFirstChildWhichIsA(S(179), true)
    return prompt, prompt and prompt.Parent
end
local autoSell = {}
local enabled = false
local lastFire = 0
local lastVisibleState = nil
local nextOpenCheck = 0
local hideConnection = nil
local function restoreFramePosition()
    local sellFrame = getSellFrame()
    if sellFrame then
        sellFrame.Position = sellFrame:GetAttribute(S(100)) or sellFrame.Position
    end
end
function autoSell.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    if not enabled then
        if hideConnection then
            hideConnection:Disconnect()
            hideConnection = nil
        end
        restoreFramePosition()
        teleport.Return()
        log(S(263))
    else
        nextOpenCheck = 0
        hideConnection = RunService.Heartbeat:Connect(function()
            local sellFrame = getSellFrame()
            if sellFrame and sellFrame.Visible then
                if sellFrame:GetAttribute(S(120)) == nil then
                    sellFrame:SetAttribute(S(275), sellFrame.Position)
                end
                sellFrame.Position = HIDDEN_POSITION
            end
        end)
        log(S(216))
    end
end
function autoSell.IsEnabled()
    return enabled
end
function autoSell.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local sellFrame = getSellFrame()
    local visible = sellFrame ~= nil and sellFrame.Visible
    if visible ~= lastVisibleState then
        log(S(400) .. tostring(visible))
        lastVisibleState = visible
        if not visible then
            teleport.Return()
        end
    end
    if visible then
        local cardCount = countCards(sellFrame)
        if cardCount == 0 then
            log(S(187))
            local closeButton = sellFrame:FindFirstChild(S(411))
            if closeButton then
                clickButton(closeButton)
            end
            nextOpenCheck = now + RECHECK_COOLDOWN
            return
        end
        local content = sellFrame:FindFirstChild(S(92))
        local selectAllButton = content and content:FindFirstChild(S(236))
        local sellButton = content and content:FindFirstChild(S(346))
        if selectAllButton and labelText(selectAllButton) == S(361) then
            clickButton(selectAllButton)
            log(S(89) .. cardCount .. S(96))
        elseif sellButton and labelText(sellButton):find(S(24), 1, true) then
            clickButton(sellButton)
            log(S(437))
        end
    elseif now >= nextOpenCheck then
        local prompt, promptPart = getSellStandPromptPart()
        if prompt and promptPart then
            teleport.To(promptPart)
            local ok, err = pcall(function()
                fireproximityprompt(prompt, Enum.ProximityPromptInputType.Keyboard, prompt.HoldDuration)
            end)
            if not ok then
                log(S(55) .. tostring(err))
            end
        end
    end
end
return autoSell
end
end
modules[S(71)] = function()
return function(import)
local getCurrentPlot = import(S(348))
local getPlotParts = import(S(32))
local log = import(S(41))
local FIRE_INTERVAL = 1
local function findUpgradeClickDetector(holder)
    local upgradePart = holder:FindFirstChild(S(368))
    return upgradePart and upgradePart:FindFirstChildWhichIsA(S(417))
end
local autoUpgrade = {}
local enabled = false
local lastFire = 0
function autoUpgrade.SetEnabled(state)
    if state == enabled then
        return
    end
    enabled = state
    log(S(97) .. (enabled and S(303) or S(217)))
end
function autoUpgrade.IsEnabled()
    return enabled
end
function autoUpgrade.Step()
    if not enabled then
        return
    end
    local now = tick()
    if now - lastFire < FIRE_INTERVAL then
        return
    end
    lastFire = now
    local plot = getCurrentPlot()
    if not plot then
        return
    end
    local parts = getPlotParts(plot)
    if not parts.Holders then
        return
    end
    for _, holder in ipairs(parts.Holders:GetChildren()) do
        local detector = findUpgradeClickDetector(holder)
        if detector then
            fireclickdetector(detector)
        end
    end
end
return autoUpgrade
end
end
modules[S(3)] = function()
local function dumpInstance(instance, log, indent)
    indent = indent or S(87)
    for _, child in ipairs(instance:GetChildren()) do
        local line = indent .. child.ClassName .. S(279) .. child.Name .. S(149)
        if child:IsA(S(228)) then
            line = line .. string.format(
                S(211),
                child.ActionText, child.ObjectText, tostring(child.HoldDuration)
            )
        elseif child:IsA(S(376)) then
            line = line .. string.format(S(18), tostring(child.MaxActivationDistance))
        elseif child:IsA(S(131)) or child:IsA(S(380)) then
            line = line .. S(266)
        end
        log(line)
        for attrName, attrValue in pairs(child:GetAttributes()) do
            log(indent .. S(439) .. attrName .. S(424) .. tostring(attrValue))
        end
        dumpInstance(child, log, indent .. S(253))
    end
end
return dumpInstance
end
modules[S(382)] = function()
return function(import)
local Players = game:GetService(S(88))
local log = import(S(255))
local function dumpVisible(root, indent)
    indent = indent or S(351)
    for _, child in ipairs(root:GetChildren()) do
        if child:IsA(S(74)) and child.Visible then
            local line = indent .. child.ClassName .. S(287) .. child.Name .. S(167)
            if child:IsA(S(23)) or child:IsA(S(132)) then
                line = line .. S(136) .. tostring(child.Text) .. S(314)
            end
            log(line)
            for attrName, attrValue in pairs(child:GetAttributes()) do
                log(indent .. S(415) .. attrName .. S(197) .. tostring(attrValue))
            end
            dumpVisible(child, indent .. S(267))
        end
    end
end
local function dumpMineGui()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(189))
    local mineGui = playerGui and playerGui:FindFirstChild(S(11))
    if not mineGui then
        log(S(242))
        return
    end
    log(S(320))
    dumpVisible(mineGui)
    log(S(341))
end
return dumpMineGui
end
end
modules[S(133)] = function()
return function(import)
local ReplicatedStorage = game:GetService(S(434))
local log = import(S(58))
local function dumpRemotes()
    local count = 0
    for _, descendant in ipairs(ReplicatedStorage:GetDescendants()) do
        if descendant:IsA(S(407)) or descendant:IsA(S(119)) then
            count = count + 1
            log(S(271) .. descendant.ClassName .. S(244) .. descendant:GetFullName())
        end
    end
    log(S(315) .. count .. S(445))
    return count
end
return dumpRemotes
end
end
modules[S(68)] = function()
return function(import)
local Players = game:GetService(S(81))
local log = import(S(429))
local function dumpSellCards()
    local playerGui = Players.LocalPlayer:FindFirstChild(S(12))
    local mineGui = playerGui and playerGui:FindFirstChild(S(194))
    local sellFrame = mineGui and mineGui:FindFirstChild(S(13))
    local scroll = sellFrame and sellFrame:FindFirstChild(S(184)) and sellFrame.Content:FindFirstChild(S(369))
    if not scroll then
        log(S(416))
        return 0
    end
    local count = 0
    for _, card in ipairs(scroll:GetChildren()) do
        if card.Name == S(226) or card:IsA(S(388)) then
            count = count + 1
            log(S(84) .. count .. S(134) .. card.ClassName .. S(371) .. card.Name .. S(215))
            for attrName, attrValue in pairs(card:GetAttributes()) do
                log(S(385) .. attrName .. S(21) .. tostring(attrValue))
            end
            for _, child in ipairs(card:GetDescendants()) do
                local line = S(49) .. child.ClassName .. S(335) .. child.Name .. S(122)
                if child:IsA(S(298)) then
                    line = line .. S(425) .. tostring(child.Value)
                end
                log(line)
            end
        end
    end
    log(S(73) .. count .. S(86))
    return count
end
return dumpSellCards
end
end
modules[S(230)] = function()
local Players = game:GetService(S(306))
local Workspace = game:GetService(S(299))
local function getCurrentPlot()
    local player = Players.LocalPlayer
    local mineWorld = Workspace:FindFirstChild(S(300))
    local plots = mineWorld and mineWorld:FindFirstChild(S(72))
    if not plots then
        return nil
    end
    for _, plot in ipairs(plots:GetChildren()) do
        if plot:GetAttribute(S(438)) == player.UserId then
            return plot
        end
    end
    return nil
end
return getCurrentPlot
end
modules[S(394)] = function()
local function getPlotParts(plot)
    return {
        Holders = plot:FindFirstChild(S(25)),
        Nodes = plot:FindFirstChild(S(260)),
        Chest = plot:FindFirstChild(S(104)),
        Zones = plot:FindFirstChild(S(173)),
        Crew = plot:FindFirstChild(S(265)),
        Loot = plot:FindFirstChild(S(54)),
        PlotIndex = plot:GetAttribute(S(143)),
        ChestCapacity = plot:GetAttribute(S(440)),
    }
end
return getPlotParts
end
modules[S(268)] = function()
return function(import)
    local Players = game:GetService(S(28))
    local createWindow = import(S(99))(import)
    local i18n = import(S(200))
    local antiAfk = import(S(61))(import)
    local interactionLogger = import(S(342))(import)
    local scheduler = import(S(427))(import)
    local log = import(S(70))
    local window = createWindow({
        Subtitle = S(435),
    })
    local getCurrentPlot = import(S(282))
    local getPlotParts = import(S(191))
    local dumpInstance = import(S(192))
    local dumpRemotes = import(S(66))(import)
    local dumpSellCards = import(S(139))(import)
    local dumpGui = import(S(284))(import)
    local autoMine = import(S(75))(import)
    local autoPlace = import(S(193))(import)
    local autoChest = import(S(8))(import)
    local autoSell = import(S(249))(import)
    local autoUpgrade = import(S(241))(import)
    scheduler.Register(S(358), autoMine.Step, autoMine.IsEnabled)
    scheduler.Register(S(190), autoPlace.Step, autoPlace.IsEnabled)
    scheduler.Register(S(150), autoChest.Step, autoChest.IsEnabled)
    scheduler.Register(S(442), autoSell.Step, autoSell.IsEnabled)
    scheduler.Register(S(33), autoUpgrade.Step, autoUpgrade.IsEnabled)
    scheduler.Start()
    local farmTab = window:AddTab(S(270), S(26))
    farmTab:AddCard(
        S(433),
        S(237)
    ):AddToggle(false, function(state)
        autoMine.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(137),
        S(238)
    ):AddToggle(false, function(state)
        autoPlace.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(60),
        S(121)
    ):AddToggle(false, function(state)
        autoChest.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(93),
        S(349)
    ):AddToggle(false, function(state)
        autoSell.SetEnabled(state)
    end)
    farmTab:AddCard(
        S(357),
        S(273)
    ):AddToggle(false, function(state)
        autoUpgrade.SetEnabled(state)
    end)
    local debugCard = farmTab:AddCard(S(206), S(377))
    local checkBtn
    checkBtn = debugCard:AddButton(S(337), function()
        local plot = getCurrentPlot()
        if not plot then
            checkBtn.Text = S(188)
            log(S(144))
            return
        end
        local parts = getPlotParts(plot)
        local nodeCount = parts.Nodes and #parts.Nodes:GetChildren() or 0
        local holderCount = parts.Holders and #parts.Holders:GetChildren() or 0
        checkBtn.Text = plot.Name
        log(string.format(
            S(409),
            plot.Name, nodeCount, holderCount
        ))
    end)
    local dumpCard = farmTab:AddCard(
        S(140),
        S(4)
    )
    local dumpBtn
    dumpBtn = dumpCard:AddButton(S(10), function()
        local plot = getCurrentPlot()
        if not plot then
            dumpBtn.Text = S(446)
            return
        end
        local ok, err = log(S(135))
        if not ok then
            dumpBtn.Text = S(294)
            dumpCard:SetSubtitle(S(148) .. tostring(err))
            return
        end
        local parts = getPlotParts(plot)
        local firstNode = parts.Nodes and parts.Nodes:GetChildren()[1]
        if firstNode then
            log(S(39) .. firstNode.Name .. S(125))
            dumpInstance(firstNode, log)
        else
            log(S(53))
        end
        local firstHolder = parts.Holders and parts.Holders:GetChildren()[1]
        if firstHolder then
            log(S(116) .. firstHolder.Name .. S(430))
            dumpInstance(firstHolder, log)
        else
            log(S(5))
        end
        dumpBtn.Text = S(316)
    end)
    local toolDumpCard = farmTab:AddCard(
        S(205),
        S(80)
    )
    local toolDumpBtn
    toolDumpBtn = toolDumpCard:AddButton(S(109), function()
        local backpack = Players.LocalPlayer:FindFirstChild(S(256))
        local tool = backpack and backpack:FindFirstChildWhichIsA(S(291))
        if not tool then
            toolDumpBtn.Text = S(370)
            return
        end
        log(S(196) .. tool.Name .. S(182))
        for attrName, attrValue in pairs(tool:GetAttributes()) do
            log(S(19) .. attrName .. S(2) .. tostring(attrValue))
        end
        dumpInstance(tool, log)
        toolDumpBtn.Text = S(274)
    end)
    local remoteDumpCard = farmTab:AddCard(
        S(292),
        S(160)
    )
    local remoteDumpBtn
    remoteDumpBtn = remoteDumpCard:AddButton(S(420), function()
        local count = dumpRemotes()
        remoteDumpBtn.Text = count .. S(50)
    end)
    local sellCardsDumpCard = farmTab:AddCard(
        S(174),
        S(180)
    )
    local sellCardsDumpBtn
    sellCardsDumpBtn = sellCardsDumpCard:AddButton(S(304), function()
        local count = dumpSellCards()
        sellCardsDumpBtn.Text = count .. S(404)
    end)
    local guiDumpCard = farmTab:AddCard(
        S(239),
        S(7)
    )
    local guiDumpBtn
    guiDumpBtn = guiDumpCard:AddButton(S(311), function()
        dumpGui()
        guiDumpBtn.Text = S(46)
    end)
    local settingsTab = window:AddTab(S(224), S(309))
    settingsTab:AddCard(S(158), S(375)):AddToggle(false, function(state)
        antiAfk.SetEnabled(state)
    end)
    settingsTab:AddCard(
        S(83),
        S(31)
    ):AddToggle(false, function(state)
        interactionLogger.SetEnabled(state)
    end)
    settingsTab:AddSlider(S(247), S(20), 50, 150, 100, function(value)
        window:SetScale(value)
    end)
    settingsTab:AddSegmented({ S(283), S(250), S(331) }, 1, function(index, value)
        local code = ({ S(318), S(436), S(329) })[index]
        i18n.SetLanguage(code)
    end)
    window:OnDestroy(function()
        scheduler.Stop()
        autoMine.SetEnabled(false)
        autoPlace.SetEnabled(false)
        autoChest.SetEnabled(false)
        antiAfk.SetEnabled(false)
        interactionLogger.SetEnabled(false)
        autoSell.SetEnabled(false)
        autoUpgrade.SetEnabled(false)
    end)
end
end
modules[S(325)] = function()
return {
    [79389059854988] = S(6),
}
end
import(S(14))(import)
